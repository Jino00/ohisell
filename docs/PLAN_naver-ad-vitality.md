# PLAN — 스프린트 VT: 흐름 유지 축(B축) — 스파이럴 조기 경보 + 즉시 복원 (D-NAO-81)

> 설계=Fable(2026-07-22) · 구현 VT1~VT2=Opus · GATE 적대 리뷰 + codex 왕복 필수 · 배포=safe_deploy.
> 근거: D-NAO-81(Jino 확정 — 트랙 파일 원문 인용). 실증 표본 = 03 스파이럴(순위 3.9→5.6 나흘 미끄럼·노출 −62%·하락 중 무개입, 07-14~21 실측).

## §0 방향 고정 (변경 금지)

1. **2축 분리(D-NAO-81)**: A축(자르기, ROAS 판정)=기존 유지·무변경. B축(살리기, 흐름 유지)=이 스프린트. 서로의 판정을 덮어쓰지 않는다.
2. **충돌 방지 규칙(핵심)**: A축 확정 손실 개체는 B축 소생 **금지** — ①userLock=True(정지) 개체 ②스톱로스/터미널정지 이력 개체(change_log `set_user_lock` + `[shopping_pause_candidates]`/`[터미널정지]` 사유) ③무전환 확정(충분 표본에서 전환 0으로 A축이 자른 것). 소생 대상 = **과거 30일 전환 이력 ≥1 또는 판정 표본 미달**(클릭이 판정 최소치 미만) 개체만.
3. **새 권한 없음**: 복원 발사는 기존 개방 레버(그룹/키워드 입찰 UP)와 기존 봉투(BEP 천장·가드레일·킬스위치·쿨다운·캡·change_log) 그대로. 이 스프린트가 바꾸는 것은 **발동 시점(조기)과 발동 근거(궤적)**뿐.
4. **관찰→개입 최소화**: 경보는 브리핑에 전건 표면화. 개입은 봉투 안에서만.

## §1 신호 설계 (VT1 — vitality_signal SA, 순수함수·read-only)

**감지 grain = 캠페인(안정 신호) → 소생 대상 선정 grain = adgroup**(레버가 사는 곳).

| # | 신호 | 정의(초기값 = 가설, 첫 주 캘리브레이션) |
|---|---|---|
| S1 | 노출 하락 궤적 | 캠페인 일별 노출(naver_hourly_snapshot 일 최종 or naver_ad_daily) **3일 연속 하락 ∧ 누적 −40%↑**(3일 전 대비) |
| S2 | 순위 이탈 궤적 | 캠페인 avg_rank(스냅샷 일 최종) 3일 연속 악화 ∧ 목표 밴드 상단(4.0) 밖 |
| S3 | (보조) 품질 위험 | 대상 그룹 키워드의 qi_grade(naver_entity 현재값) ≤3 비중 — 경보 가중치·브리핑 표기용(이력 없어 궤적 불가, 단독 트리거 아님) |

- **경보 = S1 ∧ S2** (S3는 부가 정보). ours(auto_operate=1) 캠페인만.
- 소생 대상 그룹 선정: 경보 캠페인 내에서 §0 2 충돌 방지 통과 ∧ 최근 7일 노출 하락 그룹, 우선순위 = 과거 30일 전환매출 내림차순(살릴 가치 순).
- 산출: `{alerts:[{campaign_id, 신호 수치}], revive_targets:[{adgroup_id, 근거}]}`.

## §2 반응 배선 (VT2 — 시간당 레인 편입)

- **어디에**: `auto_operator.run_hourly_lane`(매시 :20)에 vitality 스텝 추가 — 반응 지연 ≤1h(현행 뒷북=일 단위였던 것을 시간 단위로). 일 레인이 아니라 시간당 레인인 이유 = 조기 개입이 목적.
- **무엇을**: revive_targets 그룹 입찰을 **밴드 복원 방향 UP** — 기존 시간당 밴드 레인의 UP 경로(APPROVAL_SOURCE_HOURLY·rationale 접두 `[스파이럴복원]`) 재사용. 상향 폭·상한은 기존 밴드 레인 규칙 그대로(BEP 가드레일이 최종 차단자 — 이미 라이브로 작동 실증됨).
- **봉투(추가분)**: 스파이럴 복원 발사 캠페인당 그룹 ≤5/일 · 같은 그룹 48h 쿨다운(재발사 금지) · 경보 없는 시간엔 무동작. 기존 킬스위치·시간당 레인 캡 전부 상속.
- **브리핑**: 경보·발사가 있던 날만 diary(observe)+Slack — "스파이럴 경보 N캠페인·복원 발사 M그룹"(PX 브리핑 관례 미러). 없는 날 침묵.

## §3 페이즈

| 페이즈 | 내용 | 모델 |
|---|---|---|
| VT1 | vitality_signal SA(§1)+충돌 방지 게이트+테스트 | Opus |
| VT2 | 시간당 레인 배선(§2)+봉투+브리핑+테스트 | Opus |
| GATE | 적대 리뷰 → 전체 pytest 회귀 0 → codex 왕복 → safe_deploy → 라이브 검증 | — |

**GATE 최우선**: ①A축 정지 개체 소생 0(충돌 방지 관통 테스트) ②BEP 가드레일 상속 확인(복원 UP이 가드레일 우회 경로 없음) ③경보 없는 날 무동작·무브리핑 ④캡·쿨다운 ⑤ours 스코프 밖 0 ⑥기존 시간당 레인 회귀 0.

## §검증 (원칙22)

1. 03 실데이터 백테스트: 07-14~18 데이터에 신호를 돌려 **그때 경보가 떴을지** 확인(설계 타당성 — 07-16 시점 경보 기대).
2. 배포 후 첫 경보/무경보 라이브 실측(현재 03이 회복 중이면 무경보=정당, 회복 실패 시 경보+복원 발사 관측).
3. 복원 발사 시 change_log·BEP 가드레일 통과/차단 실기록.

## §체크리스트

- [x] VT1 vitality_signal SA (Opus) — `ea8bdc8`(데이터 소스=naver_ad_daily 채택 근거 주석)
- [x] VT2 시간당 레인 배선+브리핑 (Opus) — `c47c61a`
- [x] 03 백테스트(§검증 1) — S1 단독 as_of=07-16, S1∧S2 결합 경보 as_of=07-18 발화(실제 대비 3~4일 조기) 테스트 고정
- [x] GATE 적대 + codex 왕복 + 배포 + 라이브 검증 — **GATE FAIL(P1: 충돌게이트 stale·죽은 토큰)→경화 `46e6ce8`(구조화 잠금 이벤트 권위) → codex 1R P1×4·P2×2 전건 수용 수정 `0d044d5`(브레이커 재사용·부모 체인·부분적재 보류·캡 재카운트·당일 순위 재확인·쇼핑 한정) → 2R AGREE-ALL.** 2921 passed·회귀 0. **배포 07-22 16:00 KST(2파일·부팅 200)**. **라이브 첫 실행 실측: 경보 1건=맥세이프쇼검(노출 −92%·순위 6.6 실스파이럴 정확 감지) ∧ 소생 발사 0(캠페인 잠금 → 충돌 방지 게이트 fail-closed 실증)** — 감지·차단 양쪽 라이브 합격. 03 무경보=회복 중 정당.
- [ ] 첫 주 상수 캘리브레이션(§1 초기값) + 첫 소생 발사 라이브 관측(잠금 아닌 ours 쇼핑 캠페인 스파이럴 발생 시)

## §4 확장 페이즈 (D-NAO-82 — 07-22 라이브 해부 발견 2건)

| 페이즈 | 내용 | 모델 |
|---|---|---|
| VT3 | **소재 CTR 경보**: 밴드 순위(≤4.0) ∧ 누적 노출≥200(rolling 3일) ∧ 클릭 0(또는 기대 클릭 대비 −80%↓) → 브리핑(diary+Slack) "입찰로 못 푸는 소재 CTR 문제 — 사람 처방 대상(썸네일·가격·리뷰)" + 해당 개체 래더 추가 UP 중지(밴드 도달+CTR 경보 = 더 올려도 헛돎). 근거 표본 = 04(07-22 밴드 내 330노출 클릭 0) | Sonnet |
| VT4 | **신수요 개척 우선순위**: 플로어 입찰(≤100원) ∧ 노출 수요 상위(캠페인 내 노출 비중 상위 or 절대 노출≥N) ∧ 판정 표본 미달 그룹 → 탐색 래더 우선 가중(래더 대상 정렬에서 수요 크기 반영). 근거 표본 = 03 아이폰17·17프로(노출 34%가 50원 방치) | Opus |

- 두 페이즈 모두 기존 봉투·가드레일 불변(새 권한 없음 — VT3는 브리핑+래더 중지뿐, VT4는 래더 "순서"만 변경 + 발견 결함 수정).
- 체크리스트: [x] VT3 (Sonnet) — `ctr_alert.py`(SA) + 일 레인 브리핑 배선 + 탐색 래더 CTR경보 skip 게이트, 테스트 18건 신규(SA 12·게이트 관통 3·일 레인 브리핑 3), 전체 회귀 2952 passed(회귀 0) / [x] VT4 (Opus) — 쇼핑 스텝 하한 50 `2cb050a`+수요 우선 정렬 `482f1ae`(최소 증분 승격은 눈먼 경로만 — 실관측 기울기 경로는 과열밴드 오버슈트 방지로 미적용) / [x] GATE+codex+배포 — GATE 재배치 1건(`039d768` CTR skip을 트리거 뒤로=blocked 소음 차단)+alembic head 분기 수정(`90e0efe`) → **codex challenge 1R P1×2·P2×4**(P1-1 실행층 70원 하한 3겹이 60원 발사 거부=guardrail_gate·writer·쿨다운 미기록 루프 / P1-2 ctr_alert fail-open→예외 전파로 / P2 부분적재 가드·브리핑 dedupe·rank_sum=0 오탐 — 전건 수용 `dd9478a` / N+1 지적은 기존 BX1 구조라 기각→codex PARTIAL AGREE·백로그) → **2R AGREE-ALL·신규 0**. 2981 passed·회귀 0. **배포 07-22 17:33 KST(7파일+alembic 단일 head `d22ce37d3adb`·부팅 200)**. **라이브 실측: ①ctr_alert 실판정 — 04·03 무경보(D-1 클릭 존재=정당)·부분적재 가드 맥세이프 실발동(37.5%→억제)·★뮤패드 7그룹 진짜 CTR 경보(밴드 내 rank 2.6~3.7·노출 최대 1,723·클릭 0 → 내일 08:50 첫 브리핑) ②VT4 경로 — adaptive_step(50,rank6.39)→60·게이트 SHOPPING 50→60 통과 실증**. 첫 실발사(03 아이폰17 50→60)는 18:20 시간당 레인 관측.

### §4.1 VT3 상세 설계 (Fable 확정 2026-07-22 — 착수 전 실측 반영)

**착수 전 실측(07-22 16:20 prod)**: 04(SHOPPING) 오늘 16시 누적 노출 850·클릭 0·캠페인 순위 4.4~4.6.
그룹별 **3일(07-19~21) 확정치는 그룹마다 클릭 1~3씩 존재** → 스펙 초안의 "rolling 3일 클릭 0"만으로는
오늘 가뭄을 내일 아침(as_of=D-1)에도 못 잡는다. → **최근 1일 창(W1)을 병행**한다.

1. **새 SA `ctr_alert.py`** (순수·read-only, vitality_signal 관례 미러 — DB 쓰기·API 호출 0):
   - 스코프: auto_operate=1 캠페인의 adgroup grain(경보·래더 중지 대상 개체 = 그룹).
     데이터 소스 = naver_ad_daily(as_of=D0=today−1, sentinel 제외), 순위 = 노출가중 rank_sum/imp.
   - **경보 조건**(창별 독립 평가, 하나라도 발화 시 경보):
     - W1(최근 1일): D0 단일일 imp ≥ 200 ∧ clk = 0 ∧ 그 창 avg_rank ≤ 4.0
     - W3(rolling 3일, D0-2..D0): 누적 imp ≥ 200 ∧ (clk = 0 또는 clk ≤ 기대클릭×0.2) ∧ 창 avg_rank ≤ 4.0
     - 기대클릭 = trailing CTR × 창 imp. trailing CTR = 그 그룹 직전 30일(창 제외, D0-32..D0-3) CTR,
       그룹 표본(임프) < 1,000이면 캠페인 CTR 폴백, 그것도 부재면 기대클릭 게이트 생략(clk=0 분기만).
     - 기대클릭 < 5면 −80% 분기 발화 금지(소표본 오탐 방지).
   - avg_rank ≤ 4.0 조건 = "입찰로 못 푸는" 판정 근거(밴드 안인데 클릭 없음). 밴드 밖(>4.0)은 래더 영역이므로 경보 아님.
   - 반환: `{alerts: [{campaign_id, adgroup_id, window, imp, clk, avg_rank, expected_clk?, reason}]}`.
2. **브리핑**: 일 레인(08:50)에서 산출 → 경보 있는 날만 diary(observe)+Slack — PX·VT 관례 미러.
   문구에 "입찰로 못 푸는 소재 CTR 문제 — 사람 처방 대상(썸네일·가격·리뷰)" 명시. 없는 날 침묵.
3. **래더 추가 UP 중지**: `_run_exploration_for_campaign` 후보 순회에서 후보 그룹이 CTR 경보 활성이면
   skip + `_record_blocked`(사유 `[탐색] CTR경보 — 소재 처방 대상, 추가 UP 무의미`). 상태 테이블 없음 —
   레인이 ctr_alert를 직접 재산출(순수 재계산·값싼 쿼리). **차단 범위는 탐색 래더만**(스펙 그대로 —
   시간당 밴드 레인·핫셋 레인은 불변).
4. 테스트: 04 실표본 픽스처(W1: 850노출·클릭0·그룹 rank≤4.0 발화 / W3 클릭 1~3 존재 시 W3 비발화 ∧
   W1 발화) + 기대클릭 −80% 분기 + 폴백 사다리 + 래더 skip 관통 + 경보 없는 날 무브리핑.

### §4.2 VT4 상세 설계 (Fable 확정 2026-07-22 — ★결함 발견 포함)

**착수 전 실측(07-22 16:20 prod)**: 03 캠페인 17프로(rank 6.39·밴드 밖·clk 0·bid 50)·17(6.49)·17에어(6.66)
— 래더가 상향해야 할 전형인데 **탐색 발사 이력 0(change_log 전무)**. 원인 실측:
- **플로어 스텝 함정(결함)**: `adaptive_step` cap = floor(50×1.3/10)×10 = 60 → `_finalize_step`에서
  60 < `_EXPLORATION_MIN_BID`(70) → **None(스텝 영구 소실)**. 50원 그룹은 어떤 경로로도 스텝 불가.
- **70원 하한의 근거 오적용**: prod 실측 — 쇼핑 그룹 158개가 bid=50으로 라이브 노출 수신 중(70원 미만은
  SHOPPING adgroup grain에만 존재·키워드 grain 0건). 즉 **쇼핑검색 최소입찰=50원, 파워링크 키워드=70원**.
  bid_simulator._MIN_BID(70)는 키워드 grain 실측 역추적 값 — 쇼핑 탐색 래더에 그대로 쓴 것이 결함.

1. **플로어 스텝 함정 수정**: exploration.py의 스텝 유효 하한을 쇼핑 규격으로 분리 —
   `_EXPLORATION_MIN_BID_SHOPPING = 50`(prod 158그룹 라이브 실증). 탐색 래더는 SHOPPING 전용
   (`_EXPLORATION_CAMPAIGN_TYPE`)이므로 `_finalize_step`의 하한을 50으로 교체. 첫 실쓰기(50→60)가
   API에서 거부되면 harness fail-closed가 change_log failed로 잡는다(라이브 검증 항목).
   bid_simulator._MIN_BID(70)는 불변(상한 산식용 보수 규격 — 별도 맥락).
2. **수요 우선 정렬**: `exploration_candidates` 결과를 순수 정렬 함수로 재정렬 —
   1순위: 플로어(현 bid ≤ 100) ∧ 판정 표본 미달(정착 clk < 10) 그룹을 최근 7일 노출 내림차순.
   2순위: 나머지 기존 entity_id 오름차순 유지(결정성 보존). 봉투·스텝 크기·상한 전부 불변
   ("순서만" — D-NAO-82② 스펙 그대로).
3. 테스트: 50원 그룹 스텝 성립(50→60) + 70원 하한 회귀(파워링크 무관 확인) + 정렬 결정성 +
   03 실표본 픽스처(17프로 rank 6.39 → step_up 판정 관통).

**금지선 확인**: 스텝 함정 수정은 "새 권한"이 아니라 기존 개방 레버(탐색 UP)의 산식 결함 수정 —
30% 캡·경제성 상한·2h 쿨다운·킬스위치·손실고삐 제외 전부 불변. 트랙 파일에 D-NAO-82① 확장으로 기록.
