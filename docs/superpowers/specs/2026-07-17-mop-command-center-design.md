# MOP 커맨드 센터 — 프론트엔드 설계 스펙

**작성**: 2026-07-17 (KST) · **트랙**: naver-ad-optimization · **결정번호**: D-NAO-47
**상태**: 구조 승인 완료(Jino) → 구현 계획 미작성
**선행 대화**: 브레인스토밍 세션 2026-07-16~17

---

## ⚠️ 0-A. 읽기 전 필수 — 병행 세션 충돌 (원칙 20)

**이 스펙은 `5599ccd`(PR #21 머지) 기준으로 작성됐다. 그 사이 다른 세션이 PR #22를 prod에 배포했다.**

- 이 세션(2026-07-16 23:20~07-17 01:00, 워크트리 `video-content-summary-0e6c41`)이 진행되는 동안, **별도 세션**(워크트리 `spot-backtest-cadence-pacing-2e3bfa`)이 07-16 23:59에 같은 트랙을 작업하고 **PR #22를 열고 prod에 배포**했다. 서로 몰랐다 — 원칙 20의 "트랙 동시 작업 금지" 위반 상태.
- **번호 충돌**: 그쪽이 D-NAO-44(pacing 완결도 보정)·**D-NAO-45(상설 소급 채점 신설·가동)**·D-NAO-46(이중 루프 확정)을 이미 씀 → **이 문서는 D-NAO-47로 재번호**했다.
- **★내용 재조정 — 완료(2026-07-17 01:30, 실물 확인함)**:
  - **`/retro-scorecard`는 change_log 성적표와 중복이 아니라 상보다.** 라이브 확인: `naver_retro_signal`(board·direction·verdict_d·cf_asof) = **진단 보드 신호의 d3/d7 방향 정밀도** 채점. 5,377행(bleeding 3140·starving 698·shopping_growth 694·shopping_bep 581·shopping_pause 215·pause 49, 07-08~07-15). 그쪽 docstring이 선을 그음: *"방향 정확도 계기판이지 인과 성과 검증이 아니다 — 인과 승격은 카나리 몫"*.
    → **두 세션이 모르는 채로 서로의 반쪽을 만들었다.** 그쪽=**조언이 맞았나**(실행 0이어도 채점 가능), 이쪽=**한 일의 결과**(인과, change_log). **§5의 change_log 조회 API는 여전히 필요**(폐기 아님). §4 2층 ③을 두 겹으로 개정 완료.
    → **오히려 이 설계가 좋아졌다**: 1층 "우리 조작 0회"만 있으면 초라한데, 그 옆에 "우리 판단 방향정밀도"가 붙는다. D-NAO-46 개방 순서(840카나리 → **성적표 신뢰** → 시간당 개방)에서 이 스펙의 1층이 곧 그 카나리 자리.
  - **★`trigger_pacing`을 "노이즈"로 본 초판 판정은 틀렸다 — §1-3 정정 박스 참조.** 저속 경보 98.7%가 correct(769/779), 평균 최종 소진율 4.9%. **접기 → 롤업으로 처방 변경.**
  - **D-NAO-46① `hourly_snapshot` 보존 7→365일 배포됨.** §1-4의 "9일(8,469행)"은 오늘 시점 사실이나 앞으로 늘어난다 — "9일치뿐"을 설계 전제로 깔지 말 것.
- **§1의 라이브 실측 수치(커버리지 1.15%·change_log 17행·제안 900/5건·키워드 91,005 등)는 PR #22 배포 후인 2026-07-17 01시경 prod DB 직접 조회 결과라 유효.**
- **처리 완료**: PR #22 머지 확인(`c4a728e`, 07-16 23:37 KST) · 이 워크트리를 최신 main으로 fast-forward(로컬 커밋 0이라 무손실) · 구 base 위 편집이던 track/progress는 **폐기하고 최신 main 위에 재작성**(아래 §0-B).

---

## 0. 한 줄 목표

우리판 MOP가 돌리는 광고의 성과를 1층에 놓고, 백엔드 45개 모듈이 만들어낸 데이터를 계층으로 열어보는 커맨드 센터를 만든다. **N=1(오늘 04 카나리)과 N=여럿(나중)이 같은 화면이어야 한다.**

## 1. 왜 — 라이브 실측 (2026-07-17 prod, `sellc.ohitech.co.kr:/home/ubuntu/ohisell/backend/ohisell.db` 읽기전용)

이 숫자들이 설계 근거 전부다. 추측 아님.

### 1-1. 우리 MOP가 맡은 광고 = 전체의 1.15%

최근 14일, `naver_ad_daily` ⨝ `naver_campaign_settings`:

| optimizer | 캠페인 | 광고비 | 클릭 | ROAS |
|---|---|---|---|---|
| **ours** | **1** | **204,135원** | 151 | 2.62 |
| none(미설정) | 44 | 17,590,650원 | 13,778 | 2.86 |

- `naver_campaign_settings` 전체가 **1행** — `cmp-a001-02-000000008514959`(04), optimizer=ours, **mode=NULL, gamma=NULL, target_roas_override=NULL**, updated 2026-07-12.
- 03(원본 MOP 유닛 6245)은 `optimizer='mop'` **태깅 안 됨** → 3열 대조의 MOP 열이 빈칸.

### 1-2. 우리 프로그램이 자동으로 바꾼 입찰가 = 0건

`naver_change_log` **전체 17행**:

| action | 건수 | dry_run | 의미 |
|---|---|---|---|
| `external_status_change` | 15 | 0 | **외부가 바꾼 걸 우리가 감지**(전부 userLock) |
| `optimizer_change` | 1 | 1 | 04를 ours로 켬 |
| `flight_pacing` | 1 | 1 | α 계산 1회 |

- outcome 채워진 행 **0** → D+14 학습 루프 산출물 없음.
- 07-15 04 통제 왕복(1450→+10→원복)은 change_log에 **없음** — harness 밖 직접 호출이었던 듯. (미확인)

### 1-3. 제안 900건 중 행동 가능한 건 5건 (4.5%)

`naver_proposals`:

| status | type | 건수 | last |
|---|---|---|---|
| expired | trigger_pacing | **788** | 07-15 |
| pending | trigger_pacing | **102** | 07-16 |
| expired | anomaly | 33 | 07-13 |
| expired | account_brief | 10 | 07-15 |
| pending | anomaly | 5 | 07-15 |
| **pending** | **bid_up** | **5** | 07-15 |
| expired | anomaly_freshness | 1 | 07-10 |
| expired | trigger_cpc_spike | 1 | 07-14 |

- **approved / executed 상태 0건.** 04 카나리 개방(07-12) 후 5일간 승인 0.
- pending 112건 중 Jino가 실제 결정할 건 bid_up 5건 = **4.5%**. 나머지 107건이 그걸 덮고 있다.
- 프론트 `PROPOSAL_TYPE_LABEL`은 6종만 정의 → `trigger_pacing`·`pause`·`resume`·`budget_up`·`anomaly`·`account_brief` 등 **9종이 영문 원문으로 렌더**. 반대로 `budget`·`new_setup`은 백엔드가 안 만드는 유령 라벨.

> **★정정 (2026-07-17 01:30, PR #22 머지 후 재조회 — 원칙22 실패 자인)**
> 초판은 `trigger_pacing` 890건을 **"노이즈"로 규정하고 화면에서 접자**고 했다. **틀렸다.** `expired 788`을 보고 "아무도 안 보고 만료 = 무시해도 되는 것"이라 **추론**했을 뿐, 그 경보가 진짜였는지 **확인하지 않았다.**
> D-NAO-45가 그 788건을 전수 채점해뒀고(`naver_retro_pacing_score` 788행 = 정확히 일치), prod 실측 결과:
>
> | kind | verdict | n | avg_final_ratio |
> |---|---|---|---|
> | 저속 | **correct** | **769** | **0.049** |
> | 저속 | partial | 9 | 0.696 |
> | 저속 | false_alarm | 1 | 0.995 |
> | 과속 | false_alarm | 9 | 0.12 |
>
> **저속 경보 779건 중 769건 correct = 98.7%가 진짜.** `avg_final_ratio 0.049` = 그 캠페인들이 **하루가 끝나도 일예산의 4.9%만 썼다**.
> → 노이즈가 아니라 **만성 저소진이 실재**하고, 시스템이 788번 정확히 알렸는데 **대응 레버가 없어 전부 만료**된 것. **접으면 진짜 신호를 숨긴다.**
> **처방 변경: "접기(hide)" → "롤업(aggregate)".** 개별 788건을 나열하지 말되, **집계는 1층 근처로 올린다**("저속 경보 769건 correct · 평균 최종 소진율 4.9%"). 롤업은 이미 `/retro-scorecard`가 제공한다 — 새로 만들 것 없음.
> (이 정정은 D-NAO-46 "폭 우선 성장" 확정과 같은 사실을 가리킨다.)

### 1-4. 수집 데이터는 풍부한데 화면에 없다

| 테이블 | 보유 | 화면 |
|---|---|---|
| `naver_hourly_snapshot` | 8,469행 · 9일(07-09~) · 매시 정상 | cost/clk/imp만. **daily_budget·소진율 미노출** |
| `naver_entity` | **키워드 91,005** · adgroup 1,002 · campaign 45 · 전부 bid_amt 보유 | **API 0건 — 볼 방법 없음** |
| `naver_search_term_daily` | 114,285행 · 12일 · shopping only | 제외 후보 몇 줄로만 |
| `naver_ad_daily` | 29,488행 · **188일**(2026-01-09~) | 리포트 탭 — 유일하게 잘 보임 |
| **입찰가·키워드 변경 이력** | **없음** | — |

### 1-5. ★ 근본 원인 — 이력이 저장되지 않는다

`backend/app/services/naver_ad/entity_sync.py`:
- `:176` `_log_external_status_change(...)` — **status(userLock)만** 로깅
- `:182` `e.bid_amt = r.get("bid_amt")` — **입찰가는 그냥 덮어씀**

→ 매일 07:35 크론이 91,005개 키워드의 어제 입찰가를 지운다. **"CPC 변경·키워드 변경"은 화면을 아무리 잘 만들어도 보여줄 데이터가 없다.** 지금 원본 MOP의 03 조정을 감시하려고 VM에서 `mop_keyword_detect.py`가 시간당 2,056키워드를 훑어 **로그 파일**에 적고 있으나 DB 밖이라 화면에 못 올린다.

### 1-6. 프론트 현황

- 라우트 **1개** `/naver-ad` → `NaverAdReport.tsx`가 탭 3개(리포트/진단보드/콘솔) 조건부 렌더. **탭이 URL에 없음 = 딥링크 불가.**
- MOP 3파일 **1,828 LOC** / 프론트 전체 12,203 LOC(15%).
- **`index.css` = 23바이트**(`@import "tailwindcss";` 한 줄). `@theme` 없음 = **커스텀 토큰 0개**. `tailwind.config.*` 파일 없음(v4 CSS-first).
- **공통 Button/Card/Table 없음.** `px-3 py-1.5 text-xs rounded border` 인라인 반복.
- `isoKST`/`fmt`/`won`/`pct`/`roasX` **3~4벌 중복 정의**(NaverAdReport / DiagnosisBoard / OptimizationConsole / AdReport). 공용 유틸 모듈 없음.
- UI 라이브러리·아이콘 라이브러리 **없음**(이모지 직접 사용). 상태관리·데이터패칭 라이브러리 없음(useState+fetch).
- React 19.2.4 / Vite 8.0.1 / Tailwind v4.2.2 / recharts 3.8.1 / react-router-dom 7.13.2. 테스트 파일 0개.
- `_serialize_proposal`(naver_ad.py:197)이 **`target_bid`/`target_lock`/`target_budget`/`budget_auto_eligible` 미포함** → "입찰 인상" 카드가 **얼마로** 올리는지 화면에 없음.
- `campaign_settings.gamma`는 라우터가 주고받는데 `api.ts` 인터페이스에 필드 없음 → 프론트 전체 `gamma` 0건.

## 2. 원본 MOP UX/UI 리뷰 (2026-07-17, 스크린샷 4장 직접 확인)

확인한 것: `02_shopping_optimization_main`, `16_report_optimization`, `15b_report_campaign_loaded`, `13_budget_pacing`.

### 베낄 것 (일부는 이미 베낌)
- KPI 스트립 8칸 한 줄 — 스캔 빠름 (이미 차용)
- 이중축 라인차트 + 지표 셀렉트 2개 + 기간단위 토글 (이미 차용)
- 최적화 아이템별 **스택바** — 커버리지를 한 줄로 (이미 차용)
- **필터 칩** `Search Ad ⊗ +4` — 선택분만 칩, 나머지 `+N` 접기. **우리 plain select보다 나음 — 미차용**
- **지표설정(⚙) 컬럼 고르기** — 미차용
- 데이터 기준시각 표기(`2026.07.10 05:49am`) — 정직함

### 베끼면 안 되는 것
1. **KPI에 ROAS가 없다.** 광고비 5,442,825 + 전환매출 8,298,250을 나란히 놓고 ROAS(1.52)는 미표시. CPC·CPA는 있음. → MOP=클릭최대화 도구임을 화면이 자백. 우리 D-NAO-1과 정반대.
2. **차트 Y축이 0에서 시작 안 함**(15b 노출 축 40,000부터) → 67k→48k(-28%)가 바닥 추락처럼 보임. **우리 recharts 기본값 점검 필요.**
3. **빈 상태 설계 없음.** 16번은 KPI 8칸 전부 0인데 이유(조회한 최적화 1119가 2024-06-18 종료, 조회기간은 2026-07-03~09) 미설명. 15b는 쇼핑에 없는 "조회수" 칸이 항상 0.
4. **화면 30~50%가 업셀.** 13번 우측 절반이 블러+자물쇠+"Pro 플랜 업그레이드". 사이드바 12개 중 8개 🔒. 좌하단 상시 업그레이드. 헤더 제휴 배너. → **우리는 단일 사용자라 이 자리를 전부 정보에 쓴다.**
5. **장식용 차트.** 13번 "매체별 비중" = 100% 한 조각 도넛 2개, 범례 "네이버" 하나. 정보량 0.
6. **저밀도.** 02번은 2행 테이블 아래 70% 백지, 그마저 우측 컬럼 뷰포트 밖 잘림.
7. **내부 ID 노출.** `최적화 ID 4099`, `cmp-a001-01-000000006006664`.

### ★ 결정적 — ref24 지적을 화면으로 확인
**리포트>최적화(16번)에 "알고리즘이 무엇을 왜 바꿨는지" 컬럼이 0개.** Date·Division·광고비·노출·조회·클릭·전환·전환매출·CPC·CPA 전부 결과 지표. MOP를 켜면 뭔가 알아서 하는데 뭘 했는지 사용자는 영영 모른다. **이게 우리가 이길 자리이고, 지금 우리도 비어 있다(§1-2, §1-5).**

## 3. 확정 결정 (Jino 승인)

- **D-47-a. 1층 = 우리 MOP가 돌리는 광고의 성과.** Jino 원문: *"우리가 만든 MOP에서 돌리고 있는 광고의 성과가 보여야해"*, *"우리 MOP가 돌리는 광고성과를 보자는거야"*
- **D-47-b. 커맨드 센터 겸 대시보드 — "모든 걸" 본다.** Jino 원문: *"이 화면을 보면서 가격변동, 키워드변동히스토리, 네이버에서 수집되는 데이터, 광고 성과, 감시, 조사등 모든 사항들을 보기위한 commend center겸 dash board"* → **버리지 않고 계층으로 푼다.**
- **D-47-c. N=1 → N=여럿 동일 컴포넌트.** Jino 원문: *"지금은 다양한 광고를 돌린다기보다 04캠패인을 카나리로 돌리고 있는거거든. 그런데, 나중에는 이 광고 캠페인이 많아질꺼거든."* → 카나리 전용 화면 금지. 합계 + 리스트(오늘 1행, 나중 N행).
- **D-47-d. 백엔드 3개를 이번 스프린트에 포함.** Jino 원문: *"모두 포함해서 가자"* → ①entity_sync 입찰가·키워드 diff 로깅(밸브) ②change_log 조회 API ③원자료(키워드·검색어·시간당) 조회 API.
- **D-47-e. 모드 다이얼·gamma는 화면에서 제거.** 배선(D-NAO-22-②)은 **별도 스프린트**. 이유: 목적함수 변경은 돈이 걸리고, 04 실집행 0이라 지금 배선해도 검증 불가(원칙 14). Jino 확인: *"맞아"*
- **D-47-f. `/command-center`(쿠팡 회계 검산)와 무관 — MOP 커맨드 센터는 `/naver-ad` 자리에서 키운다.** 이름만 겹침.
- **D-47-g. 03을 `optimizer='mop'`으로 태깅**해야 3열 대조가 채워짐(D-NAO-42-e 철학 대결을 화면으로).
- **D-47-h. "왜 0인가"를 1층으로 승격.** MOP UX 리뷰(§2-3) 결과 — 0만 찍는 건 MOP의 실패를 복제하는 것. 지금 우리 상태에선 빈 상태 설명이 곧 성과 설명.

## 4. 정보 구조

```
/naver-ad — 우리 MOP 커맨드 센터

┌ 1층 (열면 바로) ─────────────────────────────────
│ ① 관리주체 3열 대조   우리 MOP / 원본 MOP / 수동
│    · 우리 열만 크게(위계=대비). 캠페인수·광고비·클릭·전환·ROAS·BEP대비
│    · 커버리지 바 = 우리 광고비 / 전체 (현 1.15%)
│ ② 우리 MOP 캠페인 리스트  (오늘 1행 → 나중 N행)
│    · 캠페인|광고비|클릭|ROAS|BEP대비|우리 조작 수|상태
│    · ★"우리 조작 0회" 칸 = 프로그램이 일하는지 매일 보이는 자리
│    · "왜 0인가" 설명(임계 미도달/가드레일 차단/위임 꺼짐)  ← D-47-h
│    · [캠페인 넘기기] optimizer→ours = 카나리 확대가 여기서 일어남
└──────────────────────────────────────────────────
┌ 2층 (스크롤) ────────────────────────────────────
│ ③ 성적표 = 두 겹 (중복 아님 — 상보)
│    ⓐ 우리 조언이 맞았나  `/retro-scorecard` **이미 있음**(D-NAO-45)
│       보드별 d3/d7 방향정밀도 · retro_signal 5,377행 백필
│       (bleeding 3140·starving 698·shopping_growth 694·shopping_bep 581·
│        shopping_pause 215·pause 49, 07-08~07-15)
│       ★실행 0이어도 채점됨 → "우리 조작 0회" 옆에 붙일 유일한 신뢰 근거
│    ⓑ 우리가 한 일의 결과  change_log (D-NAO-14) — **현재 0건, 밸브 필요**
│       그쪽 docstring이 선 그음: "방향 정확도 계기판이지 인과 검증 아님
│       — 인과 승격은 카나리 몫" → 그 카나리가 이 스펙의 1층
│ ④ 나를 기다리는 것 + 저소진 롤업
│    · 행동 가능: bid_up 5건 표면
│    · **저속 경보는 접지 말고 집계**(769건 correct·98.7%·최종소진율 4.9%)
│      ← 정정, §1-3 참조. 롤업은 /retro-scorecard가 이미 제공
│ ⑤ 엔진 5단                수집→예측→제안→전문가→학습 (기존 재사용)
│ ⑥ 이상 피드               anomaly (라벨 신설)
└──────────────────────────────────────────────────
┌ 3층 (드릴다운) ──────────────────────────────────
│ ⑦ 계정 전체 리포트   기존 리포트 탭 그대로
│ ⑧ 진단 7보드         기존 그대로
│ ⑨ 원자료 탐색        키워드 91k · 검색어 114k · 시간당 스냅샷  ← 신규
│ ⑩ 키워드 랩          P4 백엔드 없음 — 자리만, 이번 스코프 밖
└──────────────────────────────────────────────────
```

## 5. 스코프

### 포함
- **백엔드 3**: entity_sync diff 밸브 / change_log 조회 API / 원자료 조회 API
- **직렬화 보강**: `_serialize_proposal`에 target_bid·target_lock·target_budget·budget_auto_eligible
- **제안 라벨 13종 정합**(유령 2종 제거, 누락 9종 추가)
- **프론트**: 1~3층 재편, 3열 대조, 캠페인 리스트, 성적표, 원자료 탐색
- **디자인 시스템**(별도 섹션 — 미설계): `index.css` @theme 토큰, 공통 Card/Table/Button/Badge/Stat, `lib/format.ts`로 유틸 중복 제거
- **점검**: recharts Y축 0 시작 여부(§2-2)

### 제외
- 모드 다이얼·gamma 배선(D-NAO-22-② → 별도 스프린트)
- 키워드 랩(P4 백엔드 없음)
- 판매가 변동 이력(이력 테이블 없음 → 별도)
- `gave_score`·`hierarchical_pooling` 배선(현재 dead code, 호출 0건)
- 순위 히트맵(G4, D-NAO-36으로 X 스코프 밖)

## 6. 위험

1. **`entity_sync` 변경은 매일 07:35 도는 크론을 건드린다.** 91,005행 diff 로깅 = 쓰기 폭증 위험. 입찰가 무변동 행은 로깅하지 않는 가드 필수. codex 게이트 필수(원칙 19).
2. **1층이 대부분 "0"·"막힘"으로 채워진다.** 볼품없지만 그게 사실이고, 사실을 보여주는 게 화면의 일(D-47-h).
3. 3열 대조는 D-47-g(03 태깅) 없이는 반쪽.
4. 이 스펙은 **소스·DB 실측 기반**이며 **배포본 화면(sellc.ohitech.co.kr)을 눈으로 확인하지 않았다**(원칙 22). 구현 착수 전 `/browse`로 라이브 확인 필요.

## 7. 열린 질문

- 디자인 시스템 섹션 미설계 — 토큰 범위(색·폰트·spacing 어디까지), 공통 컴포넌트 목록, 기존 화면 리스킨 여부(`PLAN_naver-ad-dashboard-mini.md:40`은 "전면 리스킨 안 함"으로 제한했었음 → 이번에 개정할지)
- 원자료 탐색(⑨) 상세 — 91k 키워드를 어떤 그레인·필터로 볼지
- `flight_pacing` α 결정(binding_constraint 등)을 성적표에 포함할지
