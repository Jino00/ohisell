# 작업 계약 — M3: 지혜 성적표 + 확정 지식 주입 + 채점기 교정 (북극성 §5-3 ①② + D-NAO-223 집행)

> 상태: **승인됨 — D-NAO-224** (2026-08-22 13:47 KST, Jino 원문 *"추천 기본값대로 가자"*). 작성 2026-08-22(초안, Fable).
> ⇒ **§8 Q1~Q6 전부 추천 기본값으로 확정**됐다(각 질문 아래 「★확정」 줄 참조).
> ⚠️**합격기준(§4)은 이 승인으로도 바뀌지 않는다** — 정본은 북극성 ref 82 §6 M3 행이고, 미달 시 기준 인하는 목적 전환 선언 없이는 금지다(§4-B ④⑤⑥은 처음부터 «신설»로 표기됐고 이 승인이 곧 그 신설의 승인이다).
> 트랙: `docs/tracks/active/track_naver-ad-optimization.md` (PAO) · 트랙 진행률 상 **M3** 칸 (체크박스 원문: *"M3 — L4 부품: 지혜 성적표 + 확정 지식 주입 배선 (승격 지혜 ≥1건에 성적 행 + 항등식 일치)"*)
> 정본 관계: **합격기준의 정본은 북극성 ref 82 §6 M3 행 원문**이다 — 이 계약은 그것을 «옮겨 적고 관측 경로를 보강»할 뿐, 재규정하지 않는다(M2 계약이 ref 65에 대해 한 것과 같다). 단 **§4-④⑤⑥은 정본 밖 «신설» 항목**이며 신설임을 명기한다 — ④⑤의 근거는 **D-NAO-223**(Jino 2026-08-22 00:08 원문: *"그렇게 가자, 다음 세션이 M3 계약에 박도록 인계에 적어줘"*), ⑥의 근거는 ref 91 §7-1 M3-e이고, 셋 다 이 계약의 승인으로 확정된다. 인용과 신설을 섞어 표기하면 재규정 금지가 무너진다(M2-b2 전례).
> 착수 시점: **M2-z(M2 종결 QA)가 M2를 닫은 뒤**(HANDOFF_ref90 §6 ③). 단 M2 «전체» 종결을 기다릴 필요는 없다 — **북극성 §6 원문 *"★M 사이 병렬 허용: … M2∥M3(둘 다 쓰기 없는 배선). M4만 직렬 게이트다"*** 가 병렬을 허용한다(§6 순서 근거 참조).
> 저술: Fable (구조 산출물 저자 슬롯). 이 문서 1개 외 무변경 — 코드·트랙·매트릭스·prod 무접촉.

---

## §1. 목표 / 이번에 안 하는 것

### 목표

트랙 목표 원문(D-NAO-59, Jino 2026-07-19):

> *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."*

M3은 그 목표의 **L4(학습층) 부품을 붙이는 칸**이다(북극성 ref 82 §5-3 ①②·§6 M3 행). 구체적으로 셋을 집행한다:

1. **지혜 성적표(wisdom scorecard)** — 승격된 지혜 1건마다 「그 지혜가 낳은 제안들의 GAVE 점수·ad_profit 합」을 소급 채점에서 롤업. 북극성 §5-3 ① 원문: *"부품(gave_score·retro_scorer·diary `outcome_json`)은 전부 있고 **지혜 id로 묶는 조인 하나가 없다.**"* — 새 시스템이 아니라 조인·배선이다.
2. **연관 확정 지식(L1 A/B)의 판정면 주입** — 첫 사례 A#8 주말·공휴일 라벨이 성적표 API 응답에 실재하게(북극성 §5-3 ②). ⚠️M2-d(S2-ⓐ)와의 경계는 §8-Q4.
3. **★채점기 교정(D-NAO-222→223)** — 성패를 찍는 두 축이 목적함수(총이익 «절대액», D-NAO-59)가 아니라 «효율 비율»을 재고 있다(ref 90): `proposal_scoreboard`는 RPC 배율(`:97-103`)로 improved/declined을 찍고 — **라이브 improved 4건이 전건 매출 감소**(ref 90 §2-2, id 761은 매출 −48.3%인데 「개선」) — 지혜 승격 게이트 `_outcome_direction`(`wisdom_candidates.py:70`)은 `roas_c ≥ target`만 본다. **이 어긋남을 고치지 않고 1을 배선하면 성적표는 «효율 좋은 축소»를 「돈이 됐다」로 롤업한다** — 그래서 교정이 M3의 별도 명문 항목이다(§4-④). 이유는 인계가 명시한다: *"M3의 현 합격기준은 채점기가 무엇을 재든 충족된다 ⇒ 교훈 #343 「담당은 완료가 아니다」 재발"*.

이게 되면 달라지는 것: 「어떤 학습 포인트가 돈이 됐나」가 Jino 질문(2026-08-21 23:26 *"우리가 대행사보다 잘 운영할 수 있는 준비가 되었냐가 중요하지"*)에 답할 수 있는 **유일한 경로**(단위 조치 채점)가 올바른 자로 서고, 확정 지식이 문서가 아니라 API 응답에 실린다. M4(재개) 창이 열리는 순간부터 새 실집행이 **처음부터 올바른 식으로** 채점된다.

★**기각안 2개 — 다시 상정하지 않는다**(Jino 확정, HANDOFF_ref90 §6 ③): ⓒ **M2 편입**(ref 88의 M2-z 분모가 흔들린다) · ⓓ **부채 기록만**(M3 착수 시 어차피 부딪힌다). 그리고 **새 마일스톤을 만들지 않는다** — 채점기 교정의 «라이브» 증거는 새로 채점될 실집행 행을 요구하는데 실집행이 07-30 이후 0건이고 재개가 M4라, 별도 M은 원리적으로 종결 불가능한 M이 되어 분모만 2/7→2/8로 는다(전역 §1 「게이트를 새로 세우지 않는다」와 같은 결).

### 이번에 안 하는 것 (인접하지만 범위 밖 — 소관 병기)

| 안 함 | 소관 |
|---|---|
| 지혜의 **자동 적용** · 지혜 성적표를 근거로 한 **자동 파라미터 변경** | **영구 금지선**(북극성 §6-b M3 원문: *"성적표는 «재는 자»이지 «돌리는 손»이 아니다"*) — §3 |
| 새 채점식의 **첫 라이브 채점**(새 실집행 행에 대한) | **M4 창의 관측 항목**(§4-⑤ 경계 문언) — M4 계약 |
| PAO 자동 쓰기 재가동(`optimizer='none'` 7/7 해제)·소유권 분리 | **Jino 결정 2건**(북극성 §8-①②) — M4 계약. 이 계약은 재가동을 전제·촉구하지 않는다 |
| M4 점검표 후보 11건(ref 91 §7-2 M4-a~k: 액셀 대칭·대조군 분할·창 재검토·네이티브 자동입찰 등) | **M4 계약** — ref 91이 초안을 만들어 뒀다 |
| 「우리 vs 대행사」 **숫자 생산** | **의도적 미생산**(ref 90 §6: 창 비교는 교락 — D-NAO-183 준-공선. 단위 조치 채점은 이 계약이 고치는 그 채점기가 재던 것) |
| M2 잔여(M2-d S2 배선·M2-z 종결 QA) | **M2 계약**(`docs/PLAN_naver-m2-l2-wiring.md`, D-NAO-214) — A#8 «라벨 배선 자체»(S2-ⓐ)는 M2-d 소관, 경계는 §8-Q4 |
| ref 90 §8-B 미열람 40여 파일의 스윕 확장 | **소관 없음(별도 조사)** — 「19/4/4」는 훑은 범위 안의 개수라는 캐벗은 유효한 채 남는다 |
| `outcome='success'` 2행을 «누가» 썼는가의 규명 | **잔존 [미상]**(저장소 밖이라 데이터로 못 가른다 — ref 90 §8-A 2) · 이 계약은 그 2행의 **처분**(§8-Q1)만 다룬다 |
| E8 positivity 판정·F6 광고 밖 수요 대조축 | ref 91 §7-3 — 어느 M에도 미배정, Jino 결정 대기 |

---

## §2. 판단기준 ("항상 A, 왜냐하면")

1. **항상 북극성 ref 82 §6 M3 행 원문이 합격의 정의다** — 옮겨 적고 관측 경로만 보강하며, 애매하면 애매하다고 §8에 올린다. 왜냐하면 재규정이 자기채점 부활 경로이고(전역 §2 미달 후 기준 인하 금지), M2 계약 §2-1과 같은 결이다.
2. **항상 신설 항목은 «신설»이라 표기한다**(§4-④⑤⑥) — 왜냐하면 인용으로 위장된 신설은 재규정 금지를 안쪽에서 무너뜨린다(M2-b2가 같은 상황에서 세운 규율).
3. **항상 기존 부품의 재사용을 신설보다 먼저 실측한다** — 왜냐하면 올바른 목적함수의 모양(`gave_score.py:60-63`, `penalty = min(ratio**γ, 1)` × `revenue`)이 **이미 코드에 있고 정지 중**이며(ref 90 §4-3·북극성 부록 A ⑤), 북극성 §5-3 ①이 「필요한 것은 조인 하나」라고 못박았다. 새 상수·새 문턱 발명 금지는 M2 §3을 승계한다.
4. **항상 착수 첫 작업은 배선이 아니라 선행 조사다**(§4-0) — 왜냐하면 합격 ①의 원료(승격 지혜 ≥1건)가 실재하는지가 [미상]이고(북극성 [미상] 표가 「M3 착수 전 확인」으로 지정), D-NAO-223의 전제 절반(「`dry_run=False` 행 07-30 이후 0건」)이 아직 미실측이다(HANDOFF_ref90 §6 ③).
5. **항상 라이브 증거의 경계를 M4 앞에 긋는다** — M3 합격은 배선 + 소급/표본 증거까지, 새 식의 첫 라이브 채점은 M4 창의 관측 항목(§4-⑤). 왜냐하면 실집행이 07-30 이후 0건(재개는 M4)이라 이 경계 없이는 M3이 원리적으로 종결 불가가 된다(ref 91 §2-3 M3↔M4 순환).
6. **항상 기존 `outcome` 150건은 §8-Q1 결정 전까지 불변이다** — 검산은 읽기 전용 재계산으로만 한다. 왜냐하면 교훈 #274(`backfill_outcomes` 강제 실행이 `cost:0`을 영구히 굳힌 사고)가 정확히 이 자리에 붙어 있고, 판정하러 간 기가 증거를 만들면 억지 충족이 된다.

---

## §3. 금지선

- **지혜의 자동 적용 금지 · 지혜 성적표를 근거로 한 자동 파라미터 변경 금지** — 북극성 §6-b M3 「안 함」 원문 그대로: *"성적표는 «재는 자»이지 «돌리는 손»이 아니다."* 지혜의 출구는 «제안»까지(D-NAO-54 설계 금지선).
- **판정면 주입은 A/B(확정 지식)만** — C(미검증 가설) 주입 금지(북극성 §5-3 ②·§7).
- **`optimizer='none'` 7/7 불변 · 실집행 재개의 실행·전제·촉구 금지** — 재개는 M4(Jino 결정 2건 선행). 입찰·예산·제외의 신규 자동 쓰기 0건.
- **기존 `naver_change_log.outcome` 값(150건 + `success` 2행)을 §8-Q1 결정 전에 UPDATE 금지.** backfill류 소급 스크립트 강제 실행 금지(교훈 #274). 새 식 검산은 SELECT + 수기/스크립트 재계산까지만.
- **「우리 vs 대행사」 비교 숫자 생산 금지** — ①창 비교는 교락(D-NAO-183 준-공선) ②단위 조치 채점은 교정 «전» 채점기가 효율을 잰다(D-NAO-222). 어느 쪽 숫자도 만들지 않는다.
- **기각안 재상정 금지** — ⓒM2 편입 · ⓓ부채 기록만(§1).
- **새 상수·새 문턱 발명 금지** — 채점식은 `gave_score` 기존 모양·기존 상수 계열 재사용 우선. 재사용이 불가능하면 구현을 멈추고 §8 경로로 올린다(M2 §3 승계 — Q7 절차 이탈의 재발 방지).
- **DB 스키마는 마이그레이션으로만** + 배포는 `scripts/safe_deploy.sh` 전용(§7). 직접 scp/rsync 금지.
- **합격기준 미달 시 기준 인하 금지** — 목적 전환 선언 없이는(전역 §2). 미달·판정불능은 유효한 종결 상태다.
- **새 D-NAO 번호를 임의 발급하지 않는다** — 이 계약의 처분 번호는 **D-NAO-223(기지정)**이다. 추가 결정이 필요하면 `scripts/next_ids.sh`로 받되, **발급됨·미사용인 D-NAO-224**(HANDOFF_ref90 §3)를 먼저 소진한다.

---

## §4. 합격기준 — 북극성 원문 «그대로» + 관측 경로 보강 + 신설 3항목

> ①②③의 인용문은 북극성 ref 82 §6 M3 행 원문(트랙 체크박스 문언과 동일 계열)이다. **[관측]** 줄은 이 계약이 «덧붙인» 관측 경로 보강이지 기준 변경이 아니다. **[⚠️애매]** 는 원문을 고치지 않고 애매함만 표시한 것 — 해소는 §8. **④⑤⑥은 정본 밖 «신설»**이며 근거를 항목마다 병기했다.

### §4-0. 선행 조사 — 다른 어떤 항목보다 먼저 (전부 읽기 전용, 코드 변경 0)

**P1. `ops_wisdom_entries` 행수 확인.** 북극성 [미상] 표 원문: *"지혜 승격 실적(`ops_wisdom_entries` 행수) — 다음 세션 prod 읽기 1쿼리면 확정 | §3 L4 행 · **M3 착수 전 확인**"*. 승격 지혜가 0건이면 합격 ①(「승격 지혜 ≥1건에 성적 행」)이 **원리적으로 관측 불가**라, 이 조사가 §4의 다른 모든 항목보다 먼저다. 함께 확인: **「승격 지혜 1건(07-27)」 주장의 실체**(ref 91 §8-4가 미확인으로 남김 · M2 계약 §1도 *"승격 지혜 1건뿐이라 표본 없음"*이라 적었다 — 실측 전엔 둘 다 못 믿는다). 0건일 때의 처분 경로는 **§8-Q2**.
- **[관측]** `ssh sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db 'SELECT COUNT(*), MIN(created_at), MAX(created_at) FROM ops_wisdom_entries;'"` (테이블 실재는 `models.py:4051` `__tablename__ = "ops_wisdom_entries"`로 로컬 확인 완료, 2026-08-22).

**P2. 「`dry_run=False` 행이 07-30 이후 0건」 실측.** D-NAO-223의 근거(「별도 M이면 종결 불가」)는 두 전제 위에 있는데 **절반만 실측됐다**: `run_daily`의 대상 필터(`proposal_scoreboard.py:112-116` — `dry_run IS FALSE` ∧ `verify_date IS NOT NULL` ∧ `verify_date <= today` ∧ `outcome IS NULL`)는 직접 열어 확인됐고(이 계약 작성 시에도 로컬 재확인), **「실집행 07-30 이후 0건」은 여전히 미실측**이다(HANDOFF_ref90 §6 ③ ⚠️). 참이 아니면 §4-⑤ 경계 문언의 전제가 바뀌므로 착수 시 반드시 먼저 잰다.
- **[관측]** `SELECT COUNT(*), MAX(changed_at) FROM naver_change_log WHERE dry_run=0 AND changed_at > '2026-07-30';` (기대 0건 — 0이 아니면 그 자리에서 §8 경로로 보고하고 ⑤의 문언을 재검토에 올린다).

> ---
> ## ★§4-0 실측 결과 — **P1·P2 둘 다 해소됨** (2026-08-22 13:5x KST · 읽기 전용 `sqlite3 -readonly` · 승인 D-NAO-224 직후 코디네이터 인라인)
>
> **P1 — `ops_wisdom_entries` = 1행.** `SELECT COUNT(*), MIN(created_at), MAX(created_at) FROM ops_wisdom_entries;` → `1 | 2026-07-26 23:45:56 | 2026-07-26 23:45:56`.
> ⇒ **「07-27 승격 지혜 1건」은 실재한다**(UTC 저장이므로 KST 2026-07-27 08:45:56 — 인계가 말해 온 「07-27」과 일치). 북극성 `[미상]` **해소**.
> ⇒ **§8-Q2 확정 경로 중 앞쪽이 발동한다** — 그 1건으로 합격 ①을 관측한다. **ⓒ(판정불능 명기 + 부분달성 종결) 분기는 불발동.**
> ⚠️단 **표본이 1건뿐**이므로 ①은 「≥1건」 문언을 충족할 뿐 통계적 대표성은 없다 — 성적 행의 «값»을 일반화해 인용하지 말 것.
>
> **P2 — 전제는 참이나 계약 초안의 쿼리 문언이 부정확했다.** 초안 쿼리 `WHERE dry_run=0 AND changed_at > '2026-07-30'` → **595건**(기대 0). action별로 가르면:
>
> | 성격 | action | n | 최신 `changed_at` | `proposal_id` | `executed_at` |
> |---|---|---|---|---|---|
> | **외부 변경 감지**(우리 실집행 아님) | `external_bid_change` 432 · `external_keyword_added` 72 · `external_status_change` 10 | **514** | 2026-08-21 07:37:12 | **전건 없음** | **전건 없음** |
> | **제안 기반 실집행** | `update_bid` 69 · `update_budget` 2 · `budget_down_pacing` 1 | **72** | **전건 2026-07-30** | 72/72 | 72/72 |
> | 비-제안 운영 조작 | `manual_emergency_stop` 5(07-30 10:50) · `update_guardrail_params` 2(**2026-08-11 09:18**) · `set_user_lock` 2(**2026-08-10 09:57**) | 9 | 2026-08-11 | 없음 | 있음 |
>
> ⇒ **「제안 기반 실집행이 07-30 이후 0건」은 참이다** — 595가 나온 원인은 ①`external_*` 514건이 «감지»지 «집행»이 아니고 ②`> '2026-07-30'`이 **07-30 당일을 포함**해 그날 72건을 끌어온 것이다.
> ⇒ **§4-B ⑤ 경계 문언의 전제는 살아 있다**(재검토 불필요 — 초안 P2가 예고한 「0이 아니면 ⑤를 재검토에 올린다」 경로는 **발동하지 않는다**).
> ⇒ **정정된 P2 쿼리(이후 이것을 쓸 것)**:
> `SELECT COUNT(*), MAX(changed_at) FROM naver_change_log WHERE dry_run=0 AND proposal_id IS NOT NULL AND changed_at >= '2026-07-31';` (기대 **0**)
> ⚠️**「0건」이라 말할 때 무엇의 0건인지 밝힐 것** — 비-제안 운영 조작(guardrail 파라미터·user lock)은 **08-11까지 실재**한다. 「광고 조작이 완전히 멈춰 있다」는 서술은 과장이다.
> ⚠️`external_*` 감지는 **08-21까지 살아 있다**(432건) — 대행사가 계속 입찰을 바꾸고 있다는 뜻이고, 이는 ref 91 §7-2 M4-b 「대조군 분할」의 원료다(소관: M4 계약).

### §4-A. 북극성 원문 3항목 (옮겨 적음)

> ① **승격 지혜 ≥1건에 성적 행이 붙고**

- **[관측]** 성적표 산출(테이블 또는 API — 표면은 §8-Q3에서 확정) SELECT: 지혜 id 1건에 대해 「그 지혜가 낳은 제안들의 GAVE 점수·ad_profit 합」 행 ≥1. 롤업 원료 좌표 병기(`gave_score`·`retro_scorer`·`ops_diary_entries.outcome_json` — 북극성 §5-3 ① 원문의 세 부품). P1 결과가 0건이면 이 항목은 억지 충족하지 않고 **§8-Q2의 확정 경로**를 따른다(기준 인하 아님).

> ② **A#8 라벨이 성적표 API 응답에 실재하며**

- **[⚠️애매]** 「성적표 API」가 어느 표면인지 원문이 특정하지 않는다 — 이 저장소엔 성적표 계열 API가 복수다(`/api/naver/ad/retro-scorecard` · `/expert-scorecard` · search_term/creative scorecard, 2026-08-22 grep 실측). 그리고 A#8 라벨의 «배선 자체»는 M2-d(S2-ⓐ) 소관과 겹친다 → **§8-Q4**. 문언은 고치지 않는다.
- **[관측]** `curl -u $(cat ~/.ohisell_prod_auth) <확정된 성적표 API>` 응답 **HTTP body**에 `weekend_holiday`(A#8) 라벨 키 실재. ⚠️서비스층 dict이 아니라 HTTP body로 확인한다 — `response_model`이 키를 지운 사고 전례(교훈 #321, schemas.py에 같은 경고 주석 4개).

> ③ **항등식(평시+주말·공휴일=전체)이 표본 1주에서 일치**

- **[⚠️애매]** M2 계약 S2-① 합격기준(*"«평시+주말+공휴일 = 전체» 항등식이 표본 1주분에서 일치"*)과 **같은 문언이 두 M에 있다**. 어느 쪽 판정이 정본인지 원문만으로 확정 불가 → **§8-Q4**. 이중 계상(같은 검산 1회를 두 M의 달성 증거로 쓰는 것)을 피하는 처분을 Jino가 정한다.
- **[관측]** 검산 SQL 1본(Σ평시+Σ주말·공휴일 = Σ전체, 1주 창) — M2-d가 이미 검산했으면 그 산출물 좌표를 인용하고 이 계약 몫은 Q4 확정에 따른다.

### §4-B. 신설 3항목

> ④ **[신설 — D-NAO-223] 채점기 교정이 §4의 별도 항목으로 명문화된다**: 성패 판정 두 축 — ⓐ`proposal_scoreboard`의 outcome 판정식(현행 RPC 배율, `:97-103`) ⓑ지혜 승격 게이트 `_outcome_direction`(현행 `roas_c ≥ target`, `wisdom_candidates.py:70-89`) — 이 목적함수(총이익 절대액, D-NAO-59) 정합 형태로 교정·배선된다. 교정 범위(ⓐ만 vs ⓐⓑ 모두)는 **§8-Q6**, 식의 형태는 **§8-Q5**.

- **왜 명문인가**: 원문 합격기준 ①~③은 **채점기가 무엇을 재든 충족된다** — 명문화하지 않으면 교훈 #343 「담당은 완료가 아니다」가 재발한다(인계 명시. 「M3에 포함」이 아니라 「§4에 문장으로」인 이유).
- **[관측 — 소급/표본 검산, 쓰기 0건]** ref 90 §2-2의 `improved` 4건(id 221·222·761·942 — **전건 매출 감소**, id 761은 클릭 −68.5%·매출 −48.3%)을 새 식으로 **읽기 전용 재계산**해, 4건 중 「매출 절대액 감소인데 개선」으로 남는 건이 **0건**임을 표로 남긴다(각 행: 원 판정 → 새 식 산출 → 근거 수치). 이것이 「D-NAO-85 축소판을 개선으로 기록하던」 결함이 닫혔다는 증거다. ⚠️기존 `outcome` 컬럼은 건드리지 않는다(§3 · §8-Q1 결정 전 불변).
- **[관측 — 배선]** 교정된 식이 배포되어, `run_daily`(또는 후속 채점 경로)가 **다음 채점 대상 행부터** 새 식으로 찍게 배선됐음을 코드 좌표 + 배포 커밋 + prod 반영으로 확인. 단 새 채점 «행»의 라이브 발생은 ⑤의 경계 밖이다.

> ⑤ **[신설 — 경계 문언(ref 91 §7-1 M3-b)] M3 합격 = 배선 + 소급/표본 증거까지이고, 새 식의 «첫 라이브 채점»(새 실집행 행에 대한 outcome 기입)은 M4 창의 관측 항목이다.**

- **왜 필요한가**: 실집행이 07-30 이후 0건(P2가 실측)이고 재개가 M4라, 이 문언 없이는 M3이 **원리적으로 종결 불가능한 M**이 된다(ref 91 §2-3 M3↔M4 순환). M3-z 종결 QA는 이 경계를 기준으로 판정한다 — 「라이브 채점 행 0건」은 미달 사유가 아니다.
- **[관측]** M4 계약 초안(ref 91 §7-2)에 「새 식 채점 기록」이 관측 항목으로 걸려 있음을 좌표로 병기(M4-a) — 경계 반대편의 수취인이 실재함의 확인.

> ⑥ **[신설 — 값 정확도 게이트(ref 91 §7-1 M3-e·E6)] 성적표가 소비하는 「총이익」 값의 정의 — grain · 지연(전환 정착 D+1~7) · 커버리지(상품 단위 BEP 확보 230/854그룹, 미확보 624그룹은 계정 블렌디드 근사) — 가 성적표 산출물에 명시되고, 산출 행에 «상품BEP / 블렌디드» 구분이 병기된다.**

- **왜 필요한가**: E6(외부 대조)이 F4를 격상시켰다 — 「미판정 그룹이 많다」가 아니라 **「입력값이 근사인 채로 최적화를 돌린다」**(ref 91 §3-2). 성적표가 근사값을 확정값처럼 합산하면 「돈이 됐다」 숫자 자체가 오염된다.
- **[관측]** 성적표 산출 조회에서 구분 라벨 열 실재 + 표본 행 1건에서 라벨과 BEP 원천(상품/블렌디드)이 일치함을 대조. 상품BEP 확보 «확대» 자체는 소관 밖(track_product-connection-map — §1 안 함 표 아님, 트랙 헤더 `안함:` 참조).

### 완료 QA

- 슬라이스마다 별도 Sonnet QA(읽기 전용) + **M3 종결 시 §4-0 P1·P2 + ①~⑥ 전수 재판정 1회**(M3-z). 대조 대상마다 따로 판정(앵커 `대조:` 규율 — 계약 §4 / Jino 2026-08-22 00:08 지시 원문 / 트랙 목표 D-NAO-59). 어휘 5값. stale 증거 제외. QA는 처분을 지정하지 않는다(전역 §2 ★).

---

## §5. 예산 (제안 — Jino 조정 가능)

- **상한 제안**: 구현 슬라이스 3개 + 선행 조사 1개(§6) × 각 **1세션·반나절** + 종결 QA 1회. 슬라이스가 한 세션에 안 끝나면 그 자리에서 미달로 닫고 인계한다(연장하지 않는다 — M2 §5와 동일).
- **네이버 API 신규 콜: 0.** M3의 원료는 전부 내부 DB(diary·change_log·wisdom·GAVE 산출)와 기존 크론 산출물이다. 새 외부 표면을 여는 순간 이 계약 밖이다.
- **prod 쓰기**: 성적표 산출 저장(마이그레이션 1건 예상, §7)과 채점식 코드 교체·배포까지. **기존 `outcome` 값 UPDATE 0건**(§3 · Q1 결정 전).
- **토큰**: 상한 명시 없이 «한 세션 = 한 슬라이스» 시간 상한으로 갈음(M2 §5 확정 방식 승계). 직전 칸 규모 초과 조짐이면 중단·보고.

---

## §6. 슬라이스 제안

| # | 내용 | 완료 정의 | 순서 근거 |
|---|---|---|---|
| **M3-p** | **선행 조사(읽기 전용)** — §4-0 P1(`ops_wisdom_entries` 행수 + 07-27 승격 지혜 실체) · P2(`dry_run=0` 07-30 이후 0건) · 성적표 저장 표면 실측(§8-Q3의 «착수 첫 실측») · `gave_score` 정지 상태·소비처 6곳 재확인(북극성 부록 A ⑤ — 08-17 좌표라 재확인 필수, M2-b2에서 3좌표 중 미스사이트가 나온 전례) | 4건 관측값이 판정문과 §8 확정 각주로 남음. 코드 변경 0 | 착수 첫 작업(§2-4). P1이 0건이면 Q2 분기, P2가 거짓이면 ⑤ 재검토 — 배선보다 먼저 갈려야 하는 것들 |
| **M3-a** | **지혜 성적표 조인 배선**(북극성 §5-3 ①) — 지혜 id → 그 지혜가 낳은 제안들의 GAVE·ad_profit 롤업 + 산출 저장(표면은 Q3 확정에 따라) + ⑥ 값 정확도 라벨(«상품BEP/블렌디드» 구분 열) | §4 합격 **①⑥** 라이브 관측 + 기존 테스트 새 실패 0(착수 시점 main 선행 실패 목록을 기준선으로 병기 — M2 S1-⑥ 방식 승계, 북극성 항목엔 없으므로 슬라이스 완료 정의로만 둔다) | M3의 몸통. **M2-d 비의존**이라 병렬 허용의 실체가 이 슬라이스다 |
| **M3-b** | **채점기 교정**(D-NAO-223) — Q5·Q6 확정에 따라 ⓐ`proposal_scoreboard` 판정식 (ⓑ승격 게이트) 교정 + improved 4건 읽기 전용 표본 검산 + Q1 확정 처분의 집행 | §4 합격 **④⑤**(⑤는 경계 확인) + 회귀 0 | a와 파일이 겹치지 않는다(scoreboard/wisdom_candidates ↔ 롤업 신설부). Q1이 미확정이면 검산까지 하고 소급 처분만 남긴 채 닫을 수 있다 |
| **M3-c** | **확정 지식(A#8) API 층** — 성적표 API 응답에 라벨 실재 + 항등식 검산(Q4 확정 경로대로) | §4 합격 **②③** | ⚠️**M2-d(S2-ⓐ, 2026-08-28 이후 진입) 산출에 의존** — Q4 확정 전엔 판정 불가. a·b를 먼저 소화하면 대기 낭비가 없다(M2 §6의 d 배치와 같은 결) |
| **M3-z** | **M3 종결 QA** — 별도 Sonnet(읽기 전용)이 §4-0 + ①~⑥ 전수 라이브 대조 → 트랙 M3 체크박스는 이 판정으로만 | 5값 판정문 + 트랙 헤더 `확인:` 기록 | 만든 쪽 ≠ 판단하는 쪽 |

- 권장 순서: **p → (a ∥ b) → c → z**. 착수는 **M2-z가 M2를 닫은 뒤**(HANDOFF §6 ③의 시점 지정)이되, M2-d(08-28 게이트)와는 병렬 가능 — 북극성 병렬 허용 문장(§1)이 근거고, c만 M2-d 뒤로 밀린다.
- 각 슬라이스는 main 기준 워크트리에서(`git switch -c <새> origin/main`), 구현 = Sonnet, 적대 리뷰 = PR 경계 1회 의무.

---

## §7. 배포·마이그레이션 주의 (이 저장소의 금지선을 이 작업에 구체화)

- **마이그레이션이 있는 슬라이스는 a다**(성적표 롤업 저장 — 신설이든 확장이든 스키마 변경). 순서 강제: `scripts/safe_deploy.sh backend/alembic/versions/<신규>.py backend/app/models.py <코드…> --migrate --restart` — ①마이그 파일 배포 ②원격 `alembic upgrade head` ③코드 ④재시작. `--migrate` 없이 코드 먼저 올리면 ORM `no such column`으로 해당 테이블 ingest 경로가 통째로 침묵한다(2026-07-28 실증). 컬럼 삭제형은 이 계약에 없다(있으면 계약 위반 신호).
- ⚠️**마이그레이션은 PostgreSQL 실행 가능성까지 검증한다** — M2-a의 적대 리뷰 P1이 정확히 이것이었다(Boolean에 정수 리터럴 → PG는 트랜잭션 롤백, SQLite·CI는 전부 초록). 그리고 **이 저장소 alembic은 `DATABASE_URL`을 무시한다**(교훈 #341) — 격리 검증이 로컬 DB를 바꾼 전례가 있으니 검증 경로를 먼저 확인한다.
- **배포는 전 슬라이스 `scripts/safe_deploy.sh` 전용**(D-NAO-49) — CAS 거부는 다른 세션의 배포: 덮지 말고 fetch·병합 후 재시도. 프론트를 건드리면(②가 API까지면 불필요) `--frontend` CAS + 거부 시 재빌드까지.
- **PR 병합은 `scripts/safe_merge.sh`** — ⚠️현재 CI 빨강은 **결제 정지**(steps 0개·2~4초)지 코드 신호가 아니다(2026-08-22 인계 실측). `--force` 사용 시 판정문에 사유 병기.
- **배포 전 `git log origin/main..HEAD`·열린 PR 실측** — ⚠️현재 **origin 미푸시 6커밋**(병행 세션 것 포함, HANDOFF §5-5)이 있어 push가 남의 커밋을 같이 올린다. 「PR 미병합인데 prod 배포됨」(PR #315)·「다른 세션 미적용 마이그레이션에 배포가 막힘」(D-NAO-204) 전례 승계 — 배포 후 병합까지 같은 슬라이스 안에서 닫는다.
- **크론 신설이 있으면**(성적표 롤업 일 1회 등) seed 시각을 배포 전에 Jino에게 확인한다(M2 §8-Q7이 세운 절차 — 하한은 원료 크론 이후. 현행 학습 대역: 반성 08:35 · `run_naver_learning_loops`의 롤업 로그 `scheduler_service.py:629`). ⚠️prod 디스크는 인계 주장 95%가 아니라 **실측 81%·여유 20G**(2026-08-22)다 — 신설 테이블 용량 추정은 이 숫자 위에서.
- **D-NAO 번호는 `scripts/next_ids.sh`로만** — 단 **D-NAO-224가 발급됨·미사용**이니 먼저 쓴다(§3).
- 세션 종료 시 저장소를 main에 세워둔다(전역 §6).

---

## §8. Jino 확정 질문 — **전건 확정됨 (D-NAO-224, 2026-08-22 13:47 KST)**

> Jino 원문: *"추천 기본값대로 가자"* ⇒ **Q1~Q6 전부 아래 「→ 추천」이 그대로 확정값이다.**
> 확정 요약: **Q1** 소급 UPDATE 안 함(새 식 산출은 별도 표면·원 `outcome` 불변·id 974·975는 옵트아웃 라벨 병기) · **Q2** 07-27 승격 지혜 실재하면 그 1건으로 ① 관측, 부재 시 ⓒ(판정불능 명기 + 배선 완성 + 부분달성 종결 허용) · **Q3** 실측 후 확장 우선, 전제 깨면 신설 — **단 선택 사유를 구현 «전에» 이 §8에 확정 각주로 남긴다**(D-NAO-215 절차 이탈 재발 방지) · **Q4** 검산 정본은 M2-d(S2-①), M3 몫은 API 응답 층 실재(②) + M2-d 산출물 좌표 인용(③) · **Q5** `gave_score.py:60-63` 기존 모양 재사용 `min((ROAS/BEP)^γ,1)×revenue`(재사용 불가 판명 시 멈추고 §8 경로로) · **Q6** 두 축 모두(`proposal_scoreboard` outcome + `_outcome_direction` 승격 게이트), 커밋은 M3-b 안에서 가른다.
> ★확정은 **처분**이지 합격기준 변경이 아니다 — §4 문언은 무변경이다.

**Q1. ★기존 150건 `outcome`의 소급 재채점 여부** (인계가 §8에 넣으라고 지정한 미결 1건)
- **사실(양쪽 다)**: `run_daily`의 대상 필터(`proposal_scoreboard.py:112-116`)는 `outcome IS NULL`을 포함한다 ⇒ ⓐ**소급하지 않으면** 이미 값이 박힌 150건(improved 4·neutral 57·declined 89)과 특히 `outcome='success'` 2행(**id 974·975** — `verify_date`도 NULL이라 필터를 이중 위반, «채점 옵트아웃»으로 작동)은 **어떤 새 채점식으로도 영원히 재채점되지 않는다**. ⓑ**소급하면** 과거 기록이 바뀐다 — 교훈 **#274**(`backfill_outcomes` 강제 실행이 `cost:0`을 영구히 굳힌 사고)가 정확히 그 자리에 붙어 있고, 원 값을 덮으면 「교정 전 채점기가 무엇을 찍었나」라는 증거 자체가 사라진다(ref 90 §2-2의 표가 재현 불가가 된다).
→ **추천: 소급 UPDATE 하지 않는다 — 새 식의 산출은 «별도 컬럼(또는 별도 원장)»에 기록해 원 `outcome`을 불변 증거로 보존하고, id 974·975는 「옵트아웃 상태」라는 사실 라벨만 성적표 산출에 병기한다.** 두 뿔을 다 피하는 유일한 배치다: 과거 기록 불변(#274 존중) + 150건·2행 모두 새 식의 사정권 안. 채택 시 M3-b가 집행하고, 기각(소급 UPDATE) 시엔 실행 전 백업 스냅샷 좌표를 판정문에 남기는 것을 조건으로 한다.

**Q2. 승격 지혜가 0건(또는 07-27 1건이 유령)일 때 합격 ①의 처분** (P1 결과 분기 — 북극성이 「M3 착수 전 확인」으로 지정한 [미상])
- 선택지: ⓐ승격이 자연 발생할 때까지 대기(실집행 0이라 원리적으로 무기한 — 종결 불가 M이 된다) ⓑ관측 대상을 «승격 후보»까지 넓힘(기준 인하 — §3 금지 결) ⓒ①을 «판정불능(원료 부재)»으로 정직하게 명기하고 배선은 완성(다음 승격 발생 시 자동으로 성적 행이 붙는 상태를 코드·테스트로 증명), 종결은 부분달성 허용.
→ **추천: P1에서 07-27 승격 지혜 1건이 실재하면 그 1건으로 ①을 관측한다(원문이 «≥1건»이라 충족).** 부재하면 **ⓒ** — 기준을 낮추지 않고 원료 부재를 판정불능으로 남기는 것이 「미달은 유효한 종결 상태」 규율의 적용이다.

**Q3. 성적표 산출 저장 표면** — 신설 1개 vs 기존 표(retro/GAVE 산출 계열) 확장?
→ **추천: M3-p 착수 첫 실측 후 확장 우선, «다른 소비자의 전제를 깨는» 구조면 신설.** M2 Q3의 전례를 그대로 승계한다 — 원 추천은 확장 우선이었고, 실측(`forecast_scorer`가 grain 무관 백필)이 신설을 정당화해 Jino가 추인했다(D-NAO-215). 이번에는 **절차를 이탈하지 않는다**: 실측 결과와 선택 사유를 구현 «전에» 이 §8에 확정 각주로 남기고 간다.

> ---
> ## ★§8-Q3 확정 각주 — **확장**(신설 아님) · 구현 «전»에 남긴다 (2026-08-22 14:2x KST · 세션 `c7105dae` 「PAO 논의 34」 · M3-p 실측 근거)
>
> **확정: `naver_change_log` 확장.** 새 원장을 만들지 않는다.
>
> **근거 1 — 「다른 소비자의 전제를 깨는가」의 답이 «아니오»다(실측).** `naver_change_log.outcome` 소비처 전수 = **backend 14곳 + frontend 2곳**:
> `routers/naver_ad.py:550·583·1338`(응답 직렬화) · `routers/naver_ad.py:1150·1289` · `ad_external_change.py:119` · `bid_rank_curve.py:151·226` · `perf_campaign_harness.py:98·323` · `perf_today_harness.py:213` · `improvement_events.py:131`(전부 `outcome=='failed'` 필터) · `proposal_scoreboard.py:112-116`(SELECT 필터) · `:123`(**유일한 UPDATE 지점**) · `:153-154`(자기 롤업) / 프론트 `frontend/src/lib/api.ts`의 `NaverProposalExecuteResult.outcome`·`NaverChangeLogRow.outcome`(둘 다 `string`/`string|null` 느슨한 타입).
> ⇒ **전부 «기존 컬럼의 기존 값»에만 반응하고, 새 컬럼을 읽는 소비처는 0곳이다.** Q1이 확정한 「새 식 산출은 별도 컬럼」을 따르면 **추가(additive) 스키마 변경이라 어느 소비처도 구조적으로 깨지지 않는다.** D-NAO-215(M2-a `forecast_scorer`)에서 신설을 정당화했던 「grain 무관 백필이 다른 소비자의 전제를 깬다」 패턴은 **이번 목록에서 관측되지 않았다** ⇒ Q3 원문의 기본값(**확장 우선**)이 그대로 선다.
>
> **근거 2 — 신설의 이득이 없다.** 새 식의 산출은 «조치 1건»에 붙는 값이고 `naver_change_log`가 이미 그 grain의 유일한 원장이다. 별도 원장을 만들면 `change_log` 1:1 조인이 영구히 따라붙고, M3-a의 지혜 성적표가 조인을 **하나 더** 타야 한다(북극성 §5-3 ①의 문제의식이 정확히 *"지혜 id로 묶는 조인 하나가 없다"*인데, 신설은 그 조인을 하나 늘린다).
>
> **근거 3 — 원료가 이미 같은 행·같은 쿼리 안에 있다.** `_aggregate_entity_metrics`(`proposal_scoreboard.py:38-60`)가 `naver_ad_daily`에서 `clk`·`conv_direct_amt`·`conv_indirect_amt`를 걷는데 **`cost`만 없다** — 같은 `query(...)`에 컬럼 하나를 더하는 것이라 **추가 쿼리 0회**로 ROAS 원료가 확보된다.
>
> ### 확장 명세 (컬럼 4개 추가 — 삭제·타입 변경 0)
> | 컬럼 | 타입 | 이유 |
> |---|---|---|
> | `outcome_profit` | `String(12)` nullable | 새 식 판정(improved/declined/neutral). **기존 `outcome`은 불변**(Q1) — 두 컬럼이 «옛 자 / 새 자»로 나란히 남아 교정 전후 대조가 영구 보존된다 |
> | `gave_before` | `Float` nullable | 조치 «전» 창의 GAVE 점수 |
> | `gave_after` | `Float` nullable | 조치 «후» 창의 GAVE 점수 |
> | `bep_source` | `String(16)` nullable | `product_bep` / `account_default` / `unavailable` — **§4-B ⑥ 값 정확도 라벨의 원료**(상품BEP ↔ 계정 블렌디드 구분). ⚠️이 컬럼을 넣는다고 ⑥이 달성되는 것은 아니다 — ⑥의 판정면은 **성적표 산출물**이고 그건 M3-a 소관이다. 여기서는 «원료를 버리지 않는다»까지만 한다 |
>
> ⚠️`gave_before`/`gave_after`를 저장하는 이유(‖ `models.py:2750-2751`이 retro에서 *"재구성 가능하니 점수만 저장(최소 스키마)"*라 한 것과 **다른 선택**임을 밝힌다): retro는 렌즈(`cf_asof`·`bep_asof`)가 **같은 행에 얼어 있어** 점수를 되짚을 수 있지만, `naver_change_log`엔 렌즈 컬럼이 없다. 렌즈를 컬럼으로 4개 더 늘리는 대신 **산출 점수 2개만 컬럼으로 두고, 렌즈(`cost`·`bep`·`bep_source`·`gamma`·`cf`)는 기존 `actual_json`에 additive 키로 적는다** — `actual_json`은 이미 이 행의 관측 기록이고 스키마 변경이 아니다.
>
> ### 재사용 경로 (Q5 「기존 모양 재사용」의 구체 좌표 — 전부 실재 확인)
> - 식: `gave_score.compute_gave_score(revenue, cost, bep_roas, gamma)` → `{score, penalty, roas, roas_ratio, gamma}` (`gave_score.py:21-71`, 페널티 `:60-61`, 점수 `:63`)
> - γ: `NaverCampaignSettings.gamma` → 없으면 `gave_score.DEFAULT_GAMMA=1` — **`retro_scorer._gamma_for(:82-92)`의 캐시 패턴을 그대로 쓴다**
> - BEP: `campaign_target_resolver.weighted_product_value_for_campaign(db, campaign_id, NaverProductBep.bep_roas)`(`:137-141`) → None이면 `account_default_bep_roas(db)`(`:169-176`). **이 폴백의 어느 쪽을 탔는지가 `bep_source`다**
> - cf: `diagnosis.correction_factor(db, date_to)["factor"]` — `retro_snapshotter.py:57`이 `cf_asof`를 얻는 바로 그 경로
> - 문턱: **기존 `IMPROVED_RATIO=1.1` / `DECLINED_RATIO=0.9` 재사용**(`proposal_scoreboard.py:33-34`). ★**새 문턱을 만들지 않는다**(§3) — 바뀌는 것은 «문턱»이 아니라 «재는 양»이다(RPC 배율 → GAVE 배율)
>
> ### ★실측이 계약의 서술 하나를 정정한다 — 「`gave_score`가 정지 중」은 부정확하다
> §2-3·§8-Q5·북극성 부록 A ⑤가 *"이미 코드에 있고 «정지 중»"*이라 적었으나, prod 실측은 **매일 계산·저장되고 있음**을 보인다: `naver_retro_signal` 38,841행 중 `gave_score_d3` **28,964** · `gave_score_d7` **28,605** non-null, `MAX(scored_d3_at)=2026-08-22 08:30:10` · `MAX(scored_d7_at)=2026-08-22 08:30:13`(08:30 크론 `retro_scoring_loop`). 롤업 `naver_learning_state(scope='retro_board')`도 board 6종 전건 실재.
> **정확한 문언**: 멈춰 있는 것은 «계산»이 아니라 **«입찰 반영»**이다 — `bid_simulator.py`는 `gave_score`를 import하지 않는다(import 전수 5파일: `expansion_pressure`·`expansion_allocator`·`auto_operator`·`retro_scorer`·`proposal_pipeline`). GAVE `score`를 실제로 소비하는 곳은 `proposal_pipeline.py:595`(성장군 제안 «정렬»)뿐이고 나머지 3곳은 `roas_ratio`만 쓴다.
> ⇒ **Q5의 재사용은 전제보다 안전하다**(살아 있는 함수 + 테스트 `test_gave_score.py` 17개 · `test_naver_retro_gave.py` 7개). 계약 문언은 고치지 않는다 — 합격기준이 아니라 «배경 서술»의 부정확이고, 정정은 이 각주로 남긴다.
>
> ### ★부수 정정 — 「호출부 6곳」은 실측 **5곳**
> 북극성 부록 A ⑤(`docs/references/82_pao_north_star_20260819.md:244`)와 ref 65(`:191`)가 *"호출부 6곳"*이라 적었으나 **좌표 목록은 어느 문서에도 없다.** 전수 grep 실측 = 외부 호출 **5곳**(`retro_scorer.py:124` · `proposal_pipeline.py:595` · `expansion_pressure.py:166` · `expansion_allocator.py:300` · `auto_operator.py:2334`) · import 파일 **5개**. `proposal_writer.py`는 **docstring에서만** 언급하고 실제 import·호출이 없다(6번째의 유력 후보이나 확정 불가). ⇒ **개수 불일치만 확정, 6번째의 정체는 판정불능**(M2-b2의 「문서 개수 ≠ 실측」 패턴 재발).
>
> ### 두 번째 축(ⓑ)에서 실제로 무엇이 틀렸는지 — 실측으로 확정
> `wisdom_candidates._outcome_direction`(`:70-88`)의 `good = roas_c >= float(target)`에서 `target`은 `campaign_target_resolver.resolve_target_roas`가 준 값인데, **`models.py:2286`이 `target_roas = bep_roas × 공격성 배수`라고 정의한다** — 즉 `target ≥ bep`다.
> ⇒ **현행 게이트는 «BEP를 넘겨 실제로 총이익을 낸» 조치를 `bad`로 센다**(bep ≤ roas_c < target 구간). 그 구간이 정확히 D-NAO-59 원문 *"Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우"*가 사는 자리다. **교정 = 게이트의 기준자를 `target_roas` → `bep_roas`로 바꾸고, 크기(절대액)를 GAVE 점수로 병기**한다. 원료는 이미 창 안에 있다 — `_window_metrics`(`diary_outcome.py:230-238`)가 `{cost, clk, conv, roas_c}`를 주고 `roas_c=(conv/cost)×cf`이므로 **보정 매출 = `roas_c × cost`로 정확히 복원**된다(추가 조회 0).

**Q4. 합격 ②③(A#8 라벨·항등식)과 M2-d(S2-ⓐ·S2-①)의 경계** — 같은 라벨 배선과 같은 항등식 문언이 두 M에 걸쳐 있다(§4-A [⚠️애매] 2건).
→ **추천: 배선·항등식 «검산»의 정본은 M2-d(S2-①)로 두고, M3 몫은 «API 응답 층 실재»(②)의 추가 관측 + M2-d 검산 산출물의 좌표 인용(③)으로 한정한다.** 같은 검산 1회를 두 M의 달성 증거로 이중 계상하지 않게 판정문에 정본 좌표를 병기한다. M2-d가 미완이면 M3-c는 판정 대기(M3-a·b의 착수·종결은 막지 않는다 — 그 경우 M3-z는 ②③을 «M2-d 대기»로 명기하고 부분 종결).

**Q5. 새 채점식의 형태** — 무엇으로 교정하나?
→ **추천: `gave_score.py:60-63`의 기존 모양 재사용 — `min((ROAS/BEP)^γ, 1) × revenue`(효율은 페널티로, 크기는 절대액으로).** 근거: ref 90 §4-4 ② *"올바른 목적함수가 이미 코드에 있다 — 그런데 그게 정지 중이다"* + §7 사실 3 + §2-3(북극성 부록 A ⑤가 «배선·정지중»으로 분류). 새 상수·새 문턱 발명 금지(§3)와 정합하는 유일한 기본값이다. 구현 실측에서 재사용 불가(grain 불일치 등)가 나오면 멈추고 이 §8 경로로 올린다.

> ---
> ## ★★§8-Q5 재확정 — **D-NAO-225: 판정식은 «GAVE 배율»이 아니라 «총이익 델타»다**
> (2026-08-22 15:0x KST · 세션 `c7105dae` 「PAO 논의 34」 · Jino 확정 — 구현 실측이 Q5 초기 확정값을 반증)
>
> **Q5 본문이 예고한 분기가 발동했다**: *"구현 실측에서 재사용 불가(grain 불일치 등)가 나오면 멈추고 이 §8 경로로 올린다."* 멈추고 올렸고, Jino가 «총이익 델타»로 확정했다.
>
> ### 무엇이 반증됐나
> `GAVE = min{(roas/bep)^γ, 1} × 매출`에는 **비용을 빼는 항이 없다.** 성장 후보를 «정렬»할 때는 맞지만(그게 원래 용도다 — `proposal_pipeline.py:595`), **비용이 함께 변하는 «전/후 판정»에서는 적자 대상의 지출을 줄인 조치를 「매출이 줄었다」는 이유로 악화로 읽는다.**
>
> ref 90 §2-2의 정본 4건(id 221·222·761·942)을 prod 실수치로 재계산했다(읽기 전용. 재계산한 `clk`·`conv_amt`가 원장 `actual_json` **4건 전건과 정확히 일치** ⇒ 창·grain 재현 검증됨):
>
> | id | 매출 | 비용 | 총이익(BEP 3) | 총이익 방향 | GAVE 배율 판정 |
> |---|---|---|---|---|---|
> | 221·222 | 178,900 → 138,200 | 122,240 → 88,002 | −62,607 → **−41,935** | **증가** | `declined` ❌ |
> | 761 | 174,900 → 90,500 | 122,426 → 43,310 | −64,126 → **−13,143** | **증가** | `declined` ❌ |
> | 942 | 59,400 → 50,400 | 90,627 → 16,930 | −70,827 → **−130** | **증가** | `improved` ✅ |
>
> **4건 전부 총이익은 증가**했는데(BEP 2·3·5 전 구간 동일) GAVE 배율은 **3건을 `declined`로** 찍었다. ⇒ 재사용 불가.
>
> ### 확정된 식
> `총이익 = (cf 보정 매출 / bep_roas) − 비용` 의 **전/후 부호 비교**. `bep_roas`는 본전 ROAS(공헌이익률의 역수)라 `매출/BEP`가 그 매출이 낳은 공헌이익이고, 광고비를 빼면 총이익이다 — **D-NAO-59가 최대화하라고 한 그 양 자체**다.
> - **GAVE 점수는 «크기» 축으로 계속 저장한다**(`gave_before`·`gave_after`) — Q5의 「기존 모양 재사용」 지시는 그 형태로 살아 있고, M3-a 성적표가 롤업할 때 «얼마나 큰 건이었나»를 준다.
> - ★**새 문턱을 만들지 않았다**(§3): 기존 `IMPROVED_RATIO`/`DECLINED_RATIO`(±10% 배율 밴드)는 **부호 있는 양에 옮길 수 없다** — 「−70,827 → −130」의 배율은 0.002배지만 실제로는 7만원 개선이다. 밴드를 억지로 이식하는 대신 **부호 비교만** 하고, 노이즈 방어는 이미 있는 모수게이트(양쪽 창 `clk ≥ 10`)에 맡긴다.
> - ⚠️**알려진 한계**: 아주 작은 델타도 판정이 된다. 이 축은 «표시 + 자기 롤업»에만 흐르고 조작 경로가 없어(ref 90 §2-3) 지금은 무해하지만, **M3-a가 이 값을 롤업할 때 델타 «크기»를 함께 봐야 한다**(그 크기가 `gave_*`와 `actual_json.lens`다).
>
> ### ★§4-B ④ 검산 결과 — 채점된 150건 전수 (prod 읽기 전용, 2026-08-22)
> 정본 4건만으로는 **옛 자와 새 자가 갈리는지 보이지 않는다**(4건 다 양쪽이 `improved`). 그래서 채점 완료된 150건 전수로 혼동행렬을 냈다. 창·grain 재현은 위 4건 일치로 검증된 것과 같은 경로다.
>
> **BEP 3 · γ=1 · cf=1 기준 (모수 미달 0건)**
>
> | 옛 자 ＼ 새 자 | improved | declined |
> |---|---|---|
> | improved (4) | **4** | 0 |
> | declined (89) | **17** | 72 |
> | neutral (57) | **27** | 30 |
>
> **일치 76/150 (50.7%) — 74건(49.3%)이 뒤집힌다.** BEP 2에서 62건, BEP 5에서 76건으로 어느 렌즈에서도 절반 안팎이 뒤집힌다.
> ⇒ 「옛 자가 대체로 맞는데 4건만 이상했다」가 **아니다.** 특히 옛 자가 `neutral`로 흘려보낸 57건 중 **27건이 실제로는 총이익 개선**이었다 — 성적표가 그동안 이 27건을 「아무 일도 없었다」로 롤업하고 있었다.
>
> ### ★④의 검산 문언 «매출 절대액 감소인데 개선 = 0건»에 대한 처분 (Jino 확정 2026-08-22)
> **문언은 고치지 않는다. 「전제가 틀렸다」를 기록으로 남긴다.**
> 그 문언은 「매출 감소 = 나쁨」을 전제하는데, **D-NAO-59의 목적함수는 매출이 아니라 총이익**이다. 적자 대상의 지출을 줄이면 매출은 줄고 총이익은 는다 — id 942가 정확히 그것이고(−70,827 → −130), 150건 전수에서도 **「매출 감소인데 개선」이 33건**이다. ⇒ **어느 판정식으로도 0건이 될 수 없고, 0건이 되어서도 안 된다.**
> ⇒ 이 항목은 **문언 대비 미달**로 판정하고(기준 인하 금지, 전역 §2), 미달의 사유가 「채점기가 틀려서」가 아니라 **「검산 문언 자체가 목적함수와 어긋나서」**임을 판정문에 병기한다. 문언 수정은 새 계약 사안이다(§1 승인 지점 ①).

**Q6. 채점기 교정의 범위** — ⓐ`proposal_scoreboard`(outcome 축)만인가, ⓑ지혜 승격 게이트(`_outcome_direction`, `ops_diary_entries.outcome_json` 축)까지인가? D-NAO-223의 문언(「채점기 교정」)은 축을 특정하지 않는다 — **[⚠️애매]**.
→ **추천: 두 축 모두.** 근거: ref 90 §7 사실 1 — *"성적표가 원료로 삼을 판정값 두 축(§2 `naver_change_log.outcome` · §3 `_outcome_direction`)이 둘 다 효율 기반"* + 사실 2 — 승격 게이트(ROAS)와 M5 성공 정의(총이익)가 다른 것을 잰다. ⓐ만 고치면 성적표의 «입구»(어떤 지혜가 승격되나)가 여전히 효율로 걸러져, M5 합격기준(「지혜→총이익 기여」 양수)과의 어긋남이 남는다. 단 두 축은 **다른 테이블·다른 경로**임을 구현이 혼동하지 않게 슬라이스 M3-b 안에서 커밋을 가른다.

---

*작성: Fable (2026-08-22 KST, 구조 산출물 저자 슬롯). 입력: 북극성 ref 82(§5-3·§6·§6-b·§7·§8 [미상] 표) · ref 91 §7-1(M3-a~e)·§2-3·§8 · ref 90 전문(§2·§3·§4 2026-08-22 재검증판·§7·§8-A) · HANDOFF_ref90-coordinate-census_20260822 §3·§6 ③·§8 · 트랙 계약 헤더(D-NAO-59 원문·M3 체크박스 문언) · M2 계약(형식 본보기·Q3/Q7 전례). 코드 좌표 검증: `proposal_scoreboard.py:112-116`(필터 4조건) · `gave_score.py`(penalty×revenue) · `wisdom_candidates.py:70-89`(`_outcome_direction`) · `models.py:4051`(`ops_wisdom_entries`) · 성적표 API 복수 실재(`routers/naver_ad.py`) — 전부 이번 세션 로컬 파일에서 직접 재확인(2026-08-22). prod 조회는 하지 않았다 — [미상]은 §4-0이 잰다. 이 파일 1개 외 무변경 — 코드·트랙·매트릭스·prod 무접촉.*
