# TRACKS — 활성/완료 메가 프로젝트 인덱스

## 🟢 Active
- [PAO UI/UX](tracks/active/track_pao-ui-ux.md) — **0/8 · 2026-08-29 생성**. Jino *"이 작업은 PAO의 UI/UX만 다루는 트랙으로 독립운영할꺼야"*. 엔진의 판단을 만들지 않고 **엔진의 사실을 보이게** 하는 트랙 — 엔진 로직·스키마·수집은 전부 `track_naver-ad-optimization.md` 소관이다. 첫 계약 = **성과분리 목표**(`docs/contracts/CONTRACT_pao_performance_ownership_split.md`, 승인 2026-08-29 21:35): 성과 화면 `/naver-ad/performance`가 소유권 조건 없이 전 캠페인을 한 덩어리로 합산하던 것(`metrics_aggregator.py:79-83`)을 **전체 / PAO가 돌린 광고 / 안 돌린 광고 + 전환일·모름** 4밴드로 가른다. ★**「PAO가 돌린다」는 세 축의 ∧**이다 — `optimizer='ours'`(D-NAO-13 제안 대상) ∧ `auto_operate`(D-NAO-49 마스터) ∧ 광고그룹 진리표(D-NAO-244). 진리표만 보면 **07-30 10:48~08-29 12:53 한 달이 통째로 오답**이 된다(그 구간은 `auto_operate` ON인데 `optimizer='none'`이었다). ★**시간 기준은 「방법 B — 날짜별 실제 담당」**(Jino 선택) — 현재 관할을 과거에 소급하지 않는다. 실측 차이가 11배였다: 최근 30일 PAO 밴드가 현재 스코프 소급이면 2,170,514원(11.1%)인데 **당시 관할로는 0원**이고, 현재 스코프는 2026-08-29 00:25에 생긴 것이었다. 재구성 원천은 `naver_change_log`(`changed_at`=KST, 2026-07-11~, 관할 액션 22건). 이력 밖·해석불가·장중 전환일은 **0으로 뭉개지 않고 별도 밴드**로 두고, 「전체 = 관할+비관할+전환일+모름」 항등식이 화면에 상시 뜬다. 라이브 검증 좌표: 30일 PAO **0원**·비관할 19,007,252원·전환일 472,580원/2일 · 90일 PAO **522,960원/1일**(전부 2026-07-29 = 「캠페인 통째로 가져온」 실사례)·모름 **25,633,242원(44.6%)**. ★**적대 리뷰 1R FAIL(P1 4건) 전건 수리** — ①로그 `after_value`와 재구성이 어긋나면 모름으로 내린다(`auto_operate`엔 앱 writer가 없어 기록 안 된 변경이 실재한다. 실측 모순 **2건**이 이 검사로만 드러났고, 그전엔 `unparsable`도 `unknown`도 아닌 **확신에 찬 오답**이라 화면이 완벽히 깨끗해 보였다 — 90일 PAO가 610,964원으로 부풀어 있었다) ②「N일」 창 기준점이 오늘이라 확정 N−1일만 세어 07-30이 말없이 빠지던 것 ③「모름」·「전환일」 캠페인이 비PAO 필터에 담기던 것 ④**표면 렌더 테스트 부재** — 섹션 렌더 줄을 통째로 지워도 프론트 1,196건이 초록이라 표면 절단 변이 6종이 전건 생존했다(그중 둘은 계약 §6이 리뷰어용 예시로 지목한 바로 그 변이다).
- [원가 정본 원장 (cost-truth-ledger)](tracks/active/track_cost-truth-ledger.md) — **2/6 · 2026-08-22 생성**. Jino *"SellC에 원가 메뉴를 하나 붙이자 … 목표는 정확한 원가 산출이니까?"*. 원가가 지금 **Jino의 Drive 엑셀 1장**에 살고 사람 손으로 `product_master.cost_price`에 옮겨진다(그 경로에서 08-07 태블릿 36건 오기입 · 08-10 71건 역전-원복). 층 구조 **①표준원가(계약 A′) ②실제원가(계약 B) ③판매시점 원가(계약 C)**, 순서는 **B → A′ → C**(B가 기존 `cost_price` 경로를 한 줄도 안 건드리는 순수 추가라 위험이 최저). ✅**B 완료·prod 가동**(D-CPP-48·49·50): 수입건 원장 5테이블 + CI/PL/통관경비서 파서 + 검산 3종 + 배부 SA + `/api/import-cost/*` + 화면 `/import-cost`. 8/18 실건 `Glass_Ip17Pro` **3,201원(VAT포함)/2,910원(제외)** · cleaning kits 2,400개 `line_type='material'` 배부 **54,992원** 라이브. **D-CPP-50 = 관세는 «배부»가 아니라 «귀속»**(품목별 세율 — 부자재 0%·유리/필름 5.6%, 금액 일괄배부는 무관세 품목에 관세를 떠안겨 유리 개당 −134.86원). ★**ref 92(8개월 14 수입건 전수 실측)가 A′ 초안의 전제를 반증** — 「부자재 구성 × **공통 입력**」에서 «구성»은 유효하나 «공통 입력»은 공통이 아니라 **로트별**이었다(환율 207.51~226.66 평균 217.61로 엑셀 200 대비 **+8.8%** · 물류비율 5.0~16.3%로 엑셀 10% 대비 **3배 차** · 엑셀 표준원가 2,820.64 고정 vs 실제 landed 2,800.10~3,291.70, 평균 **+7.3% 과소**). ⇒ A′는 「어느 로트 단가를 표준으로 쓰는가」(층3-lite)를 선행 결정해야 한다. **2026-08-22 21:1x A′ 착수**(세션 `bd4adba0`) — Jino ★*"우리가 강화유리 정보에서 읽는 수입서류에 나와있는 부자제 단가를 참고해"* ⇒ 단가 입력은 엑셀이 아니라 계약 B 원장. 미달 4항목: A1(국내 조립형 원가가 구성 계산으로 prod 관측) · A2(플립/폴드·태블릿 이식, `cost_drift` 악화 0) · C1(재고 로트 원장) · C2(평가방법 Jino 승인 + `cost_price` 컷오버). ⚠️**AI_office에 수입원가 자산이 이미 있다**(`track_import_cost.md`·`import_payable_ledger`·`compute_landed_cost`·`hs_reference`·ECOUNT 기표 harness 2종) — SellC 원장과 데이터 흐름 0건, **Jino 결정 전엔 어느 쪽도 상대를 전제하지 않는다**. 계약 정본: `docs/PLAN_import-cost-ledger.md`(B) · `docs/PLAN_cost-table-systemization.md`(A′ 초안, 부활·개정 대상) · ref `docs/references/92_import_ledger_8months_census_20260822.md`.
- [쿠팡 손익 정합 (2계정 4채널)](../.claude/memory/HANDOFF_rocket-1p-axes+promo-file-ingest_20260805.md) — **2026-08-04 착수 · 계약 승인됨(08-05 00:03)**. 목적=스마트스토어처럼 쿠팡 숫자를 정산 원장과 맞추기. ★**대시보드가 회사 매출의 34.5%만 보여준다** — RG1 2,170만원이 매출 0인데 비용 1,736만원만 들어가 오픽스가 −1,734만원, 로켓1P는 `channel_type='consignment'`라 **행 자체가 없다**. ★**1P 매출 축 확정(2026-08-05)**: 발주액은 정산 대비 **+24.7% 과대**, 정본은 세금계산서(A·sell-in) + 운영 주지표는 판매수량×납품단가(B·sell-through) 둘 다 쓴다(부가가치세법 제15조·아마존 1P 실무로 검증). gap=쿠팡 재고 증감. ★계산서↔발주 브리지가 `vendor_payment_seqs`로 **이미 수집돼 있었다**. 구조=채널별 평행 엔진+대시보드 합산. **★2026-08-05 D-14(`tracks/active/track_coupang-rocket-1p.md`, ref `docs/references/44_rocket_1p_ledger_parity_20260805.md`): 로켓1P 원장 대조 완료** — "값은 틀린 적이 없다, 틀린 건 완결성과 귀속이다". 계산서 백필 148→**486건**(최초일 2025-07-23), 재대조 486건 전 필드 완전일치. 계산서↔발주 브리지 294덩어리 중 286(97.3%) 일치(불일치 대부분=역발행 차감, 쿠팡 문의 필요). **판매분석 페이지네이션 유실 수정**(커밋 `10cd8a5`, `sortBy` 불안정 정렬이 원인) — 13일 표본 617/623→623/623. **로켓1P leaf 종합조망 배선**(커밋 `8948fdc`, prod) — 매출=계산서 지급액, 라이브 07-05~08-03 원 단위 일치. 브랜치 `claude/rocket-1p-parity-fixes` push·미병합(단, leaf 배선 `8948fdc`는 **PR #199로 main 병합됨** — 리뷰 없이 병합, PR 본문에 명기). 미결=**C축 원가 오매핑 GMV 31.74%**(Jino 몫, 교훈 #117 금지선)·Z폴드 3종 원가 매핑(Jino)·역발행 차감 7건 내역(쿠팡 문의)·**오픽스 RG 매출 배선(합격기준①) 미착수**·PR 미생성·codex 부채(08-09 리셋 후). **★2026-08-05 오후~저녁 D-15(같은 트랙 파일)**: **매출 축 토글**(`rocket_basis=settlement|sales`, 커밋 `2c4a88d`, prod) — 라이브 계산서 1,578,000 ↔ 판매 3,885,820 전환 확인·원가 커버리지 27.3%(임계 95% 미달이라 순이익 "—" 유지), 분담금 원천 테이블 부재 시 0이 아니라 "모름"으로 방어. **성능**(커밋 `82ec4f8`) 축 토글 63.7초→0.1초(5분 쿨다운, 범인=조회마다의 외부 API 재동기화). **프론트 레이스 2건 수정**(세대 카운터 `265c5a5` + `syncAndRefresh` ref화) — 라벨은 「판매」인데 값은 계산서인 사고 차단. **ASN(발송) 정찰**(ref 45, 코드 0) — 미수금 실측 **~~14,001,320원~~ → 정정 8,033,970원**(ref 45 §12 — 입고 원장 재판정, ASN 화면 기준이 재발송 입고 52라인을 놓쳐 5,763,290원 과대) — 확정 159라인·729개, 1차 청구 권장=하차 확인 130라인 **7,108,720원**, D-14의 미해명 −459,000원을 **쿠팡 반출 3건으로 해소**, 사슬 정합 원 단위로 닫힘, 직원용 문의 자료(A4 1장+CSV+내역서/라벨 208장) 작성. 테스트 4,790 통과. **★2026-08-05 D-19(인프라 수리)**: sellc prod 간헐 502(08-03~05, 재시작 61회=502 구멍 61개)의 정체=pm2 콜드부팅 공백 규명 → 블루-그린 무중단 배포 기본화(`scripts/zero_downtime_restart.sh`, 전환 3회 7~10초·프로브 62/62 200) + 쿠팡 광고 설정 push 500(autoflush 교차 중복) SAVEPOINT 흡수 수정. 교훈 #142·#143. **★2026-08-05 저녁~심야 세션(PR #200·#201·#202·#206·#207·#208 병합)**: **1P 잔여 부채 정리 완결** — 월말 역발행 차감=정상 정산(D-16, 문의 대상 아님) · 수집 위치=Jino Mac 버튼-only 유지·VM 이전 기각(D-17) · 병행 세션과의 D-N 번호 충돌(prod↔main 이산) 해소 · ASN(발송) 상시 수집+백필(D-18, PO 커버리지 4.1%→97.8%, 954건/9,565라인/1,512박스) · 트럭 사각 실측 0건(오하이테크 트럭 미사용) 확정. **조사 4건 판정 완료**: 미확정 149,056,185원=우리가 납품불가로 확인한 물량(청구·미수금 대상 아님) · 2025-09 집중=사건 아니라 물량 집중의 그림자(규모 효과, 미입고율은 1.27%로 3위일 뿐) · 07-28 미입고 678개=확정 미수금 0(95개는 화면 착시·583개는 진행분) · 2026-06 미입고율 2.65%→**이천4 센터 7.82%**(2위의 5배, 전체 미수금의 19.5%, 78%가 입고 자체 통째 누락=만성)로 범인 특정. **★D-19 신규 발견**: 1P 광고비가 2025-07-23~2026-03-16 약 8개월간 통째로 0으로 잡혀 있던 결손 **350,366,257원** 확인(ref `docs/references/46_rocket_1p_ad_cost_gap_20260805.md`) — Jino 제안대로 옵션ID로 판별(Billboard XLSX 「판매방식」+광고집행 옵션ID, Retail 97.7%·3P 2.3%·RG 0)해 계정 총액 중 3P를 뺀 **B안(Retail만 적재) 채택**, 소급 수집 가능(`report/SALES` 2024-08-03까지 응답)하나 **적재는 다음 세션**. 인계 `.claude/memory/HANDOFF_1p-debt-closed+ad-cost-gap_20260805.md`. 현재 미충족: **오픽스 RG −17,342,298원(계약 합격기준①)** — 다음 착수 전 갈림길 2개(RG를 별도 leaf로 뺄지/오픽스 행에 합칠지, 정산비용 귀속 일할 배분 여부) Jino 답변 필요. **★2026-08-06 심야 D-21(광고비 B안 적재 완료, ref 46 §5-1)**: 위 광고비 결정은 병행 세션 충돌로 **D-19→D-20 재번호**됨. B안 집행 완료 — 2025-07-24~2026-03-16 **236일 331,939,056원** 적재, 라이브 9개월 `ad_spend` 원 단위 일치. ★**결손 실액 정정**: 350,366,257원은 계정 총액이었고 판별하면 **1P 331,939,056 / 3P 49,677,949 / 비-PA 4,527,079**. ★**2025-07은 광고비의 96%가 3P**(A안이었다면 3P 4천만원이 1P에 얹혔다) — 1P 첫날은 07-23이 아니라 **07-24**. ★**ref 46 §5의 "95~98%" 검산 기준은 틀렸다**(전환월 2025-07을 정상인데 실패 판정) → **완결성 검산**(Billboard 전체 ≈ report/SALES PA, 실측 100.01~100.02%)으로 교체, 교훈 #144. ⚠️미해결 2건(Jino 결정): 비-PA 4,527,079원(1.17%, Billboard가 PA 전용이라 미포함 — 라이브 구간은 ALL 기준이라 축이 섞임) · 옵션ID별 소급 미수집(`rocket-overview` 옵션 대사가 소급 구간에서 −100%로 뜨는 건 결함 아닌 미수집). **★2026-08-20 D-22(광고비가 순이익에 닿는다, PR #313)**: Jino가 대시보드 스크린샷과 함께 *"여기에 숫자들이 잘 안맞는거 같은데, 숫자를 좀 맞춰줘"*. prod 실측(08-18): 회사 소계·전체 순이익이 자식(로켓1P)의 광고비는 흡수하면서 `net_profit=None`인 자식은 `_add_net()`이 통째로 건너뛰어 **손익은 안 흡수**했다 — 오하이테크 186,825원(실제 −411,063) · 전체 569,635원(실제 −28,253) · RoAS 164%(실제 319%)로 오염. 원인은 `profit_calculator.py` `group_summary_by_company`. Jino 결정: *"3안처럼 2가지 옵션을 보여주자. 한 옵션은 계산서축, 다른 한 옵션은 우리 손익 (납품가 축)으로. 여기에 광고비는 비용으로 나간거기 때문에 계산서축에서도 적용이 되어야 해"* / 원가 미상 매출은 *"행 단위 경고 표시"*. 한 것 5가지: ①손익 못 재는 행도 `net=-광고비` 하한(`net_scope="ad_only"`), 판정은 **`net_contribution()` 한 곳**(적대 리뷰 1R P1이 같은 결함이 `calculate_channel_summary` 수동매출-only 분기에도 남아 있음을 잡음) ②이익률 분모를 행이 정함(`net_basis_revenue`) ③KPI 카드가 요약표와 **같은 경로**(`_channel_rows`) — 종전엔 카드만 `orders` 축이라 로켓1P를 통째로 뺐음 ④`unmapped_revenue` leaf·회사·전체 전파(자사몰 이익률 67.4%의 정체=원가 0인 「개인결제창」 180,000원) ⑤축 토글 「판매(납품가)」+계산서 축 하한 자백 뱃지. 적대 리뷰 1R FAIL(P1 2건)→2R PASS — P1-2(뱃지 typeof 버그)는 `parseList()`가 이미 number로 바꾼 뒤라 **반증**됐으나 방어 코드는 채택. 테스트 backend 5,833·frontend 471 passed. 배포 백엔드 무중단(commit `64b0d705`)·프론트(commit `844e0314`), prod 22:07:01Z/22:08:59Z. **완료 QA 달성**(ⓐ~ⓕ 전항목 라이브 대조 일치, 「안 함」 위반 0건). PR #313(`749686f0`→`236d1ee8`→`64b0d705`→`844e0314`). ⚠️**이월**: 프론트 배포 CAS가 **미병합 PR #312**(D-NAO-207) 위에서 배포된 상태를 발견 — 규칙대로 그 브랜치를 병합·재빌드해 올렸고, **main에 없는 것이 prod에 살아 있어** PR #312를 먼저 병합하는 게 순서상 맞다. 그 외: `/kpi`·`/channel-breakdown` 병렬 호출의 동기화 세대 쿨다운 락 미완비 · 음수 광고비 실사례 미확인(가드는 넣음) · 자사몰 「개인결제창」 원가 연결(Jino 몫). 부수 확정: CAFE24 배송비 수입 0은 정상(무료배송 설계), 08-18 shipping_cost 9,500원은 매출이 아니라 비용.
- [쿠팡 프로모션 손익 레이어](tracks/active/track_coupang-promo-pnl.md) — **2026-07-28 생성 · Phase 1(수집 신설) 진행 중**. 셀러 부담 즉시할인 프로모션 기간의 진짜 손익을 채널별(1P 로켓배송 / 2P RG)로 계산하기 위한 사실 수집. 확정: D-CPP-1 자동수집 우선 · D-CPP-2 1P 매출=납품가 축(쿠팡 자체 인하는 BEP 분자만) · D-CPP-3 RG 분담금 권위값=쿠폰 "사용 금액" · D-CPP-4 1P 분담금 청구 방식 미확정(9월 정산서 대사) · D-CPP-5 판매분석 BETA 접근불가 감지 필수 · **D-CPP-6 saleAmount↔seller_discount 상계는 표본 부재로 미확정**(prod 실측: seller_discount_coupon≠0 0건, RG 판매는 revenue-history에 부재). 백엔드 수집 골격 완료(테이블 3종·파서·ingest·테스트·원가시드), **수집기는 supplier 세션 만료로 정찰 미완 → 미착수**. 계획서 `docs/PLAN_coupang-promo-pnl-phase1.md`. **★2026-08-08 D-CPP-26(PR #258, `badab63`, prod 배포·라이브 검증 완료 08:0x~08:3x KST)**: 로켓1P 손익(납품가 축)에 **«근거 화면»** 신설 — 「우리 손익 (납품가 축)」 패널의 「근거 보기 ↗」 → `/rocket-1p/pnl-audit?from=&to=`. 화면 4단: 산술 검사 9종(판정 `pass`/`fail`/**`undetermined`** 셋, 모집단 0을 검사마다 별도 가드) → 손익 사다리 → 「날짜×옵션」 원자 목록(신뢰도 배지 6종) → 원자 1개의 다섯 갈래 원천 행. **라이브 합격**(창 2026-08-01~08-07): A1·A2·A3 차이 0.00/0.00/0E-12 · `ladder.net_profit` **1757901.20** == 화면 `pnl.net_profit` 동일(문자 그대로) · 배포 전후 기준선 불변 · **A7 fail은 설계대로**(옵션 광고비 320,394원이 어느 원자에도 귀속 안 됨을 검사가 실제로 드러낸 것) · B1·B2·B3 undetermined(원리적 한계·계산서 폴백·Billboard 차이 7,567원). 배포는 백엔드 무중단(다운타임 0초)·프론트 스탬프 `badab630`·DB 변경 없음. **리뷰**: codex 3렌즈 사용 한도 소진(`GATE: INCONCLUSIVE`, 리셋 08-09 16:16) → Opus 적대 리뷰 4라운드로 대체, **P1 13건 발견·전건 수정**, 변이 주입 43종(백엔드 25 전건 KILLED / 프론트 18 중 14 생존 → 테스트 보강 후 해소). 교훈 #179·#180·#181. codex 소급 리뷰 이월. 상세는 트랙 파일 D-CPP-26. **★2026-08-09 D-CPP-27(PR #260 병합, ref 52)**: 원가 링크의 «대상» 전수 감사 — `suggested` 172건을 결정적 브리지(ch5 `externalVendorSku` 코드 일치) × 제품 성격(부위·매수·소재·사생활·맥세이프+기종) 두 렌즈로 판정. **링크는 바꾸지 않았다**(교훈 #117 — 채택은 Jino). 결과(창 07-08~08-06): `replace` **4건 +69,126원** · `label_only` 12 · `agree` 38 · `no_bridge` 47(판정 불가) · `generic_vs_model` **64건 +60,223원** · `other` 7건 −8,868원. 불일치 87건·120,481.1원(★08-07 측정과 같은 값이나 **같은 조인의 다른 표면이라 교차검증 아님**, 교훈 #141). ★**「뭉침 64건」은 오류가 아니라 정책 질문** — 전부 `OHI-3D-TPU-MATTE`·`OHI-PRIV-FINGER-FILM-…` 두 SKU이고 ref 44 §4-4가 이미 「기종 무관 공통 원가 상품이라 다:1이 정상」으로 기록. 남는 건 링크가 아니라 값(공통 2,350.7 vs 기종별 2,616). ★**진짜 오류는 부위·매수 축** — 쿠팡 「액정 2매」에 「카메라 렌즈 1매」 원가(3건). ★**「금액 0」에 두 종류**(원가 동일 vs 창내 미판매=잠복): `OHI-0555` 단가차 **291%**·`22295497` **108%**인데 둘 다 0원. **리뷰**: 적대 2라운드 `GATE: PASS`(1R P1 3건 전건 수정 — 최대 건은 **브리지를 판매 창으로 잘라 19건을 「판정 불가」로 접고 교체 후보 1건을 숨긴 것**). 회귀 24건·변이 전건 KILLED. 판정기는 읽기 전용(prod 무변경). **Jino 결정 대기 4건**: 공통 원가 SKU 정책(약 +89,671원)·`replace` 4건 교체·`OHI-0555` 잠복·맥세이프 6건 실물 확인. 교훈 #183·#184·#185. 상세는 트랙 파일 D-CPP-27. **★2026-08-09 D-CPP-28(PR #262 병합 `bddf350`, prod 배포·라이브 합격)**: Jino *"광고비가 실제 광고비보다 적게 계산되고 있어"* — 맞았다. 옵션 광고 원장(Billboard)이 **네 통**으로 갈리는데 **두 통이 화면에 한 줄도 없었다**: 구멍1 판매O·손익밖(라이브 08-08 33,750원) · 구멍2 팔린 옵션의 «판매 없는 날»(창 07-31~08-06 435,916원, 근거화면 A7이 fail로만 고발하던 돈). ★샌 이유는 계산 실수가 아니라 **판정 그레인 불일치** — `pnl_ad`는 (날짜×옵션) 원자를, `ad_no_sales`는 (옵션)을 세어 그 사이로 돈이 샜다(교훈 #189). 신설 `ad_uncosted`·`ad_no_sales_days`·`ad_unattributed` + **항등식 «네 통 합 = ad_option_total»을 계약으로** 둔다. `basis='full'`이면 구멍2·3을 **둘 다** 차감(예전엔 3만 접어 종착 상태에 거짓말이 남았다), 구멍1은 **절대 차감 금지**(부분집합 매출에서 전량 비용을 빼면 적자로 위조). ★**A7의 성격이 바뀐다** — 「결손이 있는가」(상시 fail) → 「원장이 남김없이 갈렸는가」(pass), 결손 크기는 `ad_unattributed`가 상시로 말한다. **pass는 «결손 없음»이 아니다**(교훈 #190). ★**`ignored` 22건이 «결정»이 아니었다** — 전부 `note='no suggestion or low score'`·created_at 초 단위 = 2026-06-17 일괄매핑의 매칭 실패인데 화면은 "이미 결정했으니 그냥 두세요"라 안내했다(ref 47 §2). → `excluded_note`를 그대로 보이고 단정 제거(**파싱해 판정하지는 않는다**). **데이터**: 원가 링크 7건 연결(Jino 승인 — 6건 코드 다리 ch5 `externalVendorSku` 정확일치, D-CPP-27 판정기로 `conflicts_bridge=[]` 2차 확인 / 1건 이름 정확일치 워치 스트랩). **라이브 합격**(창 08-08): cost_coverage 0.9483→**1.0000** · basis→**full** · 사다리 광고비 664,449→**797,431원=원장 전액** · 네 통 합 원단위 일치(3창) · uncosted 0 · **A1~A7 전건 pass**(A1~A3 차이 0, full 창 포함). **리뷰** 적대 2라운드 `GATE: PASS` — **1R P1 2건이 둘 다 «합격기준이 달성되는 순간 터지는» 결함**(A1·A2가 full 창에서 fail 351,520원 / 각주가 사다리를 부정)이라 평상시엔 라이브로도 안 걸렸다. 생존 변이 6종 전부 «0인 값으로만 검사» 형태(교훈 #191). 부수: `next_ids.sh` 정규식 3번째 형식 수리 · ★`npx tsc --noEmit`은 이 repo에서 **아무것도 검사 안 한다**(files:[]+references) → `tsc -b`. **이월**: `ignored` status 3값화 스키마 · 나머지 15건 데이터(Jino "별도로 보자", 그중 6건은 다리 있음) · 일별 축은 «광고만 쓴 날»을 표현 못 함(기간 타일은 정확). 교훈 #189·#190·#191. 상세는 트랙 파일 D-CPP-28.
- [PAO (파오) — 네이버 SA 광고 최적화 엔진](tracks/active/track_naver-ad-optimization.md) — **★2026-08-26 밤 「점화준비 목표」 계약 초안 — Jino 승인 대기·착수 시점 미정**(`docs/contracts/CONTRACT_ignition_readiness.md`). 코드 0줄·prod 쓰기 0·광고계정 쓰기 0. 결손 11개를 S1~S6 슬라이스로 분할 + 자동 판단 규칙 7종 + 제외 등급 5종·SPECS 봉투 4종 확정. ★★**대행사 컷 재판정**: 전환이 있었는데 잘린 **16건이 RoAS 2.62(BEP 1.711의 1.53배)·13/16 BEP 초과·재개방 예정 0/16**(「오하이풀커버」 7.57 = 우리 브랜드명 포함). 산업 표준 통계기준(21클릭)을 충족하는 컷은 **419건 중 1건**뿐이고, 이력 없는 **3,571건(89.5%)은 검증이 원리적으로 불가**. 단 2건은 컷이 옳았다(0.46·1.31) ⇒ **우리 게이트의 유일한 구멍 = `conv≥1` 절대보호라 「전환 있는 적자」를 못 자름**. ★★**학습 가능한 파라미터(`guardrail_params.SPECS`)가 3개뿐이고 전부 브레이크** — 제외·탐색 게이트 숫자 9개는 하드코딩이라 학습이 못 닿는다 ⇒ **우리 시스템은 「조이는 법」만 배울 수 있게 돼 있고, D-NAO-85(ROAS +7%·매출 −52%)는 사고가 아니라 구조의 필연**. ★**탐색 3,834 시도 중 3,607(94.1%) 차단·기간 07-21~07-30 «10일»뿐·이후 27일 0건**, 차단 사유의 **52%(~1,890건)가 「소급채점 stale」** — 「나쁜 키워드」가 아니라 「채점이 최신이 아님」 ⇒ 닫힌 고리(탐색→채점→이력→탐색). ★쇼핑 전환0 비용의 **76%가 1–2클릭 구간**(1,684건 2,580,682원/14일)인데 현행 게이트는 **8%만 본다** · 파워링크 90일 **17,693,661원인데 전환 4종 전부 0 = 측정 원리적 불가** · 쇼핑 90일 RoAS **1.60 < BEP 1.711 — 이미 미달**. ★제외 원장 3,990행 중 **`next_review_at` NULL 3,987**·`cycle` 전건 1·probation/restored 0 ⇒ 재개방 상태기계가 99.9%에 미배선. 단 **제외 자체는 실효 있음**(이력 419건 중 413건이 제외 후 클릭 0). ★MOP 대조: *"레버가 전부 2주 실지출 이력 게이트 뒤 — 시동의 본질은 돈이 아니라 이력 쌓기"* · 우리 계정 실측 ROAS **1.80 = BEP 1.711의 +5%** ⇒ 벤치마크를 그대로 따라가면 총이익 ≈ 0. **MOP 게이트=「내가 아직 안 한다」 vs 우리 게이트=「너는 하면 안 된다」**. 인계 정정 2건: prod↔브랜치 소스 **md5 4/4 일치(「다른 소스」는 유령)** · 의미단위 화이트리스트는 **이미 채워져 있음**. 새 발견 2건: **재개방은 harness 우회라 execute 일기가 안 생겨 학습 사슬에 원리적으로 안 탄다** · **08-26 08:45 판사 크론 회차 통째 결손**(misfire catch-up 없음). 진행률 **2/7 불변** · ④자동화 **28세션째 0** · ⚠️학습 재료 **2026-10-28 만료**. 인계 `.claude/memory/HANDOFF_exclusion-learning-contract_20260826.md`. — **★2026-08-18 밤 D-NAO-198·199 — 매일 버리던 col7/8/9(시간대·지역·매체)를 적재했다.** prod 라이브 마진 **984,165행/171일(02-19~08-17)/307그룹** + 유료결합 33,733칸(마이그 `std1dim2axes3`, 배포 무중단, 완료 QA **달성**·미달 0). 적대 리뷰 PASS(P1 0)·변이 전건 검출. **ref 76(Fable)의 결론은 「가른다」가 아니라 「못 가른다」** — 시간대·지역은 성과등급을 못 가르고(홀드아웃 재현) 매체 집중도만 [상관·주의]로 남았으며, 밴드 총계 CTR·순위 격차는 **홀드아웃에서 깨져 강등**됐다(ref 64와 같은 구조). 매트릭스 개정5로 교차완료 **14→15/36(층2)**. ★**진도 1/5** — D-NAO-197의 ②CRITERION ③A5/A6 ④C10 ⑤커머스75건이 남았고 **「완료」≠「모든 데이터」**(층1 대비 범위 밖 존재). **D-NAO-199(Jino)**: 도착지는 수집→**Fable 연관성**→광고운영 반영이고 ③ 반영은 **별도 계약**(PAO 완전정지·대행사 칸 소유권·전략은 Jino 몫·②→③ 사이 홀드아웃 재현 필요). ⚠️**이 축은 전환·매출이 없어 총이익에 못 닿는다**(CPC·CTR·노출·평균순위까지). 교훈 #315(grain 절삭이 원격 전제를 깼다)·#316(창 없는 distinct). 인계 `.claude/memory/HANDOFF_dim-axes-ingested_20260818.md`. **★이름 확정 2026-08-09(D-NAO-162, Jino "파오로 하자"): PAO = Profit Ad Optimizer. 「우리판 MOP」의 정식 명칭이다. 광고 최적화 엔진이되 최적화 기준이 ROAS가 아니라 총이익. MOP는 계속 네이버(벤치마크)를 뜻하고, 결정 접두사 `D-NAO`는 유지한다.** — **★2026-08-03 밤: D-NAO-137 S1(APPLY_TM 관측 적재, 판정 무변경) 구현 완료·커밋 `b1268ad`·**미배포** — 착수 근거는 "APPLY_TM은 소급 수집 불가라 기다려도 표본이 안 늘어난다". ⚠️배포 선행조건=마이그 재부모(prod alembic `rg9billed7c4e`가 main 미병합 PR #190 것이라 헤드 분기). 배포는 08-04 07:45 크론 판정 뒤. **D-NAO-138 Jino 결정 3건**: 백필 행=주석만(코드 0·라이브 26행, 군집은 3개 23건이 아니라 **4개 26건**) / 04·15·P=계속 정지(★노출 0의 원인은 **캠페인이 off**임을 규명 — 재개는 auto_operate가 아니라 on/off 축) / 대행사 입찰 인하=무대응(★**대행사 자기 캠페인** 소재, 우리 자동운영과 접점 0).** 🛑 **2026-07-30 10:50 KST 우리 자동운영 광고 전면 정지(D-NAO-132, Jino "너한테 맏기면 망하겠다")** — 재개 전 선행조건(정착창 최근2일 반영·통합검색순위/소재별성과 수집·손실고삐 즉시화 등) 해소 전까지 신규 기능 개방 중단. **★2026-08-03: 정지 중 붕괴됐던 관측(소재 매핑·외부변경 탐지)은 복구됨(D-NAO-133, PR #174 배포 완료), 자동운영 정지는 유지.** 01·03은 대행사 추정 주체가 07-30 재가동, 04·15·P는 닷새째 노출 0 — 재개 여부 Jino 미결. **★2026-08-03 오후: 「수정 사항」 화면(D-NAO-135, PR #179+#180=main `593e171`) 배포 완료** — 날짜별 변경 이력 + 주체(우리 자동화/대행사/Jino) 조회·정정. 병행 세션과 alembic 헤드 2회 분기 사고, CAS가 clobber 차단. **★2026-08-03 저녁: D-NAO-136(탐지 주기 상향 선행 조사, 코드 변경 0)** — 주기는 안 바꿈(08-04 크론 결과가 선행). 실측 3건: 기준선 읽기 정상 확정 / **비용 전제 폐기**(`GET /ncc/ads{?ids}` 배치로 전량 5콜·5.5초 → 시간당도 현행의 절반) / **★`ad_edit`의 정체 = 상품 피드 재적용**(233건 중 229건, 판별자 `editTm−APPLY_TM≤120초`) — 실제 조작은 4건이고 셋은 대행사 입찰 인하. **다음 세션 최우선 = 08-04 07:45 크론이 이 233건을 어떻게 판정하는지 확인**, 그 후 계약(피드 분리 + `ids=` 배치). ★현재 주력 · 월 ~2,230만 SA 광고, 목표=한계 ROAS≥BEP×공격성 안에서 매출 최대(D-NAO-1). **P0~P2-S3a + 듀얼모드 완성 스프린트 6-Phase 전부 완료(2026-07-08, pytest 738·매 Phase codex review·origin push `c969303`) + prod 89K 실데이터 일괄 재검증 완료(같은 날)**. 진단보드7·bid_simulator·proposal_pipeline(08:00)·growth_sweeper 전수스윕·budget_allocator·trigger_watch(:07)·execution_harness골격·learning_loops(08:10)까지 가동 — "플라이트만 꺼진 MOP" 상태. **★2026-07-08 D-NAO-24/25 확정(Jino): ①MOP식 일일 예측 모델층 이식(모델 수 무제한, 게이트가 유일한 문지기) ②24시간 전문가 에이전트(부분 게이트 지향)** → 구조 승인 "진행하자" → **F0~F2+E1a 완료·prod 가동 중. 현재 스프린트 = 실행 루프 X(D-NAO-34, 2026-07-10 승인)** — MOP 갭 리뷰(ref 25)+논문 서베이(ref 26) 기반, X1a(쓰기 어댑터+제외키워드 개방+반자동)→X1b(정지재개→입찰+가드레일)→X2(당일 플라이트: GRM식 αB·αC 이분법)→X3(DHEB 풀링+GAVE 점수). **새 세션은 `docs/PLAN_naver-ad-execution-loop.md` §0 필독 → §7 체크리스트에서 현재 위치 확인 후 이어서**. 카나리=캠페인 2~3개(Jino 지정). 선결: Ava 크론 401 수리·카나리 지정. 병행 잔여: E1b(AI_office 별도 세션). main 기준 워크트리에서 작업. **2026-07-28 밤 D-NAO-106~109(워크트리 `learning-loop-repair`, 커밋 2건 미배포, 코딩 중단·인계)**: 학습 배관 수리(반응곡선 원료 분리=배포 권고·전환 정착 보정=배포 보류) + 신제품 광고 운영 4원칙 확정 + 갤럭시 대행사 캠페인 인수(범위 68그룹, Jino 최종 확인 미완) + 수명주기 국면 판정 구조 설계(★미승인). 다음 세션 최우선 = 학습밴드 충돌 해소·flight_loop 관측성 수정·dry_run 해제(순서 엄수), 상세는 트랙 파일 "현재 진행 단계" 최신 블록. **2026-07-29 06:3x D-NAO-117**: 학습밴드 스코프 불일치(계정 전체 승격 vs 캠페인별 게이트 소비 0건) 해소 — 기록 정정 + BEP 조건부 계정밴드 폴백, PR #162, prod 배포 완료(06:38). **2026-07-29 D-NAO-122·123**: 당일 그룹 grain 시간별 수집(전용 테이블 `naver_adgroup_hourly_today`+매시 :57 크론) + 출시창 하한 P1 3건 정정(사다리 position 오프바이원·is_floor·그룹입찰 경로) 구현·prod 배포·라이브 검증 완료(codex 2R 반영, PR #164 부채 회수). **D-NAO-124**: 순위 서보(4위=가중 선호로 완화, 하향 바닥 없음, D-NAO-120 개정) 설계 확정·미구현 — 다음 세션 최우선. **★2026-08-04 18:00: D-NAO-139·140 PR 경계 리뷰 완료(codex 한도 소진 → Opus 1기 적대 리뷰 대체, codex 소급 리뷰는 08-09 이후 부채)** — P1 1건 채택·수정·prod 배포(`164601f`): **대조가 「소재 행이 있는 날」만 검사해 수집이 통째로 빈 날이 초록으로 보이던 거짓 초록**(라이브 07-30·07-31이 처음 표면화). 같은 세션에서 **트랙 외 7주 묵은 운영 결함** 발견·수정(`2fba6a6`, Mac 런타임 배포+데몬 재기동): Wing 폴 데몬이 `last_rg=0.0`+`time.monotonic()` 비교라 **재부팅 후 1시간 동안 RG 갱신 버튼이 완전 침묵**(claim·로그·실패 보고 전무 → UI "Mac 응답 없음" 오진). **★「수정 사항」 화면 신뢰 경계를 트랙에 절로 신설**(Jino "sellC에서 보고 믿어도 되나") — 소재 grain은 시각 100%·피드 84.7% 분리로 믿을 수 있으나 **캠페인·그룹은 `occurred_at` 100% NULL**이고 화면은 하루 1회 아침 스냅샷이며 과거일수록 이력이 덮여 사라진다. **다음 작업 등재(Jino 지시)**: 캠페인·광고그룹에 발생 시각 부여 — `/ncc/campaigns`·`/ncc/adgroups` 응답에 **`editTm`이 이미 온다**(라이브 확정, 추가 API 콜 0), 계약 승인 대기. **★2026-08-04 밤: D-NAO-146·147 완료 — 광고 변경에 「언제」가 붙었다**(커밋 `00dc001`·`fcbcba2`·`b23de05`, 마이그 `f1a4c7e20b93`·`c5b8e3f74a12`, prod 배포·push 완료). ①정찰이 계약을 바꿨다 — 캠페인·그룹·키워드 `editTm`은 **피드 재적용으로 전진하지 않아** 판별자가 불필요(최근 3일 전진 = 캠페인 1/46·그룹 2/1,010, 소재는 같은 날 229건). 03 캠페인 editTm이 대행사 예산 4배 증액과 초 단위 일치. ②Jino "이제 모두 믿어도 되나?"에 답하다 **경로가 둘인데 절반만 고쳤음을 발견** → `naver_change_log.occurred_at` 추가 + 키워드 editTm(30일 342건) + **원천 간 중복 접기**(같은 사건이 두 경로에 다 들어옴을 라이브 확인). ③**라이브 합격**: 08-04 10:49:25/28에 외부가 끈 광고그룹 2건이 화면에 `occurred_at`·`가동→정지`로 표시. ★그 정지는 **우리 손이 아니다**(자동운영 07-30부터 정지). ④⚠️**백필 안 함** — 30일 475건 중 시각 있는 건 2건뿐, 08-05 발생분부터 쌓인다(2주 필요). ⑤⚠️prod alembic 헤드 분기 **세 번째**(병행 세션 미푸시 배포) — safe_deploy가 코드 배포 전 차단, Jino 승인 하 편입+재부모. D-N 번호도 충돌해 146·147로 재부여. ⑥화면 「믿어도 되는 경계」에 **범위** 절 신설 — ADVoost/GFA는 통째로 밖, 요일·지역·타겟팅·이름변경은 안 봄, 발견은 여전히 하루 1회 아침. **다음 = 08-05 08:00 예약 점검(`naver-ad-occurred-at-morning-check`, Slack Ava 보고) — 중복 점검 금지.** **★2026-08-06 D-NAO-156·157(손익 정합 성격, 광고 집행 축 아님, PR #229 병합·prod 배포)**: 상품 손익 화면이 원가 미상 상품을 이익률 94~96%로 표시하던 결함(`cost_map.get(pid, 0)`) 발견·수정 — 원가·이익·이익률을 `None`(«모름», 사유 `unmapped`/`zero_cost` 분리)으로 표시하고 배너로 규모(6개·956,545원) 표면화(D-NAO-156). Jino "근본 수정을 해야 하는거 아니야?" 지시로 원천 조사 → 원가 후보가 갈릴 때 `product_master.id` 기준으로 조용히 고르던 코드(«모름» 배너도 못 잡는 자신 있는 오답 경로) 및 구형 엑셀 업로드 경로에만 빠져 있던 이중 활성 매핑 가드(D-12) 구멍(라이브 22건 적체) 수정(D-NAO-157). 테스트 4,933 passed. **★2026-08-06 밤 D-NAO-158(Jino 22:14 "통합 손익 대조원장도 같이 고쳐줘", 커밋 `c77a7df`·`1c747a1`, prod 배포)**: `product_pnl.py`의 `_agg_marketplace_by_sku`에 같은 버그 클래스를 확인·수정 — 단 이 원장은 보존법칙(`conservation_ok`)이 걸려 있어 **금액은 한 톨도 바꾸지 않고**, «모른다는 사실»만 별도 필드(`cost_known`·`cost_unknown_revenue`·`cost_unknown_skus`·`cost_unknown_scoped`)로 실어 표시층이 해당 SKU 순익을 「—」로 비우게 했다. 30일 라이브 검증으로 배포 전후 `reconciled_net_profit`·`net_profit_allocated_total`·`account_adjustment_residual` 전부 원 단위 불변 확인. **수치 정정**: 이월로 적었던 "네이버 1개 상품·44,500원"은 `product_channel_mapping` 조인 오차였고, 엔진이 실제 쓰는 `Order.product_id` 조인으로는 **17,800원**이다. **★SKU 행 「—」 표시는 미검증** — `coupang_1p/cost`·`coupang_1p/net_profit` 보존법칙이 이번 변경과 무관한 **기존 결함**으로 이미 깨져 있어 `by_sku`가 모든 조회 창에서 빈 배열이다(다음 관문 = 이 보존법칙 복구). 프론트 부수 발견: `tsc --noEmit`은 통과했으나 실제 빌드 `tsc -b`가 TS2345로 실패(`ProductConnectionMap.tsx`의 `won()`이 undefined에서 "NaN원") — 수정 완료, 프론트 CAS가 배포 전 보호. 테스트 +4, 전체 4,937 passed. 이월=cost_price=0 매핑 127개·원가 미상 6개 상품(Jino 입력 대기)·`coupang_1p` 보존법칙 복구(최우선). **★2026-08-06 밤 D-NAO-159·160(같은 PR #229 계속, 커밋 `5d66eee`, prod 배포)**: Jino "이제 SellC 스마트스토어 손익표는 모두 정확히 믿어도 되는거지?" 질문에 축별 실측 점검을 하다 2건 추가 발견·수정. **D-NAO-159**: 「기간」 탭(7·15·30일)이 `ad_costs`(D+1 확정)만 써서 오늘 검색광고비가 0원으로 들어가던 결함 — 「오늘」 탭이 어제 광고비를 물던 결함(#151)의 **거울상**(라이브: 30일 창 오늘 매출 1,376,420원 vs 오늘 광고비 0원, 실제 675,561원). 「오늘」 탭과 같은 헬퍼(`_today_search_snapshot`)로 통일해 당일 검색광고 스냅샷을 더함. 디스플레이(ADVoost·GFA)는 리포트 축에 없고 비즈머니 실차감이 D−1까지만 줘 구조적으로 못 메운다 — `ad_basis`(kind="period")로 화면이 «검색은 오늘까지+디스플레이는 어제까지» 혼합 축임을 말함. **D-NAO-160**: 반품·교환 배송 손익이 패널에 통째로 없어 반품이 늘어도 이익이 반응하지 않던 결함 — 메인 엔진의 `_return_shipping_pnl`·`_exchange_shipping_pnl`을 그대로 호출해 배선(귀속=클레임 완료일, 배송건수엔 미가산). **★부호를 손실로 단정하지 않음** — 5,000원 청구 건은 배송축만 보면 +421.82로 흑자(테스트를 음수로 못 박았다가 실측이 반증, 코드가 아니라 가정이 틀렸음). **라이브(30일) 합산 영향**: 광고비 21,065,266.59→21,740,827.59(+675,561) · claim 13건 net −28,766.36 · **이익 4,563,266.14→3,892,997.86(−670,268), 이익률 11.10%→9.43%**. 테스트 +10, 전체 4,947 passed. 이월 추가=디스플레이 광고비 시차(gfa:advoost 08-04·gfa:da 08-05, 원천 없어 못 메움)·수수료 추정 비중(7일 창 24.3%로 짧을수록 큼). 기존 이월(cost_price=0 127개·원가 미상 6개·`coupang_1p` 보존법칙 복구)은 그대로 유지. 상세(갱신됨) `.claude/memory/HANDOFF_cost-unknown-not-zero+mapping-guards_20260806.md`. **★2026-08-07 06:3x D-NAO-161(같은 워크트리, 코드 변경 없음·측정만, ref 49)**: Jino "수수료 추정치는 나중에 실측으로 바뀌면 자동으로 맞아지는거지?" 질문에 답 — `sales_summary`가 조회마다 정산 실측을 우선 적용(저장 안 함, 재계산 배치 불필요) 구조 확인 + **최근 60일 4,958라인 전수로 예상=실측 오차 0원 재검증**(직전 503라인 표본과 동일 결론). 정산 커버리지 곡선도 재측정(D+0~3 19.2%부터 시작 — 종전 "D+0~2 0%" 기록 정정, D+12~14는 99.5%로 완전 100% 아님, 완전 폐쇄 D+21). **결론: 「실측 N / 예상 M」은 신뢰도 라벨이지 오차 경고가 아니다** — 이전 이월 "수수료 추정 비중" 항목 재점검 종결. **★2026-08-10 costmap 매핑 정정(문서만, 배포 없음, ref 53·교훈 #197)**: D-NAO-157 `ambiguous` 판정이 08-06 신설 후 처음 실발동(갤럭시탭 S8·S10FE, `auto_sync` 기종 오판이 원인, 사람 입력 `excel_master`가 정답) — 아이폰12 프로맥스/공용·Z플립8 외부액정 매핑까지 총 5건을 API 경유로 정정(Jino 승인). 라이브: 어제 미상 2→0개(이익 263,544→257,372), 30일 미상 2→0개(이익 2,850,167→2,822,468), 기존 상품 원가 변경 0건. cost_price=0 매핑 127개는 그대로 잔존. **★2026-08-10 오후 D-NAO-169(소급 기재)·170·171 — B-4「쓰기가 옥션에 닿는가」 종결**(커밋 `9291112`·`218c375`·`cf0cb16`·`dad5612`, prod 배포 무중단 8011→8001, 전체 5202 passed, 변이 23종 전건 KILLED). **D-NAO-170**: 그룹입찰 死 거부를 «데이터 수리 신호»로 — 가드가 거부하는 순간 쥔 라이브 소재 목록을 `naver_adgroup_product`에 upsert해 **다음 회차에 기존 라우터가 스스로 소재로 절체**한다(새 경로 0). 진단 전환 = 「재라우팅 기계가 없다」가 아니라 **「기계의 입력을 고치는 손이 없다」**. **D-NAO-171**: 그룹 판정의 집행 대상을 max 소재 1개 → **활성 false 소재 전부**(각자 기준 ±15% 승수 스텝, 비율 보존, **봉투 계수 무변경**이 성립 조건 — 락스텝이라 케이던스가 자동 성립). 캡 2층(결정 5=그룹당 한 자리 / 쓰기 15). 실측: 소재 2개+ 죽은 그룹 61개 중 **36개**가 「max만 내리면 차순위가 새 max」에 걸리고 **61개 전부**에서 나머지 **182소재 지출이 판정과 무관하게 유지**됐다(소재 1개 160그룹은 영향 0). **적대 리뷰 FAIL→P1 1건 수용**: 앵커만 동결하고 **같은 탐지기의 비교 대상 값 3종**을 덮어 사건이 `ad_edit`으로 강등 → **자동 상향 2.0× 누적 상한의 기준점 미재설정**(codex 3R이 닫았던 구멍의 재개봉) → 덮기 전에 탐지 실행, **탐지 실패 시엔 덮지 않는다**(이 한 지점만 fail-closed). **★★라이브 = 부품 통과·end-to-end 미검증 —「합격」이라 부르지 않는다**(§2): 지출 0원 그룹에서 **네이버 쓰기 0건**으로 가드 거부·write-back 8행·앵커 불변·탐지 선행·**라우터 group→ad 절체**·팬아웃 비율 6.333→6.231 보존까지 관측했으나 실행 경계는 못 넘었다. **막힌 이유가 중요하다 — PAO는 `auto_operate=0`이 아니라 `optimizer='none'`까지 내려간 «완전 정지»**(2026-07-30, 7캠페인 전부)라 harness가 **콘솔 Confirm 경로까지** `OptimizerGuardError`로 막는다. 설계대로다(07-27 change_log가 미리 규정). **Jino 결정: 재개 시점까지 보류** — 재개 첫 회차에 관찰할 잔여 합격기준 ①⑤⑥⑦은 트랙 D-NAO-171 항목에 명시. 부수: `next_ids.sh`가 **D-NAO-169를 다시 내주려던 것**을 차단(원장 누락 탓 — 충돌 방지 도구가 자기 입력의 결손은 못 본다, 교훈 #216). 교훈 #216·#217·#218. **★2026-08-12 D-NAO-174 — D-NAO-173 P2 적대 리뷰 부채 상환**: `c479353`·`fcf7b33`는 PR 경계 적대 리뷰 없이 병합된 부채였다. 사후 리뷰 **1R FAIL(P1 3건)** — 조회 실패를 「대조 가능」으로 오분류(`exclusion_survival.py`) · BEP 없을 때 비용절감 전액을 회수액으로 오신고(`search_term_scorecard.py`) · 프론트가 `unverifiable` 상태를 한 번도 안 읽음(`NaverAdExclusionList.tsx`). 전건 수리·변이 5/5 KILLED·backend 5,415 passed·frontend 335 passed. 부수 발견: iCloud dataless eviction으로 프론트 테스트 65개가 조용히 사라져 266 passed로 「초록」이었던 것 발견·복구. 교훈 #270~#273.
- [상품 연관맵 (Product Connection Map)](tracks/active/track_product-connection-map.md) — **코드 100% 완료(2026-07-04), 남은 건 데이터 입력 하나**. 같은 옵션을 자사몰·네이버·쿠팡(ofix/ohitech × 3P/RG/1P)에서 내부코드(`OHI-xxxx`) 1개로 묶어 옵션 단위 통합 손익(레벨2)까지 조망. S1~S5 main 머지 + S6a(prod 배포·라이브 self-verify, prod가 6/22 이후 이 트랙 전체 미배포였음을 스코프드 배포로 따라잡음) + **라이브 검증 중 실운영 버그 발견·즉시 수정**(순환소수 diff=3E-21→conservation_ok 오판→SKU행 531건 은폐, 백엔드+프론트 동시 수정 `c3dae2a`+`0f879ec`) + **S6b 온라인 매핑 UI 신규**(`6265df9`): 미매핑 옵션ID 목록을 화면에서 펼쳐 원클릭으로 상품에 연결 — 엑셀 없이도 매핑 보강 가능해짐, 브라우저 라이브 e2e 검증 완료. 전부 main 직접 push+prod 배포. 남은 것=오픽스(WING1/RG1) 매핑 결손을 Jino가 직접 입력(온라인 UI 또는 엑셀, 코드 아님).
- [오하이테크(1P 로켓배송) 광고비 수집](tracks/active/track_coupang-ohitech-ad.md) — 3/N · **2026-06-21 생성**. 오하이테크 광고센터(A01029796) 집행 광고비가 시스템에 0으로 통째 누락(7일 ~400만원) — 페처가 오픽스만 봄. **라이브 실측 1:1 검증 완료**(getVendorAdPerformance adCostSum=3,997,206=화면). 결정: 전액 1P 로켓배송 귀속(D-2)·Phase1 계정단위 일별(D-6)·Keychain 자동로그인(D-7)·Mac residential+핵심쿠키4개(D-4/5). 다음=S1 페처 확장(세션 분리).
- [대시보드 숫자를 Wing 실제와 일치 (매출 정본화)](tracks/active/track_revenue-wing-truth.md) — 0/N · **2026-06-20 생성**. Jino "매출/광고/수수료/원가가 미덥지 않다 → Wing과 맞을 때까지 조정". **결정 A: 닫힌 과거일 매출 정본=Wing 판매분석 GMV(net)**. 라이브 검증: 오픽스(ch1)=WING1, 취소 없는 날 정확히 일치(차이=gross−net 취소). Wing 수집 복구(CDP Chrome 9222 재기동, 6/19까지 신선). 공백(D-5): 오하이테크(ch2)·RG 다계정 Wing 미수집, RG 취소 status 없음. 다음=S1 Wing 다계정 수집.
- [쿠팡 로켓배송(1P) 종합조망 편입](tracks/active/track_coupang-rocket-1p.md) — 오하이테크 1P(supplier.coupang.com) 발주·납품·정산 수집→종합조망. 매출=발주, 순이익=발주−원가−광고(D-1~8). **★2026-06-17 S1 정찰 완료(1/N)**: 라이브 실측(ref20+D-9) — ①발주+②납품=`/po-web/app/purchase-order/list` JSON 1개(sumOfOrder/ReceivingAmount), ③정산=`/scm/settlement/general/purchase/account` SSR HTML(계산서 grain, 공급가+VAT=지급예정). Akamai방어→헤드풀 CDP 페처 필수. **2026-06-18 S2~S4.5c 완료**(모델·페처·종합조망·원가 아크). **2026-07-28 M1 완료·prod 배포·라이브 검증**: 파서가 버리던 원본 컬럼 2건 복원(전자세금계산서 전송상태 True 90행·업체납품가능수량 451행/납품부족 SKU 32건 검출, alembic `f6a8c0b2d4e6`). 과정에서 **alembic revision ID 충돌 `a1c3e5f7b9d1` 발견·해소**(promo-pnl을 uuid4 `c2998cfe1f7c`로 개명 + prod 선배포 3개 흡수 → 직렬 단일 체인, PR #135). LESSONS #49·#50. **2026-07-28 M2 유지보수**: DOM 셀 추출이 쿠팡 마크업 들여쓰기에 의존하던 문제 제거(DOMParser는 렌더 안 되어 innerText=textContent → 요소 사이 공백 없음; 미니파이 시 바코드 컬럼 오염) — 텍스트노드 명시적 공백 조인·`<br>`은 비분리자, 라이브 정산 마크업으로 회귀 0 실증, 로컬 런타임 배포 완료(커밋 `412042e`, 미푸시). 다음=S5 프론트. **M2는 codex 게이트 면제(Jino 판단 07-28)** · M1 codex 부채만 잔존(쿼터 08-02 해제). **2026-08-05 M3+D-14/D-15**: 로켓1P 원장 대조("값은 안 틀렸다, 완결성·귀속이 틀렸다") + leaf 종합조망 배선(커밋 `8948fdc`, PR #199 병합) + 매출 축 토글(`2c4a88d`)·조회 성능(`82ec4f8`)·프론트 레이스 2건·ASN 미수금 실측 → **정정 8,033,970원**(ref 45 §12). 상세는 트랙 파일 D-14·D-15. **★2026-08-06 D-CPP-11~15 + 자체 적대 리뷰**: 판매분석 퍼널 지표 적재(S1)·매출 두 축 화면(S2)·유입전환 퍼널(S3) 신설 후 **codex 대체 3렌즈 적대 리뷰**로 P1 5건 발견·수정 — 최대 건은 **43,147,487원이 종합조망 광고비에서 누락**(1P 광고 원천이 둘인데 엔진이 하나만 읽음, net_profit 83,819,364→40,671,877 정정). 교훈 #146·#147. **2026-08-06 D-21**: 1P 광고비 8개월 결손 B안 소급 적재 완료(236일 331,939,056원, 라이브 9개월 일치) + 검산 기준을 비중→완결성으로 정정(교훈 #144). **★2026-08-27 「1P계산서 목표」(계약 `docs/contracts/CONTRACT_1p_invoice_gap.md`, PR #507 머지 `7f7c43a7`)**: `/rocket-recon`에 「열린 파이프라인」·「확인요청함」 탭 신설 — 발주가 돈이 되기까지 네 칸(확인대기 2,907,817 / 발송대기 12,895,305 / **입고대기=계산서 미발행** 9,373,788 / 지급대기 116,587,510)과 ①②③ 소계 25,176,910원, 발송일 창을 걸면 8/20~ 미발행 **21 PO 9,319,638원**. RI 12건을 **살아있음 4 / 굳음 8**로 섹션 분리(굳은 8건은 지급까지 끝난 유령 — 상태만 보고 목록을 내면 죽은 줄을 누른다). 굳은 미종결 발주 재훑기를 페처에 배선(대상 날짜는 백엔드가 정한다). ★**구현이 잡은 결함 4건**: ①**시간대 규약 혼재** — `shipped_at`(발주상세 DOM 셀 유래)·`synced_at`(`kst_now()`)은 **KST 저장**인데 `po_created_at`은 UTC라, 관행대로 +9h를 걸면 마지막 수집일이 «내일»로 나와 신선도 판정이 전건 오답이 된다(근거: `shipped_at` 992건 중 **965건이 16시대**) ②**입고는 발송의 하한** — ASN 라인 0건인데 전량 입고된 PO를 「안 보냄」으로 읽어 「영영 못 보낸 분」이 225,840→**55,200원**으로 정정 ③재훑기 필터가 `SETTLED_STAGE_STATUSES`를 그대로 써 **원인인 RI를 뺐다** ④기본 120일도 같은 병(원인 8건이 2025-10~2026-01 발주라 통째로 잘림)→400일. 적대 리뷰 **PASS(P1 0)**, 변이 12종 중 11 KILLED — **표면 절단 변이 2종(잔여 금액 셀·굳음 배지) 모두 KILLED**(교훈 #362 재발 없음). 완료 QA **부분달성**(대조 3건 각각: 계약 §4 부분달성 S1 8/8·S2 3/4·S3 0/3 이월허용 / Jino 지시 원문 부분달성 — 「내가 누를 수 있게」 실행부는 계약이 스코프에서 뺌 / **트랙 궁극 목표 판정불능** — 종합조망 파일 diff 0건). **트랙 계약 헤더 신설**(프로즈 4/6 → 기계 판독 **10/12**, 이번 작업으로는 **불변**). 미해명 8,939,475원은 확정 숫자 아님(소계 합산 금지선). **★2026-08-27 S3 쓰기 정찰 완주(ref 106, 체인 n=2 — 저장소 코드 0줄)**: 「거래명세서확인」 전이의 주체는 **벤더 버튼**이다 — `#btnConfirmInvoice` → `POST /scm/purchase/order/confirmInvoice?purchaseOrderSeq={seq}`(바디 없음·CSRF 토큰 없음·응답 `{success}`·핸들러 `detail.js:215-227`), 대조군 CI·PA·RP 3종 전부 버튼 0건 ⇒ **RI 전용 서버 게이트**라 「버튼 존재 = 지금 확인 가능」의 정본으로 쓸 수 있다. ⇒ 「안함」의 「supplier 쓰기 → S3 후 별도 계약」 **전제 충족**(초안은 없음 — 되돌릴 수 없는 회계 확정이라 Jino 승인 지점. 미상 3건: 멱등성·부분실패 표면·되돌리기 부재 ⇒ **재시도 금지**가 금지선). ★★**바로 위 「굳은 8건은 지급까지 끝난 유령」은 반증됐다** — 재수집 후 8건 전부 **여전히 RI**이고 확인 버튼이 살아 있다(최고 2025-10-15, 지급일은 전건 경과). 죽은 줄이 아니라 «아직 안 누른 줄». RI **12→15건**(481,085 → **7,299,040원**), 굳음 **8→0건**(prod 화면 실관측). ★★**S2-3이 라이브에 안 보였던 원인은 「페처 미실행」이 아니라 Mac 런타임이 저장소와 다른 사본이어서였다**(교훈 **#371**) — `~/.ohisell/tools/` 설치본이 08-22판이라 재훑기 코드 **0건**(저장소 4건, diff 113줄). `install_local_runtime.sh --files-only` 배포 + 데몬 재기동 후 첫 회전에 바로 돌았다(22:24 날짜 22개·발주 41건, 발주 push 522/11 → **753/33**). **머지는 배포가 아니다.** 완료 QA 대조3: 계약 §4 **부분달성**(S3-2 판정불능 — QA가 채팅 표면 접근 불가) / Jino 지시 원문 **부분달성** / 트랙 궁극 목표 **미달**(코드 diff 0줄) ⇒ 종합 **부분달성**. **진행률 10/12 불변**. 다음=**쓰기 실행 계약 초안(Jino 승인)** 또는 트랙 S5. 2026-06-15 생성.
- [쿠팡 RG 재고·발송 관제 (Replenishment)](tracks/active/track_coupang-rg-replenishment.md) — 🔧 **Maintenance (2026-06-19, D-18 3/3 = 실용적 완료)**. RG 현재고+발송중+판매속도+리드타임으로 "언제·몇 개 발송" 역산. **완료 게이트 충족**: ①UI정합(S6.5 발송중·유효재고 컬럼·prod 배포)·②P4 백테스트(GET /replenishment-backtest, 9옵션 fill 0.90·과잉5.16일·prod 라이브)·③정직성회귀. **D-18**: 보류 축=수요신호 부족(848/857 무판매)이지 리드타임 아님 → 파는 옵션 발송 정확성으로 완료 정의. **S9/S10 예측타워는 D-17 보류**(예측가능 9옵션뿐, 데이터 누적 대기)라 completed/ 미이동·maintenance 유지. 신규코딩0·자동고도화. 재개트리거=demand-class sparse+active 증가. (이전: Phase1 S1~S6·Phase2 in-transit P3·S8 demand_classifier 완료, ref19·PLAN_phase2.)
- [통합 재고·발주 관리 — 「발주예측 목표」](tracks/active/track_inventory-management.md) — **4/9 · 2026-08-25 생성**. Jino *"ecount에 있는 우리가 발주한 수량, 폴더에 있는 우리가 픽업한 데이터와 … 판매량과 쿠팡 1P에서의 발주수량을 대비해서 과거의 패턴을 찾아서 향후에 우리가 발주를 OTAO에 낼 수 있게 발주수량 예측할 수 있는 시스템을 만들자"*. 계약 `docs/contracts/CONTRACT_inventory_unified.md` **v0.1 초안 → v0.2 개정·승인**(13:30 *"그래"* — 새 계약 만들지 않음, 동일 소관 문서 둘 금지). ★**결정변수가 둘이다**: ①**발주층**은 OTAO **생산 예약**이라 현금·창고비가 우리 장부 밖이고(Jino *"이카운트에서 발주한 수량은 OTAO가 우리를 위해 생산해놓는 수량이고, 우리가 가져오는 수량만 지급함"* / *"결국은 다 받아야 하겠지만, 지금 당장은 안가져와도 됨"*) 목적함수는 `기대 데드스톡 채무 + 기대 생산품절 손실` — **재고 최소화가 아니다** ②**픽업층**만이 Jino의 「제고 최소화」가 사는 층이다. 리드타임 셋으로 분리(**L1 생산 통상 4주**=Jino 값·관측 불가 / L2 운송·통관=관측 가능 / L3 정산 시계 = 전월 20~당월 19 픽업분). 픽업 간격 **중앙값 ~14일**(ref 92, 8개월 14건). ⇒ 생산 4주 vs 픽업 2주라 **OTAO 예약 잔량은 예외가 아니라 상시 파이프라인**. ★**이 프로젝트는 「수집」이 아니라 「연결」이다** — 필요 6축 중 5축이 이미 sellc에 있다(네이버 60일 결손 0 · 쿠팡 4계정 12~13/14일 · **1P 발주 SKU별 11,043행 커버리지 99.96%** · 픽업=수입 원장 `import_invoice_line` 158줄). ★★**S0-a 실측이 부정**: ECOUNT 발주서조회 Open API는 **품목 라인을 안 준다**(1,622건 전건 `(ORD_DATE,ORD_NO)` 유니크 · `PROD_CD` 없음 · 「외 N건」 **78.5%** · 못 읽는 라인 ~7,134줄 · **금액 87.5%**), 역파싱 불가(첫 품목명만 남고 나머지 소실 + QTY가 전표 합계), 단일품목 349건(21.5%)만 유효. 게다가 **실서버는 `EXP00001` 미검증 거부**이고 테스트존만 200 — 확보분은 테스트존 산출물이다. ⇒ **「예약 잔량 = 발주 누계 − 픽업 누계」의 발주 항이 SKU 단위로 안 채워진다** (구조 재판단 진행 중). 건진 것 = `src_purchase_lines.jsonl` **14,066행**(품목코드+수량 보유)이나 그레인이 **「매입 확정」**이라 발주 시점이 아니다. ★**금지선 6(이카운트 엔드포인트 제3자 단서 금지) 해제** — `AI_office/docs/references/ecount_automation_manual.md`가 공식 OAPIManual 전수 실측. ★**새 금지선 4개**: VM 외부 ECOUNT 호출 금지(**login 10회 실패 시 사람 웹로그인 포함 ERP 전체 차단**) · **결손을 판매 0으로 대체 금지**(cafe24 60일 중 6~8일 공백이 「주문 0건」과 구분 불가 — 결손을 0으로 읽으면 발주 축소 → 4주 뒤 품절) · 계약 A′ 소관(수입 원장) 코드 수정 금지 · 3상태 합산 표기 금지. 완료 QA 2대조 = **미달**(계약 §4 0/9) / **부분달성**(Jino 원문 8건 — 문서 정합은 달성, 시스템 구현 0%). 체인 `.claude/memory/chains/발주예측.jsonl`. ★★**n=2(2026-08-25 18:1x) 되감기 검증·규칙 복원**(ref 99): `재고(t) = 본사현재고 − Σ입고(>t) + Σ판매(>t)` 를 47코드×4시점=**188칸**에 돌려 음수 **2칸(1.1%)** — 세 축(ECOUNT·통관원장·판매 4채널)이 독립 수집인데 물리적으로 정합. ★**표본은 26건이 아니라 3건**이었다 — 입고 원장이 **2026-01-27부터**(2025년 선적 **0건**)이고 쿠팡 1P·2P/3P 판매가 **2026-06-01/06-07부터**(판매의 84%)라 유효 구간이 2026-06-07~08-25로 잘린다. ref 98 §6-A의 「26건 표」는 원리적으로 불가능했다(방법이 아니라 **원장 범위**). ★그럼에도 규칙이 나왔다: 최소 발주 단위 **50개(56/56 전건)** · 발주 **31일치**(빠름 ≥3개/일) = 실측 리드타임 **29일**과 정합 / 느림 48일치는 규칙이 아니라 **최소 단위 50의 부산물**(속도 0.23/일에 50개 = **217일치**). ★★**Jino 18:08** *"현재 본사 재고로 잡혀있는 수량들은 비슷한 수준이지 100%는 아니야"* ⇒ 민감도 ±30% 재계산이 결론을 갈랐다 — **「얼마를 시키는가」(발주서÷판매·예약잔량)는 불변, 「얼마를 채우려 하는가」(재고일수 34↔63일·목표 72↔100일)는 폐기.** 계약 §2에 **7-C**(1차 입력은 ECOUNT 무사용 축, 재고는 조정항)·**7-D**(최소 50은 규칙이 아니라 제약 — 느린 SKU엔 「안 시킨다」가 유효한 답) 신설. ★**이탈이 인사이트**: 예약잔량÷재고 중앙값 **11.6배**, `GAPIP15`는 2026-08-12에 재고 **7개(1.5일치)**인데 OTAO 예약잔량 **3,150**을 둔 채 **300개를 새로 발주** — 이 결론은 ECOUNT를 안 쓰므로 오차에 견딘다. ★ref 98의 「본사 4,138」 정정 — 그것은 56코드 중 **38개만 센 부분집계**였다. 단 정정값은 하나가 아니다: **56코드 exact 5,397**(되감기가 쓴 모집단) / GC 제외 **5,032**(순수 액정보호) / 이름 기준 5,988. **모집단 없이 「강화유리 N개」라고 쓰면 안 된다**(완료 QA가 5,988로 재계산해 이 불일치를 잡았고 지적이 옳았다). ★그 과정에서 **`GC`=카메라렌즈 확정**(품목명 원문) — ref 98의 [미상] 1건 해소. ★★**n=5(2026-08-26 09:0x) S1 달성 — 발주 메뉴가 라이브가 됐다**: `https://sellc.ohitech.co.kr/otao-po` (사이드바 「📦 발주 (OTAO)」). SKU **75행** · 발주 누계 **30,090** / 픽업 누계 **13,370** / 예약 잔량 **16,720**을 **세 칸으로** 본다(합산 단일 숫자 없음 — 계약 §3-9를 테스트가 단언). 화면이 자백하는 것도 라이브: 창 구간 「2026-01-27 이후」+창 밖 발주 310,927개 별도 칸 · 「매핑 필요 30종 8,390개」 · 음수 잔량 4코드를 0으로 안 깎음. ★**ECOUNT 대조는 표본 10건이 아니라 전수 66건 — 65/66 일치**(ref 100). 발주번호가 `ORD_DATE-ORD_NO`로 양쪽에서 같아 표본 선정이 불필요했다. 불일치 1건 `20231011-3`은 **PDF 자체가 ECOUNT와 다른** 건(헤더 검산은 통과)이고, 적재 리포트의 `tie_broken_by_mtime` 자백 필드가 **그 건을 미리 지목했다**. **ECOUNT API 호출 0건**(n=1이 받아 둔 파일 대조 — §3-3 준수). ★적재 **발주서 95 / 라인 1,205 / 총 453,127개**, 재실행 시 `신규 0 · 그대로 95`로 **멱등성 라이브 실증**. ★적대 리뷰 **PASS(P1 0)** — 1R P1 2건 해소 + P2 5건 채택(생존 변이 4종을 닫음). PR #460 머지 `262e68b3`(CI 3/3 실통과) · prod 마이그 `cst4pick59a → otaoev1merge`(병행 세션과 head 갈림을 병합 리비전으로 합류) · 무중단 배포. ⚠️**결손 2건**(`20230509-3`·`20250519-1`, ECOUNT 합 6,000개)은 발주서 PDF가 Drive에 아예 없다 — **원천 결손이지 적재 결함이 아니다**. ⚠️사전 커버리지 **35/65**가 인계값(43/65)과 다르다 — `sync_name_map`이 정본 66건에서만 사전을 만들기 때문이고, 그 맞바꿈을 의식적으로 고르지 않았다(다음 슬라이스에서 잰다). ★★**n=6(2026-08-26 13:5x) S3 달성 — 판매 4채널이 한 화면에 섰다**: 같은 `/otao-po`에 「판매 (채널 통합)」 채널 5축 7행 · SKU **545행** · 각 행 **60일 스파크라인** · 매핑률 98.35~100% · cafe24 「판매 0 7일」 · 쿠팡 5축 「구분 근거 없음」. ★★**발주 축은 «섬»이다** — `GAPIP` 코드가 prod 전수 스캔에서 이 트랙의 두 테이블에만 살고 판매 축 `internal_sku`(963종)와 **겹치는 값 0개** ⇒ **S5는 이 다리 없이 원리적으로 못 선다**(다리 «구축»은 계약 「안 함」 → `track_product-connection-map.md`, **Jino 발의 대기**). ★실측이 위임 조사 결론 둘을 뒤집었다 — 쿠팡 다리는 `product_id`가 아니라 **`vendor_item_id`**(매핑률 0%가 아니라 **99.9~100%**, **잘못된 조인은 예외가 아니라 «0»을 낸다**) · `sync_log`에 쿠팡 채널 실재. 적대 리뷰 **1R FAIL(P1 3건)→2R PASS** — P1-1은 `channel_product_id` 중복 55키·121행이 outerjoin에서 1:N으로 펼쳐져 **판매를 곱하던** 것(Wing 3P +6.0%·RG 2P +6.5%, 그 몫이 **팔린 적 없는 SKU에도 배분**)이고 **기존 픽스처가 키당 1행뿐이라 0건이 잡았다**. PR #467 `d5afaaa7` + #473 `f05f174c` · D-INV-5(사전 모집단 = 발주서 전체 95건, 층으로 집행). ★★**n=7(2026-08-27 06:4x) S2 — 정산 창 축이 화면에 섰다(진행률 4/9 «불변»)**: 「정산 창 (OTAO 지급)」 창 **7개**(2026-02~08) · 합계 **310,742 CNY**(상품 282,662 / 부자재 28,080). **체크박스는 안 찍었다** — S2는 한 문장 안에 두 요구가 있고 «앞 절반»만 됐다. ★★**뒤 절반(실제 지급액 대조)은 대조할 «대상»이 저장소 밖에 있다** — prod **123개 테이블 전수** + 워크트리 파일 검색에서 OTAO 지급·송금 원장 **0건**(정산 이름이 붙은 것은 전부 쿠팡·네이버 «판매대금», 범용 `settlements`는 행 0개, `remittance_fx_rate` 12/12 NULL). ⇒ 전 창 `reconciled: null` = **「대조 불가」이고 그것이 정직한 값**이다(`false`면 없는 사실을 만든 것). **Jino가 창 1개분 지급액을 주면 그 자리에서 대조된다.** ★**D-INV-6 — 금액 축은 CNY 인보이스**(과세금액 KRW 아님·라인이 정본·원화 환산 금지): `declared_inv_value == Σ(단가×수량)` **12선적 전건 차이 0.00**. ★부자재(cleaning kits) **28,080 CNY = 지급액의 9.0%**가 S1 픽업 누계엔 안 들어간다 — 갈라서 싣고 등식 `picked+unmapped=product_quantity`로 두 모듈을 묶었다(prod 성립). ★**이 원장엔 「OTAO 픽업일」이 없다**(한국 쪽 신고일뿐) — 창 경계 ±2일 **3건**·draft **1건**을 화면이 지목한다. 적대 리뷰 **1R FAIL(P1 2건)→2R PASS**, 둘 다 「값은 맞는데 사람에게 안 닿는다」 — 특히 P1-2는 **n=6 교훈을 적어 둔 그 파일에서 같은 구멍을 한 칸 옆에 판 것**이었다(빈 원장만 렌더해서 Jino가 보는 분기의 표면 절단이 972 passed로 살아남음). PR #486 머지 `4ac8281d` · 배포 `7a023750`(무중단·마이그 0) · ref 104. ★★**n=8(2026-08-27 13:2x) S4 — 자사 재고 축이 원장이 됐다(진행률 4/9 «불변»)**: `otao_stock_snapshot` 테이블 + `/api/otao-po/stock` + 「자사 현재고 (파생)」 화면. **체크박스는 안 찍었다** — S2와 같이 한 문장에 두 요구이고 앞 항이 원리적으로 막힌다. ★**착수 실측이 세 항 중 둘의 부재를 확정**: 판매는 GAPIP 축에 못 붙고(`product_code` 75종 ∩ `internal_sku` 963종 = **0**, 네 방법 교차 확인) 초기 실사는 **존재한 적이 없다**(grep 0건·`inventory` 0행). ★★**n=2의 되감기 실증(188칸)이 «재현 불가»** — ECOUNT 스냅샷 1,391행과 크로스워크 3종이 스크래치패드와 함께 소실(ref 99 §9가 스스로 "비커밋"이라 적어 뒀다). ⇒ **관측을 원장에 담지 않으면 관측은 주장으로만 남는다**가 이 슬라이스가 테이블부터 만든 이유다. ★**막히지 않은 쪽은 끝까지**: 「실사 표본 10 SKU 대조 오차」는 판매 축을 안 타 성립하고 계약 §2-7C ④가 S5 문턱으로 지목한 숫자다 — `build_stock(counted=…)`으로 열어 **Jino가 10개 세면 코드 수정 없이** 뜬다. ★적대 리뷰 **1R FAIL(P1 3건)→2R PASS(P1 0)** · 변이 **5/5 KILLED**. P1-1 = 병합 즉시 alembic head가 둘이 되어 **테이블이 prod에 못 서는** 것(브랜치 단독으론 초록이라 안 보였다) · P1-2 = 「대조 오차」가 본사↔본사-포장으로 계산될 수 있었다 · P1-3 = 나눠 센 실사의 앞 회차 소실. ★★**§3-3 IP 가드에 테스트 0건**이었다(가드를 `if False:`로 지워도 전건 초록). ⚠️**2R 1차 시도 미완주(INCONCLUSIVE)** — 재실행. ⚠️**ECOUNT 스냅샷 미획득**: Mac 공인 IP가 **30분 사이에 바뀌어** 가드가 막았다(요청 0건) ⇒ 「Mac IP 등록」 전제가 IP 고정을 가정하는데 반증됐다. PR **#495** · ref **105**.

## ⏸ Paused
- (없음)

## ✅ Completed
- [쿠팡 2P(로켓그로스) 손익 정합 — 「쿠팡 손익정합 목표」](tracks/completed/track_coupang-2p-parity.md) — **✅ 종결(부분달성) 2026-08-23 · 20/20**. 목표(Jino 명명, 정본 `.claude/memory/chains/쿠팡-손익정합.GOAL.md`) = **2P가 1P·3P·스마트스토어와 「같은 대접」을 받는다** — ①자기 메뉴와 자기 화면 ②그 날 팔린 것의 손익을 그 날 자리에서(판매일 축). **둘 다 라이브에 섰다**: 사이드바 그룹 「쿠팡 로켓그로스(2P)」(1P와 같은 층) + 화면 `/rocket-growth`(상품별 판매일 축 손익)·`/rocket-growth/settlement`(정산·근거) + 백엔드 조립 `rg_daily_pnl.py`·`GET /api/coupang/rg/option-pnl`. **새 손익 계산 0** — 기존 SA를 「날짜×옵션」 grain으로 조립·표시만. 계약 `CONTRACT_rg_sales_date_axis.md`(승인 08-22 22:00)·`CONTRACT_2p_own_screens.md`(D-CPP-54, 승인 08-23 13:51). ★**트랙 종결 QA 판정(별도 기·실제 브라우저, 08-23 17:55)**: 20항목 **달성 17 / 판정불능 3(ⓖⓙⓝ — 확인 못 함이지 결함 아님)** · **궁극 목표 ①+② 달성** · 「안함」 **이탈 없음**. QA가 스스로 전체 스위트 재실행(백엔드 6,348 passed/2 기존 빨간불 · 프론트 653/653). ★★**남기는 교훈 둘**: ①**「모름」이 「0/아니다」로 접히는 병이 «네 번»** 나왔고(적대 리뷰 1R P1 → 라이브 배지 반전 → 라이브 null falsy → 완료 QA 소계 충돌) **앞의 셋을 고치면서도 넷째는 안 보였다** — 넷째는 값이 아니라 **진실의 원천을 둘에서 하나로** 줄여 닫았다. 그중 둘은 **테스트 636종·변이 26종이 전부 초록인 채로** 통과했고, 배지 반전은 **단위 테스트가 원리적으로 못 잡는 자리**였다(조립 코드가 같은 잔차를 양쪽에 만들어 테스트 입력에선 정확히 상쇄된다). ②**ⓝ의 증거 좌표가 «세 번» 썩었다**(−409,639.09 → −494,006.55 → −436,868.18) — 그 창이 정산 수수료 재동기화에 계속 노출되는 구조라 **합격기준 자체가 재검증 불가능한 형태로 쓰였다.** 절대값 대신 «배포 전후 동일성»으로 쓸 것. ~~⚠️미이행 1건: 계약 §1-A-3의 `RgSettlementCard` 재사용 안 함~~ → ✅**그 뒤 이행됐다**(n=7, 2026-08-23 19:5x): 정의를 `frontend/src/components/RgSettlementCard.tsx`로 추출해 화면 B가 **같은 컴포넌트**를 읽기 전용 재사용(갱신 props는 선택 — 두 화면이 같은 Mac Wing 데몬을 경쟁적으로 안 깨우게). 종합조망 렌더 자리 불변. 완료 QA가 실제 브라우저로 「화면 B ↔ 종합조망 여섯 숫자 원 단위 일치」 확인. PR #368 병합 · prod 프론트 `9d83b23a` · 기록 `CONTRACT_2p_own_screens.md` §11. ⚠️**대신 남은 알려진 결함 1건**(트랙 종결 후 발견 · 미착수 · 새 계약 사안): 종합조망은 정산 원장 계정 row가 **0건인 창에서도** 「✅ 순이익 반영됨 / 정산총액 0원」을 그린다 — `has_data = len(rg_fees) > 0 **or** rg_deducted != _Z`(의도된 OR, `intelligence.py:1334`)라 원장 결손에서도 참이 되는데 `RgSettlementCard:47`이 그 플래그만 본다(화면 B는 `RocketGrowthSettlement.tsx:302`에서 호출부 가드로 막았고 종합조망 `CommandCenter.tsx:875`는 안 막혔다). ★**지금은 안 터진다** — prod 2계정×14일 **28회 관측 중 0회**(`coupang_rg_settlement_fee` 커버리지가 2026-03-02~오늘 연속). 코드 주석의 *"D+12라 거의 매일 그 상태"* 주장은 **현재 관측과 어긋난다**(실측 2026-08-23 22:01 KST, n=8). 종합조망 쪽 이 조합 테스트 **0건** → 소관: 이 목표의 다음 계약.
- [쿠팡 RG 수수료 회계 자동화](tracks/completed/track_coupang-rg-fee-accounting.md) — RG 판매 실청구 수수료(입출고·배송·보관·판매수수료 등)를 윙 내부 API로 옵션 단위 자동수집→종합조망 순이익 반영. **8/8 코드 완료**(S1~S4 대조뷰, S5 회계규칙 잠금, S6-core/auto 옵션단위수집·자동다운로드+scheduler 06:15, S7 net_profit 전액차감 플립(D-16), **S8 과오청구 감사**(사이즈 분류+이상치 스크리닝, GET /rg/fee-audit, prod 22옵션 15플래그)). 전 Sprint prod self-verify 완료. ★2026-06-15 라이브 감사: size_mismatch_high **1건**(아이패드미니필름 91313543029, 등록 극소형 60.5cm vs 배송청구 대형1 3배, 실측값 미확보) — Jino 결정 **자동해제 대기**(다음 입고 PRODUCT_SIZE_COMPARISON 수집 시 자동 판가름, 코드변경 없음). **2026-08-03 Completed.** 유일한 미결이던 size_mismatch_high 1건(옵션 91313543029 아이패드미니 필름, WING2)의 "자동 판가름 대기"가 종료됨 — 쿠팡 실측값이 확보됐고(극소형), 판정 기구도 정상화됐다. ★**2026-08-03 저녁 후속 세션에서 그 '불일치'는 오탐으로 규명됨(PR #190, prod 배포)**: 정산서 대조 결과 주문당 실청구 배송비는 **2,025원**이고 4,050원은 감사가 **주기 4개 합계 8,100을 주문 2건으로 나눈 값**이었다(정산 수집 2026-03-16~ vs RG 주문 수집 2026-06-03 시작 — 분자·분모 창 불일치). 2,025원은 대형1 최소금액 2,200원에도 못 미쳐 청구 등급이 대형1일 수 없다. **과오청구 아님·이의제기 0원.** 같은 원인의 오탐 4건 전부 제거(WING1 3→1·WING2 1→0 라이브 확인). 추가로 **S9**(청구 사이즈·주문수·수량을 정산서에서 직접 읽음, 마이그 `rg9billed7c4e`)와 **감사 프론트 UI**(재고관리>로켓그로스>청구 감사) 완료. 잔여: PR #190 병합(codex 리셋 08-09)·S9 컬럼 신규주기 자동충전 확인·`95501699184` 6주 0원 청구(과소청구 ≈28만원) 수동 대조. 상세는 `.claude/memory/HANDOFF_rg-fee-audit-false-positives+S9_20260803.md`.
- [Wing 세션 자동화](tracks/completed/track_wing-session-automation.md) — **완료 2026-06-15 (6/6)**. wing.coupang.com 내부 API를 세션 만료 없이 자동 호출하는 공용 헤드풀 페처(CDP 모드, Akamai 우회) → (A)매출 자동대조(vendor-summary RevenueDriftCard, ref18 3P+1.83%·RG+7.40% 재현·net_profit 불변) + (B)RG정산 자동수집(rg CLI+데몬 com.ohisell.wing 예약+갱신 버튼). S0~S5 전부 prod 라이브 self-verify. 9종 sellerReportType 코드명 확보(파서=WAREHOUSING_SHIPPING 1종). 후속(선택): 나머지 8종 XLSX 파서.
- [쿠팡 매출·광고 정합성](tracks/completed/track_coupang-revenue-ad-reconciliation.md) — **완료 2026-06-14**. 종합조망 매출·광고·순이익을 쿠팡과 일치 + 계정별 분리. S1~S7(계정분리·2중계상·RG매출편입[52%갭]·net_profit D-3·신선도 reconcile/이중차감 D-12·검산 대시보드·비-PA ALL전환 D-15/커버리지 D-13) + 옵션 30일 확대 + **vendor-summary 프로브로 매출 정합 라이브 1:1 입증**(6/8~6/13 3P+1.8%·RG+7.4%, 전부 문서화 잔차). 자동대조(드리프트 감시)는 → Wing 세션 자동화 트랙.
- [쿠팡 API 전 기능 연결 + 종합 조망](tracks/active/track_coupang-full-integration.md) — 읽기 P1~P7 + 쓰기 W1~W5 전부 prod 배포·라이브 실증 완료(2026-06-04). 잔여: W4·W5 codex 교차검증(별도). ※파일은 active/ 위치 유지(이력).
- [네이버 스마트스토어 커머스 API 전 기능 연결](tracks/completed/track_naver-full-integration.md) — N1~N8 완료(N2 skip). 읽기+쓰기(발주/발송·클레임·판매상태변경) 전부 prod 배포·dry_run 라이브·codex pass (2026-06-05).
