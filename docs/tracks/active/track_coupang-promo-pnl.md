# TRACK — 쿠팡 프로모션 손익 레이어 (coupang-promo-pnl)

> 생성 2026-07-28 · 상태 🟢 Active · Phase 1(수집) 라이브 · **Phase 2(손익 엔진·뷰) prod 배포 완료·687878 실데이터 완결**
> **2026-08-05 D-CPP-10: 제안서 엑셀 자동수집 완료(prod 라이브) — 개당 할인액이 처음으로 시스템에 들어왔다. 7월 분담금 1,720,000원 실측.**
> 원칙 20/21 형식. **확정 결정(D-CPP-N)은 Jino 승인분 — 번복 금지, 변경은 새 D-CPP-N으로.**

---

## 목표 (한 줄)

**셀러 부담 즉시할인 프로모션이 진행 중인 기간의 진짜 손익을 채널별(1P 로켓배송 / 2P RG)로 계산할 수 있게, 프로모션 사실과 실현 판매 데이터를 자동 수집한다.**

지금은 프로모션이 "우리가 얼마를 부담했는지" 시스템 어디에도 없다 —
- **1P(오하이테크·로켓배송)**: 소비자 판매 데이터 자체가 전무(발주 축뿐). 쿠팡이 자체적으로 내린 판매가도, 우리가 신청한 프로모션 분담금도 미수집.
- **2P RG(오픽스·Wing)**: 쿠폰 메타(명칭·할인액·기간·옵션)는 수집 중이나 **실사용 금액(=우리 실부담)** 은 미수집. `coupang_rg_order_item.unit_sales_price`는 등록가라 할인이 안 보인다.

---

## 확정 결정 (D-CPP-N)

### D-CPP-45 — 쿠팡 오픽스 광고비 갱신을 일일 자동 트리거에 편입 (2026-08-12, PR #290 · prod 라이브 합격)

> Jino 승인(2026-08-12 오후): 「편입한다」 — 선택지 3안(편입 / 지금은 안 함 / 오하이테크 제외) 중 1안.

**무엇이 문제였나.** 오픽스 광고비(`COUPANG_ADS1`)의 **유일한 트리거가 UI 「광고비 갱신」 버튼**이었다.
- 서버 크론 `sync_coupang_ad_cost`(00:10)는 `is_enabled=0`이고, **Akamai가 prod IP를 막아
  직접 fetch가 원리적으로 불가**하다(`ad_cost_sync.py` 모듈 주석).
- 워치독(09:20)의 `RESCUE_STREAMS`은 `frozenset({"supplier_hub"})`뿐이라 광고비는 **알림만** 간다.
  뺀 근거는 「소급 창 30일이라 자가 복구된다」였고 **그건 지금도 참이다.**

**그래서 이 결정이 막는 것은 «영구 소실»이 아니다** — 그건 30일 창이 이미 막고 있다.
막는 것은 **«며칠 동안 손익 화면이 낡은 광고비로 구르는 것»**이다. 실측(2026-08-12, push 기준):

| 구간 | 공백 |
|---|---|
| 2026-06-24 → 07-04 | **10일** |
| 2026-08-10 → 08-12 | 그날 **29일치가 한꺼번에** 적재 |

`cost_date` 결측은 **0건**이다 — 늦게 눌러도 30일 창이 메웠다. 즉 데이터는 살았지만
**「사람이 배너를 보고 누른다」는 전제가 반복적으로 안 지켜졌다.** (오늘 08:48 갱신도 Jino가
아니라 세션이 눌러서 풀렸다.)

**변경** (D-CPP-40/D-CPP-36이 판매분석에 한 것과 같은 패턴 — 새 경로를 만들지 않고 검증된
`request_refresh`/lease/페처에 **트리거만** 붙인다):
- `scheduler_service.request_coupang_ad_cost_daily_job()` 신설 — `ad_cost_sync.request_refresh(db)` 호출.
- 크론 `("request_coupang_ad_cost_daily", "25 5 * * *")` — 쿠팡 수집대(05:30~) 앞이면서
  판매분석 요청(05:20)과 1분 이상 벌린다.
- `job_func_for` 매핑 등록.
- 옛 「ofix 광고비 장중 자동 갱신 크론은 제거됨」 주석 갱신 — **«자동 fetch»는 여전히 없다.**
  되살린 것은 «요청 트리거»뿐이고 실제 수집은 Mac 페처가 한다.
- **`WATCHDOG_JOBS`에 등록** — 이건 서브에이전트가 스코프 밖이라 뺐던 것을 되돌린 판단이다.
  ★근거: 이 잡을 만든 동기가 «사람이 안 누른다»인데 그 대체물을 감시 없이 두면 실패 모드가
  **«크론이 조용히 죽는다»로 옮겨갈 뿐**이다. 게다가 광고비 나이 규칙도 `max_age_days=3.0`이라
  잡이 죽어도 「정체」로 울리기까지 3일이 걸린다 — 판매분석 트리거를 넣은 것과 완전히 같은 근거다.

**안 건드린 것**: `sync_coupang_ad_cost`(00:10) 행 · 신선도 규칙 `coupang_ad_cost_sales`
(max_age_days=3.0, **자동화가 감시를 대체하지 않는다**) · 오하이테크 광고(`ohitech_ad_sync`, 범위 밖).

**라이브 합격**(prod `scheduler_state` + 헬스 API HTTP body): `request_coupang_ad_cost_daily` =
`25 5 * * *` · `is_enabled=1` · `never_succeeded` **0건**(신규 잡 유예 작동 — 헛알림 없음) ·
`sync_coupang_ad_cost`는 `is_enabled=0` 그대로. 첫 발화는 2026-08-13 05:25.

**테스트** 5건 + 변이 **7/7 KILLED**(크론값 변경 / defaults 등록 삭제 / `request_refresh` 호출 삭제 /
`raise` 삭제 / 신선도 규칙 삭제 / `job_func_for` 매핑 삭제 / `WATCHDOG_JOBS` 등록 삭제).

### D-CPP-44 — WING1을 보존식 감시(`CONSERVATION_ACCOUNTS`)에 편입 (2026-08-12)

> 배경: D-CPP-40이 WING1을 판매분석 일일 자동수집에 넣었지만, 그때는 옵션축 행이 0건이라
> 감시에 넣으면 `summary_only`만 매일 쌓였다. 그래서 **조건부 보류** + 편입 조건을 코드 주석에
> 박아 뒀다("옵션축이 한 번이라도 들어오면 추가한다").
> 2026-08-12 08:44 Jino가 쿠팡 Wing에 로그인하자 옵션축이 최초 적재됐다(184행).

**편입 «전에» 보류 사유가 사라졌는지 prod로 실측했다**(창 2026-08-05~08-12):

| 계정 | compared | mismatch | summary_only | option_only |
|---|---:|---:|---:|---:|
| COUPANG_WING1 | 13칸 | 0 | 0 | 0 |

즉 편입해도 노이즈가 0이다. **«조건이 충족됐다»를 인계문서의 문장이 아니라 관측으로 확인하고
넣었다** — 그 문장 자체가 실측 대상이다(핵심 사실 `handoff-lists-must-be-remeasured`).

같이 확인한 계약: `CONSERVATION_WINDOW_DAYS`(7) == 페처 `vi_days`.
`~/.ohisell_wing_fetcher.json`(WING1)에 `vi_days` 키가 **없어** 기본 7이 적용되고, 실제 적재된
WING1 행이 08-05~08-11 **정확히 7일**이라 라이브로도 맞는다. 한쪽만 넓히면 «아무도 다시 받지
않는 구간»에 보존식을 단언하게 되어 수리 경로 없는 영구 빨강이 된다(적대 리뷰 P1-3, D-CPP-40).

**변경**: `scheduler_health.CONSERVATION_ACCOUNTS = ("COUPANG_WING1", "COUPANG_WING2")`
**테스트**: 종전 `test_conservation_accounts_excludes_wing1`(제외가 «의도»임을 못 박던 것)을
편입 계약 3건으로 교체 — ①두 계정 다 본다 ②WING1 불일치가 **WING1이라는 이름을 달고** 표면에
나온다 ③검사창이 7에서 조용히 바뀌지 않는다. 변이 4/4 KILLED.

### D-CPP-43 — **D-16 폐기**: RG 정산 광고비는 PA의 «공제»다, 차감하지 않는다 (2026-08-12, PR #287)

> Jino 질문(2026-08-12 08:59): *"오하이테크의 1P, 3P 광고관리처럼 오픽스도 똑같이 보면 되는거
> 아니야?"* → *"오하이테크의 1P, 3P와 같이 딱 맞아떨어지게 구조짤때까지 진행해줘"*
> 이어서: *"왜냐하면 2p의 경우는 수수료 구조도 다르잖아"* · *"우리 예전에 2P의 수수료에 대해서
> deep하게 조사한적이 있었어"* (→ ref 04·ref 17)

#### 1차 출처 (윙 로그인 라이브, 2026-08-12 09:0x)
윙 > 정산 > 로켓그로스 정산현황 > **「광고비 내역」** `/tenants/rfm-portal-2/cmg/settlement`

> *"로켓그로스 상품의 광고비는 해당 월 말에 계산되어 **매입세금계산서 1건이 발행**되며, 광고비
> 청구예정금액과 다를 수 있습니다."*
> *"로켓그로스 정산시 지급액보다 **공제할 금액(광고비)이 큰 경우 공제하지 못한 남은 광고비는
> 다음 정산으로 이월**됩니다."*

2026년 8월 상세내역 — **광고유형이 전부 `PA`**이고 **캠페인 이름이 광고센터와 그대로 일치**:

| 광고유형 | 캠페인 | 클릭 | 광고비 |
|---|---|---:|---:|
| PA | AI스마트광고 | 443 | 308,851 |
| PA | [매.최] 골프필름 | 385 | 367,422 |
| PA | [매.최] 카드케이스 | 114 / 208 | 51,336 / 74,603 |
| PA | [매.최] 플립, 폴드 지문방지 내부+사생활외부 | 27 | 31,205 |
| PA | [매.최] 사생활 지문방지필름 | 30 | 78,585 |
| PA | 〃_8 시리즈 | 189 | 234,055 |
| | **청구가능 광고비 합계** | | **1,146,057** |

우리 `coupang_ad_entity_snapshot`의 오픽스 **활성 캠페인 6종과 동일**하고, 우리 PA 08-01~11
합계 **1,209,847원**과도 규모가 맞는다(화면이 *"월 말에 계산… 청구예정금액과 다를 수 있다"*고
실토하는 미확정 상태).

→ **정산 `ad_sales`는 별개 비용이 아니라 광고센터 PA 광고비를 정산에서 «공제»하는 것**이다.

#### D-16이 왜 틀렸나 — 그리고 이건 «발견»이 아니라 «복원»이다
D-16의 근거는 `sell_type=2P` **0행 실증**이었다. 2P는 지금도 0행이다 — 그런데 **라벨이
판매경로를 뜻하지 않는다**(오픽스 PA 광고비의 97.28%가 RG로 팔리는 옵션에 쓰이고, 광고비
상위 5개 옵션의 3P 주문은 0건. ref 56 §3). 그래서 D-16이 걸어둔 감시 장치
(`ad_xlsx_rg_overlap>0`)는 2P 라벨을 보므로 **이 형태를 원리적으로 못 잡았다**(교훈 #261).

★**이미 두 문서가 맞는 말을 하고 있었다**:
- `ref 04 §2`: *"RG 정산: 최종 판매가 − 판매수수료 − **추가비용(광고비 등)** − RG 서비스이용비 후 지급"*
- `ref 17 §7`: *"RG 광고비(d)는 이 RG 정산 안에도 있음(추가상계 I). 우리 ad_costs와
  **이중계상 주의** — 회계 합산 시 출처 정합 필요."* (2026-06-08)

D-16(2026-06-09)이 **그 다음 날** 라벨 증거로 이 둘을 덮었다. 교훈 #268(main 병합 시 #265→#268 재번호, PR #287).

#### 고친 것
1. **차감**: `apply_rg_net_profit_flip(net_profit, rg_total)` → **`rg_total − ad_sales`**
   (= 구 D-15의 「광고 제외 차감」 모양으로 복귀. D-15는 결론이 맞고 근거가 틀렸던 것이다.)
2. ★**매입세액도 같이**: `payable_vat(...)`에 넘기던 `rg_total`(광고 포함) → 광고 제외분.
   종전엔 PA 광고비의 매입세액이 `ad_spend`로 한 번 + `rg_total` 안의 `ad_sales`로 또 한 번
   **이중 공제**됐다. 윙 화면이 *"매입세금계산서 **1건**"*이라 못 박으므로 한 번만 인정해야 한다.
   **차감만 고치고 VAT를 안 고치는 «반쪽 수정» 변이가 테스트에 잡힌다.**
3. 새 필드 `rg_settlement_deducted`(실제 차감액) · `flip_status: "applied_ex_ad"`
4. **화면**(Jino 선택: 「계산 + 손익 화면에 광고비 내역까지 드러내기」 · 「표시는 유지하되
   차감 안 함을 명시」): 헤더 「전액 차감」→「광고 제외 차감」, 광고비 행은 **취소선 + (차감 안 함)**,
   **정산 총액**과 **순이익 차감액**을 분리 표시, D-16 근거를 주장하던 각주를 1차 출처 문구로 교체.
   ★2P 수수료 구조가 3P와 다르다는 것(판매수수료/풀필먼트/반품/광고 항목별)은 **유지**한다 —
   ref 17 §1의 9종 체계가 그 축이다.

#### 라이브 결과 (정렬 창 2026-07-13~08-09, prod 재시작 후 `applied_ex_ad` 확인)
| 계정 | before | after | Δ |
|---|---:|---:|---:|
| **오픽스 WING1** | −2,048,579.76 | **+1,731,945.69** | **+3,780,525.45** |
| 오하이테크 WING2 | 451,646.20 | **451,646.20** | **0** |

★Δ 검산: 광고 4,158,578 덜 뺌 − VAT 378,052.55(=4,158,578÷11) 더 냄 = **+3,780,525.45** ✓
★사슬 검산: `net_profit_pre_rg − rg_settlement_deducted == net_profit_pre_shipping` 잔차 **0.00**(양 계정)
★**오하이테크 불변이 「같은 축」의 증거다** — 정산에 광고가 없는 계정(ad_sales 27주기 전부 0)은
이 변경에 영향받지 않는다. 두 계정이 이제 **같은 코드 경로**로 설명된다.

#### 검증
전체 **5412 passed** · 프론트 **266 passed** · `tsc -b` clean · 변이 **3/3 KILLED**
(차감 되돌리기 · VAT만 되돌리는 반쪽 수정 · flip_status 옛 값).
D-16을 못 박던 테스트 4건은 삭제가 아니라 **새 결정을 못 박도록 재작성**.

---

### D-CPP-42 — 빈티지 원가의 정본은 별도 파일이다 + 오픽스 원가 커버리지 **전 기간 0.000%** (2026-08-12)

> Jino 지시(2026-08-12 08:51): 「빈티지 총괄표_v1.xlsx」 경로 제시 + *"표에 있는 원가와
> 자체상품번호를 오픽스 상품번호와 비교해보면 비교가 될꺼야"*

#### 원가 정본이 하나가 아니다
`MD_원가 계산_Jino_260807.xlsx`(필름·케이스, `backend/app/data/cost_truth_20260807.json`으로
스냅샷됨)와 **별개로**, 빈티지 의류는
`GoogleDrive/Ohi/41. 빈티지/빈티지 총괄표_v1.xlsx` **[하의] 시트**가 정본이다.
- 헤더 행 **21** · **C열 「원가(+vat)」** · **L열 「자체상품번호」**(`LV…` 형식, 79종)
- 필름 정본과 마찬가지로 **(+VAT) 열이 정본**이다.

#### 연결 근거 — 추론이 아니라 식별 (세 겹)
1. 쿠팡 `seller_product_name` **끝에 자체상품번호가 그대로 박혀 있다**(예: `… 34사이즈 LV50526`)
2. **표의 소비자가 = 쿠팡 판매가**가 4건 모두 정확히 일치(179,000 / 159,000 / 87,000 / 85,000)
3. 4개 코드 모두 79종 안에서 **유일**(접두사 충돌 없음 — `LV5507`처럼 짧은 코드도 확인)

| 자체상품번호 | 오픽스 옵션 | 원가(+vat) | 판매가 |
|---|---|---:|---:|
| LV50526 | 94365168294 | 60,000 | 179,000 |
| LV55025 | 94382069823 | 60,000 | 159,000 |
| LV55033341 | 94156365382 | 35,000 | 87,000 |
| LV5507 | 94156365627 | 35,000 | 85,000 |

`internal_sku`는 **자체상품번호를 그대로** 썼다(id 957~960, category=`빈티지`).
★memo에 **「빈티지는 1점 1개체 — 이 원가는 이 개체의 매입가이고 재사용하지 않는다」**를 명시했다.
필름·케이스 원가가 «기종별 공통»인 것과 성질이 정반대다.

#### 동시에 드러난 것 — 새 판매가 새 미연결을 만든다
08:44 판매분석 갱신으로 최신 주문이 들어오자 **미연결 3건이 새로 생겼다**(옵션 169→172):
`갤럭시S25울트라 2세트/3세트` · `갤럭시S23울트라 2세트`. 전부 이미 승인된 **선형 규칙**
대상이라 파생 master 3건을 만들어 이었다(`OHI-0924-2SET` 5,672 · `-3SET` 8,508 ·
`OHI-0918-2SET` 5,672).
★**방증**: 이 리스팅은 **판매가도 15,900×N으로 정확히 선형**이다(2세트 31,800 · 3세트 47,700) —
Jino의 선형 원가 규칙을 판매가 구조가 독립적으로 뒷받침한다.
★**함의**: 커버리지는 **한 번 0%를 만들고 끝나는 값이 아니다**(교훈 #263). 새 옵션이 팔리면
다시 생긴다 — 상시 감시가 필요한 축이지 일회성 작업이 아니다.

#### 라이브 결과 (2026-08-12 08:5x)
| 창 | 미연결 |
|---|---|
| 30일 07-12~08-10 | **0건 / 0원 / 0.000%** |
| **전 기간 01-01~08-12** | **0건 / 0원 / 0.000%** |

`cost_covered_options` **273** · 전 기간 `revenue` 50,997,640 / `cost` 10,350,994.40 /
`net_profit` −7,594,694.21. 이중귀속(같은 옵션ID가 서로 다른 원가) **0건**.

---

### D-CPP-41 — 오픽스 RG 원가 미연결 45건 종결: 원가는 계정이 아니라 «상품»에 붙는다 (2026-08-12)

> Jino 확정 2026-08-12 00:5x, 원문: **"원가는 SellC에 설정되어 있는 오하이테크의 1P, 3P에서
> 사용하는 원가가 오픽스 2P, 3P에도 똑같이 작용되. 따로 보지 말고, 거기 있는지를 먼저 확인해"**
> 그리고 **"선형 — 1세트 원가 × N"**(N세트 규칙) · **"전건 승인"**((A) 근거완비 10건)

#### 이 결정이 바꾼 것 — 탐색 순서
질문이 「이 옵션의 원가값은 얼마인가」가 아니라 **「같은 상품이 오하이테크 쪽에 이미 있는가」**다.
ref 55는 이걸 「매수(N세트) 질문 하나」로 봤는데, Jino의 답은 그 질문 자체를 바꿨다 —
**계정별로 원가를 새로 정하지 않는다.** 실측 결과 45건 중 **(C) master 없음 = 0건**,
즉 신규 원가 창작은 한 건도 필요 없었다.

#### 결과 (라이브, 창 2026-07-12~08-10, `account=COUPANG_WING1`)
| | before | after |
|---|---:|---:|
| `by_option` 원가 미연결 | **45건 / 3,772,270원 (24.67%)** | **0건 / 0원 (0.00%)** |
| `cost_covered_options` | 158 | 203 |
| `cost` | 2,401,157.20 | 3,075,429.50 (+674,272.30) |
| `net_profit` | −7,661,767.04 | −8,274,741.85 (−612,974.81) |

★**순이익이 «나빠진» 것은 회귀가 아니라 이제 제대로 뺀 것이다**(교훈 #192·#193과 같은 성격).
차액 검산: 674,272.30 − 612,974.81 = 61,297.49 = 674,272.30 ÷ 11 → **매입세액공제(1/11)**. 사슬 정합.

#### 상품군별 근거 (7개 상품 / 45옵션)
| 상품군 | 건 | 연결 대상 | 근거 |
|---|---:|---|---|
| 전면 3D 풀커버 TPU 지문방지 무광 | 24 | `OHI-3D-TPU-MATTE` 2,350.7 | **정본 엑셀 r19 「지문방지필름 TPU 3매」=2350.7**. 정본에 **TPU 1매 줄이 없다** + 그 리스팅 전 옵션이 기종별 1개씩·전부 15,900원 동일가 → 「1개」=「1세트」=3매 팩 하나 |
| EZ툴 프라이버시 강화유리 2p | 6 | `OHI-0709/710/712/713/717/719` 4,880 | (A)승인. 오하이테크 ch2·ch5에 기종별 master 존재 |
| 풀커버 강화유리+이지솔루션 | 4 | `OHI-0736/737/745` 3,500 | 같은 리스팅 형제가 이미 맥스클리어 master에 연결(선례) |
| 맥세이프 이지 카드지갑 투명 | 3 | `OHI-0907` 5,082 | (A)승인. 같은 WING1 동일상품 기연결 |
| Z플립폴드 4매 N세트 | 6 | `OHI-0910/0911/0912`-2SET/3SET (파생) | **선형 규칙** |
| 사생활 저반사 2매 3세트 | 1 | `OHI-0927-3SET` 8,508 | 1세트 `OHI-0927`(2,836) × 3 |
| 맥세이프 하이브리드 소다 | 1 | `OHI-0171` 4,532 | (A)승인. 오하이테크 1P(ch5)에 존재 |

#### ★파생 master 7건을 «만든» 이유 — 매핑에 수량 축이 없다
`product_channel_mapping`에 수량 필드가 없고 엔진은 `cost = unit_cost × net_qty`
(`intelligence.py:878`)다. 즉 **3세트 옵션을 1세트 master에 붙이면 3배 과소계상**된다.
그래서 `OHI-0912-3SET`(19,218) 같은 파생 master를 만들었다. memo에 「파생이며 1세트 값이
바뀌면 같이 바꿔야 한다」를 명시했다 — **정본 엑셀에 N세트 줄이 따로 없으므로 이 값은 파생이다.**

#### ★2라운드 — 「45건 종결」은 창 의존이었다 (적대 리뷰 P1-2 수용)

리뷰어 지적: 합격기준 ①을 **30일 창**(07-12~08-10)에서만 쟀다. 창을 넓히면 여전히 미연결이
남고, **그 대부분이 방금 매핑한 바로 그 리스팅의 형제 옵션**이다. 원인은 대상을
「상품군/리스팅」이 아니라 「최근 30일 매출이 있는 옵션」으로 뽑았기 때문이다
(`[[subset-profit-overstates-margin]]`·`[[handoff-lists-must-be-remeasured]]`의 정확한 재현).

전 기간(2026-01-01~08-12) 엔진 기준 재측정 → **14건 / 852,600원 (1.686%)**. 성격이 갈렸다:

| 묶음 | 건 | 처리 |
|---|---:|---|
| G1 3D TPU 형제 | 6 | ✅ `OHI-3D-TPU-MATTE` 2,350.7 (동일 규칙) |
| G3 EZ툴 형제(아이폰15플러스) | 1 | ✅ `OHI-0716` 4,880 |
| G4 이지솔루션 형제 | 2 | ✅ `OHI-0736`·`OHI-0741` 3,500 |
| 도어락 문캅스 사생활 1세트 | 1 | ✅ `OHI-DOORLOCK-PAF-1SET` 2,930.4 (선례 `95598292077` + 정본 r73) |
| **올드스티치 리바이스 빈티지 청바지** | **4** | ❌ **보류 — Jino 확인 필요** |

★**보류 4건**(`94365168294` 179,000 · `94382069823` 159,000 · `94156365382` 87,000 ·
`94156365627` 85,000, 합 **510,000원**)은 **빈티지 중고 의류**라 원가가 개체별 매입가다.
필름·케이스 원가 규칙이 통하지 않고 정본 엑셀에도 없다 — **추정 금지 금지선**에 걸리므로
Jino가 매입가를 주기 전엔 넣지 않는다. (ref 17 §7의 「청바지 건」과 같은 계열이다.)

★**보고 방식 교정**: 앞으로 원가 커버리지는 **창을 명시**해서 말한다.
- 30일 창(07-12~08-10): **0건 / 0.00%** ✔
- 전 기간(2026-01-01~08-12): **4건 / 510,000원 / 1.01%** — 전부 청바지, Jino 대기

★**G1의 「1세트=3매」가 리스팅 이름으로 확인됐다**: 이번에 걸린 `95570603379`의 옵션명이
「오하이 전면 3D 풀커버 TPU 지문방지 무광택 휴대폰 액정보호필름 **3p** + 부착 가이드툴 세트,
아이폰 15, **1세트**」다 — 같은 리스팅이 「3p」와 「1세트」를 **한 이름 안에서** 쓴다.

#### 세는 법 함정 (다음 세션용)
- 진단 SQL은 `channel_id IN (1,3)`으로 조인했지만 **엔진은 `Channel.platform='coupang'` 전체**를
  본다(`intelligence._cost_master` 716-720행). 채널을 좁혀 세면 엔진과 다른 수가 나온다.
  **합격 판정은 라이브 API `by_option.has_cost`로 한다** — 그게 엔진이 실제로 쓰는 값이다.
- 매핑은 전부 `mapping_source='manual'`(자동동기화가 조용히 덮지 않게, `product_sync.py:106` 가드와 짝)
- 정정 경로: 생성한 master 7 + 매핑 45의 id 전량이 세션 scratchpad `rollback.json`에 있다

---

### D-CPP-40 — 오픽스(WING1) 판매분석 자동 수집 편입 + 신선도 감시 2축 (2026-08-12, PR #286)

#### 문제 — 「3P가 비었다」로 계정 전체를 판정했다
`request_wing_vendor_summary_daily_job`의 트리거 튜플이 `("COUPANG_WING2",)`뿐이었다.
제외 근거 주석은 *"오픽스 3P는 RG로 이관돼 판매분석 3P가 사실상 비어 있고(90일 3P 옵션 3개)"*.
그 **관찰 자체는 참**이다(WING1 NORMAL 90일 5,139,710원). 틀린 것은 **거기서 계정 전체를
빼는 결론**이다 — 같은 판매분석이 싣는 **RFM(RG)축이 63일 39,312,430원**으로 이 계정의
지배 축이고, 오픽스 매출의 **98.9%가 RG**다.

#### ★「수집 불가」가 아니었다 — 트리거 누락이었다
- 수집 경로는 계정 무관 범용: 데몬 · ingest 엔드포인트 · UI 버튼 `ofix_sales`(지금도 살아있음)
- 세션도 green, 요약축 **126행 06-07~08-08 연속 적재**
- 데몬 `com.ohisell.wing` 상주 확인(pid 실측) · config에 `vs_days`/`vi_days` 없음 → 기본 45/7
  → **옵션축도 같은 실행 경로로 흐른다**(ref 55 §6 미확인 항목 2건이 여기서 닫힌다)
- 그냥 **사람이 버튼을 눌러야 했고 마지막 클릭이 08-09**였다

#### ★ref 55의 「정본 대조가 무효」는 정정한다
창을 적재 범위 안(07-13~08-08)으로 잡으면 **`wing_used: true`, `factor_rg: 0.925`**로
안분이 실제로 걸린다. 대조가 없는 게 아니라 **창이 적재를 넘어설 때 꺼진다**(완전성 게이트).
그래서 갭②의 본질은 「대조 신설」이 아니라 **「적재를 안 멈추게 하는 것」**이다.

#### 라이브 합격 (2026-08-12 01:1x~01:2x KST)
- 배포된 prod 파일에 `for account_key in ("COUPANG_WING1", "COUPANG_WING2")` 확인
- 크론과 같은 잡 실행 → **WING1 `requested: true` (01:20:23)**, 두 계정 각자 요청(큐 도난 없음)
- 헬스 배너: `wing_vendor_summary/COUPANG_WING1` **stale age=4.06 > 3.0** ·
  `wing_vendor_item_sales/COUPANG_WING1` **no_data** ← 종전엔 둘 다 조용했다

#### 변이 4/4 KILLED — 그중 하나는 **기존 테스트가 못 잡던 것**
`max_age_days`를 3.0 → 999.0으로 바꾸는 변이가 **종전 테스트 49건을 전부 통과했다.**
규칙의 «존재»만 단언하면 임계값 무력화가 살아남는다 — 목록엔 있는데 영원히 안 울리는 감시선.
`test_sales_analysis_thresholds_are_pinned_not_just_present` 신설로 막았다.

#### ★남은 선행 조건 — IP가 아니라 «쿠팡 Wing 로그인»이다 (적대 리뷰 중 정정)

세션 도중 「Mac IP가 nginx 허용목록 밖이라 페처가 prod에 못 닿는다」고 적었는데 **틀렸다.**
경로별 실측(2026-08-12 01:4x KST):

| 경로 | Mac에서 |
|---|---|
| `/api/coupang/ops/...` (데몬이 폴·푸시하는 경로) | **HTTP 200** |
| `/api/scheduler/health` · `/api/health` | HTTP 403 |

즉 **데몬은 prod에 닿는다.** 403은 헬스 경로에만 걸리고, 그래서 `safe_deploy.sh`의 무중단
전환 vhost 검증만 실패한 것이다(`--restart-legacy`로 우회).

**진짜 막힌 것은 쿠팡 Wing 로그인 만료다.** 이 잡을 켠 직후 라이브 관측(Mac 로그, 시각은
CST 기록이라 +1h 해야 KST):

```
01:20:37 KST  갱신 요청 감지 — fetch 시작        ← 내가 만든 요청을 즉시 집었다
01:20:49      세션 만료 의심 — 대시보드 재진입 후 재fetch
01:20:53      자동 회복 실패 — 창에서 로그인하세요(자동 감지, 최대 180초)
01:23:54      vendor-summary 실패 status=None — 'login' 재실행 필요
```

★그리고 `coupang_wing_cookie`는 `COUPANG_WING_VS` **status=green**인 채로 `last_error`에
「로그인 필요」를 담고 있다 — 이 리포가 이미 두 번 겪은 **green-while-dead**다. WING2도 같다
(`COUPANG_WING_VS2`, 같은 시각 동일 실패). **「세션 green」을 살아있음의 근거로 쓰면 안 된다.**

**Jino가 할 일**: Mac에 떠 있는 Chrome(쿠팡 Wing)에서 로그인. IP·nginx는 손댈 필요 없다.

**이 관측이 D-CPP-40을 오히려 정당화한다** — 신선도 규칙이 없었으면 이 로그인 만료는
`data_stale` 어디에도 안 뜨고 오픽스 판매분석이 조용히 계속 낡았을 것이다.

---

### D-CPP-39 — 쿠팡 광고비·로켓1P 정산을 신선도 감시(`DATA_FRESHNESS_RULES`)에 편입 (2026-08-11 밤, PR #285)

> Jino: *"어떻게 뭔가 할때마다 마무리가 안되고 맨날 할 일이 또 생겨?"*(2026-08-11 23:3x,
> §6 8건 전수 실측의 계기 — 교훈 #256) → 그 실측 직후 **판정 목록 자체의 커버리지**를 확인하다
> 발견.

#### 문제 — 판정 목록에 없으면 배너는 아무 말도 하지 않는다
`DATA_FRESHNESS_RULES`가 4개(RG 정산 2 + 판매분석 2)뿐이라 쿠팡 광고비·로켓1P 정산은 멈춰도
헬스가 조용했다. 라이브 실측(2026-08-11 00:0x KST)에서 그 상태가 그대로 관측됐다:
- 광고비 최신 `cost_date`=08-09(나이 2일), 1P정산 최신 `issue_date`=08-08(나이 3일)
- 그런데 `GET /api/scheduler/health`의 `data_stale` = **0건**

광고비는 `net_profit` 차감 축이라 멈추면 순이익이 틀려진다. 같은 계열 사고가 이미 있었다 —
판매분석이 13일 멈췄는데 `healthy: true`였다(D-CPP-36, 교훈 #239). #239는 원인(감시 규칙을
늘릴 자리가 코드에 없음)을 고쳤을 뿐, **그 자리에 실제로 규칙을 채우는 것**은 별도 작업으로
남아 있었다 — 이번 결정이 그 나머지다.

#### 결정
`DATA_FRESHNESS_RULES`에 2건 추가:
- `ad_cost`(`coupang_ad_cost_sales`, `account_key=COUPANG_WING1` — 광고센터 광고주로 잡히는
  계정은 WING1뿐이므로 표시용 account_key도 WING1) — `max_age_days=3.0`
- `rocket_settlement`(`rocket_1p_settlement`, `account_key=COUPANG_WING2`) —
  `max_age_days=10.0`

**임계 근거(추정 금지, 라이브 분포 실측)**:
- `ad_cost` 3.0 — report/SALES 축(`vendor_id=ADV_SALES`)은 최근 21일 중 20일 적재되는
  **일별** 소스. 정상이면 최신 `cost_date`는 어제(age≈1), 한 회차 놓치면 2 → 3.0이면
  **두 회차 연속 실패**에서만 울리고 정상 운영에선 조용하다(판매분석 규칙 3.0과 같은 논리).
- `rocket_settlement` 10.0 — 최근 90일 `issue_date` 간격 분포: 1일×47 · 2일×7 · 3일×4 ·
  **7일×2**(관측 최대 7). 7.0으로 잡으면 정상 운영에서 두 번 울려 알림 피로가 생기고, 그게
  이 감시선 계열이 실제로 죽는 방식이다(WING1을 판매분석 규칙에서 뺀 것과 같은 이유). 10.0 =
  관측 최대 7 + 여유 3.
- `vendor_id`는 새로 하드코딩하지 않고 기존 상수를 재사용한다(`_SALES_KEY`,
  `ROCKET_1P_VENDOR_ID`) — 진실이 두 곳에 생기지 않게.
- ★두 규칙의 `account_key`는 **표시용**(배너 문구·verdict 라벨)이고 쿼리 필터는 vendor_id로
  건다 — 기존 4개 규칙은 account_key가 **실제 필터**라 같은 필드가 규칙에 따라 다른 역할을
  한다. 적대 리뷰 P2 채택으로 이 비대칭을 코드 주석에 명시했다.

#### 검증
전체 **5410 passed** · 변이 **3/3 KILLED**(임계 무력화·vendor 필터 제거·규칙 삭제) · 적대 리뷰
**1R PASS(P1=0)** — 리뷰어가 자체 변이 1건을 추가해 KILLED 확인 + `TestClient`로 HTTP
body까지 두 규칙이 실제로 실려 나가는 것을 확인(스위트는 서비스층 dict까지만 봄).
- P2 채택 1: account_key 표시용/필터용 비대칭을 주석으로 명시(위 결정에 반영)
- P2 기각 1: 임계 표본 한계(재현 불가) — 기존 `rg_settlement` 14일과 같은 방법론이라 이번
  스코프에서 추가 조치 없음

#### 라이브 배포·확인 (prod, 2026-08-11 밤)
백엔드 무중단 3단계가 또 403에 막혀 `--restart-legacy` 1회(다운타임 있음, pid `3711063`→
`3737723`). 프론트 변경 **0줄**.
- 배포 전: `data_stale` = 0건
- 배포 후(2026-08-12 00:22 KST 재확인): `data_stale`에 `coupang_ad_cost_sales`
  `state=stale age_days=3.0155 max_age_days=3.0`가 실제로 뜬다. `rocket_1p_settlement`는
  조용(설계대로 — age가 아직 10일 미만).
  배너 문구: 「쿠팡 광고비(report/SALES) 정체 — net_profit 차감 축이 낡은 값으로 구른다」
- 이 age 상승은 **회귀가 아니라 정상 동작**이다 — 원인은 §5(403, Mac IP 회전으로 광고비
  수집이 지연)이지 이 결정의 결함이 아니다.

#### PR 경계
PR #285. ★**CI가 리포 전체에서 정지 상태**(러너 미할당, job에 스텝 자체가 없음 — 문서만
바꾼 커밋도 failure, main의 다른 브랜치도 전부 failure)라 `--force` 병합했다(자백 기록됨).
FAIL이 아니라 **INCONCLUSIVE**다(교훈 #123). 근거는 로컬 5410 passed + 위 prod 라이브 재확인.

### D-CPP-38 — 광고센터 소스 미적용 계정에는 `ad_confirmed_*`를 0이 아니라 **`None`**으로 준다 + 판정 플래그 (2026-08-11, 브랜치 `claude/ad-basis-truth`)

> Jino: *"남은것 셋을 지금 진행하자"*(2026-08-11 10:01) · *"화면 시각 확인해봐"* · 광고 결함
> 수정 승인 *"그래"*.

#### 문제 — 게이트는 옳았다, 표현이 거짓말을 했다
D-CPP-32/D-CPP-28의 게이트(오픽스 계정처럼 광고센터 report/SALES가 안 태깅되는 계정에
`pnl.ad_spend`를 남의 계정 값으로 차감하지 않는다)는 그대로 맞는 결정이다. 문제는 그 «미적용»
상태를 코드가 `{"pa": 0, "total": 0, "nonpa": 0}`으로 실었다는 것 — 프론트 폴백 `?? s.ad_spend`는
**null만** 잡으므로 0이 «측정된 0원»처럼 그려졌다. 화면은 「전체 광고비(ALL) 0원 · net_profit
차감 기준」이라 **단정**했지만, 같은 화면 요약·옵션표는 **11,247원**을 보이고 있었다.
★**돈은 안 틀렸다(차감액은 처음부터 옵션축 실측값이었다) — 실토가 틀렸다.**

#### 결정
- 광고센터 소스가 **적용되지 않는 계정**에는 `ad_confirmed_{total,pa,nonpa}`를 **`None`**으로
  준다(0이 아니다) + 판정 플래그 `ad_confirmed_applies`를 같이 싣는다.
- 화면은 미적용이면 라벨을 「광고 (옵션축 — 광고센터 미태깅 계정)」으로 바꾸고, **값은 옵션축**
  (=실제 `net_profit` 차감액)을 쓴다. 「집행(DELIVERED)」 분해 행과 각주의 「ALL로 차감·분해」
  문구도 **같이** 갈랐다 — 행만 고치면 각주에서 거짓이 되살아난다(교훈 #252).
- 비-PA는 「—」(값을 모른다)로 표시한다. 값이 없다는 사실을 숨기지 않는다.
- ★**«측정된 0원»은 그대로 0원이다**(광고주 계정에서 실제로 0원 집행된 날은 사실이다) —
  「전부 null로 주기」식 과잉 수정을 테스트가 죽인다(변이 주입으로 확인).

#### 라이브 배포·검증 (prod, 2026-08-11)
백엔드 `intelligence.py` 배포 후 무중단 3단계가 **또 403**에 막혀 가드가 전환을 되돌림 →
`--restart-legacy`(다운타임 약 50초)로 재시작(pid `3657531`→`3676132`). 프론트 2회 배포
(`index-FdOfnlkX.js` → 각주 수정 후 `index-o3LJS8vz.js`).
- WING2: `applies=False · total=None` (종전 `total=0`에서 전환)
- WING1: `applies=True · total=5,166,137`(불변)
- 전체 합산 뷰: 종전대로(이월 참조) · **`net_profit` 478,220.09 불변** — 이 결정은 표시만 고친다
- 화면 확인: 「광고 (옵션축 — 광고센터 미태깅 계정) / 전체 광고비 11,247원 / ㄴ 비-PA — 」
  + 각주 갈림(적용/미적용 문구 분기) 렌더 확인

#### 적대 리뷰 — 1R PASS(P1=0)
리뷰어가 배포 순서 호환을 실측: **신BE+구FE는 종전보다 낫다**(거짓 0원 → 실제 11,247원,
라벨만 부정확) / **구BE+신FE는 무효**(`applies`가 `undefined`면 종전 분기로 떨어진다) ⇒
프론트만 배포하면 이 수정은 아무 일도 안 한다(배포 순서 주의).
- P2 채택 3: 비-PA를 `—`로 표시 · 픽스처가 계정축·광고축에 같은 값을 줘 축 구분을 못 하던
  구멍 보강 · **내 테스트가 전체 합산 뷰의 남은 거짓말을 «정상»으로 봉인하던 것**을
  characterization 테스트로 간극을 명시(고치지 않고 드러냄)
- P2 기각 4: 등가 변이 `??`→`||` · `Row` 컴포넌트 호이스트 되돌리기(순수 리팩터, 손해 없음) ·
  도달 불가 폴백 2건
- 이월 2 (아래) / 확인 안 됨 1: 「차감 기준」 문구가 RG 정산 광고비 제외를 말하는지 — 기존
  문구라 이번 스코프에서 미확인

#### 검증
백엔드 **5379 passed** · 프론트 **326 passed**(24파일) · tsc 0 · eslint 0 errors/51 warnings ·
변이 **9/9 KILLED**(`PYTHONDONTWRITEBYTECODE=1` + `__pycache__` 제거로 캐시 배제 — 2026-08-10에
거짓 KILLED를 낸 함정, 교훈 #238 재확인).

#### 이월 2건 (라운드를 늘리지 않고 넘김)
1. **전체 합산 뷰(`account=None`)에는 같은 거짓말이 남아 있다** — `ad_confirmed_total`
   5,166,137원을 「차감 기준」이라 인쇄하지만 실제 차감액은 비광고주 계정 옵션축까지 더한
   약 5,177,384원이다. characterization 테스트로 간극만 명시해 두었다(고치지 않음).
2. **광고주 계정에 report/SALES 행이 0건이면 여전히 「0원」 단정**이다 — 게이트 축의
   «없음→0» 오독은 이번에 고쳤지만, **신선도 축**(수집이 아직 안 됐을 뿐인 0건)은 그대로다.

#### 곁다리
`Row`가 `ReconciliationCard` 안에서 정의돼 있어 `react-hooks/static-components`가 사용처마다
경고(8건)를 냈다 → 모듈 스코프로 올려 eslint 경고 **57(main 기준) → 51**(상한 54. main은 이미
초과 상태였다).

### D-CPP-36 — 판매분석 옵션축 신설: **새 테이블**로 적재 + 보존식 상설 배선 + 신선도 규칙에 `source` 도입 (2026-08-10 밤~08-11, PR #281)

> ⚠️ **번호 재부여**: 이 결정은 처음 D-CPP-35로 붙였으나, 기록하는 사이 병행 세션이 커밋
> `053a83c`(「엑셀 업로드가 버퍼 값을 쓰기 전에 거부한다」)로 D-CPP-35를 선점해 **D-CPP-36으로
> 뒤로 옮겼다**(main이 트렁크, 본문 불변, 참조 20곳 갱신). 커밋 `0d3826c`·`914718f` 메시지의
> 「D-CPP-35」는 고칠 수 없으므로 여기 남긴다.

> Jino 승인(2026-08-10): 계약 승인 + 여러 후보 중 「신선도 표면 먼저 묶어서」 선택.

#### 정찰 (2026-08-10)
`POST /tenants/rfm-ss/api/business-insight/vi-detail-search`(m-wing same-origin). 자체 fetch는
Akamai가 `TypeError: Failed to fetch`로 막아 페처의 검증된 `_POST_JSON_JS`(XSRF 헤더+
`redirect:'manual'`) 재사용이 필수. 요청은 startDate/endDate **창 집계**(일별 값 없음)라
일자별로 호출해야 한다. 조인 키는 `vendorItemId`(=`coupang_revenue_fee.vendor_item_id`=
`orders.platform_product_id`), `externalSkuIds`는 전건 빈 배열이라 SKU 조인은 안 된다.
대조 3회 전부 차 0(08-01~09 450,700 / 08-05 188,800 / 08-07 86,500).

#### 확정 (번복 금지)
- **옵션축은 «일자×옵션» grain의 새 테이블**(`coupang_vendor_item_sales_daily`)로 적재한다 —
  요약축(`coupang_wing_vendor_summary`류) 확장이 아니다. 요약축이 **독립된 대조 상대**로 남아야
  Σ옵션==요약 보존식이 자기 대조가 안 된다(같은 테이블에 얹으면 검사가 자기 자신을 검사한다).
- **보존식**(Σ옵션 GMV == 요약 GMV, **허용오차 0**)을 헬스에 상설 배선한다
  (`SchedulerHealthOut.vendor_item_conservation`) — 검사기를 만들어도 안 부르면 없는 것과 같다
  (D-CPP-33과 같은 교훈을 이 축에도 적용).
- **`DATA_FRESHNESS_RULES`에 `source` 지시를 도입**한다. 이전엔 규칙 조회가 RG 정산 테이블
  한 곳에 하드코딩돼 있어 새 파이프라인을 넣을 수단이 없었다(교훈 #235). 판매분석 두 축
  (요약·옵션)을 **WING2만** 추가 — WING1은 3P가 RG로 이관돼 판매행이 원천적으로 옵션축을
  못 채운다(영구 빨강을 신선도 배너에 만드는 것은 이번 스코프가 의도적으로 피하는 것). 임계 3.0일.
- **갱신 요청 트리거를 사람 버튼 → 크론(05:20 KST)으로 이관**한다. WING2 판매분석은 UI가
  갱신 요청 자체를 만들지 않아(코드 주석 「현재 휴면」) 07-26 이후 13일 정체했다 — 「버튼-only」는
  «사람이 매일 누른다»를 전제하는데 누를 버튼조차 없는 계정이 있었다(교훈 #234).
- **검사창(보존식 대조 기간) = 수집창(페처 요청 기간) = 7일로 묶는다.** 한쪽만 늘리면 그 사이
  간극(8~14일 전 등)을 어떤 회차도 못 고치는 영구 빨강이 된다 — 배너는 최대 2주 켜져 있는데
  운영자가 할 조치가 없다(교훈 #237). 두 상수를 테스트로 양방향 고정.

#### 구현
- 마이그 `vi1s2a3x4b5c` → `coupang_vendor_item_sales_daily`(`raw_metrics` JSON 보존)
- SA `vendor_item_sales_sync`(멱등 upsert + 배치 내 dedupe + `conservation_check`)
- 라우터 `POST /api/coupang/ops/wing/vendor-item-sales/ingest`
- 페처 `_vi_days`/`_vi_payload`/`_parse_vi_page`/`_fetch_vi_detail`/`_push_vendor_item_sales`
  (요약축 성공 직후 같은 페이지·같은 회차, 창 7일, 페이지 상한 20)
- 스케줄러 `request_wing_vendor_summary_daily_job`(05:20 KST, `WATCHDOG_JOBS` 포함)
- `SchedulerHealthOut.vendor_item_conservation` + 배너 분기 + `check_failed` 상태

#### 라이브 합격 (prod, 2026-08-10 23:10 KST · 무중단 배포 `a92aca7`, 활성 :8011)
- 옵션축 469행·14일(07-27~08-09) · ingest `received=469 ingested=469` · 403 0건
- 08-01~08-09 3P 옵션축 GMV **450,700** == 요약축 **450,700** → **차 0**
- 보존식 전 기간 어긋난 (일자,유형) 칸 **0** · 라이브 HTTP `compared=14 · mismatch=0`
  (다음날 창 이동 후 `compared=12 · mismatch=0`)
- `GET /api/scheduler/health` **HTTP body에 `vendor_item_conservation` 실존**
- 양방향 전이: 수집 전 `healthy:false`+`wing_vendor_item_sales no_data` → 수집 후
  `healthy:true`+`data_stale` 0건
- 번들 `index-BdiAJ7g_.js`에 새 배너 문자열 실존(「판매분석 옵션↔요약 합 불일치」)
- 조인: 판매옵션 16개 중 14개가 요율 SoT와 직결
- 요약축도 같은 회차에 90행 재적재(06-26~08-09)
- ★**「판매옵션 11개」는 12개가 옳았다**: `87287287411`이 08-05 +20,700 / 08-06 −20,700으로
  **창 집계에선 정확히 상쇄돼 사라진다**. 일별 grain이 그 사실을 되살렸다(GMV 총액 불변) —
  grain 결정이 실측으로 정당화됐다.

#### 적대 리뷰 — 1R FAIL(P1 3건) → 전건 수용 → 2R PASS(P1=0)
리뷰어가 재현으로 확인:
- **P1-1**: prod `autoflush=False`라 **배치 내** 중복이 UNIQUE 위반 → HTTP 500 + 회차 전량
  롤백. **테스트 픽스처가 prod와 다른 의미론(`autoflush=True`)이라 500을 완전히 가렸다.**
  → 배치 내 dedupe(마지막 값 승) + 픽스처 정정 + `ingested`/`received` 분리.
- **P1-2**: 마이그 미적용 상태에서 `data_stale` 4건이 유지돼야 하는데 1R엔 0건이었다(RG 정산
  까지 소실). 오타 `source`도 `check_failed`로 표면화하도록 수정.
- **P1-3**: D+1부터 그 날짜가 두 창(검사창·수집창)에서 **동시에** 빠져 「고칠 수 있는 동안만
  울린다」— 위 「검사창=수집창=7일」 확정으로 흡수.
- 수정이 만든 새 결함 없음.
- **P2**: 채택 6(페이지 상한 20 · 행 단위 파싱 방어 · `total_orders` 상한 ·
  `ingested`/`received` 분리 · 페처 옵션축 테스트 신설 · `_CATCHUP_ORDER` 근거 주석) /
  기각 2(옵션별 `int(round())` 합산 — 라이브 3회 전부 정수고 소수가 오면 영구 빨강이
  **정당한 경보** / `vendor_item_id` 길이 검증 — SQLite 무시, Postgres 이관은 별 트랙) /
  이월 1(`option_only`가 판정·배너에서 버려진다) / 해소 1(nginx `client_max_body_size 50M`
  실측 — 7일 0.81MB·14일 1.61MB, 여유 30배 이상)

#### 검증(격리 사본 `git archive a92aca7`에서 측정)
전체 **5325 passed** · 변이 **21/21 KILLED** · 신규 테스트 파일 2개(`test_vendor_item_axis.py`
39건 · `test_wing_vi_detail.py` 22건) · 프론트 tsc 0 · eslint 0 errors(경고 54=상한) · 배너
테스트 19건.

#### 남은 것 (다음 세션 — 상세는 `.claude/memory/HANDOFF_wing-option-axis_20260811.md`)
- ★**PR 병합 후 `bash tools/install_local_runtime.sh` 필수** — 데몬은 repo가 아니라
  `/Users/jino/.ohisell/tools/wing_browser_fetcher.py` 설치본을 돈다. 안 하면 05:20 크론이
  요약축만 받고 옵션축은 3일 뒤 stale로 뜬다.
- `option_only`가 판정·배너에서 버려진다 — 요약축 신선도가 max(date)만 보므로 «중간 구멍»을
  못 본다(요약축의 기존 성질). 옵션축을 손익 엔진에 배선하는 슬라이스에서
  «option_gmv≠0인 option_only»를 판정에 넣을 것.
- `check_failed`가 배너엔 `impact`만 나가 「조회 실패」가 「정체」로 보인다(`reason`은 API body
  에만) — 2R 리뷰어 관찰, P1 아님.
- 옵션축을 손익 엔진이 소비하는 재계산은 이번 범위 밖.

### D-CPP-32 — 정산 통보는 «금액»이 아니라 «요율»을 알려준다. 그러니 정산을 기다리지 않는다 (2026-08-10, PR #273)

> ⚠️ **번호 재부여**: 이 결정은 처음에 D-CPP-30으로 붙였으나, 기록하는 사이 병행 세션이
> D-CPP-30(원가 두 층)·D-CPP-31을 선점해 **D-CPP-32로 뒤로 옮겼다**(main이 트렁크, 본문 불변).
> 커밋 메시지·PR 본문의 「D-CPP-30」은 고칠 수 없으므로 여기 남긴다. 같은 이유로 교훈도
> #204~#207 → **#212~#215**로 재부여했다. `scripts/next_ids.sh`가 "main이 앞서 있다"고
> 경고한 그 상황이 실제로 일어난 것이다 — 번호는 «받는 순간»에 받아야 한다.

**Jino 원문(2026-08-10)**: *"수수료는 정해진 수수료가 있으니까 그걸 때면 되는거 아니야?"* → 맞다. 데이터가 그렇게 말한다.

**라이브 실측(prod, 2026-08-10)**:
- `service_fee = round(sale_amount × service_fee_ratio)`가 **661건 전수 성립**(이탈 2건도 반올림 1원 미만, 최대 0.8원)
- `service_fee_vat = round(service_fee × 0.1)`이 **661/661** 성립
- 한 옵션이 시기에 따라 다른 요율로 정산된 사례 **0건** → 요율은 옵션당 상수
- 요율 분포: **WING2 7.8%·6.4%** / **WING1 7.8%·10.5%·10.8%**

**결정**: 3P 수수료 = **(3P매출 − 반품차감) × 그 옵션의 요율 × 1.1**. 요율은 `coupang_revenue_fee.service_fee_ratio`(쿠팡이 준 값)이고, 이력이 없으면 채널 정률 7.8%. 두 엔진(`profit_calculator`·`intelligence`)이 같은 SA(`option_fee_rate.py`)를 쓴다.

**이 결정이 고친 것 셋**:
1. **종합조망이 미정산 주문 수수료를 0원으로 계산**했다 — `_agg_fees`는 `recognition_date` 창이라 매출(주문일 창)과 **다른 주문**을 가리켰고 폴백이 없었다. 라이브 30일 WING2 49라인 중 **25라인·450,700원이 수수료 공짜**.
2. **폴백이 계정 단일 정률**이라 옵션 요율 차이를 뭉갰다 — WING2 6.4%는 과대, WING1 10.5~10.8%(1,320,600원어치)는 과소.
3. **두 엔진이 수수료를 다른 축에서** 계산했다.

**과세표준은 3P 매출뿐이다** — 1P(로켓배송)는 쿠팡 사입이라 판매수수료가 없고, RG는 수수료가 RG 정산에 통째로 들어와 전액차감된다(D-16). 기존 테스트 `test_net_profit_d3_formula`가 이 이중계상 회귀를 실제로 잡았다.

**전제 검증 장치**(`fee_reconciliation`): 이미 정산된 라인에서 계산값 vs 실측값을 대조한다. 실측을 net_profit에서 뺐으니 이 대조가 유일한 표면이다. 판정은 부호합만이 아니라 **라인당 최대 어긋남**도 본다(상쇄 은폐 방지).

**남은 차이(이번 스코프 밖, 다음 작업)**: `profit_calculator`엔 반품차감 항이 없다(반품 주문을 status로 통째 제외). 라이브 90일 두 엔진 수수료 차 **43,647원**. 「반품 실비용·부가세 두 엔진 통일」의 몫이며, 조용히 흐르지 않도록 `test_two_engines_share_the_rate_and_vat_but_not_the_return_deduction`으로 고정했다.

**부수 정정 — REFUND 행은 «음수로 저장되지 않는다»**: 옛 계약(`revenue_fee_source` docstring·`models.py` D-3)은 "REFUND는 음수 저장"이라 적었으나 prod는 **3/3 양수**다(order `17100183465800`: SALE 18,795 / REFUND 18,795, 참값 순 0원). 부호를 저장값이 아니라 `sale_type`으로 정하도록 고쳤다. **이 오독은 옛 코드에도 있었다** — 옛 `_line_commission`·`_agg_fees` 둘 다 환불 건 수수료를 2배로 잡았고, D-CPP-32이 그걸 net_profit에서 걷어냈다.

**반증된 옛 진단(인계 §6 ①의 전제)**: 「3.9% 감면 상품 2건」은 **존재하지 않는다**. `orders.selling_price`가 이미 라인총액인데 `× quantity`를 곱한 계산 착오였고, 정산 유실도 없다(올바로 재면 284라인 전부 매출 일치, 차 0원). **수량 1인 라인이 279건으로 압도적이라 전수 대조가 초록으로 보였다** — 곱셈 오류는 수량 2 이상에서만 드러난다.

### D-CPP-28 — 광고 원장은 **네 통으로 남김없이** 갈린다. 통에 이름이 없으면 그 돈은 화면에서 사라진다 (2026-08-09, PR #262 병합·prod 배포·라이브 합격)

> Jino: *"1. 광고비가 실제 광고비보다 적게 계산되고 있어. 2. 원가가 안붙은 SKU가 있어. 3. 「원가 제외」로 결정된 SKU 6개가 팔렸습니다 — 이거 모두 해결해줘"*

**세 문제가 한 뿌리였다.** ①은 ②③의 귀결이고, ③은 «결정»이 아니었다.

#### 결정 ①: 옵션 광고 원장을 네 통으로 가르고 **항등식을 계약으로 둔다**

```
ad_option_total(Billboard)
  = 손익 귀속분(pnl.ad_spend 중 귀속 몫)
  + ad_uncosted      구멍1 판매O·손익밖
  + ad_no_sales_days 구멍2 팔린 옵션의 «판매 없는 날»
  + ad_no_sales      구멍3 판매행 없는 옵션
```

★**구멍1·2는 화면 어디에도 없었다.** 라이브 08-08에 33,750원(계정 총액의 4.2%), 창 07-31~08-06에 435,916원. 각주는 그 둘을 «차이의 대부분»이라는 **말**로 대신했다 — 말은 금액이 아니다.

★**샌 이유는 판정 그레인이 갈라져서다.** `pnl_ad`는 «손익에 들어간 원자»를 세고 `ad_no_sales`는 «옵션»으로 판정한다. 그 사이에 (ㄱ)팔렸지만 손익 밖인 옵션 (ㄴ)팔린 옵션의 안 팔린 날이 끼어 있었다. **한쪽이 (날짜×옵션)이고 다른 쪽이 (옵션)이면 반드시 틈이 생긴다.**

- `basis='full'`이면 구멍2·3을 **둘 다** 세후 차감한다. 예전엔 3만 접어서, 커버리지 100%가 되어 화면이 「기간 전체」라 말하는 상태에도 2가 빠졌다 — **의도된 종착 상태에 남은 거짓말이 한 겹 아래에 또 있었다.**
- **구멍1은 절대 차감하지 않는다**(금지선). 그 매출도 손익 밖이라 비용만 빼면 이익이 거꾸로 위조된다.
- `ad_no_sales_included`에 `has_pnl`을 곱한다 — `is_full`은 «결손 0»이라 판매분석 미수집 창에서도 참이 되어, 사다리가 없는 창(2026-05-01~07, 3,421,987원)에서 「이미 순이익에 반영됨」이라 말하고 있었다.

#### 결정 ②: **근거화면 A7의 성격이 바뀐다** — 「결손이 있는가」 → 「원장이 남김없이 갈렸는가」

좌변에 구멍2를 넣으면 등식이 닫혀 **상시 fail이던 검사가 pass가 된다.** 결손의 크기는 이제 fail 여부가 아니라 `ad_unattributed`가 **상시로** 말한다.
★**pass는 «결손 없음»이 아니다.** 그 혼동을 테스트가 못 박는다. 지금 fail이 뜨면 그건 «새로 생긴 다섯째 경로»라는 뜻이라 검사 자체를 의심해야 한다.

#### 결정 ③: `ignored`는 «결정»과 «매칭 실패»를 겸한다 — 화면은 **기록된 사유를 보여주기만** 한다

prod `ignored` 22건이 **전부** `note='no suggestion or low score'`이고 `created_at`이 2026-06-17 23:2x에 초 단위로 붙어 있다 = 일괄 매핑이 후보를 못 찾은 것을 제외로 찍은 것이다(ref 47 §2가 같은 결론). 그런데 화면은 "샘플·증정으로 이미 결정한 것이니 그냥 두세요"라고 안내했고, **그래서 정상 판매 상품이 손익 밖에 남아 있었다.**

→ `excluded_note`를 싣고 문구에서 단정을 뺐다. ★**사유 문자열을 파싱해 화면이 «이건 매칭 실패다»라고 판정하지는 않는다** — 그건 또 하나의 추측을 코드에 굳히는 것이다. 사람이 보고 가른다.
⚠️ **status를 3값으로 쪼개는 스키마 마이그레이션은 이번 범위 밖**(Jino: 나머지 15건은 "별도로 보자"). 근본 해법은 그쪽이다 — 이월.

#### 데이터 조치 — 원가 링크 7건 (Jino 승인 2026-08-09)

| 상품번호 | → 내부 SKU | 원가 | 근거 |
|---|---|---:|---|
| 59459681 Z폴드SE | OHI-0468 | 6,186 | 코드 다리(ch5 `externalVendorSku` 정확일치) · 종전 `no_link` |
| 37350957 Z폴드5 | OHI-0480 | 6,186 | 코드 다리 · 종전 `ignored` |
| 48102855 Z폴드6 | OHI-0476 | 6,186 | 〃 |
| 48102856 Z플립6 | OHI-0451 | 3,713 | 〃 |
| 37350956 Z플립5 | OHI-0455 | 3,713 | 〃 |
| 27217829 Z플립4 | OHI-0459 | 3,713 | 〃 |
| 65568328 워치 실리콘 그레이 | OHI-0688 | 1,400 | **다리 없음 · 이름 정확일치**(Jino 개별 승인). 실리콘 20mm 8색 전부 1,400원이라 색 판정이 틀려도 원가값 불변 |

앞 6건은 D-CPP-27의 판정기로 2차 확인 — `conflicts_bridge=[]`(부위·매수·기종 축 충돌 0).
⚠️ **D-CPP-25의 「`manual`은 불가침」과 문언상 긴장이 있다.** 이 22건은 `match_method='manual'`이지만 `note`·`created_at`이 **일괄 배치가 쓴 것**임을 보인다 — 규칙의 문언은 걸리는데 근거는 안 걸린다. Jino 승인으로 진행했고 `note`에 근거를 남겼다(멱등 upsert라 되돌릴 수 있다).

#### 후속 — `ignored` 22건 **전건 해소** + 각주 교정 (2026-08-10 07:0x~07:4x, Jino 지시)

> Jino: *"나머지 15건 중 다리 있는 6건도 연결해줘"* → 이어서 *"10건도 다 넣어줘 … 51606930 탭S10울트라
> 종이질감 2매의 경우 후보중 S10울트라에 붙는거라 모두 맞는거네"*

- **+16건 연결로 `status='ignored'`가 0건이 됐다**(22건 전부). 6건은 코드 다리(ch5 `externalVendorSku`
  정확일치, 판정기 `conflicts_bridge=[]`), 10건은 이름 정확일치(워치 스트랩 8·트라이폴드·탭S10울트라).
  ★스트랩은 **소재별 원가가 같아**(실리콘 1,400 / 밀레니즈 3,000) 색 판정이 틀려도 원가값이 불변이다.
  ★51606930은 후보가 둘(OHI-0073 S10울트라 전용 / OHI-0076 S8·S9·S10 공용, **원가 동일 5,892.7**)이라
  Jino가 전용 쪽을 명시 지정했다 — 손익엔 차이가 없고 링크 대상만 다르다.
- **라이브(창 08-08·08-03~09·08-01~07·07-31~08-06 전부)**: `cost_coverage=1.0000` · `basis=full` ·
  **사다리 광고비 = 옵션 원장 전액** · 「연결 필요」·「원가 등록」 0건 · 근거화면 A1~A7 전건 pass.
  ★**`basis='full'`인 다일 창이 라이브에 처음 생긴 것**이고, 그 자리가 곧 적대 리뷰 P1-1이
  지목했던 곳이다 — 안 고쳤으면 세 창 모두 A1·A2가 fail이었다.
- ★**순이익이 내려간다**: `full`이 되면서 귀속 불가 광고비 두 통이 순이익에서 차감된다.
  창 08-01~07 이익률 12.03% → **8.52%**. 숫자가 나빠진 게 아니라 **안 빼던 광고비를 이제 뺀다.**
- **PR [#264](https://github.com/Jino00/ohisell/pull/264)** — 일별 표 각주가 `full`에서 「위 손익에
  섞지 않았다」고 사다리를 부정하던 것 교정 + Σ일별 순이익이 타일보다 큰 금액(창 08-04~10에서
  521,187원)을 화면이 숫자로 말하게 했다. 적대 리뷰 2라운드 `GATE: PASS`, 변이 12종 전건 KILLED.
  ★1R P1: 내가 원인을 **「붙일 날짜가 없어서」로 지어냈다** — 실제로는 날짜별 값이 다 있고
  진짜 이유는 그 열이 「원가 확인분」 축이라는 것이었다(교훈 #193).
- 교훈 #192·#193

#### ✅ 해소 — prod nginx `index.html` 캐시 금지 (2026-08-10 08:5x, Jino 승인)

`sellc.ohitech.co.kr` nginx에 `index.html` 전용 `Cache-Control`이 없다. 그래서 **프론트 배포 후에도
브라우저가 옛 `index.html`을 붙들어 옛 번들을 계속 쓴다** — 2026-08-10 07:4x에 내가 직접 당했고
서버가 새 번들을 정확히 내주는데도 화면은 옛 문구였다(«배포 실패»로 오독할 뻔했다).
★이 프로젝트는 배포 검증을 **눈으로** 하는데, 그 눈이 거짓말을 할 수 있다는 뜻이다.
**적용**(`/etc/nginx/sites-available/sellc.ohitech.co.kr`, 백업 `.bak-20260809-235449`):
```nginx
location = /index.html {
    include /etc/nginx/snippets/ohisell-allowlist.conf;   # ★★이 줄이 없으면 IP 차단이 뚫린다
    root /home/ubuntu/ohisell/frontend/dist;
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    add_header Pragma "no-cache" always;
}
```

★★**참조한 `os.ohitech.co.kr` 블록을 그대로 복사하면 안 됐다.** 거기엔 allowlist가 없는데
sellc의 `location /`에는 있고, `try_files`의 `/index.html` 폴백은 **내부 리다이렉트라 location
매칭을 다시 한다** — allowlist를 빼면 **모든 SPA 경로가 IP 차단을 우회**해 2026-07-17에 막은
공개 노출(익명 쓰기 112개)이 그대로 되살아난다. 「캐시 헤더 하나 넣는 일」이 보안 구멍이 될
수 있었던 자리다.

★`assets/*`는 파일명에 콘텐츠 해시가 박혀 있어 **건드리지 않았다**(os 패턴은 JS/CSS도 no-store
하는데, 그러면 배포와 무관하게 매번 다시 받는다). ★`expires` 지시자는 안 썼다 — `add_header`와
같이 쓰면 `Cache-Control`이 두 줄로 나온다.

**검증(라이브)**:
- **allowlist 회귀**: 서버 안 127.0.0.1(차단 대상)에서 `/` `/index.html` `/rocket-1p-revenue`
  `/assets/*.js` `/api/health` **전부 403** — 우회 없음
- **헤더**: `/index.html`·SPA 경로 → `Cache-Control: no-store, no-cache, must-revalidate`(한 줄)
  + `Pragma: no-cache` / 해시 번들 → 헤더 없음(의도대로)
- **효과**: 브라우저가 **하드 리프레시 없이** 새 번들(`index-BF2yAXW0.js`)을 집었고 PR #264의
  새 각주가 그대로 떴다. 직전까지는 옛 번들(`index-gQQTtIoL.js`)을 붙들고 있었다
- `nginx -t` 통과 후 `systemctl reload`(무중단)

**롤백**: `sudo cp /etc/nginx/sites-available/sellc.ohitech.co.kr.bak-20260809-235449 \
/etc/nginx/sites-available/sellc.ohitech.co.kr && sudo nginx -t && sudo systemctl reload nginx`

#### 라이브 합격 증거 (prod, 2026-08-09 23:3x~23:5x KST)

- **창 08-08**: `cost_coverage=1.0000` · `basis=full` · **사다리 광고비 797,431원 = 원장 전액** · 네 통 합 원 단위 일치 · `uncosted.skus=0` · `excluded=0`
- **근거화면 A1~A7 전건 pass**(3창: 08-08 / 08-01~07 / 07-31~08-06). **A7이 상시 fail → pass**. A1~A3 차이 0.00/0.00/0E-12 — `basis=full` 창 포함
- 항등식 3창 확인. 구멍2가 07-31~08-06에서 **435,916원** — A7이 고발하던 금액과 문자 그대로 일치
- 배포: 백엔드 무중단(다운타임 0초) · 프론트 스탬프 `bddf350326df` · DB 스키마 변경 없음
- **리뷰**: 적대 리뷰 2라운드 `GATE: PASS`. **1R P1 2건 — 둘 다 «합격기준이 달성되는 순간 터지는» 결함**이었다(A1·A2가 full 창에서 fail / 각주가 사다리를 부정). 생존 변이 5종은 전부 «0인 값으로만 검사» 한 형태. 2R에서 전건 KILLED 확인
- 교훈 #189·#190·#191

### D-CPP-37 — 로켓1P 화면의 세 구멍은 **하나의 뿌리**였다 (2026-08-11 새벽, PR #280 + prod 데이터 조치)

> Jino가 화면을 보고 셋을 지적했다: *"1. 광고비가 실제 광고비보다 적게 계산되고 있어. 2. 원가가
> 안붙은 SKU가 있어. 3. 「원가 제외」로 결정된 SKU 6개가 팔렸습니다. 이거 모두 해결해줘."*
> ★번호: 병행 세션 PR #281이 D-CPP-36을 선점해 **37**로 받았다.

#### 진단 — ①의 원인이 ②였다
30일 창(07-11~08-09)에서 원가 미연결 SKU **11건(우리 매출 91,760원 = 전체의 0.19%)** 때문에
`is_full=False`가 되고, 그 불리언 하나가 `include_no_sales`를 꺼서 **광고비 2,532,362원
(옵션 원장의 12.7%) 차감이 통째로 멈춰 있었다**(`rocket_1p_revenue.py:670-683`).
**0.19%의 원가 결손이 12.7%의 광고비 미반영을 좌우**했다(교훈 #234).

③은 **이미 해소돼 있었다** — 창 8개를 훑어도 `excluded_skus`가 전부 0이고, prod
`rocket_product_cost_map`에 `excluded` 행이 **0건**(268건 전부 `confirmed`)이다. 08-10 낮
잔여 16건 연결로 닫힌 것을 Jino가 그 이전 화면으로 본 것이다. **가드는 정상 작동 중**
(`allow_excluded`는 사람 입구 라우터만, `note` 필수, 자동 계열 `match_method`면 422).

#### 확정 — 원가 매핑 11건 (전건 Jino 항목별 승인, 2026-08-11)
| 쿠팡 | 내부 SKU | 원가 | 근거 |
|---|---|---|---|
| 57316058 아이폰14+ · 57316043 갤S25+ · 57316045 아이폰16프로 · 63835703 아이폰17프로 | OHI-0272·0286·0266·0262 | 922 | Jino: *"맥세이프 무 922원 맞아"* — 쿠팡명에 「맥세이프」 없음. **프로맥스(0265·0261) 아님** |
| 63858202 아이폰17에어 | OHI-0236 | 2,400 | Jino: *"아이폰에어는 아이폰17의 한 옵션명이야"* |
| 69517683 Galaxy S26 | OHI-0152 | 2,222 | 내부에 「히든범퍼」명 없음 — 하이브리드 소다 |
| 52404706 갤탭S10+ · 52420641 갤탭S9FE+ | OHI-0109·0098 | 5,430.7 | Jino: 저반사 지문방지 **PET** 계열 |
| 11373208 갤S20 | OHI-0560 | 1,690.7 | 무광택=매트=지문방지 동일 기능, 2매 일치 |
| 54737755 갤탭A9+ | OHI-0878 | 4,352.7 | 6H·유리코팅·블루라이트·기종 전부 일치 |
| 63185983 방수팩 | OHI-0770 | 3,000 | 색상(블랙)까지 일치. 4색 전부 3,000원 |

★**후보 자동 선택을 하지 않았다.** 일미리 4건은 후보 1·2위 **점수가 동일한데 원가가 2,400 vs
922(2.6배)**였고, 태블릿 2건은 후보가 전부 **폰 필름**이었다(태블릿↔폰 오판). 자동 매핑이
금지된 설계(2026-06-17 사고)가 옳다는 것이 이번에도 확인됐다.

#### 라이브 증거 (prod, 2026-08-11 05:5x)
| | 전 | 후 |
|---|---|---|
| 원가 미부착 SKU | 11 | **0** |
| `cost_coverage` / `basis` | 0.9981 / `costed_subset` | **1.0000 / `full`** |
| 손익 반영 광고비 | 17,378,318 | **19,917,755 = 원장 전액** |
| `ad_no_sales_included` | False | **True** |
| 순이익 / 이익률 | 9,025,133 / 18.56% | **6,774,967 / 13.91%** |

★**순이익이 내려간 것이 정상이다** — 안 빼던 광고비 2,532,362원을 이제 빼는 것이다
(D-CPP-28 후속 때 12.03%→8.52%가 된 것과 같은 성격).
되돌리는 법: `DELETE /api/coupang/ops/rocket/cost-map/{상품번호}` × 11.

#### 화면 거짓 단정 2건 교정 (PR #280, 병합됨)
- 일별 열 머리 **「광고비(전량)」 → 「광고비(판매행)」**. `ad_spend_all`은 판매 원자 루프 안에서
  누적되므로 판매행 있는 옵션분만이다. 실측 Σ일별 5,412,823 vs 원장 6,092,627 —
  **679,804원(11.2%) 부족**이고 그 차이가 정확히 `ad_no_sales + ad_no_sales_days`.
  ★**같은 화면 아래 각주는 이미 「세 통이 빠져 있습니다」라고 말하고 있었다** — 화면이 스스로
  모순돼 있었고 위쪽 단정이 아래쪽 사실을 덮었다(교훈 #235).
- 광고 대사 각주의 **「정의 차이입니다(수집 결손이 아닙니다)」 단정 제거**. 원시 대조가 뒤집는다:
  report 합 19,925,049 · option **원시** 합 19,929,002(계정보다 많다, 매일 같은 방향 ≈0.02% —
  이 몫은 정의 차이가 맞다) · 그런데 원장 축은 19,917,755로 원시보다 **11,247원 적고** 그게
  부호를 뒤집어 +7,294를 만든다. **그 11,247의 원인은 미확인.**
- 테스트: 백엔드 2건(Σ일별 + 구멍2 + 구멍3 = 원장 — **문구가 아니라 그 문구가 참인 이유**를 잠근다)
  + 프론트 1건. 변이 4종 KILLED. ★1차엔 **열 머리 변이가 살아남았다**(각주만 단언했다, 교훈 #236).

#### ✅ 화면 배포 완료 (2026-08-11 09:20 KST, B안 — Jino 승인)
프론트 CAS가 거부했다: prod가 **PR #281(OPEN)** 브랜치 `claude/wing-option-axis`의 커밋
`a92aca7`에서 배포돼 있고, 거기 프론트 변경 **137줄**(Layout 배너·api 타입)이 prod에 살아 있다.
병합만 하고 밀면 그 137줄이 사라진다(2026-08-06 clobber와 같은 형태) → **배포 보류**.
`--force-frontend`는 이 경우에 쓰면 안 된다(교훈 #237).

**해소(Jino 「B로 해줘」)**: PR #281 병합을 기다리지 않고, **main + 그 브랜치를 합친 배포 전용
브랜치**(`deploy/main-plus-281`, 커밋 `ee2f20a`)로 빌드해 **양쪽이 다 들어간 번들**을 올렸다.
prod에 이미 살아 있던 코드라 잃는 것이 없고, 내 수정만 얹힌다. ★이 브랜치는 **어디에도
병합하지 않는다**(문서 충돌 2건을 main 쪽으로 눌러 해결했으므로 소스 외엔 의미가 없다).
- 라이브 실물 검증: 번들 `index-BbHjjvEy.js`(이전 `index-BdiAJ7g_.js`에서 교체) ·
  **「광고비(전량)」 0건**(옛 거짓 문구 소멸) · 「광고비(판매행)」 2건 · 「그날 전부가 아닙니다」 1건 ·
  PR #281의 `registration_type` **생존 확인** · 백엔드 무해(손익 광고비 = 원장 19,917,755 · basis full)
- **부채 해소(같은 날 09:26)**: 직후 PR #281이 병합되어 main이 #280·#281을 **둘 다** 갖게 됐다.
  `ee2f20a`의 `frontend/src`와 그 시점 main의 diff가 **완전히 비어** 잃을 것이 없음을 확인하고
  (내용 동일 = 재배포는 스탬프만 바꾼다), main(`cd432d5`)에서 재빌드해 `--force-frontend`로
  올렸다. ★이것이 CLAUDE.md가 말하는 «옛 커밋이 역사에서 빠진 경우»의 정당한 용법이다 —
  «남의 배포분을 지우는» 경우가 아님을 **빈 diff로 입증한 뒤**에만 썼다. 매니페스트에 기록됨.
  결과: 스탬프 `cd432d5`가 main의 조상 → **다음 프론트 배포는 CAS를 정상 통과한다.**
  번들·내용 불변(`index-BbHjjvEy.js`, 「광고비(전량)」 0건 · 「광고비(판매행)」·`registration_type` 생존).

#### QA (2026-08-11 밤, PR #282) — 화면을 눈으로 보자 부호 버그가 나왔다
prod가 IP 차단(403)이라 **prod `dist` + `ssh -L` 터널 백엔드**를 로컬에 띄워 브라우저 QA를 했다
(허용목록은 안 건드렸다 — 교훈 #250).
- ✅ 열 머리 **「광고비(판매행)」** 렌더 확인 · 「광고비(전량)」 0건 · 각주 「그날 전부가 아닙니다」·
  구멍2·구멍3 표시 · 「원가 제외」 경고 없음 · 콘솔 에러 **0건** · 모바일 375px 정상
- ✅ **라이브 항등식 성립**: Σ일별 16,642,617 + 구멍 2,407,577 = 원장 19,050,194 (창 07-13~08-11).
  Σ일별 < 원장이 실데이터로 확인 — 열 이름이 「판매행」이어야 하는 이유다.
- ❌→✅ **부호 버그 발견·수정(PR #282)**: *"계정 확정 광고비는 19,057,661원이고 원장과의 차는
  **−7,467원**"*. `diff`는 원장 − 계정인데 문장이 「계정」을 먼저 놓아 **반대 방향으로 읽혔다.**
  → 「원장은 그보다 N원 적습니다」로 방향을 말로 못 박음. 변이 4종 KILLED. 교훈 #249.
  ★**전날 밤 내가 고친 바로 그 문장**이다 — 문장은 봤는데 그 안의 숫자는 안 봤다.
- 라이브 재검증: 번들 `index-BK2Tk3Ch.js`(commit `22e6ee7`) · 화면 문구
  *"계정 확정 광고비는 3,543,374원이고, 원장은 그보다 10,476원 적습니다"* — 검산 일치.

#### QA에서 같이 보인 것 (스코프 밖 — 표면화만)
- **디스크 85.7%** 경고 배너. 08-10에 83%로 내렸는데 다시 올랐다.
- **수집 신선도 4건 지연**: ofix 판매분석 2일 · ofix 광고비 36시간 · ohitech 로켓광고 2일 ·
  로켓 발주/정산 2일.
- **1년 창엔 원가 미부착 11건이 남아 있다** — 내가 연결한 11건은 전부 `confirmed`로 살아 있고,
  이건 **다른 SKU들**(노트10·도어락 문캅스·아이폰15프로·셀카봉·갤탭S7FE·카드지갑 등 옛 판매분).
  ★즉 ②는 **운영 창에서 0**이지 전 기간 0이 아니다.

#### 이월
- 「전부 아니면 전무」 절벽(`include_no_sales = is_full`): 커버리지 99.81%에서도 구조적 두 통을
  0% 차감한다. **새 폰 출시마다 다시 선다.** 안분 차감 같은 대안은 D-CPP-28의 `costed_subset`
  분기를 새로 정하는 일이라 Jino 결정 필요.
- `ad_account_total` − 원장의 11,247원 원인 미확인(0.037%).
- CI 과금 정지 계속 — PR #280도 `--force` 병합(자백 기록).

### D-CPP-35 — **탐지 위에 예방을 얹는다**: 엑셀 업로드가 버퍼 값을 쓰기 전에 거부 (2026-08-10 밤, PR #279)

> Jino: *"그럼 여지껏 이거 안하고 뭐한거야?"* → 두 번 이월만 됐던 예방 지점을 그 자리에서 닫았다.

D-CPP-33의 배너는 **되돌아간 «뒤에»** 켜진다. 옛 엑셀 v3를 올리면 177건이 통째로 복귀하고
그 사이 손익은 과소 계상된 채 조회된다. D-CPP-34가 버퍼를 「단순 오류·재입력 금지」로
확정했으므로 처분은 경고가 아니라 **거부**다.

#### 확정 (번복 금지)
- **두 경로의 처분이 다르다 — 행의 존재 이유가 다르기 때문이다.**
  · `/upload`(상품 원가표 시트) → **행 전체 건너뜀**(기존 음수 원가 가드와 같은 처분)
  · `/upload-by-name`(원가 매핑) → **원가만 안 씀, 매핑은 정상 처리**. 행을 통째로 버리면
    매핑이 사라져 주문이 미연결로 남는다.
- **신규 상품이 버퍼로 오면 0(모르는 값)으로 만든다.** 버퍼는 «틀린 값»이고 0은 «모르는 값»이며,
  손익 엔진이 이미 0을 미상으로 다룬다. 틀린 값보다 모르는 값이 낫다.
- **`ok`도 `undetermined`도 막지 않는다** — 이 가드가 답할 수 있는 건 «정본 + 알려진 버퍼인가»
  뿐이다. 모르는 값을 막으면 케이스·거치대 계열(459건)이 통째로 못 올라간다.
- **산술 사본을 만들지 않는다** — `cost_truth_audit.classify`를 임포트한다.
- **로딩 성공 ≠ 검사 가능**(2R P2-N2). `try_load_truth`가 **판정을 한 번 시연**해 보고 못 하면
  스냅샷 부재와 같이 취급한다. 안 그러면 버퍼가 들어가면서 응답은 「검사함·0건」이 된다.
- **배너 문구는 순수 함수**(`frontend/src/pages/uploadResultText.ts`)에 둔다 — 컴포넌트 인라인이면
  표시가 회귀로 안 잠긴다(2R P2-N1: 원가 거부를 되돌리는 변이 2종이 프론트 290건에서 생존했다).

#### 적대 리뷰 — 1R FAIL → 2R PASS
- **1R P1(수용)**: HTTP 응답 스키마는 지켰는데 **바로 다음 경계**인 프론트 소비자가
  `cost_buffers`를 안 읽어 사유가 또 사라졌다. 배너는 「수정 3」이라고 적극적으로 오도했다.
  돈은 안 틀어졌고(원가 정본 유지) 깨진 것은 표면화다. 교훈 #229.
- **1R P2 채택 4**(cost_guard 미표시 · fail-soft가 classify 미포함 → 500 · 버퍼 5종 중 4종
  무검증 · 센티널 미고정) / **이월 2**(아래) / **P3 3건**.
- **2R GATE PASS**(미해소 P1 0건) + 새 P2 2건도 채택 — 둘 다 「검사 못 함이 이상 없음으로
  보인다」는 이 작업의 존재 이유와 같은 형태였다.
- **변이**: 1R 10종 + 2R 4종 + 3R 5종(리뷰가 **살렸던** M-a·M-d·M-f·MF-1·MF-2 포함) **전건 KILLED**.

#### 라이브 증거 (prod, 2026-08-10 22:2x KST)
- **end-to-end 실업로드**: 버퍼 2616이 든 엑셀을 `POST /api/products/upload`에 태움 →
  `updated: 0` · `cost_buffer_blocked: 1` · `cost_guard: "active"` · **원가 2350.7 불변** ·
  사유 문구가 응답에 그대로. **DB 쓰기 0건**(폭발 반경을 1행으로 설계).
- 버퍼 **5종 전부** 차단 확인(폰·도어락/플립·폴드·버디·태블릿) · 정본/판정불가는 통과
- 헬스 무해: `healthy: true` · `cost_drift: null` · `disk_low: []`(배포 전과 동일)
- 대조 불변: 949행 · **ok 490 · buffered 0 · undetermined 459**
- 배포: 백엔드 무중단(8001→8011, 다운타임 0초) · 프론트 번들 `index-C1n9Ei8W.js`
- 테스트 20건 신규 · 백엔드 **5,238 passed** · 프론트 **299 passed** · `tsc -b` exit 0

#### ⚠️ CI 없이 병합했다 (자백)
`safe_merge.sh 279 --force`. 이유는 **CI가 깨진 게 아니라 안 돌았기 때문**이다 —
GitHub Actions 과금 문제로 잡이 미시작(21:22 KST~ 전 실행). 대신 CI가 돌릴 명령을 같은
형태로 전부 돌렸고, prod Python **3.10 문법 검사 5/5 OK**(매트릭스가 3.10/3.14로 갈린 이유).
★한계: 3.10은 **문법만** 봤지 전체 테스트를 3.10으로 돌리지는 못했다. 교훈 #231.

#### 이월 (트리아지 — 라운드를 늘리지 않고 넘김)
- **1R P2-4**: `/upload`에서 **신규 SKU** 행이 차단되면 같은 요청의 시트2 「채널 매핑」이
  「SKU 없음」으로 그 SKU 매핑을 전부 건너뛴다 — 처분 차이의 근거가 반쪽만 성립한다.
- **1R P2-6 / P3**: `rocket_1p_channel_pnl.py:328`·`rocket_1p_revenue.py:82`가 `cost_price
  IS NOT NULL` 기준이라 **0을 「원가 0원」으로 계산**한다(다른 엔진은 `> 0`). 이 브랜치가
  0 생산 경로를 하나 늘렸다(오늘은 도달 불가 — 신규 상품엔 1P 링크가 없다).
- **P3**: `/upload`의 원가 파싱이 `Decimal("Infinity")`를 통과시킨다(음수 가드도 못 잡음).

### D-CPP-34 — D-CPP-31(원가표 시스템화) 착수 전 확정 2건 + **착수 연기** (2026-08-10 밤)

> 계약 초안을 올렸으나 Jino: *"원가표 시스템화는 다음에 하자. 이 세션에서 작업을 너무 길게 했다"*
> → **착수 연기.** 다만 계약 승인에 필요했던 질문 2건은 이 자리에서 답이 나왔으므로 먼저 굳힌다.
> 계약 초안 전문: `docs/PLAN_cost-table-systemization.md` (승인 대기 상태)

#### 확정 (번복 금지)
- **`known_buffers`는 «단순 오류»다 — 재입력 금지.** Jino: *"단순 오류 — 재입력 금지"*.
  폰 +265.3 · 도어락·플립 +232.6 · 폴드 +96.4 · 버디 +80.5 · 태블릿 −99.0.
  → D-CPP-30이 177건을 정본으로 내린 것이 **맞는 조치**로 확정됐고, 구성층에 «운영 버퍼»
  같은 항목을 **만들지 않는다**. 버퍼가 다시 나타나면 그건 회복이 아니라 재발이다.
  (`cost_truth_20260807.json`의 `known_buffers`는 **탐지용 사전**으로만 남는다 — 허용 목록이 아니다.)
- **컷오버에 동의한다.** Jino: *"동의 — 그때 시스템으로 옮긴다"*. 전 계열 이식 + 판정가능분
  전건 일치가 관측되면 **원가 편집을 엑셀에서 멈추고 시스템에서만** 한다. 그 시점에 정본
  스냅샷 검사기의 기준도 시스템 생성 스냅샷으로 교체된다(검사 방향이 «DB가 엑셀과 같나» →
  «캐시가 구성 합계와 같나»로 넘어간다). ★**컷오버 선언 전까지 엑셀이 정본**이고, 그 전에
  스냅샷을 건드리는 것은 금지선이다.

#### 미결 (계약 승인 시 답해야 함)
- 엑셀 부자재 단가·수식 **파싱 결과를 「Jino 확인분」으로 인정하는가** — 「추론 금지」 규율의
  경계 해석이라 모델이 못 정한다. 파싱 원본이 Jino 자신의 엑셀이라는 점이 논점.

### D-CPP-33 — 감시 장치는 «판정이 맞을 때»가 아니라 «사람 눈에 닿을 때» 완성된다 (2026-08-10, ref 54 §9, PR #275)

> Jino: *"진행해줘"* (ref 54 §7-6 「검사기를 크론·CI에 배선」 이월 건)

원가 버퍼 검사기는 오전에 완성됐지만 **아무도 안 불렀다.** 그 상태는 검사기가 없는 것과
관측상 같다. 배선처를 정하려 prod를 실측했더니 **기존 ohisell 크론 셋 중 둘이 출력을
`/dev/null`로 버리고** 있었고, prod엔 **git 체크아웃도 `docs/`도 없어** 스크립트·근거 파일을
배포할 수단 자체가 없었다.

**결정**: 크론이 아니라 **앱 안(요청 시점 계산)**에 넣는다.

```
product_master → compute_scheduler_health → build_health
              → GET /api/scheduler/health (`cost_drift`) → 전역 파이프라인 헬스 배너
```

왜냐하면 크론 결과 파일은 그 자체가 낡을 수 있고 **«낡음»과 «이상 없음»은 같은 모양**이기
때문이다(교훈 #123의 재판). 요청 시점 계산이면 그 질문이 아예 없어진다.

- 판정 산술은 `backend/app/services/cost_truth_audit.py` **한 벌**이고 CLI가 그걸 임포트한다
  (사본 둘이면 한쪽만 고쳐져 감시자가 감시 대상보다 낡는다).
- 정본 스냅샷을 `docs/references/data/` → **`backend/app/data/`**로 이사(배포되는 자리).
- **알고 감수**: 드리프트 0건과 «스냅샷 없어 판정 못 함»이 응답상 같다(둘 다 `null`) —
  fail-soft가 헬스 API를 살리는 대가. 로그가 유일한 구분자다.
- **동봉(Jino 승인)**: `disk_low` 배너 분기 신설 — prod가 `healthy=false`인데 배너가 침묵하고
  있었다(실측 93.8%). 판정 목록과 표시 목록이 갈라져 있었다(교훈 #223).

**라이브 합격 4/4**(2026-08-10 17:3x KST, prod :8011 · 번들 `index-CvniPoVg.js`):
①`cost_drift` 키 실재 + `null` ②prod DB 사본에 버퍼 5건 심으니 **5건 검출 + exit 1**
③배너 문구가 번들에 배선됨(드리프트 0건이라 화면엔 안 뜸 = 정상) ④배포 스탬프 일치.

적대 리뷰 2라운드 PASS(1R FAIL→P1 1건 해소, 변이 5/5 KILLED). 교훈 #219~#223.

> ⚠️ **번호 재부여**: 이 결정의 교훈은 처음 #216~#220으로 붙였으나, 기록하는 사이
> 병행 세션이 #216~#218을 선점해 **#219~#223으로 뒤로 옮겼다**(main이 트렁크, 본문 불변).
> 커밋 메시지의 「#216~#220」은 고칠 수 없으므로 여기 남긴다.

### D-CPP-30 — 원가는 **두 층**이다: 링크(어느 SKU) 위에 값(그 SKU의 원가가 정본인가) (2026-08-10, ref 54)

> Jino가 감사 4건을 «하나하나» 처리하라 지시했고, 그 과정에서 **두 번째 층**이 드러났다.

#### 확정 (번복 금지)
- **원가의 정본은 엑셀 `MD_원가 계산_Jino_260807.xlsx` 「제품 원가표」**다. `product_master`가
  아니다. ★그 엑셀은 목록이 아니라 **부자재 구성으로 원가를 계산하는 계산기**다(ref 54 §1).
- **정본 스냅샷을 repo에 둔다**(`backend/app/data/cost_truth_20260807.json`, 69항목,
  출처 파일명·sha·시각 포함). 엑셀 없이도 대조가 되고, 어느 시점 기준인지가 남는다.
- **버퍼 177건을 정본으로 내렸다** — 폰 +265.3 · 도어락·플립 +232.6 · 폴드 +96.4.
  이익 회복 전 채널 90일 **+1,012,405원**(스마트스토어 941,659) + 로켓1P 186,279원.
  ★취소·반품·입금전은 빼고 센 값이다 — 손익 엔진이 그 주문을 건너뛰므로 그 원가 과대는
  이익에 반영된 적이 없다. 상태 무관으로 세면 1,059,518원이지만 그건 장부 회복액이 아니다
  (적대 리뷰 P1-1이 잡았다).
- ★★**«공통 vs 기종별»은 질문이 틀렸다** — 84건의 차이가 양쪽 다 정확히 265.3인데 방향이
  반대였고, 판별자는 **버퍼 유무**였다. 낮은 쪽이 늘 정본이다.
- **재발은 검사기가 잡는다** — `scripts/audit_cost_buffer.py`(읽기 전용, 드리프트 시 exit 1).
  ★**이름 매칭을 하지 않고 값 산술만** 본다. 좁은 대신 틀리지 않는다.
- «판정 불가»(459건, 케이스·거치대 등 CNY 계열)를 **«정상»으로 세지 않는다** — 셋을 따로 낸다.

#### 링크 감사 4건 처분 (D-CPP-27 종결)
①`replace` 4건 교체(렌즈↔액정 3 · 폰↔태블릿 1) ②공통원가 84건은 원가표가 답(A 71 유지·B 13 교체)
③맥세이프 7 + 도어락 1 교체 ④`no_bridge` 76건은 **작업 목록**(Jino 입력 필요).
감사 재실행: `replace` **4 → 0** · `agree` **83 → 100**.

#### ⚠️ 내가 낸 사고 (교훈 #204)
Jino의 「같은 제품이다」를 「기종별 2,616이 맞다」로 읽고 **71건을 반대로 바꿨다가 되돌렸다.**
그건 **제품 동일성**에 대한 답이었지 **가격**에 대한 답이 아니었다. 정본을 보기 전에 움직인 것이 원인.

#### 라이브 증거 (prod, 2026-08-10)
- 검사기: 고치기 전 백업 **177건 exit 1** / 고친 후 **0건 exit 0** · 정본 일치 313 → **490**
- 로켓1P(창 07-10~08-08) 순이익 9,242,226 → **9,426,179**(+183,953, 링크·값 조치 합산)
- 백업: `ohisell_before_costbuffer_20260810_122857.db` + 177건 CSV(ref 54 §6에 복원법)
- 테스트 19건 · 변이 10종 전건 KILLED · 전체 **5,140 passed**

#### 이월
원가표 시스템화(**D-CPP-31**) · 매핑 엑셀 v4 재생성 · `OHI-TGLASS-IP17PRO` 12기종 뭉침
(판매 1,287개, 원가표에 없는 3,500원) · `no_bridge` 76건 입력.

### D-CPP-29 — 「제외」는 **뜻이 하나**다: 연결 안 함 · 재제안 방지 · 손익에선 「모름」 (2026-08-10, PR #267, 마이그 `e4c7a1b8d206`)

> Jino: *"ignored status 3값화도 진행해줘"* → 조사 결과 **3값으로 쪼갤 일이 아니었다.**

#### 무엇이 문제였나 — 한 값이 세 뜻
| 모듈 | `ignored`를 이렇게 읽었다 |
|---|---|
| `rocket_promo_pnl` | 원가 **0원** · 해결됨 |
| `rocket_intelligence` | 원가 **0원** + `resolved_amount`·`coverage_pct`에 **가산** |
| `rocket_1p_revenue` | 원가 **미상** · 작업 목록에서만 제외 |

2026-06-17 일괄 매핑이 «후보를 못 찾은» 22건을 이 값으로 찍자 두 엔진이 **원가 0원 = 전액
이익**으로 셌다. **90일 발주 실측: 발주 8,146,140원 / 진짜 원가 3,311,826원.** 커버리지까지
부풀어 「원가가 다 붙었다」는 **거짓 안심**을 줬다 — 금액 오차보다 이쪽이 나쁘다.

#### 왜 3값이 아니라 통일인가
prod 사유 전수(268행)에 **사람이 제외를 결정한 흔적이 한 건도 없다.** 전부 `auto score=…`
또는 `Jino 확인/승인`이고 둘 다 **연결**에 관한 것이다. 한 번도 안 쓰인 개념을 둘로 쪼개는
셈이라, **뜻을 하나로 못 박고 자동이 못 쓰게 막는** 쪽을 Jino가 택했다.

#### 확정 (번복 금지)
- `excluded` = **연결 안 함 · 재제안 방지 · 손익에서는 「모름」.** 「원가 0원」 해석은 코드
  어디에도 없다. ★「이 물건은 정말 원가가 0이다」는 원가 0원을 **등록**할 일이지 연결을
  거부하는 상태가 대신할 일이 아니다.
- `excluded`는 `resolved_amount`·`coverage_pct`·`has_cost`에 **안 든다**. 「제외」와 「매핑
  없음」은 둘 다 미해결이되 **계속 구분된다**(할 일이 다르다).
- **사람만 쓴다** — 가드 셋: ①`allow_excluded=True` **명시**(기본 거부. 라우터만 넘긴다)
  ②`note` 필수 ③`match_method` 자동 계열 거부.
  ★★**①만이 실제 판별자다**(적대 리뷰 P2-1): 그 22건은 `manual`이었고 사유도 달려 있어
  ②③은 **못 막았을 것**이다. 사고를 실제로 닫은 것은 가드가 아니라 「원가 0원」 해석의 제거다.
- **사유 문자열을 파싱해 자동 분류하지 않는다** — 추측을 코드에 굳히지 않는다.
- 프론트: `upsertRocketCostMap`의 `status`를 **`"confirmed"`로 좁혀** 제외를 타입으로 막았다.
  제외는 `excludeRocketCostMap` 하나로만 간다 → 회귀가 **컴파일 불가**(`TS2322`).

#### 라이브 합격 증거 (prod, 2026-08-10 10:4x~10:5x KST)
- **배포 전후 diff = 키 이름 둘뿐**(`ignored_*`→`excluded_*`), **값은 양쪽 0**. 손익·커버리지
  숫자가 문자 그대로 동일 — prod `ignored` 0건이므로 **안 움직이는 것이 예상값**이었다.
  (창 08-01~07 `net 1,596,482.17` · 07-31~08-06 `net 1,506,888.87` · 종합조망 `coverage 0.9993` 불변)
- alembic `e4c7a1b8d206 (head)` 적용 · status 분포 `confirmed 268` (excluded 0)
- **가드 라이브 검증**: 사유 없음 **422** · `suggested` **422** · 옛 값 `ignored` **422** ·
  사람 입구+사유 **200**(`cost_price=None` — 0이 아니다). 검증용 행은 삭제 후 0건 확인
- 배포: `--migrate --restart` 순서 강제 · 무중단(다운타임 0초) · 프론트 스탬프 `a533847f1581`
- **리뷰**: 적대 2라운드 `GATE: PASS`. 1R P2-1이 **내 주석의 거짓**을 잡았고(가드가 실제
  경로를 못 막는데 막는다고 씀), 2R가 남긴 F5(프론트 호출부 무방비)는 이월 대신 **타입으로 해소**
- 교훈 #198·#199·#200·#201

### D-CPP-27 — 원가 링크의 «대상»을 두 렌즈로 전수 판정한다. 판정은 창에 종속되지 않고, 교체는 Jino가 한다 (2026-08-09, ref 52)

**계기**: 원가 링크 172건이 `match_method='suggested'` — 이름이 비슷하다는 이유로 붙은 것이다.
D-CPP-25가 «값 층»과 «링크 층»을 가르면서 ★**원가표가 정확해질수록 잘못된 링크의 오차는 커진다**를
확인했고 전수 판정은 이월됐다. 산출물 `scripts/audit_rocket_costmap_bridge.py`(읽기 전용) +
`backend/tests/test_rocket_costmap_bridge_audit.py`(22건) + ref 52.

**확정된 것**
- **두 렌즈가 다 지지할 때만 교체 후보다.** 브리지(ch5 `externalVendorSku` 코드 일치) 단독으로는
  안 된다 — 브리지가 «다르다»고 말하는 64건이 실은 **의도된 공통 원가 상품**이었다(ref 44 §4-4).
- ★**브리지는 시간 축이 없는 구조적 사실이라 판매 창으로 자르지 않는다.** 금액만 창에 종속된다.
  창을 물렸더니 19건이 「브리지 없음」으로 접혔고 그 안에 교체 후보 1건(단가 오차 108%)이
  통째로 숨었다 — 게다가 「판정 불가 66건」이라는 정직한 이름의 버킷에 숨었다(교훈 #183).
- ★**「금액 0」을 「영향 없음」으로 읽지 않는다.** 0원에는 «원가가 같아서»와 «이 창에 안 팔려서»가
  섞인다. 후자는 잠복이다 — `OHI-0555`는 단가차 **291%**인데 금액이 0이었다(교훈 #184).
- **`manual`은 불가침**(D-CPP-25 승계) — 어긋나도 보고만 하고 Jino 결정 목록에 올리지 않는다.
- **교체는 자동으로 하지 않는다**(교훈 #117). 판정기는 DB에 쓰지 않는다.

**결과** (창 2026-07-08~08-06, prod 스냅샷 · `suggested` 172건)
| 판정 | 건수 | 30일 금액 |
|---|---:|---:|
| `replace` | 4 | +69,126원 |
| `label_only` | 12 | 0원(11건 원가 동일 · **1건 잠복 291%**) |
| `agree` | 38 | 0원 |
| `undetermined:no_bridge` | 47 | 판정 불가 |
| `undetermined:lens_tie:generic_vs_model` | 64 | +60,223원 ← **정책 질문** |
| `undetermined:lens_tie:other` | 7 | −8,868원 |

브리지 불일치 87건·120,481.1원. ⚠️08-07 세션 측정과 같은 값이지만 **같은 조인의 다른 표면이라
교차검증이 아니다**(교훈 #141) — 재현이지 검증이 아니다.

**Jino 결정 대기 넷**: ①공통 원가 SKU 둘(`OHI-3D-TPU-MATTE`·`OHI-PRIV-FINGER-FILM-…`)을 공통으로
둘 것인가 기종별로 가를 것인가(약 +89,671원) ②`replace` 4건 교체 ③`OHI-0555` 잠복 291%
④맥세이프 6건 실물 확인.

**리뷰**: 적대 리뷰 2라운드. 1라운드 **P1 3건 전건 수정**(브리지 창 종속 · 문서 구성 오류로
`manual`이 결정 목록에 섞인 것 · 교차검증 아닌 것을 교차검증이라 부른 것). 변이 21종 중 7종
생존 → **hard 축 5개 중 3개가 이 데이터에선 판정을 한 건도 만들지 않았다**는 것이 드러나
합성 입력 회귀 테스트 24건으로 못 박았다(교훈 #185). 교훈 #183~#185.

**이월**: 「형제 상품 대조」 렌즈(같은 SKU에 붙은 다른 쿠팡 상품과의 대조 — 맥세이프 7건을
근거 있는 후보로 승격시킬 수 있다) · 0건 판정값을 표에 명시.

---

### D-CPP-26 — 손익에 «근거 화면»을 붙인다. 근거는 화면이 낸 숫자를 재도출하지 않는다 (2026-08-08 새벽, PR #258)
Jino 원문(2026-08-07 23:3x): *"우리 손익(납품가 축)이 정말 실수 없이 나오는지 어떻게 확신할 수
있는지가 궁금해. 확인 버튼 누르면 서브창으로 넘어가서 근거를 보여주는거지"* → 「우리 손익
(납품가 축)」 패널의 **「근거 보기 ↗」** → `/rocket-1p/pnl-audit?from=&to=`.

**구조 (되돌리지 말 것)**
- **근거 창은 계산을 새로 하지 않는다.** 검사·원자 서비스는 화면 모듈을 **참조할 수 없고**
  (D-CPP-2 가드가 `app/services/` 아래의 원시 문자열을 금지한다 — 주석 산문도 걸린다),
  **라우터가 화면 응답을 주입**한다(`app/routers/overview.py:21`과 같은 승인된 패턴).
  문서 규칙이 아니라 구조다 — 서비스는 화면 숫자를 재도출할 길이 없다.
- **원자는 화면과 같은 창으로 뽑는다.** `day_option_atoms`의 `net_profit`·`burden_known`은
  **창-종속**이다(분담금 가드가 창에 걸친 프로모션 전체를 본다). 하루로 좁히면 화면이 «—»로
  그린 행에 숫자가 찍힌다(실측: wide net=None ↔ narrow 363,636.36). `/atom`은 창 필수·창 밖 422.
- **판정은 셋이다**: `pass`/`fail`/**`undetermined`**. 판정 못 하는 검사를 초록으로 칠하지 않는다.
  **B1(두 축 대사)은 영구 undetermined** — 1P 재고 데이터가 없어 차이를 설명할 수 없다.
- **통과해도 좌·우변 숫자를 싣는다** — 발견 0건과 실행 안 됨은 같은 숫자로 보인다(교훈 #123).
- **모집단 0건은 pass가 아니다** — A6(수집 신선도)·B2(계산서 0건·라인 테이블 부재)·A7(옵션 광고
  행 0·판매분석 미수집)이 각각 그 가드를 갖는다.
- **A3(그리고 A1·A2)은 동어반복이다** — 응답의 `vat`가 잔차라 좌변이 항상 순이익이다. 숨기지
  않고 note로 공개하고 테스트로 고정했다. 숨기면 그게 거짓 초록이다.

**실측으로 계획을 바로잡은 것 셋 (prod 근거)**
1. `rocket_product_cost_map` 267행 중 **`ignored`에도 `match_method='manual'`이 붙어 있다**(22건)
   → `status`를 먼저 봐야 «원가 제외 결정»이 «수기 확인»으로 둔갑하지 않는다. 배지 6종.
2. 원자 상세의 광고 원천을 `fetchone()`으로 뽑으면 **8,430 그룹에서 0원·축소된 광고비**를 근거로
   보인다(유니크 키에 `conv_option_id`가 있어 다행. 116,543 그룹 중 8,442개가 다행, 최대 21행).
   → SUM+GROUP BY. 다행 그룹 전부 `SUM=MAX`라 중복계상 아님을 확인.
3. **A7이 라이브에서 fail인 것이 정상이다** — 창 2026-07-31~08-06에서 「판매행 있는 옵션의 판매
   없는 날」 광고비 **435,916원**이 원자에도 `ad_no_sales`에도 귀속되지 않는다. 검사가 실제 결손을
   드러내는 것이다(엔진 수리는 별건).

**리뷰**: codex 한도 소진(리셋 08-09 16:16)으로 **Opus 적대 리뷰 4라운드로 대체** — P1 13건 발견·
전건 수정, 변이 43종 주입(백엔드 25 전건 KILLED / 프론트 18 중 14 생존 → 테스트 보강 후 해소).
**codex 소급 리뷰 이월.**

### D-CPP-25 — 원가 매핑은 «값»과 «링크 대상»이 다른 층이다. 링크는 두 렌즈가 일치할 때만 바꾼다 (2026-08-07 밤)
**원가표 업로드는 링크를 고치지 않는다.** 같은 날 「SellC 원가표 업로드」 세션이 `product_master.cost_price`
687행을 정본화하고 미연결 62건을 수기 매핑했지만, **링크가 가리키는 대상이 맞는지는 보지 않았다**(Jino 확인).
★**원가표가 정확해질수록 잘못된 링크의 오차는 커진다** — `OHI-0297`이 2,516→1,800(PET 정가)이 되자
거기 잘못 연결된 **강화유리** 상품의 원가가 더 낮아졌다.
판정 렌즈 둘: ①**제품 성격**(소재×기능) 대조 — 브리지 유무와 무관 ②**결정적 브리지**(ch5 `externalVendorSku`
코드 정확일치). **둘 다 지지한 11건만 교체**했고, 브리지 없음 3·브리지도 어긋남 1·상품명 없음 1은
**판정불가로 남겼다**(원칙22). **manual은 불가침**(Jino 확인분을 브리지가 덮지 않는다).
라이브: `suggested` 183→172 · `manual` 62→73 · 커버리지 **95.57%** · 순이익 **7,642,293원**(과대분 약 168만원 제거).
★필름 성격 판별 규칙(Jino 원문): **판별자는 「사생활」 유무 하나뿐**이고 저반사·무광·매트·지문방지는
**같은 기능의 다른 표현**이다. 이 규칙 하나가 1차 판정을 **34건 뒤집었다**(불일치 49→15).


> ⚠️**재번호 이력(2026-08-07 병합 시)**: 이 작업의 산출물은 처음 **ref 49 · 교훈 #155·#156**으로
> 붙였다가, 병행 세션이 먼저 main에 올린 것과 충돌해 **ref 50 · 교훈 #163·#164**로 뒤로 밀었다
> (main이 트렁크 → 내 것을 재번호). 본문은 바꾸지 않았고 번호와 참조만 고쳤다.
> **커밋 메시지·PR #231 본문의 「ref 49/교훈 #155·#156」은 옛 번호다** — 고칠 수 없어 여기 남긴다.
> 원인: `next_ids.sh`가 번호를 준 시점(origin/main=154)과 병행 세션이 #155~#162·ref 49를
> 가져간 시점이 겹쳤다. 도구가 막을 수 없는 동시성 구간이다.

### D-CPP-21 — 미정산 라인의 연령 기준일은 **하차일(제3자) → 센터도착일 → 같은 PO 정산입고일 → 판정불가** 순이다 (2026-08-06 밤, ref 50)
채무가 생기는 사건은 발주도 발송도 아니라 **인도**이고, 인도를 **제3자(한진)가 기록한** 시점이
하차일이다. `receiving_finished_at`이 의미론적으로는 더 정확하지만 원천이 **최근 90일 창만**
주므로 청구 159라인 중 40건(1,611,220원)에만 닿는다 — 그것을 정본으로 삼으면 8,033,970원 중
**6.4M이 판정불가**가 된다(교훈 #163). 하차일 기준이면 판정불가는 **2라인 88,590원**,
즉 애초에 「추적없음·청구 제외 권장」이던 라인들뿐이다.
방증: 하차일과 「같은 PO 정산라인 입고일」이 둘 다 있는 117라인의 격차 **중앙값 0일**(−6~+6) —
독립한 두 원천(한진 추적 ↔ 쿠팡 입고원장)이 같은 날을 가리킨다.
★**증거등급(집하/도착/하차)과 기준일은 다른 축**이다 — 등급은 "갔는가", 기준일은 "언제 갔는가".
섞지 않고 교차표로 낸다(ref 45 §17-6 오염 재발 방지).
라인마다 **어느 단계가 쓰였는지 `연령기준일출처` 컬럼에 남긴다** — 사슬을 되짚을 수 있어야 한다.

### D-CPP-24 — 원가 매핑 완료 후 드러난 손익 구조: **최대 매출 상품이 적자** (2026-08-07 라이브 실측)
Jino가 미연결 SKU를 전부 매핑한 직후의 실측이다. **커버리지 42.1% → 97.3%**(8/1~7 창,
30일 창은 65.8% → 95.9%). 연결 필요 0건, 남은 2.7%는 「원가 제외」로 결정된 11개뿐이다.
새 매핑 62건은 전부 `manual`(제안 수락 아님)이고, 단가가 갈리는데 원가를 공유하는 그룹은
새로 생기지 않았다.

- **이번 주 실측 손익 1,952,831원 · 이익률 12.4%**
  ```
  우리 매출 15,805,235 − 원가 6,246,901 − 분담금 1,926,000 − 광고비 5,484,219 − VAT 195,283
  ```
- ★★**Z폴드8 3종이 적자다 — 이번 주 −578,822원**(전체 적자 13건 −681,580원의 85%).
  | SKU | 매출 | 원가 | 분담금 | 광고비 | RoAS / BEP | 순이익 |
  |---|---|---|---|---|---|---|
  | 76350897 Z폴드8 | 5,676,000 | 2,925,978 | 1,419,000 | 1,700,647 | **3.34 / 4.26** | −336,022 |
  | 76350896 Z플립8 | 920,200 | 319,318 | 258,000 | 508,890 | 1.81 / 2.68 | −150,916 |
  | 76350898 울트라 | 996,000 | 513,438 | 249,000 | 334,635 | 2.98 / 4.26 | −91,884 |
- **원인은 둘이 겹친 것이다**(하나가 아니다):
  ① **원가율 51.6%** — 납품가 12,000원 / 등록원가 6,186원. 다른 상품 평균은 32%대다.
  ② **프로모션 분담금이 매출의 25%** — 프로모션 `686180`(7/23~8/15) 개당 3,000원 전액 부담.
  개당으로 보면 12,000 − 6,186 − 3,000 = **공헌이익 2,814원**인데 광고비가 개당 약 3,595원이다.
  ★**이번 주 분담금 1,926,000원 전액이 이 3종에서 나온다.**
- ★**개당 분해**(Jino 질문 "플립8의 BEP RoAS는 몇이야?"에서 나온 것):
  | | 납품가 | 원가 | 분담금 | 공헌이익 | 광고비 | BEP RoAS |
  |---|---|---|---|---|---|---|
  | Z플립8 | 10,700 | 3,713(34.7%) | 3,000 | **3,987** | 5,917 | **2.68** |
  | Z폴드8 | 12,000 | 6,186(51.6%) | 3,000 | **2,814** | 3,595 | **4.26** |
  플립8은 **원가율이 정상인데도 BEP가 2.68**이다 — 분담금 3,000원이 납품가의 28%라 공헌이익을
  그만큼 깎기 때문. 폴드8은 거기에 원가율 51.6%가 얹혀 4.26이 된다.
  → **분담금이 BEP를 밀어올린 공통 주범이고, 폴드8만 원가 문제가 하나 더 있다.**
  프로모션(7/23~8/15) 종료 시 플립8 BEP는 약 1.53으로 내려간다(현재 RoAS 1.81 → 흑자 전환).

★**RoAS 3.34는 통상 좋은 수치인데 이 상품은 손익분기가 4.26이다.** D-CPP-23의 「손익분기
  RoAS」 열이 도입 당일 실제로 쓸모를 증명한 자리다 — RoAS만 봤으면 «잘 되는 상품»으로 읽었다.

★**이익률 29.6% → 12.4%는 악화가 아니라 «가려진 것이 드러난 것»이다.** 매핑 전엔 원가가
안 붙은 상품이 손익에서 통째로 빠졌고(분담금·광고비도 함께), 남은 부분집합만 계산돼 이익률이
높게 보였다. D-CPP-23이 «이익률을 전체 매출에 곱하지 마세요»를 화면에 박아둔 근거가 바로
이것이고, 실측으로 확인됐다 — **빠진 부분집합은 무작위가 아니라 «원가가 안 붙은 신제품»이고,
그건 대개 마진이 얇은 쪽이다.**

⚠️미결(Jino 결정 영역): Z폴드8 3종 처리(광고 축소 / 납품가 인상 / 원가 절감 / 프로모션 종료).
프로모션 원천은 7건 중 4건만 할인액 보유 — 나머지 기간을 조회하면 손익을 내지 않고 사유를 낸다.

### D-CPP-23 — 1P 매출 화면에 손익을 올린다. 단 **원가 확인분만** 더한다 (2026-08-07, Jino 지시)
Jino 원문: *"여기에 비용을 넣어서 우리 손익까지 넣자. 광고비까지는 나오는데, 옆에 원가,
그래서 우리 손익이 얼마인지까지 나오게 하자"* / *"해당 항목을 넣어주고, 원가는 등록을 별도로 해줄께"*

- **종전 결정을 뒤집는다**: `rocket_1p_revenue`는 "순이익을 여기 놓지 않는다"였고 근거는
  「원가 축이 없다」였다. 그런데 `_sell_through_window`가 **이미** 원가·분담금·VAT를 계산하고
  있었고 화면만 그걸 못 보고 있었다. 근거가 사라진 금지선은 유지하지 않는다.
- **D-CPP-2는 그대로 불변**: 손익 축은 **우리 매출(납품가) 하나뿐**이다. 소비자 매출(쿠팡가)은
  손익 어디에도 안 들어간다 — 그건 쿠팡의 매출이다. 이 화면은 여전히 조회 전용이고 종합조망
  `net_profit`에 결합되지 않는다(참조 금지 가드 테스트 유지).
- ★**원가 미상을 0으로 넣지 않는다.** prod 실측 2026-08-06 원가 커버리지 **48.9%**
  (우리 매출 2,234,640원 중 1,142,810원이 원가 미등록). 0원 원가로 채웠으면 그 매출이 통째로
  이익이 되어 순이익이 두 배로 보였을 것이다. 대신 **매출·원가·분담금·광고비를 전부 같은
  부분집합으로 제한**하고(`pnl.basis='costed_subset'`), 화면이 그 사실과 커버리지를 배지로 말한다.
  커버리지 100%면 자동으로 `basis='full'`이 되어 창 전체의 손익이 된다 — 코드 변경 없이.
- ★**"—"로 끝내지 않는다**: 원가 미등록 SKU를 매출 상위 10개까지 **이름으로** 나열한다.
  죽은 대시가 아니라 작업 목록이다(Jino가 SellC에 등록하면 그만큼 커버리지가 오른다).
  라이브 08-01~07: 미등록 48 SKU · 우리 매출 9,409,810원, 1위는 Z폴드8 필름 5,676,000원.
- 분담금을 모르면(제안서 미수집 기간) **손익 자체를 내지 않는다** — 0으로 접으면 그 할인액만큼
  이익이 부풀어 보인다. 판정은 손익 엔진 한 곳(`promo_burden_by_option`)이 한다.
- 옵션 순이익은 **전 단위로 반올림**하고 합계는 그 행들의 **합**으로만 만든다. VAT가 ÷11이라
  반올림 없이 두면 누적 순서 차이만으로 표와 타일이 끝자리에서 어긋난다(실측 2e-25원).
- 라이브 검증(2026-08-07 13:4x KST, prod): 08-01~07 순이익 2,026,181원(이익률 29.6%,
  커버리지 42.1%) · 옵션 123행 합 = 타일 원 단위 일치(순이익·원가·광고비 3축 모두).

### D-CPP-22 — D-CPP-20의 축 전환은 **숫자를 바꾸지 않는다**. 그래도 유지한다 (2026-08-07, ref 50 §10)
라이브 실측: 두 축(작성일 vs 라인 실입고일)의 **일별 합계가 305일 전건 동일**, 한쪽에만 있는 날 0,
총액 934,800,723원 동일, 월별 diff 14개월 전부 0원. 계산서가 하루치 입고 단위로 끊기고
(480/480, 여러 날 걸친 계산서 **0건**) 작성일이 곧 그 입고일이라 **정의상 같은 값**이다.
- D-CPP-20의 근거였던 "작성일−실입고일 −4~+7일 / 다른 달 입고분 6,356,855원 / 여러 날 섞인
  계산서 18,388,235원"은 **라인이 없던 시점에 PO 납품예정일을 대리 변수로 삼아 잰 값**이었고
  실제 라인이 들어오자 뒤집혔다. 코드 주석도 실측으로 교체했다.
- 인계의 월별 이동액(04 +10,494 / 05 +83,127 / 07 +830,500)은 **라인 없는 역발행 차감 계산서
  3건의 금액과 정확히 일치** — 이동이 아니라 그 시점 「후」 계산에서 폴백분이 빠졌던
  **측정 아티팩트**다. 배포 코드의 폴백은 정상 동작한다.
- **되돌리지 않는다**: ①작성일=입고일은 관측이지 쿠팡의 보장이 아니다(관행이 바뀌면 라인 축만
  옳다) ②라인은 SKU 그레인이라 재판정·연령·원가 결합이 여기 얹힌다.
★D-CPP-20을 "날짜를 고쳤다"로 기억하면 안 된다 — **고친 것은 없고 연 것이 있다**(정산 라인 9,926행).

### D-CPP-20 — 계산서 축 일별 매출의 귀속 기준은 작성일이 아니라 실입고일이다 (방향 확정, 2026-08-06 저녁)
계산서 1건이 평균 **5.8 PO**를 묶고, **79%(380/480)는 계산서 안의 납품예정일이 1개**이며,
작성일−납품예정일 = **−4~+7일**로 흔들린다. 현행 작성일 귀속은 **총액은 맞지만 날짜 배치가
틀린다** — 2026-07 실측으로 작성일 기준 51,193,984원 중 **다른 달로 가야 할 6,356,855원** +
**여러 날이 섞인 18,388,235원**이 나왔다. 구현은 다음(D-CPP-19가 선행 조건).

### D-CPP-19 — 로켓1P 발주는 실입고 시각을 담는다 (2026-08-06 저녁, PR pending)
`receiving_started_at`/`receiving_finished_at`. 원천 API 응답에 `receivingStartedAt`/
`receivingFinishedAt`이 계속 오고 있었는데 파서가 버리고 있었다. `expected_delivery_date`는
**예정**이라 실제와 어긋난다 — 납품예정 08-04인 PO들의 실입고 완료가 08-04 05:17 /
08-05 07:05 / 08-05 17:24로 갈렸다. 입고 전이면 NULL로 두고 예정일로 대체하지 않는다
(원칙22). 페처는 raw 페이지를 그대로 push하는 구조라 **Mac 페처 수정 0 · 추가 API 호출 0**,
과거 90일치도 다음 수집에서 자동으로 채워진다.
배포: `safe_deploy.sh --migrate --restart`, 마이그레이션 `e2c7a4f19b30 → f4a9c2e70b58`,
무중단 재시작 다운타임 0초, CAS 3파일 통과. 라이브 검증: 원천 표본 150건 ↔ DB 시각 단위까지
전건 일치(불일치 0), 수집 창 안 「거래명세서확인」 426건 전건 값 있음, 입고 전 상태(발주확정
58·거래처확인요청 34) 전건 None, 전체 442건 채워짐(2026-05-09~08-06).

### D-CPP-18 — 옵션ID별 광고비 401일 전수 소급 완료 (2026-08-06 오후~저녁)
`tools/ohitech_ad_option_backfill.py`로 2025-07-01~2026-08-05 401일 전부 적재했다. **빠진 날 0.**
178,956행(Retail 131,935 / 3P 47,021). `options_only=True` 경로라 계정 총액 머니 테이블(`ad_costs`)
에는 영향이 없다 — 옵션 단위 분해만 채운 것.

### D-CPP-17 — 비-PA = NCA, 판매방식은 비례배분하지 않고 실측으로 가른다 (2026-08-06 오후~저녁)
ref 46이 "3P 비중은 추정 불가"로 남겨둔 비-PA 4,527,079원의 정체를 `reportType='nca'` 보고서로
실측했다. 「판매방식」 컬럼을 직접 읽으면 열 달 전부 NCA 합계가 원천 비-PA와 **0.09% 안에서
일치**했다 — 이게 그 비-PA다. Retail 비중은 **달마다 뒤집힌다**(2025-07 0% / 08 94.5% /
09 100% / 10 99.4% / 11 53.7% / 12 0% / 2026-01 0%) → 어떤 고정 비율로도 비례배분하면 틀린다.
소급 구간(D-CPP-16 결손 구간)에 Retail분 **4,199,736원** 가산. 전환매출도 **8,245,140원** 같이
올렸다 — 기존 `conversion_revenue`가 ad_spend와 같은 PA Retail 축이라 한쪽만 올리면 RoAS가
인위적으로 떨어진다(D-CPP-2 취지 위반).

### D-CPP-16 — 1P 광고비 결손이 2026-03-16에 안 끝났다 — 2026-03-17~06-17 64,349,348원 추가 발견·적재 (2026-08-06 오후~저녁)
D-CPP-14/D-21이 메운 2025-07-24~2026-03-16 결손 뒤에 **또 다른 결손 93일**이 있었다. 소급 도구의
`GAP_END="2026-03-16"`과 "라이브는 2026-05-18~"이라는 전제 사이가 아무도 안 보는 구멍이었다.
근거: 그 구간 Billboard 판매방식 100% Retail(3P 0%) + NCA도 2026-04·05·06 전부 100% Retail이라
3P가 얹히지 않는다 — `report/SALES` ALL 전액으로 93일 안전하게 적재했다.
★특히 **2026-05-28~06-17은 행은 있는데 값이 거의 0**이라 결손 카운터가 "있다"고 세어 63일간
아무도 못 봤다(행 부재 판별자만으로는 못 잡는 결손 — D-CPP-14 P1-④와 같은 계열의 함정).
교차검증: 세 API 항등식 `DB = report/SALES ALL − Billboard PA 3P − Billboard NCA 3P`가
+0.018%(60,066원)에서 성립. `reconciliation diff_pct`가 소급 구간에서 0.00% → −0.05~−6.40%로
바뀌었고 **이게 정상이다** — 이전 0.00%는 D-21이 같은 XLSX를 계정 총액에 넣어 같은 숫자를
두 번 보던 것(교훈 #141의 재현).

### D-CPP-15 — 적대 리뷰 이월 2건 처분 (2026-08-06 오전, PR #222·#223)
Jino 지시로 D-CPP-14의 이월 2건을 닫았다. **둘 다 착수 전 실측으로 지적을 부분 정정했다** —
리뷰어 결론을 그대로 받지 않고 근거를 다시 세운 것이 이번 처분의 핵심이다.

- **GFA 신선도 판정을 데이터→수집기로**(PR #222, 교훈 #147). 이 배지는 **두 판 연속 틀렸다**:
  1판 소스별 MAX(ad_date)=거짓 빨강 63일 → 2판 계열 합집합 MAX=**거짓 초록**.
  ★근본 원인은 판정식이 아니라 **신호가 겹친 것**이다 — `ad_costs`의 '행 없음'이 「소진 0」과
  「수집 실패」를 겸한다. 겹친 신호로는 어떻게 집계해도 한쪽이 틀린다(오탐의 **방향만** 바뀐다).
  → 겹치지 않는 신호로 판정 대상을 옮겼다: `scheduler_state.last_run_at`(마지막 성공).
  **새 테이블·마이그레이션 0.** 잡 실패/미실행/비활성이면 데이터가 신선해도 빨강, 잡 성공 +
  소스 행 없음이면 초록. 판정 근거 없으면(잡 미등록) 초록으로 넘기지 않는다.
  ⚠️**정정**: 착수 시 "거짓 초록이 라이브에 떠 있다"고 했으나 실측하니 advoost가
  30k→5,925→0으로 잦아든 것이었고 잡은 정상(08-06 07:10 ok) — **당시 초록은 결과적으로 맞았다.**
  고친 것은 "그걸 구별할 수 없다"는 쪽이다.
- **광고 리포트 ROAS의 축을 행마다 표기**(PR #223). ★**지적 절반 기각**: D-CPP-2는 소비자
  실현가를 "BEP ROAS의 분자로만" 쓰는 것을 **명시적으로 허용**한다(금지는 회계 매출 사용).
  "축이 중간에 바뀐다"도 실측 252.7%↔266.0%로 미미해 **근거 부족 기각**.
  ★진짜 결함은 **같은 ROAS 열인데 행마다 뜻이 다른 것**: 3P·2P 전환매출=우리 매출,
  Retail=**쿠팡 매출**. 라이브 3P 217.4% vs Retail 254.0%로 "1P가 낫다"로 읽히는데 1P를
  납품가로 환산하면 **약 151%로 오히려 나쁘다 — 결론이 뒤집힌다.** 합계는 둘을 더한 값이라
  어느 축으로도 해석 불가.
  → 행별 `roas_basis` 노출 + 「ROAS 기준」 열 + 비교 금지 상설 경고. 합계는 `mixed`로 밝힌다.
  ★**환산값을 만들지 않았다** — 우리 몫 비율은 기간마다 다르고 판매분석이 롤링 2개월만 덮어
  과거는 환산 근거가 없다. 추정하느니 축을 이름으로 밝힌다. **숫자는 하나도 안 바꿨다**(회귀 가드).

### D-CPP-14 — codex 대체 자체 적대 리뷰 → P1 5건 수정 (2026-08-06)
Jino: *"일단 codex리뷰는 우리의 자체적인 적대리뷰로 대체하자"*(codex 한도 08-09). 3렌즈 병렬
(데이터 무결성 / 돈·회계 / 운영·실패모드), 각 렌즈에 **재현 가능한 근거**를 요구하고 못 대면 P2로 강등.
P1=0 게이트 규칙 그대로 적용. **P1 5건 전부 재현 확인 후 한 라운드로 수정·배포·라이브 검증.**

- ★**P1-① 광고 축이 두 엔진에 중복 구현 → 43,147,487원 누락**(교훈 #146). `_agg_rocket_ad`가
  수기 XLSX(`ad_costs` ch5)를 안 읽어 2026-03-17~05-18 63일이 통째로 빠졌다. 라이브 정정:
  `ad_spend` 552,537 → **43,700,024** / `net_profit` 83,819,364 → **40,671,877**.
  데이터를 옮기지 않고 **호출을 `_ad_spend_by_day` 하나로 모았다**(값을 맞추면 또 갈라진다).
  ⚠️D-21이 앞 8개월을 채우면서 이 구멍이 **완결된 것처럼** 보이게 됐던 것이 핵심 교훈이다.
- **P1-② 판매분석 미수집 창에서 「우리 매출 0원·RoAS 0.00」을 사실처럼 표시** → 판매 축 전부 null +
  `coverage.sales_data_covered`/`sales_data_from` 노출. ★판별자는 qty가 아니라 **행 존재**다.
- **P1-③ 화면이 "7일"이라며 5일을 표시(−19%)** — 판매분석은 당일·전일치를 안 준다.
  `window_freshness()` 신설(data_as_of·days_no_data·lag_days·stale) + 두 화면 상설 배너.
  ★정상 지연(D-1~D-2)과 진짜 정지를 색으로 가른다 — 늘 경고가 떠 있으면 경보가 죽는다.
- **P1-④ 결손 카운터가 행 부재를 못 잡는데 "없음"이라 단언 + 이중계수**(pv 결손+orders 결손을
  더해 같은 하루를 2번). 정의를 하나로 통일하고, **행 자체가 없는 결손은 날짜 축(freshness)으로
  따로** 센다 — 둘은 다른 고장이다.
- **P1-⑤ 비율 두 개의 지지집합 불일치**: `Σqty/Σorders`는 과대, `Σpv/Σvisitors`는 과소.
  `cvr`만 pv·orders가 함께 결손이라 우연히 안전했다. 각 비율의 분자·분모를 함께 건다.
- P2 채택 3건(평점 미관측 처리·`median_page_views` null이 "0"으로 렌더·`_ATTRS_SQL` vendor 필터).
- ⚠️**이월**: D-21이 소비자 판매가 838,896,920원을 `conversion_revenue`에 적재 → 광고 리포트
  화면의 1P ROAS가 **1.68배 과대**(345.8% vs 납품가 기준 ≈205.6%). 회계엔 안 들어가 P2지만
  D-CPP-2 취지를 화면에서 어긴다. 수정하려면 광고 리포트 화면을 건드려야 해 이번 스코프 밖.
- ⚠️**스코프 밖 발견(다른 세션 변경)**: GFA 신선도 배지가 형제 소스 하나가 죽어도 초록
  (`by_source` 합집합 MAX로 판정). 라이브에 그 상태가 떠 있다(`gfa:advoost` 08-05 행 없음).
  선례 있음 — ADVoost+GFA 59일 488만원 누락으로 RoAS가 BEP 아래였던 것이 사후 판명.

### D-CPP-13 — 1P 유입·전환 퍼널 전문 메뉴 신설 (S3, 2026-08-06)
Jino: *"조회·주문·장바구니·검색량까지 보는건 **별도의 전문 메뉴**를 만드는게 이 디테일한 자료까지
보는데 도움이 될거 같은데?"* — 매출 화면에 칸을 늘리려던 내 안을 Jino가 갈랐고 그게 맞았다.
두 화면이 답하는 질문이 다르다(「얼마 벌었나」=합계 중심 ↔ 「왜 안 팔리나」=옵션 단위로 깊게).

- 신설 `GET /api/overview/rocket-1p-funnel` + `/rocket-1p-funnel` 페이지(내비 「🔎 로켓1P 유입·전환 퍼널」)
- 축: 방문자 → 조회 → 주문 → 판매수량 + 조회/방문(탐색 깊이)·주문당 개수·평점(리뷰)·품절
- ★★**전환율은 Σ주문 ÷ Σ조회로만 낸다.** 일별 비율의 평균이 아니다 — 조회 4회에 1건인 날(25%)과
  400회에 40건인 날(10%)을 평균 내면 17.5%가 되는데 실제 기간 전환율은 10.1%다. **작은 날이 큰
  날과 같은 표를 갖는다.** (S1이 orders·page_views를 담기 전에는 이 계산이 애초에 불가능했다.)
- ★**전략 추천 금지선 준수**: 「위치」는 기간 중앙값 대비 상/하 **서술**이지 권고가 아니다.
  판정에 쓴 임계값(중앙 전환율·중앙 조회수·최소 조회수·판정 대상 수)을 **전부 응답과 화면에 노출**한다
  — 숨은 기준으로 나눈 분류는 사실이 아니라 의견이다. 최소 조회수는 화면에서 사용자가 바꾼다.
- ★**표본이 얕으면 판정하지 않는다**(`low_sample`): 조회 3회에 주문 1회(33%)를 상위로 올리면 표
  전체가 거짓말을 한다. 값은 그대로 보여주되 위치만 매기지 않는다.
- ★**리뷰 0건의 평점은 `null`**(0점 아님) — 0.0으로 그리면 신상품이 최악의 상품 자리에 앉는다.
- ★**부분 결손을 센다**: `SUM`은 NULL을 조용히 건너뛴다. S1 이전 행이 섞이면 합계가 부분값이 되어
  전환율이 이유 없이 튄다 → 옵션별·기간 전체로 결손 일수를 세어 화면에 띄운다(현재 0일).
- ⚠️**`is_item_winner`는 열로 내지 않았다** — 1P 244옵션 **전부 False**(실측)라 정보량이 0이다.
  3P와 의미가 다를 수 있다(쿠팡이 직접 파는 구조). 값이 생기면 그때 넣는다. S1에서 0값 지표
  4종을 뺀 것과 같은 판단이다.
- 라이브 실측 근거: 08-04 방문자 1,443·조회 2,763·주문 363·판매 342·전환 **13.14%**(쿠팡 화면 일치).
  최근 7일 조회 30+ 옵션 35개의 전환율 분포 **5.95%~31.71%** — 편차가 실재해 진단 가치가 있다.

### D-CPP-12 — 1P 매출 두 축 대조 화면 신설 (S2, 2026-08-06)
Jino: *"나는 SellC 화면에서 소비자 판매가도 포함된 매출 대시보드를 보고 싶은데."*

**문제**: 쿠팡 판매분석 화면엔 **우리 매출이 없고**, 우리 종합조망엔 **소비자 판매가가 없다.**
08-04 하루를 두 화면에서 보면 6,536,000원과 3,885,820원이 나오는데 **왜 다른지 어디서도 확인할 수 없었다.**
- 신설 `GET /api/overview/rocket-1p-revenue` + `/rocket-1p-revenue` 페이지(내비 「로켓1P 매출(소비자가∥납품가)」)
- 라이브 실측 08-04: 소비자 6,536,000 ∥ 우리 3,885,820 → **우리 몫 59.45% · 쿠팡 마진 40.55%**
- ★**재도출 금지**: 합계의 우리 매출·계산서 매출·광고비는 대시보드가 쓰는
  `compute_rocket_1p_summary_row`를 **그대로 호출**해 얻는다. 같은 숫자를 두 곳에서 따로 계산하면
  언젠가 갈라지고 그때 어느 쪽이 맞는지 아무도 모른다(테스트가 문자열 동일성으로 고정).
- **정직성 규칙 4개**(테스트로 고정): ①두 매출을 더하지 않는다(이중계상) — 열 이름에 축을 박는다
  ②납품단가 미상은 `null`(0 아님) — 0은 "공짜로 줬다"는 뜻. 소비자 매출은 살려 보여준다(LEFT JOIN)
  ③RoAS는 **우리 매출 기준** — 소비자 매출로 내면 못 번 돈으로 광고를 정당화한다
  ④순이익을 이 화면에 놓지 않는다(D-CPP-2 불변) — **가드 테스트가 다른 서비스의 참조를 막는다**
- 회귀 없음 라이브 확인: 종합조망 1P leaf가 신설 전후 동일(revenue 3,885,820 · net_profit null · 커버리지 27.3%)

### D-CPP-11 — 판매분석 퍼널 지표·옵션 속성 적재 (S1, 2026-08-06)
이미 받고 있던 `vi-detail-search` 응답에서 **버리던 값**을 담는다. 추가 API 호출 0.
- 일별(`coupang_rocket_sales_daily`): `page_views`(조회)·`orders`(주문) — 06-01~08-04 **2,938행 전량 충전**
- 현재값(`coupang_rocket_option_sku`): `brand_name`·`category_path`·`is_item_winner`·`is_oos`·
  `rating_count`·`rating_score`·`attrs_observed_at` — 244/244 충전
- ★**축을 가른 근거는 실측**: 08-04와 06-05를 공통 옵션 22개로 대조하니 위 속성이 **전부 동일**했다
  (같은 응답의 metrics는 날짜별로 달랐으므로 대조군 성립). 즉 **조회 시점 현재값**이지 그 날짜 상태가
  아니다 → 일별에 넣었으면 6월 행에 오늘의 품절 상태가 찍혀 **가짜 시계열**이 됐다.
- ⚠️**안 담은 것**: `totalAddToCart`·`searchVolume`·`srpClick`·`srpClickShare` — 3개 날짜 전 옵션에서 0
  (프리미엄 데이터 2.0 등급 지표로 보인다). 0을 담으면 "관측 없음"과 "정말 0"이 영영 구분되지 않는다.
  **유료 등급 전환 후 재확인할 것.**
- upsert 규칙이 기존과 다르다: 신규 필드는 **None이면 덮지 않는다**(excel 폴백·구버전 페처가 아는 값을
  지운다). 속성이 하나도 안 오면 `attrs_observed_at`도 전진시키지 않는다.
- 매핑 검산: 08-04 Z폴드8 조회 1,540·주문 162 → `162/1540 = 0.1051948052`가 쿠팡 `pvToOrder`와 자릿수까지 일치
- 조회창은 **롤링 약 2개월**(서버 실측 응답 `viewable period [2026-06-01 ~ 2026-08-05]`) — 소급은 그 안에서만.
  무료체험 종료 2026-08-20이나 **Jino 확정: 유료로 계속 운영**(2026-08-06)이라 구독 시한은 제약 아님.

### D-CPP-1 — 프로모션은 자동 수집한다 (수기 입력은 폴백)
Jino가 프로모션 시작 시각을 알려주는 수기 경로는 **최후 폴백**이고, 기본은 공급자허브/Wing에서 자동 수집한다. 이유: 수기는 잊혀지고(원칙 22의 "낡음은 조용하다"), 프로모션은 초 단위 기간을 갖는다.

### D-CPP-2 — 1P 매출 인식 = 납품가 축. 쿠팡 자체 인하는 우리 비용이 아니다
1P 회계 매출은 기존대로 **발주(납품)금액**(트랙 rocket-1p D-3)이다. 판매분석의 소비자 실현가(쿠팡이 자체 마진으로 내린 가격 포함)는 **회계 매출로 쓰지 않는다.** 그 값은 **BEP ROAS의 분자(수요·전환 신호)에만** 반영한다.
→ 새 테이블 `coupang_rocket_sales_daily.revenue`는 **회계축이 아님**을 스키마 주석에 못 박는다.

### D-CPP-3 — RG 분담금 권위값 = 쿠폰 "사용 금액"
2P RG에서 우리가 실제로 부담한 즉시할인 금액의 정본은 **쿠폰별 "사용 금액"**(Wing 화면 표기, 예: 쿠폰 94177420 = 156,000원)이다. 등록가(unit_sales_price)나 할인액×수량 추정으로 대체하지 않는다.

### D-CPP-4 — 분담금 청구 방식은 미확정 — 9월 정산서 도착 시 대사
1P 프로모션(예: Request 687878, 2026-07-24~26, 분담비율 100%)의 분담금이 **어떤 형태로 청구되는지 확인되지 않았다.** 매입정산(`coupang_rocket_settlement`)에 07월 프로모션 흔적 없음 — 정산일이 9월이기 때문. **추측으로 회계에 반영하지 않는다.** 9월 정산서가 도착하면 실제 라인과 대사한 뒤 D-CPP-N으로 확정한다.

### D-CPP-5 — 판매분석은 BETA + "Basic 무료체험중" — 접근불가 감지가 필수
`.../web-view?type=SALES_ANALYSIS`(RPD)는 BETA 표기 + 구독 체험 상태다. 유료화·권한 회수로 **조용히 끊길 수 있다.** 수집기는 403/구독 오류를 성공으로 접지 말고 실패로 표면화해야 한다(원칙 22).

### D-CPP-6 — RG saleAmount ↔ seller_discount 상계 관계: **표본 부재로 미확정** (2026-07-28 prod 실측)
prod SELECT 실측 결과:
- `coupang_revenue_fee` 전 기간 643행 중 **`seller_discount_coupon <> 0`인 행이 0건**.
- 07월 쿠폰 대상 옵션(95536607339·95570603512·95570603530)은 `coupang_revenue_fee`에 **0행** — RG(2P) 판매는 revenue-history(3P 정산)에 애초에 잡히지 않는다.
- 같은 옵션의 `coupang_rg_order_item.unit_sales_price`는 쿠폰 기간 내내 **불변**(16,900 / 15,900). 07-02 18:33~07-03 23:59 S26울트라 4,000원 쿠폰 구간에서 **수량만** 4→10→15로 뛰고 단가는 안 움직였다 → 브리핑의 "등록가라 할인 미반영"이 라이브로 재확인됨.

**판정**: "saleAmount가 셀러부담 할인 차감 전인지 후인지"는 **표본이 없어 확정 불가**. 다만 구조적으로 **RG 판매에는 revenue-history 자체가 없으므로 그 경로는 RG 분담금의 권위값이 될 수 없다** → D-CPP-3(쿠폰 사용 금액)이 유일 권위값이라는 근거가 강해졌다. 3P 판매에서 셀러쿠폰이 걸린 표본이 생기면 재측정한다.

### D-CPP-7 — 프로모션당 할인액은 **단일값** → 수기 입력 1칸 (2026-07-28 Jino 확정)
한 프로모션에 상품이 여러 개 들어가도 **할인 가격은 모두 같다.** 그리고 공급자허브 프로모션
목록·상세 API에는 **상품별·단위 할인액 필드가 없다**(라이브 실측 — 있는 건 `discountBudget`(총예산),
`supplierFundRate`(분담%), `discountType`(할인방식)뿐). ⇒ `coupang_rocket_promotion.unit_discount_amount`
1칸을 **수기 입력**(ops PATCH)으로 받는다. 페처는 이 칸을 절대 쓰지 않으므로 재수집이 수기값을 지우지 않는다.
> "한 프로모션당 할인하는 가격이 하나로 정해지게 되어 있어. 그래서, 한 프로모션에 제품은 여러개가 들어갈 수 있지만 할인 가격은 모두 같은게 맞아."

### D-CPP-8 — 원가는 환율에 따라 상시 변동 → 시드 스크립트 폐기, 단일 진실은 `product_master.cost_price`
Jino 원문: *"원가가 환율에 따라서 조금씩 변해. 그래서, 항상 약간씩 변해."* 원가를 특정 스크립트에
고정값으로 박아 넣는 방식(`backend/scripts/seed_promo_pnl_costs_20260728.py`)은 이 현실과 맞지 않는다
→ **폐기(삭제)**. 원가의 단일 진실 원천은 sellC `product_master.cost_price`(수기 관리)이며, 스크립트가
아니라 이 값을 그때그때 고친다. `product_master.cost_price`는 조회 시점마다 소급 적용되는 값이므로
(`profit_calculator.py`·`bep_calculator.py` 실측 — D-CPP-4 시드 스크립트 주석 참조), **단일 현재값의
소급 적용은 승인된 근사**로 취급한다(과거 시점 원가와 정확히 일치하지 않을 수 있음을 알고 쓴다).
시점별 원가를 이력화(환율 변동에 따른 원가 스냅샷 저장)하는 것은 **필요성이 확인되면 별도 D-CPP-N으로
재론**한다 — 지금은 착수하지 않는다.

### D-CPP-10 — 개당 할인액의 유일 원천은 **제안서 엑셀**이다 (2026-08-05 라이브 확정, 구현·배포 완료)

D-CPP-7은 "쿠팡이 안 주니 수기 1칸"이었다. 2026-08-05 세 표면을 **전부** 열어 확인한 결과 그 전제가
확정됐고, 수기 대신 **파일 자동수집**으로 대체했다(수기 칸은 폴백으로 남긴다).

| 확인한 표면 | 결과 |
|---|---|
| `GET /promotion/promotion-request/{id}` | `discountBudget`(총예산)·`supplierFundRate`(분담%)·`discountType`뿐 |
| 프로모션 상세 **화면** | 「할인액(원)」 칸이 **빈칸**, 「적용 상품」은 개수만(클릭 불가) |
| **계약서 본문** `GET /restapi/v2/contract/contracts/{id}/details` | 대상 상품이 "첨부. 즉시할인 대상 상품"으로 빠져 본문에 없음 |

**구조**: Mac이 구글드라이브 폴더를 읽어 push, **매칭은 서버**(틀리면 분담금이 엉뚱한 행사에 붙는
규칙이라 테스트가 붙는 쪽에 둔다). 파일명 규칙 =
`instant_upload_template_<시작YYMMDD>_<종료YYMMDD>_<라벨>.xlsx` → 기간으로 프로모션 매칭,
기간이 같으면 라벨로 가름, 못 가르면 **붙이지 않는다**.

**신규**: `coupang_promo_discount_item`(라인 단위, 마이그 `c8e1a4b7d201`) ·
`clients/coupang/rocket_promo_file.py`(SA) · `services/coupang/rocket_promo_file_sync.py`(Harness) ·
`POST /api/coupang/ops/rocket/promotion/discount-files/ingest` · `tools/promo_file_fetcher.py` · 테스트 13개.

**라이브 합격(prod 2026-08-05 14:30)** — 4파일 8항목 적재, 분담금 실측:

| 프로모션 | 기간 | 판매량 | 분담금 | 예산 | 소진율 |
|---|---|---:|---:|---:|---:|
| 685840 | 07-22~23 | 58 | 232,000 | 1,000,000 | 23.2% |
| 686180 플립폴드8(진행 중) | 07-23~08-15 | 367 | 1,101,000 | 3,000,000 | 36.7% |
| 687878 | 07-24~26 | 81 | 324,000 | 1,000,000 | 32.4% |
| 688054 폴드7 | 07-24~26 | 21 | 63,000 | 100,000 | 63.0% |
| **계** | | 527 | **1,720,000** | 5,100,000 | 33.7% |

★687878·688054는 **기간이 완전히 같은데 라벨로 정확히 갈렸다**. ★분담률은 13건 전부 **100%**(전액 우리 부담).
★★**교훈 #140**: macOS 파일명 한글은 NFD, 쿠팡 API는 NFC — 눈에 같은데 비교가 False라 매칭이
**에러 없이 0건**이 됐다. `unicodedata.normalize("NFC")`를 양쪽 경계에 강제하고 NFD 리터럴로 회귀 고정.

⚠️**여전히 손익 미반영**: 1,720,000원은 **우리 추정 부담액**이지 청구액이 아니다(D-CPP-4 유지 —
9월 정산서 대사 후 확정). 1P 일별 손익(B축) 화면을 만들 때 비용으로 싣는다.

⚠️**즉시할인 계약 13건 중 프로모션 API는 7건만 준다** — 2~5월 5건(예산 합 2,751만원)이 목록에 없다
(원인 후보: 수집기가 `requestType=COMMON`만 조회, 화면에 「특별 기획전」 탭 별도).
**Jino 지시로 추적하지 않는다**: *"파일이 없는건 그냥 무시하고 넘어가자. 그건 나중에라도 찾지 말자."*

---

## 확정된 API 스펙 (2026-07-28 라이브 정찰 — 추측 아님)

| 스트림 | 호출 | 확인된 사실 |
|--------|------|-------------|
| 판매분석 | `POST /retail-insight/api/business-insight/vi-detail-search` body `{startDate,endDate,registrationTypes:["RETAIL"],pageNumber,pageSize,sortBy:"GMV",sortOrder:"DESC",isKanCategoryCode:true}` | ★**요청 구간을 합산해** 준다 → 옵션×일을 만들려면 **하루 단위 호출**. 응답 `vendorItems[] + paginationDetails{pageSize,pageNumber,totalResults,totalPages}`. 옵션ID=`vendorItemId`, SKU=`externalSkuIds[0]`, 수량=`totalUnitsSold`, GMV=`totalGmv`, 유입=`totalUniqueVisitor`, 전환=`pvToOrder`(**이미 0~1 소수** — 140/1141 검산 일치) |
| 유효 구간 | 범위 밖 날짜 요청 시 `400 {"code":"INVALID_DATE","message":"... viewable period [2026-06-01 ~ 2026-07-27]"}` | **롤링 창(약 57일)** — 서버가 본문에 구간을 적어 준다. 페처가 파싱해 자동 보정(일수 하드코딩 금지) |
| 프로모션 | `GET /promotion/promotion-request?requestType=COMMON&page&size` (Spring Page) + `GET /promotion/promotion-request/{id}` | 실측 `totalElements=7`(계정 전체가 1페이지). **상세 = 목록과 필드 동일**(신규 필드 없음). `settlement_date`(정산일)는 **API에 없다** → NULL |
| 구독 게이트 | `GET /rpd/v2/supplier/subscription/detail` | 200 `data.permittedLevel=BASIC`, `detailInfo.subscribedLevel=FREE`, **`freeTrialEndDate=2026.08.20`** — 이 날짜 이후 조용히 끊길 수 있다(D-CPP-5) |

---

## 사용자 원문 인용 (왜곡 방지)

> "내가 sellC에서 이런 프로모션을 시작하게 되는 시간을 알려주면 너가 그것을 보고 계산에 적용하는 구조로 가면 되지 않을까?"

> "로켓배송이기 때문에 납품하는 회사는 입고비용, 배송비용, 반품비용 모두 쿠팡이 책임져."

> "이렇게 쿠팡이 자체적으로 낮춘 판매가격도 가져올 수 있나?"

> (2026-08-07) "매출 두축이라고 했는데, 이게 일일판매 매출을 너가 나타낸거 같아. 그렇다면 이걸 명확히 어떤 내용의 데이터인지 알려주는게 좋을거 같아. … 여기서 우리가 손익을 계산하고 싶어. 그래서, 여기에 비용을 넣어서 우리 손익까지 넣자. 광고비까지는 나오는데, 옆에 원가, 그래서 우리 손익이 얼마인지까지 나오게 하자"

> (2026-08-07) "해당 항목을 넣어주고, 원가는 등록을별도로 해줄께"

---

## 체크리스트

### Phase 1 — 수집 신설 (진행 중)
- [x] 트랙·계획서 개설, `TRACKS.md` 등록
- [x] 라이브 정찰 시도 (supplier.coupang.com) — **세션 만료로 API 특정 실패**(상세는 계획서 §2)
- [x] prod 실측: D-CPP-6 판정(표본 부재로 미확정) + RG 등록가 불변 재확인
- [x] 신규 테이블 3종 (`coupang_rocket_sales_daily`, `coupang_rocket_promotion`, `coupang_coupon.used_amount*`) + alembic
- [x] 정규화 파서 SA (`clients/coupang/rocket_promo.py`) — **레코드 계약은 우리 것**(쿠팡 원시 스키마 추측 안 함)
- [x] ingest Harness + 라우터 3종 (X-Ingest-Token)
- [x] 원가 시드 스크립트 (SKU 62178970 / 69411570) — ★**폐기됨(D-CPP-8)**. 69411570→OHI-0497 매핑은
      `POST /api/coupang/ops/rocket/cost-map`으로 직접 등록 완료(2026-07-28, prod 실측). 62178970 원가
      갱신(3,400→3,500)은 스크립트와 함께 미실행 상태로 폐기 — 필요 시 `product_master.cost_price`를
      직접 갱신(D-CPP-8 방침).
- [x] 테스트 (파서 fixture · ingest 멱등 · 라우트 3종) — 50건
- [x] **적대적 교차 리뷰 4라운드 — PASS** (2026-07-28). codex는 계정 한도 소진(리셋 08-02)이라
      **적대적 Claude 리뷰어 1기(신선 컨텍스트)로 대체**(Jino 승인 방식 2026-07-18).
      지적 22건 중 수용 20 · 기각 2(라우트 참조0 테스트=grep이 적절 / 쿼리-후-삽입 경합=하우스 패턴).
      실사고급 3건: ①원가 시드가 회계를 움직이는데 계획서가 "한 톨도 안 바꾼다"고 단정(§0.2 정정)
      ②쿠폰 사용금액이 coupon_kind를 안 걸어 DOWNLOAD 행에 권위값이 앉을 수 있었음(D-CPP-3 무력화 경로)
      ③NaN/Inf·상한 초과가 배치를 죽이거나 NUMERIC을 오염(엑셀 폴백 경로에서 실재).
- [x] **정찰 재시도 — 완료**(2026-07-28, 세션 복구 후). ①판매분석 ②프로모션 목록·상세 ③구독 게이트
      **전부 특정**(위 "확정된 API 스펙" 표). 엑셀 폴백은 **불필요해짐**(JSON API로 충분)
- [x] 수집기(페처) 확장 — `tools/rocket_supplier_fetcher.py`에 두 스트림 추가(판매분석 일별 롤링·
      프로모션 전량). PR #147(`4a1717a`) 배포·라이브 완주(판매분석 일별 271행 07-22~27, 프로모션 7건 첫 수집)
- [x] D-CPP-7 수기 단위 할인액 — 컬럼(alembic `df7b34a2f46e`) + `PATCH /rocket/promotion/{id}/unit-discount`
- [x] D-CPP-5 접근불가 감지 배선 — 구독 조회 실패·판매분석 403을 `_SalesAccessDenied`로 올려
      run rc≠0 → 기존 `fetch-error` 보고 경로로 표면화(조용한 skip 없음), 구독 게이트(BASIC 체험 08-20 종료) 확인
- [x] **prod 배포 + push 실증 — 완료 확인**(2026-07-28 저녁 prod SELECT): `alembic_version` =
      `df7b34a2f46e`, `coupang_rocket_sales_daily` **271행**(06-22~07-27 중 6일), `coupang_rocket_promotion`
      **7건**(687878 포함, `unit_discount_amount`=4000 실재). Phase 1은 라이브 가동 중이다. 배포 중 별도
      사고 발견·수리: 초기 배포(PR #131)의 `coupang_ops.py`가 import하던 신규 파일 2개가 미배포로 남아
      재시작마다 즉사(누적 265회) — 누락 파일 배포로 복구(LESSONS #54).
- [ ] RG 쿠폰 "사용 금액" 수집 경로 확정 (Open API에 없음 — 계획서 §3 참조)
- [x] sellC UI에서 단위 할인액 입력 — **Phase 2에서 완료**(카드 내 입력 + PATCH 배선)

### Phase 2 — 손익 엔진·뷰 (2026-07-28 오케스트레이터 스프린트 지시로 착수 — 구현 완료, prod 배포 대기)
- [x] 프로모션 창 ⨝ 판매 조인 — `services/coupang/rocket_promo_pnl.py`
- [x] 1P 프로모션 기간 손익 계산 + **진짜 BEP ROAS**
- [x] 대상 SKU 수기 지정(`target_sku_ids`, alembic `890f276375e3`) — API에 적용상품 목록이 없어 추정 금지
- [x] 읽기 전용 API `GET /api/overview/rocket-promo-pnl`
- [x] 화면 — 커맨드센터 rocket 탭 "프로모션 손익" 블록(카드·SKU 분해·수기 입력)
- [x] 신선도·감시 2종 — 판매분석 결손(만료 임박순) · 구독 체험 종료 D-7 경고
- [x] 테스트 26건(값은 prod 실측 고정) · 전체 3,723 passed · 프론트 빌드 통과
- [x] **prod 배포 완료**(PR #150, main `27dc00e`) — alembic 형제 head 2건(`890f276375e3` 대상SKU ·
      `96e8319324f5` BP/VT3) 병합 리비전 `4b66ae3706e6`로 재결합, safe_deploy CAS 전부 통과·무사고.
- [x] **687878 실데이터 완결(2026-07-28 밤)** — `target_sku_ids=["62178970","69411570"]` PATCH,
      `unit_discount_amount=4000` 보존 확인. `GET /rocket-promo-pnl` 라이브 검증: 강화유리
      BEP ROAS=5.1082(qty 62·funding 248,000=62×4000) · 지문방지 SKU 미해결(납품가 미상, qty 19
      totals에서 제외) · net_profit=N/A(광고비 옵션 귀속 불완전, blockers 사유 유지) — 설계대로 동작.
- [ ] RG 쿠폰 손익 엔진 편입 — `used_amount` 원천 확정 후(지금은 나열 수준)
- [ ] 광고비 옵션 귀속 — 1P(Retail)는 `coupang_ad_option_daily`에 **0행**이라 순이익이 N/A다
      (아래 "Phase 2 실측 결과" ③). 빌보드 옵션 수집이 Retail을 채우면 엔진은 **코드 변경 없이**
      순이익을 내기 시작한다(테스트로 고정).

### Phase 2 실측 결과 (2026-07-28 prod SELECT — 추측 아님)

프로모션 **687878**(07-24 00:01:00~07-26 23:59:59, 분담 100%, 개당 할인액 4,000)로 스모크:

| | 62178970 (17프로 강화유리) | 69411570 (S26U 지문방지) |
|---|---|---|
| 창 내 판매 | 62개 / 실현매출 1,057,800 | 19개 / 실현매출 302,100 |
| 실현 소비자가 | 17,061.29 | 15,900 |
| 납품단가 | 10,740 | **없음**(발주 이력 0건, 신상품) |
| 원가 | 3,400 | 2,351 (OHI-0497 — 07-28 매핑 등록됨) |
| 개당 공헌이익 | **3,340** | N/A (납품가가 없어 산출 불가) |
| **진짜 BEP ROAS** | **5.11배** | N/A |

> 69411570은 **원가는 알지만 납품가가 없다**(발주 이력 0건 — 아직 한 번도 납품 안 한 신상품).
> 한 조각만 없어도 손익은 성립하지 않으므로 미해결로 빠지고, 사유가 화면에 뜬다.
> 첫 발주가 들어오면 자동으로 해결된다.

- BEP 광고비(해결분) = 665,880 − 210,800 − 248,000 = **207,080원**.
- ★**순이익은 N/A다** — 계정 Retail 광고비(창 3일 합 **2,270,306**)는 있지만 **옵션 귀속이 안 된다**
  (`coupang_ad_option_daily`에 A01029796·Retail **0행**, 3P 계정만 수집 중). 0으로 접으면 207,080원
  흑자로 보이므로 **null + 사유**로 둔다(원칙22). 계정 합은 **상한 프록시**로만 제공한다.
- ★"광고 ROAS 5.11배 이상이어야 본전" — 이 값이 이 레이어의 존재 이유다(소비자가 기준 ROAS 왜곡 보정, D-CPP-2).
- ⚠️ **신선도 실측(07-28 낮): 유효구간 [06-01~07-27] 57일 중 51일이 비어 있었다**(수집된 날 =
  07-22~07-27 6일). 최신일은 07-27이라 "수집이 멈췄다"는 신호는 없지만, **06-01분은 오늘이 마지막
  회수 기회**(D-0)였다. → **아래 D-CPP-9로 백필 완료·해소됨.**

### D-CPP-9 — 51일 결손 백필 완료 (2026-07-28 16:20 KST, Jino 승인)

소멸 시한(06-01이 D-0) 직전에 백필 실행. **결과: 57일 전량·결손 0.**

- 실행 경로 = **직접 1회 실행**(`python3 ~/.ohisell/tools/rocket_supplier_fetcher.py run`), 임시 config
  `sales_backfill_days=58` + `collect_po_detail=false`, 종료 후 config 원복(백업 대조 후 삭제).
  ★UI '갱신' 버튼 경로로는 백필이 **불가능**하다 — 상주 데몬 `com.ohisell.rocket`은 `main()`에서
  config를 **한 번만** 읽고 그 dict를 계속 쓴다(`cmd_poll(cfg)`). 파일을 고쳐도 재기동 전엔 안 먹는다.
- 라이브 로그: 유효 구간 `[2026-06-01 ~ 2026-07-27]` 서버 수신 → 58일 요청 중 **57일 수집·1일
  범위밖(07-28 당일분 미개시)·0일 실패**, vendorItems 2,533 → push 6청크(dedupe 40).
- prod 실측 검증: `coupang_rocket_sales_daily` **2,493행 / 57 distinct date / 06-01~07-27, 재귀 날짜
  대조로 빈 날 0건**. 표본 06-01 = 51행·187개·3,285,500원.
- 구독 게이트 통과 확인: `permittedLevel=BASIC subscribed=FREE freeTrialEnd=2026.08.20`(D-CPP-5 시한 유효).
- ⚠️ **재발 위험(구조적)**: 페처는 **순수 버튼-only**(자동 일별 실행 제거됨, `cmd_poll` docstring)이고
  기본 창은 `sales_days=7`이다. → **8일 이상 갱신 버튼을 안 누르면 그 공백은 롤링 창 밖으로 밀려
  영구 소멸**한다. 이번 51일 결손이 정확히 그 기전. 자동 트리거 or 창 확대(예: `sales_days=30`)가
  없으면 같은 사고가 반복된다 — 미결.

### Phase 2 설계 판단 (지시와 다르게 간 지점 — Jino 확인 필요)

1. **대상 SKU = 신규 컬럼 수기 지정.** 지시가 준 두 선택지 중 "PATCH에 함께 저장" 쪽을 택했다
   (조회 시 파라미터로 받으면 매번 다시 입력해야 하고 어디에도 남지 않는다). 페처가 손대지
   않는 칸이라 재수집이 지우지 않는다 — `unit_discount_amount`와 같은 성격.
2. **수기 입력 PATCH의 X-Ingest-Token 해제.** 브라우저에 ingest 토큰을 심을 수 없는데 지시는
   "PATCH 배선"된 입력 UI를 요구한다. 같은 성격의 `/rocket/cost-map`이 이미 토큰 없는 사용자
   CRUD이고 앱 전체가 IP 허용목록 뒤에 있다. 이 라우트는 **prod 미배포**라 기존 호출자가 없다.
   (404 = 유령 프로모션 생성 방지는 그대로 유지.)
3. **순이익을 계산하지 않고 N/A로 둔다.** 지시 공식은 `순이익 = 납품매출−원가−분담금−광고비`인데
   광고비가 옵션 귀속 불가다. "조용히 0 처리 금지"라는 지시에 따라 **null + blockers**로 올리고,
   대신 `net_profit_lower_bound_resolved_only`(계정 광고비 전액을 이 프로모션에 물린 하한)를
   함께 낸다. ★적대적 리뷰 후 이름이 바뀌었다 — 이건 **해결된 SKU만의** 하한이지 "최악값"이
   아니다(미해결 SKU가 적자였다면 참값은 이 밑이고, 경계일 과대 포함도 낙관 방향으로 민다).

---

## 현재 진행 단계 (2026-07-28 밤 — Phase 2 prod 배포 완료·687878 실데이터 완결)

**Phase 2 배포 완료(PR #150, main `27dc00e`)**: 손익 엔진·커맨드센터 뷰 라이브. alembic 형제
head 2건(대상SKU `890f276375e3` · BP/VT3 `96e8319324f5`)이 병합 리비전 `4b66ae3706e6`로
재결합되어 prod 무사고 배포. 687878(아이폰17프로_강화유리, S26울트라_지문 프로모션)
`target_sku_ids=["62178970","69411570"]` PATCH 완료 — `unit_discount_amount=4000` 보존
확인. `GET /rocket-promo-pnl` 라이브 재검증: 강화유리 BEP ROAS 실측 5.1082(qty 62·funding
248,000)·지문방지 SKU는 납품가 미상으로 미해결(totals 제외)·`net_profit`은 여전히 N/A
(광고비 옵션 귀속 불완전 blocker 유지) — 설계 의도대로 동작 확인(원칙22).

남은 결정: 06-01 백필(D-0, 아래 참조)·리뷰 등급제(스코프 밖). 다음 작업: 발주→납품→명세서→
계산서 통합 대사 화면(진행 중).

Phase 2(손익 엔진 + 커맨드센터 뷰)를 오케스트레이터 스프린트 지시로 착수해 **구현을 끝냈다**
(브랜치 `claude/coupang-promo-pnl-phase2`, 커밋 2건). 위 "Phase 2 실측 결과"가 라이브 근거다.

- 엔진 `services/coupang/rocket_promo_pnl.py` — 창 조인·손익·진짜 BEP ROAS·신선도·RG 쿠폰 나열
- API `GET /api/overview/rocket-promo-pnl`(읽기 전용) + `PATCH .../unit-discount` 확장(대상 SKU)
- 뷰 — 커맨드센터 rocket 탭 "프로모션 손익" 블록(배너 2종 포함)
- alembic `890f276375e3`(target_sku_ids) — 단일 head 유지, upgrade/downgrade 왕복 검증
- 테스트 36건 신규 · 전체 **3,733 passed** · 프론트 `tsc -b && vite build` 통과

### 적대적 교차 리뷰 (2026-07-28 · 커밋 `afa6518`)

codex CLI 사용 한도(08-02 해제)로 **적대적 Claude 리뷰어 1기(Opus·신선 컨텍스트) 3라운드
왕복**으로 대체했다(원칙19 대체 수단). 17건 지적 중 14건 수용·3건 기각, 리뷰어가 2R 반박을
받아 추가로 낸 2건(N1·N2)도 수용. 지적 4건은 실행으로 재현해 확인한 뒤 고쳤다.

**★막은 것 — 엔진이 스스로 선언한 원칙22가 실제로 깨지던 두 지점:**

| ID | 결함 | 조치 |
|----|------|------|
| **F1** (BLOCKER) | `resolved` 판정이 **판매행 유무를 안 봐서** 미수집이 `qty=0`·`BEP 광고비 0원`으로 접혔다. 사유 문자열은 `resolved=True`라는 이유로 버려져 blocker도 0건. **51일 결손이 알려진 상태라 배포 즉시 밟는 경로.** | `_window_coverage` SA 신설 — 창 날짜를 covered/missing(결손)/pending(미도래)로 분리. **3상태 판정**: 완전커버+행0=진짜 0판매 / 부분커버+행有=해결하되 `qty_is_lower_bound` / 부분커버+행0=판정 불가 |
| **F2** (MAJOR) | 옵션 광고비가 **부분만** 들어와도 `available=bool(by_option)`이 완전 귀속으로 뒤집혀, 계정 광고비 2.27M 중 1천 원만 물린 채 "순이익 확정 흑자"가 blocker 없이 나갔다. 빌보드 Retail ingest가 붙기 시작하는 순간이 사고 지점 | 판정자를 **일별 금액 정합**으로 교체 — 옵션합 ÷ 계정 롤업 ≥ `_AD_COVERAGE_MIN`(0.95, env 조정·일별 실비율 동봉). 집행 0원인 날=자명 커버, 미도래일=분모 제외 |
| **F3·F4** (MAJOR) | `net_profit_lower_bound`가 **화면에 한 번도 렌더되지 않아** 설계 3번의 세 번째 다리가 백엔드에만 존재. 이름도 거짓말("최악값"인데 해결 SKU만의 하한) | 순이익 카드에 `≥ 값 (미해결 N건 제외)`로 렌더 + 필드명 `net_profit_lower_bound_resolved_only` |
| **N2** (리뷰어 3R) | 과잉교정 경보 — F1을 2상태(결손이면 전부 미해결)로 고치면 51일 결손 동안 **기능이 통째로 빈 화면**이 된다. F1의 거울상이고 똑같이 원칙22 위반 | 위 3상태로 확정. "부분적으로 아는 것"은 버리지 않고 하한으로 라벨링 |

**나머지 수용 10건**: F5 진행 중 프로모션의 부분 창을 완결된 창처럼 표기 / F6 공헌이익 음수인데
"적자 확정은 아니지만"+음수 % 출력 / F7 `sales_days`가 날짜가 아닌 **행 수**(옵션 N개면 창 길이
초과) / F8 쿠폰 `start_at`(DateTime) vs 창(date) 경계로 **종료일 당일 시작 쿠폰 전량 누락** +
계정 컬럼 노출 / F9 `_supply_price_by_sku` N+1(최대 4천 쿼리)→1회 + vendor 필터 / F10
`target_sku_ids` 보존 계약이 문서에만 있고 **회귀 가드 없음** / F11 납품가 출처 `po_seq` /
F12 `sku_id` NULL 행 / F13 음수 금액 빨강 / F14 프론트가 항상 두 키를 보내 백엔드 부분갱신
계약을 무력화 / N1 판매·광고 지평선 불일치 명시(방향은 **과소**라 보수적).

**기각 3건**(전부 리뷰어 3R에서 수용): F15 캡은 방어선으로 유지(다만 자기참조 상수를 고정값
200으로) · F16 `op.drop_column` 선례 33건 + sqlite 3.53 · F17 오차 3e-7이 4자리 quantize에
도달 못 하고 화면 실현단가와의 자기정합이 우선.

**미합의: 없음** — 3라운드에서 전 항목 합의 도달, Jino 이관 건 없음.

> ⚠️ **F2 판정자는 휴리스틱이다**: 0.95 임계값과 "그 날 계정 롤업과 정합하면 완결"이라는
> 가정 둘 다 실측 근거가 아니다. 빌보드 Retail 옵션 ingest가 실제로 붙는 날
> `option_vs_account_ratio`(응답 동봉)를 보고 재조정할 것.

⚠️ **prod에 필요한 것(2026-07-28 저녁 prod SELECT로 확인)**: prod `alembic_version` =
   **`df7b34a2f46e`**(Phase 1 전량 적용 완료 — `unit_discount_amount` 컬럼 실재, 판매 271행·
   프로모션 7건 적재됨). 따라서 Phase 2 배포는 **마이그 1건 `890f276375e3`** + 코드다:
   `safe_deploy.sh backend/alembic/versions/890f276375e3_*.py backend/app/models.py \
    backend/app/services/coupang/rocket_promo_pnl.py backend/app/routers/overview.py \
    backend/app/routers/coupang_ops.py --migrate --restart` → 프론트 `--frontend`.
   ★`models.py`를 마이그보다 먼저 올리면 ORM이 `no such column: target_sku_ids`로
   **프로모션 테이블 전체 접근이 죽는다**(프로젝트 CLAUDE.md) — `--migrate`가 순서를 강제한다.

⚠️ **회계 무변경 확인**: 신규 엔진은 회계 코드에서 참조 0(테스트로 고정 —
   `test_pnl_engine_is_not_referenced_by_accounting_code`). net_profit·종합조망 숫자 불변.

---

## 이전 진행 단계 (2026-07-28 오후 13:30 — 페처 확장 완료·prod 배포·라이브 가동 중)

**Phase 1 — 수집층 완성·prod 배포·라이브 가동 중.** ⚠️ 배포 중 별도 사고 발견·수리: Phase 1
초기 배포(PR #131)가 `coupang_ops.py`에 `rocket_promo_sync.py`·`rocket_promo.py` import를
추가했는데 그 2개 신규 파일이 prod에 배포되지 않은 채 남아있어 재시작마다 즉사(누적 265회) —
누락 파일 배포로 복구(LESSONS #54). 아래는 배포 전 작성된 상세 기록.

Phase 1 **수집 골격 + 수집기 완성**. 오전 세션의 백엔드 골격(테이블·파서·ingest·원가 시드)에
이어, supplier 세션 복구 후 **정찰을 끝내고 페처 두 스트림을 붙였다.** 예측이 아니라 라이브 실증:

- 판매분석 3일 창 요청 → 서버가 유효구간 `[2026-06-01 ~ 2026-07-27]`을 돌려줘 오늘(07-28)이
  자동 클램프됨 → **2일 수집 88레코드**(07-26: 51옵션·146개·GMV 2,585,750 / 07-27: 37옵션·121개·GMV 2,064,750),
  sku_id 채움 88/88. 백엔드 파서 통과 시 skipped 0 · blank_qty 0 · blank_revenue 0.
- 프로모션 목록 7건 전부 상세 병합 성공(687878 = 07-24 00:01:00~07-26 23:59:59, 분담 100%, 예산 100만, 적용상품 2).
- 구독 게이트: BASIC / 무료체험 종료 **2026-08-20**(D-CPP-5 시한이 눈에 보이는 상태).

⚠️ **push는 실증하지 못했다** — prod에 Phase 1 라우트가 **아직 배포되지 않았다**(`/rocket/sales/ingest`
404 vs 기존 `/rocket/po/ingest` 401). 즉 "Mac IP 차단"이 아니라 **미배포**가 원인이다(prod 자체는 200 응답).
push 경로는 유닛 테스트로만 검증된 상태.

### 적대적 교차 리뷰 3라운드 (2026-07-28) — codex 대체

codex CLI 쿼터 소진(08-02 해제)으로 **신선 컨텍스트 Opus 리뷰어 1기**와 왕복(원칙19 형식).
지적 12건: 전면 수용 9 · 부분 3 · 결함 기각 0. 반영 후 전체 스위트 **3,684 passed**.

**BLOCKER 2건**
1. **alembic 그래프 파손** — 우리 신규 마이그가 `a1c3e5f7b9d1`을 부모로 물었는데, 병행 세션이
   PR #135로 그 ID 충돌을 해소하며 우리 Phase 1을 `c2998cfe1f7c`로 개명한 뒤였다. 그대로
   병합하면 ①중복 정의로 **alembic 전체 사망** ②형제 head 2개 ③테이블을 만드는
   `c2998cfe1f7c`보다 먼저 돌아 없는 테이블에 ADD COLUMN. → main 편입 + revision ID를
   `uuid4()`로 재생성(`df7b34a2f46e`, LESSONS #50) + 부모를 main head `f6a8c0b2d4e6`로 재배선.
   **실측 검증**: 72리비전 중복 0 · head 단일 · 임시 DB에 upgrade/downgrade 왕복 성공.
2. **구독 게이트가 장식이었다** — `permittedLevel`/`freeTrialEndDate`를 읽어 로그만 찍고 무조건
   통과시켰고, 실집행은 403뿐이었다. 그런데 D-CPP-5가 겁내는 실패 모드는 정확히 "403이 아니라
   조용히 0행"이다(체험 종료 08-20). → 읽은 값으로 판정 + 창 전체 판정 추가.

**MAJOR — 조용한 실패 계열 (전부 "성공으로 보이던" 것들)**
- 하루 실패가 **이미 수집한 앞날 전부를 버렸다**(백필 45일이면 44일째 실패가 43일치 유실) → 날짜별 격리.
- 구간 안인데 두 번 400난 날을 **"범위밖"으로 위장**하고 rc=0 → `days_out_of_range`/`days_failed` 분리.
  세 카운터 합 = 요청 일수 불변식을 테스트로 고정(어긋나면 하루가 새는 것).
- 프로모션 상세 1건의 **예외**가 7건 전부를 날렸다(비200만 폴백하고 raise는 통과) → 예외도 폴백.
- `{**item, **detail}`이 상세의 **null로 목록의 정상 값을 덮었다**(ingest는 그 None을 그대로 저장) → None 제외 병합.
- `pageNumber`/`totalResults`를 파싱만 하고 **버렸다** → 에코 검증·수신량 대조로 조용한 절단 차단(원칙14).
- 페이지 상한 소진이 **정상 종료와 구분 불가**했다 → 절단으로 판정·경보.
- 실패 보고 문구가 `"로켓 발주/정산 수집 실패"`로 **고정** — 이 스트림이 붙은 뒤로 거짓말이다
  (발주/정산은 성공했는데 운영자를 엉뚱한 곳으로 보낸다) → 어느 스트림인지 실어 보낸다.
- `stats`가 전량 폐기됐다(`_stats`) → 부분 성공·실패 날짜를 로그·판정에 반영.

**설계 판단 (리뷰어 제안을 기각하고 대안 채택)**
- 리뷰어: "프로모션손익을 rc에서 빼라". **기각** — D-CPP-5가 실패의 표면화를 요구하고, 기존
  `detail_failed<=0`(발주상세)이 이미 같은 방식이라 새 스트림만 약하게 만든다. 리뷰어 3R에서 철회.
- 대신 **영구/일시 실패를 가른다**: 구독 만료·매핑 파손은 재시도해도 40초 뒤에 낫지 않는데
  버튼 1회가 Chrome을 3번 띄운다 → `RC_ACCESS_DENIED` + `kind="access_denied"`로 재시도 0회
  소멸(기존 `login_required`와 같은 결). **실패로는 남는다.**
- **부분 실패는 rc를 올리지 않는다**: 롤링 7일 창이 다음 회차에 스스로 메우고, 같은 서버 조건에
  40초 뒤 재시도는 무의미. 전 날짜 실패만 계통 고장으로 rc≠0.
- **접근 차단 ≠ 매핑 파손**: vendorItems가 0이면 구독(사람이 결제로 해결), vendorItems는 왔는데
  레코드가 0이면 필드명 변경(사람이 커밋으로 해결). 뭉치면 구독을 갱신하며 코드 버그를 쫓는다.
  ★레코드 0이면 push가 없어 백엔드 `blank_qty` 경보가 `accepted=0`으로 침묵하므로 여기가 유일한 탐지 자리.

**테스트** — 371줄이 순수 매핑만 덮고 제어 흐름 9개 함수는 **참조 0**이었다(결함 대부분이 그 층).
스크립트된 가짜 `page`로 제어 흐름 12건 추가 + 동어반복 테스트 1건 교정
(`_promotion_record`의 하드코딩 `None`을 assert하던 것 → 실측 응답에 그 필드가 없다는 전제를 검증)
+ 수기값 보존을 소스 가드로 고정.

미해결(Jino 판단 필요, 스코프 밖):
- `collect_sales=false`로 스트림을 끄면 **아무 잔여 신호가 없다** — 의도적 중단인지 고장인지
  prod가 구분 못 한다. 신선도 배너에 "의도적 비활성" 표시를 넣을지.
- 롤링 창이 못 메운 영구 구멍(창 밖으로 밀려난 빈 날짜) 탐지는 페처가 아니라 prod 쪽이 맞다(Phase 2).

> ⚠️ 계획서 §0.6 금지선("기존 rocket 페처 수정 금지")은 **이번 스프린트 지시로 해제**됐다
> (같은 파일에 스트림 추가). 라이브 데몬 파일이므로 배포 시 페처 재기동 필요.
> ⚠️ §5 원가 시드 스크립트는 **폐기됨(D-CPP-8, 2026-07-28)** — 원가는 환율에 따라 상시 변동하므로
> 스크립트 고정값이 아니라 `product_master.cost_price`(수기 관리)가 단일 진실 원천이다. 69411570 매핑은
> API로 등록 완료, 62178970 원가 갱신(3,400→3,500)은 미실행인 채로 스크립트만 삭제됨.

## 다음 액션

> **2026-07-28 밤 갱신** — Phase 2 prod 배포 완료(PR #150, main `27dc00e`) + 687878 실데이터
> 완결(target_sku_ids PATCH·BEP ROAS 실측 확인). **우선순위 순으로 아래를 다음 액션으로 삼는다**:
>
> 1. **발주→납품→명세서→계산서 통합 대사 화면** — 선행조건(로켓 1P M1, PR #130) prod 배포 완료로
>    착수 가능. **진행 중.**
> 2. ~~**06-01 백필 결정(D-0)**~~ — ✅ **완료**(2026-07-28 16:20, D-CPP-9). 57일 전량·결손 0.
    후속 미결 = **재발 방지**(버튼-only + `sales_days=7` 조합이 8일 공백을 영구 소멸시킨다).
> 3. **리뷰 등급제** — 스코프 밖 후보, Jino 판단 대기.
> 4. RG 쿠폰 "사용 금액" 수집 경로 확정(Open API에 없음 — 계획서 §3) — 미해결 채 남아있음.
> 5. 광고비 옵션 귀속 완성 — 빌보드 Retail 옵션 수집이 붙으면 net_profit이 코드 변경 없이 확정된다.
> 6. 9월 1P 정산서 도착 시 D-CPP-4 대사.

## D-CPP-7 — 프로모션 분담금은 개당 할인액 수기 입력(정찰 API로 못 얻는 값)

정찰 결과 프로모션 목록/상세 API는 프로모션 메타(기간·대상 SKU·분담비율)는 주지만 **"개당 할인액"은 API에 없다**(sellC 화면 표기값). 새 프로모션 신청 시 sellC PATCH 엔드포인트로 개당 할인액을 수기 입력하는 것이 정본 경로다(D-CPP-1의 "자동 수집 우선·수기는 폴백" 원칙과 배치되지 않음 — 프로모션 존재/기간은 자동, 금액 필드 하나만 수기).

## 일일 프로모션 수기 입력 규칙 (D-CPP-7)

**새 프로모션 신청 시 sellC PATCH로 개당 할인액을 입력한다.** 프로모션 자체(기간·대상)는 페처가 자동 수집하지만, 금액 필드는 API에 노출되지 않아 이 한 값만 수기다.

## 마지막 구조 감사
- (없음 — 트랙 개설일 2026-07-28)
