# TRACK — 통합 재고·발주 관리 (「발주예측 목표」)

<!-- TRACK-CONTRACT v1 -->
목표: "ecount에 있는 우리가 발주한 수량, 폴더에 있는 우리가 픽업한 데이터와 스마트스토어, 쿠팡(오하이테크, 오픽스의 2P, 3P), 자사몰에서의 판매량과 쿠팡 1P에서의 발주수량을 대비해서 과거의 패턴을 찾아서 향후에 우리가 발주를 OTAO에 낼 수 있게 발주수량 예측할 수 있는 시스템을 만들자" (Jino 2026-08-25 11:03 원문)
안함:
- 수입 원장의 수리·확장(스키마·파서·`line_type` 분류·`imported_goods` 배선) → 소관: 계약 A′/B (`docs/contracts/CONTRACT_cost_menu_a_prime.md` 계열 · 체인 `sellc-원가-메뉴`). ★단 **「품목명 → 상품 매핑」은 2026-08-25 22:52 Jino 승인(*"해도 돼"*)으로 본 트랙 범위에 편입**(D-INV-1) — 원장은 읽기 전용 유지, 매핑은 본 계약 소유의 별도 테이블·서비스. 근거·경계·Jino 확정 규칙 3은 계약 §1
- 자동 발주서 발송·픽업 지시·재고 임의 수정의 «실행» → 소관: 없음(되돌릴 수 없는 돈 — 영구 비범위)
- 신제품 콜드스타트 전이 규칙 → 소관: 본 계약의 후속 슬라이스(별도 승인)
- 쿠팡 1P SKU별 PO 축 수리 → 소관: `docs/tracks/active/track_coupang-rocket-1p.md`
- ECOUNT 「오하이→오하이테크」 사업장 이관 → 소관: `AI_office`(별도 저장소)
- 발송관제 엔진 정확도 개선·백테스트 → 소관: `docs/tracks/active/track_coupang-rg-replenishment.md`
- 2P 손익 정합 → 소관: `docs/tracks/active/track_coupang-2p-parity.md`
- 원가·매입단가 정본 → 소관: `docs/tracks/active/track_cost-truth-ledger.md`
- 상품 연결맵 «구축» → 소관: `docs/tracks/active/track_product-connection-map.md` (본 트랙은 소비만)
- 광고(PAO) 수요 신호 결합 → 소관: `docs/tracks/active/track_naver-ad-optimization.md`
- 전 SKU 커버 → 명시적 비목표(계약 §2-3, RG 선례 1.05%)
합격:
- [x] S0-a 발주서조회 API가 품목(SKU) 라인을 주는지 확정 — **안 준다**(1,622건 전건 헤더 그레인·`PROD_CD` 없음·「외 N건」 78.5%·금액 87.5% 소실). ★대신 **발주서 PDF가 정본**임을 확인: 26건 파싱·검산 3종 전건 통과, `import_invoice_line.order_no`가 발주 Serial을 문자 그대로 보유(156/158=98.7%). 증거: `docs/references/98_po_forecast_s0_gate_20260825.md` §1·§5
- [ ] S0-b ECOUNT 발주 축의 생산자 저장소 결정 — 계약에 D-N으로 기록 (★2026-08-25 16:39 Jino가 작업 Mac IP 등록 → **sellc 직접 호출이 실증됨**(16:42:46 성공)으로 전제가 바뀜. 결정 미완)
- [x] S0-c cafe24 60일 중 8일 공백이 「주문 0건」인지 「수집 실패」인지 확정 — **수집 결손 0일**(SyncLog 8일 전건 success·`[부분수집]` 마커 0·30일 롤링창으로 각 날짜 ~30회 재조회·주말 4/8·일평균 4.69건). 확신도 낮은 2건(07-16·07-20)만 Jino 콘솔 대조 잔여. 증거: ref 98 §3
- [x] S1 콘솔 발주 메뉴에서 SKU별 발주 누계·픽업 누계·OTAO 예약 잔량을 3칸으로 본다 — ECOUNT 원본 대조 표본 10건 일치 — **라이브** `https://sellc.ohitech.co.kr/otao-po` (2026-08-26 09:0x KST · SKU 75행 · 발주 30,090 / 픽업 13,370 / 잔량 16,720 세 칸) · **ECOUNT 전수 66건 대조 65/66 일치**(ref 100) · PR #460 머지 `262e68b3` · prod 마이그 `otaoev1merge` · 적재 09:01:48(발주서 95·라인 1,205) · ⚠️결손 2건(`20230509-3`·`20250519-1`, 6,000개)은 **발주서 PDF 원천 부재**
- [ ] S2 정산 창(전월 20~당월 19) 픽업 합계가 보이고 실제 19일 OTAO 지급액과 1개 창 이상 대조 일치 — ★**앞 절반 라이브·뒤 절반 대조 대상 부재**(체크 안 함). `https://sellc.ohitech.co.kr/otao-po` 「정산 창 (OTAO 지급)」 (2026-08-27 06:33 KST · 창 **7개** · 합계 **310,742 CNY** = 상품 282,662 / 부자재 28,080 / 미분류 0 · 창 경계 3건·draft 1건 자백). **지급액 원장이 prod 123개 테이블 전수 + 파일 검색에서 0건**이라 전 창 `reconciled: null`(「불일치」가 아니라 「대조 불가」) — Jino가 창 1개분 지급액을 주면 그 자리에서 대조된다. PR #486 머지 `4ac8281d` · 배포 `7a023750` · 적대 리뷰 1R FAIL(P1 2건)→**2R PASS** · 증거 `docs/references/104_otao_settlement_window_20260827.md`
- [x] S3 채널 통합 SKU별 판매수량 시계열 + 매핑률이 보이고, 결손일이 「0」이 아니라 「데이터 없음」으로 구분 표시 — **라이브** `https://sellc.ohitech.co.kr/otao-po` 「판매 (채널 통합)」(2026-08-26 13:5x KST · 채널 5축 7행 · SKU 545행 · 60일 스파크라인 545개 · 매핑률 98.35~100% · cafe24 「판매 0 7일」 · 쿠팡 5축 「구분 근거 없음」 · 미매핑 30개 표면화). PR #473 · 배포 `0d254169` · 적대 리뷰 1R FAIL(P1 3건)→**2R PASS** · 증거 `docs/references/102_sales_axis_census_20260826.md`
- [ ] S4 파생 현재고(초기 실사 + 픽업 입고 − 판매)가 보이고 실사 표본 10 SKU 대조 오차 표시
- [ ] S5 Jino가 발주 추천안(근거 4항 + insufficient_data·last-buy 라벨)을 보고 실제 월 발주 1회에 참조
- [ ] S5-커버리지 추천 커버 SKU의 최근 30일 판매수량 합 ≥ 전체의 50%(수량 가중)
상태: 진행중
확인: 2026-08-25 13:3x [f243f6f4] — 트랙 신설. 계약 v0.2 개정·Jino 승인(13:30 *"그래"*). 진행률 0/9. S0 착수(코드 0줄·ECOUNT API 호출 0건).
확인: 2026-08-25 17:0x [f243f6f4] — **S0-a·S0-c 달성, 진행률 0/9 → 2/9.** 코드 0줄·prod 쓰기 0건 유지. ★S0-a는 「막혔다」에서 **「다른 경로로 뚫렸다」**로 역전(발주서 PDF 26건). ★계약 4회 개정: 생산 마디 삭제(Jino *"이 제품은 otao에서 완제품이 들어오기 때문에 생산이 없어"*) / 판단기준 7-A **자기 반증 후 정정** / 금지선 3 조건 변경(Jino가 Mac IP 등록) / 창고 5개 의미 신설. ★**ECOUNT API 개통**(16:42:46, 1,391행, 로그인 1·조회 1·재시도 0). 리드타임 **29일** 실측 = 「통상 4주」 확인. 예약 잔량 정본 **16,100**(갭 4,100 완전 분해). S0-b만 잔여.
확인: 2026-08-25 18:1x [75c58ce3] — **진행률 2/9 불변**(체크박스 안 움직임 — 이번 슬라이스는 S1~S5의 «화면»이 아니라 그 앞의 «방법 검증»이다). 코드 0줄·prod 쓰기 0건·ECOUNT 호출 0건 유지. ★**되감기 성립 실증**: `재고(t) = 본사현재고 − Σ입고(>t) + Σ판매(>t)` 를 47코드×4시점=**188칸**에 돌려 음수 **2칸(1.1%, −15·−1)** — 세 축(ECOUNT·통관원장·판매 4채널)이 독립 수집인데 물리적으로 정합. ★★**표본이 26건→3건으로 줄었다**: 입고 원장이 **2026-01-27부터**(2025년 선적 0건)이고 쿠팡 1P·2P/3P 판매가 **2026-06-01/06-07부터**(판매의 84%). ref 98 §6-A의 「26건 표」는 원리적으로 불가능했다 — 방법이 아니라 **원장 범위**의 문제. ★**규칙 복원**: 최소 발주 단위 **50개(56/56 전건)** · 발주=**31일치**(빠름 ≥3개/일) / 48일치(느림, 최소단위 부산물) — 31일은 실측 리드타임 **29일**과 정합. ★★**Jino 18:08 원문** *"현재 본사 재고로 잡혀있는 수량들은 비슷한 수준이지 100%는 아니야"* ⇒ 민감도 ±30% 재계산 결과 **「얼마를 시키는가」는 불변, 「얼마를 채우려 하는가」(재고일수·목표일수)는 34↔63일로 밀려 폐기.** ★**이탈 실증**: 예약잔량÷재고 중앙값 **11.6배**, `GAPIP15`는 08-12에 재고 **7개(1.5일치)**인데 예약잔량 **3,150**을 두고 **300개를 새로 발주** — 이 결론은 ECOUNT를 안 쓰므로 오차에 견딘다. ★ref 98의 「본사 4,138」 정정 — 그것은 56코드 중 **38개만 센 부분집계**였다. 단 정정값은 하나가 아니다: **56코드 exact 5,397**(되감기가 쓴 모집단) / GC 제외 **5,032**(순수 액정보호) / 이름 기준 5,988. **모집단 없이 「강화유리 N개」라고 쓰면 안 된다**(완료 QA가 5,988로 재계산해 이 불일치를 잡았고 지적이 옳았다). ★그 과정에서 **`GC`=카메라렌즈 확정**(품목명 원문) — ref 98의 [미상] 1건 해소. 산출: `docs/references/99_po_forecast_retro_stock_20260825.md`.
확인: 2026-08-25 23:4x [79e11935] — **진행률 2/9 불변**(S1 미달 — 백엔드 원장은 섰으나 «화면»에 못 닿았다). ★**발주 축이 처음으로 코드가 됐다**: 발주서 PDF 파서(순수 함수) + 발주 원장 3테이블 + 마이그레이션 `otao1po4n4a`(head 단일 확인) + 품목명 사전 + S1 3칸 집계. 적재기(`ingest.py`)·API·화면·테스트는 **미작성** — 그래서 prod 테이블은 비어 있고 로스터는 빈 값을 돌려준다. ★★**인계 수치 둘을 실측이 뒤집었다**: ①발주서는 26건이 아니라 **66건 고유**(파일 121 = 발주서 95 + Packing List·Invoice 26). n=2의 「26건」은 **n=1이 ECOUNT에서 받아온 사본 26개**였고(해시형 파일명 26개 = 2025년 14 + 2026년 12, 전부 08-25 mtime), 2023~2024년 32건은 시야 밖이었다. ②**「최소 발주 단위 50개」 반증**(D-INV-4) — `20251210-1`에 **12·395·420**개가 실재하고 헤더 `Q;ty 1,927`과 검산 일치. n=2의 「56/56 전건」은 **3건·56행**짜리 좁은 창의 참이었다. ★**Jino 확정 3건**: 품목명 매핑 규칙 3(공용≡단일·`screen protector` 접미 무시·`2ea`=2매입, 22:47·22:50) / 매핑을 이 트랙 범위로 편입(22:52 *"해도 돼"* → **D-INV-1**) / `Revise`=개정본(23:13). ★**발주서에 `Product Code` 칸이 실재**해 매핑 사전을 추론 없이 얻었다 — 원장 품목명 자동 일치 **수량 기준 87.2%**(exact 78.7 + 규칙 8.5), 나머지 12.8%는 「매핑 필요」로 표면화. ★파서 검산 **수량 95/95**·1,206라인·`Product Code` 미독 0(독립 재실행 확인). 금액 15건은 발주서 헤더 자체의 1~6원 반올림이라 억지로 맞추지 않았다. ★**기존 원장 무접촉 유지**(§3-8) — `import_invoice_line`은 읽기 전용이고 `internal_sku`(prod 0/158)를 우리가 채우지 않는다. ⚠️미해결: 파서가 삼성 `2.5D`의 앞 `2`를 흘려 사전 키가 `.5D`로 들어오는 건(같은 파일 안에서도 비일관) · `2ea` 줄의 수량 단위(세트/낱장) [미상] 잔여.
확인: 2026-08-26 09:2x [a2d4f9cd] — **진행률 2/9 → 3/9 (S1 달성 주장, 판정은 완료 QA 몫).** n=4가 「코드는 있으나 아무도 못 본다」로 닫은 자리를 잇는 것이 이번 세션 몫이었고, **적재기·API·화면이 다 섰다.** ★적대 리뷰 **PASS(P1 0건)** — 1R P1 2건(order_count 그레인 · 모델↔마이그 nullable 파리티) 해소, 표면 절단 변이 9종 중 7종 사망. 생존 4종은 전부 테스트 갭이라 **P2 5건을 채택해 닫았다**(특히 창 시작일 `min`↔`max`가 전건 초록이던 건 — prod 선적 12건이 2026-01-27~08-18이라 `max`면 거의 모든 발주가 「창 밖」이 된다). ★**ECOUNT 대조는 표본 10건이 아니라 전수 66건, 65/66 일치**(ref 100). 발주번호 체계가 `ORD_DATE-ORD_NO`로 양쪽에서 같아 표본을 고를 필요가 없었다. 불일치 1건 `20231011-3`은 **PDF 자체가 ECOUNT와 다른** 건이고(헤더 검산은 통과) 적재 리포트의 `tie_broken_by_mtime`이 **그 건을 미리 지목했다**. ★**ECOUNT API 호출 0건** — n=1이 받아 둔 파일로 대조해 §3-3 금지선을 피했다. ★멱등성 라이브 실증: 재실행 시 `신규 0 · 그대로 95`. ⚠️**결손 2건**(`20230509-3`·`20250519-1`, ECOUNT 합 6,000개)은 발주서 PDF가 Drive에 아예 없다 — 원천 결손이지 적재 결함이 아니다. ⚠️**사전 커버리지 35/65**로 인계값(43/65)과 다르다 — `sync_name_map`이 정본 66건에서만 사전을 만들기 때문. 오염 방지 ↔ 커버리지 맞바꿈을 **의식적으로 고르지 않았다**, 다음 슬라이스에서 잰다. ⚠️사고 2건 자백: ①배포가 중간에 멈춤(prod에 `services/otao_po/` 디렉터리 부재 — `main.py`는 이미 배포된 상태라 그때 재시작됐으면 부팅 실패. 복구 후 다운타임 0초) ②P2-7 수정이 **변이 원복에 지워진 채 커밋됨** — 커밋 전에 변이를 주입한 내 잘못이고, 잡은 건 같이 쓴 테스트다. ⚠️병행 세션(pao-논의 n=54)의 마이그 `ev1preserve51`이 같은 부모에서 갈라져 head가 둘이 됐다 — 병합 리비전 `otaoev1merge`로 합류. **그쪽 마이그 동시 적용은 Jino 승인 후 진행**(09:2x).
확인: 2026-08-26 11:5x [93509286] — **진행률 3/9 불변**(체크박스 이동 0개 — 이번 슬라이스는 S항목이 아니라 n=5가 이월한 «사전 모집단 재판단»이다). ★**D-INV-5 확정·배포·라이브 반영 완료**: 사전 모집단을 정본 66건 → 발주서 전체 95건으로 넓히되 **층**으로(정본이 말하는 키는 정본이 이기고, 침묵하는 키만 비정본이 채운다). ★**재보니 n=5가 우려한 맞바꿈이 실재하지 않았다** — 커버리지 61.44% → **87.18%**(35종 13,370 → 43종 18,970), 차이 8종 5,600개가 **전부 `unmatched`→일치**, 새 `ambiguous` **0건**·코드 뒤바뀜 **0건**. ★**진짜 발견**: 새로 붙는 코드 **17종이 17/17 정본 라인에 실재**하는데 그 라인의 `name_en`은 **0/17**이다 — **ECOUNT 사본은 B형 발주서라 영문상품명 칸이 아예 없다.** 즉 정본 규칙 D-INV-3은 «수량 축»의 규칙인데 그것을 «라벨 축»에 그대로 적용한 것이 결함이었고, **필터가 막은 것은 오염이 아니라 재료였다**(그 대가로 5,600개가 조용히 「매핑 필요」에 남아 있었다 — 계약 §2-9가 겨눈 그 자리). ★라이브(11:56~11:57 KST): `--sync-map-only` 실행 → prod 사전 **43/65** · 재실행 **멱등** · `/api/otao-po/roster` **200**에서 픽업 **13,370 → 18,970** · 잔량 **16,720 → 11,120** · 매핑 필요 **30종 8,390 → 22종 2,790** · **발주 누계 30,090 불변**(수량 축 무접촉) · `import_invoice_line` 158행 `internal_sku` **0** 불변(§3-8 준수). PR #467 · 배포 `e20604bb`(CAS 3건 통과·무중단 0초·마이그 **없음**) · 근거 `docs/references/101_po_name_map_population_20260826.md`. ⚠️미일치 잔여 22종 2,790개는 `For ` 방언 16(**규칙 2 = 상품 지식**, 사람이 확정해야 함) + 삼성 `2.5D` 6(**파서 이월 P2-2**) — 이번 변경으로는 원리적으로 안 붙는다. ⚠️공개 URL은 401(로그인)이라 **Jino의 브라우저 확인 1칸이 남아 있다**.
확인: 2026-08-26 13:5x [93509286] — **진행률 3/9 → 4/9 (S3 달성 주장, 판정은 완료 QA 몫).** ★**판매 4채널이 처음으로 한 화면에 섰다** — `https://sellc.ohitech.co.kr/otao-po` 「판매 (채널 통합)」에 채널 5축 7행 · SKU **545행** · 각 행 **60일 스파크라인**(svg polyline 545개×60점). 매핑률 98.35~100%, 미매핑 30개 표면화. ★**착수 실측이 위임 조사 결론 둘을 뒤집었다**: ①「쿠팡 매핑률 0%」 → 다리가 `product_id`가 아니라 **`vendor_item_id`**였고 실제 **99.9~100%**(잘못된 조인은 예외가 아니라 «0»을 낸다) ②「쿠팡 축엔 수집 로그 전무」 → `sync_log`에 ch1·2·3·4·6·7 실재. 단 `sync_type`이 `'orders'` 하나뿐이라 쿠팡 전용 테이블을 덮는지는 **[미상]**이고, **근거가 아닌 것을 근거로 쓰지 않아** 쿠팡 5축은 「구분 근거 없음」으로 둔다(§2-8 정직한 이행). cafe24는 「판매 0 **7일**」로 S0-c 판정과 정합. ★★**발주 축은 «섬»이다** — `GAPIP` 코드가 prod 전수 문자열 스캔에서 이 트랙의 두 테이블에만 살고(1,100+43행, 그 밖 **0건**) 판매 축 `internal_sku`(963종)와 **겹치는 값 0개**. 그래서 두 표를 같은 줄에 놓지 않고 화면이 다리 부재를 자백한다. **S5는 이 다리 없이 원리적으로 못 선다** — 다리 «구축»은 계약 「안 함」(→ `track_product-connection-map.md`)이라 **관측만 하고 처방하지 않았다.** ★**적대 리뷰 1R FAIL(P1 3건) → 2R PASS(P1 0건)**. P1-1이 아팠다: `product_channel_mapping.channel_product_id`에 unique 제약이 없어 중복 55키·121행이 outerjoin에서 1:N으로 펼쳐져 **판매를 곱하고 있었다**(Wing 3P 오픽스 원장 1,980 → 화면 **2,099**(+6.0%), RG 2P 2,117 → **2,254**(+6.5%), 그 몫이 **팔린 적 없는 SKU에도 배분**). 배포 «전»에 잡혔고 수정 후 라이브가 원장과 정확히 일치. 기존 테스트가 0건 잡은 이유는 **픽스처가 키당 매핑을 1행만** 넣었기 때문 — 「테스트가 상상 못 한 데이터 모양」이 구멍이었다. P1-2는 **계약 원문의 첫 요구인 「시계열」이 화면에 없던 것**(SKU×창합계만 있었다), P1-3은 두 테스트 파일의 사각지대 **교집합**으로 **prod 상태(원장 차 있음+판매 정상)를 렌더하는 테스트가 0건**이라 판매 섹션을 통째로 지워도 18/18 초록이던 것. 셋 다 상환·변이 재주입 사망 확인. ★성능은 outerjoin 제거로 **10배 빨라졌다**(days=60 0.14s·days=365 0.19s). PR **#473** · 배포 `0d254169`(무중단·**마이그레이션 0**) · 백엔드 **6,749 passed/0** · 프론트 **942/66파일** · tsc 0 · eslint 96 · 증거 `docs/references/102_sales_axis_census_20260826.md` · 리뷰 `docs/reviews/REVIEW_otao_sales_s3_20260826.md`. ⚠️이월: 쿠팡 수집이 `sync_log`에 남는지 [미상] / `days=365` 926KB 희소 행렬 / 라우터 body↔화면 계약 테스트 부재(2R R2-B).
확인: 2026-08-27 06:4x [1e623970] — **진행률 4/9 불변**(체크박스 이동 0개 — S2는 한 문장 안의 두 요구 중 **앞 절반만** 됐고, 반만 된 것을 `[x]`로 찍지 않는다). ★**정산 창 축이 처음으로 화면에 섰다** — `https://sellc.ohitech.co.kr/otao-po` 「정산 창 (OTAO 지급)」에 창 **7개**(2026-02~08) · 열 **12개** · 합계 **310,742 CNY**. 창 경계는 「전월 20일 ~ 당월 19일」이고 **20일 픽업부터 다음 창**이다(Jino 원문 ④). ★★**착수 실측이 지급액 축의 부재를 확정했다**: prod **123개 테이블 전수** + 워크트리 파일 검색에서 OTAO 지급·송금 원장 **0건**(정산 이름이 붙은 것은 전부 쿠팡·네이버 «판매대금», 범용 `settlements`는 행 0개, `remittance_fx_rate`는 12/12 NULL이고 모델 docstring이 *"이번 범위가 아니다 — 필드만 예약한다"*고 명문화). ⇒ 전 창 `reconciled: null`이고 **그것이 정직한 값**이다 — `false`(불일치)면 없는 사실을 만든 것이고 `true`면 거짓 확신이다. **Jino가 창 1개분 지급액을 주면 `build_settlement(payments=…)`가 그 자리에서 대조한다.** ★**D-INV-6 — 금액 축은 CNY 인보이스다**(과세금액 KRW 아님). `declared_inv_value == Σ(unit_price_foreign × quantity)`가 **12선적 전건 차이 0.00**이라 라인이 정본이고, 그래야 한 선적을 창으로 쪼개고 `line_type`별로 가를 수 있다. 원화 환산은 **하지 않는다**(우리가 안 쓰는 신고환율로 지어낸 숫자가 된다). ★**부자재가 9.0%다** — `material`(cleaning kits @0.8 CNY) 35,100개 **28,080 CNY**는 지급액엔 들어가고 S1 픽업 누계(=`product`만)엔 안 들어간다. 합치면 두 숫자가 왜 다른지 설명할 자리가 사라져 **갈라서** 싣고, 회귀가 `roster.picked + unmapped = settlement.product_quantity` 등식으로 두 모듈을 묶었다(prod 실측 18,970+2,790=21,760 성립). ★**이 원장엔 「OTAO 픽업일」이 없다** — 가진 건 한국 쪽 신고일·도착일(간격 0~2일, 창 배정 동일)뿐이고 B/L 발행일이 1~2일 이르다. 경계(19/20일) ±2일 선적 **3건**(2026-03-19·05-18·08-18)을 화면이 지목한다 — 대조 불일치의 첫 후보다. ★**적대 리뷰 1R FAIL(P1 2건) → 2R PASS(P1 0)**. 둘 다 「값은 맞는데 사람에게 안 닿는다」였다: **P1-1** 미분류(`unknown`) 금액이 픽업 합계엔 들어가는데 화면에 칸이 없었다 — `line_type` 기본값이 `unknown`이고 분류는 사람이 나중에 하므로 **갓 적재된 선적은 예외 없이 전부 이 상태다**(지금 0건인 건 우연이지 안전이 아니다). **P1-2** 표면 절단 변이 생존 — 새 표면 테스트가 `fetchOtaoRoster`를 «빈 원장»으로 목해 **Jino가 실제로 보는 분기를 한 번도 렌더하지 않았고**, 그 분기에서 정산 섹션을 통째로 지워도 **972 passed**였다. ★★**파일 헤더에 「n=6 P1-3이 이 파일의 존재 이유」라 적어 놓고 같은 구멍을 한 칸 옆에 팠다** — 기본 목을 「원장 있음」으로 바꾸니 같은 변이가 **14개를 죽인다**. ⚠️**내 사고 자백**: M6 변이 원복 시 `git checkout -- settlement.py`가 **같은 파일의 P1-1 수리분까지 지웠다**(n=5가 남긴 교훈의 재발). 커밋 전 `git status`로 잡았고 커밋 메시지에 적었다 — **변이는 수정을 커밋한 «뒤»에 주입해야 한다.** ⚠️훅 오탐 **열일곱 번째**(`[체인] ⛔` — 공유 폴더 **507커밋** stale) + **표면 변이 훅 오탐 1건**(배포·관측 위임을 적대 리뷰로 오인 — 계약 §6 계수 대상). 백엔드 **6,804 passed/0** · 프론트 **973/68파일** · tsc 0 · eslint 96(래칫 불변) · 마이그레이션 **0** · ECOUNT 호출 **0** · 원장 쓰기 **0**. 증거 `docs/references/104_otao_settlement_window_20260827.md` · 리뷰 `docs/reviews/REVIEW_otao_settlement_s2_20260826.md`.
<!-- /TRACK-CONTRACT -->

> 계약 정본: `docs/contracts/CONTRACT_inventory_unified.md` (v0.2, 2026-08-25 승인)
> 기획 정본: `docs/PLAN_inventory-unified-management.md`
> 체인: `.claude/memory/chains/발주예측.jsonl`

## 1. 목표 (사람용 서술)

우리는 ECOUNT를 통해 OTAO에 발주를 보내고, 그 발주 수량 안에서 매달 몇 번씩 배송(픽업)을 받는다. OTAO 정산은 **매달 19일, 전월 20일~당월 19일에 픽업한 금액** 기준이다. 발주 수량은 **OTAO가 우리를 위해 생산해 두는 수량**이고, 우리는 **가져온 것만 지급**한다 — 결국 다 받아야 하지만 당장 안 가져와도 된다.

그래서 결정변수가 둘로 갈린다:

- **①발주층** — OTAO 생산 예약. 현금·창고비가 우리 장부 밖이라 「재고 최소화」가 작동하지 않는다. 목적함수는 `기대 데드스톡 채무 + 기대 생산품절 손실`의 최소화다.
- **②픽업층** — 예약 잔량 상한 안의 단기 재고·현금 결정. **Jino의 「제고 최소화」는 이 층에 산다.**

## 2. 확정 결정 (D-N 누적)

> 접두 **`D-INV`** — 이 트랙의 결정 접두다. 저장소 전수 검사에서 기존 접두는 `D-NAO`·`D-CPP` 둘뿐이고 `D-INV`는 미사용이라 충돌 0이다(2026-08-25 실측). `scripts/next_ids.sh`는 `D-NAO`·교훈만 발급하므로 이 접두는 본 트랙이 직접 채번한다.

| # | 결정 | 근거 | 일자 |
|---|---|---|---|
| **D-INV-1** | **「품목명 → 상품 매핑」을 본 트랙 범위에 편입** — 계약 「안 함」이 A′ 소관으로 지목했던 `internal_sku` 매칭 로직 중 **매핑 부분만** 본 계약이 갖는다. 원장 행·스키마는 **읽기 전용 유지**, 매핑은 본 계약 소유의 별도 테이블·서비스. A′가 나중에 `internal_sku`를 채우면 **그쪽이 정본**이 되고 우리 매핑은 검산 축으로 내려간다. | prod 실측 `import_invoice_line.internal_sku` **0/158(0%)** ⇒ 매핑 없이는 S1 「픽업 누계」 칸이 원리적으로 못 서고 S1·S2·S4가 연쇄로 막힌다. Jino 원문 *"해도 돼"* | 2026-08-25 22:52 |
| **D-INV-3** | **발주서 정본 규칙 — ①ECOUNT 사본이 있으면 그것이 정본 ②없으면 로컬 파일 중 `Revise_` 접두 또는 후행 사본 ③중복은 항상 `Serial No.`로 접는다.** 발주서 PDF 121개 = 발주서 95 + 비발주서 26(Packing List·Invoice) → **고유 발주번호 66**(2023:16·2024:16·2025:22·2026:12). 최상위 18개는 연도 폴더의 중복본이다. | ①**Jino 목표 원문이 정한다** — *"**ecount에 있는** 우리가 발주한 수량"* ②라이브 대조: `20260107-2`가 5,700본·1,300본으로 갈렸는데 통관 원장 `order 107 （20260107-2）` 실입고가 **정확히 1,300**(ECOUNT 사본과 일치) ③Jino 2026-08-25 23:13 *"revise가 있는건 개정본이겠지?"* ④해시 파일명 26개 = 2025(14)+2026(12), **전부 오늘 mtime** ⇒ ★**n=2의 「발주서 26건」은 「전체 발주가 26건」이 아니라 「n=1이 ECOUNT에서 받아온 사본이 26개」였다** — 2023~2024년 32건은 시야 밖이었다 | 2026-08-25 23:1x |
| **D-INV-4** | **★「최소 발주 단위 50개」는 일반 규칙이 아니다 — ref 99 §5-4·계약 §2 7-D의 전제를 정정한다.** `20251210-1` 발주서에 **12·395·420**개가 실재하고 헤더 `Q;ty 1,927`과 검산 일치한다(50의 배수 아님). n=2의 *"56/56 전건 예외 없음"*은 **3건·56행**짜리 표본(2026-06~08 창)에서 참이었을 뿐이다. ⇒ 7-D의 처방(느린 SKU에 「안 시킨다」)은 유지하되 **근거를 「50이 규칙을 압도한다」에 두지 않는다**; 66건 전수 재측정 후 갱신한다. | prod·PDF 원문 양쪽 확인(2026-08-25). ★교훈: 좁은 창의 「전건 예외 없음」을 **일반 규칙으로 승격**한 것이 병이다 — 표본의 창을 문장에 같이 적지 않으면 다음 세션이 규칙으로 읽는다 | 2026-08-25 23:0x |
| **D-INV-2** | **품목명 매핑 규칙 3 확정** — ①`screen protector` 접미는 상품 구분이 아니다(쌍 12개) ②공용 표기 ≡ 단일 표기(`For iP15/16/14Pro` ≡ `Glass_iP15 pro`, 6.1" 하나) ③`2ea` = 2매입 포장(별개 상품 아님). ★이로써 ref 99의 *"미접힘 16줄 = 다중모델 합본, 코드별 분해 근거 없음"*은 **틀린 판정으로 정정**된다 — 그 16줄은 `For ` 접두 **방언**이고(짧은 방언 142 + 긴 방언 16 = 158, prod 전수 일치) 공용이면 분해할 것이 없다. ⚠️`2ea` 줄의 `quantity`가 세트 수인지 낱장 수인지는 **[미상] 잔여**. | Jino 원문 2026-08-25 22:47 *"공용이라는 뜻이야"* · 22:50 *"①②③ 다 같은 상품이야, 2ea는 2매입 포장이고"* + prod 품목명 전수 조회 | 2026-08-25 22:50 |
| **D-INV-5** | **★사전의 모집단은 발주서 «전체 95건»이다 — 수량의 모집단(정본 66건)과 다르다.** 정본 규칙 D-INV-3은 **수량 축**의 규칙이고, 라벨 축에 그대로 적용하면 사전이 원리적으로 빈다(ECOUNT 사본 = B형 발주서라 `영문상품명` 칸이 **아예 없다**). 집행은 «필터 삭제»가 아니라 **층**이다(`build_layered_dictionary`) — ①정본이 말하는 키는 정본이 이긴다 ②정본이 **침묵하는** 키만 비정본이 채운다. **수량 축은 불변**(`build_roster` ①은 계속 정본만). | prod 전수(2026-08-26 11:40 KST): 새로 붙는 코드 **17종이 17/17 정본 라인에 실재**하는데 그 라인의 `name_en`은 **0/17** · 새 키 공급 라인 41개 전부 비정본·`local` · 대체 사유 29건 중 **27건이 「ECOUNT 사본이 정본」**. 커버리지 **61.44% → 87.18%**(+8종 5,600개), 변화는 **전부 `unmatched`→일치**, 새 `ambiguous` **0건**·코드 뒤바뀜 **0건**. ⇒ n=5가 우려한 맞바꿈은 **실재하지 않았다**. 근거 `docs/references/101_po_name_map_population_20260826.md` | 2026-08-26 |

| **D-INV-6** | **★OTAO 지급액의 금액 축은 «CNY Commercial Invoice»다 — 과세금액(KRW)이 아니다.** 그리고 헤더가 아니라 **라인**(`unit_price_foreign × quantity`)이 정본이다. **원화 환산은 하지 않는다.** | ①지급액은 우리가 OTAO에 주는 돈이고 `customs_value_krw`는 관세청이 세금을 매기는 값(CIF 기준·운임 포함)이라 **OTAO는 그 숫자를 모른다** ②prod 12선적 전건에서 `declared_inv_value == Σ(라인)` **차이 0.00**이라 라인을 믿을 수 있고, 라인이라야 한 선적을 창으로 쪼개고 `line_type`별로 가른다 ③`fx_rate`는 **신고환율**이고 `remittance_fx_rate`(실송금)는 **12/12 NULL** — 모델 docstring이 *"이번 범위가 아니다 — 필드만 예약한다"*고 명문화했다. 환산하면 우리가 쓰지도 않은 환율로 지어낸 숫자가 화면에 선다. 근거 `docs/references/104_otao_settlement_window_20260827.md` §2 | 2026-08-27 |

## 3. 진행 기록

- **2026-08-27 06:4x [1e623970]** — **S2 정산 창 축이 화면에 섰다. 진행률 4/9 불변**(앞 절반만 됐으므로 `[x]` 안 찍음). ★**D-INV-6**(금액 축 = CNY 인보이스·라인 정본·원화 환산 금지). ★★**지급액 원장 부재를 인구조사로 확정** — prod 123개 테이블 전수 + 파일 검색 **0건** ⇒ 전 창 「대조 불가」(`reconciled: null`)가 정직한 값이고, Jino가 창 1개분을 주면 그 자리에서 대조된다. ★부자재 **9.0%**(28,080 CNY)가 지급액엔 있고 S1 픽업 누계엔 없다 — 등식 `picked+unmapped=product_quantity` prod 성립. ★창 경계 ±2일 **3건**·draft **1건**을 화면이 지목(이 원장엔 OTAO 픽업일이 없다). 적대 리뷰 **1R FAIL(P1 2건)→2R PASS** — 둘 다 「값은 맞는데 안 닿는다」였고, 특히 P1-2는 **n=6 교훈을 적어 둔 그 파일에서 같은 구멍을 한 칸 옆에 판 것**이었다. PR #486 머지 `4ac8281d` · 배포 `7a023750`(무중단·마이그 0) · 산출 `docs/references/104_otao_settlement_window_20260827.md`.
- **2026-08-25 18:1x [75c58ce3]** — 되감기 방법 검증 + 발주 규칙 복원. 진행률 **2/9 불변**(이번 슬라이스는 S1~S5의 화면이 아니라 그 앞의 방법 검증). ★되감기 188칸 중 음수 2칸(1.1%) — 성립. ★★표본 **26건 → 3건**(입고 원장 2026-01-27부터·쿠팡 판매 2026-06부터) ⇒ ref 98 §6-A의 「26건 표」는 원리적으로 불가능. ★규칙: 최소 단위 **50개(56/56)** · 발주 **31일치**(빠름) = 리드타임 29일. ★Jino 18:08 *"비슷한 수준이지 100%는 아니야"* ⇒ 민감도 ±30%로 **수준 결론 폐기, 비율 결론 존치**. ★이탈: 예약잔량÷재고 **11.6배**, `GAPIP15` 재고 7개(1.5일치)에 예약잔량 3,150 두고 300개 신규 발주. 계약 §2에 **7-C·7-D 신설** + 7-B에 실측 주석. 산출 `docs/references/99_po_forecast_retro_stock_20260825.md`.
- **2026-08-25 13:3x [f243f6f4]** — 트랙 신설. 계약 v0.1(미승인 초안) → v0.2 개정, Jino 승인. 실측이 v0.1 전제 5개를 뒤집었다: ①ECOUNT 자격증명·발주 이력 1,622건(10년치) 실재 ②공식 매뉴얼 실측 문서 존재 → 금지선 6 해제 ③픽업 데이터 = 수입 원장 ④리드타임 확정(생산 4주 / 픽업 간격 ~14일) ⑤판매 4채널 prod 정상(cafe24만 판정불능). 진행률 **0/9**.
