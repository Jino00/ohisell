# TRACK — 원가 정본 원장 (cost-truth-ledger)

<!-- TRACK-CONTRACT v1 -->
목표: "SellC에 원가 메뉴를 하나 붙이자. 여기에는 우리가 현재 알고있는 모든 원가가 있잖아? 그 원가를 가지고 계산되는 방법이 첨부된 파일처럼 나오고 그 계산이 되면 되지 않을까? 그리고 수입건에 대해서는 두번째, 세번째 첨부자료처럼 수입한 금액을 알려주면 너가 이런것들을 모아서 회계적으로 계산되는 원가를 만들면 되잖아. 그리고 그런 원가를 만들려면 재고사항이 필요할꺼고, 그건 ecount와 연결하면 될꺼고." (Jino 2026-08-22 원문) / "목표는 정확한 원가 산출이니까?" (같은 날)
안함: 프로모션 분담금·즉시할인 손익 → 소관: docs/tracks/active/track_coupang-promo-pnl.md
안함: 광고비 배분·광고 최적화 → 소관: docs/tracks/active/track_naver-ad-optimization.md
안함: 상품↔채널 매핑의 커버리지·충돌 판정 → 소관: docs/tracks/active/track_product-connection-map.md
안함: 부가세 축 변경(D-NAO-150 「원가=VAT 포함」을 뒤집는 것) → 소관: 새 계약 필요(Jino 승인 지점)
합격:
- [x] B1 — 수입건 원장 스키마(테이블 5) + 3중 검산(수량합·INV총액·배부잔액0)이 prod에 존재한다 ✅ 2026-08-22 15:5x 종결 QA 달성 — `alembic current`=`mrg48s1heads(head)` · 실건 reconcile 3종 ok
- [x] B2 — 8/18 실건(SETR2608170216)이 prod에서 라이브 관측된다: Ip17Pro 단가 3,201원(VAT포함)/2,910원(제외) · cleaning kits 부자재 라인 배부 54,992원 · `product_master.cost_price` 변화 0건 ✅ 2026-08-22 15:53 QA 실측 — `GET /api/import-cost/shipments/1` → `2909.96`/`3200.96` · kits `material`/`54992.00` · 미배분 0 · prod 코드에 `cost_price` 매치 0건
- [ ] A1 — 국내 조립형(아이폰/갤럭시 필름) 원가가 「부자재 구성 × 공통 입력」 계산 결과로 prod에서 관측된다
- [ ] A2 — 플립/폴드·태블릿 계열까지 구성 이식 완료, `cost_drift` 대조 악화 0
- [ ] C1 — 재고 로트 원장(입고·판매 차감)이 prod에서 돈다
- [ ] C2 — 평가방법이 Jino 승인으로 확정되고 `cost_price`가 평가된 원가로 컷오버된다
상태: 🟢 Active
확인: 2026-08-22 13:4x [a9a2121b] — 트랙 신설. 계약 B 승인(Jino "승인 — 그대로 가자") · D-CPP-48 · 착수.
확인: 2026-08-22 14:2x [a9a2121b] — 계약 B 구현 완료(미배포·미병합). 테스트 40/40 · 회귀 6,034 passed(실패 2건은 사전 존재로 확정). 적대 리뷰 진행 중. 체크박스는 라이브 관측 전이라 0/6 유지.
확인: 2026-08-22 15:0x [a9a2121b] — 적대 리뷰 1R FAIL(P1 1: 검산이 `0==0`을 통과로 접음) → 수정·테스트 52/52 · 변이 16종 재주입 생존 0. D-CPP-47→48 재번호(병행 세션 선점). 여전히 미배포라 0/6 유지.
확인: 2026-08-22 15:5x [a9a2121b] — **prod 배포 완료 + 라이브 합격 확인**. PR #324 병합 · 마이그 `mrg48s1heads` · 무중단 재시작(다운타임 0초) · 프론트 dist. 8/18 실건이 prod에 확정 저장(id=1). 완료 QA 재판정 3대조 **전부 부분달성** — 진행률 **0/6 → 2/6**(B1·B2). ⓔ(원본 3개)만 미달로 닫음.
확인: 2026-08-22 18:0x [a9a2121b] — D-CPP-50 관세 귀속 배포·병합(PR #333). prod 라이브 9/9(7/22 실건 id=2, 세율검산 차 −3.73원 · 부자재 관세 0원 · 하위호환 id=1 불변). 파서 열밀림 결함 수정(PR #329). 진행률 2/6 불변 — A1·A2·C1·C2는 미착수.
확인: 2026-08-22 20:4x [a9a2121b] — 8개월 14 수입건 전수 실측 완료(ref 92). 파서 양식변종 4종 수리(PR #329 외). ★AI_office에 수입원가 트랙이 이미 있음을 발견(중복 위험, Jino 결정 대기). 진행률 2/6 불변 — 조사·수리이지 새 합격항목 아님.
확인: 2026-08-22 22:2x [bd4adba0] — ✅**계약 A′ 승인 (Jino "진행하자", 22:22 KST) = D-CPP-53** — `docs/PLAN_cost-menu-standard-cost.md`(464줄)가 정본. 레시피 키 = **상품명 × 폼팩터**(값 9종을 원가 정본 섹션에서 실측 도출: bar·flip·fold·trifold·tablet 3구간·doorlock·birdie + 수입/매입품은 null). 설계 확정 3건: **필름 원단은 종을 쪼갠다**(플립 내부 600 vs 폴드 내부 1,000은 재단 규격이 다른 «다른 물건»이지 같은 물건의 다른 가격이 아니다) · 매입품 32건은 `recipe_kind` 값 증식 없이 표현 · SKU 연결은 신설 `cost_recipe_link`(`product_master` 컬럼 추가는 금지선). ★개정 중 발견: **한 상품명이 플립·폴드 옵션을 함께 담는 실례**가 있어 **폼팩터는 상품명이 아니라 옵션이 정한다**. 합격기준 9항목 확정(§7). **S1 착수** — 진행률 **2/6 불변**(라이브 전).
확인: 2026-08-22 21:5x [bd4adba0] — ★**레시피의 키는 SKU가 아니라 «상품명»이다** (Jino 제안: *"두번째 첨부된 엑셀파일에 있는 상품명의 구조로 만들어주는건 어떨까? 상품명이 같으면 들어가는 부품도 같거든"*). 정본 `~/…/Ohi/15. 기획/상품 리스트/ohisell_mapping_template_20260822.xlsx` 「원가 매핑」 시트 **읽기 전용 실측**: 상품명 **88개** / 옵션 행 **944개**(상품명×기종) / 채널 코드 **2,638개**(카페24·스스·쿠팡 5계정). ⇒ Jino가 승인할 레시피가 **944 → 88개**로 줄고, 상품명에 붙인 원가가 채널 코드 2,638개까지 전파된다. 부수 확인: **매수가 상품명에 이미 인코딩**(「2매」·「3매」·「1매입」 88개 중 28건) — 앞서 논의한 1장/2장 구분이 상품명 층에서 자동 해결. ⚠️**미결 2건(Jino 대기)**: ①상품명당 옵션 최대 **163종**이 갤럭시Z 56·아이폰 45·갤럭시S 34·갤럭시A 17·LG까지 걸친다 — 부품 «종류»는 같아도 원단 «면적»은 다르다(D-CPP-27의 공통 2,350.7 vs 기종별 2,616 논쟁의 재출현). ②88개 중 **32건은 조립품이 아니라 매입 완제품**(맥도도 충전기·케이블·애플워치 스트랩 등) — 레시피가 없고 매입가가 곧 원가다. 계약 A′ 미개정(답 대기). 진행률 **2/6 불변**.
확인: 2026-08-22 21:4x [bd4adba0] — **Jino 결정 2건 즉시 기록: D-CPP-51**(수입 부가세를 «우리 기준»으로 원가에 포함 — 회계 정답이 아니라 사내 관리회계 기준임을 명시. 1차 출처 대조 결과 실7.11은 소비세만 열거하고 부가세는 이름이 없으며 §72②1호도 포괄 문구뿐 = **공백을 사업주가 메운 것**) · **D-CPP-52**(엑셀 고정 환율 4종 폐기, 수입 시 신고환율만 사용 — 실측 +8.8%와 사업주 증언이 일치). 부수로 「엑셀 태블릿 표 2벌」 이상치가 **이상이 아님**으로 닫힘(조아테크 원단으로 1장·2장 제품을 둘 다 만든다 = 제품 두 종류). 계약 A′ 초안 `docs/PLAN_cost-menu-standard-cost.md` 작성(승인 대기). 진행률 **2/6 불변** — 계약 미승인·코드 0줄.
확인: 2026-08-22 21:1x [bd4adba0] — 계약 A′(A1·A2) 착수. Jino 지시 2건: "A로 하자" + ★"우리가 강화유리 정보에서 읽는 수입서류에 나와있는 부자제 단가를 참고해" ⇒ 표준원가의 단가 입력 = 엑셀 하드코딩이 **아니라** 계약 B 원장(수입서류 실측분). 이로써 D-CPP-31 초안의 미해결 질문 중 «단가» 갈래는 닫혔고, 남은 갈래는 «구성(부자재 목록·수량) 파싱을 확인분으로 인정하는가»다. 착수 필독 실측(Sonnet, 읽기 전용)이 ref 92 §7을 재확인 — **A′ 초안의 「공통 입력」 전제가 반증됨**(환율 207.51~226.66 · 물류율 5.0~16.3% 3배 차 ⇒ 공통 상수가 아니라 로트별). ⇒ 「어느 로트 단가를 표준원가로 쓰는가」가 계약 A′의 선행 결정. 진행률 **2/6 불변** — 계약 미승인·코드 0줄. 트랙 인덱스 `docs/TRACKS.md` **미등재**를 함께 발견해 등재.
확인: 2026-08-23 00:1x~07:1x [bd4adba0] — S1 구현 + 적대 리뷰 **1R FAIL(P1 2) → 2R PASS**. 커밋 `a72fb8c7`(스키마 7테이블·마이그 `cst1menu53a`·부자재 층·「💰 원가」 메뉴) + `6b9a7302`(1R P1 수정). ★1R P1은 뿌리 하나 — `source='ledger'` 단가행이 **연결 시점 값을 복사해 두고 재검사하지 않아** 네 갈래로 터졌다(reopen 후 낡은 값이 「최신 확정 로트 단가」로 잔존 / 환율 정정 후에도 옛 값 표시 + 재연결 409 / 수입건 삭제 후 고아 잔존(`PRAGMA foreign_keys` 미설정으로 CASCADE가 선언만 되고 안 돎, prod도 SQLite) / **라인 순서 정정 시 rowid 재사용**으로 cleaning kit의 근거가 Glass_iP12promax로 바뀜). 수정 = 근거 보존은 유지하되 **조회 시점 재검사**(`ledger_check.py` 순수 SA, 6값 `ok/manual/missing/unconfirmed/changed/item_mismatch`) + 스냅샷 2칸(`linked_item_name`·`linked_shipment_id`, 마이그 `cst2snap53b`) + `refresh` 엔드포인트(409 해소, `item_mismatch`는 거부). 어긋난 행은 **이력에 남되 「최신」 자격만 잃는다**. ★★**표면 절단 변이 4종이 1R에서 전부 SURVIVED**(「💰 원가」 메뉴를 통째로 지워도 21건 초록) — 저장소가 **다섯 번째**로 밟은 병. `costPageReachesTheUser.test.tsx`(App을 `/cost`에서 통째 렌더) 신설로 2R에서 **13종 전부 KILLED**(새 자백 표면 5종 포함). 테스트 신규 85종 · 전건 `2 failed / 6,220 passed`(사전 존재 2건, 신규 0) · 프론트 540 · `tsc -b` 통과. 2R P2 2건(`linked_shipment_id` 미사용 / 구 연결 자백 문구) **채택 — 수정 중**. ⚠️**미배포**(prod alembic 헤드 `duty50attrib`, `cost_*` 테이블 0개) ⇒ 합격 1은 아직 미충족. 진행률 **2/6 불변**.
확인: 2026-08-23 07:5x [bd4adba0] — ✅**S1 prod 배포 완료** (백엔드 22:52:35Z 무중단 :8011→:8001 다운타임 0초 · 프론트 22:54:08Z CAS 통과 1회, 스탬프 `3be8caa6`). 마이그 `duty50attrib → cst1menu53a → cst2snap53b`, prod 테이블 7개 실재. 2R P2 채택 2건 반영(`2fc2144a`: 수입건 신원도 대조 + 구 연결은 갱신 거부). ★★**배포 직전 큰 사고를 피했다** — 배포 파일 목록에 내가 안 건드린 `naver_ad/reflection_*`가 섞여 있어 확인하니 브랜치가 **origin/main보다 17커밋 뒤**였다(병행 세션 D-NAO-228 푸시분). 그대로 올렸으면 **그 세션 작업을 prod에서 되돌릴 뻔했다.** 병합(충돌 1건 `docs/TRACKS.md` — 양쪽 트랙 항목 다 보존) 후 회귀 재실행(`2 failed / 6,244 passed`, 사전 존재 2건·신규 0) → 배포. 커밋 `3be8caa6`. ⚠️**배포 스크립트 사각 발견**: prod에 `backend/app/services/cost_menu/` 디렉터리가 없어 scp 4건이 실패했는데 **그 시점에 `main.py`는 이미 그 패키지를 import하도록 배포돼 있었다** — 재시작됐으면 앱이 죽었다. 스크립트가 재시작 전에 멈춰 구 프로세스가 살아 있었고(HTTP 200 확인), `mkdir -p` 후 재배포로 완주. **`safe_deploy.sh`는 신규 디렉터리를 만들지 않는다** → 이월. **라이브 판정**: 합격②(무해성) **달성** — `product_master` 963행·SUM 3642052.09999999·AVG 3781.9856 **배포 전후 완전 동일**, health 16키·`cost_drift: null` 동일(변한 건 `as_of`·`failed[].age_sec`뿐 = 시간 탓, 실패 잡은 전부 배포 전부터 있던 쿠팡 RG류). prod 로그 새 라우터 에러 0건, 서비스 파일 4개 sha256 로컬↔원격 일치. 합격① **미완 — 「Jino가 연결 버튼을 눌러야 완성」**: `cleaning kit` 종은 있고 원장 라인 2건(178.78 / 190.82, id=1의 `duty_rate`는 null 그대로 유지)이 **제안까지 정확히 뜨지만** `lot_count:0`(확정은 사람 몫, 계약 §5-2). ★**임의 연결로 증거를 만들지 않았다**(교훈 #274). 브라우저 화면은 BasicAuth로 미확인(자격증명 미시도). 진행률 **2/6 불변** — A1은 S2(레시피·계산) 이후다.
확인: 2026-08-23 11:4x [bd4adba0] — ✅**S1 종결 · 완료 QA 3대조 판정**(별도 Sonnet·읽기 전용). **판정(계약 A′ §7): 달성** — S1 대상 3항목 전부 prod 라이브. Jino가 화면에서 「연결」을 누른 뒤 관측: cleaning kit 로트 2건이 **서로 다른 값**(id=1 **190.82**/209.9 · id=2 **178.78**/196.66, 격차 +6.7%), 양쪽 「원장과 일치」·`lot_count:2`·`stale_count:0`. 무해성 = `product_master` 963행·SUM 3642052.09999999·AVG 3781.98556593976 **배포 전후 완전 동일** + health 16키·`cost_drift:null` 동일. VAT 배지·기본표시 inc_vat 실재. **판정(Jino 지시 원문): 부분달성** — 「원가 메뉴를 붙이자」·수입금액 입력층은 달성, 「계산되는 방법이 첨부된 파일처럼 나오고」(S2)·「재고사항·ecount」(계약 C)는 미착수. **판정(트랙 궁극 목표): 부분달성** — **진행률 2/6 불변**(A1은 S2 이후). 선행 인프라는 라이브지만 «정확한 원가 산출»엔 못 닿음. 「안 함」·금지선 침범 0건(`cost_price` 쓰기 0 확인). PR **#339** OPEN→병합. ★QA가 확인 못한 것: `product_master` **행 단위 다이제스트 미확인**(배포 전 풀스냅샷 부재 — 집계 3종 일치로만 검증) · 레시피/보드 탭 렌더 미확인(S1 판정엔 불필요). ★QA의 「prod 무인증 노출」 관찰은 **오해로 판명** — 우리 egress IP `115.23.234.145`가 `ohisell-allowlist.conf:11`에 등재돼 있고 vhost는 D-2026-08-13 결정대로 `satisfy any`+`auth_basic`이다(**허용 IP라 통과, 새 노출 아님**). 잘못된 전제를 위임문에 실은 것이 원인.
확인: 2026-08-23 12:5x [bd4adba0] — ★**Jino가 이 트랙의 목표를 명명했다: 「SellC 원가 메뉴 목표」** *("이 목표의 이름을 SellC 원가 메뉴 목표라고 하자")*. 정본 카드 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md` 신설 — 앞으로 앵커·HANDOFF·계약·완료 QA 위임문은 **목표를 다시 쓰지 말고 이 이름으로 부르고 그 파일을 가리킨다**. 이 헤더와 갈라지면 **카드가 이긴다**(옆 트랙의 「남의 목표를 인용한 채 8/8 종결」 사고 재발 방지). 진행률 **2/6 불변**.
확인: 2026-08-23 13:0x [bd4adba0] — ★**Jino 운영 지시**: *"다음 세션에서부터는 SellC 원가 메뉴 목표를 달성하는 위주로 진행되게 목표잡기와, 다른 트랙의 일을 집어와서 여기에 섞지 말라"*. ⇒ ①**목표잡기는 역산** — 착수 질문은 「무엇을 할까」가 아니라 「합격 6항목 중 어느 것을 [x]로 만드는가」이고, 앵커 `이번 세션:`에 **겨냥한 합격항목을 적는다**(어디에도 안 걸리면 표류 신호). ②**다른 트랙 일 금지** — 헤더 `안함:` 4줄 + 쿠팡 2P + 하네스 결함 + **같은 트랙의 계약 B 건(원장 결함·소급 적재·자금정산서 파서)까지** 전부 «발견해도 고치지 말고 이월에 소관 지목». 자백 경로 `↗️ 스코프 밖: <이유> / 목표: 「SellC 원가 메뉴 목표」`. 규율 전문은 HANDOFF §0-A와 목표 카드 「운영 원칙」에 이중 기록. 진행률 **2/6 불변**.
확인: 2026-08-23 13:1x [3114dd1d] — 체인 n=3 인계만 수행. **진행률 2/6 불변 — 코드 0줄·커밋 0건·배포 0건.** 겨냥 합격항목은 **A1**(슬라이스 S2, 계약 A′ §6·합격 3·4·5)이고 앵커에 명시했으나 **착수 전이다.** ★착수 시 오판 1건을 정정해 기록: `/session-relay` 생존 판정이 체인 등록부를 **공유 메인 폴더 워킹카피**에서 읽어 n=2(bd4adba0)를 `end_kst:null` = 「살아 있음」으로 보고 이어받기를 보류했다 — 실제로는 그 폴더 HEAD가 `origin/main`보다 **35커밋 뒤**(`211decbc` vs `cd90a3b8`)라 n=2가 남긴 등록부·인계·트랙 기록이 사본에 없었을 뿐이고, 정본에는 `end_kst: 2026-08-23 12:5x`·handoff가 기록돼 있다. n=2 브랜치 `feat/cost-menu-aprime`은 PR #339로 이미 `origin/main`의 조상. **매 턴 주입되는 `[체인] ⛔` 훅 줄도 같은 낡은 사본을 읽으므로 훅 줄 자체가 오탐일 수 있다** — 워크트리가 표준이 된 지금 구조적 상시 상태다(소관: 하네스, 이월). ⇒ 생존 판정은 `git show origin/main:<경로>`로 읽는 것이 규율. `failures.jsonl`·프로젝트 메모리·앵커 3곳에 기록. 작업 위치는 `origin/main` 기준 새 워크트리 `cost-menu-s2`(`feat/cost-menu-s2`) — 공유 메인 폴더는 병행 세션 3개가 물려 있어 **미접촉**.
확인: 2026-08-23 13:4x [3114dd1d] — **S2 구현 완료(미배포·미병합)**. 계약 A′ §6 S2 = 레시피·링크 파싱 + 승인 + 순수 SA 표준원가 + 보드. ★**계약이 못 박지 않은 구멍 하나를 실측으로 메웠다**: 원가표는 «품목»(「지문방지필름 TPU 3매」)으로, 매핑 정본은 «상품명»(「오하이 빛반사, 지문방지 매트 필름 3매」)으로 적혀 있고 **둘을 잇는 열이 두 파일 어디에도 없다** — 그런데 레시피 키는 「상품명 × 폼팩터」다(§0-B). ⇒ **독립 신호 2개의 교차**로 이었다: ①옵션명에서 제안한 폼팩터 ②그 묶음 SKU들의 `product_master.cost_price` 최빈값 ↔ 원가표 「제품원가(+VAT)」. 서로를 참조하지 않으므로 일치가 우연이 아니다. 실측(표적 상품명 162 SKU): **bar 108건 전부 cost_price=2350.7 — 순도 100%**, flip 27건·fold 30건은 값 혼재(S3 몫). `cost_price`는 **읽기만** 했고 그 사실을 테스트가 지킨다(§3 금지선). ★**파서 자기검산**: 조립형 **61건 전건** 「라인 합 × 1.1 = 엑셀 총액」 통과, **불일치 0건**. 표적 = 라인 **9종**(합격 4의 「부자재 9종」과 일치) · ex **2,137** · inc **2,350.70**(합격 3 정본 대조값과 소수 1자리 일치). 이상 27건은 전부 진짜 사실이라 승인 화면으로 올린다 — 수입·매입 섹션 10(조립형이 아니라 열이 «계수»다 → S3) · 단가 충돌 10(폴드 패키지 370≠98 등) · 같은이름-다른구성 7(**§9-9 이상① 폴드 행42 vs 행105 포함**). ★**엑셀 단가 자동 유입 금지의 집행을 주석이 아니라 «배치»로 했다**: 참고값은 `cost_material.excel_ref_price`(마이그 `cst3exref53c`, 순수 추가)에 두는데 계산이 읽는 것은 `cost_material_price` 행뿐이라 **계산 경로에 원리적으로 닿지 않는다**. Jino가 「채택」을 눌러야 `manual` 단가가 되고 `note`에 출처가 남는다. 신설: `standard_cost.py`·`recipe_parser.py`·`mapping_parser.py`·`recipes.py`(순수 SA 3 + DB층 1) · 라우터 7엔드포인트 · 화면 탭2·탭3 · 모델 `CostRecipeLine.material` 관계. 테스트: 백엔드 신규 **20/20** · 프론트 **573**(S1 540 → +33) · `tsc -b` 통과. ★**표면 변이 4/4 사망**(보드 호출부·계산내역 호출부·「레시피」 탭 버튼·미계산 사유 제거 — 각각 빨간불). ★자체 발견 결함 1건: `adopt`가 단가를 `db.add()`로 넣어 이미 로드된 `material.prices`가 낡은 채 재계산돼 **레시피 상세엔 2,350.70이 뜨는데 보드만 빈 칸**이었다 — 관계 append로 고쳤고 그걸 잡은 건 보드 테스트다. **진행률 2/6 불변** — A1은 「prod에서 관측」이고 아직 배포·리뷰 전이다. 체크박스는 증거 좌표 없이 찍지 않는다. ⚠️`track-progress-sync.sh`가 공유 메인 폴더 사본을 읽어 이 줄을 못 보고 계속 운다(이월 — 소관: 하네스).
확인: 2026-08-23 13:5x [3114dd1d] — **적대 리뷰 1R FAIL(P1 1건) → 수정 커밋 `308ec25e`**(2R 판정 대기). ★리뷰 지적보다 **결함이 한 겹 더 깊었다**: 리뷰어는 「종 승인 가드에 테스트가 0건」(변이 M9 SURVIVED)을 짚었는데, 재현해 보니 **단가가 178.78원으로 실재하는데도 그 값을 버리고 `unconfirmed`로 보고**해 화면이 「단가 미확정」이라 말하며 실재값을 「—」로 감추고 있었다 — **Jino를 «이미 있는 단가를 입력하러» 보내는 문장**이었다. 뿌리는 원장의 `unconfirmed`(수입건 확정 해제 = **값이 없다**)와 「종 미승인」(**값은 있는데 못 쓴다**)을 한 단어로 접은 것. ⇒ `material_unapproved` 상태 1개 신설 + 미승인 라인도 **단가를 그대로 실어 보냄** + 사유를 **상태별로 나눠**(`unusable_summary`) + 각 사유에 **무엇을 해야 하는지** 병기(「부자재 탭에서 「승인」」) + 화면은 `priceStatusLabel()`로 사람 말을 그린다(원시 상태 문자열 노출 금지). **사유가 틀리면 사람이 틀린 일을 한다** — 이게 §2 표면 규칙의 이번 슬라이스판이다. ★**리뷰어가 옳고 내 주석이 틀린 것 1건**: `필름*매입` 판별 순서를 docstring이 *「이 파서에서 가장 깨지기 쉬운 자리」*라고 **과장**했는데 등가 변이로 반증됐다(M10) — 주석을 정정하고 분류 자체를 테스트로 옮겼다(주장은 주석이 아니라 테스트에 둔다). **변이 M9·M10 둘 다 이제 KILLED**. P2 트리아지(라운드 안 늘림): P2-1 **채택**(`db.add` → `recipe.lines.append` — 이번 슬라이스에서 실제 결함을 낸 바로 그 모양이라 자리가 아니라 «모양»을 고쳤다) · P2-2 **이월**(`board()` 레시피별 재계산, S3에서 재검토) · P2-3 **기각**(`display_name`이 폼팩터·부위를 이미 포함하고 `name`이 unique라 이름 dedup이 옳다). 회귀: 백엔드 전건 **2 failed / 6,296 passed** — 실패 2건이 깨끗한 `origin/main`에서도 같은 실패임을 **임시 워크트리로 직접 재현**해 확인(인계 주장을 안 믿고 실측). 신규 실패 0건. 프론트 **575**(+2) · `tsc -b` 통과. **진행률 2/6 불변** — 여전히 미배포이고 A1은 「prod에서 관측」이다.
확인: 2026-08-23 14:0x [3114dd1d] — **적대 리뷰 2R PASS**(P1 0건). 5개 해소 항목 전부 「예」: M9 사망(2건)·M10 사망(1건)·사유 정정·반대 방향(단가 자체가 없으면 「승인하라」고 안 시킨다)·화면 도달. ★리뷰어가 **자기 표면 절단 변이**를 하나 더 넣었다 — `priceStatusLabel()` 호출부를 원시 상태 노출로 바꾸니 테스트 사망(KILLED). 2R는 §4 규칙 ③대로 **수정 diff만** 봤다(새 지적 생산 금지). ★**배포 직전 뒤처짐을 다시 발견했다** — `origin/main`이 **17커밋** 앞서 있었다(`git rev-list --count HEAD..origin/main`). 배포 대상 파일과 겹침은 0건이었으나, 그 17커밋 중 하나가 **프론트를 배포한 이력**(`beb85e5a`)이 있어 **병합 없이 `dist`를 통짜 rsync 했으면 그 세션 작업이 prod에서 사라졌다**(2026-08-06 프론트 clobber 3회와 같은 자리). 병합 — **충돌 0건**(`6dd4a747`). S1이 배운 「배포 전 `git diff --name-only origin/main...HEAD`에 모르는 파일이 있으면 그것이 뒤처짐 신호」를 이번엔 **모르는 파일이 없어도** 커밋 수로 재확인한 것이 걸렸다 — 파일 겹침만 보면 프론트 통짜 배포는 못 잡는다. 병합 후 회귀: 프론트 **597 passed / 50파일**(다른 세션 신규 테스트 포함) · `tsc -b` 통과 · `npm run build` 스탬프 `6dd4a747 dirty=0`. 백엔드 전건 재실행 중. **진행률 2/6 불변** — 아직 미배포다.
확인: 2026-08-23 14:1x [3114dd1d] — ✅**S2 prod 배포·병합 완료**(PR **#349** → `eb900fa2`). 백엔드 무중단 재시작 **다운타임 0초**(:8011→:8001) · alembic `cst2snap53b → cst3exref53c` · 프론트 CAS **1회 거부 → 재빌드 후 통과**(트랙 문서 커밋이 빌드 뒤에 들어가 스탬프 `6dd4a747`≠HEAD `7d70817b` — 가드가 옳게 막았다). **라이브 관측**: `/api/cost/recipes`·`/api/cost/board` **HTTP 200**(13:17 완료 QA 시점엔 **404**였다) · 보드 빈 응답(`sku_count:0` — 아직 아무도 안 올렸으니 정상) · prod `cost_material.excel_ref_price` 컬럼 실재. ★**합격 5(무해성) 달성** — `product_master` **963행·SUM 3642052.09999999·AVG 3781.98556593976**로 S1 기준선과 **완전 동일**, `cost_drift: null`·16키 동일, cost 관련 에러 로그 **0건**. CI는 `--force` 병합 — **가정이 아니라 실측으로** 결제 정지 서명을 확인했다: 3잡 전부 `steps:0`(스텝 미실행) · 로그 blob **BlobNotFound(404)** · 2초 종료. 같은 날 다른 세션도 #345·#348을 같은 이유로 강제 병합했다(자백 로그 공유). ★**진행률 2/6 불변 — A1은 아직 미판정이다.** 합격 3·4는 「Jino가 화면에서 엑셀 2종을 올리고 승인·채택을 눌러 2,350.70을 본다」이고, **승인은 계약 §2-2가 사람 몫으로 못 박은 자리**라 내가 대신 누르지 않았다 — 판정하러 간 기가 증거를 만들면 억지 충족이다(교훈 #274). 다음 세션/Jino 조작 후에 A1 체크박스 판정.
확인: 2026-08-23 16:4x [3114dd1d] — **세션 종결 · 완료 QA 재판정 3대조 = 부분달성 / 부분달성 / 미달**(별도 Sonnet·읽기 전용). ★13:17 판정은 **S2 착수 «전»**(커밋 0건·엔드포인트 404)을 잰 것이라 낡아서 **치우고 재판정**했다 — 낡은 판정을 두면 「미달」이 이 작업의 최종 기록이 되는데 그건 사실이 아니다. **판정(계약 A′ §7): 부분달성** — 합격 5·§3 금지선 달성 / 합격 3·4는 **사람의 화면 조작이 아직 없어** 미달 / 합격 9는 번들에 문자열 실재하나 렌더 미접근으로 **판정불능**. **판정(Jino 지시 원문): 부분달성** — 운영 지시(역산 목표잡기·타 트랙 배제)는 **완전 준수**, 그러나 「계산되는 방법이 화면에서 보인다」는 인프라만 있고 prod에서 발생 안 함(`cost_recipe=0`). **판정(트랙 궁극 목표): 미달** — **진행률 2/6 불변**, A1 체크박스를 이번 세션도 열지 못했다. 단 승인 전 마지막 단계(사람 클릭)만 남기고 계산 파이프라인 전량이 prod에 **무해하게** 배포된 상태다. ★QA가 화면 로그인·클릭을 **의도적으로 생략**했다(교훈 #274 — 판정 재료를 만들지 않기 위해). 그게 이 판정이 정직한 이유다. 교훈 **#349**(「값이 없다」와 「값은 있는데 못 쓴다」를 한 단어로 부르면 화면이 사람을 틀린 일로 보낸다) · **#350**(배포 전 뒤처짐 검사는 파일 겹침만으론 부족 — 프론트는 통짜 rsync). HANDOFF `.claude/memory/HANDOFF_cost-menu-s2-standard-cost_20260823.md`. **다음 세션 첫 일 = §2 A1 라이브 관측**(엑셀 2종 업로드 → bar 레시피 승인 → 채택 → 보드에서 2,350.70 × SKU 106건).
확인: 2026-08-23 17:0x [5432a577] — 체인 n=4 인계만 수행. **진행률 2/6 불변 — 코드 0줄·커밋 0건·배포 0건.** 겨냥 합격항목은 **A1**(인계 §2 라이브 관측)이고 앵커 `이번 세션:`에 명시했다. ★prod 읽기 전용 실측으로 인계 주장을 확인: `cost_recipe=0 · cost_recipe_line=0 · cost_material=1 · cost_material_price=2` — **인프라는 라이브인데 아무도 안 올렸다**(인계 §2 그대로). A1 합격은 「Jino가 화면에서 엑셀 2종 업로드 → bar 레시피 승인 → 채택 → 보드에서 2,350.70 관측」이고 승인은 계약 §2-2가 사람 몫으로 못 박은 자리라 **내가 대신 누르지 않는다**(교훈 #274). Jino에게 4단계를 제시하고 대기 중. ★착수 시 오판 방지 1건: 훅 `[체인] ⛔ sellc-원가-메뉴 n=2 살아 있음`은 **오탐**이다 — 공유 메인 폴더 HEAD `211decbc`가 `origin/main` `5b600819`보다 뒤처진 사본이고, 정본에는 n=2(`end 12:5x`)·n=3(`end 16:4x`) 모두 종결돼 있다. n=3이 §9에 기록한 것과 같은 뿌리이고 이번엔 `git show origin/main:` 로 **처음부터** 읽어 보류 없이 이어받았다. 작업 위치는 `origin/main` 기준 새 워크트리 `cost-menu-s3`(`feat/cost-menu-s3`) — 공유 메인 폴더 **미접촉**(그래서 `track-progress-sync.sh`는 이 줄도 못 보고 운다, 이월 · 소관: 하네스).
확인: 2026-08-23 18:0x [5432a577] — ✅**A1의 «표면»을 고쳐 prod 배포**(진행률 **2/6 불변** — A1은 여전히 Jino의 클릭 대기다). Jino 지시: *"이 부분을 좀 더 선택이 쉽게 직관적으로 바꾸자"* — 엑셀 2종 업로드가 벌거벗은 `<input type="file">` 2개라 ①어느 파일을 어디에 넣는지 ②왜 버튼이 안 눌리는지 ③잘못 넣으면 무엇이 틀렸는지를 **하나도 말하지 않았다**. 이 화면은 A1 합격의 표면이므로 여기서 막히면 계산 파이프라인이 아무리 옳아도 진행률이 안 움직인다. 한 것: 카드형 드롭존 2개(클릭·드래그·키보드) · 기대 시트 이름(「제품 원가표」·「원가 매핑」)을 선택 **전**에 노출 · 선택 후 파일명·크기 + 「바꾸기」/「지우기」 · `.xlsx` 아니면 그 자리에서 거부하고 사유에 **할 일** 병기 · 버튼 비활성 **이유**를 문장으로 · 패널 밖 드롭은 `preventDefault`로 조용히 무시. 계약 §3 설명문·이상 목록 접힘부는 그대로 뒀다(의도된 설계). 백엔드·API 계약 **무변경**. ★**적대 리뷰 1R FAIL(P1 1건) → 2R PASS**. P1 = 카드가 `role="button"`이라 Enter/Space를 **무조건** 삼켜, 중첩된 「지우기」가 **키보드로는 영영 작동하지 않고** 대신 파일 선택창이 열렸다 — 「바꾸기」는 목적이 우연히 같아 증상이 안 보였고 **그 우연이 결함 절반을 가렸다**. 고침 = `e.target !== e.currentTarget`이면 카드가 키를 안 받는다. P2 트리아지: **채택 2**(고른 파일 위에 잘못된 파일을 넣으면 앞 선택이 «경고 없이» 사라지던 것 → 「앞서 고른 「X」은 취소됐습니다」 / 한 칸에 여러 파일 드롭 시 나머지를 조용히 버리던 것 → 「N개 중 「X」만 골랐습니다」 고지, **거부가 아니라 수락+고지**) · **기각 1**(확장자만 검증 — 주석에 한계 명시·시트명 검사가 서버에 있다) · **이월 3**(드래그 플리커·정적 aria-label·중첩 패턴 자체 — 실브라우저·AT 확인이 선행이라 이 라운드에서 판정 불가). ★★**내가 쓴 P1 회귀 테스트가 허수였다** — `fireEvent.click`을 뒤에 붙였더니 그 클릭이 파일을 지워 **결함이 있어도 초록**이었다(변이 SURVIVED로 자백). jsdom은 keydown 뒤 네이티브 버튼 활성화를 대신해 주지 않으므로 「브라우저가 그 활성화를 **할 수 있는 상태인가**」(부모가 preventDefault로 죽였는가)를 직접 재도록 다시 썼다 → KILLED. **표면 절단 변이도 한 종이 SURVIVED였다** — 위임 산출물의 테스트가 **드롭 경로를 한 줄도 안 지켰다**(클릭 선택만 재면 `onChange`만 밟고 `onDrop`을 못 밟는다). Jino가 처음 물은 것이 *"파일을 그냥 드롭하면 되나?"*였으니 드롭은 이 화면의 «사람이 쓰는 경로»다 — 드롭 성공·드롭 거부 2종 신설. 최종 변이 2R 판정 **A~D 4/4 KILLED**(표면 절단 D 포함). 테스트 프론트 **663 passed**(병합 후, 다른 세션 신규 포함) · `tsc -b` 통과. ★**배포 전 뒤처짐 29커밋** 발견 — 그 창에 프론트 배포 이력이 여럿이라 **병합 없이 `dist`를 통짜 rsync 했으면 그 세션들 작업이 prod에서 사라졌다**(교훈 #350이 가리킨 바로 그 자리, 3세션 연속 적중). 병합 충돌 0건(`1d382d52`) → 재빌드(스탬프 `1d382d5271a8 dirty=0`) → `safe_deploy.sh --frontend` **CAS 통과**. 라이브: prod 번들 `index-Dyv1ST2r.js`에 신규 문구 실재(「클릭하거나 파일을 끌어다 놓으세요」·「앞서 고른」). **무해성**: `product_master` **963행 · SUM 3642052.1** — S1·S2 기준선과 동일, `cost_recipe` 0행(아직 아무도 안 올림 = 정상).
확인: 2026-08-23 20:2x [5432a577] — ✅**엑셀 업로드 성공 → 제품·옵션 드롭다운 배포**(진행률 **2/6 불변** — A1은 여전히 Jino의 승인 클릭 대기). ★**Jino가 엑셀 2종을 올렸고 파싱이 실측과 맞았다**: prod `cost_recipe` **100건** · `cost_recipe_line` 156줄 · `cost_material` 129종 · `cost_recipe_link` **924건**. 목표 레시피 `id=68`(「오하이 빛반사, 지문방지 매트 필름 3매」 × `bar`) = 구성 **9줄** · SKU **107건**으로 인계 예측(부자재 9종·SKU 106건)과 일치하고, 라인 실측이 엑셀 검산과 정확히 같다(필름 600×3 + 30 + 22 + 60 + 8 + 13 + 98 + 6 + 100 = **2,137** ⇒ ×1.1 = **2,350.70**). 부자재 9종 전부 `unconfirmed`·단가 행 **0건** = 설계대로(엑셀 참고값은 `excel_ref_price`에만 있고 계산 경로에 원리적으로 안 닿는다). 구성 0줄 82건은 매입 완제품(케이스·애플워치 스트랩)·강화유리·태블릿 = 원가표에서 열이 부자재가 아니라 «계수»인 섹션 → S3 몫(인계 예고와 일치). ★**그런데 업로드 성공이 새 병목을 만들었다** — 보드 924행·레시피 100건에서 **다음에 눌러야 할 레시피를 눈으로 못 찾는 상태**가 됐다(Jino: *"「오하이 빛반사, 지문방지 매트 필름 3매」 제품을 찾는게 쉽지 않네"*). **화면이 값을 다 갖고도 사람이 거기 못 닿으면 A1은 안 열린다 — 이 화면의 합격은 계산이 아니라 «도달»이다.** ⇒ 공용 `ProductOptionPicker` 신설: 제품 검색칸 + 제품 셀렉트(건수 병기) + 2단 셀렉트(제품 선택 전엔 비활성) + 초기화. **보드 = 제품 → 옵션(SKU)** · **레시피 = 제품 → 폼팩터**(레시피엔 옵션 개념이 없다). 세 번 조작이면 목표 레시피 1건에 닿는다. 필터가 걸려도 **「N건 중 M건 표시 중 — 필터: …」**를 반드시 말한다(「미승인·미계산 SKU도 빠짐없이 싣는다」가 계약 조항이고, 지금 924건 중 924건이 미계산이라 조용한 필터는 곧 커버리지 착시다). 0건이면 빈 표 대신 사유를 그린다. 백엔드·API **무변경**(클라이언트 필터). ★**적대 리뷰 1R FAIL(P1 2건) → 2R PASS.** P1은 둘 다 **라이브 결함이 아니라 무방비**였다 — 제품 변경 시 이전 옵션 선택을 비우는 줄(`setBoardOption(null)`·`setRecipeFormFactor(null)`)을 **지워도 28건 전부 초록**. 그 줄이 없으면 필터가 「제품 B AND 제품 A의 SKU」가 되어 **행이 있는데도 「해당 조건에 맞는 SKU가 없다」**고 화면이 거짓말한다. P2 채택 4건(0건 안내 렌더 분기를 통째로 지워도 초록이던 것 — 기존 테스트가 0건 경로를 **일부러 피해갔다** / 옵션 셀렉트 「전체 (N건)」이 전체 건수로 바뀌어도 초록 / `form_factor: null` 레시피가 **픽스처에 하나도 없어** sentinel 경로 전량 미검증 / **코드 수정** — 검색어가 이미 선택된 제품을 목록 밖으로 밀면 `<select>`가 빈 것처럼 보이던 «유령 선택」을 핀 고정으로 해소). 2R 변이 6/7 KILLED(표면 절단 2종 = 0건 안내 렌더 제거 포함), M7(핀 가드 완화)은 라이브 결함 아닌 커버리지 구멍 → 이월. ★★**이 슬라이스에서만 「값은 맞는데 사람이 못 본다/못 찾는다」가 일곱 번 나왔다** — 드롭 경로 테스트 0건(리드 변이) · 「지우기」 키보드 불가(리뷰 1R) · 리드가 쓴 회귀 테스트가 `fireEvent.click`으로 결함을 덮음(리드 변이) · 옵션 셀렉트 종속성 미보호(위임 자체 발견) · 제품 변경 리셋 무방비 ×2(리뷰 1R) · 0건 안내 미검증(리뷰 1R) · 유령 선택(리뷰 1R). **전부 단위 테스트는 초록이었다.** 테스트 프론트 **735 passed**(병합 후·다른 세션 신규 55건 포함) · `tsc -b` 통과. ★배포 전 뒤처짐 **32커밋**(직전 배포 때 29커밋에 이어 연속 적중 — 프론트 통짜 rsync라 병합 없이 올리면 남의 작업이 prod에서 사라진다). 병합 충돌 0건 → 재빌드(스탬프 `0567d04b2800 dirty=0`) → `safe_deploy.sh --frontend` CAS 통과. 라이브: prod 번들 `index-4mgWPU-q.js`에 신규 문구 실재(「…명으로 찾기」·「먼저」·「해당 조건에 맞는」). **무해성**: `product_master` **963행 · SUM 3642052.1** 불변 · `cost_recipe` 100건 중 승인 **0건**(사람 몫 유지).
확인: 2026-08-23 20:5x [5432a577] — ✅**세션 종결 · 레이아웃 결함 수리·배포**(PR #375 `0ab864a2`). **진행률 2/6 불변 — 체크박스 0개.** 이번 세션이 한 일은 **A1까지 «가는 길»을 세 번 뚫은 것**이다: ①엑셀 업로드 UI(PR #366) ②제품·옵션 2단 필터(PR #374) ③필터 바가 레시피 탭에서 옆 패널을 덮던 레이아웃(PR #375). ★**세 번 다 「값은 맞는데 사람이 못 본다/못 찾는다」였다** — 계산 파이프라인은 S2에서 이미 정확했고 사람이 거기 «닿지» 못했을 뿐이다. ★그 사이 **Jino가 엑셀 2종을 실제로 올렸다**: prod `cost_recipe` **100건**·`cost_recipe_link` **924건**·`cost_material` 129종, 목표 레시피 id=68이 구성 **9줄**·SKU **107건**으로 엑셀 검산(2,137 ×1.1 = **2,350.70**)과 정확히 일치. 승인 **0/100**·부자재 단가 행 0건 = 설계대로(사람 몫). ★**같은 병이 여덟 번 나왔고 여덟 번째는 Jino 눈이 잡았다** — jsdom은 레이아웃을 계산하지 않아 「겹쳤는가」를 **원리적으로 못 잰다**(테스트 736건 전부 초록인 채로 라이브에 나갔다). 앞의 일곱은 리드 변이 2·적대 리뷰 4·위임 자체 발견 1이 잡았다. 특히 **리드가 쓴 P1 회귀 테스트도 허수였다**(`fireEvent.click`을 뒤에 붙여 결함을 덮음) — 단언의 «자리»가 결함을 덮는다. 적대 리뷰 2회 전부 **1R FAIL → 2R PASS**, 변이 SURVIVED 1종(M7 핀 가드 — 라이브 결함 아닌 커버리지 구멍, 이월). ★배포 전 뒤처짐 **29 → 32 → 1커밋** 세 번 다 적중(프론트 통짜 rsync, 교훈 #350). 무해성 `product_master` **963행 · SUM 3642052.1** 3회 배포 전후 불변. 테스트 프론트 **736 passed** · `tsc -b` 통과 · 백엔드 무변경. ⚠️**완료 QA 판정은 «미수신»이다** — Jino 지시(*"이번 handoff는 QA판정 기다리지 말고 바로 해"*, 20:58)로 판정기를 띄운 채 닫았다. **판정 없음을 달성으로 읽지 말 것.** HANDOFF `.claude/memory/HANDOFF_cost-menu-a1-surface-reachability_20260823.md` §6에 재실행 절차를 남겼다. **다음 세션 첫 일 = §2 A1 라이브 관측**(레시피 탭 필터로 3매×bar 찾기 → 승인 → 채택 → 보드에서 2,350.70).
확인: 2026-08-23 22:0x [1e34245f] — 체인 n=5 인계. **진행률 2/6 불변 — 코드 0줄·배포 0건.** 겨냥 합격항목은 **A1**(인계 §2 라이브 관측). ★**착수 시 생존 판정 오탐 2건을 갈랐다** — 훅 `[체인] ⛔ n=2 살아 있음`은 공유 메인 폴더 사본이 뒤처져 나온 것(정본엔 n=2 `end 12:5x`·n=3 `end 16:4x`)이고, **`origin/main`의 `n=4 end_kst:null`도 유령**이었다: n=4가 자기 종결을 21:01에 기입한 커밋 `27423cbf`가 `origin/feat/cost-menu-s3`에 **미병합**으로 남아 main에서 안 보였을 뿐이다(프로세스 부재·워킹트리 clean·PR #366/#374/#375 병합 완료로 교차 확인). 그 커밋을 내 브랜치에 cherry-pick해 main 경로로 올린다 — 안 옮기면 다음 세션도 같은 조사를 반복한다. ★prod 읽기 전용 실측으로 인계 숫자 전건 재현: `cost_recipe` **100** · **승인 0/100** · `cost_recipe_line` 156 · `cost_recipe_link` **924** · `cost_material` 129 · `product_master` **963행 / SUM 3642052.1**(S1·S2·n=4 기준선 불변). 목표 레시피 `id=68`은 `status=draft`·`approved_at=NULL`, 구성 9줄 전부 `unconfirmed`·단가 행 0건이며 `600×3+30+22+60+8+13+98+6+100 = 2,137` ⇒ ×1.1 = **2,350.70**으로 엑셀 검산과 일치. prod 프론트 스탬프 `9b2414c7 dirty=0`(PR #375 레이아웃 포함)이고 번들에 **「이 구성을 승인한다」·「엑셀 참고값을 단가로 채택」·「표준원가 보드」** 실재 — **길은 다 뚫려 있고 남은 것은 사람의 클릭 1스텝뿐**이다. ⚠️인계 §1의 「부자재 단가 행 **0건**」은 계정 전체로는 **2건**이라 갈라 두었다: 그 2건은 계약 B가 넣은 cleaning kit 로트(178.78 / 190.82원, `source=ledger`)이고 **레시피 68의 부자재 9종이 0건인 것이 맞다**(설계대로 — 엑셀 참고값은 `excel_ref_price`에만 있다). 승인 버튼은 계약 §2-2가 사람 몫으로 못 박은 자리라 **내가 대신 누르지 않는다**(교훈 #274) — Jino에게 4단계를 제시하고 대기 중. n=4가 못 받은 **완료 QA 3대조 판정**을 별도 Sonnet(읽기 전용)에 승계 위임했다. 작업 위치는 `origin/main`(`e65ece2d`) 기준 워크트리 `cost-menu-n5`(`feat/cost-menu-n5`) — 공유 메인 폴더 **미접촉**(그래서 `track-progress-sync.sh`는 이 줄도 못 보고 운다, 이월 · 소관: 하네스).
확인: 2026-08-23 22:3x [1e34245f] — **진행률 2/6 불변.** Jino가 라이브 화면에서 결함 1건 + 개선 4건을 발의했고, 그중 결함을 수리해 커밋 `e74b6611`(미배포). **결함**: 레시피 탭에서 제품 필터로 목록이 3건으로 좁혀져도 상세 패널이 필터 밖 레시피(「100W C타입 케이블」)를 계속 붙들고, **그 상태에서 「승인」을 누르면 화면에 안 보이는 엉뚱한 레시피가 승인된다** — A1 판정의 바로 그 버튼이다. 원인 `CostPage.tsx:1447` `selectedRecipe`가 `filteredRecipes`가 아니라 전체 `recipes`에서 조회 + 필터 변경 시 선택 재검사 코드 부재. ★**같은 병의 아홉 번째** — n=4가 「유령 선택」으로 닫은 건 셀렉트 박스였고(테스트 `P2-D`) **상세 패널은 그 수리에서 빠졌다**(테스트 736건 전건 초록이던 이유 = 이 성질을 재는 테스트가 0개). 수리 = `reconcileSelectedRecipeId()` 단일 함수 + `useEffect` 단일 호출부, `load()`는 선택을 안 건드림(진실의 원천 둘 → 하나). 폭 `max-w-6xl`(1152px) → `max-w-[96rem]`(1536px, Jino 요청). ★★**적대 리뷰 1R FAIL(P1 1건)** — 변이 13종 중 **표면 절단 3종이 SURVIVED**: 「이 구성을 승인한다」 버튼을 `<span/>`으로 지워도 · `status === "approved"`를 `!==`로 뒤집어도(미승인에 「승인 취소」가 뜨고 승인 영영 불가) · `disabled={true}`로 박아도 **742건 전부 초록**. 전수 grep 결과 **「이 구성을 승인한다」·「승인 취소」가 테스트 코드에 0회 등장** — **이 트랙 합격 판정의 바로 그 버튼이 무방비였다(열 번째)**. ★★**리드 위임문이 그 구멍을 반쯤 만들었다**: 표면 절단 변이를 요구하며 예시로 「상세 패널 통째 제거」 같은 **굵은 절단**을 들었고 구현 기가 그걸 쳐서 KILLED를 받았다 — **합격기준이 지목한 «버튼 한 개짜리 절단»은 아무도 안 쳤다.** ⇒ 일반형: **표면 절단 변이는 «가장 굵은 절단»이 아니라 «합격기준이 지목한 그 위젯»을 끊어야 한다.** P2 트리아지 4건 = 채택 3(보존 테스트가 보존을 안 잼 · 최초 진입 1프레임 깜빡임 · 0건일 때 오른쪽이 「왼쪽에서 고르라」) / 지적 아님 1(폭 — 빌드 산출물에 `.max-w-[96rem]` 실재). 리뷰어 한계 명시: **P1-1은 「표면이 무방비다」이지 「지금 깨져 있다」가 아니다**(prod 버튼 미클릭). ★**새 발견 2건(미수리, 다음 배치)**: ①`excel_ref_price`가 **화면에 원리적으로 못 닿는다** — 주석은 *"참고값으로 실려 화면에 보이기만 하고"*인데 `grep -rn "excel_ref" frontend/src/` **0건**·API 응답 0건. prod 실측 **단가 보유 1/129 vs 참고값 보유 128/129**인데 목록은 전부 「단가 없음 — 원장 연결 또는 수동 입력 필요」라 **할 일을 둘로 제시하고 가장 싼 셋째 길(「엑셀 참고값 채택」)을 안 말한다** ⇒ 화면이 사람을 더 비싼 일로 보낸다(교훈 #349 재발). ②부자재 탭 UX 3건(Jino 발의): 종 목록 독립 스크롤·종 칸 폭 확대(`260px`가 원인 — 이름 2~3줄 접힘·「미확인」 배지 세로 깨짐)·종 드롭다운(폼팩터 8칸: fold 30·flip 30·tablet 27·bar 14·trifold 10·doorlock 10·buddy 7·null 1, 백엔드 무변경으로 가능).
확인: 2026-08-23 23:1x [1e34245f] — **진행률 2/6 불변** (A1은 여전히 Jino의 클릭 대기). 적대 리뷰 1R P1-1 해소 커밋 `3ed01a7d` + Jino 발의 배치 커밋 `4a358768`(9파일 +884/−47, **미배포**). **P1-1 해소**: 승인 버튼 표면 가드 3종 신설 — 변이 M2·M11·M12·M7·M13 전부 KILLED, **프로덕션 코드 0글자 변경**(`git diff CostPage.tsx` 빈 출력). ★그 과정에서 위임받은 기가 **자기 테스트의 헛통과 2건을 스스로 잡았다**: ①`waitFor`가 재조회 «시작 전» 상태를 보고 통과 → 성공 토스트를 동기점으로 삼아 해소 ②패널이 **같은 내용으로** 리마운트돼 내용 재조회만으론 원리적으로 구별 불가 → 재조회 전후 DOM 노드 **신원 대조(`toBe`)**로 해소. **배치 A~G**: A 종 목록 독립 스크롤(`sticky`+`max-h`+`overflow-y-auto`) · B 종 칸 `260px`→`minmax(22rem,28rem)`(이름 2~3줄 접힘·「미확인」 배지 세로 깨짐 해소, **고정폭 새로 안 박음**) · C 종 드롭다운(공용 `ProductOptionPicker` 재사용, 1단 폼팩터 8칸·2단 `part`) · D 부자재 API에 `excel_ref_price` 노출 + 안내문 · E 계산 내역에 「엑셀 참고값(채택 전 · VAT 제외)」 열 · F `useLayoutEffect`(1프레임 깜빡임) · G 필터 0건 시 오른쪽 안내문. ★**C에서 사실을 숨기지 않았다** — prod 129종 중 **83종이 `part` 공백**이라 「(부품 미지정) (N)」로 건수를 라벨에 박았다(안 그러면 2단 필터가 왜 대부분 안 걸리는지 아무도 모른다). ★**D의 핵심은 「없는 버튼을 가리키지 않는다」** — 「채택」은 **레시피 단위 동작**(`POST /recipes/{id}/adopt-excel-prices`)이라 부자재 탭엔 그 버튼이 없고, 문구가 그 사실을 직접 밝힌다. ★**E의 VAT 기준을 이름에 박았다**(리드 결정, §1 승인 지점 아님 — 목표·범위 변경 아니고 되돌릴 수 있다): 이 화면 기본 표기가 VAT **포함**(D-CPP-51)이라 기준을 안 적으면 참고값이 반대로 읽히고, `adopt_excel_prices`가 이 값을 `unit_price_ex_vat`로 쓰므로 **VAT 제외가 사실**이다. ★**부수(시키지 않았으나 옳다)**: 부자재에 필터가 생기면 레시피에서 고친 결함(상세 패널이 필터 밖 항목을 붙든다)이 그대로 재발하므로, 복사본 대신 `reconcileSelectedRecipeId` → **제네릭 `reconcileSelectedId<T>`**로 올려 두 탭에 같은 규율을 적용했다. ★★**§3 금지선을 «반대 방향» 변이 2종으로 잠갔다** — ①프론트 합계에 `Σ(참고값×수량)` 더하기 ②백엔드 `_resolve_line`이 참고값으로 단가 폴백 — **둘 다 KILLED**(초록이었으면 「참고값이 합계로 새도 아무도 모른다」는 뜻이다). 변이 **11종 SURVIVED 0** · 프론트 745→**767 passed / 58 files** · `tsc -b` 통과 · 백엔드 cost_menu·import_cost **156 passed**, 전체 6397 passed/2 failed(`git stash`로 HEAD 대조 → **사전 존재**, 헬스 라우트 소관). DB 스키마 변경 없음. ★**A·B는 jsdom이 원리적으로 못 잡는다**(레이아웃·스크롤 미계산) — 가드는 「그 클래스가 살아 있나」까지가 한계임을 테스트 주석에 자백했고, **배포 후 Jino 눈 확인이 합격 조건**이다. 배치 적대 리뷰 진행 중(위젯 단위 표면 절단 8종 지정 + 직전 P1 해소 재확인, 판정문 분리).
확인: 2026-08-23 23:5x [1e34245f] — ✅**착지 완료 · prod 배포**(PR #394 병합 `4cd3de25`). **진행률 2/6 불변 — A1은 여전히 Jino의 승인 클릭 대기다.** 적대 리뷰 **1R FAIL(P1 3건) → 2R PASS(미해소 0건)** · 변이 누적 **40여 종**. **라이브 증거 7종 전부 달성**: ①무해성 `product_master` **963행 / 3,642,052.1 배포 전후 완전 동일** ②**아무도 안 눌렀다** — `cost_recipe` 100건 전부 `draft`·`approved_at NULL`·`cost_material_price` 2행, 전후 불변(★모델이 승인 버튼을 대신 누르지 않았다는 증거이고, 그래야 Jino가 누른 결과가 A1의 정직한 증거가 된다) ③`excel_ref_price`가 API에 실림(200) ④★**참고값이 합계로 안 샌다** — 레시피 68 라인별 `"600.00"·"30.00"` 실리는데 `std_cost_*`는 여전히 `null`·`computable:false`(계약 §3이 화면에서도 지켜진다) ⑤번들에 「엑셀 참고값(채택 전 · VAT 제외)」·「부품 미지정」·「필터를 해제」 전건 검출 ⑥빌드 스탬프 `4cd3de25 dirty=0` HEAD 일치 ⑦백엔드 무중단 블루-그린(다운타임 0초)·health 200·500 없음. **배포 전 뒤처짐 20커밋** 발견 → 병합 → 재빌드 → 배포(프론트 `dist` 통짜 rsync라 병합 없이 올리면 그 창의 다른 세션 작업이 prod에서 사라진다 — 교훈 #350, 4세션 연속 적중). 병합은 `safe_merge.sh --force`, CI 3건 FAILURE였으나 `steps:0`·2초 종료·로그 없음으로 **결제 정지 서명을 실측 확인**(코드 결함 아님, 자백 로그 남김). 프론트 병합 후 **778 passed / 58 files**. ⚠️**남은 것은 사람 몫 2가지**: ①A1 = 레시피 탭에서 「빛반사」→`bar`→구성 9줄·참고값 열 확인→**「이 구성을 승인한다」→「엑셀 참고값을 단가로 채택」**→보드에서 **2,350.70** ②**jsdom이 원리적으로 못 재는 4건 눈 확인**(종 목록만 스크롤되는가 · 「미확인」 배지가 세로로 안 깨지는가 · 폭 768~1000px에서 안 겹치는가 · 최초 진입 깜빡임이 없는가) — 이 트랙의 여덟 번째 결함이 정확히 그 자리였고 그때도 잡은 건 Jino의 눈이었다.
확인: 2026-08-24 00:1x [1e34245f] — **계약 A′ 개정 승인**(Jino *"승인. 커밋하고 다음 세션으로 넘겨줘"* 00:18). **진행률 2/6 불변.** Jino 지시 *"이것도 계약에 같이 넣어줘"*(00:10)로 **S4「화면 도달성 + 수입/비수입 구분」 슬라이스 + 합격기준 10~13 신설** — 순수 추가 96줄·**삭제 0줄**(§1 목표·§3 금지선·S1~S3·합격 1~9 바이트 단위 불변). ★**§0-C 정정 블록**: Jino의 「fix 될 숫자는 제품원가(CNY) 뿐」이 **수입건 한정** 발언인데 리드가 **모든 부자재에 대한 엑셀 불신으로 확대 인용**해 화면 문구 근거로 삼은 것이 오류였다 — prod 실측 **수입 부자재 1종(cleaning kits) vs 비수입 128종**이고 **비수입은 엑셀 값이 정본**이다. ★**남은 결함 2건은 하나의 뿌리**: ㉮레시피 탭 목록 독립 스크롤 미적용(`MaterialList` 호출부엔 `md:sticky md:max-h md:overflow-y-auto`가 있는데 `RecipeList` 호출부엔 없다 — **다섯 번째 「한쪽만 고침」이고 이번엔 리드 위임문이 「부자재 탭」으로 좁게 지목한 탓**) ㉯수입/비수입 미구분(원장 부자재 라인 표가 전건 무필터라 필름 종에도 `cleaning kits`가 뜨고, 128종에게 「원장 연결」이라는 **영영 안 오는 길**을 첫 번째로 안내한다). ★**합격 13이 이 개정의 안전장치**다 — ㉯를 고치며 미매칭 표면화를 통째로 없애면 「원장에 부자재 라인이 있는데 어느 종에도 안 붙었다」가 화면에서 사라져 단가 이력이 조용히 빈다. **고치는 게 아니라 자리를 옮기는 것**임을 합격기준이 붙든다. ⚠️위임받은 기가 지시 밖 판단 1건(§8 「세션 1회」 상한 — 문서 내부 모순 방지), 승인 전 Jino에게 밝혔다. **다음 세션 = S4 구현**(합격 10~13 겨냥). **A1은 여전히 Jino의 승인 클릭 대기**이고 S4는 그 경로를 막지 않는다.
확인: 2026-08-24 00:3x [f65d2a6f] — **세션 착수(체인 n=6). 진행률 2/6 불변.** 겨냥 = **계약 A′ S4 남은 몫(합격 10~13)**. ★착수 실측이 인계 주장 2건을 뒤집었다: ①훅 `⛔ 살아 있는 세션 n=2`는 **오탐** — 공유 메인 폴더가 `origin/main`보다 **200커밋** 뒤처진 사본이고 정본엔 n=2~n=5 전부 종결(n=5 end 00:28) ②인계 §2-B의 「착수 전 `f32cb611`을 push·병합하라 — 계약 개정이 아직 origin/main에 없다」는 **이미 해소됨**(`git merge-base --is-ancestor f32cb611 origin/main` → YES). n=5의 착지가 인계 문서 작성 «뒤»에 완주한 것이고, 등록부 2단계 규격(`handoff` 먼저·`end_kst` 나중)이 정확히 이 구간을 위해 있다. ★**㉮ 착수 전 구조 인구조사**(n=5 교훈 「위임문이 표면을 좁게 지목하면 그 좁은 범위만 정확히 지켜진다」의 적용): `CostPage.tsx` 전체에서 `md:sticky` 호출부 **1개뿐**(1828줄, 부자재 탭) · 목록+상세 2단 그리드 **2곳**(1789 부자재 `minmax(22rem,28rem)` / 1949 레시피 `320px`) · 탭 **3개**(materials·recipes·board) — 즉 같은 구조가 최소 2곳인데 규율은 1곳에만 있다. 좁게 안 고친다. 작업 위치 워크트리 `cost-menu-n6`(브랜치 `feat/cost-menu-n6`, base `71d341b0`) — **공유 메인 폴더 미접촉**(200커밋 뒤진 사본에 줄을 얹으면 그 폴더 pull 시 충돌한다, n=5와 같은 처분). **A1은 여전히 Jino의 승인 클릭 대기**이고 S4는 그 경로를 막지 않는다.
<!-- /TRACK-CONTRACT -->

> 생성 2026-08-22 · 원가 결정 접두사는 **D-CPP 유지**(D-CPP-30/31/34가 이미 원가 결정이라 계열을 끊지 않는다).
> 선행 문서: `docs/PLAN_cost-table-systemization.md`(D-CPP-31 초안, 2026-08-10 승인 대기 → 계약 A′로 부활·개정)
> ★**목표의 이름은 「SellC 원가 메뉴 목표」**(Jino 명명 2026-08-23) — **정본 카드는 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md`이고, 위 헤더와 갈라지면 카드가 이긴다.**
> 계약 B 정본: `docs/PLAN_import-cost-ledger.md`
> 계약 A′ 정본: `docs/PLAN_cost-menu-standard-cost.md` (D-CPP-53)

---

## 1. 목표 (사람용 서술)

원가가 지금은 **Jino의 Drive 엑셀 1장**에 산다. 그 엑셀은 한 시트 안에 공식이 5종이고, 환율이 4곳에
하드코딩(200 / 1500 / 1450 / 1400)돼 있으며, 계수(물류 10% · 관세 5.6%)가 행마다 복사돼 있다.
값은 사람 손으로 DB(`product_master.cost_price`)에 옮겨진다 — 그 경로에서 2026-08-07 태블릿 36건 오기입,
2026-08-10 71건 역전-원복이 났다.

그리고 **수입 실제 원가는 어디에도 없다.** 통관 서류(CI·PL·통관경비서)는 파일로만 존재하고,
거기 적힌 실제 환율·실제 관세·실제 운임이 원가에 반영되지 않는다.

이 트랙이 끝나면 원가는 **①구성의 계산 결과**(층1)이고 **②수입 실적으로 검증**되며(층2)
**③재고 평가를 거쳐 판매 시점 원가**(층3)가 된다.

## 2. 층 구조

| 층 | 이름 | 성격 | 계약 |
|---|---|---|---|
| 1 | 표준원가 | 계수로 «만드는» 원가(환율×물류율×관세율, 부자재 합산). 견적·기획용 | **A′** |
| 2 | 실제원가 | 수입건 서류에서 «관측된» 원가. 로트별 landed cost | **B** ← 먼저 한다 |
| 3 | 판매시점 원가 | 재고 평가(이동평균/선입선출/최근매입가)를 거친 원가. 손익 엔진이 쓰는 값 | **C** |

**순서가 B → A′ → C인 이유**: B는 기존 `cost_price` 경로를 한 줄도 안 건드리는 순수 추가라 위험이
가장 낮고, A′의 Slice 3(CNY×환율 계열)은 B가 생기면 표준원가가 견적용으로 강등되므로 지금 시스템화하면
곧 대체될 것을 굳히게 된다.

## 3. 확정 결정 (D-CPP-N)

### D-CPP-52 — 환율은 «수입 시 실제 신고환율»만 쓴다 · 엑셀 고정 환율 폐기 (2026-08-22)

> Jino 원문 (2026-08-22 21:4x KST): *"환율도 그 표를 만들때의 기준이었어. 이제는 우리가 매번
> 확인이 가능하니까 고정된 환율을 쓰지 말고 수입시의 환율이 적용된 정확한 원가를 만들자."*

**이 결정이 닫는 것**: 엑셀 원가표의 하드코딩 환율 4종(200 / 1500 / 1450 / 1400)은 **표를 만들던
시점의 기준값**이었음이 사업주 증언으로 확정됐다. ref 92 실측(14로트 **207.51~226.66**, 평균
217.61 — 엑셀 200 대비 **+8.8%**)과 증언이 **일치**한다 — 실측이 반증한 값을 사업주가 같은
방향으로 설명한 드문 자리라 근거가 두 겹이다.

**집행**: 표준원가의 환율 입력은 계약 B 원장의 **로트별 신고환율**에서만 온다(계약 A′ §2-9).
엑셀은 **구성(부자재 목록·수량)만** 파싱 대상이고 환율·단가·계수는 읽지 않는다(계약 A′ §3 금지선).
⇒ 부수로 「엑셀 행 16이 환율 `$F$8`=1500을 참조해 32,044원」이라는 이상치는 **처분 대상에서 빠진다**
— 환율 자체를 안 읽기 때문이다(계약 A′ §9-4).

### D-CPP-51 — 수입 부가세는 «우리 기준»으로 원가에 포함한다 (환급 예정이어도) (2026-08-22)

> Jino 원문 (2026-08-22 21:41 KST): *"부가세는 우리 기준에서는 비용으로 넣자. 나중에 환급을
> 받게 되긴 하지만, 수입때의 부가세는 그냥 우리 비용으로 잡자"*

**성격 — 「회계기준상 정답」이 아니라 「사업주가 택한 사내 관리회계 기준」이다.** 근거:
일반기업회계기준 제7장 **실7.11**은 "1차적으로 납부한 소비세는 … 일종의 대납금으로서
재고자산의 취득원가에 포함되는 부대비용이 **아니다**"라고 하지만 **부가가치세는 그 문단에
이름이 없다**(주세·개별소비세·담배소비세만 열거 — 부가세 적용은 유추일 뿐이다). 법인세법
시행령 **§72②1호**도 "매입가액에 … 그 밖의 부대비용을 가산한 금액"이라는 포괄 문구뿐이고
「부가가치세」·「매입세액」이라는 단어가 **없다**(국가법령정보센터 원문 대조, D-CPP-48 ③).
⇒ **공식 1차 출처가 «넣어라»라고도 «빼라»라고도 말하지 않는 공백**이고, 그 공백을 사업주가
메운 것이다. 외부 감사·세무 조정에서 다른 판단이 나올 때 「무엇을 근거로 이렇게 했나」의
답이 이 문단이다.

**산술적 결과**: 원가가 약 10% 높게 잡히고(수입 부가세 10%가 원가에 남으므로) **표시
이익률이 실제보다 보수적으로(낮게) 나온다.** 결함이 아니라 선택의 결과다 — 그래서 화면이
이 기준을 스스로 밝힌다(계약 A′ 합격 9의 배지).

**가역성**: `unit_cost_ex_vat`·`unit_cost_inc_vat` 두 값을 계속 다 저장한다(D-CPP-48 ②
계승). 결정이 바뀌면 재계산 없이 표시 스위치만 바꾼다.

**집행 지점**: 기본 표시 = 포함(계약 A′ §2-8) · 화면 자백 배지(합격 9) · `allocator.py`
docstring을 결정 근거로 갱신(후속 부채, 계약 A′ §9-2).

> 번호 51·52는 origin/main·현 브랜치 양쪽 D-CPP 최댓값이 **50**임을 수동 실측해 부여했다
> (`scripts/next_ids.sh`는 D-CPP를 발급하지 않는다 — 트랙 §5의 47→48 재번호 사고와 같은 사각).

### D-CPP-50 — 관세는 «배부»가 아니라 «귀속»이다 · 품목별 세율 (2026-08-22)

> Jino 승인 원문: 「라인별 관세율 입력」

**발단.** 두 번째 실서류(자금정산서 `SETR2607220324`, 7/22 입금건)가 착수 시점 기록을 뒤집었다.
「엑셀 관세율 5.6%가 과대(실제 5.13%)」는 **틀렸다** — 품목별 세율이 다르다. cleaning kits(부자재)
**0%**, 유리·필름 **5.6%**. 5.13%는 무관세 kits가 섞인 혼합 평균이었다.

**검증 3건**:
- ①8/18 예측 `(1−1,920/23,100)×5.6% = 5.1345%` vs 실제 **5.1344%**
- ②7/22 예측 `과세금액×(1,220/10,820)×5.6% = 15,204원` vs 실제 **15,200원**
- ③부가세 `(과세금액+관세)×10% = 242,305` vs 실제 **242,300**

**문제의 크기**: 금액 기준으로 관세를 일괄 배부하면 무관세 품목이 관세를 떠안는다. 8/18(kits 금액
비중 8.3%) → Ip17Pro 개당 **−11.96원**(원가의 0.4%) / 7/22(kits 금액 비중 **88.7%**) → 유리 개당
**−134.86원**(원가의 **5.0%**). 무관세 비중이 클수록 왜곡이 커진다.

**정한 것**: 비용을 **공통비**(운임·수수료 → basis 배부)와 **관세**(라인 `duty_rate`로 귀속)로
가른다. ★자기검산 = 라인 세율로 «계산»한 관세 총액 vs 서류의 관세 총액 — 맞으면 세율이 옳다는
증명이다. ★`Σ배부 == pool` 불변 유지. ★세율을 하나도 안 넣으면 `duty_mode="blended"`로 **종전과
완전히 동일**.

**prod 라이브(18:0x)**: 신규 id=2 → `duty_mode=by_rate` · 검산 차 **−3.73원** · Glass_Ip16 공통
2,819 + 관세 **15,200** → 개당 **2,878.34** · cleaning kits 공통 22,181 + 관세 **0** → 개당
**178.78** · 미배분 **0**. 하위호환: 기존 id=1이 `blended` 유지, Ip17Pro **2,909.96 불변**,
status=confirmed.

★**교훈**: 「우리 계수가 실제와 다르다」를 **한 건으로** 판정했고, 그 한 건의 품목 구성이 계수를
흐려 놓은 것을 못 봤다. **계수 검증에는 구성이 다른 두 건이 필요하다.**

### D-CPP-48 — 계약 B(수입건 원장) 승인 · 배부기준=금액 · 부가세 두 값 저장 (2026-08-22)

> Jino 승인 원문: 「승인 — 그대로 가자」 (2026-08-22 13:4x KST)

**정한 것 3가지.**

**① 배부기준 = 인보이스 금액.** 8/18 실건(SETR2608170216)으로 4개 기준을 전부 계산해 비교했다:

| 기준 | Ip17Pro 개당(VAT제외) | cleaning kits 개당 배부 | 킷이 흡수하는 통관비 |
|---|---:|---:|---:|
| 금액 | 2,910원 | 22.9원 | 8.3% |
| 부피 | 2,909원 | 31.6원 | 11.5% |
| 중량 | 2,901원 | 31.6원 | 11.5% |
| 수량 | 2,724원 | **163.4원** | **59.3%** |

금액·부피·중량은 **0.3% 이내로 수렴**한다. 갈라놓는 것은 수량 기준 하나뿐이고 원인은
cleaning kits 2,400개(전체 수량의 59%)다 — 개당 가치 168원짜리 부품에 배부 163원이 얹혀
배보다 배꼽이 된다. **금액 기준을 채택한다** — 관세 자체가 가액 비례로 매겨지니 성격도 맞고,
다른 합리적 기준과 숫자상 사실상 같다.

**② 부가세는 포함·제외 두 값을 계산 시점에 확정 저장하고, 기본 표시는 «포함»이다.**
왜냐하면 손익 엔진 전 축이 **D-NAO-150**(Jino 원문 *"Sellc에 들어있는 원가는 부가세 포함된 가격이야."*
2026-08-04)으로 통일돼 있고, `payable_vat()`가 매출VAT−매입세액을 순이익에서 한 번 상쇄한다.
제외를 기본으로 두면 승인된 결정과 충돌한다. **부가세 축 변경은 이 트랙의 「안 함」이다.**

**③ 「공제받는 매입세액은 취득원가에서 제외」는 법령 조문으로 확인 안 됐다 — 보류.**
국가법령정보센터 공식 API로 원문을 직접 대조한 결과:
- 부가가치세법 **제38조**(수입재화 수입세액도 공제 대상)·**제39조**(불공제 8호에 우리 경우 없음)·
  **제35조**(수입세금계산서) — 전부 조문 확인됨.
- **법인세법 시행령 제72조(자산의 취득가액)에 「부가가치세」·「매입세액」·「관세」라는 단어가 없다.**
  "매입가액 + 취득세·등록면허세 + **그 밖의 부대비용**"이라는 포괄 문구뿐이다.
⇒ 실제 근거는 일반기업회계기준(재고자산 매입원가에 "추후 환급받을 수 있는 금액은 제외")인데
그 원문은 직접 대조하지 못했다. **세무사 확인 전까지 「부가세 제외가 회계 정답」이라고
화면·문서에 확정 기재하지 않는다**(금지선). 두 값을 다 저장하므로 답이 언제 와도 수용 가능하다.

**부수 확정.** cleaning kits 2,400개(@0.8 CNY)는 **판매 상품이 아니라 우리 제품에 들어가는 부품**이다
(Jino 원문: *"우리 제품에 들어가는 부품이야"*). 따라서 계약 B는 라인을 「판매 SKU / 부자재」로 구분하고,
부자재 라인의 실제 단가를 **층1(계약 A′)의 부자재 마스터가 소비할 값**으로 보존한다 —
지금 엑셀에 「부자재(밀대외) 22원」「알콜솜 2EA 60원」처럼 수기로 박혀 있는 그 자리다.

## 4. 실측 기록 (2026-08-22 착수 세션)

**엑셀 표준원가 vs 8/18 실제 — 계수 3개가 전부 어긋나 있으나 상쇄되어 합계는 +3.4%에 그친다.**

| 계수 | 엑셀 | 8/18 실제 | 격차 |
|---|---|---|---|
| 환율(CNY) | 200 (하드코딩) | 209.88 (신고환율) | 엑셀이 **4.9% 낮음** |
| 물류비율 | 10.0% | 8.0% (B/L소계÷과세금액) | 엑셀이 과대 |
| ~~관세율~~ | ~~5.6%~~ | ~~5.13%~~ | ~~엑셀이 과대~~ → **아래 정정** |

> ### 🔁 정정 (2026-08-22 17:4x) — 「관세율 5.6%가 과대」는 **틀렸다**
> 두 번째 실서류(`SO-WSOH-114` 자금정산서 SETR2607220324)로 드러났다.
> **품목별 관세율이 다르다**: cleaning kits(부자재)는 **관세 0%**, 유리·필름은 **5.6%**.
> 내가 잰 5.13%는 «무관세 kits가 섞인 혼합 평균»이었다 — 엑셀의 5.6%가 맞다.
>
> **두 건에서 독립적으로 검증**:
> - 8/18: 예측 `(1 − 1,920/23,100) × 5.6% = 5.1345%` vs 실제 **5.1344%**
> - 7/22: 예측 `과세금액 × (1,220/10,820) × 5.6% = 15,204원` vs 실제 관세 **15,200원**(오차 4원)
> - 부가세 검산도 성립: `(과세금액 + 관세) × 10% = 242,305` vs 실제 **242,300**
>
> ★**교훈**: 「우리 계수가 실제와 다르다」를 **한 건으로** 판정했고, 그 한 건의 품목 구성이
> 계수를 흐려 놓은 것을 못 봤다. 계수 검증에는 **구성이 다른 두 건**이 필요했다.

⇒ 정정 후에도 결론은 유지된다: **환율이 실제로 움직인다.** 209.88(8/18) → **221.16**(7/22)로 한 달새
+5.4%였고, 엑셀 하드코딩 200 대비로는 **+10.6%** 벌어져 있다. 층2를 만들어야 하는 실증 근거는
관세율이 아니라 **환율**이었다.

### ★설계 결과 — 관세는 «배부 대상»이 아니라 «귀속 대상»이다 (**해소됨 → D-CPP-50**, 2026-08-22 18:0x 배포)
품목별 세율이 다르므로 관세를 금액 기준으로 일괄 배부하면 무관세 품목이 관세를 떠안는다.
실측 오차:
- 8/18(kits 금액 8.3%): kits가 20,752원을 잘못 떠안고 Ip17Pro가 개당 **−11.95원** 과소 (원가의 0.4%)
- 7/22(kits 금액 88.7%): kits가 13,486원을 떠안고 유리가 개당 **−135원** 과소 (원가의 **5.0%**)
⇒ **무관세 품목 비중이 클수록 나머지 품목 원가가 과소 계상된다.** 계약 §2-3(금액 기준 배부)은
공통비(운임·수수료)에는 그대로 유효하고, 관세만 분리해야 한다.

**엑셀 이상치 4건**(A′ 착수 전 처분 필요):
- 행 16 `오타오_투명 강화유리 (iP18_fold)`만 환율 참조가 `$F$8`(=1500)이라 상품원가가 **32,044원**
  — 같은 표 다른 행(3,100~7,200원)과 자릿수가 다르다. 오타인지 의도인지 표에서 판단 불가.
- 부가세 컬럼(H)의 0.1이 **수식에 안 들어간다** — 끝의 ×1.1이 그 역할을 한다. 컬럼이 장식으로 남음.
- 태블릿 필름 표가 **두 번**(57~71행, 86~100행) 나오고 매입 장수만 2매→1매로 다르다. 어느 쪽이 산 건지 불명.
- 행 111·112(폴드/플립 케이스)는 계산식 없이 **상수 2300·1900**이 박혀 있다.

**[미상] 2건**:
- 통관경비서 신고환율 **209.88** vs 과세금액÷INV = **210.50** — 0.3% 격차. 과세가격이 CIF 기준이라
  그럴 가능성이 있으나 서류만으론 확인 안 됨. **계약 B는 209.88(신고환율)을 쓴다**(합격기준 판정을
  결정적으로 만들기 위해 못 박음).
- cleaning kits 168원/개가 엑셀 부자재 목록의 어느 항목인지 불명(「부자재(밀대외) 22」「알콜솜 2EA 60」
  「패키지 98」 등과 금액이 안 맞는다). **A′ 착수 전 Jino 확인 필요.**
- `2025-09 ~ 2026-01` 통관비 서류가 이 라벨·발신자 메일에 없다(Drive엔 그 기간 CI/PL 30건 존재).
  다른 포워더인지 다른 라벨인지 확인 안 됨(ref 92 §4).
- 부자재 판정이 품목명 규칙(`clean`·`wipe`·`kit`)에 의존한다(ref 92 §4).

**기존 부채로 기록(이 트랙 밖)**: Meta·쿠팡 광고비가 실제로 VAT 포함 축인지 코드·문서 어디에도
검증 기록이 없다. 코드는 출처 무관 일괄 VAT포함 취급(`profit_calculator.py:956-978, 1010`).
`bep_calculator.py:384`만 공헌이익 전체를 `/1.1`로 나누는 다른 경로를 쓴다.

## 5. 진행 기록

- **2026-08-22** [a9a2121b] 트랙 신설 · 계약 B 승인(D-CPP-48) · 착수. 진행률 0/6.
- **2026-08-22 14:1x** [a9a2121b] 계약 B 구현 완료(미배포) — 커밋 전 스테이징 17파일 / +4,342줄.
  - 스키마 5테이블(계약서엔 4로 적혔으나 합격기준 ⓔ「원본 파일 다운로드」에 `import_document`가 필요).
    마이그 `imp1ledger47a`(head `m2b2devw1eight` 위) — **SQLite 왕복 + PostgreSQL DDL 렌더링** 양쪽 통과,
    Boolean 정수 리터럴 0건(교훈 #341 회피).
  - 순수 SA 2개: `allocator.py`(최대잔여법 · 4기준) · `reconciler.py`(3중 검산 · `missing≠ok`).
  - API 11라우트 · 프론트 화면 1(`/import-cost`, 메뉴 「📥 수입건 원장」).
  - 테스트 40(배부 24 + HTTP 왕복 16) 전건 통과. 전체 회귀 **6,034 passed / 2 failed**이고
    그 2건은 **내 변경을 stash하고 재실행해 사전 존재 실패로 확정**(`test_health_partial_sync`,
    `test_vendor_item_axis` — 둘 다 이 PR과 무관).
  - ★**합격기준 ⓒ 숫자 정정**: 계약 승인본 54,993 → 실제 **54,992**(661,620×1920/23100 = 54,991.79).
    구현 «전»에 발견한 기획 단계 반올림 오기이고 미달 판정 뒤의 기준 완화가 아니다(계약 §4-3 주석).
  - ★**파서 범위 강등**: `xlrd` 부재 + BIFF8 직접 구현이 250~350줄로 계약 §5 예산 규율 초과 →
    **xlsx 전용**. 실무 영향 = .xls는 Jino가 Excel에서 .xlsx로 한 번 저장해 올려야 한다.
  - ★**위임 사고 1건(무해)**: 파서 담당 서브에이전트가 지시 범위 밖에서 **Microsoft Excel을
    AppleScript로 구동**해 검증용 xlsx를 만들었다. 원본 파일은 무변경 확인(79,360 bytes·13:17).
    앞으로 위임문에 「사용자 애플리케이션 구동 금지」를 명시한다.
- **2026-08-22 14:3x~15:0x** [a9a2121b] 적대 리뷰 **1R FAIL(P1 1건) → 수정 → 변이 재주입 통과**.
  - ★**P1 = 통관경비서가 한 줄도 없는 건이 「검산 3종 전항 통과」로 확정됐다.** `check_allocation`이
    `pool − allocated == 0`만 봐서 **배부할 것이 아예 없는 상태(`0 == 0`)를 통과로 접었다**.
    나머지 두 검산은 원료 부재를 `missing`으로 잡는데 이것만 그 구분이 없었다 —
    **모듈 자신의 docstring이 선언한 규약**(「발견 0건과 실행 안 됨은 같은 숫자로 보인다」, 교훈 #123)을
    자기가 어긴 자리다. 결과: 통관비 661,620원이 통째로 빠진 단가(2,560.54원)가 「확정」으로 저장되고
    화면엔 초록 「미배분 0원」이 떠서 사람이 구분할 수 없었다.
    ⇒ 수정: `check_allocation(..., allocation_ran, has_costing_lines)` 2축 추가 → 둘 중 하나라도
    거짓이면 `missing`. `build_reconcile`이 그 두 사실을 넘기고, quantize 전 pool이 새던 자리도 함께 막음.
  - ★**변이 16종 중 5종이 살아남아 있었다** — 그게 이 리뷰의 진짜 산출물이다:
    #16 `check_allocation` 항상 ok(=P1의 근거·**40/40 전부 초록**) · #13·#14 배부기준 기본값 `amount`가
    무테스트(뒤집히면 Ip17Pro 2,910→2,724인데 아무도 안 잼) · #12 수입건 교차 문서 접근 무테스트 ·
    #5 `allocate` 기본 basis. **테스트 12개 추가 후 재주입 → 5종 전부 사망. 누적 16종 / 생존 0.**
  - P2 채택 2건: ①목록 API가 문서 BLOB을 통째로 SELECT(limit 500×20MB, prod 디스크 94%) →
    `content`를 `deferred=True`로 + SQL을 직접 재는 테스트 ②`parser.py` docstring이 **사실이 아닌
    배선**을 주장(「라우터가 예외를 잡아 빈 폼을 연다」 — 그런 라우터 코드가 없다) → 미배선임을 명시.
    나머지 P2 7건은 이월(앵커).
  - 테스트 **40 → 52** 전건 통과.
  - ★**D-CPP 번호 재부여: 47 → 48.** 병행 세션 `a1ae61e2`가 13:5x에 D-CPP-47을 「RG 매출 콘솔 net 축」에
    선점하고 **이미 커밋**(`7851cd8f`)했다. 둘 다 origin/main 밖이지만 저쪽이 커밋했고 내 것은 미커밋이라
    내 쪽을 뒤로 옮겼다(프로젝트 규칙: main이 트렁크, **본문 불변·번호와 참조만** 정정).
    ★원인은 `scripts/next_ids.sh`가 **D-NAO와 교훈만 발급하고 D-CPP는 발급하지 않는 것**이다
    (실행 결과 `D-NAO-224`/`교훈 #346`만 출력) — 도구가 막으려던 사고가 도구의 사각지대에서 재발했다.
    ⚠️마이그 파일명·revision id `imp1ledger47a`의 「47」은 **결정 번호가 아니라 불투명 리비전 id**라
    그대로 둔다(이미 체인에 물린 id를 바꾸는 것이 더 위험하다).
  - 다음: 배포 → 완료 QA 재판정. **체크박스는 라이브 관측 전엔 찍지 않는다.**
- **2026-08-22 15:3x~15:5x** [a9a2121b] **prod 배포 + 라이브 합격 확인** (Jino 「너가 해」).
  - PR **#324 병합**(`--force` — CI 3 job 전부 **steps 0·2초** = 결제 정지, 코드 신호 아님. 자백 로그 기록).
  - ★**배포가 두 번 막혔고 둘 다 가드가 잡았다.** 원인은 다른 세션(`collection-stability-s1`)이
    마이그·`models.py`를 **prod에 배포하고 PR을 안 올린** 상태였던 것 — 저장소가 경고하는
    「배포 → 나중에 PR」 관례가 만드는 상태다.
    ① `alembic upgrade head` → **Multiple head revisions**(같은 부모 `m2b2devw1eight`에서 형제 분기).
       → merge revision `mrg48s1heads` 신설(DDL 0줄).
    ② `safe_deploy` **CAS가 `models.py` clobber 차단** — 덮었으면 그쪽 `last_error_kind` 기능이
       죽었다(2026-07-17 qi 수집 사고와 같은 패턴). **가드가 제 역할을 했다.**
       → **Jino 결정**(「그쪽 브랜치를 병합해 함께 배포」)으로 그 브랜치 8커밋(2,290줄) 병합.
       병합 후 회귀 재실행 **6,098 passed**.
  - ⚠️**중간에 반쪽 상태가 한 번 났다**: prod에 `services/import_cost/` 디렉터리가 없어 scp가 멈추며
    `main.py`·라우터만 올라갔다. 그 상태로 재시작했으면 부팅 실패였다. → 디렉터리 생성 후 나머지 배포,
    **재시작 전에** `python -c "from app.main import app"`로 부팅 가능 여부를 먼저 확인하고 재시작했다.
    ★교훈 후보: `safe_deploy.sh`는 신규 **디렉터리**를 만들지 않는다.
  - prod venv에 선택 의존성 설치: `xlrd==2.0.1` · `pypdf==6.16.1`.
  - **라이브 합격 14/14**(실서류 2개를 prod에 업로드→확정): 검산 3종 ok · 미배분 0 ·
    Ip17Pro `2909.96`/`3200.96` · kits `material`/`54992.00` · 원본 다운로드 바이트 일치 ·
    **`product_master` 963행·합계·updated_at 전후 완전 동일**.
  - **완료 QA 재판정(같은 QA 기, §2의 재판정 1회)**: 계약 §4 **부분달성**(ⓔ 미달) ·
    Jino 지시 원문 **부분달성** · 트랙 궁극 목표 **부분달성 2/6**.
    ★ⓔ 미달 = 「원본 서류 **3개**」인데 prod엔 2개. CI·PL이 **한 .xls의 두 시트**라 물리 파일이
    2개인데 계약이 3개로 셌다. **고치지 않았다** — 같은 파일 재업로드는 기준 충족용 증거 생산이고,
    문언 수정은 미달 판정 뒤 기준 완화라 금지선이다(§2). 미달로 닫고 운반한다.
- **2026-08-22 17:4x~18:0x** [a9a2121b] Jino가 두 번째 실서류를 주면서 두 가지가 드러났다.
  - **파서 결함(PR #329 병합·배포)**: 같은 공급사인데 **양식마다 열이 한 칸씩 밀린다**
    (8/18 `D=품명`·`E=수량` / `SO-WSOH-114` `C=품명`·`D=수량`). 초판은 열을 하드코딩해
    **예외 없이** 품명 자리에 `100.0`(수량), 수량 자리에 `12.2`(단가)를 실었다. 계약서에
    「값이 «있는데 틀린 것»이 «없는 것»보다 나쁘다」고 써 놓고 그걸 만들었다. ⇒ 열을 **헤더
    텍스트로** 찾고, **품명 자리에 숫자가 오면 거부**하고, **CI·PL에도 자기검산**(라인 합 ==
    총계 행)을 넣었다. 통관경비서엔 있었고 CI·PL엔 없어서 조용히 통과했던 것이다. 부수:
    `POST /parse`가 `pl_file`을 따로 받는다(CI·PL이 별도 파일인 양식). ★테스트 픽스처도
    실물보다 관대했다(교훈 #292 재현) — 합성 xlsx에 `Invoice No.:` 라벨 없이 값만.
  - **관세 귀속(D-CPP-50, PR #333)**: 위 §3 D-CPP-50 요약.
  - **cleaning kit의 성격 확정**(Jino 원문): *"우리가 Cleaning Kit를 강화유리에도 넣지만
    우리사무실에서 포장하는 일반 PET, TPU로 만든 다양한 필름에도 들어가. 그래서 중국의 OTAO라는
    업체에서 만든 Cleaning Kit를 별도로 한국으로 수입해오는거야"* ⇒ **여러 완제품에 공통으로
    들어가는 부자재**다. 결과: ①계약 A′의 부자재 마스터가 이 값을 소비한다 ②**같은 부자재가
    로트마다 다른 원가로 들어온다**(환율 209.88 → 221.16, 한 달 +5.4%) → **부자재도 재고 평가
    대상**이다(계약 C의 범위가 완제품만이 아니다).
  - **환율 실측**: 8/18 **209.88** → 7/22 **221.16**(+5.4%). 엑셀 하드코딩 200 대비 **+10.6%**.
    층2가 필요한 진짜 이유는 관세율이 아니라 **환율**이었다.
  - **서류가 한 종류 더 있다**: `자금정산서(수입)` = **실제 정산**. 「통관비 예상내역서」는
    **예상**이다. ★**Jino 결정: 자금정산서(실제) 우선**. ⚠️단 정산서엔 **해상운임이 없다**
    (별도 청구) — 그것만 쓰면 운임이 원가에서 빠진다. **파서 미구현 · 다음 슬라이스 후보.**
  - **13개월치 수입 이력이 폴더에 통째로 있다**: `.../11. 거래업체/1. OTAO_China (필름)/2. IV & PL/`
    아래 `2025-09 ~ 2026-08` 월별 「입금건」 폴더(CI·PL·정산서 PDF). 소급 적재하면 층2가
    시계열이 된다.
- **2026-08-22 18:1x~20:4x** [a9a2121b] **8개월 14 수입건 전수 실측**(ref 92 `docs/references/92_import_ledger_8months_census_20260822.md` — 데이터에서 생성, 정본).
  - 세아트랜스 메일 전수(라벨 `01. Admin/Logistics`·발신 `seahtransco@gmail.com`, 30건·첨부 45개) →
    **14/14 수입건 파싱·검산·배부 성공**. Gmail은 AI_office와 같은 방식(`secrets/token.json` +
    `googleapiclient`, **읽기 전용**).
  - ★**Jino 진단이 숫자로 확인됐다**(*"사실은 fix 될 숫자는 제품원가(CYN) 뿐이고 나머지는 계속
    이렇게 업데이트가 되는게 맞거든"*): CNY는 14건 전부 동일해 고정이 맞고, 환율은
    **207.51~226.66**(엑셀 200 대비 −8.8%, 즉 엑셀이 낮음), 물류비율은 **5.0%~16.3%**로 로트마다
    3배 차이 난다.
  - ★**관세율 정정 재확인**: 품목 수준에서 **5.6%는 엑셀이 맞다**. 로트별 「과세표준 대비 관세율」이
    흔들리는 것은 무관세 부자재 비중이 로트마다 달라서다(예: SETR2607220324는 부자재가 금액의
    88.7%라 로트 관세율이 0.63%로 떨어진다).
  - ★**엑셀 표준원가(2820.64원 고정, 12.2 CNY 유리 품목)가 실제 대비 평균 과소**: 13개 로트 중
    12개에서 엑셀이 과소, 범위 2,800.10~3,291.70원, 엑셀 대비 −0.7%~+16.7%, **평균 +7.3%**.
  - **파서 양식변종 4종 수리**(PR #329 외): ①열이 한 칸씩 밀리는 문제 → 헤더 텍스트로 열 판정
    ②헤더 라벨이 어긋나는 문제 → 라벨은 힌트, 판정은 데이터(`_resolve_item_col`) ③시트명
    `CI (2)`류 접미사 → 정확일치→접두일치 순 매칭 ④CI·PL 별도 파일 → `POST /parse`가
    `pl_file`을 따로 받음. **CI·PL에도 자기검산(라인 합==총계 행) 추가** — 통관경비서엔 있었고
    CI·PL엔 없어 열 밀림이 조용히 통과했었다.
  - ★**Nora(AI 회계)가 재고 평가방법·부가세 질문에 답을 못 했다** — `POST
    https://os.ohitech.co.kr/v1/agents/accounting_sa/chat`이 「내부 기록 검색」으로 라우팅돼
    `{"intent":"search","answer":"관련 기록 없음"}`만 돌아온다. 둘 다 미결로 남는다(ref 92 §5).
  - ★★**AI_office에 이미 수입원가 트랙이 있다**(`docs/tracks/active/track_import_cost.md` ·
    `import_payable_ledger`(선적 건별, `fx_diff_krw` 포함) · `compute_landed_cost` SA ·
    `hs_reference`/`freight_quotes` · ECOUNT 기표 harness 2종). 이 트랙이 만든 SellC 수입건
    원장과 **데이터 흐름 0건**(조인·FK·API 호출 없음), AI_office 쪽은 **HTTP 미노출**(라우터
    호출 0건). **어느 쪽이 생산자가 될지는 Jino 결정 사항**(ref 92 §6).
  - **Fable 종합 판단**(ref 92 §7): 정본은 로트 실적, 표준원가는 견적 도구로 강등하되 계수는
    사람이 아니라 원장이 자동 갱신해야 한다. 계수 단독 최신값 갱신(ⓐ)도 실적 단독(ⓑ)도 불가 —
    로트→스칼라 변환 규칙(재고 평가층)이 사이에 반드시 있어야 한다. 생산자는 하나여야 한다.
    A′(D-CPP-31)의 「구성×공통 입력」 전제 중 «공통 입력»은 로트별임이 반증됐다(구성은 유효).
    제안 순서: **B → 층3-lite((a) 계열 한정) → A′(구성) → C 나머지**.
  - 진행률 2/6 불변 — 이번 실측은 조사·파서 수리이지 새 합격항목이 아니다.
