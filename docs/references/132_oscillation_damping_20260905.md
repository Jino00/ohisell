# ref 132 — 되먹임 진동 차단: 실측·설계·대칭 수 (2026-09-05, D-NAO-287)

> 계약: `docs/contracts/CONTRACT_oscillation_damping.md` (「진동차단 목표」, Jino 승인 11:5x KST)
> 선행: `ref 129`(되먹임 진동 최초 관측, 09-03) · 북극성 §7
> 체인 `pao-진동차단` n=1 · 세션 `a833394c` · 전부 prod **읽기 전용** 측정

---

## §1. ref 129가 본 것과 7일 전수가 본 것 — 같은 병, 다른 크기

ref 129 §1은 09-03 하루를 보고 *「UP 9 · DOWN 2」*, 궤적 *「1,320 → 1,980 → 1,440」*이라 적었다.
7일 전수(`naver_change_log` ⋈ `naver_proposals`, `entity_type='ad'`, `dry_run=0`)를 다시 재니
**그건 나흘짜리 톱니의 하루 조각**이었다.

**소재 `nad-a001-02-000000554755092` — 실쓰기만 (`$.adAttr.bidAmt`)**

| 날짜 | 궤적 | 방향 | 사유 | 발화 |
|---|---|---|---|---|
| 09-02~03 | 1320 → 1510 → 1730 → **1980** | UP ×3 | ROAS-UP(정착창 3.53→4.83→4.70) | 5회 |
| 09-03 | 1980 → 1690 → 1440 → **1230** | DOWN ×3 | **CPC급등** | 10~23시 **14연속** |
| 09-04 | 1230 → 1410 → **1620** | UP ×2 | ROAS-UP(정착창 3.79→3.72) | 5회 |
| 09-04 | 1620 → 1380 → 1180 → **1010** | DOWN ×3 | **순위고삐** | 12~23시 **12연속** |
| 09-05 | 1010 → 1160 → **1330** | UP ×2 | ROAS-UP(정착창 3.03→2.96) | 3회 |

★**4일 · 54회 판정 · 16회 실쓰기 → 1,320원에서 1,330원.** 순변위 **+10원(+0.8%)** = 극한주기.

### ref 129가 못 본 것 셋

1. **브레이크는 둘이다.** ref 129의 DOWN 2건은 전부 `CPC급등`이었다. 전수엔 `순위고삐`(장중loss)도
   같은 모양으로 12시간 연속 발화한다. **처방을 CPC급등에만 대면 절반이 남는다.**
2. ★**두 브레이크 다 «한 번 걸리면 자정까지 안 풀리는 래치»다** — 입력이 전부 당일 «누적»이라 단조롭다.
   - `CPC급등`: `today_cpc = Σcost/Σclk`(누적 평균). 실측 — 정착창 기준 **794.7원이 14시간 동안
     한 번도 안 움직였고** 당일 CPC는 1633~1791에서만 흔들렸다.
   - `순위고삐`: 게이트 ④ `today_cost ≥ avg_daily_cost`는 **원리적으로 단조 증가**(비용은 줄지 않는다).
     실측 — 당일소진 11,397 → 14,583 단조 · 추정ROAS 1.4741 → 1.1520 단조.
3. ★★**§7 비대칭의 정확한 좌표**: 09-04 11:20에 `순위고삐`는 추정ROAS<BEP를 **이미 알고 있었지만**
   ④소진 문턱(하루평균 6,919원) 미달이라 *"당일 소진<하루평균 — 판정 유보"*로 침묵했고,
   **같은 시각 `ROAS-UP`은 정착창 3.7176만 보고 올렸다.**
   ⇒ 오늘 나쁘다는 증거가 손에 있는데 **그걸 보는 눈이 액셀 쪽에만 없었다.**

`·->·`(before/after NULL) **27건/54건** = ref 129 §7 [미상] 「값없음 12건」의 정체 —
같은 래치 판정이 매시 재발화했으나 가드레일(쿨다운 2h·일일상한)이 실쓰기를 막은 행이다.
**진동의 실제 크기는 가드레일이 눌러 줘서 이 정도다.** 📄 어느 가드레일이었는지는 `change_log`에 사유 열이 없어 미상.

---

## §2. 고친 것 — 창 선언 한 자리 + 미러 거부권 2개

창 «길이»는 하나도 안 바꿨다(계약 §1 안 함). 바꾼 것은 **비교 대상**뿐이다.

**S1 `_HOURLY_DECISION_WINDOWS`** — 네 게이트가 어떤 창을 보고 그 창에 **우리 자신의 당일 쓰기가
섞이는지**를 한 자리에 선언한다. 브레이크 두 갈래는 `self_mixed: True`가 나란히 찍힌다 —
그게 되먹임의 좌표다. 선언만 남지 않게 테스트가 실제 소비와 대조한다(ref 131 §5 신3).

**S2 미러 거부권** — 둘 다 `hold`만 만들고 새 액션을 만들지 않는다. 한 커밋으로만 들어간다.

| | 무엇을 멈추나 | 조건 | 안 건드리는 것 |
|---|---|---|---|
| **A-veto** | 상향 | 정착창 ok인데 **오늘 추정ROAS < BEP** ∧ **당일 전환 ≥ 2** | 고삐의 소진 문턱(하향 조건 불변) |
| **B-veto** | CPC급등 하향 | 당일 CPC ≤ 정착창 CPC × 2 × **자기유발 배수** | 문턱 비율(`CPC_SPIKE_RATIO`) |

### ★★A-veto의 전환 하한 — 없었으면 이 수리가 D-NAO-85가 됐다

초판 구현은 `today_sub_bep is True`만 봤다. `intraday_roas.py`를 열어 보니 **정직 경계 ②**가
*「장중 추정 ROAS/총이익은 구조적으로 **과소추정**」*이라 적어 뒀고 `estimated_intraday_roas`엔
**자체 전환 하한이 없다**(전환 0 → 추정ROAS 0). 즉 「오늘 BEP 하회」는 **아침에 전환 0인 유닛
거의 전부**에서 참이라, 하한 없이 걸면 **매일 아침 액셀이 통째로 눌린다** — ROAS +7%·매출 −52%를
만든 그 모양(D-NAO-85)의 정확한 재현이다.

⇒ 하한은 **액셀 자신의 기준**을 쓴다: `_INTRADAY_UP_MIN_CONV`(=2)는 「오늘 숫자를 믿어도 되는가」에
UP 경로가 **이미** 정해 둔 값이다. 브레이크 기준을 액셀에 씌우는 게 아니라 액셀 기준을 액셀에
되돌려 적용한 것 — ref 129 §6의 *"브레이크에도 UP과 같은 전환 하한을 준다"*를 거울로 세웠다.
**새 매직넘버 0개.** 방어선은 `test_a_veto_does_not_choke_the_accelerator_on_zero_conversion_mornings`.

★**교훈의 일반형**: **완화 장치는 자기 입력의 «정직 경계»를 안 읽으면 반대편 극단이 된다.**
이 저장소는 그 경계를 이미 docstring에 적어 뒀고, 안 읽었으면 진동을 고치려다 매출을 반토막 냈을 것이다.

---

## §3. §7 대칭 검사 — 수 (D-NAO-234 §9-2 형식)

`scripts/measurements/oscillation_symmetry_count.py` · prod 읽기 전용 · 창 7일 · 후보 53건 ·
측정 2026-09-05 12:2x KST.

| 방향 | 종전 → 새 | 감소 | 정확도 |
|---|---|---|---|
| 액셀 UP | **27 → 22건** | −5 (−18.5%) | 🧠 **대리 지표 — 측정 아님** |
| 브레이크 DOWN | **26 → 24건** | −2 (−7.7%) | 소급 정확 |

★**둘 다 0이 아니게 줄었다 = 한쪽만 조이지 않았다.** 자격이 다르므로 병기한다.

- **B-veto(−2)는 소급 정확하다** — 필요한 입력이 전부 원장에 있다. 억제된 2건은
  09-03 CPC급등 14연속의 **머리 2건**이고, 그중 10:20 행이 실제로 1,980 → 1,690원을 **썼다**.
  ⇒ ★**−2는 궤적 효과의 «하한»이다.** 라이브였다면 그 쓰기가 없어 뒤따른 12건의 입력 자체가 달랐다.
  계수기는 행 단위 독립 재평가이지 궤적 모의가 아니다 — **작은 게 아니라 하한이다.**
- ★**A-veto(−5)는 «측정»이 아니다 — 소급 판정불능.** 필요한 입력이 그 시각의 hh24 곡선인데
  **네이버가 7일만 보존하고 우리는 저장하지 않는다.** 표의 5건은 대리 지표(「같은 날 뒤에 고삐가 난 UP」)로
  전부 09-04 02:20~11:20의 그 상향 연속이다. 필요조건도 충분조건도 아니다.
  **정확한 수는 배포 후 운영 일기(`blocked`, action='bid_up')로 센다.**

⚠️**계수기 초판 버그(고침)**: 평가 중인 행 «자신의» 하향 결과를 「판정 전 상태」로 읽는 off-by-one이 있어
09-03 10:20의 자기유발 배수를 ×1.31이 아니라 **×1.12**로 셌다. 라이브 코드엔 이 창이 없다
(`_own_bid_multiple_today`는 쓰기 «전»에 돈다) — 소급 재현에서만 생기는 오차다.

---

## §4. 안 잰 것 · 미상 (숨기지 않는다)

- **A-veto의 소급 정확한 발동 수** — hh24 곡선 미보존. 판정불능. 배포 후 일기로만 확정된다.
- **09-04 11:20의 당일 전환 건수** — 같은 이유로 미상. 그래서 회귀 테스트 ⓐ는 «그날 그대로»가 아니라
  **«같은 구조»** 픽스처다. 이 구분을 흐리지 않는다.
- **진동이 총이익에 물린 금액** — 시간×소재 마진 교차 원장이 없어 못 잰다(ref 129 §7과 같은 벽).
- `·->·` 27건이 막힌 가드레일의 내역 — `change_log`에 사유 열 없음.
- 다른 소재 2건(`…558104404`·`…558107660`, 09-01 UP 3회씩) 이후 발화가 끊긴 이유 — 범위 밖.
- **배포 후 라이브 증거 전부** — 이 문서 작성 시점엔 0건이다. 「됐다」는 아직 못 쓴다.
