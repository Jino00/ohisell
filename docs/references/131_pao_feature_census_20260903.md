# ref 131 — PAO 기능 전수 목록 (북극성 5요소·5층을 분모로) — 2026-09-03 KST

> **발주**: Jino 2026-09-03 17:21 「PAO의 기능별로 하나씩 구현을 해나가자」 → 17:25 「**PAO 북극성 정한것에 포함된 모든 기능이 여기 다 리스팅됬어?**」 → 17:27 「그럼 이걸 다시 구현해야할 기능 리스트에 붙일 수 있어?」
> 세션 `e91888d5` (체인 pao-논의 **n=83**) · 계약 「목적관철 목표」 D-NAO-280 · 결정 **D-NAO-285**
> **읽기 전용 — 코드 0줄 · prod 쓰기 0 · 네이버 광고계정 쓰기 0.**

---

## §0. 왜 이 문서가 필요했나 — Jino의 반문이 맞았다

「하나씩 구현하자」는 방향이 정해진 뒤, 나는 `ref 115 §2`의 **기능 31개 표**를 목록으로 제시했다.
Jino가 되물었다 — *"PAO 북극성 정한것에 포함된 모든 기능이 여기 다 리스팅됬어?"*

**답은 아니오다.** `ref 115`는 제목 그대로 **「missing hands」 감사**이고, **「손이 있나」를 축으로** 만든 표다.
북극성 §4가 세운 **간극 5개 중 3번(L3 집행) 하나만** 전수한 것이고, 손 문제가 아닌 것은 **원리적으로 그 표에 안 걸린다.**

| 북극성 §4 간극 | 층 | `ref 115` 31행에? |
|---|---|---|
| 간극 1 — 효율 검정이 안 돌았다 (**★현재 병목**) | L1 | ❌ 0행 |
| 간극 2 — 논문 설계 완성인데 계약 미개시 | L2 | △ 4행 |
| 간극 3 — 손은 완성, 스위치가 꺼짐 | **L3** | ✅ **31행 전부 여기** |
| 간극 4 — 사슬은 배포됐는데 「개선의 성적표」 없음 | L4 | △ 1행 |
| 간극 5 — 커머스 절반: 연결 «키» 없음 | L0~L1 | ❌ 0행 |

★**목적이 다른 표를 커버리지 근거로 쓰면 샌다.** 그리고 그 새는 방향이 하필 북극성 §2가 경고한 그 방향이다:

> **L1이 얇으면 L2는 «약한 신호 위의 정교한 제어»가 된다** — `flight_loop`이 관측기로 강등된 그 함정

즉 31행 목록으로만 「하나씩」을 하면 **L3(손)만 계속 고치고 L1(개연성)은 영영 안 건드린다.**

---

## §1. 분모 — 북극성 5요소 × 5층

`ref 82` §1 원문의 5요소, §2의 5층. **이 문서는 이 격자를 분모로 쓴다.**

| 요소 | 원문 요소명 | 대응 층 |
|---|---|---|
| ① | API 지표 ↔ 성과등급 **개연성** 확정(홀드아웃) | L1 |
| ② | **논문 기법** + 이 트랙이 세운 실행 구조 | L2 |
| ③ | 모든 광고형태를 **총이익 절대액** 최대 방향으로 | L2 |
| ④ | **자동화 운영** | L3 |
| ⑤ | 일기→반성→지혜 승격 **학습·상시개선** | L4 |

(L0 지반=수집은 ①의 전제다 — 요소 번호가 없지만 층은 있다.)

---

## §2. 세 목록의 겹침·누락

| 목록 | 축 | 행수 | 창 |
|---|---|---:|---|
| `ref 115 §2` | 손(A인구/B배선/C표면/D되돌림) | 31 | 2026-08-30 |
| `ref 65 §1-1-b` | 논문 채택 기법 | 13 | **2026-09-03**(재측정) |
| `ref 82` §2·§4 | 층·간극 | 5층 / 간극 5 | 2026-08-19 |

<!-- MEASURED 2026-09-03 17:4x KST -->
**겹침 = 2행** (`ref 115` ∩ `ref 65` 13기법): #28 페이싱 α(`flight_loop.py`) · #29 탄성 ε(`response_curve_builder.py`) 둘 다 `ref 65` **①GRM 응답곡선 컨트롤러**와 같은 대상이다.

★**초판에서 「4행」이라 적었다가 정정했다** — #19 순위서보·#23 확장압력은 `ref 65` 본문이 **언급**은 하지만(각각 5회·1회) **13기법 «행»은 아니다**. 언급과 목록 등재는 다르고, 나는 세지 않고 4라고 적었다. 이 문서가 경계하는 바로 그 오류를 이 문서가 저질렀다.

**층 사이 중복 3건**(같은 대상이 두 층 표에 등장):
- L2 `[1][6]` 데이터 하한 게이트 ≡ L4 S2-ⓑ 하한 게이트
- L2 `⑤` GAVE ≡ L4 S2-ⓒ GAVE 라우터 노출
- L3 `#22` 탐침 «실행 입구» ↔ L4 §5-3③ 탐침 «예산» — **다른 것**(입구 vs 원료). 중복 아님

**합집합 = 60행** — 산식(각 층 표의 «실제 행»을 세었다):

| 층 | 행 | 계산 |
|---|---:|---|
| L3 | **31** | `ref 115` 31 − 2(#28·#29 → L2 ①로 이관) + 2(신설 L3-신1·신2) |
| L2 | **13** | `ref 65` 13기법 (①이 #28·#29를 흡수) |
| L0 | **4** | 전부 신규(앞의 둘에 없음) |
| L1 | **5** | 전부 신규 |
| L4 | **5** | 6 − 1(하한 게이트 = L2 `[1][6]` 중복) |
| M6 | **1** | 5 − 4(③·④·`[5]`·`[0][2]`가 전부 L2 13에 이미 있음) → 「진입 조건 재측정 루틴」만 신규 |
| 메타 | **1** | 신3 계기판 갱신의 손 |
| **합** | **60** | |

★**초판에서 「52행」이라 적었다가 정정했다** — 북극성발 신규를 「7」로 어림했는데 실제로 세니 **L0 4 · L1 5 · L4 5 · M6 1 = 15**였다. 그리고 §5 신설 3 중 둘(L3-신1·신2)은 **이미 L3 31에 들어 있어 이중 계상**이었다. **세지 않고 적은 수가 이 문서에서만 두 번 나왔다**(겹침 4→2, 합집합 52→60).
<!-- /MEASURED -->
---

## §2-b. 한눈 — 52행이 층별로 어디에 있나

<!-- MEASURED 2026-09-03 -->

| 층 | 요소 | 행 | 됐다 | 부분·정지 | 없다 | 이 층의 한 줄 |
|---|---|---:|---:|---:|---:|---|
| **L0** 수집 | ①의 전제 | 4 | **2** | 0 | 2 | 북극성이 「없다」던 둘은 이미 됐고, **endpoint 91개만 17일째 불변** |
| **L1** 지식 | **①개연성** | 5 | **0** | 2 | 3 | ★**됐다 0인 유일한 층** — 그리고 북극성이 「현재 병목」이라 지목한 층. 병목은 원료가 아니라 **범위 결정** |
| **L2** 판단 | ②논문 ③총이익 | 13 | 2 | **7** | 4 | 하한 게이트 부재가 **세 곳에서 동시에 1순위** |
| **L3** 집행 | ④자동화 | 31 | **16** | 0 | 15 | `ref 115` 31행 중 **6행이 4일 만에 낡았다**(19.4%) |
| **L4** 학습 | ⑤상시개선 | 5 | **4** | 1 | 0 | ★북극성이 「없다」던 **지혜 성적표가 이미 배포·라이브**다. 단 값은 공란 |
| **M6** | ②③ 상단 | 1 | 0 | 0 | 1 | 진입 조건 **4/4 미충족** — 알고리즘이 아니라 «조건을 재는 루틴»이 먼저 |
| **메타** | — | 1 | 0 | 0 | 1 | 계기판 갱신의 손 (§5 신3) |
| **합계** | | **60** | **24** | **10** | **26** | 24+10+26=60 ✓ |

★★**세 목록을 합쳐 보고서야 보이는 것 셋**:
1. **북극성이 「없다」고 적은 것 중 최소 4개가 이미 있다** — L0 ④⑤ · L4 지혜 성적표 · L4 채점기 교정. 반대로 **31행 목록이 「있다」고 적은 것 중 6개는 그 사이 새로 생겼다.** 어느 쪽 목록도 단독으로는 현재를 못 말한다.
2. **L1이 유일하게 「됐다 0」인 층이다** — 그리고 북극성 §4가 **「현재 병목」**이라 지목한 층이다. 「하나씩」을 L3 목록으로만 하면 이 층은 계속 0으로 남는다.
3. **가장 자주 나온 병은 기능 결손이 아니라 «계기판 지체»다**(신3) — 오늘 하루 4번 관측됐다.

---

## §3. ★`ref 115`는 4일 만에 6행이 낡았다 — 이 표도 낡는다

<!-- MEASURED 2026-09-03 17:2x KST · 워크트리 feat/pao-n83 grep + prod 읽기 -->

| # | 기능 | 08-30 판정 | **09-03 실측** | 근거 좌표 |
|---|---|---|---|---|
| 1 | 엔진 점화·킬스위치 `auto_operate` | 손 **없음** — 직접 SQL UPDATE | ✅ **해소** | `frontend/src/lib/api.ts:5019` — 주석 *"이 저장소 최초의 auto_operate 쓰기 경로"* (계약 P2 H1) |
| 3 | 스코프 지정·해제 | 그룹 **1개씩만** | ✅ **해소** | `api.ts:7380` `PUT /api/naver/ad/scope/campaign` (일괄) |
| 4 | 점화 선행검사 | 표면 **없음** — 아무도 안 봄 | ✅ **해소** | `api.ts:5032` + `pages/NaverAdScope.tsx` 렌더 |
| 14 | 원장 무효화(void) | API만 · **버튼 0** | ✅ **해소** | `api.ts:5126` DELETE `executions/{id}` |
| 20 | 콜드스타트 첫 입찰 on/off | **env + 재시작** | ✅ **해소** | `guardrail_params.py:227` SPECS `naver_cs_dry_run` |
| 25 | 소재 레인 킬스위치 | **배포로만** | ✅ **해소** | `guardrail_params.py:211` SPECS `ad_bid_routing_enabled` |

**⇒ 31행 중 6행(19.4%)이 4일 만에 낡았다.** 재는 법은 §6.

---

## §4. 층별 기능 전수

### L3 집행층 — 요소 ④ 자동화 운영 (출처: `ref 115 §2`, 오늘 6행 정정 반영)

**남은 25행.** `A`=사람 입구 · `B`=배선 · `C`=표면 · `D`=되돌림. **굵게 = 없음.**

| # | 기능 | A | B | C | D | 오늘 실측 근거 |
|---|---|---|---|---|---|---|
| 12 | **검색어 제외 «실행»** | **없음** | 있음 | 화면 | **없음** | prod `negative_keyword` **pending 12건**, 표본 4건 합 **202,996원**. 90일 `exclude_search_term` 실집행 **1건**(07-22) |
| 18 | **키워드 승격** | **없음** | **executor 0** | 「자동 만료」 | n/a | prod `search_term_promote` **pending 280 · 만료 320**. 승인 **0** |
| 15 | **제외 재개방(복귀)** | **없음** | 게이트 닫힘 | **없음** | n/a | `search_term_ss_lane.py::_run_reexamination` · `_PROBATION_DAYS`=14 |
| 16 | **제외 일괄 편입** | **없음** | 있음 | n/a | n/a | `POST …/executions/import` — curl로만 |
| 22 | **탐침 실행** | **없음** | 있음 | 일부 | 자동 | `probe_revert.py::run_settlement` — 사람이 회수하는 입구 0 |
| 19 | **순위 서보** | **없음** | 있음 | **없음** | **없음** | `rank_servo.py::decide_servo_step` — 파라미터 배포로만 |
| 21 | **시장가 사다리 수집** | **없음** | 있음 | **없음** | n/a | `market_bid_probe.py::collect_daily` |
| 23 | **확장 압력 on/off** | **없음** | 있음 | **없음** | **없음** | `expansion_pressure.py::EX_PRESSURE_RATIO` — 배포로만 |
| 24 | **카나리 지정** | **없음** | 있음 | **없음** | **없음** | 이름은 분리됨(n=77 D-NAO-282)이나 화면 입구 **미확인** |
| **26** | **총이익 스코어카드** — ★목적함수 그 자체 | n/a | 크론 08:40 | **일기+Slack만** | n/a | 화면·API **0건**. `pao_scope_roster::_profit`(#27)은 **다른 산출 경로**다 |
| 30 | 대행사 되돌림 감지 | n/a | 있음 | 화면 | **대응 손 없음** | prod 30일 external 쓰기 **691건**(입찰 410·키워드삭제 198·추가 72·상태 11) vs 우리 **26건** = **3.6%** |
| 6·7 | **SPECS 밖 상수 438개** · `_PARAMS_FROM_DB` 전역 원복 | **없음** | 있음 | **없음** | **없음** | `ref 115 §4` 📄 |
| 2·5·8·9·10·11·13·17·27·31 | optimizer · SPECS 7키 · 제안 승인 · 입찰 · 예산 · 정지재개 · 제외원장 · 제외자동발견 · 총이익(스코프화면) · 지혜승격 | 있음 | 있음 | 화면 | 대체로 있음 | **손 있음 — 이 목록의 «된 것» 쪽** |

★**L3에서 오늘 새로 세운 행 2개** (어느 목록에도 없던 것 — §5):
| L3-신1 | **상향 누적 상한 도달 후 리셋** | **없음** | 있음 | **없음** | **없음** | 09-01 넣은 2소재가 **+50.8% / +49.5%**에서 멈춰 **이틀째 실집행 0**. 상한은 「사람 개입으로만 리셋」인데 개입할 입구가 없다 |
| L3-신2 | **되먹임 진동 차단** | n/a | **없음** | **없음** | n/a | 오늘 `cmp-…8425541` 소재 1개 `1,510→1,730→1,980→1,690→1,440→1,230` = **순 −18.5%**. 액셀=정착창 과거 ROAS 4.83 / 브레이크=당일 CPC |

### L2 판단층 — 요소 ②논문 · ③총이익 (출처: `ref 65 §1-1-b`, **2026-09-03 재측정**)

| # | 기법 | 상태(09-03) | 근거 |
|---|---|---|---|
| ②/[9] | DHEB 계층 EB 축소추정 | ✅ **가동중** | prod `naver_pooled_estimate_daily` **86,577행**(08-21~09-03·키워드 6,943), 크론 `30 9 * * *` |
| ⑤ | GAVE 점수 | ✅ **가동중** | prod `/proposals` rationale에 `[GAVE사전: …]` **15건** |
| [3] | 증액 트리거 | ⚠️ **가동중이나 실집행 07-30이 마지막** | 크론 오늘 15:20:31 ok · 90일 실집행 **4건**, 이후 **35일 0건** |
| [10] | 168슬롯 시간대 풀링 | ⚠️ 부분(배선 해소) | `naver_hourly_pattern_history` 168칸 전건. ★`hourly_pattern.py`는 여전히 `bidWeight` 미판독 |
| [8] | 의미 단위 회수 마이닝 | ⚠️ 부분 | `judge_semantic_units` 실호출(`judge_search_terms:275`), 소비 `ss_lane:917`. 전용 테이블 0 |
| [7] | UCB + 최소노출 강제 | ⚠️ 부분 | `ucb|upper confidence` grep **0건**. 「최소노출 강제」 해당 코드 없음 |
| **[1][6]** | **soft ROAS + 데이터 하한 게이트** | ⚠️ 부분 — ★**하한 게이트 없음** | `min_weekly`·`data_floor` grep **0건**, SPECS 9키에도 없음. 있는 건 `cost<=0 → unknown`(존재 여부 게이트) |
| [4] | 증액 롤백(한계ROAS) | ⚠️ 부분 | `marginal_roas` **17건 전부 「안 한다」 자백 주석**. 계산 0건 |
| [0][2] | 두 승수 min + SGD 슬랙 | ⚠️ 부분 | `pacing_controller.py:90` min 실재 · `slack|dual_var|lagrang` grep 0건 |
| ① | GRM 응답곡선 컨트롤러 | 배선·정지중 | `flight_loop.py` writer import **0건**. 단 `naver_forecast_model.recent_mape` non-null **183행** 축적 중 |
| ③ | EBaReT LP 쌍대 | 미구현 | grep 0건 |
| ④ | Bayesian AdComB | 미구현 | ★`scipy`·`ortools`·`pulp`가 **의존성에 아예 없다** |
| [5] | 한계ROI 균등화 + TS | 미구현(의도적 보류) | `budget_allocator.py` 머리주석 보류 자백 |

★**하한 게이트([1][6])는 세 곳이 동시에 1순위로 지목한다** — 이 표 · 계약 P4(`bid_down` 표본 하한) · M2 S2-②. 그리고 그 부재가 **오늘 라이브에서 값을 치렀다**(L3-신2, 전환 0~2건 위 ±50%).

### L0 지반 — 수집 (요소 ①의 전제 · 출처: `ref 82` §2 L0 + `ref 75` census)

<!-- MEASURED 2026-09-03 · prod 직접 SELECT + 문서 대조 -->

| 기능 | 08-19 북극성 | **09-03 실측** | 근거 좌표 | 꼬리표 |
|---|---|---|---|---|
| ④ C10 상품메타 적재 | 「없는 것」 | ✅ **가동중** — `naver_product_meta_current` **1,213행** · `_change` **956행** 누적(08-21 06:59 → 09-03 09:55) | prod 직접 쿼리 | — |
| ⑤ 커머스 75건 미개봉 문서 | 「없는 것」 | ✅ **문서 개봉 완료** — 75/75 HTTP 200, 실패 0. 기존 41 + 75 = **116/116** | `data/71_commerce_surface/75_unopened_opened_20260819.md` · D-NAO-209 | 📄 |
| ⑤-b 커머스 **축 개통** (C2·C3·C7·C8·C9·C11) | (뭉쳐 있었음) | ❌ **미착수** — 「호출가능·미적재」 6축 그대로 | `data/84_band_x_product_meta/README.md:40` | 📄 |
| **광고 endpoint 91개 미배정** | 미배정 91 (교차가능 후보 26) | ⏸ **불변 — 17일간 재측정 0회** | `data/75_api_surface_census/ADS_CENSUS_20260818.md:240` · `PLAN_naver-accel-opening.md:51` 「소관: 없음(신규 계약 필요)」 | 📄 |

★★**북극성 §2 L0 절은 «작성 당일 저녁부터» 부정확했다.** ⑤는 08-19 23:2x(D-NAO-209 — 문서가 쓰인 22:0x대의 몇 시간 뒤), ④는 다음날 08-21(D-NAO-212)에 해소됐다.
⇒ **L0의 정본은 북극성이 아니라 트랙 헤더 M1 행**이다(`track_naver-ad-optimization.md` M1 `[x]` 진도 5/5).

★**⑤는 두 행으로 갈라야 한다** — 북극성이 「커머스 75건」으로 뭉쳤는데 **완료된 건 «문서 열람»뿐**이고 «API 호출+적재 개통»은 미착수다. 뭉친 채로 두면 「완료」로 읽힌다.

★**91개는 통짜로 한 행에 넣지 않는다** — `ADS_CENSUS_20260818.md:240`이 이미 5버킷으로 나눠 뒀다(①등급교차가능 26 ②grain 불닿음 ③쓰기전용 ④정적/설정 ⑤이미포함). 버킷마다 별도 행이라야 「하나씩」의 슬라이스 크기가 된다. 통짜로 두면 「설계는 있는데 배선 없다」류 뭉뚱그림이 재발한다.

### L1 지식층 — 요소 ① 개연성 (★북극성 §4가 「현재 병목」이라 지목한 층)

<!-- MEASURED 2026-09-03 17:34 KST · 문서 3종 상호 대조 + diff 직접 확인 -->

| 기능 | 08-19 북극성 | **09-03 실측** | 근거 좌표 | 꼬리표 |
|---|---|---|---|---|
| 연관 확인도 **A+B / 36축** | 4/36 (11%) | **5/36 (14%)** — 마지막 실제 변화 **08-20**(4시드 재분할 재검, ref 83). 이후 **14일 불변** | `ref 80` §2 개정2(`80_association_census_20260819.md:47-56`) · `ref 73` 개정11 · `ref 121` §1 | — |
| **북극성 §3 표 자신** | 「4/36, 측정 08-19」 | ❌ **오늘도 4/36 그대로 — stale** | `82_pao_north_star_20260819.md:72` | — |
| 효율(ROAS/CPA/CTR) 검정 | 「안 돌았다」 | ⚠️ **좁게는 돌았다** — 08-19 밤 `ref 81`(M0)이 CRITERION grain 2축(#4 S3·#9 A1) 홀드아웃 완주(#9 C→A 승격). 이후 **추가 축 확장 0건** | `ref 80` §10(`:150-193`) | — |
| 커머스 축 A·B | 0 / 0 | ⏸ **0 / 0 그대로** — C10 08-21 승격은 「교차완료(단면 존재)」일 뿐, 단면이라 **홀드아웃 원리적 불가** | `ref 80` §2(`:53`) · `ref 84` 머리말 | — |
| 홀드아웃 검증 **가동** | 규율·실적 있음 | ⏸ **08-20 이후 신규 판정 0건** | `ref 80` §11·§11-3(재검 후 승격 후보 3건 전부 기각) | — |

★★**병목은 「원료 부재」가 아니라 「범위 결정」이다.** `ref 80` §8-1이 지목한 처방(*"새 축을 더 붓는 것이 아니라 이미 있는 홀드아웃 표에 임프레션·전환 열을 붙이는 것"*)은 그 뒤 **미실행**이고, 원료가 이미 있는 **#10(A2 요일·시간)**은 표본 문제가 아니라 **D-NAO-203 계약의 「안 함」(범위 밖 결정)**으로 눌려 있다.
⇒ L1의 첫 슬라이스는 코드가 아니라 **Jino 결정**일 수 있다.

★★★**계기판이 낡았다** — 북극성 §4 간극1 원문(*"효율 검정이 안 돌았다"*)은 **인용된 그 순간 이미 낡아 있었다**: 같은 문서가 근거로 드는 `ref 80`이 **08-19 23:4x(개정1)**에 정확히 그 검정 2건을 완주하고 A+B를 4→5로 옮겼다. 그리고 **오늘 커밋 `d95cad84`가 §3 표의 L2 행은 재측정 줄을 추가했으나 L1 행은 손대지 않았다.**
⇒ 이건 L0에서 관측한 것과 **같은 병의 세 번째 인스턴스**다(§4 L0 절 ★★ 참조). **북극성은 계기판인데, 계기판을 갱신하는 «손»이 어느 기능 목록에도 없다.**

### L4 학습층 — 요소 ⑤ 학습·상시개선 (출처: `ref 82` §2 L4·§5-3 · `ref 65` §6 S4)

<!-- MEASURED 2026-09-03 17:33 KST · prod 직접 SELECT -->

| 기능 | 08-19 북극성 | **09-03 실측** | 배선 | 발화 실적 | 꼬리표 |
|---|---|---|---|---|---|
| 지혜 승격 실적 | `[미상 — prod 미접속]` | **2건** (id=1 07-26 23:45:56 · id=2 08-25 07:42:43). 후보 51 중 promoted 2 · rejected 34 · pending 8 · hidden 7 | 판사 가동 | 승격 2건 | — |
| **지혜 성적표(M3-a)** | 「없는 것」 (§5-3① *"지혜 id로 묶는 조인 하나가 없다"*) | ✅ **배포·라이브** — `wisdom_scorecard.py` + `GET /wisdom-scorecard` + 콘솔 섹션4 (PR#335 `16ce5c97`) | 완료 | ⚠️ **has_evidence:false 2/2 · 값 전건 공란** | — |
| **채점기 교정(M3-b, D-NAO-225)** | 「입력값이 근사」 | ✅ **배포·라이브** — 판정식 GAVE배율→**총이익 델타**, `outcome_profit`·`gave_before`·`gave_after`·`bep_source` 4컬럼 (PR#323 `969e6afc`) | 완료 | `outcome_profit` **0/27** — 정착창(D+1~7) 미도래로 **정상 미채점** | — |
| 확정 지식(A/B) 판정면 주입 — **A#8 주말·공휴일** | 「없는 것」(§5-3②) | ⚠️ **부분** — 라벨은 `routers/naver_ad.py:1655`·`pao_scope_roster.py:480`에 실재. 성적표 API 응답 실재·항등식(M3-c)은 **M2-d 의존, 미착수** | 부분 | — | 📄 |
| 탐침 예산(§5-3③) | 「재가동 종속」 | ✅ **가동 중 — 재가동과 «무관»했다** · `explore` 누적 **6,817건**(08-29 이후 2,983) · `probe` 31 · 최신 발화 **오늘 17:20 KST** | 완료 | 매시간 발화 | — |
| **하한 게이트(S2-ⓑ)** | 「없는 것」 | ❌ **0건 그대로** ≡ L2 `[1][6]` 동일 대상 | 미배선 | 부재의 대가 라이브 발생 중 | — |

★★★**북극성·M3 계약이 깔아 둔 전제가 깨져 있다.** 둘 다 *"실집행 07-30 이후 0건, 재개는 M4"*를 전제로 L4·M3·M5의 경계를 그었는데, 오늘 실측은 `optimizer='ours'` **2개** · 07-31 이후 실집행 **27건**(전부 `update_bid`, 09-01~09-03)이다. M3-b가 심어 둔 채점 컬럼이 있으므로 **이 27건이 정착 후 «첫 살아있는 원료»가 될 후보**다. 아직 0/27이므로 「곧 채워질 자리」이지 「채워진 값」이 아니다.

★**새 미상 1건** — 승격 지혜 **id=2는 `param_proposal_id`가 NULL**이다. §5-3①이 말한 *"조인 하나가 없다"*와 다른 병이다: **조인할 «다리» 자체가 없는 승격 지혜가 존재한다.** 자유 텍스트 주입 경로(`wisdom_apply.active_wisdom_prefix()`) 승격 추정 🧠 — 미확인.

★**「M3 미완」은 부정확하다** — M3-a·M3-b는 **완료·배포**됐고 남은 건 M3-c(M2-d 의존)·M3-z(종결 QA) 둘뿐이다. 트랙 체크박스가 빈 것은 「손 안 댐」이 아니라 **「부분 완료·미종결」**이다.

### M6 — S4 정교 수학 (요소 ②③의 상단)

**진입 조건 4개 — 전부 미충족** (`ref 65` §6 S4 원문 기준, 09-03 판정):

| 조건 | 판정 | 근거 |
|---|---|---|
| ① 신호 안정(홀드아웃 「재현」 시작) | ❌ | `flight_loop` 여전히 관측기. `recent_mape` non-null **183/32,307** 축적 중이나 「재현 시작」 미보고 |
| ② 하한 충족 단위 확대 | ❌ | **28일 창(~09-02) 직접 SELECT**: 캠페인 **8/25(32%)** · 광고그룹 **4/408(0.98%)**. ⚠️`ref 65`의 「7/32」는 **90일 창**이라 직접 비교 금지 |
| ③ S0 원료 한 시즌 축적 | ❌ | 아이폰 출시 통상 9월 — 오늘 09-03, **시즌 한 바퀴 미도래** |
| ④ 소유권 분리 | ❌ | 북극성 §8-② Jino 결정 대기, 미결 |

**4종 구현**: ③LP 쌍대 **미구현**(grep 0) · ④AdComB **미구현**(`scipy`·`ortools`·`pulp`·`cvxpy` 의존성 **0건**) · `[5]`한계ROI+TS **미구현(의도적 보류)** · `[0][2]` **부분**(min 실재·슬랙 변수 0).

★**M6의 첫 기능은 알고리즘 4종이 아니다** — **「진입 조건 재측정 루틴」 자체**다. 지금은 세션마다 수동 grep·SELECT로 재고 있어서 주기 감사가 **「재측정 안 됨 → 미상」으로 3회 반복** 떨어졌다. 조건이 미충족인 동안엔 4종을 슬라이스로 쪼갤 단계가 아니다.



---

## §8. 진행 기록 — 기능 #1 시도 (2026-09-03, D-NAO-285)

> 이 표가 «진행판»이 되려면 시도한 것과 그 결과가 여기 쌓여야 한다. 닫힌 것만 적으면
> 「왜 안 했나」가 사라지고, 다음 세션이 같은 벽에 다시 부딪힌다.

**고른 것**: L3 #12 「검색어 제외 실행」 — Jino 지시 「1번을 자동화하자」(승인할 사람이 없는 카드들).
**슬라이스**: (b) 되돌림 정합성 → (a) 레인 배선. Jino 승인 「(b) → (a) 순서로 한 슬라이스로 가자」.

### (b) — 완료

`exclusion_grade.grade_review_audit()` + `repair_grade_review()` 신설. 등급↔`next_review_at` 어긋남을 세고 **규칙값으로만** 고친다(`default_next_review_at`이 단일 출처 — 값 발명 0).

prod 실측 어긋남 **18건**: `오컷의심` 13 · `성과미달`(NULL) 4 · `미검증`(날짜 박힘) 1. ★**초판에 「20건」이라 적었다가 정정** — 나머지 2건은 어긋남이 아니라 **내 감사의 위양성**이었다(적대 리뷰 P1-1, 아래).
원인은 결함이 아니라 **백필의 설계** — `set_grade(keep_review_at=True)`가 라벨만 붙이고 만료일을 일부러 안 건드렸다(그 판단은 옳다. 3,987행에 예정일을 한꺼번에 주면 그게 곧 재개방 «실행»의 예약이다). 문제는 **그 «일부러»가 어디까지였는지를 아무도 세고 있지 않았다**는 것.

★★**`오컷의심` 13건은 자동 수리에서 분리했다** — BEP의 **4~7배**로 돌던 검색어를 잘라 둔 것들이다(`갤럭시울트라S26지문방지` RoAS 7.829 · `오하이풀커버` 7.569 · `액정필름갤럭시S26` 5.976, BEP 1.676). 날짜를 채우면 다음 08:50 레인이 네이버 제외키워드를 **실제로 삭제**하고, 모듈이 「실행은 소유권 분리 후(계약 §1-2)」라 못 박았으며 소유권 분리는 **북극성 §8-② Jino 결정 대기**다. ⇒ `pending_decision`으로 **목록에만** 낸다.

### (a) — ⛔ 불가 판정

**대상 12건이 전부 SHOPPING이다**(prod 실측: 캠페인 `cmp-…8425541` · `campaign_type=SHOPPING` · rationale `source=shopping` 12/12). 그리고 SS3-A 실행기가 못 박아 뒀다:

> **SHOPPING 명시 거부**(§실측-0): ncc restricted-keywords API가 SHOPPING 그룹 쓰기 불가 — **HTTP 400 code 3728 실측**

⇒ 자동승인해도 **실행 단계에서 100% 실패**한다. 12건은 「승인할 사람이 없는 카드」가 아니라 **「승인해도 집행할 수 없는 카드」**다. 90일 `exclude_search_term` 실집행이 **1건**뿐이던 이유가 이것이다.

★그리고 SS3-A는 자동 발사가 **미배선이 아니라 «의도적으로 안 배선»**됐다: *"★자동 발사 없음(PLAN §3) … 자동 승인원(`ss_exclude`)은 정의만 돼 있고 어디에서도 자동 승인을 배선하지 않는다"*.

### ★적대 리뷰 FAIL(P1=1) → 2R 상환 — 내가 오늘 지적하던 그 모양을 내가 저질렀다

**P1-1**: `should`를 «오늘» 기준으로 계산했는데 `next_review_at`은 «등급이 붙던 날» 기준으로 박힌 값이다. 그래서 **규칙에 완벽히 부합하는 행**이 「어긋남」으로 떴고(`id=1`: 07-22 + 30일 = 08-21 ✓), 수리는 그걸 10-03으로 **+43일 밀었다**.

★★그 `id=1`이 하필 **이 저장소가 이미 「due 1건」으로 표면화하고 있는 행**이고 `ignition_preflight`가 그걸로 **점화 경고**(`WARN_REOPEN_DUE`)를 낸다. 수리를 돌리면 **아무것도 해결하지 않은 채 그 경고가 1건 → 0건으로 사라진다** — 이 저장소가 반복해서 자기 병으로 지목한 「모름=괜찮음」·거짓 초록과 같은 모양이다.

그리고 리뷰어가 **내 산술도 잡았다** — 헤드라인 「20건」인데 서사는 13+4+1=**18**이었고, **차이 2가 정확히 그 위양성**이었다. 「기타 2」라고 쓰고 세지 않았다. 오늘 세 번째 「세지 않고 적은 수」다.

**2R 수정**: ①값 비교를 **NULL 정합성 한 방향**으로 좁혔다(이 감사가 고칠 수 있는 병은 그것뿐이다) ②**도래한 행(`next_review_at <= today`)은 세되 고치지 않는다** — `due_untouched` 버킷 신설 ③P2-1 `max(int(row.cycle or 1),1)` 중복 제거(**M3에서 지운 것과 같은 모양을 새로 넣고 있었다**) ④P2-5 수리 «후» 상태 반환.

**2R 판정: PASS(미해소 P1=0)** — 리뷰어가 prod에서 **독립 재현**했고(`ok=3971 repairable=5 pending=13 due_untouched=1`, `id 1·2 in repairable? False`), 우회 사냥(5등급 × 도래 + 「정확히 오늘」 **7행**)에서 **수리로 바뀐 행 0**을 확인했다. 안전 축 절단 변이는 1R 2건 → **3건**으로 강화됐다.

★**넘긴 트레이드오프 판정**: NULL 한 방향 비교의 손실(값이 틀린 날짜를 `ok`로 셈)은 **받아들일 만하다** — ⓐ새 규칙은 **원리적으로 기존 날짜를 옮길 수 없다**(NULL 채우기·날짜 지우기 두 방향뿐, 도래 행은 아예 못 들어옴) ⓑ`why` 문구가 탐지 가능한 두 병만 말해 「전부 센다」를 참칭하지 않는다 ⓒprod 손실 사례 0건.

**검증**(prod 읽기 전용, 2026-09-03 21:0x): `repairable` **7 → 5**(진짜 병만) · `due_untouched` **1**(점화 경고 대상, 보호됨) · `ok` 3,970 → **3,971**. 재주입 변이 6종 **전건 사망**. 테스트 40 → **45**.


### ↗️ 이 슬라이스가 남긴 이월 (2R 리뷰어 발견 — 라운드를 늘리지 않고 적는다)

- **R7·R8·R10 변이 생존** — 셋 다 **커버리지 구멍이지 결함이 아니다**(실동작은 실측으로 옳음). ①도래 가드의 `continue`는 **무관·미검증** 도래 행에만 하중을 지는데 그 테스트가 없다(성과미달은 NULL 정합성이 이미 막아 이중 보호) ②「정확히 오늘 도래」 경계 무테스트 ③`cycle>1`인 repairable 행 테스트 없음 — **P2-1 수정은 테스트가 아니라 독해로만 검증된 상태다.** prod는 전건 `cycle=1`이라 도달 불가.
- ★**앵커 발명이 수리 «값»에는 남아 있다** — NULL이던 성과미달 4건은 `today+30 = 2026-10-03`으로 채워진다. 등급일 기준이면 09-16(이미 과거)이다. 방향은 안전하나(NULL → 날짜 = 더 열림) 같은 발명이 값에 남았다.
- ★★**값 검사 부분 회복안**(리뷰어 제안, 채택 권고): 앵커 없이도 증명 가능한 상한이 있다 — 정당한 `next_review_at`은 어떤 앵커(≤ 오늘)에서도 `today + REVIEW_BACKOFF_MAX(90)`을 넘을 수 없다. ⇒ `actual > today+90`은 **앵커와 무관하게 틀렸다.** 질문②의 손실 중 이 부분은 앵커를 발명하지 않고 되찾을 수 있다(실측 `+200일`·`+3650일`이 이 그물에 걸린다).
- **도래 가드가 HOLD 분기 «앞»**이라, 오컷의심 행의 날짜가 이미 도래하면 `pending_decision`이 아니라 `due_untouched`로 빠진다. 수리는 여전히 안 하므로 안전하나, **「가장 급한 오컷의심」이 `pending_decision` 계수에서 빠진다**(prod 13건 전부 NULL이라 오늘 사례 0).
- **전수 스캔 2회** — P2-5 수정으로 비-dry_run 경로가 감사를 두 번 돈다(3,990행 × 2). 정확성 문제 아님, 비용만.
- **P2-4**(어휘 밖 등급이 감사 전체를 죽인다)·**P2-6**(호출부 0 = 사람이 보는 표면 없음).


### ⇒ 표의 정정

| 행 | 종전 | 정정 |
|---|---|---|
| L3 **#12** 검색어 제외 «실행» | 「사람 입구 0」 | **「SHOPPING은 API가 원리적으로 거부 — 자동화 대상 아님」**. WEB_SITE 경로만 유효하고 그건 콘솔 Confirm 전용(설계) |

### 남긴 갈래 (Jino 결정)

1. 못 낼 카드를 **안 만든다** — 쇼핑 검색어에 `negative_keyword` 후보 생성 중지 (지금은 매일 만들어 만료 = 소음)
2. **사람 목록으로** 돌린다 — 「API로는 못 자르니 콘솔에서」 표면화
3. 쇼핑 제외의 **다른 경로 조사** — 존재 여부 자체가 **[미상]**

### 이 슬라이스가 남긴 교훈

변이 9종 중 **2종이 생존**했고 둘 다 상환됐다.
- **M3** — 「의도된 NULL」 특례 분기가 **아무 행위도 안 하는 중복**이었다(일반 경로의 `default_next_review_at`이 같은 답을 낸다). ⇒ 분기·상수 삭제. **가드라고 부른 것이 가드가 아니었다.**
- **M8** — `db.commit()`을 지워도 초록. 같은 세션이 트랜잭션 안의 값을 읽어서다. ⇒ `rollback()` 후 읽는 테스트로 잠갔다(교훈 「픽스처의 «모양»이 무엇을 재는지 정한다」의 재현).

---

## §5. 어느 목록에도 없던 것

| 신설 | 층 | 왜 어느 목록에도 없었나 |
|---|---|---|
| L3-신1 상한 리셋 입구 | L3 | `ref 115`는 «기능이 있나»를 물었고, 이건 **안전장치가 작동한 뒤의 복귀 경로**라 기능 목록의 축에 안 걸렸다 |
| L3-신2 되먹임 진동 차단 | L2↔L3 | 액셀과 브레이크가 **각각은 정상**이라 어느 단일 기능 행에도 안 걸린다. 병은 **둘 사이의 창 불일치**다 |
| **신3 — 계기판(북극성) 갱신의 손** | 메타 | `ref 115`(손 축)에도 `ref 65`(기법 축)에도 안 걸린다. **북극성은 계기판인데 그것을 갱신하는 손이 어느 목록에도 없다** — 오늘 **4번 관측**: ①L0 절이 «작성 당일 저녁»부터 부정확 ②§3 표 L1 행이 아직 4/36(실제 5/36) ③§4 간극1이 인용 시점에 이미 낡음 ④M6 진입조건이 감사에서 3회 「재측정 안 됨」. **오늘 커밋 `d95cad84`는 같은 표의 L2 행만 고치고 L1 행은 지나갔다** |

---

## §6. 이 표를 다시 재는 법 (§3이 증명한 대로 이 표도 낡는다)

| 무엇 | 명령 |
|---|---|
| L3 손 6축 해소 여부 | `grep -n "scope/campaign\|auto-operate\|executions/" frontend/src/lib/api.ts` |
| SPECS 런타임 키 | `grep -n "ParamSpec(" backend/app/services/naver_ad/guardrail_params.py` |
| 실집행 실적 | prod `naver_change_log` `dry_run=0 ∧ action NOT LIKE 'external%'` 를 action별 집계 |
| 대기 카드 | prod `naver_proposals` 를 `proposal_type × status` 집계 |
| 논문 13기법 | `ref 65 §1-1-b` 재측정 절차 |

**prod 조회**: `ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python - " < q.py` (첫 줄 `load_dotenv("/home/ubuntu/ohisell/backend/.env")`)

---

## §7. 확인 못 한 것

- **L3 「됐다」 10행**(#2·5·8·9·10·11·13·17·27·31)은 `ref 115` 08-30 판정을 **재확인 없이 옮겼다** 📄 — 같은 표의 6행이 4일 만에 낡았으므로 **이 10행도 낡았을 수 있다.** §2-b의 「됐다 23」은 그만큼 부풀 수 있는 수다.
- A#8 라벨의 **성적표 API 응답 실재** — 좌표만 확인, `curl` 미실시 📄.
- 승격 지혜 **id=2의 `param_proposal_id`가 왜 NULL인지** — 자유 텍스트 주입 경로 추정 🧠, 미확인.
- **M2-d(S2) 5항목 중 정확히 몇 개가 남았는지** — 09-03 확인줄도 「항목별 대조는 미완」이라 자백.
- AUDIT_PROTOCOL 「발견 주체」 카운트 — 2026-08-10 기준선(사람1:시스템0)에서 갱신 흔적 없음(M5 합격기준의 절반).
- 「등급 교차 가능 후보 26개」가 지금도 26인지 — ref 75 08-18 census 이후 재분류 0회 📄.
- #24 카나리 지정의 «화면 입구» 유무 — 이름 분리(n=77)까지만 확인, 프론트 입구 미확인.
- L3 표의 「있음」 10행(2·5·8·9·10·11·13·17·27·31)은 `ref 115` 08-30 판정을 **재확인 없이 옮겼다** 📄 — 6행이 낡은 표에서 왔으므로 이 10행도 낡았을 수 있다.
- 오늘 −18.5% 진동이 **총이익에 얼마를 뺐는지** 안 쟀다(입찰 궤적만 관측).
- 이 문서는 **순서를 정하지 않는다** — 목록과 근거까지가 이 판의 범위이고, 채택은 Jino 몫이다.

---

## §9. ★B「됐는데 안 돌아간다」 11행 재측정 — 2026-09-05 00:4x KST

<!-- MEASURED 2026-09-05 00:4x KST · prod 읽기 전용 · 세션 deb55836 (체인 pao-논의 n=86) -->

> 발단: Jino 질문 *「이 B가 지금 진행중인거 맞지? 얼마나 진행됬어?」*(2026-09-05 00:41).
> ★그 B 표(A 26 / B 11 / C…)는 n=83/84 대화 화면에만 있었고 저장소·아티팩트 어디에도 없었다
> (`HANDOFF_the-abc-was-never-written-down_20260904.md`가 원문을 보존한 유일한 사본이다).
> **여기에 실측을 붙여 두지 않으면 같은 일이 두 번째로 일어난다.**

**답: B 11행 중 n=85가 닫은 것은 0개이고, 두 행은 오히려 악화됐다.**

| # | 기능 | B 표(09-03) | **09-05 실측** | 변화 |
|---|---|---|---|---|
| 1 | 예산 증액·원복 | 07-30 이후 0건 | 실집행 **0**(제안 전부 07-29 이전) | 불변 |
| 2 | 정지·재개 | 07-30 이후 0건 | 07-30 이후 제안 **0** · 마지막 `set_user_lock` 08-10 | 불변 |
| 3 | 검색어 제외 실행 | 90일 1건 | 90일 **1건**(07-22) · pending **12** | 불변 |
| 4 | 키워드 승격 | 승인 0 · 만료 **320** | 승인 **0** · 만료 **495** · pending **280** | ⚠️**악화** (+175) |
| 5 | 탐색 증액 | 승인 30건 미연결 | pending **637** · 30일 실집행 **0** | ⚠️**악화** (09-04 628 → 09-05 637) |
| 6 | 증액 트리거 [3] | 35일 0건 | `trigger_pacing` 승인 **0** · `budget_up` 승인 **0** | 불변 |
| 7 | 지혜 성적표 | 값 전건 공란 | 승격 지혜 2건 · **성적 컬럼 자체가 0개** | 불변 |
| 8 | 채점기 교정 | `outcome_profit` 0/27 | **0 / 1,253**(90일) | 불변(분모만 커짐) |
| 9 | 대행사 되돌림 감지 | 691건 감지 · 대응 0 | 30일 **689건**(입찰 410·키워드삭제 198·추가 72·상태 9) · 대응 손 0 | 불변 |
| 10 | 킬스위치·void·스위치 3종 | 미관측 | 킬스위치 **여전히 미관측**(`auto_operate`는 `change_log`를 안 남긴다 — 감사는 `naver_campaign_settings.memo`에만) · `adgroup_scope_change` 1 · `update_guardrail_params` 2 | 일부만 |
| 11 | 점화 선행검사 | 미관측 | 일기 흔적 **0건** | 불변 |

### ★★B 표의 「왜」 열이 틀렸다 (n=85 실측, D-NAO-286)

1번을 자동화하러 갔다가 전제가 반증됐다. 3·4·5행의 원인은 **「승인 주체가 없음」이 아니다**:

- **3행 제외** — SHOPPING API가 **원리적으로 거부**(HTTP 400 code 3728). 「승인할 사람이 없는 카드」가 아니라 **「승인해도 못 쏘는 카드」**(§8 참조)
- **4행 승격** — executor가 **의도적으로 없다**(`search_term_ss_lane` 원문: *"proposal_type=search_term_promote는 `_ACTION_BY_PROPOSAL_TYPE`에 절대 등록하지 않는다"*). 900건 전부 `decided_at` NULL = 한 번도 승인 절차를 탄 적이 없다
- **5행 탐색** — 637건 **전건이 스코프 밖**. 승인원 `explore_op`은 살아 있고 스코프 «안»에서는 지금도 돈다. 막는 것은 `engine_approve` → `_scope_hold_reason` → `blocked_by_scope`이고 그 스코프는 **Jino가 08-28·09-01에 일부러 좁힌 카나리**다

### ★실질 병목은 11개가 아니라 한 개다

- **5행(탐색 637)** → 스코프 확대 = **Jino 결정 하나**. 손은 이미 완비(`PUT /scope/adgroup` 단건 · `PUT /scope/campaign` 일괄 · 화면·라우트·탭 링크). 가장 큰 덩어리이고 가장 싸다
- **7·8행(성적표·채점기)** → 원료가 «실집행»이라 **5행이 풀리면 저절로 풀린다.** 따로 만들 것이 없다
- **4행(승격 775)** → executor 신설 = **새 계약**
- **3행** → 원리적 불가. 닫을 항목이 아니라 **성격을 바꿔 적어야 할 항목**
- **9행(대행사 689)** → 별개 작업(대응 손 없음)

⇒ **B 11개 중 5·7·8 세 개가 Jino의 스코프 결정 하나에 매달려 있다.**

### n=85가 만든 것은 이 목록에 없다

표본 하한 게이트(D-NAO-286)는 B를 닫는 항목이 아니라 **B를 닫으려면 먼저 있어야 하는 것**이었다 —
2026-09-01에 Jino가 `auto_operate`를 끈 이유가 정확히 그 부재였기 때문이다(*"이틀·14클릭 표본으로 +49%"*).
머지 완료(PR #711 `ab777c03`)이나 **prod 미배포**다.

---

## §10. C「안 됐다」 델타 + 「26갈래 병렬로 쪼갤까」에 대한 실측 (2026-09-05 09:5x KST, n=87)

<!-- MEASURED 2026-09-05 09:5x KST · prod·저장소 읽기 전용 · 세션 deb55836 -->

> 발단: Jino *「PAO에 맡기기 전에 우리가 안 되어있는 작업이 많았잖아」* → *「순차적으로 하면
> 시간이 오래 걸리니 하나하나 세션으로 쪼개서 한번에 진행할까?」*
> ★그 지적이 맞다 — 직전 세션(n=86)이 「실질 병목은 스코프 결정 하나」라고 적었는데
> **그건 B 11행만 본 프레임이다.** 전체는 46행이고 **C 26개가 안 돼 있다.**

### 오늘의 델타 — C 26 → 25

**「L2 판단 미구현 4」의 «하한 게이트»가 배포됐다**(D-NAO-286, 2026-09-05 09:12 KST).
SPECS도 **9 → 11키**(438개 상수 중 편입분). 나머지 C 항목은 실측상 불변:

| C 묶음 | 실측 |
|---|---|
| 신설 ① 상한 리셋 입구 | `auto_up_base`·리셋 API/화면 **grep 0건** — 없다 |
| 신설 ② 되먹임 진동 차단 | 전용 배선 **0건**(관련 언급 3건은 주석) |
| 신설 ③ 계기판 갱신의 손 | **0건** |
| 카나리 지정 화면 입구 | `api.ts` **0건** |
| 순위 서보 파라미터 입구 | `api.ts` **0건** |
| 확장 압력 on/off 입구 | 라우터·`api.ts` **0건** |

⚠️ 이 중 **신설 ①②는 n=83이 이미 라이브로 값을 치른 것**이다(2소재가 +50%에서 이틀째 정지 ·
소재 1개 순 −18.5%). **스코프를 넓히면 이 부재가 그룹 수만큼 곱해진다.**

### ★★「26갈래 병렬」에 반대하는 실측 넷

1. **처리량은 병목이 아니다** — 최근 14일 머지 PR **200건+**(gh 조회 상한). 같은 기간 트랙
   진행률 **2/7 그대로**. 이미 아주 많이 병렬로 돌고 있고 그게 마일스톤을 못 옮겼다.
2. **C의 「손이 아예 없다 13」은 같은 두 파일에 몰린다** — 카나리·순위서보·확장압력·상한리셋
   입구가 전부 `frontend/src/lib/api.ts`(**7,917줄**)와 `backend/app/routers/naver_ad.py`
   (**3,105줄**)에 입구를 다는 일이다. 동시에 열면 충돌·clobber가 확정적이다.
3. **이 저장소는 병행 세션 사고로 도구를 4개 만들었다** — `safe_deploy` CAS(07-17 배포
   덮어쓰기) · 프론트 dist stamp(08-06 clobber 2회) · `next_ids`(번호 충돌 3회) ·
   pre-commit 훅(08-07 남의 HANDOFF가 내 커밋에). 전부 「빨리 하려고 병렬로」의 결과다.
4. **오늘이 이유를 하나 더 보탰다** — 하한 게이트 하나를 만들려니 그 아래에서 **소재 grain
   결함**(`_settlement_agg`가 ad를 못 읽음)이 나왔다. 목록을 병렬로 밀면 그런 발견이 26갈래로
   흩어지고 서로 모순되는 판단이 동시에 커밋된다. B 11행 중 3행의 「왜」가 틀렸던 것도 같은 종류다.

### 대안 — 충돌 축으로 4덩어리

| 덩어리 | 내용 | 병렬 |
|---|---|---|
| **A 화면·입구** | 카나리 지정·순위 서보·확장 압력·상한 리셋 입구·SPECS 편입 | ⛔ **한 세션 독점**(`api.ts`+`naver_ad.py` 충돌 · `pao-uiux` 트랙과 소관 겹침) |
| **B 판정 로직** | 되먹임 진동 차단·한계ROI·하한 잔여 | ⛔ 한 세션(`auto_operator` 4,193줄·`guardrail_*`) |
| **C 조사·측정** | L1 지식 3 · L0 수집 2 · M6 진입조건 · 계기판 갱신 | ✅ **진짜 병렬 가능** — 코드 0줄·파일 안 겹침 |
| **D L2 수학** | LP 쌍대 · AdComB(의존성조차 없음) | ⏸ 북극성 M6 — 지금 열 게 아니다 |

★**C(조사)를 먼저 돌리면 A·B의 순서가 정해진다.** 오늘이 그 증거다 — 재보니 목록의 「왜」가
틀렸고 만들 것이 바뀌었다. 순서 없이 26개를 밀면 그 정정이 안 일어난다.

**⚠️ 이 절은 제안이지 결정이 아니다** — 채택은 Jino.

---

## §11. M6 진입조건 재측정 + 계기판 정합 감사 — 2026-09-05 (C3 조사)

<!-- MEASURED 2026-09-05 KST · prod 읽기 전용 · 세션 deb55836(체인 pao-계기판) -->

§224의 M6 진입조건 4개를 09-05 기준으로 다시 쟀다 — **판정 4/4 미충족 불변**(신호 안정 · 하한 확대 · S0 시즌 축적 · 소유권 분리 모두 그대로). 넷을 「4/4」로 뭉치지 않고 각각 다른 이유로 비어 있다는 것, 그리고 북극성 §3·§6·부록A · `ref 65` §1-1-b · `ref 115`를 교차한 계기판 어긋남 9건(개선 여지 4건·정합 확인 3건·자연 drift 1건·자기모순 아님 1건)은 **`ref 121` §7이 정본**이다(같은 표를 여기 또 복제하면 이 문서 §0·§3이 지적한 「계기판 지체」를 이 절 자신이 반복하게 된다 — 그래서 복제하지 않는다).

한 줄 결론: **M6은 아직 안 열렸고, 그 이유 4가지 중 2개(신호 안정·하한 확대)는 공학 작업이 이미 진행 중, 1개(S0 시즌)는 달력, 1개(소유권 분리)는 Jino 결정 하나로 즉시 해소된다.**
