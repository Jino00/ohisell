# ref 54 — 원가 정본 대조와 링크 감사 종결 (2026-08-10)

> 트랙: 쿠팡 손익 정합 · **D-CPP-30** · 선행 D-CPP-27(ref 52 링크 감사)·D-CPP-25(값/링크 층 분리)
> 산출물: `scripts/audit_cost_buffer.py`(읽기 전용 검사기) · `backend/tests/test_cost_buffer_audit.py`(19건) ·
> `backend/app/data/cost_truth_20260807.json`(정본 스냅샷 69항목) · 이 문서
> ⚠️ 이 세션은 **prod 데이터를 바꿨다**(코드 아님). 변경 목록과 되돌리는 법은 §6에 있다.

## 0. 한 줄 결론

원가 오류가 **두 층**에 있었다. **링크 층**(어느 SKU를 가리키나)은 D-CPP-27이 잡았고,
**값 층**(그 SKU의 원가가 정본인가)에서 **177건이 버퍼가 얹힌 채** 남아 이익이 과소 계상되고
있었다 — 전 채널 90일 **1,012,405원**(손익 엔진 축). 둘 다 해소했고, **값 층은 검사기로 재발을 잡는다.**

## 1. 원가의 정본은 엑셀이고, 그건 목록이 아니라 **계산기**다

`MD_원가 계산_Jino_260807.xlsx` 「제품 원가표」 (Google Drive `Ohi/21. MD/소싱/`,
sha256[:16] `7ed336b4c55ea71b`, 2026-08-07 18:33).

```
지문방지필름 TPU 3매 = 2,350.7원
  = 필름 600 × 3매 + 부착안내문 30 + 부자재 22 + 알콜솜 60
    + 비닐 8 + 비닐 13 + 패키지 98 + 폼텍스티커 6 + 부착지그 100
```

**원가는 입력값이 아니라 부자재 구성의 결과다.** 계열마다 구성이 다르다 — 일반 폰(부자재 9종),
플립/폴드(내부·외부·후면·힌지 각각), 태블릿(하드보드지·스퀴즈 추가),
케이스·거치대(CNY 단가 × 환율 + 물류비 + 관세 + 부가세).

★그래서 **숫자만 옮기면 안 된다.** 필름 단가가 오르면 전 제품이 같이 움직여야 하는데,
지금은 사람이 엑셀에서 다시 계산해 DB에 넣는다. 시스템화는 D-CPP-31로 이월(§7).

★★**눈으로 옮기지 말고 파싱한다.** 2026-08-07에 눈으로 옮기다 태블릿 36건을 틀렸고,
파싱 판정기가 그걸 잡았다. 이번에도 파싱만 했다(`openpyxl`).

## 2. 값 층 — 버퍼 177건

`product_master.cost_price`가 **정본 + 일정한 차이**였다. 08-07 인계본이 이미 이름을 붙여 뒀다:
*"원가 매핑 마스터의 값은 **버퍼가 얹힌 값**이었다 — 폰 계열이 정확히 +265.3."*

| 버퍼 | 건수 | 예 |
|---|---:|---|
| 폰 +265.3 | 141 | 2,616 → **2,350.7** (지문방지필름 TPU 3매) |
| 도어락·플립 +232.6 | 18 | 3,713 → **3,480.4** (플립 내부3+외부3) |
| 폴드 +96.4 | 18 | 6,186 → **6,089.6** (폴드 내부3+외부3) |

**대조 결과(949행)**: 정본 일치 313 · **버퍼 177** · 판정 불가 459.
「313」이 08-07 인계본의 «제품 원가표 기준 갱신 313건»과 정확히 일치한다 — 파싱이 맞다는 증거.

**판정 불가 459건**은 케이스·거치대·셀카봉·강화유리(오타오) 등이다. 08-07 결정
(«판정 불가 계열 → 마스터 값 유지»)대로 두었다.
★**«판정 불가»를 «정상»으로 세지 않는다** — 셋을 따로 낸다.

⚠️ **459 전부가 «원가표에 답이 없는 것»은 아니다**(적대 리뷰 P2-1). 이 스냅샷은 원가표의
**D열(「제품원가 (+VAT)」)만** 담았는데, 케이스·오타오 섹션은 최종 원가가 **I열(「상품원가」)**
이나 C·E열에 있다. 그래서 **최소 75건**(반올림 허용 시 109건)은 원가표에 값이 있는데도
「모름」으로 세어진다 — 2,222(39건)·4,202(21)·4,532(11)·5,082·2,300·5,240(2)이 I/C열에
정확히 있다. **검사기가 못 보는 범위**이지 «원가표에 없는 것»이 아니다.
→ 섹션별로 원가 열을 고르도록 파서를 넓히는 것은 별건(§7-5).

### 손익 영향 (버퍼 제거 = 원가 하락 = **이익 회복**)

| 축 | 창 | 원가 과대였던 금액 |
|---|---|---:|
| 네이버 스마트스토어 | 90일 | **+941,659** |
| 자사몰(cafe24) | 90일 | +61,372 |
| 쿠팡(오하이테크·오픽스) | 90일 | +9,374 |
| **주문 축 계** | 90일 | **+1,012,405** |
| 로켓1P (별도 축) | 07-10~08-08 | +186,279 |

★★**취소·반품·입금전 주문은 빼고 센 값이다**(적대 리뷰 P1-1이 잡았다). 손익 엔진은
`REVENUE_EXCLUDED = {cancelled, returned, pending}`를 통째로 건너뛰므로
(`profit_calculator.py:257`), 그 주문들의 원가가 과대였던 것은 **이익에 반영된 적이 없다.**
상태를 안 가르고 세면 **1,059,518원**이 나오는데 그건 «장부에 실린 회복액»이 아니다 —
처음에 그렇게 적었다가 고쳤다(4.4% 과대였다). 90일 창 해당 SKU의 상태 분포:
delivered 3,991 · active 256 · **cancelled 167** · shipped 105 · **returned 33** ·
exchanged 17 · confirmed 14 · **pending 1**.
★교훈: **금액을 인용할 땐 그 금액을 «누가 세는지»를 같이 본다.** 나는 `orders`를 직접
집계했고 엔진은 상태로 거른다 — 같은 테이블을 봐도 다른 숫자가 나온다.

★**1P는 링크 오류로 «과대», 값 버퍼로 «과소»였다 — 방향이 반대인 두 오류가 겹쳐 있었다.**

## 3. 링크 층 — D-CPP-27 4건 처분

| 안건 | 결과 | 손익(창 07-10~08-08) |
|---|---|---|
| ① `replace` 4건 | **교체** — 「카메라 렌즈 1매」↔「액정 강화유리 2매」 3건, 「갤럭시S8플러스(폰)」↔「갤럭시탭S8플러스」 1건 | 원가 +69,126 |
| ② 공통원가 84건 | **원가표가 답을 줬다** — §4 | A 71건 유지 / B 13건 교체(−4,245) |
| ③ 맥세이프 7 + 도어락 1 | **교체** — 쿠팡명 「맥세이프」 유무로 갈림 | 원가 −11,824 |
| ④ `no_bridge` 76건 | **작업 목록**(§5) — 감사 불가, Jino 입력 필요 | — |

감사 재실행 결과: `replace` **4 → 0** · `agree` **83 → 100**.

## 4. ★★「공통 vs 기종별」은 질문이 틀렸다 — 답은 버퍼였다

84건이 두 무리였고 **차이가 양쪽 다 정확히 265.3원**인데 **방향이 반대**였다:

| 무리 | 공통 SKU | 기종별 SKU | 원가표 정본 |
|---|---:|---:|---|
| A. 3D TPU 무광 3p (71건) | **2,350.7** | 2,616 | **2,350.7** (행 19) |
| B. 사생활 프라이버시 TPU 2매 (13건) | 4,266 | **4,000.7** | **4,000.7** (행 21) |

즉 «공통이냐 기종별이냐»가 아니라 **«버퍼가 얹혔느냐»**가 판별자였다. 낮은 쪽이 늘 정본이다.
`2,616`·`4,266`은 **원가표에 아예 없는 값**이다(파싱으로 확인).

⚠️ **이 과정에서 내가 71건을 반대로 바꿨다가 되돌렸다.** Jino의 「같은 제품이다」를
「기종별 2,616이 맞다」로 읽었는데, 그건 **제품 동일성**에 대한 답이었고 **가격**에 대한 답이
아니었다. 원가표를 보기 전에 움직인 것이 원인이다(교훈 #204).

## 5. `no_bridge` 76건 — 감사할 수 없는 구멍

브리지(ch5 `externalVendorSku` 정확일치)가 없어 **판정 불가**다(0원이 아니라 「모름」).
창 07-10~08-08에 **10건이 실제로 팔린다.**

| 상태 | 건수 | 비고 |
|---|---:|---|
| 판매 중 · 값이 정본 | 8 | Z폴드8 3종(6,089.6/3,480.4) · A35 필름(2,350.7) · 워치 스트랩 5종(1,400) |
| **판매 중 · 원가표에 없는 값** | 2 | ⚠️ `OHI-TGLASS-IP17PRO` **3,500원** |
| 판매 없음 | 66 | `suggested` 44 · `manual` 22 |

★★**`OHI-TGLASS-IP17PRO` 하나에 12개 기종이 매달려 있다** — 아이폰 12/12미니/12프로맥스/
13프로맥스/14/16/16플러스/16프로/16프로맥스/17프로/17프로맥스/Air. 창 30일 판매 **1,287개**.
ref 44 §4가 지목했던 그 건이고 **아직 살아 있다.** 값 3,500원도 원가표에 없다.
브리지가 없어 이번 감사로는 판정 못 했다 — 별건(§7).

## 6. ★이 세션이 바꾼 prod 데이터 (되돌리는 법)

코드 배포가 아니라 **데이터 변경**이다. 전부 되돌릴 수 있다.

| 대상 | 건수 | 방법 | 되돌리기 |
|---|---:|---|---|
| `rocket_product_cost_map` 링크 | 16 + 4 + 13 + 8 = **41** | `POST /api/coupang/ops/rocket/cost-map`(멱등) | 같은 API로 옛 `internal_sku` 재지정. `note`에 종전 값·근거가 남아 있다 |
| `product_master.cost_price` | **177** | 직접 UPDATE | 백업 CSV로 복원 |

**백업**(prod `/home/ubuntu/ohisell/backups/`):
- `ohisell_before_costbuffer_20260810_122857.db` — 전체 DB 스냅샷(1.7GB)
- `costbuffer_before_20260810_122857.csv` — 177건의 `sku,현재값,정본,차,이름`

**복원**(CSV 3열=정본, 2열=옛값 → **되돌릴 땐 2열을 쓴다**):
```bash
cd /home/ubuntu/ohisell/backend
while IFS=, read -r sku old truth d nm; do
  sqlite3 ohisell.db "UPDATE product_master SET cost_price=$old WHERE internal_sku='$sku';"
done < /home/ubuntu/ohisell/backups/costbuffer_before_20260810_122857.csv
```

★**A 71건은 바꿨다가 되돌렸다** — 순 변화 0이지만 `note`에 그 이력이 남아 있다.

⚠️ **DB 스냅샷은 세션 전체의 복원점이 아니다**(적대 리뷰 P2-7). 12:28:57에 떴으므로
링크 41건 중 **8건만** 그 이후이고 나머지 33건은 이미 반영된 상태로 찍혔다.
값 177건은 완전히 덮는다. 링크를 되돌리려면 스냅샷이 아니라 **`note`를 보고 API로** 되돌린다.

## 7. 다음 (Jino 결정 대기)

1. **원가표 시스템화(D-CPP-31)** — Jino: *"엑셀은 아무래도 불안하잖아"*. 오늘 그게 증명됐다
   (파일을 못 찾아 30분 · 내가 추론으로 71건을 거꾸로 · 옛 엑셀 업로드 시 되돌아갈 위험).
   **부자재 구성까지 옮겨야** 필름 단가 변경이 자동 전파된다(§1). 착수 전 계약 필요.
2. **원가 매핑 엑셀 v4 재생성** — v3는 08-07 시점이라 오늘 고친 177건이 반영 안 됐다.
   **그 엑셀을 지금 올리면 버퍼가 되돌아온다**(08-07 인계본 경고. 이제 §8 검사기가 잡는다).
3. **`OHI-TGLASS-IP17PRO` 12기종 뭉침** — 판매 1,287개, 원가표에 없는 3,500원.
4. **`no_bridge` 76건** — 쿠팡 `externalVendorSku` 입력.
5. **정본 파서를 섹션별 원가 열로 넓히기** — 지금은 D열만 본다. 케이스·오타오는 I·C·E열이라
   판정 불가 459건 중 **최소 75건이 대조 가능한데도 「모름」**이다(§2). 넓히면 100원 컷오프도
   필요 없어진다(컷오프는 D열에 CNY/USD 단가가 섞여 생긴 것이다).
6. ~~**검사기를 크론·CI에 배선**~~ — ✅ **2026-08-10 종결(§9)**. 크론이 아니라 **앱 안**으로
   넣었다. 이유는 §9에.

## 8. 재발 방지 — `scripts/audit_cost_buffer.py`

`product_master`를 정본 스냅샷과 대조해 세 갈래(`ok`/`buffered`/`undetermined`)로 가른다.
**읽기 전용**(`mode=ro`), 드리프트가 있으면 **종료 코드 1**(크론·CI 게이트로 쓸 수 있다).

```bash
python3 scripts/audit_cost_buffer.py --db /home/ubuntu/ohisell/backend/ohisell.db
```

**실증**(2026-08-10):
- 고치기 전 DB 백업 → **177건 검출, exit 1**
- 고친 후 prod → **0건, exit 0** (정본 일치 313 → **490**)

★**이름 매칭을 하지 않는다.** 정본 항목명과 마스터 상품명은 표기가 달라(「지문방지필름 TPU
3매」 ↔ 「빛반사, 지문방지 매트 필름 3매, 아이폰에어」) 이름으로 이으면 또 오판한다.
**값 산술만** 본다 — 근거가 좁은 대신 틀리지 않는다. 그래서 이 검사기는 «어느 상품의 원가가
얼마여야 하나»는 **답하지 못한다**. 답하는 것은 «지금 값이 정본 + 알려진 버퍼인가»뿐이다.

회귀 테스트 **30건** + 변이 주입 **14종 전건 KILLED**(베이스라인 GREEN 확인 후 — 교훈 #200).

★적대 리뷰가 1라운드에서 **14종 중 11종을 생존**시켰다. 살아남은 것이 두 무리였다 —
①`main()`(종료 코드·리포팅·기본 경로)이 테스트 밖 ②스냅샷의 출처·완결성이 테스트 밖.
`classify()` 산술만 촘촘히 물려 있었다. **«이 검사기의 존재 이유가 조용한 복귀를 시끄럽게
만드는 것인데, 시끄럽게 만드는 부분이 무보호였다»**(P1-3). CLI 스모크 테스트로 해소했다.

★그리고 내 1라운드 변이 실행에 **거짓 KILL**이 있었다 — `mode=ro` 문자열만 바꾸니 `uri=True`가
남아 연결 자체가 깨져서 «죽었다»고 나온 것이지 읽기 전용이 지켜져서가 아니었다.
변이는 **의도한 결함 하나만** 주입해야 한다(교훈 #207).

## 9. ★배선 — 검사기를 «부르는 사람»을 만들었다 (2026-08-10 오후, §7-6 종결)

§8의 검사기는 완성된 채로 **아무도 안 불렀다.** 그 상태는 검사기가 없는 것과 관측상 같다.

### 크론이 아니라 앱 안에 넣은 이유

prod 크론을 실측했더니(2026-08-10) 기존 ohisell 잡 셋 중 **둘이 출력을 `/dev/null`로 버리고**
있었다(`run_mop_activation.sh`·`run_mop_keyword.sh`). 나머지 하나는 로그 파일에 append한다.
즉 크론 + 로그는 **「아무도 안 부른다」를 「아무도 안 읽는다」로 옮길 뿐**이다.
게다가 prod엔 **git 체크아웃도 `docs/` 디렉터리도 없어서**(실측) 스크립트와 스냅샷을 그 경로로
배포할 수단 자체가 없었다.

그래서 판정을 **요청 시점 계산**으로 옮겼다:

```
product_master → compute_scheduler_health → build_health
              → GET /api/scheduler/health (`cost_drift`) → 전역 파이프라인 헬스 배너
```

이러면 «검사가 언제 돌았나 / 결과가 낡았나»라는 질문이 **아예 없어진다.** 크론 결과 파일은
그 자체가 낡을 수 있고, 낡은 것과 «이상 없음»은 또 같은 모양이다(교훈 #123의 재판).

### 구성

| 파일 | 역할 |
|---|---|
| `backend/app/services/cost_truth_audit.py` | **순수 판정 SA** — `load_truth`/`classify`/`summarize_drift`. DB·SQLAlchemy 미임포트 |
| `backend/app/data/cost_truth_20260807.json` | 정본 스냅샷(**이사함** — `docs/`는 prod에 없어 배포 불가였다) |
| `scheduler_health.py` | I/O 경계에서 `product_master` 조회 → 판정 → `cost_drift` 주입. **fail-soft**(실패해도 헬스 API는 산다) |
| `Layout.tsx` `buildPipelineHealthBanner` | 배너 문구. `count>0`일 때만 — 0건은 아무 말도 안 한다 |
| `scripts/audit_cost_buffer.py` | CLI 유지. 이제 위 순수 SA를 **임포트**한다(사본 두 벌 금지) |

★**산술은 한 벌**이다. 사본을 두면 한쪽만 고쳐져 «감시자가 감시 대상보다 낡는» 형태가 된다.

### 알고 감수한 것

- **드리프트 0건과 «스냅샷이 없어 판정 못 함»이 응답상 같다**(둘 다 `cost_drift: null`).
  fail-soft가 헬스 API 전체를 살리는 대가다. 로그가 유일한 구분자다.
- **원가 0원은 드리프트가 아니다.** 이 스키마에서 `cost_price`는 `nullable=False, default=0`이라
  «원가 미입력»이 0원으로 들어온다 → `undetermined`로 센다. 0원을 드리프트에 합치면 배너가
  상시 켜져 **진짜 복귀를 가린다.** 원가 미입력은 별개 문제다.
- **배너는 한 줄이라 건수 + 버퍼 계열만** 쓴다. 세 갈래 전부를 보려면 API 응답이나 CLI를 본다.

### ⚠️ 배포 체크리스트 — 스냅샷을 빠뜨리면 **조용히 무장해제된다**

`backend/app/data/cost_truth_20260807.json`을 배포 목록에서 빠뜨리면 fail-soft가 `None`을 내고
화면은 초록이다(적대 리뷰 P2). **로그가 유일한 구분자다.** 배포 시 항상 같이 올린다:

```bash
scripts/safe_deploy.sh \
  backend/app/data/cost_truth_20260807.json \
  backend/app/services/cost_truth_audit.py \
  backend/app/services/scheduler_health.py \
  backend/app/schemas.py --restart
```

⚠️ **프론트도 같이 배포한다** — 백엔드만 올리면 `cost_drift`를 **주기만 하고 아무도 안 그린다**
(2R 적대 리뷰 P2-2). 배너는 프론트 전용 변경이다:

```bash
(cd frontend && npm run build) && scripts/safe_deploy.sh --frontend
```

### ★★1라운드 적대 리뷰가 잡은 것 — 배선을 만들면서 **HTTP 경계에서 스스로 끊었다**

**P1-1**: 라우터가 `response_model=SchedulerHealthOut`인데 그 스키마에 `cost_drift`가 없어
**FastAPI가 응답에서 조용히 지웠다.** 서비스층 dict엔 있고 HTTP body엔 없다. 프론트에 그
실제 body를 먹이면 배너가 `null`을 반환한다 — 즉 **드리프트가 있어도 화면은 「이상 없음」**.

★내 배선 테스트 6건이 **하나도 안 죽었다.** `compute_scheduler_health`의 dict까지만 보고
경계를 한 번도 안 넘었기 때문이다. 그 파일 헤더가 인용한 교훈 #208(«도구의 값어치는 경계층에
있는데 순수 함수만 촘촘히 물리면 통과하는데 아무것도 안 지키는 테스트가 된다»)을
**그 파일 자신이 재현했다.**

수정은 스키마 한 줄이 아니라 **TestClient로 실제 라우트를 호출해 body를 단언하는 테스트**와
짝이다. 그게 없으면 다음 사람이 같은 자리에서 같은 방식으로 다시 끊는다.

**P2 처분**:

| 지적 | 처분 |
|---|---|
| M19 — 드리프트 1건만 시드해 `count` 상수화가 안 잡힘 | **채택** — 3건·2계열 시드로 교체, 변이 KILLED 확인 |
| M17 — try/except가 넓어져 진짜 버그를 삼켜도 안 잡힘 | **채택** — 라우트 테스트가 덮는다, 변이 KILLED 확인 |
| M04 — `isnot(None)` 필터 제거가 안 잡힘 | **채택(형태 변경)** — 실측하니 prod DDL이 `NOT NULL`이라 **그 상태를 만들 수 없다.** «전제가 바뀌면 우는» 가드로 대체(§아래) |
| M09 — `round(float(cp),1)` 제거가 안 잡힘 | **기각** — 원가는 소수 1자리로만 저장된다(정본·마스터 모두). 무해 |
| 스냅샷 배포 누락 = 조용한 무장해제 | **채택** — 위 체크리스트 |
| 캐시 없음(매 요청 파일 재파싱 + 949행 스캔) | **기각** — 실측 13ms(최악 26ms) · 폴링 5분. 캐시가 오히려 «낡은 판정» 문제를 새로 만든다 |
| `disk_low` 침묵 경로 | **채택** — 처음엔 이월로 적었으나 **Jino 승인 후 같은 커밋에 동봉**했다(§9-1). prod에서 실재하는 침묵이었다 |

★**M04는 «테스트가 부족»이 아니라 «그 상태를 만들 수 없다»였다.** 지워도 안 죽는 코드를
남길 땐 «검증했다»고 말하지 않고, **전제가 바뀌는 순간 우는 가드**를 옆에 둔다.

### 검증

- 배선 테스트(`backend/tests/test_cost_drift_wiring.py`) **10건** — 순수 2 · 실 DB 배선 5 · **HTTP 경계 2** · 휴면 가드 1
- 배너 테스트 5건(0건 침묵 · null 하위호환 · 판정불가 미혼입 · 타 항목과 공존)
- 2R 변이 확인: 스키마 필드 제거 **KILLED**(2건이 잡음) · M17 **KILLED** · M19 **KILLED**
- 라이브 합격기준·실측은 **§9-2**

### 9-1. `disk_low` 침묵도 같이 고쳤다 (Jino 승인)

이 PR을 만들다 **prod가 지금 `healthy=false`인데 배너가 침묵한다**는 것을 발견했다.
원인: `build_health`는 `disk_low`로 `healthy=false`를 만드는데 `buildPipelineHealthBanner`에
**분기가 아예 없었고**, 프론트 타입에도 `disk_low`가 없었다.

**2026-08-10 prod 실측**: 사용률 **93.8%**(여유 5.9GB, 경고선 85%) → `healthy=false` → 화면 조용.
2026-08-03 ENOSPC 사고(디스크 포화로 서버 3시간 40분 마비, 자동수집 12개 유실)를 막으려고
만든 **유일한 사전 신호**가 화면까지 이어지지 않은 채 있었다.

★내가 고치던 함수가 바로 그 함수라 같은 커밋에 넣었다(Jino 승인). 변이(분기 삭제 = 종전 상태)
로 KILL 확인.

### 9-2. 라이브 합격기준

1. prod `/api/scheduler/health` 응답에 **`cost_drift` 키가 실재**하고, 지금은 `null`
2. 버퍼가 얹힌 값이 있으면 잡힌다(재현)
3. 드리프트 0건이면 배너에 원가 문구가 **안 뜬다**
4. `disk_low` 문구는 **뜬다**(93.8%) — 종전엔 안 떴다
