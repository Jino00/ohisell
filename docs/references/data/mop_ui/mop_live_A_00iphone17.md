# MOP 라이브 관찰 A안 — 00.아이폰_17 (우리판 MOP 구조 비교)

> 목적: 실제 MOP가 이 캠페인을 관리하며 **뽑아내는 데이터·판단 구조**를 매일 관찰해,
> 우리가 만든 naver_ad(우리판 MOP)와 단계별로 비교한다. (A안, HANDOFF_ohisell-mop-live-observation 계승)
> 시작일(D0): 2026-07-11 (KST)

## 0. 대상·환경 (실측 고정)
- 캠페인: **00. 아이폰_17**, ID `cmp-a001-02-000000009793536`, 유형=쇼핑검색
- 네이버 계정: `theohi11` / 광고주 `a001` (광고주센터 ad-accounts/2094736)
- 애드그룹 3개: `01. 강화유리`(입찰 50원), `02. 사생활`(50원), `03. 6H 사생활`(500원)
- **상태: 2026-07-11 Jino가 MOP 테스트용으로 방금 ON** (그 전까지 캠페인+그룹 전부 OFF, 7일 지출 0)
- 하루예산: 150만 → **5만원**(테스트 소액으로 조정), 예산배분=일반노출, 계속노출
- MOP 계정: `오하이_구민정` (advertiserId **756**, Operator, **Basic plan**) — ★2026-07-12 정정: 1422는 "Google Ads"임(직전 세션 오기). `GET /v1/advertisers`로 확정.
- MOP API 백엔드: `be.mopapp.net` (콘솔 도메인 mop.co.kr와 분리)

## 1. 데이터 지표 스키마 비교 (원천은 같은 네이버 stats)

MOP 캠페인 summary 응답 필드(16종) ↔ 우리 `naver_ad_daily` 컬럼

| MOP 필드 | 우리 컬럼 | 동일/차이 |
|---|---|---|
| `cost` | `cost` (원, VAT별도) | ✅ 동일 |
| `impressions` | `imp` | ✅ 동일 |
| `clicks` | `clk` | ✅ 동일 |
| `adRankSum` | `rank_sum` (avg_rank=rank_sum/imp) | ✅ 동일 개념 |
| `conversions` | `conv_direct_cnt` + `conv_indirect_cnt` | ⚠️ 우리는 **직접/간접 분리**, MOP는 통합 표기 |
| `conversionRevenue` | `conv_direct_amt` + `conv_indirect_amt` | ⚠️ 우리는 **직접/간접 분리** |
| `roas` | (파생: convRev/cost) | 둘 다 파생 |
| `cpc` | (파생: cost/clk) | 둘 다 파생 |
| `cpa` | (파생: cost/conv) | 둘 다 파생 |
| `ctr` | (파생: clk/imp) | 둘 다 파생 |
| `cvr` | (파생: conv/clk) | 둘 다 파생 |
| `cpm` | (파생) | 둘 다 파생 |
| `salesAmount` | — | ⚠️ MOP 고유: 전환매출과 별개 매출(커머스/스마트스토어 연동 흔적) |
| `viewCount`,`vtr`,`cpv` | — | ⚠️ MOP 동영상/멀티채널(VA) 지표. 우리는 네이버 SA 전용이라 없음 |

**요약:** 원자 지표(노출/클릭/비용/전환/순위합)는 동일. 차이 3가지 = ①우리는 전환 직접/간접 분리, ②MOP는 매출(salesAmount) 별도 축, ③MOP는 멀티채널(동영상) 지표까지 스키마에 보유.

## 2. 최적화 개념·엔진 비교 (핵심 차별점)

| 축 | MOP | 우리 naver_ad |
|---|---|---|
| 최적화 단위 | 최적화 유닛(애드그룹 묶음). 목표입찰=소재 단위(Pro) | 캠페인/키워드 단위 제안 |
| 목표 설정 | 운영모드 **균형/성장**(다중목표) — **숫자 목표 없음**. 목표입찰=Avg Rank/ROAS/CPA(Pro) | **BEP-ROAS 숫자 다이얼**(하한 불변, D-NAO-1) + 듀얼모드(균형/성장) |
| 엔진 단계 | 수집→러닝(ML예측)→플래닝(입찰계획)→플라이트(집행) | 수집→forecast_engine→제안→가드레일→실행→학습(D+7/14) |
| 캠페인 자격 | **Wait→OPT**(노출점유·운영일수 채워야 실제 최적화) | 제안 pending→approved→executed |
| 예산 산정 | 이지모드=**7일 평균 광고비 기반**(직접입력 없음) | 다이얼/예산 로직 |
| 안전장치 | 자동(블랙박스) | 가드레일(±15%·쿨다운·일일상한·스톱로스·BEP증액금지·예산불가침)+위임게이트 |
| 학습/투명성 | 최적화 리포트·입찰이력 | change_log·expert_desk(Ava)·5단엔진 대시보드·proposal_scoreboard |

## 3. 관찰 일지 (매일 채움)

### D0 — 2026-07-11 (baseline)
- 캠페인 방금 ON(운영가능), 하루예산 5만, 그룹3 ON.
- MOP 상태: 위저드에서 아직 **Wait/OFF**로 표시(전일 실적 반영이라 오늘 ON이 미반영). 7일 평균 광고비 **0원** → 이지모드 예산계산 0원.
- MOP 최적화 유닛: 활성 0개(기존 4099·1119 종료), 신규 1개 생성 가능(Basic 0/1).
- summary 실측: 전 지표 `-` (지출 0, 정상).
- **아직 MOP 최적화 유닛 미생성**(예산 0원 회피 위해 지출 반영 후로 보류 — Jino 확인 대기).

### D+1 — 2026-07-12 (실측)
- ★**중대 정정(원칙22)**: 관찰 대상 advertiserId **1422가 아니라 756**. 1422="Google Ads"라 네이버 캠페인 조회가 전부 `-`로 나왔던 것(직전 세션이 `-`를 "지출0 탓"으로 오귀속). 763만원 지출한 다른 쇼핑캠페인도 1422에선 `-` → 756으로 바꾸니 정상 숫자. `GET /v1/advertisers`로 광고주 4개(756 오하이_구민정/1422 Google Ads/1428 Google Ads_CID/780 오하이 카카오) 확정. mop_snapshot.js 수정 완료.
- **MOP에 07-11 반영 확인 ✅**: availablePeriod maxDate=2026-07-11. adgroupsTree SA_SHOPPING/NAVER=**25개 캠페인**에 `● 00. 아이폰_17`(활성) 포함.
- **00.아이폰_17 실측(756)**:
  - 07-05~07-11(7일): **cost=0, imp=181, clk=0, conv=0**
  - 07-11 단일: cost=0, **imp=181**, clk=0 (prod naver_ad_daily adgroup 노출 178+3=181과 정확 일치 — MOP 데이터 정확)
  - 07-12 당일(부분): cost=0, **imp=57**, clk=0 (계속 노출 중, 여전히 클릭0)
- **이지모드 예산 0원 탈출? ❌ 아니오** — 7일 지출 0(노출만 있고 클릭0→지출0)이라 7일평균 광고비=0 → 이지모드 예산계산 여전히 0원.
- **prod 교차검증**: naver_ad_daily 00.아이폰_17 07-11 = imp181·clk0·cost0(원칙22). 캠페인은 서빙되나 day1 클릭 0.
- **sanity**: 지출캠페인 01.갤럭시_지문방지_TPU(8425541) 07-11 = MOP cost 219,628·clk143·conv17·roas1.20 → summary API 정상.
- **판단**: MOP는 지출·전환 신호가 0이라 최적화 재료 없음. 최적화 유닛 생성은 **보류 권장**(예산 0·학습신호 0). 며칠 더 관찰해 클릭/지출이 붙는지 확인 후 Jino 결정. → 아래 "미결 결정" 참조.

### D+1 SPA 위저드 라이브 재확인 (원칙22, 07-12)
- 콘솔 홈 → 오하이_구민정 헥사곤 **SPA "Go opt"** 클릭 → `mop.co.kr/shopping-optimization` 진입 성공(svg.SPA face).
- **SPA 입찰최적화 목록**: 최적화 애드그룹 **0/30**. 기존 유닛 2개 전부 **종료**: ①4099 `250617_ROAS최적화`(44그룹·하루예산193,940·ROAS최적화·25.06.24~26.06.17) ②1119 `전환_240424_쇼검_통합`(23그룹·40,200·전환최적화·24.04.26~24.06.18).
- 좌측 SPA 메뉴 잠금 상태: 품질이상감지·캠페인생성·커머스리포트·예산민감도·경쟁심화도·제외키워드 🔒(Pro). **목표입찰=잠금 없음**(Basic 가능 추정, AD_RANK_TARGET=1·RANK_TARGET=5 한도) — 후속 조사 가치.
- **위저드 예산 산식(24b 스크린샷, 라이브 구조 동일)**: 운영모드 **균형 운영**(추천, "최근 광고비 유사수준 효율개선", 다중목표) / **성장 운영**("광고비 증가시켜 더 많은 성과"). 하루예산 슬라이더 `7일 평균 ↔ 공격적 입찰`, **"하루예산은 선택 캠페인의 최근 7일 평균 광고비 기준 산출"** 명시. 7일평균 광고비 표시=0원+[계산하기].
  - → 00.아이폰_17은 7일 지출0(라이브 API 확인)이라 산출 예산=0. 성장모드가 배수로 키워도 0×배수=0. **저장 차단 여부는 미확인**(아래 로그아웃으로 중단) — 단, 0예산 유닛은 저장돼도 지출0이라 클릭 못 밀어 무의미.
- ⚠️ **gstack 헤디드 세션 또 로그아웃**(SPA 위저드 조작 중 about:blank로 튕김→재접속 시 sid null·`/` 리다이렉트). §5 반복 로그아웃 재현. 자동 라이브관찰의 실질 걸림돌 — 원인 점검 필요(세션TTL vs 클릭이 트리거한 네비게이션 vs target=_blank).

### D+1 결론 — "MOP로 클릭 더 나오게" (Jino 질문 답)
- **가능**: MOP SPA 입찰최적화 유닛, 특히 **성장 운영 모드**가 입찰가·광고비를 올려 상위노출→클릭/전환을 더 만드는 게 정확히 그 기능.
- **단, 지금 이 캠페인은 막힘**: 이지모드 하루예산이 "그 캠페인 7일 평균 지출"로 산출되는데 00.아이폰_17은 그게 0(노출181·클릭0→지출0). 예산0→성장모드도 0을 못 키움. 학습할 클릭/전환 데이터도 0.
- **근인**: 입찰가 50/50/500원이 낮아 쇼핑검색에서 노출만 나오고 클릭이 안 붙음.
- **해결(Jino 결정)**: (A) 입찰가 소폭 상향(1회 시동)→첫 클릭/지출 발생→MOP 성장모드 유닛 생성→이후 입찰·예산 MOP 이관. (B) 그대로 관찰(50원으론 계속 0클릭 위험). (C) 목표입찰(미잠금) 경로 별도 조사.

### D+2 — 2026-07-12 세션(관찰일 2026-07-12) 실측
- **prod 라이브 재확인(원칙22)**: naver_ad_daily 최신 완전일=**2026-07-11**(synced 07-12 07:50). 07-12 당일치는 **아직 미수집**(네이버 일단위 → 07-13 아침 크론이 07-12 확정치 수집).
- **00.아이폰_17 07-11 확정**: 실애드그룹 `grp-...822`(178)+`grp-...868`(3)=**imp 181, clk 0, cost 0**. MOP summary(imp181)와 정확 일치. **day1(07-11)도 클릭0·지출0 — D+1 대비 변화 없음.**
- ⚠️ **데이터 품질 관찰(별건, D-NAO-41 자체생성 관련)**: 같은 campaign_id·07-11에 `adgroup_id='__backfill__'` 합성행(imp 181)이 별도 존재 → `SUM(imp) GROUP BY campaign_id`가 **362로 이중계상**. 실그룹 합(181)이 authoritative(MOP 일치). 우리판 집계 로직이 `__backfill__` 행을 필터하는지 후속 점검 필요(캠페인 단위 KPI 부풀림 위험). ← naver_ad 대시보드 검산 시 확인.
- **계정 전체 07-11 정상**: imp 94,872·clk 920·cost 1,207,218 (수집 파이프라인 건전, D-NAO-41 유지 확인).
- **MOP 가동 상태 = 여전히 못 돎**: 우리 캠페인 7일 지출0 → 이지모드 예산0 → 최적화 유닛 생성 무의미. **관찰이 설계상 정체**(지출 신호가 0이라 MOP가 관리할 재료가 없음). 시동(Jino 결정1) 전까지 매일 스냅샷은 "여전히 0"만 재확인.
- ~~SPA 엔진 캡처·4099 리포트 미수행~~ → **Jino 재로그인(gstack `$B connect` 헤디드) 후 아래 완료**.

### D+2 SPA 엔진 캡처 (2026-07-12, Jino 재로그인 후 라이브)
- **광고주 전환 확정법 발견**: UI 헥사곤 클릭은 pointer-events 미수신으로 실패 → `sessionStorage.setItem('advertiserId','756')` 로 확정 전환.
- **★SPA 전용 엔진 경로 확정**(mop_engine_outputs.md §5): `/dashboard/saShopping/*` = **존재 안 함(전부 500)**. SPA 산출물은 `/v1/optimizations/sa/shopping/*`·`/v1/report/opt/*`.
- **★MOP가 우리 캠페인에 아직 아무것도 못 하는 근본 확인**(§5-a, `spa_candidate_adgroups_20260712.json`): 위저드 후보목록에서 00.아이폰_17 3그룹 전부 **`predicted:false`·`inBidding:false`**. 계정 전체 450그룹 중 predicted=150·**inBidding=0**. → 클릭·전환 이력 0 → **MOP가 ML 예측모델조차 안 만듦**. (지출0→예산0에 더해, 예측모델 부재가 최적화 불가의 더 근본 원인.)
- **4099 최적화 리포트 = 소멸**(§5-b): report/opt 보존기간 **2026-05-13~07-11(~2개월)** 뿐, 4099 활동기(2025-06~2026-04) 밖 → 전 지표 0/`-`. MOP 실제 입찰궤적 복구 불가. 남은 건 유닛 **설정** 스냅샷(`spa_opt_4099_detail_20260712.json`: ROAS·EXPERT·132그룹·kpiValue -1=하드숫자 미설정).
- **★시동(결정1) 재검토용 순위/CTR 실측**(prod, 원칙22) — "입찰↑→클릭" 단순가정을 흔드는 사실:

  | 애드그룹 | 입찰 | 노출(8d) | 클릭 | 평균순위 |
  |---|---|---|---|---|
  | 01. 강화유리(617822) | 50원 | 178 | **0** | **3.8 (상위권)** |
  | 02. 사생활(617868) | 50원 | 3 | 0 | 20.0 |
  | 03. 6H 사생활(091017) | 500원 | **0** | 0 | 미서빙 |

  - **01.강화유리는 이미 평균순위 3.8(상위)인데 178노출·0클릭 = CTR 0%** → 위치 문제 아님. 입찰만 올려선 클릭이 붙는단 보장 없음(소재/가격/상품경쟁력 이슈 가능성).
  - **03.6H사생활은 입찰 500원인데 0노출** → 정확한 원인 = 소재 4개 `statusReason=AD_ABNORMAL_INTERLOCK`(=소재 연동 상태 비정상, 토글 ON·userLock=false). 입찰 무관.
  - → **시동=입찰상향은 02.사생활(rank20·저노출)엔 유효할 수 있으나, 주력 01(rank3.8·0CTR)엔 효과 불확실**. 실제 입찰 조정·전략 판단은 Jino 몫(전략추천 금지 규칙). Claude는 사실만 제시.

### D+1(계속) — Jino 지적 반영: "켠 시점(07-11 저녁)부터 지금까지" 궤적 + CTR 결론 철회
- ★**날짜 정정**: 오늘은 **2026-07-12**(D0=07-11 켠 날 → 오늘은 관찰 D+1). 직전 소절의 "D+2/07-13"은 Claude 오기 → 07-12로 정정, 파일명 `20260712`로 정정.
- **켠 시점~지금 MOP 일별 궤적**(POST report/campaign/756/summary, 일자별):

  | 날짜 | 노출 | 클릭 | 평균순위(adRankSum) |
  |---|---|---|---|
  | 07-11 (저녁 ON) | 181 | 0 | 4.07 |
  | 07-12 (오늘, 부분) | 57 | 0 | 7.21 |
  | 합계 | **238** | **0** | — |

- ★**"CTR 0% = 소재 문제" 결론 철회(원칙22)**: 켠 지 **하루 남짓·노출 238**은 표본이 너무 작아 아무것도 단정 불가. 순위도 4~7로 정상. 앞선 "관찰 정체·매일 0만 재확인"도 과잉 — **아직 초기라 데이터가 쌓이는 중**이 정확한 표현.
- **Jino 질문 답①: 이 경우 MOP는 어떻게 판단?** → 판단 신호 = 후보목록의 `predicted`·`inBidding` 플래그(별도 상태텍스트 없음). 우리 3그룹 `predicted:false·inBidding:false` = **MOP는 "아직 최적화 대상 아님/수집 대기"로 취급**(부정판단이 아니라 모델 만들 이력이 아직 없음, day1 당연).
- **Jino 질문 답②: 노출시키기 위한 작업(메커니즘)이 있나?** → 있음, 단 자동은 아님:
  1. **입찰최적화 성장운영 모드**(SPA 유닛): 입찰·예산 올려 노출→클릭 밈. **지금 막힘**(7일평균 지출0→예산0, predicted:false).
  2. **목표입찰(`/target-bidding`, `GET /v1/rank-maintenance/shopping/ad`, Basic·무료)**: 소재 1개(**0/1 사용**)에 **입찰기준 키워드 + 목표 지표(목표순위 등)** 설정 → MOP가 그 목표 맞추게 자동입찰. **★지출이력 불필요** — 유일하게 지금 바로 쓸 수 있는 노출 레버. 현재 등록 0.
  - 결론(1차): MOP는 0-이력 신규 캠페인에 **스스로** 노출을 밀지 않음. 목표입찰이 유일 후보로 보였음.

### D+1(계속) — 목표입찰 실증 시도 & ★최종 결론(A 조사 완료, 2026-07-12)
- **소재 ID = SA API로 확보**(UI 로그인 불필요): `/ncc/ads?nccAdgroupId=` → `nad-a001-02-...`. 파일 `00iphone17_soljae_ids_20260712.md`.
  - 01.강화유리 4소재 ELIGIBLE(hero **739856=132노출**), 02.사생활 4소재 ELIGIBLE(890=3), **03.6H사생활 4소재 전부 AD_ABNORMAL_INTERLOCK(소재 연동 비정상)**.
- **★03 0노출 원인 규명(정정)**: 소재 4개 `status=PAUSED, statusReason=AD_ABNORMAL_INTERLOCK`(="소재 연동 상태 비정상"). ON/OFF 토글 ON·userLock=false·inspectStatus=APPROVED → **정지가 아니라 소재↔쇼핑상품 연동 비정상**이라 enable=false 미노출. **토글 ON으로는 안 풀림 — 소재 연동 정상화 필요**. ⚠️처음 status만 보고 "PAUSED=정지"로 오보고→statusReason으로 정정.
- **★입찰가 정정**: MOP 소재 detail(`/v1/ads/nad-...856`) = **현재입찰가 1,390원**(HANDOFF "50원"은 애드그룹 기본값, 소재는 1,390원). → **낮은 입찰이 0클릭 원인 아님**. 1,390원·rank~4·132노출·0클릭(하루치).
- **★목표입찰 등록 시도 → 저장 불가**: MOP 폼에 hero 소재 입력 → 정상 인식(productTitle·campaign/adgroup 로드). 그러나 **`keywordStats=[]`(2주 키워드 실적 없음)** → 입찰기준 키워드 선택 불가 → **저장 disabled**. **목표입찰도 2주 이력 필요 → 신규 캠페인 등록 불가.**
- **★★최종 결론(Jino 질문 완결)**: MOP의 노출 레버 **둘 다 history-gated** — 입찰최적화(성장)=7일평균 예산+predicted, 목표입찰=2주 키워드 실적. **day-1 캠페인엔 MOP가 지금 할 수 있는 작업이 없음.** 레버를 열려면 **~2주 이력 축적이 선행**(자연 노출 지속 또는 소재/입찰 조정). = 시동의 본질은 "돈 넣기"가 아니라 "MOP 자격을 여는 이력 2주 쌓기".

## 부록: MOP API 재현법 (라이브 추출용)
- 백엔드: `https://be.mopapp.net`, advertiserId=**756**(오하이_구민정, 네이버 SA). ★1422는 Google Ads라 네이버 조회 시 `-`.
- 광고주 목록(계정 확인): `GET /v1/advertisers` → 756/1422/1428/780.
- 인증: 헤더 `x-session-id: <sessionStorage.sessionId>` — 없으면 `SESSION_EXPIRE`. 쿠키는 httpOnly.
- 캠페인 요약: `POST /v1/report/campaign/756/summary?startDate=YYYYMMDD&endDate=YYYYMMDD`
  - body: `{...,"saShoppingCampaigns":{"naverCampaignIds":["cmp-a001-02-000000009793536"]},...}` (쇼핑캠페인 필터)
- 애드그룹 트리(캠페인 목록 포함): `GET /v1/report/campaign/756/adgroups` → SA_SHOPPING/NAVER에 25개 캠페인.
- 조회기간: `GET /v1/report/available-period/756?reportType=REPORT_CAMPAIGN`
- ⚠️ MOP summary `-` 반환 = **그 광고주 계정에 그 캠페인 데이터가 아예 없을 때**(예: 1422=Google Ads에 네이버 cmp-a001-* 조회). ★"지출0이면 `-`"는 오해였음 — 756에선 우리 캠페인이 cost=0인데도 imp=181을 정상 반환(지출0이어도 `-` 아님). 계정이 맞으면 노출·클릭·지출 실수치가 나온다.
