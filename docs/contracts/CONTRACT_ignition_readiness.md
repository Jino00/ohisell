# 계약: 점화 준비 — 결손 11개의 슬라이스 완주

```
목표이름: 점화준비 목표
트랙: docs/tracks/active/track_naver-ad-optimization.md
결정번호: [승인 후 scripts/next_ids.sh로 채번 — 초안은 선점하지 않는다]
상태: 초안 — Jino 승인 대기
작성: 2026-08-26 23:1x KST (Fable 저자, 세션 pao-n56 / 체인 PAO 논의 56)
착수 시점: 미정 — Jino가 연다 (2026-08-26 23:11 원문: "바로 시작할거는 아니야. 일단 general하게 만들꺼니까.")
발단 원문: Jino 2026-08-26 23:08 — "위의 모든걸 완성할 수 있는 계약을 짜줘. … 모든 선택은
  우리의 목표와 우리 스마트스토어 광고 계정의 상황을 판단해서 자동 진행하도록 해줘."
목적함수: D-NAO-59 총이익 절대액 (ROAS 아님)
```

## 이 계약이 하는 일 / 안 하는 일 / 왜 자동 진행이 안전한가 (3줄)

- **하는 일**: 2026-08-26 세션이 확정한 결손 13개 중 **코드·DB로 채울 수 있는 11개**를 의존 순서대로 6개 슬라이스로 잘라, 착수하는 세션(들)이 **묻지 않고** 구현→리뷰→배포까지 완주한다. 끝나면 시스템은 「점화 결정만 나면 도는」 준비 완료 상태가 된다.
- **안 하는 일**: 점화(#1)·소유권 분리(#2)·의미단위 콘솔 실증(#7) — 상대(Jino 손·대행사)가 있는 셋은 이 계약 밖이다. **광고 계정에는 아무것도 쓰지 않는다.**
- **왜 안전한가**: 전 캠페인 `auto_operate=0`이고 이 계약은 그 값을 만지지 않는다. 신설 코드는 전부 판정·적재·표면층이며, 광고계정 쓰기 API 호출 0을 diff로 증명한다. **이 계약을 승인해도, 그리고 진행해도, 돈이 나갈 경로가 구조적으로 없다**(§3에서 경로별 증명).

---

## 1. 목표 / 이번에 안 하는 것

**누가 뭘 못하나**: 2026-08-26 실측으로 「임대 제외·재개방 실험·파라미터 학습·초기 탐색 허용」 운영 방식이 합의됐으나, 그 방식이 서기 위한 배선 11개가 없다 — 탐색은 stale 차단(차단의 52%, [G])에 막히고, 제외 원장 3,990행 중 3,987행에 만료일이 없고([D]), 재개방은 harness를 우회해 **학습 사슬에 원리적으로 안 타며**(`search_term_ss_lane.py` diary 호출 970·984·1004 전부 observe/브리핑 — 코드 재확인 완료), 제외·탐색 숫자 9개는 학습 불가능한 하드코딩이고([F]), 파워링크 문안은 DB 0행이다(ref 103 §5).

**되면 뭐가 달라지나**: 점화 결정(§8-①)이 나는 순간 — 그 전이 아니라 — ⓐ탐색이 stale 벽 없이 돌고 ⓑ새 제외마다 등급·만료일이 붙고 ⓒ재개방이 일기→지혜 사슬에 타며 ⓓ학습이 게이트 숫자 4개를 승인 카드로 조정할 수 있고 ⓔJino가 명령 한 줄로 각 부품의 생존을 확인한다. **이 계약의 산출물은 성과가 아니라 «준비 완료 상태»다.**

**이번에 안 하는 것 (소관 병기)**

1. **점화**(#1, `auto_operate`/`optimizer` 변경) → 소관: 북극성 §8-① Jino 결정. 이 계약은 그 결정의 **전제를 전부 초록으로** 만든다. 시한 정보만 기록한다: 학습 재료 3,863건이 **2026-10-28** 수확 창을 이탈한다([I]).
2. **소유권 분리**(#2) → 소관: §8-② 대행사 협의 + Jino. 재개방 «실행»과 A급 16건 실험은 이것 없이 원리적으로 불가(모집단이 우리 칸 2건뿐 — 2026-08-26 실측).
3. **의미단위 등록 표현 실증**(#7) → 소관: **Jino 손**(콘솔 수동 1~2건, 2026-08-26 승낙분). 이 계약은 실증 «후» 열릴 실행 슬라이스를 만들지 않는다 — 실증 결과가 설계 입력이므로.
4. **`conv≥1` 절대보호 개정**(#16) → 소관: Jino 결정(자(尺)의 변경 + §7 대칭 검사 필수).
5. **정식 키워드 등록 쓰기 손**(#17) → 소관: ref 65 §10-④ Jino 결정.
6. 재개방·제외·입찰의 **실행 일체** / 제목·태그 **쓰기** / 249종 태그 정비 → 소관: 점화 후 별도 계약·상품 축.

## 2. 판단기준

1. **항상 광고 계정을 만지지 않는다**, 왜냐하면 자동 진행의 유일한 정당성이 「돈이 나갈 경로 없음」이고, 이 계약의 모든 산출물은 점화 «후»를 위한 준비이기 때문이다.
2. **항상 슬라이스 단위로 독립 완결한다**(구현→테스트→리뷰→배포→표면 확인을 슬라이스마다), 왜냐하면 이 트랙의 세션당 실적은 1~3슬라이스이고, 한 덩어리로 가면 첫 장애가 전체를 물귀신처럼 끌고 내려가기 때문이다.
3. **항상 기존 숫자를 재사용하고 새 상수를 발명하지 않는다**(등급 만료 30·90일 = 재심사 백오프 기존값, SPECS 봉투 끝값 = 현행 게이트·산업 표준 21클릭 등), 왜냐하면 근거 없는 상수는 다음 감사가 들추는 부채이기 때문이다.
4. **항상 실측이 전제를 반증하면 §4-B ④를 따른다**, 왜냐하면 2026-08-26 하루에만 전제 반증이 세 번 났고(70/70 「15그룹」→라이브 5그룹 등), 반증을 삼키면 계약이 거짓 위에 선다.
5. **항상 대행사 칸에는 라벨만 붙이고 실행하지 않는다**(백필 = 우리 DB 원장의 컬럼 갱신, 광고계정 무접촉), 왜냐하면 남의 조치를 되돌리는 것은 소유권 분리 전 금지선이기 때문이다(ref 66 §5-3).
6. **항상 Jino가 명령 한 줄로 확인할 수 있는 표면을 슬라이스마다 남긴다**, 왜냐하면 자동 진행의 신뢰는 보고가 아니라 관측 가능성에서 오기 때문이다.

## 3. 금지선 — 「돈이 나갈 경로가 없다」의 증명

- **광고계정 쓰기 API 호출 0.** `naver_sa_writer`의 쓰기 함수 호출 경로를 신설하지 않는다 — 각 슬라이스 PR에서 diff로 증명(리뷰 검사 항목).
- **`auto_operate`·`optimizer` 값 변경 0.** 현재 전 캠페인 0/none([I])이 실집행의 유일 게이트이고, 이 계약의 어떤 코드도 그 값을 쓰지 않는다. S6의 «관할 등록»조차 `auto_operate=0`으로만 행을 만든다.
- **DB 스키마는 마이그레이션으로만** + `scripts/safe_deploy.sh --migrate` 순서(마이그→upgrade→코드→재시작). 신설 컬럼은 전부 additive nullable — 구코드가 새 스키마 위에서 그대로 돈다.
- **마이그 실패 시 수동 downgrade 금지**(§4-B ②의 처분을 따른다 — 순서 규칙상 실패 시점의 prod는 구스키마+구코드로 무결).
- **재개방(`_open_exclusion`)·제외 발사·probation 전이의 «실행» 0건** — S3는 배선까지이고, 실행 게이트(`auto_operate`)는 닫혀 있다.
- 백필은 `console_import` 행의 **라벨 컬럼만** 갱신 — `restrict_kwd_id`·라이브 상태에 무접촉.
- force-push 금지 · `git add -A` 금지(경로 지정 커밋) · 시크릿 파일 무접촉 · 네이버 API는 **읽기(GET)만**, 그것도 S5 수집 경로에 한정.
- 「전수 완료」·「준비 완료 = 성과 보장」이라 쓰지 않는다.

## 4. 슬라이스 분할 · 자동 판단 규칙 · 합격기준

### 4-A. 슬라이스 (의존 순서 · 전부 독립 배포 가능)

| S | 내용 (결손 #) | 산출물 | 독립 배포 | 선행 |
|---|---|---|---|---|
| **S1** | 학습 사슬 회복 — 탐색 stale 수리(#5) + 크론 misfire catch-up(#6) | stale 허용 기준의 명시적 상수화+우회 수리 · 판사/수확 크론의 당일 내 보충 실행 | ✅ (마이그 0 예상) | 없음 — **최우선**(첫 주 최대 리스크 제거) |
| **S2** | 제외 «임대» 스키마(#8) + 소급 백필 | `grade` 컬럼 신설(마이그 1) · 4+1 등급 부여 · `next_review_at` 기본값 규칙 · 백필 3,990행 | ✅ | 없음 (S1과 병렬 가능) |
| **S3** | 재개방=실험 배선(#11·#12) | 재개방·재제외 경로에 execute 일기 기록(harness 우회 경로 포함) · 복귀 후 성적 채점기(실측 후 신설/재사용 판단) | ✅ (라이브 검증은 점화 후 — 해당 항목 판정불능 처리) | S2 권장(등급 문맥) — 필수 아님 |
| **S4** | 파라미터 SPECS 승격(#13) + 원본 보존 연장(#14) | 게이트 숫자 4개를 `guardrail_params.SPECS`로 (아래 4-B ⑤ 표) · 검색어 원본 보존 16일→창 상한 이상으로 | ✅ | 없음. 단 `_SS_WINDOW_DAYS` hi 개정은 보존 연장 배포 «후» |
| **S5** | 파워링크 광고 제목·설명 수집(#18) | `get_ads` 응답의 버려진 절반 적재(신설 테이블·변경 이력 append·마이그 1) + C10 동결 실상 기록 정정 | ✅ | 없음 |
| **S6** | 표면·절차(#21·#3·#4) | 그룹별 제외 사용률·소진 예상일 표면 · 스코프 행 선행 검사(켤 때 0행이면 경고) · 관할 등록(`auto_operate=0`으로만) | ✅ | S2 이후 권장(등급 분포를 같은 표면에) |

**진행 규칙**: 위에서 아래로. **한 슬라이스가 막히면(리뷰 미해소·마이그 실패·전제 반증) 그 자리에 기록하고 다음 독립 슬라이스로 넘어간다** — 막힌 슬라이스가 뒤 슬라이스를 인질로 잡지 않는다. 세션 하나가 못 끝내면 체인 릴레이로 다음 세션이 이어받는다(현실 예상: **2~3세션** — 이 트랙 실적 세션당 1~3슬라이스).

### 4-B. 자동 판단 규칙 — 갈림길마다 미리 내려 둔 답

**① 적대 리뷰가 P1을 내면**: 그 슬라이스의 diff 안에서 고칠 수 있으면 같은 세션에서 수정→2R(수정 diff만). 2R에서 **새** P1이 나오거나 수정이 슬라이스 경계를 넘으면 → 그 슬라이스는 **머지 없이 닫고**(반착지 — PR은 남긴다) 부채 기록 후 다음 슬라이스로. P1=0만이 머지 조건이다.

**② 마이그레이션이 prod에서 실패하면**: 중단이다 — 롤백 시도 금지. `--migrate` 순서 규칙상 실패 시점의 prod는 구스키마+구코드로 무결하므로 **아무것도 되돌릴 필요가 없다.** 실패 로그·재개 명령을 기록하고 마이그 없는 슬라이스(S1·S3·S6 일부)로 넘어간다.

**③ 예산 소진 시**: 새 슬라이스를 시작하지 않는다. 진행 중 슬라이스는 최소 «커밋→PR»까지 착지시키고, HANDOFF에 잔여 슬라이스와 이 계약 좌표를 실어 다음 세션이 이어받는다. **반쯤 배포된 상태로 세션을 닫는 것이 유일한 금지 — 배포는 슬라이스 완결 시에만.**

**④ 실측이 계약의 전제를 반증하면**(2026-08-26에 세 번 있었다): ⓐ반증을 증거 좌표와 함께 먼저 기록 ⓑ슬라이스의 목표가 유지되면 구현만 사실에 맞춰 조정(합격기준 원문은 안 고친다 — 무의미해진 항목은 「전제 반증 — 판정불능」으로 남긴다) ⓒ슬라이스의 존재 이유가 죽으면 그 슬라이스를 건너뛰고 기록 ⓓ**계약 전체의 목적이 흔들리면 멈추고 Jino에게 남긴다** — 그건 목적 전환이 아니라 새 계약 영역이다(전역 §2).

**⑤ SPECS 승격 — 무엇을, 어떤 봉투로** (착수 세션이 발명하지 않도록 지금 확정):

| 파라미터 | 현행 | 봉투 [lo, hi] | 끝값의 근거 (전부 기존 숫자) |
|---|---|---|---|
| `_SS_MIN_CLICK` | 10 | [5, 21] | 5=파워링크 게이트 현행값 · 21=산업 표준 통계 컷([E]) |
| `_SS_WINDOW_DAYS` | 14 | [7, **16**] | 7=민감도 관례 축(ref 66 §8-5) · 16=**원본 보존 상한**([F]) — S4 보존 연장 배포 후 hi를 28로 개정 «허용»(이 문장이 그 승인이다) |
| `_PL_MIN_CLICK` | 5 | [5, 10] | 하한은 현행 유지(D구간 264건이 표본 미달의 실증, [E]) · 10=쇼핑 게이트값 |
| `_PL_WINDOW_DAYS` | 30 | [14, 90] | 14=쇼핑 창 · 90=재심사 백오프 상한 기존값 |

**승격 전 검사(각 파라미터 필수)**: `max_change_pct`를 SPECS에서 일부러 뺀 전례의 이유 — **생성기 7곳 분산 import**([F]) — 를 기준으로, **grep으로 사용처가 판정기 한 파일에 갇혀 있는지 확인하고, 분산돼 있으면 승격하지 않고 기록만 한다.** 나머지 5개(캡 4종·`_PL_GROUP_SLOT_CAP`)는 **이번 승격에서 제외** — 캡은 학습 대상이 아니라 안전 상한이고, 조정 근거(실측 유입률)가 점화 후에야 생긴다.

**⑥ 제외 등급 — 이름·경계·기본 만료** (스키마가 서려면 지금 굳혀야 한다):

| 등급 | 뜻 | 기본 만료(`next_review_at`) |
|---|---|---|
| `무관` | 상품과 무관(경쟁사 브랜드 등 — Jino 판별분 포함) | **영구**(NULL — grade로 「보류」와 구분) |
| `광의` | 관련은 있으나 우리 제품군 밖·과광폭 | **+90일** (재심사 백오프 상한 재사용) |
| `성과미달` | 관련 있으나 창 내 확정 손해 | **+30일** (백오프 `min(30×cycle,90)` 기저값 재사용) |
| `미검증` | 판정 근거 없음 | **보류**(NULL) — B층(검색량 사멸) 데이터 도래 시 재분류 |
| `오컷의심` | **제외 후에도 남은 증거가 BEP 초과** — 컷이 틀렸다는 실측이 있는 유일 부류 | **즉시 도래** — 단 재심사 «대상 목록»에만 오르고, 실행은 소유권 분리 후(전건 대행사 칸) |

**⑦ 소급 백필 3,990행의 부여 규칙** ([E] 실측 그대로): A급 16건 중 BEP 초과 13건 → `오컷의심` / BEP 미달 2건(0.46·1.31) + B 1건 + C 3건 → `성과미달` / D 264건·E 135건·이력 없는 3,571건 → `미검증` / `무관`·`광의`는 백필에서 **부여하지 않는다**(텍스트 관련성 판단은 자동 부여 근거 없음 — 신규 발사분과 Jino 판별분에만). 합계가 3,990과 일치해야 하며 불일치는 그 자체를 표면화한다. **백필은 라벨링이다 — 대행사 칸 실행 무접촉**(§3).

### 4-C. 합격기준

체크 = 증거 좌표 병기 «주장», 판정은 완료 QA. 각 항목이 «Jino가 보는 곳»을 지목한다.

- [ ] **S1-a** 배포 다음 크론 회차 로그에서 stale 차단 카운트가 사유별로 분리 집계돼 찍히고, 그 grep 명령 한 줄이 HANDOFF 최상단에 있다(정확 명령은 구현 PR 본문에 확정 기재) ⟨판정 전사 — 완료 QA(n=57, 2026-08-27 07:2x) **부분달성**: 배선 라이브 확인되나 ⓐ숫자는 전제반증(`auto_operate` 9/9=0)으로 구조적 미도래 ⓑ「grep 명령이 HANDOFF 최상단에」는 QA 시점 HANDOFF 미존재로 미충족. **미체크 유지** — 좌표 PR #487 머지 `a27d7eea`(D-NAO-258)⟩
- [x] **S1-b** 판사·수확 크론이 결손 회차를 당일 보충한 흔적(catch-up 로그 라인)이 있거나, 결손 0이면 그 카운터 키가 로그에 존재한다 — 2026-08-26 회차 통째 결손([I])의 재발 방지가 표면으로 증명 ⟨판정 전사 — 완료 QA(n=57) **달성** · PR #487 머지 `a27d7eea`(D-NAO-258)⟩
- [x] **S2-a** Jino가 HANDOFF의 1줄 관측 명령(readonly)을 실행하면 **등급 분포표**(오컷의심 13 · 성과미달 6 · 미검증 3,970 · 합계 3,990 정합)가 출력된다 — 수치가 [E]와 다르면 다른 이유가 함께 출력·기록돼 있다 ⟨판정 전사 — 완료 QA(n=58) **달성**(라이브 분포 오컷의심 13·성과미달 6·미검증 3,971·합계 3,990, 이탈 +1 사유 동시 출력) · PR #494 머지 `d9132d10`(D-NAO-259)⟩
- [x] **S2-b** 신규 제외 경로(autofire·Confirm)가 등급·만료일 없이 행을 만들 수 없음이 테스트로 고정 ⟨판정 전사 — 완료 QA(n=58) **달성**. ★착수 실측이 계약 전제를 반증: 신규 제외 경로가 「autofire·Confirm 둘」이 아니라 **넷**이라 팩토리 1지점 + 인구조사 테스트로 수리 · PR #494 머지 `d9132d10`⟩
- [x] **S3-a** 재개방·재제외 경로가 execute 일기를 남김이 테스트로 고정(harness 우회 경로 포함 — diary 호출이 브리핑 3곳뿐이던 상태의 종결) · **라이브 발생은 점화 전 불가이므로 완료 QA는 이 항목을 «배선 존재 확인 + 라이브 판정불능»으로 남긴다** ⟨판정 전사 — 완료 QA(n=59) **달성**(계약이 예정한 「배선 존재 확인 + 라이브 판정불능」으로 정상 귀결 — 미달 아님) · PR #498 머지 `12fa0c43`(D-NAO-261)⟩
- [x] **S3-b** 복귀 후 성적 채점의 존재 여부 실측 기록(있으면 재사용 좌표, 없으면 신설분 테스트) — `[미상]` 해소가 이 항목의 산출물 ⟨판정 전사 — 완료 QA(n=59) **달성**. `[미상]` 해소 = 신설(`exclusion_return_score.py`) · PR #498 머지 `12fa0c43`⟩
- [ ] **S4-a** 승인 카드/성적표 표면(prod GET 응답)에 새 파라미터 4개가 현행값·봉투와 함께 뜬다 — Jino가 그 응답에서 4행을 본다
- [ ] **S4-b** 분산 import 검사 결과가 파라미터별로 기록돼 있다(승격 4 · 보류 시 사유)
- [ ] **S5-a** readonly 1줄 명령으로 파워링크 소재 행(제목·설명·edit_tm)과 캠페인 커버 카운트가 출력된다 · 익일 크론 로그에 수집 라인
- [ ] **S5-b** ref 103 §10-3 「스냅샷 동결 미착수」가 실상(C10이 08-21부터 name·seller_tags_json 변경 추적 중 — `naver_product_meta_ingest.py:92`)으로 정정된 한 줄이 남아 있다
- [ ] **S6-a** 그룹별 사용률·소진 예상일이 표면(API/배너)에 뜨고 70/70 그룹(현재 5개 — 2026-08-26 22:1x 실측)이 무조건 빨강
- [ ] **S6-b** 스코프 0행 상태에서 켜기 절차를 밟으면 경고가 나옴이 테스트로 고정
- [ ] **공통** 슬라이스별 광고계정 쓰기 0(diff 증명) · 적대 리뷰 PASS(P1=0, 표면 변이: «일기 배선 절단»·«백필 절단» 각 1종 포함) · 마이그 전건 `--migrate` 순서

## 5. 예산

- **세션당 토큰 ≤ 400K**(리뷰·QA 위임 포함) · **예상 2~3세션**(세션당 1~3슬라이스 — 이 트랙 실적 기준). 1세션 완주를 약속하지 않는다.
- 마이그: S2·S5 각 1건(additive nullable만). 신규 상시 비용: S5의 WEB_SITE 그룹당 1콜/일(그룹 수는 착수 실측 — A5/A6 스윕 1,013콜/일보다 크면 착수 전 표면화하고 멈춤) 외 0.
- **시효**: 이 계약 자체는 마감에 걸리지 않으나, **점화 결정이 2026-10-28 뒤로 밀리면 옛 학습 재료 3,863건과의 대조가 소실**된다 — 각 세션 HANDOFF에 D-day를 1줄 병기한다. 그리고 제목·태그는 콘솔에서 누가 만지는 순간 원복 좌표가 사라지므로 S5는 늦을수록 잃는다.

## 6. 종료 조건

- **적대 리뷰**: 슬라이스마다 PR 경계 1회(스키마 변경 슬라이스는 스키마 slice 포함). 위임문에 이 계약 §3·§4-B 원문과 표면 변이 2종을 싣는다(서브에이전트는 이 문서를 못 본다).
- **완료 QA(항상)**: 슬라이스 착지마다 Sonnet high·읽기 전용·항목별 5값. 라이브 미도래 항목(S3-a 등)은 판정불능이 정상 종결이다.
- **계약 닫힘**: 6슬라이스 전부가 «달성 또는 명시 이월»로 처분되면 종합 완료 QA 1회 — 대조 대상은 §4-C 전체 + 「점화 준비 체크리스트」(안 하는 것 1~3을 제외한 모든 전제가 초록인가). **트랙 종결 QA 해당 없음**(2/7).
- **착수 규칙**: 착수 시점은 Jino가 정한다. 착수하는 세션은 이 계약 원문 + 앵커(목표이름 복사) + §4-A 순서로, 완료된 슬라이스는 건너뛰고 첫 미완 슬라이스부터 진행한다. **Jino에게 묻는 경우는 §4-B ④-ⓓ 하나뿐이다.**

## 7. 북극성 대조

1. **M 귀속**: 어느 M 체크박스도 직접 못 움직인다 — 이 계약은 **M4(L3 재개)의 «승인 필요» 칸에 적힌 Jino 결정 2건을 제외한 모든 전제를 초록으로 만드는 준비 계약**이다. S1·S3은 L4 사슬 입구 정비(M3 인접), S2는 L3 집행 원장, S4는 L2·L4 경계, S5는 L0 신규 슬라이스(§6-b M1 «안 함»이 예고한 다음 계약감), S6은 표면층.
2. **5요소**: 직접 이동 없음 — ⑤학습·상시개선의 «전제 정비»이며, **④자동화는 이 계약으로도 0이 유지된다**(28세션째 — 그대로 적는다). 움직이는 것은 점화 결정 «후»의 회전 속도다.
3. **§7 대칭**: 이 계약의 브레이크 손질은 제외를 **영구→시한부(임대)로 바꾸는 것이라 방향이 브레이크 «완화»**이고, 재개방을 학습에 태우는 S3도 같은 방향이다. 액셀 쪽 짝(S5 제목 수집)이 같은 계약 안에 있어 「제외 손 하나 = 액셀 손 하나」 규칙이 계약 구조로 지켜진다. D-NAO-85형 표류(브레이크만 강화) 요소는 0이다.

---

*이 계약은 Jino 승인 대상이다. 승인 전에는 어떤 슬라이스도 착수하지 않는다.*

---

## 부록 — 근거 실측 (2026-08-26 세션 pao-n56, prod 읽기 전용)

이 계약의 수치는 전부 아래 실측과 기존 문서(ref 66·103, 상수 목록)에서 왔다. 새로 만든 숫자는 SPECS 봉투의 조합뿐이며 끝값은 전부 기존 숫자의 재사용이다.

- **[A] 쇼핑 낭비 분포**(14일, 전환0): 0클릭 82,293건/0원 · **1–2클릭 1,684건/2,580,682원** · 3–4 84건/363,322 · 5–9 28건/203,627 · **10+ 8건/280,860** ⇒ 현행 게이트(clk≥10)가 보는 건 전환0 비용의 **8%**, 76%가 1–2클릭 구간
- **[B] 축별 90일**: 쇼핑 클릭 18,386·비용 25,735,839·전환 2,517+2,137·매출 41,072,560 ⇒ **CVR 13.7%·RoAS 1.60(BEP 1.711 미달)** / 파워링크 클릭 17,633·비용 **17,693,661**·**전환 4종 전부 0 = 측정 원리적 불가**
- **[C] 판정기 라이브**(prod 직접 실행, 창 08-13~08-26): 쇼핑 제외후보 **0**·파워링크 **0**·**승격 384**·의미단위 **4**·잔여토큰 **8**·대행사 파워링크 **16**(30일 17건/973,192원)
- **[D] 제외 원장 3,990행**: `next_review_at` **NULL 3,987** · 도래 1건(08-21) · `cycle` 전건 1 · probation·restored **0** · live alive 2,554/missing 1,365/unknown 67 · source `console_import` 3,987. **제외는 실효 있음** — 이력 419건 중 **413건이 제외 후 클릭 0**
- **[E] 대행사 컷 재판정**(419건): **A 전환있었음 16건** 광고비 110,993·전환 29·매출 291,100 ⇒ **RoAS 2.62(BEP의 1.53배)·13/16 BEP 초과·재개방 예정 0/16**. 단 2건은 컷이 옳았다(0.46·1.31) ⇒ **우리 게이트의 유일한 구멍 = 「전환 있는 적자」**. B(21clk+) 1 · C 3 · D(1–9clk) 264/579,991원 · E 135 · **이력 없음 3,571(89.5%)** ⇒ 산업 표준 통계기준(21클릭) 충족 컷은 **419건 중 1건**
- **[F] 학습 가능 파라미터 = 3개, 전부 브레이크**(`cooldown_hours`·`max_daily_auto_bid_downs`·`max_auto_up_multiple`). 제외·탐색 게이트 숫자 9개는 전부 하드코딩. ⚠️`max_change_pct`는 생성기 7곳 분산 탓 SPECS에서 «일부러 뺀» 전례
- **[G] 탐색 실태**: 시도 **3,834** = 차단 **3,607(94.1%)**/실행 159/관측 68 · 기간 **07-21~07-30 «딱 10일», 이후 0건** · 차단 사유 bleeding 716 · **「소급채점 stale」 약 1,890(52%)** · CTR경보 124
- **[H] MOP 벤치마크**(운영주체 `[미상]`): *"레버가 전부 2주 실지출 이력 게이트 뒤에 있다 — 시동의 본질 = 돈이 아니라 이력 쌓기"* · 우리 계정 실측 **ROAS 1.80**(켬1.80/끔1.66) ⇒ 벤치마크 최고 성적이 우리 BEP 1.711을 **5% 넘는 수준**
- **[I] 정지 상태**: 우리 마지막 실쓰기 `update_bid` **07-30 10:20** / 대행사 `external_bid_change` **08-25까지 731건** · 일기 `blocked` 3,863건 **07-30에서 끊김** · 지혜 후보 48건 **`judged_at` 전건 NULL** · **2026-08-26 08:45 판사 크론 회차 통째 결손**(프로세스 다운·misfire catch-up 없음)

**`[미상]`으로 남긴 것**: 복귀 후 성적 채점기의 존재(S3-b가 해소) · WEB_SITE 그룹 수(S5 착수 실측) · stale 허용 기준의 적정값(S1이 상수화하며 근거 병기)

**2026-08-26에 확인된 정정 2건**: 의미단위 화이트리스트는 **이미 채워져 있음**(적대 리뷰 P1-1 반영 2026-08-21, residual 제외는 불변식 근거 있음) · 재개방은 harness 우회라 **execute 일기가 안 생김**(diary 호출 970·984·1004 전부 observe/브리핑 — 코드 재확인)
