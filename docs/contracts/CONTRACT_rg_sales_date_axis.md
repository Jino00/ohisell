# 계약 — 2P(로켓그로스) 순이익을 «판매일 축»으로 낸다

> 초안 2026-08-22 21:5x KST · 사슬 「쿠팡-손익정합」 n=4 · 세션 `f0fe421f`
> ★**승인됨 2026-08-22 22:00 KST** — Jino 원문 *"그러자"*. (§1 승인 지점 ① — 트랙 목표를 바로잡으므로 새 계약이었다)
> 상위 목표: **「쿠팡 손익정합 목표」** — 정본 `.claude/memory/chains/쿠팡-손익정합.GOAL.md`

---

## 0. 「쿠팡 손익정합 목표」 (Jino 원문 — 이 계약도, 이 트랙의 다음 계약도 여기에 복종한다)

**발단 (2026-08-21)**
> ***"sellC 에 보면 어디에도 오픽스의 자료가 안나오고 있어"***
> *"오픽스 2P도 나오게 한다는거지?"*
> *"이렇게 진행하면 우리가 1P, 3P진행하면서 겪었던 모든 과정(원가매칭, 광고비 매칭등 모든 과정)을 다시 진행해야 하는거야?"*

**확정 (2026-08-22)**
> *"지금 문제는 2P에 대한 내용이 https://sellc.ohitech.co.kr/의 데시보드와 서브메뉴에 전혀 안보이는게 문제인거잖아."*
> *"그리고 2P의 순이익을 내기 위한 모든 계산이 1P, 3P, 스마트스토어에 했던 것과 같이 정확히 나와야 하고."*
> *"어제 어떤 제품이 몇개가 팔리고 그 판매분의 정산공제, 원가, 세금, 기타비용등을 빼고 남는 이익이 있잖아. 다른 판매와 같이 2P도 그걸 보자는거지"*

**⇒ 목표: 2P(오픽스 로켓그로스)가 1P·3P·스마트스토어와 「같은 대접」을 받는다.**
**① 자기 메뉴와 자기 화면을 갖는다   ② 그 날 팔린 것의 손익을 그 날 자리에서 본다(판매일 축).**

**★이 트랙의 모든 작업은 이 목표에만 복종한다.** 트랙 헤더가 다른 목표를 인용하고 있으면
그건 헤더가 틀린 것이다(2026-08-22 실사고 — 헤더가 1P 트랙의 목표를 인용한 채 트랙이
「8/8 종결(달성)」까지 갔다).

---

## 1. 이번 계약의 범위 / 이번에 안 하는 것

### 이번에 한다 — 목표 ②의 본체

**2P 순이익 계산에서 「정산공제」 항을 정산 인식일 축의 주간 통짜에서 판매일 축으로 옮긴다.**

지금 상태(2026-08-22 prod 실측, 증거 `.claude/memory/chains/쿠팡-손익정합.INVENTORY.md` 부록 A):

| 항 | 값(08-21) | 축 |
|---|---:|---|
| 매출 | 187,120 | ✅ 판매일 |
| 원가 | 39,352.40 | ✅ 판매일 |
| **정산공제** | **153,058** | ❌ **정산 인식일 — 08-17~23 «한 주 전체 통짜»** |
| 광고비 | 30,650 | ✅ 집행일 |
| **= 순이익** | **−32,673** | **부호가 뒤집혀 있다(실제는 +41,000~+57,000)** |

네 항 중 셋은 이미 맞고 **한 항만 틀렸다.** 그 한 항이 그날 매출의 81.8%다.

**산출물 4가지**
1. 옵션별 **건당 물류비 단가**(`delivery`·`warehousing`)를 그날 판매수량에 곱하는 SA
2. **판매수수료 요율**을 완결 정산주기에서 실측·캐시하고 매출에 곱하는 SA (3P `option_fee_rate` 패턴 차용)
3. `rg_channel_pnl.py`의 `commission` 항을 위 둘로 교체 + **못 덮는 부분의 자백 필드**
4. **트랙 재개** — `docs/tracks/completed/track_coupang-rg-net-ledger.md`를 `active/`로 되돌리고
   이름·목표·합격 M항목을 「쿠팡 손익정합 목표」 기준으로 다시 쓴다

### 이번에 안 한다 (인접하지만 범위 밖 — 발견해도 고치지 말고 앵커 `## 이월`에 적는다)

| 안 하는 것 | 소관 |
|---|---|
| **①2P 전용 메뉴·화면 신설** | **이 트랙의 다음 계약.** 순서가 이렇다 — 숫자가 틀린 채 화면을 늘리면 틀린 숫자를 더 많은 자리에 뿌린다 |
| 교환 배송손익 배선 (인벤토리 표2) | 이 트랙의 다음 계약 |
| 3PL 고정비 배분 (`fixed_cost: "0"` 하드코딩) | 이 트랙의 다음 계약 |
| **옵션 단위 정산 데이터의 «수집» 개선** (커버리지 70% 문제의 근본 해결) | 별건(윙 페처). ★단 이번 계약은 그 결손을 **화면에서 자백**해야 한다 |
| 비-PA 광고비 · 매출 정산화 보정 (2P·3P 공통 결손) | 별건(대시보드 집계층) |
| 로켓1P 계산서 축의 원가·프로모션 `ZERO` 하드코딩 | `docs/tracks/active/track_coupang-rocket-1p.md` |
| 3P·네이버에 원가 커버리지 게이트가 없는 것 | 별건(3P 손익 엔진) |
| 종합조망(`intelligence.py`)의 RG 경로 | ★같이 옮겨야 하는지는 **착수 실측으로 판정**한다(D-CPP-49 전례: 소비처가 인계보다 하나 더 많았다). 판정 결과를 계약에 추가 기록한다 |

---

## 2. 판단기준 (항상 A, 왜냐하면 ○○)

1. **항상 «그날 판 것»에 «그것에 붙는 비용»을 붙인다** — 왜냐하면 Jino가 보려는 것이 그것이고,
   기간 통짜는 그 기간의 어느 하루도 설명하지 못한다(08-18은 매출의 **625%**를 뺐다).
2. **항상 못 덮는 부분은 「모름」으로 말한다 — 0으로도, 통짜로도 채우지 않는다** — 왜냐하면
   0으로 채우면 순이익이 부풀고 통짜로 채우면 부호가 뒤집힌다. **둘 다 조용히 틀린다.**
3. **항상 요율·단가의 출처를 행에 싣는다** — 왜냐하면 실측값과 기본값이 같은 얼굴을 하면
   화면이 거짓 확신을 준다. 3P가 이미 `basis="default_rate"`로 이렇게 한다(`option_fee_rate.py:97`).
4. **항상 장부 총액을 보존한다** — 일별로 쪼개도 **창 합계가 실제 청구와 맞아야 한다.**
   왜냐하면 안 맞으면 「날짜별로 예뻐졌는데 총액이 틀린」 상태가 되고, 그건 지금보다 나쁘다.
5. **항상 3P의 기존 구조를 먼저 읽고 차용한다** — 왜냐하면 요율 캐시·폴백·basis 표시가
   라이브에서 검증돼 있고(정산 실측 661건 전수), 새로 만들면 같은 함정을 다시 밟는다.
   ★단 **RG는 「매출×요율」이 아니라 「수량×정액」이 섞인 구조**다(물류비는 개당 정액,
   판매수수료만 요율) — 패턴은 베끼되 식은 베끼지 않는다.
6. **원가 커버리지 게이트는 유지한다** — 왜냐하면 이건 2P가 3P·네이버보다 **나은** 유일한
   항목이고(그 둘은 원가를 몰라도 순이익을 그냥 낸다), 「3P와 같게」를 이유로 없애면 후퇴다.
7. **판정은 화면에서 한다** — 왜냐하면 「값이 계산된다」로 합격을 내면 사람에게 닿는 마지막
   칸이 검사되지 않는다(§2 ★, 전역 하네스).

---

## 3. 금지선

- **추정으로 채우고 자백 없이 순이익을 «단정»하지 않는다.** 요율·단가가 없으면 `basis`를 실어 보낸다.
- **원가 커버리지 게이트를 낮추거나 없애지 않는다.**
- **3P·네이버·로켓1P의 계산을 건드리지 않는다.** 공용 함수를 고쳐야 하면 그 자체가 설계 재검토 신호다.
- **정산 원장·판매 원장을 «쓰지» 않는다.** 이번 작업은 읽기만 한다(수집 개선은 범위 밖).
- **prod 배포는 `scripts/safe_deploy.sh`로만.** 직접 scp/rsync 금지(D-NAO-49). DB 변경 있으면 `--migrate`.
- **PR 병합은 `scripts/safe_merge.sh`로만.** CI가 결제정지로 빨간불이면 **서명을 직접 확인**하고 `--force`+자백.
- **트랙 목표를 이 계약 밖에서 바꾸지 않는다.** 바꾸려면 새 계약이다.

---

## 4. 합격기준 — 전부 `https://sellc.ohitech.co.kr/` 화면에서 판정한다

> 「API가 응답한다」·「테스트가 통과한다」는 합격이 아니다.

- [ ] **ⓐ 「어제」 화면에서 오픽스 RG 순이익이 판매일 축으로 뜬다.**
      08-21로 재현 시 **−32,673원이 아니라 +41,000 ~ +57,000원 범위**의 흑자.
      (범위 근거: 부록 A-5 감도 분석. 착수 시 요율을 실측해 범위를 좁힌 뒤 확정한다)
- [ ] **ⓑ 같은 주의 날짜들이 «서로 다른» 정산공제를 갖는다.**
      08-17·18·19·20·21 다섯 날이 지금은 전부 `153,058`이다 — 다섯 값이 달라야 한다.
- [ ] **ⓒ 장부 총액이 보존된다.** 완결 정산주기(예 08-10~08-16)에서
      **Σ(일별 정산공제) vs 계정 단위 실제 청구액**의 차이가 화면에 «수치로» 보이고,
      차이가 임계 초과면 행이 자백한다. **차이를 숨기고 0으로 만들지 않는다.**
- [ ] **ⓓ 판매수수료 요율의 출처가 행에 표시된다** — 실측 요율인지 기본값인지 화면에서 구분된다.
- [ ] **ⓔ 옵션 단위 커버리지가 얇은 창에서 순이익을 단정하지 않는다.**
      커버리지 수치가 화면에 뜨고, 임계 미달이면 「모름」 또는 하한으로 말한다.
- [ ] **ⓕ 30일 창에서 값이 바뀐 이유가 설명된다.** 종전 −409,639원 대비 변화량과 그 근거가
      증거 문서에 기록된다(같은 창을 두 방식으로 낸 대조표).
- [ ] **ⓖ 회귀 없음** — 3P·네이버·로켓1P·자사몰의 값이 불변임을 라이브로 대조한다.
- [ ] **ⓗ 트랙이 「쿠팡 손익정합 목표」로 다시 선다** — 트랙 파일이 `active/`에 있고,
      계약 헤더 `목표:`가 §0 원문이며, 합격 M항목이 이 목표에서 유도된 것이다.

---

## 5. 완료 판정 (Jino 지시 2026-08-22: *"QA 완료되면 완료판정하도록"*)

1. **적대 리뷰** (PR 경계 의무, 별도 기 1기) — P1=0이면 PASS. 변이 주입 필수이고,
   **변이 중 하나는 「값이 화면 픽셀이 되는 마지막 칸」을 끊는 것**이어야 한다(전역 §4 ★).
2. **완료 QA** (별도 기 · Sonnet high · **읽기 전용**) — 대조 대상마다 **따로** 판정한다:
   - `판정(계약 §4 합격 ⓐ~ⓗ): <5값>` — 항목별 「실행한 명령 → 관측(타임스탬프) → 판정」
   - `판정(「쿠팡 손익정합 목표」 ②): <5값>` — 그 날 팔린 것의 손익을 그 날 자리에서 보는가
   - `판정(「쿠팡 손익정합 목표」 전체): <5값>` — ①(메뉴·화면)이 남아 있으므로 **부분달성이 정상**이다
   어휘 5값: 달성 / 부분달성 / 미달 / 목적전환 / 판정불능.
3. **QA는 처분을 지정하지 않는다** — 어긋난 것을 적을 뿐, 「Jino 추인 필요」를 쓰지 않는다(전역 §2 ★).
4. **트랙 종결 QA는 이번에 돌지 않는다** — ①이 남아 트랙이 M/M이 아니기 때문이다.
5. 판정은 세 곳에 남긴다: 앵커 · HANDOFF 「완료 QA」 절(원문 그대로, 미달 포함) · 트랙 `확인:` 줄.

---

## 6. 예산

| | |
|---|---|
| 세션 | 1~2 (이번 세션 + 필요 시 1) |
| 토큰 | 약 600k (조사분 이미 소진 약 400k 별도) |
| 모델 | 구현 Sonnet(단일파일 범위 명확) / 다중파일·설계 Opus / 적대 리뷰·완료 QA 별도 기 |
| 중단 조건 | 착수 실측에서 **요율이 안정적이지 않아 ⓐ 범위를 못 좁히면** 중단하고 Jino에게 보고한다 — 그 경우 「요율 실측 가능성」 자체가 선행 계약이 된다 |

---

## 7. 착수 전 확정해야 할 것 (승인 후 첫 슬라이스)

1. **요율 실측** — `sale_fee` 요율이 완결 주기 5개에서 8.42~10.23%로 흔들린다. 원인이
   ①정산 인식 지연 ②카테고리 믹스 변화 ③VAT 처리 중 무엇인지 가려 ⓐ의 범위를 좁힌다.
2. **소비처 전수** — `rg_channel_pnl`의 `commission`을 읽는 곳이 대시보드 외에 몇 곳인지
   grep으로 전수 확인(D-CPP-49에서 인계가 셋이라 했는데 실제로는 넷이었다).
3. **보관비 처분** — WING1은 0.095%지만 **WING2는 전체 차감의 71%**다. 판매일 귀속이
   원리적으로 안 되는 비용이라 「계정 기간비용으로 분리 표시」가 기본안이나, WING2에서
   결과를 지배하므로 실측 후 확정한다.
