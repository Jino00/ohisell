# CONTRACT — 배송비 자(尺) 정합 (**D-NAO-283 · 승인됨 2026-09-01**)

> **목표이름: `배송비 자(尺) 정합 목표`** · 체인 `배송비-자` · 앱 제목 「배송비-자 N」
> 승인: Jino, 2026-09-01 12:2x KST — 4지선다 중 **「ⓐⓑⓒ 전부」** 채택.
> 초안 출처: `HANDOFF_shipping-yardstick-recon_20260901.md` §6 (체인 n=1이 조사만 하고 남긴 미승인 초안).
> 트랙: `docs/tracks/active/track_naver-ad-optimization.md` (PAO)

---

## §0. 북극성 대비 — 이 계약이 미는 것 (D-NAO-226·227)

| # | 검토 | 결과 |
|---|---|---|
| 1 | 어느 M인가 | ref 82 §6 표의 M에 직접 안 속한다. **§3 L2 표의 두 행**을 움직인다 — 「상품 단위 BEP 확보 230/854」(ⓒ가 +166 방향) · 「★총이익의 자」(D-NAO-230 각주 계열). **M4·M5의 전제**다: 자가 틀리면 카나리 성적과 「지혜→총이익 기여」가 같이 틀린다 |
| 2 | 궁극 목표 5요소 | **③총이익 방향**만. ①개연성·②논문 무관, **④자동화·⑤학습은 0** |
| 3 | §7 액셀·브레이크 대칭 | ★**이번엔 액셀 쪽이다.** §7이 경계하는 「브레이크가 액셀보다 쉬워 ROAS 방어로 기운다」(D-NAO-85)의 **반대 방향** — 눌려 있던 BEP를 되돌려 광고 상한을 올린다. D-NAO-234·236이 **매출 쪽 자**에서 고친 병(`PLAN_naver-profit-yardstick-review.md` §1-6 「§7 대칭이 자 때문에 뒤집혀 있다」)의 **원가 쪽 판본** |
| 4 | 관련 절 원문 | §목차 훑고 §1·§2·§3·§6·§7 읽음. `PLAN_naver-profit-yardstick-review.md`(D-NAO-230)는 **다른 자**(매출 귀속 보정계수) — **중복 아님**. 내 것은 공헌이익의 물류비·판매가 항 |

---

## §1. 목표 / 이번에 안 하는 것

### 목표 — 누가 뭘 못해서, 되면 뭐가 달라지나

BEP 배송비를 **Jino가 설명한 실제 배송 구조**에 맞춘다. 지금은 세 자리에서 자가 눌려 있어, **벌 수 있는 광고를 못 켜고 있다**.

> Jino 원문 (2026-08-31 21:32 KST):
> **"이건 폴드 세븐 플립 세븐 다 똑같이 우리가 엔배송으로 진행을 하면은 네이버가 자기들 멤버십 가지고 있는 사람들한테는 배송비를 제해주고 우리한테 그 배송비를 주는 거. 그런데 내일 배송 같은 경우는 우리가 배송을 하는 경우야. 우리가 엔배송의 재고가 없을 때 내일 배송으로 돌리게 되거든. 내일 배송으로 돌리게 되면은 우리가 한진택배를 통해서 물건을 납품하게 돼. 물건을 배송하는데 우리가 한진택배에 내는 비용은 1900원이지. 그런데 고객한테 삼천원을 받으니깐 우리는 1100원이 남는 거야. 이런 구조로 배송은 진행이 돼."**

- **ⓐ 클램프 해제** — `net_ship = max(0, 지불 − 수취)`의 `max(0, ·)`를 없앤다. 내일배송은 **1,100원이 남는** 구조인데 현행은 그 이익을 0으로 지운다.
- **ⓑ 주문 0건 폴백 교체** — 「수취 0 가정 = 배송비 전액 차감」 대신 **같은 `group_product_no` 형제 실측**을 쓴다.
- **ⓒ 판매가 폴백 ③ 추가** — `orders median` → `mapping.selling_price` → **`naver_product_meta_current.discounted_price`**.

### 이번에 안 하는 것 (옆면의 정의 — 금지선과 다르다)

| 안 하는 것 | 소관 |
|---|---|
| 최근 10건 표본 크기 조정 | 이 계약의 이월 (근거 부족 — n=1이 자기 가설을 반증했다) |
| 타채널(1·2·7) 배송방식 판별 불가 100% | 미지정 (BEP엔 무해 — `_naver_order_rows`가 `channel_id=6`으로 거른다) |
| 광고계정 쓰기 · PAO 스위치 · 캠페인 정지 해제 | Jino 콘솔 작업 |
| 4매입 `52308509`를 광고 소재로 등록 | Jino 콘솔 작업 |
| PAO 승인→집행 단절 원인 규명 | 체인 `pao-논의` 「목적관철 목표」 |
| RG 판매일 축 테스트 2건 수리 (저장소 전체 머지 차단 중) | RG 판매일 축 트랙 |

---

## §2. 판단기준 ("항상 A, 왜냐하면 ○○")

1. **항상 실제로 받은 돈을 받은 것으로 센다** — 왜냐하면 「보수적」이라는 이름의 편향도 편향이고, 이 자는 **한 방향으로만** 틀려 있다(물류비 과대 → 공헌이익 과소 → BEP 과대 → 벌 수 있는 광고를 끈다). 같은 논증이 이미 D-NAO-168 주석에 적혀 있다.
2. **항상 주문 0건 폴백은 같은 `group_product_no` 형제의 실측을 쓴다** — 왜냐하면 형제는 같은 상품의 용량·기종 변형이라 배송 구조가 같고, 「모른다」의 가장 정직한 답은 상수가 아니라 **가장 가까운 관측**이다.
3. **항상 형제가 없으면 현행 폴백을 유지한다** — 왜냐하면 근거 없는 개선은 개선이 아니고, 폴백을 바꾸는 것 자체가 목적이 아니다.
4. **항상 판매가 폴백은 `selling_price<=0`일 때만 발동한다** — 왜냐하면 `orders` 실거래 median이 가장 정직한 값이고(n=1이 「4매입이 2매입 판매가를 물려받았다」는 자기 주장을 여기서 반증했다), 폴백은 **스스로 은퇴**해야 한다.
5. **항상 세 출처를 행 단위로 기록한다** — 왜냐하면 출처를 안 남기면 다음 세션이 「이 숫자가 실측인가 추정인가」를 물을 수 없고, 그게 이 파일 머리말(`bep_calculator.py:7-8`)이 이미 부채로 적어 둔 바로 그 구멍이다.
6. **항상 배포 전에 prod에서 «인메모리 읽기 전용»으로 전후를 재고 시작한다** — 왜냐하면 614개 상품의 자가 한꺼번에 움직이고, D-NAO-236이 같은 방식으로 「하한이 방향을 1건도 안 바꾼다」를 배포 전에 확정한 선례가 있다.

---

## §3. 금지선

- **정가(`sale_price`)를 판매가로 쓰지 않는다** — `discounted_price`만. (그룹 `52308509`에서 둘의 차이는 19,900 vs 27,500 = 38%다.)
- **타채널 주문을 BEP에 끌어들이지 않는다** — `channel_id=6` 필터를 건드리지 않는다.
- **prod 배포는 `scripts/safe_deploy.sh`만**. DB 변경이 있으므로 `--migrate` 순서 강제(마이그 → `alembic upgrade head` → 코드 → 재시작).
- **광고계정에 쓰지 않는다** — 이 계약은 «자»만 고친다. 스위치·캠페인·입찰에 손대지 않는다.
- **다른 체인이 만지는 파일에 손대지 않는다** — 착수 시점 살아 있는 세션: `발주예측 n=8` · `승인배선 n=2`.

---

## §4. 합격기준 — 라이브 증거 시나리오

**표면**: 콘솔 `/naver-ad` 성과뷰 **⑤BEP 구성 표**(`NaverAdPerformance.tsx` `BepRowView`) + `/naver-ad` 리포트의 **BEP 목록 표**(`NaverAdReport.tsx`).

- [ ] **①** 콘솔 성과뷰 BEP 표에서 **`13687558209`·`13687558210`(그룹 52308509)에 BEP ROAS 숫자가 뜬다** — 지금은 두 행 다 `bep_roas=NULL`
- [ ] **②** `bep_roas IS NULL` 행이 **202 → 36 이하** (prod 재계산 후 실측)
- [ ] **③** 클램프 걸리던 **81개 행의 BEP가 낮아진다** — 재계산 전후 대조, **평균 −5% 이상**
- [ ] **④** 판매가 있던 **555행의 `selling_price` 전건 불변** (회귀 0 — 폴백이 실거래를 밀어내지 않았다는 증거)
- [ ] **⑤** **물류비 출처**(실측/형제/기본폴백)가 **화면과 API 응답 둘 다에서** 구분된다
- [ ] **⑥** 적대 리뷰 **PASS(P1=0)** — **표면 절단 변이 1개 이상**(전역 §4: 최종 산출물까지 가는 경로를 끊는 변이)

★체크는 **주장**이고 증거 좌표를 병기한다. 최종 판정은 완료 QA 몫이다.

### §4-1. 착수 실측 — 계약이 선 자리 (2026-09-01 12:3x KST, prod 읽기 전용)

| 항목 | 관측값 | 인계 주장 대비 |
|---|---|---|
| `naver_product_bep` 행수 | 758 | ✅ 일치 |
| `bep_roas IS NULL` | 202 | ✅ 일치 |
| `selling_price<=0` (전부 `cost>0`) | 201 | ✅ 일치 |
| `logistics_cost = 0` (클램프 자국) | **81** | ✅ 일치 |
| `logistics_cost < 0` | **0** (클램프가 살아 있다는 직접 증거) | — |
| `sp<=0` & `meta.discounted_price>0` | **166** ⇒ 202−166 = **36** | ✅ 일치 |
| 주문 0건 행 중 **주문 있는 형제 보유** | **161 / 201** | 신규 실측 |
| 조인 키 `meta.channel_product_no` ≡ `bep.channel_product_id` | **721/758 (95.1%)** · 중복 0 · 대안 키 `origin_product_no`는 2건뿐 | **[미상] 해소** (models.py:4419-4423) |
| 조인의 독립 증거 (판매가 대조) | 555행 중 **정확 일치 510 (91.9%)** | ✅ 재현 (n=1: 509/555) |
| 마지막 재계산 | `calculated_at = 2026-09-01 07:30:16` | — |

---

## §5. 예산

토큰 ~250k · 3시간.

## §6. 종료 조건

**적대 리뷰**(코드 경계 — `bep_calculator.py` + 마이그 + 라우터/프론트) → **완료 QA**(3대조: 계약 §4 / Jino 승인 원문 / 트랙 궁극 목표) → 트랙 `확인:` 줄.
왜냐하면 이 변경은 코드 슬라이스를 갖고, 614개 상품의 자를 한꺼번에 움직여 PAO 입찰 상한에 곧장 닿기 때문이다.

## §7. 계약 범위의 명시적 추가 1건 (승인 후 착수 세션 판단 — 자백)

`naver_product_bep`에 **`logistics_basis`** 외에 **`price_basis`**(orders/mapping/meta)도 같은 마이그레이션에 넣는다.
합격기준 ⑤는 물류비 출처만 요구하지만, **ⓒ가 판매가 출처를 2개→3개로 늘리는데** 어느 것이 쓰였는지 행에 안 남기면 `bep_calculator.py:7-8`이 이미 부채로 적어 둔 구멍을 **한 칸 더 키운다**. 컬럼 1개·마이그 1개의 비용이고 §1 「안 함」 어디에도 걸리지 않는다. 이 줄이 그 판단의 사후 가시성이다.
