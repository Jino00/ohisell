# CONTRACT_pao_purpose_and_hands — 목적 관철: 판정 규칙·손·되돌림·저노출 극복의 종합

```
목표이름: 목적관철 목표
상태: 승인 대기 (v6 — Jino 결정 5건 반영 + P0 실측에 따른 처치 전환)
결정 번호(승인 시): D-NAO-280
트랙: docs/tracks/active/track_naver-ad-optimization.md
      + 인수: docs/tracks/active/track_pao-ui-ux.md (Jino "인수해도 돼" 2026-08-30 22:05 —
        이 계약이 그 트랙에도 확인줄·진행률을 남긴다. 등록부 pao-uiux.jsonl n=2 인수 행 기록 완료)
저술: Fable (구조 산출물 저자 슬롯) · 옮겨 적기·P0 전환은 리드 인라인
발단(Jino 원문, 2026-08-30):
  "이제 전체적으로 보자. 1번 계약, 1번 이슈를 포함해서 운영을 위해 손을 달고 쿠팡에서 잘 팔리는데도
   불구하고 노출을 일으킬 수 없는 경우 어떻게 극복방안을 종합해서 계약서를 작성하자" (21:45)
  "어, 모두 보완해줘. 그리고 의도적인 제외는 왜그런거야? 나는 다 넣었으면 좋겠는데?" (22:00)
  "이 세션이 최우선 세션이야. 여기서는 모든 다른 세션의 소관을 가져와서 작업해도 돼" (22:0x)
  "인수해도 돼" (pao-uiux, 22:05)
  "너의 추천대로 꺼두자" / "전부 할껀데" / "예산은 나한테 AVA를 통해서 제안을 줘" /
  "이건 이 광고 그룹에만 적용되는 이야기잖아. 우리한테 이런 걸 정할 로직이 있어야 하는거 아니야?" (22:18)
  "쿠팡건은 빼자" (22:30)
  "그래. 그렇게 수정하자" (P0 전환 승인, 23:0x)
```

**★「목적축 목표」 흡수**: 이름만 확정되고 6요소가 없던 1번 계약을 이 계약이 흡수한다. 이슈1(목적 대비 구현 간극)·이슈2(손 부재)·이슈3(저노출 사례)은 **같은 병의 세 단면**이고, 셋을 가르면 「기능은 만들고 손은 안 만드는」 병을 계약 구조가 재생산한다. 「목적축 목표」라는 이름은 소멸하고 이 계약이 승계한다.

**★v6 전환 — P0의 처치가 뒤집혔다**: v5까지 P0의 중심은 「Z폴드8와이드 입찰을 R=1,300으로 올려 정보를 산다」였다. **2026-08-30 22:38·22:55 KST 실측이 그 전제를 반증했다** — 아래 §0. 계약의 나머지(판정 규칙·손·되돌림·눈·학습)는 **그대로 유효하며, 오히려 이 실측이 그 필요성을 증명했다**: 규칙 없이 감으로 갔으면 리드가 1,300원 상향을 추천했고 하루에 수만~수십만 원을 태웠을 것이다. 산식이 그것을 막았다.

**★v3 소관 사용 원칙**: 인수(pao-uiux) 소관의 산출물은 원 트랙에도 확인줄·진행률로 기록한다. **소관 개방은 워킹트리 개방이 아니다**(§5).

**★v1 자기 정정 — 계약 자신의 비대칭**: v1은 「올리는 쪽」만 촘촘히 설계하고 **「잘린 것을 되살리는 쪽」을 통째로 비웠다.** 액셀·브레이크 대칭을 강조해 놓고 계약이 비대칭으로 쓰였다는 지적을 수용해 **되돌림 축을 독립 묶음(P2)으로** 세운다.

**문서 지위**: `docs/references/` 114(이슈1)·115(이슈2)·116(이슈3)은 **증거 문서로 존치**한다(정정·참조만 소급 — P1). **이슈3은 「사례 연구(증거)」**다. 살아있는 값의 유일 등록부는 정본 `docs/PAO_OPS.md` §12.
★**이슈3의 교차 채널 서사(「쿠팡 1위인데 네이버 −95%」)는 상품 동일성이 데이터로 확정되지 않은 채 종결된다** — 서사로는 유효하나 근거로는 미확정(ref 116 §8-4의 자백이 해소되지 않는다). 확정하려면 `track_product-connection-map` 소관의 매핑이 선행이며 **이 계약은 그것을 요구하지 않는다**(Jino 22:30 "쿠팡건은 빼자").

**M 귀속·북극성 검토 (D-NAO-226·227 — §목차 훑음, §1·§5·§6·§7·§8·부록A 읽음)**
1. **어느 M인가** — P4(자·대칭)·입찰 산식은 M2 성격(판정 규율), P5의 6-1 사전 배선은 M3 성격, P8-a는 M4 그 자체(조건부). 손·눈·되돌림(P2·P3)은 **M 밖 인프라이되 M4의 전제**다. P3의 UI/UX 인수는 M 밖 인프라 + 인수 트랙(10/12)의 잔여 승계. 표류 아님의 근거: 2026-08-30 하루에 「기능→손 없음 / 계획→손 없음 / 처방→손 없음」을 세 번 밟았다 — M4로 가는 길 자체가 손 부재에 막혀 있다.
2. **5요소 중 움직이는 것** — ③총이익 방향(「모름」을 「나쁨」으로 처분하던 기본값 교정 + 총이익의 화면 표면화)·④자동화의 전제(끄는 손·발의 손·되돌림 손)·⑤학습의 사전 배선(P5). ④ 실집행 재개 확대 자체는 조건부 P8-a(Jino 결정 2건 선행).
3. **§7 대칭 검사** — 이 계약의 핵심이 그 대칭의 **코드 수리**(P4)이자 **계약 구조 수리**(P2 되돌림 축)다. 브레이크(지출 백스톱)는 약화하지 않는다. 입찰 산식은 액셀에 «근거»를 주는 장치이지 봉투를 푸는 것이 아니다.
4. **관련 절 원문** — P4·P5 위임문에 북극성 §5-3·§7·§6 M2~M4 행 원문을, P2 위임문에 이슈2 §2 표 해당 행 원문을, P3 위임문에 `HANDOFF_pao-uiux-ownership-bands_20260830.md` 핵심 절을 싣는다(서브에이전트는 이 문서들을 못 본다).

---

## §0. ★v6 P0 전환의 근거 — 실측이 처방을 뒤집었다

**2026-08-30 22:38 KST (prod 코드 직접 import 실행)**

| 항 | 값 | 산출 |
|---|---|---|
| **E** 손익분기 CPC | **610원** | 자기층 rpc(raw) 1,154.78 → 캠페인 prior와 수축 혼합 1,209.66 → 상품가 환산 1,212.67 ÷ BEP 1.9757 = 613.79 → 610 |
| **F** 최소노출가 | **1,050원**(MOBILE) / 1,640원(PC) | 오늘자 사다리 |
| **R** 밴드 진입가(4위) | **1,300원**(MOBILE) / 1,410원(PC) | 오늘자 사다리 |
| 현재 입찰 | 810원 | `ad_bid_amt`, `use_group_bid_amt=False` |

⇒ **E(610) < F(1,050) < R(1,300)** — **§2-E 산식의 ③분기(자동 탐침 금지·손해 구매 카드)**. 보이는 것 자체가 이미 적자이고, `cold_start_bid_decider`를 라이브로 돌려도 MOBILE·PC 둘 다 **`not_viable`**로 판정된다.

**그리고 이 그룹은 「모르는」 상태가 아니다** — 90일 누적 클릭 **2,508**·전환 **168**·매출 **289만원**, `confident=True`. `visibility.evidence_window()`가 이미 **`active=False`**(사유: 표본 충분·예산 소진·클릭 확보)를 반환한다. **코드 자신의 판단으로도 「정보 구매가 필요한 콜드 소재」가 아니라 「이미 충분히 측정된 소재」다.**

**정점 때도 적자였다**: W32(08-10~16) CPC = 745,046 ÷ 529 = **1,408원** vs rpc **1,154원**. 「보이면 팔린다」는 맞지만 **「팔려도 손해」**였다. ⇒ 08-18·08-24 대행사 하향은 **적자를 줄인 판단이었을 수 있다** — ref 116의 「대행사가 죽였다」 서사는 이 실측으로 완화된다.

**원가 검증 (22:55 KST)**: BEP 1.9757은 **산식·저장값 기준으로 맞다**(직접 재현 16,800/8,503.32 = 1.97573). 판매가 16,800원의 분해 — 원가 6,089.60(36.25%)·수수료 987.84(5.88%, `delivery_case` 최고신뢰 등급)·물류비 368.87(2.20%)·VAT 몫 850.37(5.06%) ⇒ 공헌이익 8,503.32(50.61%).
★**오히려 원가가 stale하다**: 이 상품 레시피(`cost_recipe` id=70, 10라인, 2026-08-28 승인)의 표준원가는 **6,220.30원**인데 `product_master.cost_price`는 **6,089.60원**(07-28 Jino 확정)에 머물러 있다 — **레시피 승인이 상품 원가로 자동 반영되는 배선이 코드에 없다.** 반영하면 BEP는 **≈2.004**로 더 높아진다 ⇒ **결론을 뒤집지 않고 강화한다.**
**라이브가 독립적으로 확증**: 이 그룹 30일(07-31~08-29) 광고비 **2,093,046원** · 직접 ROAS **1.131** · 직간접 합 **1.512** — BEP 1.9757에 크게 못 미침.

★**유령 정정**: 리드가 근거로 인용한 「Z폴드8 적자 −578,822원」은 **이 상품·네이버와 무관**하다 — `LESSONS_LEARNED.md` #303이 그것을 **쿠팡 로켓1P 채널 손익**이라고 이미 정정해 뒀고, 08-16 세션이 이름만 보고 네이버로 잘못 봉합한 건이었다. 리드가 그 정정을 못 보고 다시 꺼냈다. **결론에는 영향 없다**(라이브 ROAS 1.13이 독립 근거).

⇒ **P0-ⓒ가 「입찰 상향」에서 「처분 결정 재료 정리」로 바뀐다.** 산식 §2-E는 **폐기하지 않는다** — 다른 상품·다음 판정에 그대로 쓴다. 이 상품에 대해서는 산식이 「사지 마라」를 낸 것이고, **그것이 산식이 작동한 증거다.**

---

## §1. 목표 / 이번에 안 하는 것

**누가 뭘 못하나** — 네 겹:
- **엔진이 「나쁨」과 「알 수 없음」을 못 가른다.** 전환 0이면 `est_ROAS=0<BEP`가 자동 성립해 하향 트리거인데, 같은 무표본이 상향에선 판단 보류다(ref 116 §5 — DOWN 표본 하한 없음, UP 4중 요건). 표본 없는 그룹은 **내려갈 수만 있다.**
- **사람이 엔진을 만질 손이 없다.** 켜진 캠페인을 API로 못 끄고, 입찰 제안을 새로 만드는 입구가 없고(쓰기 엔드포인트는 승인·실행 둘뿐), 급한 불은 네이버 콘솔 직행 = 대행사와 같은 경로.
- **잘린 것을 되살리는 손이 통째로 없다.** 제외 재개방은 라이브 `probation` **0행** — 「14일 대기」가 아니라 「영영 안 열림」. 킬스위치 3종 중 2종이 배포·재시작으로만. 대행사가 내리면 보기만 하고 못 되돌린다(`feed_reapply`는 재적용기가 아니라 분류기).
- **올릴 때 «얼마로»를 정하는 로직이 없었다** — 그래서 리드가 개별 판단을 냈고, 그건 §4-7(전역 규칙)·D-NAO-65 ③의 정신에 어긋났다.

**되면 뭐가 달라지나**: ①「모름」이 탐침(한정 예산의 정보 구매) 대상으로 분류되고 **목표가가 산식으로 나온다** ②사람이 우리 콘솔에서 **끄고·발의하고·되살리고·일괄 지정한다** ③판정불능·최소노출가 미달·바닥 고착·총이익이 **화면 숫자로 보인다** ④예산 판단 재료가 **AVA 브리핑으로 사람에게 온다**.

**이번에 안 하는 것** (인접하지만 범위 밖 — 금지선과 다르다: 금지선=영원히 위험, 비목표=이번엔 안 함)

| 안 함 | 이유·소관 |
|---|---|
| **교차 채널(쿠팡) 전부** — 매핑·외부 수요 신호·탐침 가중 | Jino 결정(22:30 "쿠팡건은 빼자"). 소관: `track_product-connection-map` |
| **예산 변경 «개방»** | AVA «제안»(P3)으로 대체 — 트랙 계약 「안함」 무충돌, **개정 승인 불필요** |
| **`product_master.cost_price` ↔ `cost_recipe` 동기화 배선** | ★§0에서 발견. **원가 메뉴 트랙 소관** — §9에 요청 명세로 넘긴다 |
| M4 재개 확대(카나리 밖 해제·소유권 분리) | 조건부 P8-a — 북극성 §8-①② Jino 결정 2건 선행 |
| 상품 grain 근본 수리 | 조건부 P8-b — M2 풀링 + 상품 BEP 커버리지 선행 |
| 배포 전용 상수 438개 **전면** 개방 | P5에서 **선별**만 — 일부는 고정이 안전 |

---

## §2. 극복방안 — 설계

### 2-A. 「성과가 나쁨」과 「성과를 알 수 없음」을 가르는 전역 규칙

**원리: 가시 노출 상태에서 잰 나쁨만이 나쁨이다.** 최소노출가 아래에서 잰 성과는 성과가 아니라 **결측**이다.

**상태 「판정불능(저노출)」의 정의** (전역 상수만 — 캠페인 id 하드코딩 0건, D-NAO-65 ③):
> ⓐ **노출이 구조적으로 눌려 있다** — (ⓐ1) 실효 입찰 < 당일 최소노출입찰가(`market_bid_probe.py::fetch_exposure_min` — ★**표본 없이도 판정 가능한 유일한 신호**, 매일 수집 중) **또는** (ⓐ2) 정착창 일평균 노출 < 자기 이전 정점 창의 전역 비율 하한(바닥 고착).
> ⓑ **전환 표본이 판정 하한 미만** — 정착창 전환 < 전역 하한.
> (상수값은 구현 시 근거와 함께 제안 — 이 계약은 값을 발명하지 않는다.)

**처분 규칙 넷**:
1. **성과 판정 기반 하향 동결** — 판정불능 상태에서 `est_ROAS<BEP`는 「나쁨」의 증거로 소비 금지. **단 지출 절대액 기반 백스톱(출혈밸브·예산 상한·킬스위치)은 그대로 산다** — 「모르면 올려라」가 위험한 이유가 이것이고, 돈이 새는 것은 표본 없이도 사실이다. **동결되는 것은 «성과의 해석»이지 «지출의 지혈»이 아니다.**
2. **탐침 후보 편입** — 판정불능은 방치가 아니라 정보 구매 대상(2-B). ★단 §0이 보였듯 **판정불능이 아닌 그룹(표본 충분)은 이 경로에 안 들어간다** — 그것이 이 규칙의 정확도다.
3. **「진짜 나쁨」의 판정면** — 정착창에서 (입찰 ≥ 최소노출가 ∧ 노출 ≥ 하한 ∧ 전환 표본 ≥ 하한)인데 보정ROAS < BEP → **나쁨 확정, 하향 허용**. DOWN 경로(`auto_operator.py::_intraday_loss_leash`·CPC 급등 게이트)에 **표본 하한 신설**. 기존 종료 판정(`not_rank` 소재 병리·`not_serving` 연속 무노출)은 유지 — 「올려도 소용없음」 확정 그룹은 탐침에서도 제외된다(**진짜 나쁜 것의 상향을 막는 두 번째 벽**).
4. **소급 적용** — 이 규칙은 **이미 생성된 pending 제안에도 적용**된다: 판정불능 그룹에 대한 절감·하향성 pending은 실행하지 않고 반려·보류하며 사유를 원장에 남긴다. 규칙이 제안보다 늦게 태어났다는 이유로 규칙 이전의 제안이 실행되면, 규칙은 앞으로만 좋고 지금 굶는 그룹은 못 지킨다.

### 2-B. 탐색 = 정보를 사는 것 — 기제 판정

| 판정 | 근거 |
|---|---|
| 탐색 기제는 정확히 이 상황군을 위해 설계돼 있다 | `exploration.py` — 트리거 사유 원문이 「클릭 표본 부족 = ROAS 판단불가 = **증거 구매** 대상」(D-NAO-70). 순위 피드백 상태 기계·적응 스텝·경제성 상한·자가 치유 완비 |
| 탐침 예산의 실물도 이미 있다 | `visibility.py::evidence_window`(D-NAO-83) — 창당 7일 5만원·클릭 15 확보 시 종료·고수요 판별. «정보 구매의 봉투»가 코드로 존재 |
| ★제안 0건의 원인은 결함이 아니라 «가입 조건» | `exploration.py::exploration_trigger` ①이 **정착창 클릭 < 10**만 후보로 받는다. **클릭은 충분한데 전환을 모르는 «웜 크랙»이 탐색과 핫셋 사이에 떨어져 있다** |
| 증거창도 같은 열쇠 문제 | `evidence_window`의 표본 결핍 판정도 **클릭 열쇠** — 전환 결핍을 못 본다 |

⇒ **처방 = 자격 열쇠를 「클릭 표본 부족」에서 「판정불능(2-A)」으로 확장**(잇기) + DOWN 하한 신설(소수리). **새 레인·새 봉투 발명 없음.** 탐침의 계획성(대상·우선순위·잔여 예산 일 산출·표면화)을 여기 붙인다.
★**탐침은 제외 재개방과 한 몸이다** — 제외로 잘린 쿼리는 입찰을 올려도 안 돌아온다. 되살릴 손(P2) 없이 「정보를 산다」는 성립하지 않는다.

### 2-C. 교차 채널 — 이번 계약 밖

Jino 결정(22:30)으로 전부 제외. 매핑·외부 수요 신호는 `track_product-connection-map` 소관으로 남는다. **이 계약의 모든 판정·근거는 네이버 자체 데이터로 완결된다.**

### 2-D. 탐침 목표 입찰가 — 전역 산식 ★

**세 항 전부 기존 재료다 — 새 발명 0.**

| 항 | 정의 | 좌표 |
|---|---|---|
| **F** 가시성 하한 | 당일 최소노출입찰가 — 이 아래는 정보가 아예 안 사진다 | `market_bid_probe.py::fetch_exposure_min` |
| **R** 밴드 진입가 | 이익 스팟 밴드 하단(rank 4.0) 가격 — 탐침의 목적이 **전환 표본**이므로 목표는 F가 아니라 R | `market_bid_probe.py::fetch_position_ladder` + `exploration.py::_EXPLORATION_TARGET_BAND` |
| **E** 경제성 상한 | 클릭당 손익분기 CPC = 수축 rpc ÷ BEP. 굶은 창의 rpc 문제는 이미 풀려 있다 — `resolve_rpc`가 자기 실측과 상위 층 prior를 **연속 수축**으로 섞는다(D-NAO-119) | `bid_ceiling_calculator.py::ceiling_from`·`resolve_rpc`·`resolve_bep` |

**산식**: `target = min(E, R)`, 단 `target ≥ F`여야 발사.
1. **E ≥ R** → target = **R** (밴드 하단까지만 — 과열밴드 진입 금지).
2. **F ≤ E < R** → target = **E** (경제성 안에서 보이는 데까지).
3. **E < F** → ★**자동 탐침 금지.** 「보이게 하려면 손해」이므로 **«손해 구매 카드»로 사람에게 올린다.** 손해를 살 수 있는 것은 사람뿐이다.
4. **스텝**: 봉투 그대로 — 사이클당 탐색 상한 30%·쿨다운 2h로 **스텝 분할** 도달. **봉투 면제 신설 없음**(§5).
5. **정지**: `evidence_window` 종료 조건 재사용(클릭 15 확보 / 7일 5만원 소진 / 창 비활성) + 래더 `stop_observe`·`not_rank`·`not_serving`.
6. **되돌림**: 실패 판정 시 `probe_revert.py::run_settlement` 관례 재사용 — 탐침 «전» 입찰 복원. 복원값 < F면 「복원 = 판정불능 재진입」을 카드에 표기(사람 결정).
7. **사다리 미수집 그룹**: R 미상 → fail-closed, 기존 래더의 적응 스텝 경로만.

★**«손해 구매 카드»의 정보 가치 근거 3종 — 전부 네이버 자체 데이터**:
1. **과거 정점 실적 (직접 증거)** — 정점 창의 노출·전환·그때의 입찰·순위. **매핑이 필요 없는 자기 데이터이고 「보이면 팔린다」의 직접 증거**다.
2. **판정불능 지속 일수** — 오래 모를수록 기회비용 추정이 커진다.
3. **동류 대비 초과 낙폭** — 같은 기간 캠페인 전체 낙폭 대비 이 그룹의 낙폭. **시장 전체가 꺼진 게 아니라 이 그룹만 꺼졌다**면 회복 여지가 그룹 내부 요인에 있다는 신호다.

★**⚠️ 카드가 「사라」를 뜻하지 않는다** — §0이 실증했다: 근거 3종이 모두 성립해도(정점 실적 있음·지속 일수 큼·초과 낙폭 −92%), **E가 F보다 한참 낮으면 사지 않는 것이 정답**이다. 카드는 «사람이 판단할 재료»이지 «사라는 권고»가 아니다. **이 구분을 카드 문구에 명시한다.**

**Phase 배치**: P0 = 산식 1장 + 적용(규칙 층, 사람이 손으로 계산) → P4 = 산식의 코드화(기존 부품 조립).

---

## §3. 범위 — Phase 구조 (각 Phase는 자체 완료 QA로 닫힌다 — 어느 접두 구간에서 멈춰도 유효한 종결)

### P0 — 즉각 처치·확인 — 선행조건: 없음(승인 즉시)
- **ⓐ 제외 11건 생사 확정** — `live_state` 7건 `missing`·4건 `alive`의 뜻을 가른다(「콘솔에서 제거됨」 vs 「우리 조회가 그 grain을 못 봄」, D-NAO-263류). 네이버 콘솔 실사 대조.
- **ⓑ pending 10건 처분** — `negative_keyword` 5건: 2-A ④ 소급 규칙으로 반려(사유 원장 기록). `search_term_promote` 5건: 실행기 부재이므로 P6 전까지 만료 예정임을 기록(처분하지 않음 — 기록만).
- **★ⓒ (v6 전환) 「이 상품 처분 결정 재료 정리」** — v5까지의 「입찰 상향」을 §0 실측이 반증했다. 대신:
  - 실측 3종(E/F/R · 라이브 ROAS 1.131 · 원가 분해)을 **1장으로 정리해 Jino에게 제시**한다.
  - **광고 밖 선택지를 재료로 병기**한다 — ①상품명·썸네일(쇼핑 광고는 키워드가 없고 **상품명이 곧 광고 제목**이라 전환율의 주 레버다) ②자연 검색 노출(광고비 0) ③묶음 구성으로 객단가 ④가격·원가.
  - ★**전략 판단은 Jino 몫이다** — 이 계약은 재료까지 낸다. 다만 **「입찰 상향은 하지 않는다」는 산식의 판정이고 그것은 우리 몫**이다.
  - **처분이 정해지면 그것이 P0의 종결**이다(광고 유지·축소·중단 중 무엇이든).
- **ⓓ 비입찰 원인 관측** — 08-16 첫 꺾임은 입찰 무변동에 노출 −31%였다. 처분이 「유지」로 나면 이 항목을 D+7 관측에 포함한다(시장 사다리 1~4위 추이·형제 소재 대비 대조축). 처분이 「중단」이면 이 항목은 해당 없음으로 닫는다.
- **ⓔ `optimizer='none'` 임시 전환** — Jino 확정("너의 추천대로 꺼두자"). S0 기간 우리 쓰기 전면 차단으로 엔진 되돌림 위험 0. **복귀 시점 = 처분 결정 후 Jino 확인 1줄.**
- **ⓕ ★원가 동기화 결손을 원가 메뉴 트랙에 넘긴다** — §0에서 발견(`cost_recipe` 승인 → `product_master.cost_price` 자동 반영 배선 부재). §9의 요청 명세로 기록하고 **이 계약은 고치지 않는다.**
- 예산: **세션 1 · 토큰 35만 · 2일.**

### P1 — 정본·정정 전파 (문서만) — P0와 병렬 가능
- 정본 `PAO_OPS.md` §12를 **유일 등록부**로 개정: ①삼중 계상 4건(학습재료 만료·마지막 실집행 N일·profit_scorecard 표면·밀린 액셀 카운터)을 §12로 모으고 ref 114·115가 참조를 가리키게 정정 ②등록부 분류를 **「단계(눈·자·손·표면·되돌림·학습) × 상태(없음/값이 빔/꺼짐/삶/잡을 것 없음)」 행렬**로 채택([[existence-gate-is-not-maturity-gate]] 편입 — 새 발명 아님) ③7고리 체크리스트에 **「이상 시 손: 있음/없음」 열** 복원(이 열이 떨어진 자리에서 이슈2가 자라났다) ④이슈3을 「사례 연구(증거)」로 지위 명기 ⑤ref 114 §4-4의 29.7%에 정정 주석 연결(구속 조건 3후보 미분리 — 게이트/스코프/손).
- **ref 111 반증 4건의 처분 완결** — 죽은 카드·`changed_at`=KST에 더해: §14 부품생존 레시피의 prod 실행 불가 해소(실행 가능한 명령으로 정정) · 「하루 밀림」·「크론 35 vs 36」의 정본 반영 확인.
- ★**§0의 정정 2건도 여기서 전파**: 「대행사가 죽였다」 서사의 완화(ref 116) · 「Z폴드8 적자 −578,822원」이 이 상품과 무관하다는 사실(LESSONS #303 재확인).
- 예산: **세션 1 · 토큰 40만 · 1일.**

### P2 — 되돌림·킬스위치·발의 — «손 묶음» ★계약 비대칭의 해소 — 선행조건: P0-ⓐ
- **H1** `auto_operate` 쓰기 API + 화면 버튼 + 감사 로그.
- **킬스위치 나머지 2종** — `auto_operator.py::AD_BID_ROUTING_ENABLED`(배포로만)·콜드스타트 `NAVER_CS_DRY_RUN`(env+재시작, prod 라이브)의 런타임 스위치화.
- **H2 사람 발의 제안 입구** — `POST /proposals` 신설(그룹·방향·크기·근거를 받아 제안 생성, 이후 기존 승인→실행 경로). **D-NAO-13 하드체크·가드레일 봉투 그대로 통과**(봉투 면제 신설 없음 — 큰 폭은 스텝 분할).
- **제외 재개방 입구** — 건별 해제(원장 상태 전이 + `naver_sa_writer::delete_restricted_keywords` + 감사 기록). 자동 재심사가 `auto_operate` 게이트 뒤라 발화 0인 구조도 정리(**사람 입구는 게이트와 독립**).
- **대행사 되돌림 «대응» 버튼** — 외부 하향 감지 행에서 「복원 발의」 원클릭(H2 재사용 — **자동 복원 아님**).
- **원장 무효화(void) 버튼** — 기존 `DELETE /search-term/executions/{id}` 프론트 배선.
- **H5 스코프 캠페인 단위 일괄 지정.**
- **`AD_BID_CANARY_CAMPAIGNS` 개명 또는 주석 강화** — 의미가 「Confirm-only 제외 목록」으로 반전됐는데 이름이 그대로라 **오늘 두 조사기가 오독**했다.
- ★**라이브 검증 분리**: 코드·테스트·변이는 지금 완성하되, **발의→실행 라이브 1건은 `optimizer='ours'` 복귀 후**(P0-ⓔ의 귀결)로 합격기준에 명시.
- 예산: **세션 3 · 토큰 180만 · 4일** (PR 2 — 쓰기 표면은 리뷰 밀도가 생명).

### P3 — 눈 + pao-uiux 인수 + AVA 예산 브리핑 — P2와 병렬 가능
- ★**`track_pao-ui-ux.md` 인수 명시** — PAO UI/UX 소관을 이 계약이 흡수한다(Jino 22:05, 등록부 n=2 인수 행 기록 완료·과거 행 무수정). 화면 배선을 **이 계약이 직접 만든다.** **P3 산출물은 그 트랙 헤더에도 확인줄·진행률(현행 10/12)로 기록한다** — 두 트랙이 갈라지면 아무도 진행률을 못 센다.
- **착수 필독** — P3 착수 세션은 `frontend/src/pages/NaverAdPerformance.tsx`와 `.claude/memory/HANDOFF_pao-uiux-ownership-bands_20260830.md`를 먼저 읽는다. 선행 산출물(성과 화면 밴드 분리, PR #574·#577 머지·prod 배포) **위에 얹는 것**이지 새로 그리는 것이 아니다.
- **미호출 GET 4종 렌더** — `exclusions`(P0-ⓐ·P2 재개방의 표면)·`exclusion-slots`·`bm/agency-ops`(P2 대응 버튼의 표면)·`bm/snapshot` + `ignition_preflight` 렌더.
- **`profit_scorecard` → 화면 배선** — 일일 총이익 스코어카드를 콘솔에. `pao_scope_roster.py::_profit`과 **산출 경로가 둘임을 화면에 병기**(둘이 다른 숫자를 낼 수 있다는 사실 자체가 정보다).
- **카운터 4종** — 판정불능 수·최소노출가 미달 수·바닥 고착 수·「마지막 실집행 이후 N일」.
- **값-빔 감사** — P1 행렬의 「있으나 값이 빔」을 검사하는 정기 항목(`launch_target_rank` NULL류). 가능하면 `scripts/check_pao_canon.py` 확장으로 기계 검사.
- ★**AVA 예산 브리핑** (Jino "예산은 나한테 AVA를 통해서 제안을 줘") — `expert_briefing_builder.py::build`에 **「예산 병목 판정」 섹션**을 얹는다: 「지금 예산이 병목인가 아닌가 + 근거」(소진율·`budget_pacing` 데이터). ★**병목이 아닐 때 「아니다」라고 말하는 것**이 첫 산출물이다(현행 소진율 6.3% — 병목은 입찰). 주기 = 기존 크론 `generate_expert_desk` 08:05(새 채널 불요). **금지선 무충돌**: 자문·관찰모드가 코드 구조로 강제돼 있고(`expert_ledger.py` C3 — `NaverProposal` import 자체가 금지) 이것은 «개방»이 아니라 «제안»이므로 **트랙 「안함(예산 변경 개방)」을 건드리지 않는다 — 트랙 계약 개정 승인 불필요.**
- 예산: **세션 2.5 · 토큰 130만 · 3일** (PR 1 — 읽기 표면은 큰 PR 허용).

### P4 — 자·대칭 + 산식 코드화 — 선행조건: P0 재료 + P2 킬스위치
- 2-A 판정불능 상태(전역 상수·근거 병기) + DOWN 표본 하한 + 탐색·증거창 자격 확장 + 탐침 계획성 + **pending 소급 재판정 규칙의 상시화**(P0-ⓑ는 1회 수동, 여기서 규칙화).
- **§2-D 산식의 코드화** — `ceiling_from`·사다리·밴드 상수 조립. 신설 상수는 **태어날 때부터 SPECS**.
- 첫 명령은 실측 2건: 해당 그룹 정착창 clk · 29.7% 구속 조건 분해(게이트만 풀면 얼마가 실제로 풀리나).
- 예산: **세션 2 · 토큰 120만 · 3일** (PR 1~2 — 판정 경로 수리라 적대 리뷰 2R 상정).

### P5 — 학습·측정·상수 확장 — 선행조건: P4
- **정밀 한계ROAS 무실집행 점등** — `response_curve_builder.py`는 순수 함수이고 탄성 입력이 **견적 API 샘플**(`fit_elasticity`의 `estimate_samples`)이라 **실집행 데이터가 입력에 아예 없다** ⇒ 점등(곡선 산출·기록·표면화)은 실집행 없이 가능하다. 곡선의 «참» 검증은 실집행 대조 필요 ⇒ **관측·표면화까지만** 편입, α 실적용은 P8-a 조건부.
- **6-1 사후→사전 «배선만 미리»** — `wisdom_apply.py::propose_param_changes`가 `outcome_profit`을 참조하도록 배선. 출구는 여전히 «제안»(금지선 안), 원료가 빌 동안은 **no-op임을 테스트로 고정.** 원료가 생기는 날 받을 그릇을 먼저 놓는다.
- **주말·공휴일 판정면 라벨**(M2 S2-ⓐ 설계 실행).
- **상수 선별 SPECS 편입** — 기준: ⓐ**운영 판단이 걸린 것** ⓑ**다파일 사본**(단일출처화 우선 — 정착창 4파일·`EX_PRESSURE_RATIO` 2파일). 1차 후보: `EX_PRESSURE_RATIO`·정착창 창 길이·`EX_MIN_CAMPAIGN_CLK`·`DEFAULT_ELASTICITY`·`_PROBATION_DAYS`·`_HARVEST_LOOKBACK_DAYS` + P4 신설 상수. **438 전면 개방은 하지 않는다** — 비편입 사유를 목록화.
- **축소 가능 표기**: 주말·공휴일 라벨·기존 상수 편입 일부는 «없어도 이 계약의 목적은 달성» — 기간 압박 시 1순위 이월.
- 예산: **세션 2 · 토큰 110만 · 3일.**

### P6 — 승격 executor — 선행조건: P2
- `naver_sa_writer`에 **키워드 «생성» writer 신설**(현행 쓰기 9종에 생성 없음 — pending 280+만료 320건의 근본 원인). 쓰기 표면 확장이라 적대 리뷰 2R·변이 무겁게, **생성은 사람 승인 카드 경유만**(자동 생성 개방은 범위 밖).
- **축소 가능 표기**: 나선 차단과 독립인 별개 병 — 이월해도 목적 훼손 없음(단 pending 만료는 계속된다).
- 예산: **세션 2 · 토큰 110만 · 3일.**

### P8 — 조건부 2건 (발동 조건이 차면 이 계약 안에서 착수 — 착수 전 슬라이스 설계 1장)
| | 내용 | 발동 조건 | 착수 시 첫 산출물 |
|---|---|---|---|
| **P8-a** | **M4 재개 확대** — 카나리 밖 `optimizer` 해제·α 실적용 | **Jino 결정 2건**(북극성 §8-①② — 해제 범위·소유권 분리) | 확대 절차서(M4 합격기준·자의 각주 D-NAO-230 준수) |
| **P8-b** | **상품 grain 근본 수리** — 목적 grain(상품) ↔ 현행 grain(캠페인·그룹) | M2 풀링 가동 + 상품 BEP 커버리지 확대(현행 미확보 624그룹) | ★**설계 문서 선행**(구현 아님 — 매우 크다). 설계 승인 후 구현 |
- 예산(발동 시): P8-a 세션 2·100만 / P8-b 설계 1·50만(구현은 별도 상한).

---

## §4. 판단기준 (항상 A, 왜냐하면)

1. **항상 산출물마다 「손 좌표」(무엇을 어디서 클릭·실행하면 그것이 움직이는가)를 설계 시점에 명기하고, 완료 QA가 그 손을 실제로 누른다** — 왜냐하면 2026-08-30 하루에 「기능→손 없음 / 계획→손 없음 / 처방→손 없음」을 세 번 밟았고, **손 없는 산출물은 총이익에 0원**이기 때문. **제1원칙.**
2. **항상 「모름」은 나쁨이 아니라 결측으로 처분한다** — 왜냐하면 최소노출가 아래에서 잰 `est_ROAS=0`은 성과가 아니라 관측 실패이고, 결측을 나쁨으로 처분하는 기본값이 **죽음의 나선의 수학적 원인**이기 때문.
3. **항상 성과 해석은 동결해도 지출 백스톱은 살린다** — 왜냐하면 「모르면 올려라」의 위험은 진짜 나쁜 것의 상향인데, **돈이 새는 것은 표본 없이도 사실**이라 지혈과 해석은 다른 층이기 때문.
4. **항상 있는 기제를 잇는 것을 새 기제 발명보다 먼저 검토한다** — 왜냐하면 탐색·증거창·시장 사다리·외부변경 감지가 전부 실재하고, 제안 0건은 기제 부재가 아니라 **자격 열쇠의 문제**였기 때문.
5. **항상 사람의 개입은 우리 원장을 통과시킨다** — 왜냐하면 네이버 콘솔 직행은 대행사와 같은 경로라 **귀속·학습이 오염**되기 때문.
6. **(휴면) 항상 교차 채널 신호는 순서(우선순위)까지만 쓰고 판정에는 넣지 않는다** — 대상이 이번 계약을 떠났으나 **경계를 미리 갖고 있는 것이 싸므로 삭제하지 않는다.**
7. **항상 전역 규칙으로만 쓴다** — 왜냐하면 개별 캠페인 하드코딩(D-NAO-65 ③)은 **이 상품을 살리고 다음 상품을 죽이기** 때문.
8. **항상 자르는 손을 셀 때 되살리는 손을 같이 센다** — 왜냐하면 v1 자신이 올리는 쪽만 설계하고 되돌림을 통째로 비웠고, 이 비대칭은 코드만이 아니라 **계약·조사 문서에서도 재발하는** 이 트랙의 기본 결함 모드이기 때문(제외 원장 3,986행 전건 `excluded`·`probation` 0행이 그 증거).
9. **항상 «얼마»는 산식이 내고, 사람은 산식이 못 정하는 것(손해 구매·봉투 밖)만 정한다** — 왜냐하면 값을 개별 판단으로 내는 순간 §4-7이 그 자리에서 죽고, 리드의 「1,300원대」가 그 실증이기 때문.
10. ★**항상 「사라는 카드」와 「판단 재료 카드」를 구분한다** — 왜냐하면 §0에서 근거 3종이 모두 성립했는데도 **정답이 「사지 마라」**였고, 카드를 권고로 읽으면 그 판정이 뒤집히기 때문.

## §5. 금지선

- **「모르면 올려라」 전역 금지** — 상향은 탐침 봉투(예산 상한·종료 조건·쿨다운) 안에서만. **봉투 면제 타입 신설 금지.**
- **캠페인·그룹 id 하드코딩 0건**(D-NAO-65 ③) — 사람의 수동 처치는 사람 행위라 이 금지선 밖이되, **코드에는 특례 0건.**
- **홀드아웃 없이 발견을 판정면에 주입 금지** — 판정면 주입은 A/B 등급만.
- **`optimizer≠'ours'` 캠페인 쓰기 절대 금지**(D-NAO-13) — 이 계약은 그 하드체크를 약화·우회하지 않는다. **사람 발의 제안도 같은 게이트를 통과한다.**
- **지출 백스톱(출혈밸브·예산 상한·킬스위치) 약화 금지** — 판정불능 상태는 그 어느 것의 면제 사유가 아니다.
- **판정불능 그룹에 트래픽을 자르는 pending 제안 실행 금지** — 반려·보류만.
- **되돌릴 수 없는 집행·봉투를 «푸는» 방향은 사람 승인**(D-NAO-172) — 자동의 출구는 제안까지. **대행사 되돌림 «대응»도 자동 복원이 아니라 사람 발의다.**
- **prod 배포는 `safe_deploy.sh`만 · DB는 마이그레이션만 · 커밋은 경로 지정.**
- **정본 §12 밖에 살아있는 값 등록부를 새로 만들지 않는다.**
- ★**인수한 것은 «소관»이지 «남의 워킹트리»가 아니다** — pao-uiux 세션의 워크트리(`~/.claude-worktrees/ohisell/pao-uiux-n1`)·브랜치(`feat/pao-uiux-n1`)는 건드리지 않는다. 우리 브랜치에서 `origin/main` 기준으로 작업한다. **소관 개방·스코프 확장과 무관하게 유지되는 금지선이다** — Jino가 연 것은 소관이지 작업 중인 트리가 아니다.

## §6. 합격기준 (체크박스 — 각 항목이 «표면»과 «손 좌표»를 지목한다)

**P0**
- [ ] 제외 11건의 생사 판정(`missing`의 뜻 포함)이 관측 일지에 건별로 실려 있다. *표면: `docs/references/` 일지.*
- [ ] `negative_keyword` pending 5건이 반려 상태이고 사유가 원장에 있다. *손: 콘솔 반려. 표면: 제안 목록.*
- [ ] ★**「이 상품 처분 결정 재료」 1장**이 존재하고 실측 3종(E/F/R · 라이브 ROAS · 원가 분해)과 광고 밖 선택지 4종이 실려 있으며, **Jino의 처분 결정이 트랙에 D-N으로 기록**돼 있다. *표면: 일지 + 트랙 파일.*
- [ ] §2-D 산식 1장이 존재하고, 이 그룹 적용 결과(E<F ⇒ ③분기 ⇒ 「사지 않음」)가 근거와 함께 기록돼 있다. *표면: 일지.*
- [ ] `optimizer='none'` 전환이 콘솔에서 이뤄지고 복귀 조건이 트랙에 적혀 있다. *손: 콘솔 optimizer 버튼. 표면: 트랙 파일.*
- [ ] 원가 동기화 결손이 §9 요청 명세로 기록되고 원가 메뉴 트랙에 전달됐다. *표면: §9 + 그 트랙 파일.*

**P1**
- [ ] `PAO_OPS.md` §12가 유일 등록부(단계×상태 행렬 + 「이상 시 손」 열)이고 ref 114·115의 중복 계상 4건이 §12 참조로 바뀌었다. *표면: 정본 §12 + `scripts/check_pao_canon.py` 통과.*
- [ ] ref 111 반증 4건이 처분 표로 완결됐고 §14 레시피가 prod에서 실행 가능한 명령으로 정정돼 있다. *표면: 정본 §14 — 명령 실행 결과.*
- [ ] §0의 정정 2건(대행사 서사 완화·「−578,822원」 무관)이 ref 116과 정본에 반영돼 있다. *표면: 그 문서들.*

**P2**
- [ ] 콘솔 버튼으로 `auto_operate`를 끄고 켤 수 있고 감사 행이 남는다. *손: `NaverAdScope.tsx` 버튼. 표면: 배지 전환 + 감사 로그.*
- [ ] 소재 레인·콜드스타트 스위치가 배포·재시작 없이 바뀌고 현재값이 화면에 보인다. *표면: 설정 화면.*
- [ ] 콘솔 발의 폼 → 승인 → 실행이 `naver_change_log` 행까지 닿는다. **★`optimizer='ours'` 복귀 후 판정** — 복귀 전엔 코드·테스트·변이(되돌림 절단 포함)로 «입구 생존»까지만 체크하고 «복귀 대기»로 명시 이월. *손: 발의 폼. 표면: 제안 카드 + change_log.*
- [ ] 제외 1건이 콘솔에서 해제돼 원장 전이 + 네이버 반영이 확인된다(대상 0건이면 변이 테스트로 입구 생존 증명 + 「대상 0건」 명시). *손: 해제 버튼. 표면: 원장 + 네이버 콘솔.*
- [ ] 외부 하향 감지 행의 「복원 발의」 버튼이 H2 경로 제안을 생성한다. *표면: 외부변경 화면.*
- [ ] void 버튼·스코프 일괄 지정이 살아 있고 「스코프 밖」 죽은 카드 신규 발생 0이 주간 §12 SQL로 관측된다. *표면: 각 화면 + §12.*
- [ ] `AD_BID_CANARY_CAMPAIGNS`가 현재 의미를 말하는 이름/주석이 됐다. *표면: 리뷰 diff.*

**P3**
- [ ] GET 4종 + preflight가 콘솔에 렌더되고, 일일 총이익 스코어카드가 화면에 뜨며 산출 경로 이원 병기가 있다. *표면: 콘솔.*
- [ ] 카운터 4종(판정불능·최소노출가 미달·바닥 고착·마지막 실집행 N일)이 화면에 있다. *표면: 콘솔/커맨드센터.*
- [ ] 「값이 빔」 검사가 감사 항목 + 검사 명령으로 존재하고 `launch_target_rank` NULL이 잡힌다. *표면: 검사 출력.*
- [ ] AVA 데스크 브리핑에 「예산 병목 판정」 절이 실재하고, 병목이 아니면 「아니다 + 근거」가 출력된다. *표면: 전문가 데스크 브리핑(크론 08:05 산출물).*
- [ ] pao-uiux 트랙 헤더에 이 계약 세션의 확인줄과 진행률 갱신이 있고, P3 산출물이 선행 화면 위에 얹혔음이 diff로 확인된다. *표면: `track_pao-ui-ux.md` + 리뷰 diff.*

**P4**
- [ ] 판정불능 상태가 진단 응답에 사유와 함께 뜨고, 판정불능 그룹에 `est_ROAS=0` 단독 근거 하향 제안이 생성되지 않음이 회귀로 고정된다. *표면: `GET /api/naver/ad/diagnosis` + 제안 목록.*
- [ ] 판정불능 그룹이 탐침 후보 목록에 들고 봉투 잔여·소진이 표면에 뜬다. *표면: 탐침 카드 + 봉투 카운터.*
- [ ] 산식이 코드로 살아 탐침 카드에 target·F/R/E 세 항과 분기 근거가 표기되고, ③분기 카드에 **「사라는 권고가 아니라 판단 재료」** 문구가 있다. *표면: 탐침 카드.*
- [ ] 실측 2건(정착창 clk·29.7% 구속 분해)이 기록돼 있다. *표면: 일지 또는 §12.*

**P5**
- [ ] 한계ROAS 곡선이 실집행 없이 산출·표면화되고 «관측까지·적용은 P8-a» 경계가 명시돼 있다. *표면: 콘솔/일지.*
- [ ] 6-1 배선이 존재하고 원료 공백 시 no-op이 테스트로 고정돼 있다. *표면: 테스트 + 제안 생성 경로 diff.*
- [ ] 주말·공휴일 분리 집계가 성적표 응답에 실재하고 항등식(평시+주말·공휴일=전체)이 1주 표본에서 일치한다. *표면: 성적표 API.*
- [ ] SPECS 편입 목록(편입·비편입 사유 병기)이 정본에 있고 편입분이 화면에서 바뀐다. *표면: 가드레일 설정 화면.*

**P6**
- [ ] 승격 제안 1건이 콘솔 발의·승인 → 실행 → 네이버 콘솔 키워드 실재까지 닿는다. *손: 승인 카드. 표면: change_log + 네이버 콘솔.*

**공통**
- [ ] 각 Phase 종료마다 완료 QA 판정 원문이 HANDOFF·PAO 트랙 `확인:` 줄에 있다(P3은 인수 트랙에도). *표면: 트랙 파일들.*

## §7. 예산

| Phase | 세션 | 토큰 | 달력 | 비고 |
|---|---|---|---|---|
| P0 | 1 | 35만 | 2일 | 처분 재료·산식 손계산 (v6 전환으로 축소) |
| P1 | 1 | 40만 | 1일 | 문서만 · P0와 병렬 |
| P2 | 3 | 180만 | 4일 | PR 2 |
| P3 | 2.5 | 130만 | 3일 | PR 1 · AVA 포함 · P2와 병렬 |
| P4 | 2 | 120만 | 3일 | 산식 코드화 포함 · 리뷰 2R 상정 |
| P5 | 2 | 110만 | 3일 | 축소 가능 |
| P6 | 2 | 110만 | 3일 | 축소 가능 |
| **정규 소계** | **13.5** | **725만** | **병렬 2레인 약 2.5~3주** | |
| P8-a / P8-b (조건부) | +2 / +1 | +100 / +50만 | 발동 시 | P8-b 구현은 설계 승인 후 별도 |

**빠른 길** (목적만 = 나선 차단 + 최소 손): P0 → P2코어(`auto_operate` 버튼·발의 입구·재개방) → P4(+카운터 2종 동승) ≈ **5.5~6세션 · 약 350만 · 1~1.5주.** 나머지(P3 나머지·P5·P6)가 부가가치 층이다.

초과 시 이월 순서: **P6 → P5 → P3의 일부** — 자·손·되돌림이 눈·확장보다 먼저 살아남는다(눈은 §12 SQL로 수동 대체 가능하지만 손은 대체 경로가 없다).

## §8. 종료 조건

**적대 리뷰(코드 PR마다 1회) + Phase별 완료 QA(항상) + 전체 종결 시 계약 완료 QA 1회.** 트랙 종결 QA 아님(진행률 2/7 — M/M 미도달. 단 P8-a 완주 시 M4 체크는 그 Phase QA가, P3 완주 시 pao-uiux 트랙의 잔여 체크는 같은 QA가 병기 판정).

**리뷰 변이 의무 2종 + 1**: **표면 절단**(카운터 렌더 제거 — 테스트 초록인 채 사람이 못 보게 되는지) · **대칭 절단**(DOWN 표본 하한 제거 — 나선이 침묵 복귀하는지) · **P2·P6은 되돌림 절단**(해제·복원 경로 제거) 추가.

## §9. 이번에 «진짜» 안 하는 것 + 다른 트랙 요청 명세 + 스코프 밖 이월

| 항목 | 처리 |
|---|---|
| **교차 채널(쿠팡) 전부** | Jino 결정(22:30)으로 제외. 소관 `track_product-connection-map`. **이슈3의 교차 채널 서사는 상품 동일성 미확정인 채 종결.** |
| **예산 변경 «개방»** | AVA «제안»(P3)으로 대체 — 트랙 「안함」 무충돌, **개정 승인 불필요.** |
| ★**원가 동기화 배선** — 요청 명세 | **소관: 원가 메뉴 트랙.** 요청: *「`cost_recipe` 승인(status=approved) 시 그 표준원가가 `product_master.cost_price`로 반영되는 경로가 코드에 없다. 실증: 상품 `OHI-Z-LOWREF6-FOLD8-WIDE`(id=915)의 레시피 id=70이 2026-08-28 07:56 승인돼 std_cost_inc_vat=6,220.30원을 산출했으나 cost_price는 6,089.60원(2026-08-07)에 머물러 있다. 이 간극이 «의도적 유예»인지 «누락된 동기화»인지 판정하고, 후자면 배선하거나 최소한 화면에 «레시피 승인값과 다름»을 표기해 달라. 이 결손은 PAO의 BEP를 낮게(=유리하게) 계산되게 만든다.」* |
| (기록만) **MEMORY.md 24.4KB 한계 접근** | 트랙 밖 — 하네스 정비 소관. 다음 하네스 정비 세션이 인덱스 압축을 다뤄야 한다(넘으면 다음 세션들이 인덱스를 못 읽는다). |
| (기록만) **훅 오탐 3건** | `review-surface-mutation.sh`가 읽기 전용 조사 위임을 적대 리뷰로 오인해 3회 deny(전부 재전송으로 통과). `CONTRACT_surface_rule_enforcement.md` §6 계수 대상. |

---

## Jino가 승인 전에 결정할 것

1. **이 상품의 처분** — P0-ⓒ가 재료를 내면 결정. 선택지: 광고 유지(적자 감수)·축소·중단 / 상품명·썸네일 개선 / 묶음·가격·원가 변경. **재료는 우리가, 결정은 Jino가.**
2. **진행 순서** — 「전부」 확정 하에, **빠른 길**(P0→P2코어→P4, 1~1.5주)을 먼저 완주하고 나머지를 잇는 순서로 할지, **Phase 번호 순**으로 갈지.
