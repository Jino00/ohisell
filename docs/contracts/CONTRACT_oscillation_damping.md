# CONTRACT — 액셀·브레이크가 서로 다른 창을 봐서 생긴 되먹임 진동을 끊는다 (✅승인됨 · D-NAO-287)

> ## ✅ 승인됨 — 2026-09-05 11:5x KST · Jino 「승인 — 그대로 착수」


> **목표이름: 「진동차단 목표」**
> 체인 `pao-진동차단` n=1 · 세션 `a833394c` · 착수 2026-09-05 11:35 KST
> 파일 독점: `auto_operator.py` · `bid_simulator.py` · `guardrail_gate.py` · `exploration.py`
> 배경 실측: `docs/references/129_hourly_bid_feedback_and_time_axis_20260903.md` §1 · 북극성 §7

---

## §0. 착수 실측 — 인계가 말한 것보다 나쁘다 (2026-09-05 11:4x KST, prod 읽기 전용)

인계문은 *「궤적 1,320 → 1,980(UP 3연속) → 1,440(DOWN 2회)」*, ref 129는 *「UP 9 · DOWN 2」*라 했다.
7일 전수를 다시 재니 **그건 사흘치 사건의 하루 조각**이었다.

**검증 명령**(인계 원문 그대로 · `naver_change_log` ⋈ `naver_proposals`, 읽기 전용):
```sql
select entity_id, changed_at, before_value, after_value from naver_change_log
where action='update_bid' and dry_run=0 and entity_type='ad'
  and changed_at>=datetime('now','-7 days') order by entity_id, changed_at
```

**소재 `nad-…554755092` 4일 궤적 (실쓰기만, `$.adAttr.bidAmt`):**

| 날짜 | 궤적 | 방향 | 사유 | 발화 시간대 |
|---|---|---|---|---|
| 09-02~03 | 1320 → 1510 → 1730 → **1980** | UP ×3 | ROAS-UP(정착창 3.53→4.83→4.70) | 22·23·03·08·09시 |
| 09-03 | 1980 → 1690 → 1440 → **1230** | DOWN ×3 | **CPC급등**(당일 1683 > 정착창 794.7×2) | 10시~23시 **14연속** |
| 09-04 | 1230 → 1410 → **1620** | UP ×2 | ROAS-UP(정착창 3.79→3.72) | 02·03·09·10·11시 |
| 09-04 | 1620 → 1380 → 1180 → **1010** | DOWN ×3 | **순위고삐**(추정ROAS 1.47→1.15 < BEP 2.00) | 12시~23시 **12연속** |
| 09-05 | 1010 → 1160 → **1330** | UP ×2 | ROAS-UP(정착창 3.03→2.96) | 03·10·11시 |

★**4일 · 54회 판정 발화 · 16회 실쓰기 → 1,320원에서 1,330원.** 순변위 **+10원(+0.8%)**.
극한주기(limit cycle)다 — 움직임은 전부 있었고 도달한 곳은 없다.

★★**인계가 못 본 것 셋:**
1. **브레이크는 하나가 아니라 둘이다.** ref 129의 DOWN 2건은 전부 `CPC급등`이었다. 7일 전수엔
   **`순위고삐`(장중loss)도 같은 모양으로 12시간 연속 발화**한다. 처방을 CPC급등에만 대면 절반이 남는다.
2. ★**두 브레이크는 «한 번 걸리면 자정까지 안 풀린다».** 둘의 입력이 전부 **당일 누적**이라 구조적으로 단조롭다.
   - `CPC급등`: `today_cpc = Σcost/Σclk` — 누적 평균이라 남은 시간에 되돌릴 수 없다.
     실측: 정착창 기준 **794.7원이 14시간 동안 한 번도 안 움직였고**, 당일 CPC는 1633~1791에서 진동했다.
   - `순위고삐`: 게이트 ④ `today_cost ≥ avg_daily_cost`가 **원리적으로 단조 증가**한다(비용은 줄지 않는다).
     실측: 당일소진 11,397 → 14,583 단조 · 추정ROAS 1.4741 → 1.1520 단조.
   ⇒ 브레이크는 「급등을 잡는 트리거」가 아니라 **그날 남은 시간 전체를 먹는 래치**다.
3. ★★**액셀은 기다리지 않고 브레이크는 하루치를 기다린다 — 그게 §7 비대칭의 정확한 좌표다.**
   09-04 11:20에 `순위고삐`는 추정ROAS<BEP를 **이미 알고 있었지만** ④소진 문턱(하루평균 6,919원) 미달이라
   *"당일 소진<하루평균 — 판정 유보"*로 침묵했다. 같은 시각 `ROAS-UP`은 정착창 3.7176만 보고 **올렸다.**
   ⇒ **오늘 나쁘다는 증거가 이미 손에 있는데, 그 증거를 보는 눈은 액셀 쪽에만 안 달려 있다.**

`·->·` 행 27건(전체 54 중)의 정체도 해소된다(ref 129 §7 [미상] 「값없음 12건」) —
**같은 래치 판정이 매시 재발화했지만 가드레일 쿨다운 2h·일일상한이 실쓰기를 막은 행**이다.
즉 진동의 실제 크기는 지금 **가드레일이 눌러 주고 있어서** 이 정도다.

📄 미실측: `·->·` 27건이 정확히 어느 가드레일에서 막혔는지는 `change_log`에 사유가 없다(§4-C ⓘ에서 잰다).

⚠️**§0 정정 (2026-09-05 12:2x, 계수기 실측)**: 위 표의 「09-03 UP ×3」은 **09-02 22:20부터** 세어
1,320 → 1,980원 = ×1.5인데, **09-03 «당일» 배수는 ×1.31**이다(그날 첫 실쓰기의 before가 1,510원 —
1,320 → 1,510은 09-02 22:20 것). B-veto는 «오늘» 원장만 보므로 실제로 적용되는 값은 **×1.31**이다.
초판 §0의 ×1.5 표기는 이틀에 걸친 수였다 — 판정에 쓰이는 수가 아니다.

---

## §1. 목표 / 이번에 안 하는 것

**목표** — 액셀과 브레이크가 **어떤 창을 보는지 코드에서 한 자리로 드러나게** 하고,
그 창이 서로를 오염시켜 생기는 **일 단위 톱니 진동을 감쇠**한다. 결과 지표는 순변위가 아니라
**「같은 소재에서 UP 직후 DOWN(또는 그 역)이 같은 날 나오지 않는다」**이다.

**이번에 안 하는 것** (인접하지만 범위 밖):
- `frontend/**` · `backend/app/routers/naver_ad.py` — **B1 세션 독점**
- `docs/references/75·80·81·121·131` · 북극성 §3 — C1·C2·C3 동시 작업 중
- 상한 리셋 입구(`max_auto_up_multiple` 기준점) — B1 몫
- **창 길이 변경 0** — 정착창 D-8~D-2(7일)은 손대지 않는다(ref 65 S2 「안 함」 원문 · 북극성 M2 안 함)
- **표본 하한 게이트(D-NAO-286) 손대지 않음** — `guardrail_gate._check_data_floor` ·
  `_FLOOR_GATED_TYPES` · `_settlement_agg`의 `ad` 분기는 **한 글자도 안 고친다**
- 가드레일 쿨다운·일일상한·스텝 클램프 값 변경 0 (진동을 지금 눌러 주고 있는 것들이다)
- 새 자동 쓰기 경로 신설 0 · 시간대별 문턱값 0 · SD `bidWeight` 0 (ref 129 §6 「하지 말 것」 원문)
- **작업 종류**: 리팩터 / 성능 최적화 / 테스트 범위 확대 / 새 SA 파일 추가

---

## §2. 판단기준 — "항상 A, 왜냐하면 ○○"

1. **항상 «자기 창이 상대 창에 오염됐을 때 자기 발화를 멈춘다»로 고친다**, 왜냐하면 오염된 증거로 내리는
   결정은 방향이 어느 쪽이든 틀렸고, **멈춤(hold)은 새 액션을 만들지 않아 §7 대칭을 깨지 않기 때문이다.**
2. **항상 두 거부권을 한 커밋으로 넣는다**, 왜냐하면 브레이크 쪽만 넣으면 안전장치 단독 완화이고
   액셀 쪽만 넣으면 **D-NAO-85(ROAS +7% · 매출 −52%)의 재현**이기 때문이다 — §7이 이름 붙인 그 병.
3. **항상 창은 선언하고 그 선언을 테스트가 검증한다**, 왜냐하면 이 저장소의 상습 실패 모드가
   「라벨은 붙고 집행은 안 됨」이라 **선언만 있는 표는 다음 세션에 조용히 거짓이 되기 때문이다**(ref 131 §5 신3).
4. **항상 기존에 이미 계산된 값을 재사용한다**, 왜냐하면 `_intraday_loss_leash`가 매시 이미 산출하는
   `est_roas`·`bep_roas`를 다시 조회하면 **판정창과 실행 재검증창이 갈라지기 때문이다**(D-NAO-265 실측 재발 방지).
5. **항상 「오늘 우리 쓰기가 0건이면 종전과 완전 동일」을 보장한다**, 왜냐하면 그것이 이 변경의
   **회귀 0 증명**이자, 대행사 관할 유닛에 우리 로직이 새로 개입하지 않는다는 보장이기 때문이다.
6. **항상 브레이크의 «발동 조건»은 그대로 두고 «발동 근거의 오염»만 벗긴다**, 왜냐하면 문턱값을 만지면
   그건 감쇠가 아니라 **브레이크 완화**이고, 되돌릴 근거가 관측이 아니라 취향이 되기 때문이다.

---

## §3. 금지선

- **표본 하한 게이트(D-NAO-286) 회귀 금지** — `backend/tests/test_naver_guardrail_floor_gate.py` **43개 전건 유지**.
  이 파일은 2026-09-05 09:12 prod 가동분을 지킨다.
- **브레이크만 남기는 수정 금지 / 액셀만 조이는 수정 금지** (북극성 §7). §4-C ⓒ의 «수»로 증명한다.
- **`--force` 금지** (safe_merge·force-push 둘 다) · **`git add -A` 금지**.
- 정착창 길이·가드레일 상수·`CPC_SPIKE_RATIO` 값 변경 금지 — 이번 변경은 **비교 대상**만 고친다.
- 되돌릴 수 없는 액션(prod 배포)은 §2 승인 지점을 상속한다 — safe_deploy로만, CAS 거부 시 덮지 않는다.

---

## §4. 합격기준 — 라이브 증거를 체크박스로, 표면을 지목해

### §4-A. 산출물 (구현)

**S1 — 창 선언 한 자리 (`_HOURLY_DECISION_WINDOWS`)**
`auto_operator.py` 모듈 상수 하나에, 시간당 레인의 세 게이트가 **각각 어떤 창을 읽고 그 창에 «우리
자신의 당일 쓰기»가 섞여 있는지**를 선언한다. 세 게이트는 창 경계를 이 선언에서 받아 쓴다.

| 게이트 | 방향 | 창 | 우리 당일 쓰기 포함? |
|---|---|---|---|
| `roas_up` (정착창 실측) | 액셀 | 정착창 D-8~D-2 | 아니오(과거) |
| `intraday_up` (장중 tally) | 액셀 | 당일 hh24 누적 | **예** |
| `cpc_spike` | 브레이크 | 당일 hh24 누적 ÷ 정착창 D-8~D-2 | **예**(분자) |
| `loss_leash` | 브레이크 | 당일 hh24 누적 ÷ 정착창 D-8~D-2 | **예**(분자) |

**S2 — 미러 거부권 2개** (한 커밋 · 둘 다 `hold`만 만들고 새 액션을 만들지 않는다)

- **A-veto(액셀 쪽) — 「오늘 증거 거부권」**
  정착창이 `ok`여도, `_intraday_loss_leash`가 **이미 산출한** 당일 추정ROAS가 BEP 아래로 **확인되면**
  UP을 금지하고 `hold`한다. 고삐의 ④소진 문턱(`today_cost ≥ avg_daily_cost`)은 **손대지 않는다** —
  하향은 여전히 하루치 소진 뒤에만 나간다. 이 거부권은 **하향을 만들지 않고 상향만 멈춘다.**
  (지금 GATE P2-A-1은 «정착창 below ∧ 장중 ok» 한 방향만 막는다. 이건 그 **거울**이다.)

  ★★**구현 중 발견 — 전환 하한이 «반드시» 붙어야 한다** (2026-09-05 12:3x, 계약 승인 후):
  초판 구현은 `today_sub_bep is True`만 봤는데, `intraday_roas.py`를 열어 보니 정직 경계 ②가
  *「장중 추정 ROAS/총이익은 구조적으로 **과소추정**」*이라 적어 뒀고 `estimated_intraday_roas`엔
  **자체 전환 하한이 없다**. 즉 「오늘 BEP 하회」는 **아침에 전환 0인 유닛 거의 전부**에서 참이다
  (추정ROAS 0 < BEP). 하한 없이 걸면 **매일 아침 액셀이 통째로 눌린다** — 이 계약이 §3에 금지선으로
  적어 둔 D-NAO-85(ROAS +7% · 매출 −52%)의 정확한 재현이다.
  ⇒ 하한은 **액셀 자신의 기준**을 쓴다: `_INTRADAY_UP_MIN_CONV`(=2)는 「오늘 숫자를 믿어도 되는가」에
  UP 경로가 이미 정해 둔 값이다. 브레이크의 기준을 액셀에 씌우는 게 아니라 **액셀의 기준을 액셀에
  되돌려 적용**한다 — ref 129 §6의 *"브레이크에도 UP과 같은 전환 하한을 준다"*를 거울로 세운 것.
  **새 매직넘버 0개.** 이 방어선은 `test_a_veto_does_not_choke_the_accelerator_on_zero_conversion_mornings`가 고정한다.

- **B-veto(브레이크 쪽) — 「자기유발 CPC 제외」**
  `CPC급등` DOWN은 당일 CPC 상승이 **우리 자신의 당일 상향분으로 설명되는 범위 안이면** 발동하지 않고
  `hold`한다. 비교식: `today_cpc > baseline_cpc × CPC_SPIKE_RATIO × own_bid_multiple_today`.
  `own_bid_multiple_today` = 현재 입찰가 ÷ **오늘 00시 입찰가**(= `naver_change_log`의 오늘 첫 실쓰기
  `before_value.adAttr.bidAmt`). **오늘 우리 쓰기가 0건이면 1.0 = 종전과 완전 동일.**
  ★배수를 못 읽으면(행 부재·파싱 실패) **1.0으로 fail-closed** — 모르면 브레이크를 유지한다.

**S3 — 회귀 고정 테스트** (§4-B)

### §4-B. 테스트 (재현 조건에서 궤적이 안 나온다)

- [x] ⓐ 09-04 **구조** 재현 픽스처(정착창 보정ROAS 3.0 ≥ 목표 2.0 · 당일 추정ROAS 0.83 < BEP 2.0 ·
      당일소진 2,400 < 하루평균 3,000 · 당일 전환 2 ≥ 하한 2)에서 **종전 코드는 `up`, 새 코드는 `hold`**
      — 둘 다 단언한다. ★**실측 숫자 그대로가 아니라 «같은 구조»다**: 09-04 11:20 그 시각의 당일 전환
      건수는 hh24 곡선이 안 남아 있어(네이버 7일 보존·우리 미저장) **판정불능**이고, 전환 하한(위 A-veto
      절)을 충족시키려면 소진·BEP 비를 그에 맞춰야 했다. 고정하는 것은 「그날이 이렇게 막혔다」가
      아니라 **「이 입력이면 막힌다」**다.
- [x] ⓑ 09-03 실측 재현 픽스처(정착창 CPC **794.7** · 당일 CPC **1683.3** — 사유문 원문값 그대로 ·
      오늘 우리 상향 1320→1980 = ×1.5)에서 **종전 코드는 `down`, 새 코드는 `hold`** — 둘 다 단언한다.
      (테스트는 ×1.5로 고정한다. prod 09-03 «당일» 배수는 ×1.31이었고 그 값으로도 억제된다 — §0 정정 참조.)
- [x] ⓒ **오늘 우리 쓰기 0건**이면 ⓑ 픽스처가 **종전과 동일하게 `down`** (회귀 0 증명).
- [x] ⓓ **외생 CPC 급등**(우리 상향 ×1.0인데 당일 CPC가 기준×2 초과)은 **여전히 `down`** — 브레이크 생존.
- [x] ⓔ 창 선언 표 ↔ 실제 코드 일치 테스트 — 각 게이트가 선언한 창의 경계를 실제로 읽는지 단언
      (선언만 있고 집행이 없는 표를 만들지 않는다).
- [x] ⓕ **표본 하한 게이트 43개 전건 통과** · 백엔드 회귀 전건 통과(현재 기준선 7,735+).

### §4-C. 라이브·대칭 증거

- [x] ⓖ ★**§7 대칭 검사 — «수»로 보고**(D-NAO-234 §9-2 방식). **측정 완료 2026-09-05 12:2x KST**
      (prod 읽기 전용 · `scripts/measurements/oscillation_symmetry_count.py` · 창 7일 · 후보 53건):

      **재측정 2026-09-05 13:0x KST**(적대 리뷰 P1-5 수정 후 · 창 7일 · 후보 54건):

      | 방향 | 종전 → 새 | 감소 | 정확도 |
      |---|---|---|---|
      | 액셀 UP | **28 → 23건** | −5 (−17.9%) | 🧠 **대리 지표**(측정 아님 — 아래) |
      | 브레이크 DOWN | **26 → 24건** | −2 (−7.7%) | 소급 정확 |

      📄 P1-5(servo/rank 오분류) 수정은 **이 창의 수를 바꾸지 않았다** — 최근 7일 UP이 전부
      평범한 `bid_up` 타입이라 오분류 대상이 0건이었다. 12:2x 측정의 27/53과의 차 1건은
      그 사이 새로 발화한 행이다. 즉 이 수정은 **보고된 수치의 정정이 아니라 지뢰 제거**다.

      ★**둘 다 0이 아니게 줄었다 = 한쪽만 조이지 않았다.** 다만 두 수의 «자격»이 다르므로 아래를 병기한다.
      - **B-veto(−2)는 소급 정확하다.** 필요한 입력이 전부 원장에 있다(사유문의 당일 CPC·정착창 기준 +
        change_log의 자기유발 배수). 억제된 2건은 09-03 CPC급등 14연속의 **머리 2건**이고, 그중
        10:20 행이 실제로 1,980 → 1,690원을 **썼다**. ⇒ **이 −2는 궤적 효과의 하한이다** — 라이브였다면
        그 쓰기가 없어 뒤따른 12건의 입력 자체가 달랐다. 계수기는 행 단위 독립 재평가이지 궤적 모의가 아니다.
      - ★**A-veto(−5)는 «측정»이 아니다 — 소급 판정불능.** 필요한 입력이 그 시각의 hh24 곡선인데
        네이버가 7일만 보존하고 우리는 저장하지 않는다. 표의 5건은 대리 지표(「같은 날 뒤에 고삐가 난 UP」)이며
        전부 09-04 02:20~11:20의 그 상향 연속이다. **정확한 수는 배포 후 운영 일기로 센다(ⓙ).**
      - ⚠️초판 계수기는 −2를 **−2로 세면서 배수를 ×1.12로 잘못 계산**했다(평가 중인 행 «자신의»
        하향 결과를 「판정 전 상태」로 읽은 off-by-one). 고쳐서 ×1.31 — 라이브 코드엔 이 창이 없다
        (`_own_bid_multiple_today`는 쓰기 «전»에 돈다). 소급 재현에서만 생기는 오차다.

      **판정**: 두 방향 다 움직였으므로 **비대칭 아님** — 다만 액셀 쪽 수는 확정이 아니라는 것을
      Jino 보고와 완료 QA에 원문으로 남긴다.
- [ ] ⓗ 배포 후 라이브: `naver_change_log`에서 **같은 소재·같은 날 UP과 DOWN이 함께 나온 날 수**가
      배포 전 창(09-01~09-05, 실측 **2일**: 09-03·09-04) 대비 어떻게 되는지 **1주 관측**해 수로 남긴다.
- [ ] ⓘ `·->·`(실쓰기 없는 재발화) 27건/54건 비율의 배포 후 변화 — 래치가 실제로 풀렸는지의 대리 지표.

### §4-D. 표면 — 사람이 어디서 보는가

★**이번 슬라이스의 사람 표면은 «화면»이 아니라 «판정 사유문»이다** — `frontend/**`가 안 함이기 때문이다.
두 거부권이 발동하면 `naver_proposals.rationale`·운영 일기(`blocked`)에 **왜 멈췄는지가 원문으로** 남는다:
- A-veto: `UP 보류(오늘 증거 거부권) — 정착창 ok({…})인데 당일 추정ROAS {…} < BEP {…}`
- B-veto: `CPC급등 보류(자기유발분) — 당일 {…}원 ≤ 정착창 {…}원×{ratio}×자기상향 {mult}배`
- [ ] ⓙ 배포 후 prod에서 위 두 문구 중 **최소 1건이 실제로 기록된 것**을 타임스탬프와 함께 관측한다.
      (0건이면 «안 걸렸다»이지 «됐다»가 아니다 — 그대로 적는다.)

**표면파일**: `backend/app/services/naver_ad/auto_operator.py`
**표면: 판정 사유문(`naver_proposals.rationale` · 운영 일기 `blocked`) — 화면 없음(frontend 안 함)**

---

## §5. 실행 — 돌리면 결과가 보이는 명령

```bash
cd /private/tmp/claude-501/pao-b2/repo/backend && python3 -m pytest \
  tests/test_naver_guardrail_floor_gate.py tests/test_naver_auto_operator.py \
  tests/test_naver_oscillation_damping.py -q
```

대칭 검사(§4-C ⓖ, prod 읽기 전용 · 쓰기 0) — prod에서 돌린다:
```bash
scp scripts/measurements/oscillation_symmetry_count.py sellc.ohitech.co.kr:/tmp/osc_sym.py && \
  ssh sellc.ohitech.co.kr "python3 /tmp/osc_sym.py --db /home/ubuntu/ohisell/backend/ohisell.db --days 7"
```
⚠️초판 §5는 `--window 7d`라 적었는데 **존재하지 않는 인자**였다(실제는 `--db` 필수 + `--days`).
적대 리뷰 P2-1 채택 — 계약의 `실행:` 명령은 «돌리면 결과가 보여야» 의미가 있다.

---

## §6. 항목 — 구현 목록 (진입점·배선·렌더까지)

1. `auto_operator.py` — `_HOURLY_DECISION_WINDOWS` 선언 + 네 게이트가 이 선언에서 창 경계를 받아 쓰게 배선
2. `auto_operator.py` — `_intraday_loss_leash` 반환에 **「오늘 BEP 하회 확인됨」 3상 플래그** 추가
   (`True`/`False`/`None=판정불가`). 기존 2-튜플 소비자는 **호출부 1곳**(`_judge_hourly`)뿐임을 확인함
3. `auto_operator.py` — `_judge_hourly`에 **A-veto** 삽입(정착창 ok ∧ 오늘 BEP 하회 확인 → hold)
4. `auto_operator.py` — 오늘 00시 입찰가 조회(`naver_change_log` 오늘 첫 실쓰기 `before_value`) +
   `_judge_hourly`의 CPC급등 분기에 **B-veto** 삽입
5. `backend/tests/test_naver_oscillation_damping.py` — ⓐ~ⓔ 5종
6. `scripts/measurements/oscillation_symmetry_count.py` — §4-C ⓖ 대칭 계수기(읽기 전용)
7. 문서: `docs/references/132_oscillation_damping_20260905.md`(실측·대칭 수) ·
   트랙 `확인:` 1줄 · 이 계약의 체크박스 갱신

---

## §7. 예산

- 코드: `auto_operator.py` 1파일(+테스트 1·스크립트 1) · 순증 **150줄 이내** 목표
- 세션: 1세션(반나절) · 적대 리뷰 1R + 필요 시 2R
- prod 쓰기: **배포 1회**(백엔드 1파일 · 마이그레이션 **0** · 무중단 재시작)

---

## §8. 종료 조건

- **적대 리뷰 1회**(PR 경계 · 구현과 **다른 기** · 표면 절단 변이 포함 — 「사유문이 안 남는 변이」와
  「거부권 호출부 삭제 변이」를 반드시 넣는다) → **P1=0이면 PASS**
- **완료 QA 1회**(별도 Sonnet 읽기 전용) — 대조 3: ①이 계약 §4 ②트랙 목표 D-NAO-59 ③북극성 §1·§6
- 이유: L 규모(반나절+·돈 경로·자동 실집행에 직접 닿음)이고 **폭발 반경이 실입찰**이라
  §1 표의 L 절차를 전부 붙인다.

---

## §9. 북극성 대비 검토 (D-NAO-226 · 227 — 4줄)

1. **어느 M인가**: **M4**(L3 재개 — 카나리 실집행). 이미 재가동돼 매시 쓰고 있는 상태의 **품질** 문제다.
   ★M4 합격 관측(*"실집행 diary 행 생성 + 가드레일 위반 0 + 대행사 되돌림 0"*)에는 **총이익 항이 없어서
   이 진동이 M4 기준으로는 «합격»으로 읽힌다** — D-NAO-230 각주가 경고한 바로 그 구멍이고,
   이번 슬라이스는 그 구멍 안에서 일어나고 있는 손실을 다룬다.
2. **5요소 중 움직이는 것**: **③총이익 방향** · **④자동화 운영**(품질). ①②⑤는 이번에 0.
   ★④가 「0이 아닌」 세션은 오랜만이다 — 다만 새 자동화를 여는 게 아니라 **이미 열린 자동화의 진동을 끄는 것**이다.
3. **§7 금지선 검사**: 이 계약의 설계 원리 자체가 §7이다(§2-2 · §4-C ⓖ). 「액셀만 조이기」·
   「브레이크만 남기기」를 §3 금지선에 명시했고, 채택 여부를 **수**로 판정한다.
4. **관련 절 전부 읽었는가**: §목차부터 훑음(§0~§9+부록 A). 읽은 절 — §1(궁극 목표 원문) · §4 간극3(L3 재개) ·
   §6·6-b(M4 합격·안 함) · §7(전문) · D-NAO-230 각주. **서브에이전트 위임문에는 §7 원문과 이 계약 §0을 싣는다.**

---

## §10. [미상] — 이 계약이 모르는 것

- `·->·` 27건이 어느 가드레일(쿨다운 2h / 일일상한 / BEP 하한)에서 막혔는지 — `change_log`에 사유 열이 없다(§4-C ⓘ에서 대리 관측)
- 진동이 **총이익에 얼마를 물렸는지** — 시간×소재 마진 교차 원장이 없어 이번 창에선 못 잰다. 재지 못했다고 적고 넘어간다
- 다른 소재 2건(`…558104404`·`…558107660`, 09-01 UP 3회씩)이 그 뒤 왜 발화가 끊겼는지 — 이번 범위 밖


---

## §11. 완료 QA (별도 Sonnet · 읽기 전용 · 2026-09-05 13:2x~13:33 KST) — **원문 그대로**

> ★미달·판정불능을 포함해 옮긴다. 기준을 낮추지 않는다.

**판정(계약 §4): 부분달성** — §4-A·§4-B(ⓐ~ⓕ)는 전부 달성(구현·테스트·회귀 0·표본하한 43개 전건 생존,
직접 재실행 확인 13:24~13:30 KST). §4-C ⓖ는 부분달성(비대칭 아님은 재현되나 A-veto 쪽 수는 계약 스스로
"측정 아님"), §4-C ⓗⓘ·§4-D ⓙ는 배포 후 25분 경과 시점이라 라이브 발동 자체가 아직 0건 관측 —
판정불능/미달. 즉 **"만들어 붙였다"는 확인됐고 "붙인 것이 실제로 작동해 진동을 줄였다"는 아직 증거가 없다.**

**판정(트랙 목표 D-NAO-59): 판정불능** — 이 슬라이스는 "총이익 절대액 최대화(ROAS 방어 아님)"라는
목표 자체를 전진시키는 처방이 아니라, 액셀·브레이크가 서로 다른 창을 오염시켜 만든 "래치형 톱니
진동"(4일 54회 판정·16회 쓰기로 순변위 +0.8%)을 없애는 위생 조치다. 계약 §10이 스스로 "진동이 총이익에
얼마를 물렸는지 — 이번 창에선 못 잰다"고 자백하고, 실제로 이 세션은 이익액을 재는 어떤 새 원장도 만들지
않았다. UP/DOWN 왕복을 줄이면 방향상 총이익 방어에 유리할 것이라는 추론은 가능하나, D-NAO-59가 요구하는
"ROAS 하락을 감수하고 매출을 늘려 총이익을 늘렸는가"를 직접 재는 관측치는 이번 산출물에 없다.

**판정(북극성 §1·§6): 부분달성** — 이 세션은 M4(L3 재개, 카나리 실집행)의 **품질** 축 안에서 움직였고
M을 닫지 않았다(진행률 2/7 불변). 5요소 중 ③총이익 방향·④자동화 운영(품질)만 움직였고 ①②⑤는 0.
§7 「액셀·브레이크 대칭」 검사는 구조적으로(두 거부권이 한 커밋·둘 다 hold-only) 그리고 수치적으로
(양방향 모두 0 아닌 감소, 13:27 KST 독립 재현) 통과했다. 다만 북극성 §6 각주(D-NAO-230)가 "M4
합격기준엔 총이익 항이 없어 자가 부풀어도 통과한다"고 미리 경고한 그 구멍 안에서, 이번 조치가 실제
총이익에 미친 효과는 아직 라이브로 0건 관측(§4-D ⓙ)이라 검증되지 않았다.

**종합: 부분달성**

### ★QA가 잡은 것 — 만든 쪽 보고에 없던 것 2건

1. **§7 예산 초과** — 「코드 순증 150줄 이내 목표」인데 실측 **315줄**(`auto_operator.py` 단독).
   체크박스 항목이 아니라 판정엔 미반영이나, **초과는 사실이다.** 다음 계약의 예산은 이 실적을 보고 쓴다.
2. **문서와 실측이 갈린 지점** — §4-B ⓕ가 `[ ]` 미체크인 채였는데 QA 재실행에서 **7,852 passed** ·
   표본 하한 게이트 **43 passed**로 통과가 확인됐다(이 커밋에서 `[x]`로 정정).

### QA가 확인하지 못한 것 (원문)

- **적대 리뷰의 "P1=0 PASS" 자체를 내가 재현하지 않았다.** 수정이 실재함은 코드로 간접 확인했으나,
  리뷰 세션의 변이 주입 로그 원본은 저장소에 없어 직접 본 것은 아니다.
- §4-C ⓗ·ⓘ, §4-D ⓙ는 배포 후 경과 시간이 약 25분뿐이라 **원리적으로 지금 판정 불가능**하다 —
  다음 QA 주기가 재야 한다.
- 배포 후 `naver_proposals` 45건의 rationale을 A/B-veto 패턴 외 각도로는 안 봤다.

### 라이브 관측 (배포 후 첫 무인 발화)

`2026-09-05 13:20:00.034511` · `nad-…554755092` · `bid_up 1330→1520` ·
사유 `[시간당밴드] ROAS-UP … 정착창 보정ROAS 2.9602 >= 목표 1.87`.
★as_of 소수점이 크론 슬롯과 일치 = **무인 발화**(수동은 임의 시각). **정상 경로가 막히지 않았다** =
회귀 0의 라이브 증거. 두 거부권은 **0건** — 오늘 아직 CPC급등도 BEP 하회 확인도 없다.
**0건은 「안 걸렸다」이지 「됐다」가 아니다**(§4-D ⓙ 규약 그대로).
