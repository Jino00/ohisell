# 계약 — PAO 우리 엔진 점화(카나리)  ★승인됨

> **v1.0 · 2026-08-25 00:33 KST Jino 승인** (원문: *"승인해."*) — **D-NAO-247**
> 발의: 세션 `a3d1c01c` · 체인 `pao-논의` n=48 · 저자: Fable(구조 산출물 위임) + 세션 편집(P1 철회 반영)
> **목표이름: 점화 목표** (중복 검사 완료 — 체인 5개·계약·트랙에 동명 없음)
> 트랙: `docs/tracks/active/track_naver-ad-optimization.md` · 북극성 **M4** 슬라이스
> 남은 게이트 = 북극성 §8-① 하나 (②는 D-NAO-241로 해제·통보 완료 2026-08-24 16:18 이전)
>
> ⚠️ **승인 시점의 미결 1건**: P4(지혜 id 조인)를 선행에 둘지에 대한 세션 유보가 초안에 적혀 있었고, Jino는 초안 전체를 승인했다 — **P4는 선행에 남는다.** 예산 초과 조짐이 있으면 §5의 규정대로 이월한다.

---

## 1. 목표 / 이번에 안 하는 것

**목표**: 07-30 10:48 이후 **26일째 0건**인 우리 실집행을 재개한다. 지금은 `auto_operate=0`(9캠페인 전건)·`optimizer='none'`이라 auto-lane 순회가 0회 돌고(`auto_operator.py:1169-1172`), 일기 사슬도 `blocked`가 07-30에서 끊겨 학습이 멈춰 있다.
이 계약이 끝나면: **Jino가 승인한 카나리 캠페인에서 우리 엔진이 실집행하고, 그 행위·결과가 일기→후보→지혜 사슬에 다시 실리며, 성적이 구간 자 병기로 화면에 보인다.**

**이번에 안 하는 것** (소관 병기):
- 대행사 원장에서 «현상» 배우기 → Jino 08-25 00:21 지시로 **이월된 별건**
- 예산 변경 개방 → 스프린트 X부터 불변의 스코프 밖
- 상품(소재) 축 성과 원장 신설 → 액셀 측정의 근본 해법이나 **별도 계약감**
- 전 캠페인 확대·M5 루프 상시화 → 카나리 성적은 카나리 창의 숫자다(북극성 §6 M4 안 함)
- 콘솔 페이지네이션 수리 · `OptimizerGuardError` 일기화 → **나중**(Q1), 이월 기록
- ~~로깅 결손 수리~~ → **★철회**: 결함이 아니었다(ref 96 §2 정정). 선택적 소품 1건(WRITE_FAILURE 20건에 `before` 한 줄)만 남고 그것도 이번 범위 밖

## 2. 판단기준

- **항상 「점화 전 필수」와 「점화 후 병행」을 가른다** — 왜냐하면 n=45 사고가 8개 중 2개만 하고 켠 것이고, 반대로 전부 요구하면 「빨리」가 죽는다. 가르는 기준은 하나: **«없으면 카나리 1주의 관측이 판정불능이 되는가»**
- **항상 판정은 날짜가 아니라 표본으로 한다** — 왜냐하면 실측이 「30일」류 일수에 근거가 없음을 보였다(WEB_SITE 계열은 효과가 산포에 파묻혀 표본 공식 붕괴)
- **항상 총이익은 «있는 그대로 + 구간[×0.827~×1.3016]» 병기·하한 운용으로 잰다** — 왜냐하면 D-NAO-230: 자가 부풀면 카나리가 그대로 통과한다
- **항상 못 재는 것은 [미상]·판정불능으로 적는다** — 왜냐하면 가짜 합격이 7월의 정확한 재방송이다
- **항상 있는 안전장치를 조합한다** — 왜냐하면 «사고가 나도 게이트를 새로 세우지 않는다»가 원칙이고, 스코프 이원화·D-NAO-242 수리·자 정직화가 이번 주에 이미 배선됐다
- **항상 행위 분포로 브레이크 편중을 감시한다** — 왜냐하면 ROAS 방어 표류가 상습 실패 모드이고(D-NAO-85: ROAS +7%·매출 −52%), **성과 대칭은 지금 원리적으로 못 재므로 행위 대칭이 유일한 관측 가능 대리다**

## 3. 금지선

- `optimizer='ours'` 아닌 캠페인에 쓰기 **절대 금지** (D-NAO-13 — 코드 하드체크 존재 확인됨)
- **memo에 실험 배치 표기(A/B 열·MOP 열·대조군)가 있는 캠페인 점화 금지** — 강화유리 03 실사고의 직접 처방
- **선행 완료 + Jino §8-① 승인 전 스위치(`auto_operate`·`optimizer`) 조작 금지** — n=45의 13분 철회가 이 줄의 근거
- 예산 변경 개방 금지 · 개별 캠페인 하드코딩 금지(전역 규칙만) · 카나리 성적의 즉시 일반화 금지
- 홀드아웃 없이 발견을 집행에 주입 금지
- 새 게이트·새 안전장치 발명 금지 — 있는 것 조합만
- **하한 자 기준 적자인데 카나리 확대 금지** (D-NAO-230 운용)

## 4. 합격기준 (체크박스 = 주장, 판정은 완료 QA)

### 선행 (점화 전 — 이것이 「빨리」의 전부다)
- [ ] **P2. 대상 선정 절차 실행** — Q5 규칙으로 prod 캠페인 **전수**의 memo·rationale·외부집행·데이터량을 판독한 **후보 목록 + 배제 사유표**가 채팅 화면에 제출되고, Jino가 §8-①로 최종 지정
- [ ] **P3. 대조군 지정·기록** — 처치(스코프 행)·대조(명단+사유)가 트랙 파일에 D-N으로 기록. **코드 아님, 절차다**
- [ ] **P4. M3 최소 조각 — 지혜 id 조인** — 성적표 부품을 지혜 id로 묶는 **읽기 조인 1개**, 콘솔에서 「지혜별 성적」이 조회됨. **표면**: 콘솔 화면 1곳(구현 시 지정)
  - ⚠️ 세션 유보: P4가 카나리 «판정»에 정말 필수인지 이견 있음 — Jino 판단 몫

### 카나리 창 (점화 후 — M4 합격 관측 + D-NAO-230 각주)
- [ ] **C1. 점화 상태** — `/naver-ad/scope` 화면의 카나리 스코프 행이 승인 명단과 **정확히 일치**(초과 0) + prod `auto_operate=1 ∧ optimizer='ours'`가 같은 명단과 일치
- [ ] **C2. 실집행** — `ops_diary_entries`에 카나리 귀속 `execute` 행 ≥1, 그 행들의 대응 change_log에 before/after 실값 존재(성공 경로 정상 동작의 확인)
- [ ] **C3. 안전** — 가드레일 위반 0 · 대행사 되돌림 0 · 스코프 밖 쓰기 0 (change_log 카나리 창 전수 대조, 우리분/외부분 분리)
- [ ] **C4. 지속** — C1~C3이 **7일 연속**(M4 원문) ∧ 실집행 이벤트 중 «전후 7일 완전창» 보유 건이 표본 하한 도달. 하한은 SHOPPING 광고그룹 실측 ≈52건을 앵커로 하되 **[미상: n=34라 약함 — 첫 주 실발생률로 재산정해 병기]**, 미달이면 합격 선언 대신 **창 연장**
- [ ] **C5. 총이익** — 카나리 캠페인 **합산** 총이익이 「있는 그대로 + 구간」 병기로 콘솔 손익 표면에 관측되고 **하한 자 기준 ≥ 0**. 자의 가정·창 병기 의무
- [ ] **C6. 행위 대칭 보고** — 카나리 창 diary의 행위 분포(상향/하향/제외/재개 건수)를 주간 보고 1장에 병기. **성과 대칭은 판정하지 않는다 — [판정불능 예약]을 보고서에 명기**
- [ ] **C7. 학습 재개** — 카나리 창의 `blocked`·`execute` 일기가 harvest에 집혀 `ops_wisdom_candidates` 신규 행 ≥1, 또는 **「후보 0의 사유」가 기록됨** — 판사의 기각도 유효한 관측이다

## 5. 예산
선행 슬라이스 1~2세션·통상 예산 이내. 카나리 창 관측은 세션당 경량(Sonnet 읽기 전용 위임). 초과 조짐이면 P4를 이월하고 P2·P3만으로 점화 여부를 Jino에 표면화.

## 6. 종료 조건
- P4의 코드 경계 → 해당 PR **적대 리뷰**(표면 변이 1개 포함 — 값이 화면까지 닿는 경로를 끊는 변이)
- 계약 전체 → **완료 QA**가 닫는다. 합격기준 전 항목이 라이브 관측(콘솔·DB·change_log)이라 대조 가능하고, C4 표본 미달이면 QA는 **판정불능**을 낸다(유효한 종결)
- 트랙 종결 QA 아님 — M4는 M0~M6 중 1칸이다

---

## 부록. 설계 질문 답변 (Fable)

### Q1. 최소 필수 집합 — 기준: «없으면 카나리 1주가 판정불능인가»

| 항목 | 판정 | 근거 |
|---|---|---|
| ~~로깅 결손 수리~~ | ~~필수~~ → **불요** | ★세션 정정: 결함이 아니었다. 194건은 「안 바뀐 것」이고 전건 `blocked` 일기로 이미 학습 사슬 안에 있다 |
| 대상 선정에 memo 확인 | **필수** | 비용 거의 0(절차 1줄)인데 대조군을 켤 뻔한 실사고가 있다 |
| 대조군 지정 | **필수, 단 코드 아님** | 없으면 판정 불가. 그러나 «지정»은 명단+기록이면 되고 03/04 A/B 구조가 memo에 이미 있다. **코드 0건은 코드가 필요하다는 뜻이 아니다** |
| 액셀 축 처분 | **«처분»만 필수** | 원장 신설은 별건. 이번 처분 = 액셀 개별 귀속을 판정 대상에서 빼고 캠페인 합산+행위 분포로 대체 — **정직한 판정불능이 처분이다** |
| 데이터 하한 게이트 | **불요** | 지혜 판사가 이미 그 일을 한다(27건 중 22건을 표본 부족으로 기각·승률 98%도 4일치라 기각) — 새로 만들면 게이트 중복 |
| 주말·공휴일 라벨 | **나중** | 분석 해상도 문제지 점화·판정의 전제가 아니다 |
| 7월 잔여물 처분 | **나중** | 본체(유령 스텝 역설)는 D-NAO-242가 08-24에 이미 수리 |
| 콘솔 페이지네이션 | **나중** | 판정 표면을 카나리 필터 쿼리로 지정하면 우회 — 이월 |
| `OptimizerGuardError` 일기화 | **나중** | 카나리는 `ours`라 그 가드가 판정에 안 걸린다 — 관측 사각이니 이월 기록 |

### Q2. 「M2·M3 먼저」와 「빨리」의 긴장
북극성이 M2·M3을 앞에 둔 이유는 «약한 신호 위의 자동화» 방지 = **판정·학습 준비 없이 손부터 재개하는 것**의 금지다. 그러니 앞당길 것은 M2·M3 전체가 아니라 **카나리 «판정»이 실제로 소비하는 조각**뿐:
① M3의 「지혜 id 조인」 하나(§5-3 ①이 «부품은 다 있고 조인 하나가 없다»고 자백) ② 대조군 지정(코드 아닌 절차)
나머지는 순서대로 간다 — 역설적으로 **카나리 창 자체가 M2·M3이 먹을 데이터를 생산한다**(blocked이 execute의 15배였던 구조가 재개된다). 이것이 「빨리」와 「순서」를 둘 다 만족시키는 교차점이다.

### Q3. 총이익 합격기준 — 모순 셋의 해소
① M4 원문에 총이익 항이 없다 → **C5로 항을 신설**하되 D-NAO-230 각주 형식 그대로(자·가정·창 병기)
② 구간 자 운용은 「하한으로도 흑자인가」 → C5 판정값을 **하한 기준 ≥0**으로 못 박고, 하한 적자면 **미달로 닫는다**(미달은 유효한 종결)
③ 액셀은 원리적으로 못 잰다 → 총이익을 **개별 행위 귀속이 아니라 캠페인 «합산»**으로 잰다. 합산 축(date×campaign)은 그레인이 존재하고 08-24 자 정직화가 이미 라이브로 계산했다
⇒ **잴 수 있는 층위에서 잰다. 못 재는 층위는 주장하지 않는다.** C5는 「카나리 창에서 흑자였다」는 관측이지 「우리 엔진이 흑자를 만들었다」가 아니다 — 후자에 접근하는 유일한 길이 P3 대조군 대비이고 그것도 관찰 대비이므로 한계 병기

### Q4. 액셀·브레이크 대칭 (§7 금지선)
**성과 대칭은 이번 카나리에서 검사할 수 없다.** 액셀(상품 단위 입찰)은 성과 축이 없어 「액셀이 총이익을 늘렸나」는 판정불능이 예약돼 있다.
정직한 대체는 **행위 대칭**(C6): 상향/하향/제외/재개 건수 분포를 주간 보고에 병기하고, 브레이크로 편중되면 그 자체를 표류 신호로 기록.
★**귀결을 명문화한다**: 이 카나리의 합격은 「액셀·브레이크가 대칭으로 **작동**했다」까지만 주장할 수 있고, 「대칭으로 **성과**를 냈다」는 상품 축 원장(별건)이 생기기 전까지 **누구도 주장하지 못한다.**

### Q5. 대상 선정 규칙 (전역 규칙 — 이름 하드코딩 없음)
1. prod 캠페인 **전수**를 후보 모집단으로 한다(표본 아님)
2. **배제**: memo·rationale에 실험 배치 표기(A/B 열·MOP 열·대조군·홀드아웃)가 있는 캠페인 — 배제 사유를 표에 남긴다
3. **확인**: 대행사가 현재 집행 중인 캠페인은 D-NAO-241 통보가 **그 캠페인에** 유효한지 확인 — [미상이면 미상으로 표기]
4. **우선**: 실험 설계상 우리 운영이 예정된 캠페인 + 판정 표본이 모일 만큼 노출·클릭 발생률이 있는 캠페인 — 발생률 실측치 병기
5. 산출 = 후보 목록 + 항목별 판독(memo 원문 인용) + 배제 사유표 → **최종 지정은 Jino §8-① 승인의 몫** → 승인 즉시 스코프 행 등록 + 대조군 명단 동시 기록

### Q6. 되돌리기 (전부 있는 것 — 신설 0)
**즉시**: 킬스위치 `auto_operate` OFF 한 줄(전체 정지) · **스코프 행 삭제**(`blocked_by_scope`를 생성·실행 두 게이트가 같이 읽으므로 **행 삭제 = 행위 0**, 캠페인 단위 부분 정지) · `optimizer='none'` 복귀(실행 직전 하드체크가 막는다)
**상시**: CD1~5 출혈밸브(매시) · DL 일일 손실 고삐(pause 아닌 leash) · 자동 상향 누적 상한 2.0배(리셋은 사람만)
되돌림 실행 시 그 사실을 diary(`voided` 경로 존재)와 트랙 확인줄에 남긴다 — **되돌림 자체도 학습 재료다**

---

## 초안의 자기 한계 (저자 자백)
1. C4 표본 하한 ≈52건은 **n=34 위의 숫자라 약하다** — 첫 주 실발생률로 재산정을 합격기준 안에 내장했다
2. M2의 내용·대조군 지정의 M 소속은 이 초안이 못 본 절에 있다 — 승인 전 북극성 §목차 대조(D-NAO-227)를 승인 절차에 포함할 것을 권한다
