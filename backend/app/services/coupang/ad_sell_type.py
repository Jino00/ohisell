"""광고 옵션 원장(`coupang_ad_option_daily.sell_type`)의 판매방식 축 — **단일 정의**.

## 왜 이 파일이 따로 있나 (2026-08-13)

쿠팡에서 우리가 파는 방식은 셋이고, **돈의 모양이 서로 다르다**:

| 코드 | 이름 | 물건 위치 | `orders` 테이블 |
|---|---|---|---|
| `Retail` | 로켓배송(1P) | 쿠팡이 **우리에게서 매입** | **행이 없다** — 판 게 아니라 납품이다 |
| `2P` | 로켓그로스(RG) | 우리 재고를 쿠팡 물류센터에 | 있다(`coupang_rg_order_item`) |
| `3P` | 마켓플레이스(Wing) | 우리 창고 | 있다(`orders`) |

문제는 **오하이테크가 이 셋을 같은 `vendor_id`(A01029796)로 갖는다**는 것이다. 그래서
`vendor_id`만으로 거르면 1P 광고비가 3P 매출 옆에 붙는다 — 매출은 없는데 비용만 있는 축이라
이익이 통째로 뒤집힌다.

**이 사고는 두 번 났다.**
1. 2026-08-03 커맨드센터: 3P 매출 160,500원에 1P 광고비 5,450,601원이 얹혀 net_profit이
   +67,821 → −5,382,780으로 뒤집혔다. `intelligence._agg_ads`에 필터를 넣어 고쳤다(교훈 #103).
2. 2026-08-13 쿠팡 운영 패널: **같은 결함이 그대로 있었다.** `coupang_ops.sales_summary`는
   별도 쿼리를 직접 써서 1번의 수정이 닿지 않았다. 라이브(2026-08-12 오하이테크):
   매출 53,700원 · 광고비 576,573원(= 3P 40,361 + Retail 536,212) → 이익 −543,622원.
   실제 값은 −7,410원이고, 같은 화면 하단 표는 이미 그 값을 보여주고 있었다.

두 번 같은 모양이면 개별 수리를 멈춘다(교훈 #283·#281). 그래서 축의 정의를 **여기 한 곳**에
두고 주문 기반 화면들이 전부 이걸 참조한다. 새 화면이 광고 원장을 읽을 때도 여기서 가져간다.
"""

# 광고 XLSX(광고센터 보고서)의 판매방식 코드. ad_costs 적재 시 확정된 리터럴이다.
AD_SELL_TYPE_1P = "Retail"  # 로켓배송
AD_SELL_TYPE_2P = "2P"      # 로켓그로스(RG)
AD_SELL_TYPE_3P = "3P"      # 마켓플레이스(Wing)

#: **주문 기반 화면이 쓰는 축.** 매출이 `orders`/`coupang_rg_order_item`에 실재하는 판매방식만.
#: 1P는 주문 테이블에 행이 없으므로(쿠팡 매입) 이 축에 넣으면 «매출 없는 비용»이 된다.
#: 1P 손익은 별도 엔진(`rocket_1p_channel_pnl`)이 납품가 축으로 다룬다.
ORDER_BASED_SELL_TYPES: tuple[str, ...] = (AD_SELL_TYPE_3P, AD_SELL_TYPE_2P)

#: 주문 기반 화면에서 **의도적으로 뺀** 판매방식. 뺀 금액은 숨기지 말고 화면에 남긴다
#: (오하이테크 최근 30일 Retail 광고비 20,947,574원 — 조용히 사라지면 정합이 아니라 은폐다).
#:
#: ⚠️**쿼리에는 이 열거형을 쓰지 마라.** 「담은 것 + 뺀 것 = 원장 전액」이 성립하려면 뺀 쪽은
#:   `ORDER_BASED_SELL_TYPES`의 **여집합**이어야 한다. 열거형으로 세면 제4의 코드가 생기는 날
#:   어느 통에도 안 잡혀 조용히 사라진다(적대 리뷰 1R P2-2). 이 상수는 «지금 무엇이 빠지는가»를
#:   문서로 밝히는 용도다.
EXCLUDED_SELL_TYPES: tuple[str, ...] = (AD_SELL_TYPE_1P,)

#: 판매유형으로 **가를 수 없는** 금액이 가는 통. 일별 폴백(옵션 분해 없는 날의 계정 단위
#: 광고비)과, 아직 이름 없는 채널 축이 여기로 온다. 이 통이 있어야 «분해의 합 = 총계»가
#: 코드 구조상 항상 참이 된다 — 없으면 새 축이 생기는 날 이익이 조용히 어긋난다
#: (적대 리뷰 1R P1-2: 오픽스 7일 탭에서 실제로 1,363원 어긋나 있었다).
UNASSIGNED_CHANNEL_TYPE = "미분류"

#: 광고 판매방식 → 화면의 채널 축 라벨(`_CHANNEL_META`의 두 번째 원소와 같은 어휘).
SELL_TYPE_TO_CHANNEL_TYPE: dict[str, str] = {
    AD_SELL_TYPE_3P: "Wing",
    AD_SELL_TYPE_2P: "로켓그로스",
    AD_SELL_TYPE_1P: "로켓배송",
}
