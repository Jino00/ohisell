# TODOS

> 트랙 외/후속 작업 백로그. 트랙 결정사항은 docs/tracks/active/*.md 우선.

## RG 수수료 회계 트랙 (track_coupang-rg-fee-accounting)

- [x] **크로스채널 대시보드 쿠팡 순이익에 RG 비용 반영** (S7 eng-review D4, 완료 7269a38)
  - **What:** `app/routers/dashboard.py` / `app/services/profit_calculator.py`의 쿠팡 순이익에도 RG 정산 비용(non-ad, D-15)을 반영.
  - **Why:** S7 후 command-center 쿠팡 순이익(RG 반영)과 다채널 대시보드 쿠팡 순이익(RG 미반영)이 화면 간 불일치. 예전 배송비 회계 4화면 불일치(docs/PLAN.md 이력)와 같은 종류.
  - **Pros:** 화면 간 순이익 일관성.
  - **Cons:** profit_calculator 구조 재조사 필요(쿠팡 정산 경로가 command-center와 방법론 다름 — 옵션단위 실측수수료 vs 정산). 머니코드라 별도 신중 검토.
  - **Context:** 두 화면은 이미 방법론 차이로 기존에도 쿠팡 순이익이 다를 수 있음. command-center가 쿠팡 deep-dive 권위. S7은 command-center만 플립(의도적 스코프).
  - **Depends on:** S7 완료(D-15 non-ad 차감 공식 확정 후 동일 규칙 적용).

## 배포 하니스 (트랙 외)

- [x] **~~`/codex review` — safe_deploy.sh 마이그레이션 순서 가드~~ → Jino 면제 (2026-07-28, PR #129)**
  - **결정:** Jino 원문 — "이 건은 codex review 건너띄자". 원칙 19 게이트를 **이 건에 한해 면제**.
    (codex 쿼터 소진 2026-08-02 21:52 대기 대신 면제 선택 — 재개 후에도 소급 실행하지 않음.)
  - **면제 시점의 검증 근거:** 스텁 ssh/scp/alembic 회귀 하니스로 12개 시나리오·19개 단언
    (정상/대기차단/--migrate 순서/파일누락/upgrade실패/CAS/락/restart차단/멱등/fail-closed) + 실 prod
    읽기전용 프로브(`alembic current`=`heads`=a1c3e5f7b9d1). **다른 모델의 독립 리뷰는 없음.**
  - **미검토로 남는 지점(면제된 리스크, 나중에 문제 나면 여기부터):** ①`current` vs `heads` 판정이
    다중 head/mergepoint에서 안전한가 ②`git ls-files` 기반 누락 탐지의 오탐 여지
    ③`--migrate` 자동 upgrade의 권한 범위.
  - **오탐 시 대처:** 이 가드가 배포를 잘못 막으면 원인이 위 ①②일 가능성이 높다.
    긴급 시 게이트 우회 플래그는 **없음** — `scripts/safe_deploy.sh`에서 §2 블록을 임시 무력화하거나
    수동 `scp` + `alembic upgrade head` 로 배포하고 즉시 가드를 고칠 것.

## PAO 트랙 (track_naver-ad-optimization) — gstack `/plan-eng-review` 2026-09-06 배출분

- [ ] **계기판 5개를 하나로 — 「지금 어디까지 됐나」를 말하는 표가 다섯 군데다** (D-NAO-294, 관측 11건)
  - **What:** `ref 131`(PAO 기능 전수 60행) · `ref 137`(A/B/C 46행) · `ref 80`(연관 확인도 36축) · `ref 115`(빠진 손) ·
    북극성 `ref 82` §3(층별 %)을 **하나의 정본 + 나머지는 포인터**로 정리한다.
  - **Why:** 다섯 표의 측정일이 전부 달라 **어느 걸 봐도 현재를 정확히 못 말한다.** 실제 비용이 두 번 관측됐다 —
    ①2026-09-03 census가 「제외 재개방」을 「손이 아예 없다」로 세었는데 실은 **2026-09-01에 이미 닫혀 있었다**(`ref 137` §2)
    ②2026-09-06 「A-7 36일 정지」가 **4곳에 복사된 같은 한 숫자**였고 검산할 곳이 없어 그대로 지시로 올라왔다(실제 8일, `ref 137` §10-2).
  - **Pros:** 「안 됐다고 적혀서 아무도 안 보는데 실은 돌아가던 것」을 구조로 막는다. 다음 census 세션이 분모를 새로 발명하지 않는다.
  - **Cons:** 다섯 문서가 **각각 다른 세션의 판정문**이라 합치는 순간 소급 수정 문제가 생긴다. 반나절+ 작업이고 별도 계약감이다.
  - **Context:** 이미 알려진 문제다 — n=83이 *「세 목록 어느 것도 단독으로 현재를 못 말한다」*를 적었고,
    n=88 C1·C2·C3가 「계기판 지체」 4건을, n=90이 C-② 1건을, n=91이 A-7·B-11 2건을 더했다. **관측 11건, 진단만 있고 집행 0.**
    `docs/wiki/WISDOM.md`의 `enforcement:` 부채 후보. 시작점은 `ref 137` §9(재측정 방법)와 `ref 131` §4(행 단위 분모).
  - **Depends on:** 없음. 다만 소급 수정 경계를 먼저 정해야 한다(원문 보존 + 포인터 방식이 이 저장소 관례).

- [ ] **ADVoost·GFA 90일 528만원 — 성과를 원리적으로 못 재는 지출의 처분** (북극성 §8-④, 어느 M에도 미배정)
  - **What:** 북극성 §8 ④가 *「ADVoost·GFA 취급(90일 528만원, 성과 관측 원리 불가) — 현상 유지 / 대조축 개통 / 지출 재검토」*를
    미결로 적어 뒀는데 **M0~M6 어느 칸에도 배정되지 않았다.** 세 선택지 중 하나로 처분한다.
  - **Why:** 트랙 궁극 목표가 **총이익 절대액 최대화**(D-NAO-59)인데 **가장 불투명한 지출이 관할 밖**이다.
    분기마다 500만원대가 나가고 그에 대한 결정이 어느 일정에도 없다. 3주째 §8에서 안 움직였다.
  - **Pros:** 「모든 광고 형태」라는 §1 문언과 실제 관할의 간극이 닫히거나, 최소한 **명시적으로 열린 채로 기록**된다.
  - **Cons:** 성과 관측이 원리적으로 막혀 있어 **데이터로 답이 안 나온다** — Jino 판단이 필요한 종류다.
    「대조축 개통」을 고르면 스마트스토어 주문 축(§8-⑤, 창 5.5개월 대기)과 묶여 범위가 커진다.
  - **Context:** §1이 이미 *「ADVoost·GFA는 성과 관측이 원리적으로 막혀 있어 별도 취급」*으로 괄호 처리해 뒀으므로
    **「7/7 완주해도 미완성」은 아니다** — 남는 건 지출 결정 하나다. 근거 좌표는 북극성 §4-5 · `ref 65` §10-2.
  - **Depends on:** 없음(Jino 발의로만 착수 — 전역 §10).

- [ ] **광고 API 표면 개봉 — 층1 잔여 89 endpoint** (트랙 「안함」의 배출처, D-NAO-294)
  - **What:** 광고 endpoint **21/110** 개봉. 트랙 「안함」이 *「소관: 없음(신규 계약 필요)」*으로 적어 두어 배출구가 없던 것을 여기로 뺀다.
  - **Why:** 「우리 일 아니다」고만 적고 갈 곳을 안 적으면 그 일은 **아무 데도 안 가고 사라진다.**
  - **Pros:** 사라질 일감이 좌표를 갖는다. **Cons:** 신규 계약 1장이 여전히 필요하다(범위·예산·순서).
  - **Context:** 분모 정본은 `data/75_api_surface_census/` + 매트릭스 §0-A. 「모든 데이터 완료」 선언은 여전히 금지(북극성 §6-b M1 「안 함」).
  - **Depends on:** 신규 계약(Jino 승인).
