# 세션 인수인계: **「점화 목표」 계약 승인 + PAO 학습 사슬 병목 규명**

> 저장일시: 2026-08-25 00:4x KST · 체인 `pao-논의` **n=48** · 세션 `a3d1c01c`
> 앞 문서: `HANDOFF_pao-adgroup-scope_20260824.md`(n=47)
> **코드 변경 0줄 · prod 배포 0건 · 광고 계정 쓰기 0건** — 이번 세션은 전부 «조사와 계약»이다

---

## 0. ★★우리가 향하는 곳 — PAO 북극성 궁극 목표 (원문 그대로)

> **정본**: `docs/references/82_pao_north_star_20260819.md` §1 (D-NAO-208)

**PAO의 궁극 목표는, 네이버 광고·스마트스토어 API가 주는 모든 지표와 4등급 성과등급 사이의 «개연성(연관)»을 홀드아웃 검증까지 통과한 지식으로 확정하고(①), 논문에서 채택한 기법과 이 트랙이 이미 세운 실행 구조(레인·가드레일·고삐·확장압력) 위에서(②), 우리가 운영하는 모든 광고 형태를 총이익 절대액이 최대가 되는 방향으로(③, D-NAO-59) 자동화 운영하며(④), 운영의 매 행위와 결과를 일기→반성→지혜 승격의 학습 사슬에 태워 어느 지점을 배우면 성능이 더 오르는지를 스스로 찾아, 신호가 허락하는 최단 주기로 성능개선을 반복 시도하는 것(⑤)이다.**

### 트랙 최상위 목표 (D-NAO-59, Jino 원문 2026-07-19)
> *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야."*

**진행률 2/7 (M0·M1) — 이번 세션 불변.** ④자동화 **21세션째 착수 0**.

---

## 1. 프로젝트 위치 및 환경

- 로컬: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling`
- ⚠️ **공유 메인 폴더는 origin/main보다 ~300커밋 뒤처져 있다.** 코드·문서는 `git show origin/main:<경로>` 또는 워크트리로 읽어라.
- 이 세션 워크트리: `~/.claude-worktrees/ohiselling/pao-n48` · 브랜치 `docs/pao-n48`
- prod: `sellc.ohitech.co.kr` · DB `/home/ubuntu/ohisell/backend/ohisell.db` · 배포는 반드시 `scripts/safe_deploy.sh`
- prod 읽기 전용: SQL을 파일로 써서 scp 후 `ssh sellc.ohitech.co.kr 'sqlite3 -readonly "file:...?mode=ro" < /tmp/q.sql'` (ssh heredoc은 따옴표를 먹는다)
- ⚠️ **훅 오탐 12건** — `track-progress-sync.sh`가 **공유 폴더의 stale 트랙 사본**을 읽어 워크트리의 확인줄을 못 본다. 공유 사본에 쓰면 앞선 세션들의 확인줄 9개가 되돌려질 위험이 있어 **쓰지 않았다.** 구조 문제이므로 다음 감사 안건.

---

## 2. 이번 세션 완료 목록

### 2-A. 인계 §6 미완 15항목 실측 인구조사
- 유효 9 / 부분유효 3 / 관측불가 2 / **유령 0**
- 신규 사실 2: `inventory` 테이블 **행수 0**(h 문턱 재료가 실제론 없다 — 인계 누락) · 승인은 쌓이는데 실집행 0

### 2-B. ⑪ 과거 입찰 변경 반응 곡선 실측 → `docs/references/96_bid_response_curve_census_20260824.md` (커밋 `3d2e7ef3`)
- **원래 물음의 답은 「못 정한다」**: WEB_SITE 키워드 계열 노출변화 중앙 효과 **−0.022**·sd **2.982** → 효과가 산포에 파묻혀 표본 공식 붕괴. SHOPPING 광고그룹만 μ=1.312·σ=3.381 → ≈52건(7.64건/일) → **7일급**이나 n=34라 약함
- ⇒ **계약에 일수를 박을 근거가 없다.** 「날짜가 아니라 표본으로 판정」이 실측으로 뒷받침됨
- 전후 7일 완전창은 716건 중 **109건(15%)**뿐
- **액셀 축 구조적 벽**: `update_bid`의 **32%(136건)가 상품(소재) 단위**인데 `naver_ad_daily` 그레인에 상품 축이 없다 ⇒ 원리적으로 성과를 못 붙인다

### 2-C. ★ref 96 정정 (커밋 `123283ca`) — 같은 세션이 자기 결론을 뒤집었다
- 초판이 *"`update_bid` 425건 중 194건(45.6%)이 before/after를 비운 채 기록 — 우리가 대행사보다 못 남긴다"*고 썼는데 **틀렸다**
- 194건은 **입찰이 실제로 바뀌지 않은 행**: **GUARD_BLOCK 174**(BEP·스톱로스·일예산·쿨다운 — API 호출 자체가 안 나감) + **WRITE_FAILURE 20**(네이버 400 `code=3830`). 값도 빈 문자열이 아니라 **NULL**
- `before/after`를 채우는 코드는 **성공 실쓰기 1곳**(`naver_execution_harness.py:412-420`)뿐이고 나머지는 설계상 안 넘긴다 — **바꾼 게 없으니 적을 값이 없다**
- **비교 자체가 성립하지 않았다**: 대행사 `external_bid_change` 621건은 `entity_sync`가 **이미 일어난 변경을 관측**해 적는 것이라 결손 0이 당연. 나는 **「우리의 시도 기록」과 「대행사의 실제 변경 기록」**을 견주고 있었다
- ★**학습 재료는 하나도 안 잃었다** — GUARD_BLOCK 174건은 `_guard_failure()`(`:274-303`)가 `blocked` 일기를 남긴다
- ⇒ **켜기 전 「로깅 수리」는 선행조건이 아니다.** 임계 경로에서 중형 작업 하나가 빠졌다

### 2-D. ★학습 사슬 병목 규명 (일기 4,420 → 후보 27 → 지혜 1)
- `ops_diary_entries` 4,420행의 정체: `blocked` **3,863**(전건 outcome 보유, **07-30에서 끝**) · `execute` 250(~08-11) · `reject` 43 · **`observe` 263**(지금도 쌓이지만 **outcome 0건**이라 harvest가 원리적으로 못 집음) · `voided` 1
- `ops_wisdom_candidates` **27행**(마지막 08-13) → `ops_wisdom_entries` **1행**(07-26 이후 0)
- ★**판사는 정상이다** — 27건 중 22 `rejected`, 전건 근거 원문 보유. 사유의 압도적 다수가 **「관찰이 하루~며칠에 몰려 재현성 판단 불가」**(81회·승률 98%도 "4일치라 부족"으로 기각). 유일 승격 1건은 **71회·다일 재현**
- ★★**승격은 실집행을 요구하지 않는다** — `blocked`(검토했으나 집행 안 함)도 재료다(`diary_outcome.py:26` · `wisdom_candidates.py:23`). 인계의 「M3는 실집행 없이 원리적으로 못 채운다(순환)」은 **뒤 토막에만 참**이다
- 크론은 정상 발화(`scheduler_service.py:788`, `"45 8 * * *"`, 08-22·08-23 `{harvest:ok, judge:ok, writer:ok, retention:ok, apply:ok}`)
- ⚠️**08-24 08:20~10:08 KST prod 백엔드 1시간 48분 완전 다운** → 08:35 반성·08:40 손익성적표·**08:45 지혜** 크론 3개 통째 미발화(원인 미상)

### 2-E. ★`reviewed=0`의 원인 — 코드 한 줄
- `auto_operator.py:1169-1172`(hourly)·`:294-298`(daily)가 `WHERE auto_operate IS True`로 순회 대상을 고르는데 **prod 9행 전건 `auto_operate=0`** ⇒ `for` 루프가 0회 실행, `reviewed`는 그 안쪽(`:3237`)에서만 증가
- 로그가 `reviewed=0 held=0 skipped=0`으로 **셋 다 0**인 것이 「서킷브레이커」가 아니라 **「순회 대상 없음」**을 확증
- **07-30 10:48:30~31, 6개 캠페인 전부 `optimizer: ours→none` 일괄 전환**(콘솔 PUT, rationale "커맨드 센터 관리주체 스위치", **주체 미기록**). auto-lane 마지막 활동이 07-30 09:20~10:48이라 정확히 겹친다
- 나머지 캠페인의 `auto_operate=0`은 **change_log에 기록이 없다**(로그를 안 남기는 직접 UPDATE 경로 — **언제·누가 껐는지 관측불가**)
- ★**「무접촉이면서 학습 재료를 만드는」 스위치는 코드베이스에 없다**: auto-lane 4종이 `auto_operator.py:868/1052/2715/2846/3807`에서 **`dry_run=False` 하드코딩** ⇒ 가드레일 통과분은 곧장 네이버 API PUT. 유일한 dry-run 스위치(`NAVER_BP_DRY_RUN`)는 prod 환경변수에 **키 자체가 없고**, 그 경로는 일기를 안 남기며 가드레일 평가도 건너뛴다
- ⚠️`OptimizerGuardError`(`naver_execution_harness.py:2509-2513`)는 **일기에 전혀 안 남는다**(호출부가 `except Exception: failed+=1; log.warning`로 삼킴). 같은 함수의 킬스위치·스코프 가드·`_guard_failure`는 전부 `blocked` 일기를 남기는 것과 대조 — **관측 사각**

### 2-F. ★★D-NAO-247 — 「점화 목표」 계약 Jino 승인 (커밋 `b488ec92`)
- 정본 `docs/contracts/CONTRACT_pao_ignition_canary.md` · 승인 2026-08-25 00:33 KST, 원문 *"승인해."*
- **켜기 전 할 일이 8건 → 3건으로 줄었다**(실측이 5건을 걷어냄): 로깅 수리 불요 / **데이터 하한 게이트 불요 — 지혜 판사가 이미 그 일을 한다** / 7월 잔여물은 D-NAO-242가 수리 / 주말 라벨·페이지네이션은 판정 전제 아님
- **남은 선행 = P2(대상 선정) · P3(대조군 기록) · P4(지혜 id 조인)**
- ★§7 「액셀·브레이크 대칭」을 **성과로는 못 잰다**고 계약에 명문화: *"이 카나리의 합격은 「대칭으로 «작동»했다」까지만 주장할 수 있고 「대칭으로 «성과»를 냈다」는 상품 축 원장이 생기기 전까지 누구도 주장하지 못한다"*
- 금지선에 실사고 처방: **memo에 실험 배치 표기가 있는 캠페인 점화 금지**

### 2-G. P2 대상 선정 판독 → ★**모집단 오류 발견** (커밋 `970b4852` · `d5ec816e`)
- 1차 판독(9개 기준): 배제 5(실험 배치 2·기타 3) / 후보 4
- ★★**그런데 모집단이 틀렸다**: `naver_entity` 캠페인 **46개** · `naver_campaign_settings` **9행** · **최근 7일 실집행 24개**. 「9개」는 계정 전수가 아니라 **«우리가 설정 행을 만든» 9개**였다
- **7일 실집행 24개 중 settings 보유는 2개**(01.TPU · 03.강화유리). 나머지 22개는 `NO_SETTINGS`이고 거기에 지출 상위가 대거 있다(○갤럭시 파워링크 **740,960원/7일** · ●01.갤럭시_지문방지_사생활 507,609 · ●09.기타상품 381,985 · ●02.갤럭시_보급형_M 211,875)
- ⇒ **엔진의 관할이 실집행 캠페인의 8.3%(2/24)에 그친다.** 「9개를 다 켜도 살아 있는 건 2개」
- ★**후보 4개 중 3개는 이미 멈춘 캠페인**이었다(마지막 노출일): 컨텐츠매체 **08-10** · 아이패드 파워링크 **07-30** · 폴드8 **07-30**. 「일평균 노출 47,447로 최대」는 08-10 이전 과거분을 30으로 나눈 값 — **집계 창이 «현재»를 가렸다**
- ⇒ **후보 중 지금 살아 있는 건 01.TPU 하나뿐**(7일 노출 79,721·비용 **1,156,332원**)
- [미상] ⓑ **해소**: `status='off'`의 근거는 `user_lock` 하나뿐(`entity_sync.py:83-95`, D-NAO-97)이고 후보 3건이 `status_reason=CAMPAIGN_PAUSED`(자기 OFF) ⇒ **「사람이 꺼 둠」이 맞다**

---

## 2-1. 완료 QA (별도 Sonnet·읽기 전용, 2026-08-25 00:4x KST) — **판정 원문 그대로**

- **작업 목적(정본)**: 인계 HANDOFF §6 원문(Jino 08-24 13:07 *"키워드 관련한 광고 운영 관련해서 더 논의를 해서 기획을 완성하고 싶어"* + 17:1x *"아직 오늘 논의한 내용조차 완성이 안됬는데"*) + 세션 중 Jino 00:21 *"이건 나중에 하고 일단 우리껄 빨리 켤수 있게 우리것부터 진행하자"*
- **합격기준(정본)**: 승인된 계약 D-NAO-247 §4 — 선행 P2·P3·P4 + 카나리 창 C1~C7

**판정(인계 HANDOFF §6 작업 목적 원문): 목적전환(선언 있음)** — 인계는 "논의를 완성한 뒤 켜라"였으나 세션 중 Jino 본인이 00:21 *"이건 나중에 하고 일단 우리껄 빨리 켤수 있게 우리것부터 진행하자"*로 직접 방향을 좁혔다. 이 전환은 트랙 확인줄(`91221f0a`, 00:26:45)에 Jino 원문 그대로 인용·선언되어 있어 §2의 "선언 없는 전환=미달" 규정에 걸리지 않는다. (2026-08-25 KST)

**판정(승인된 계약 §4 합격기준 D-NAO-247): 미달** — P2(대상 선정)는 착수했으나 모집단 오류(9캠페인≠전수)를 세션 스스로 발견해 체크박스를 찍지 않고 재실행이 필요한 상태로 남았다. P3(대조군 지정)·P4(지혜 id 조인)는 착수 흔적이 없다. C1~C7은 계약 승인 시각(00:33)이 세션 종료 직전이라 미착수가 정상이나, 그렇다 해도 §4 전 10항목 중 체크된 것은 0개다. 다만 **계약 자체의 성립(D-NAO-247 승인)과 P2 모집단 오류의 자기발견·자백은 §4 바깥의 실질 성과**로, 미달이 부실이 아니라 정직한 자기교정의 결과임을 병기한다. (2026-08-25 KST)

**판정(PAO 북극성 §1·§6 D-NAO-59): 부분달성** — 트랙 진행률은 **2/7 불변**(M4 체크박스 미이동), ④자동화는 실행 없음(라이브 SQL로 스위치·쓰기 0 확인)이 계속됐다. 그러나 이 세션은 M4 게이트를 "Jino 결정 2건→1건"으로 정확히 좁혔고(§8-② 소유권 분리는 D-NAO-241로 기승인 확인), §7 "학습 오염" 금지선을 memo 판독으로 실제 집행해(A/B 대조군 캠페인 `03.강화유리`를 정확히 식별·배제, DB memo 원문과 확인줄 인용이 완전 일치) 실사고(n=45)의 재발을 절차로 막았다. §7 액셀·브레이크 대칭도 "성과는 판정불능, 행위 대칭만 관측 가능"이라는 정직한 한계를 계약에 명문화했다(귀결 문구 확인). ⑤학습 사슬 병목 원인(원료 고갈)도 이 세션 전반부에 규명됐다. §7 위반은 발견되지 않았다. (2026-08-25 KST)

**종합 판정: 부분달성** — 세 대조 대상 중 목적 전환은 정당하게 선언됐고, 북극성 대비로는 진단·게이트 정리·안전장치 검증에서 실질 진전이 있었으나, 계약 §4의 문자 그대로의 합격기준은 하나도 달성되지 않았다(P2 미완·P3/P4 미착수). 스위치·쓰기·스코프 행 등 "안 함" 경계는 라이브 증거로 전건 불변 확인됐고, 산출물은 문서 3건뿐(코드·PR·배포 없음)이라는 자기 신고와 완전히 일치했다.

**QA가 라이브로 검증한 「안 함」 경계** (전건 통과):
- `naver_campaign_settings` 9행 전건 `auto_operate=0 ∧ optimizer='none'` — **불변**
- `naver_adgroup_scope` **0행** — 불변
- `MAX(changed_at) WHERE dry_run=0 AND action NOT LIKE 'external_%'` → **2026-08-11 09:18:40** (08-24~25 신규 실쓰기 0건)
- 08-24 이후 change_log 전건 = `flight_pacing`(dry_run=1) 80 + `optimizer_change`(dry_run=1) 2건뿐. 후자는 **n=45의 카나리 on/off(17:02·17:15)**로 이 세션 시작(23:22:56) 이전임을 커밋 타임스탬프로 교차 확인
- `git diff origin/main --stat` → 변경 파일 전부 `docs/` — **코드 0줄·PR 없음·머지 없음**

**미달·미판정 항목** (다음 세션 슬라이스의 기본 후보):
- P2 재실행(모집단 오류) · P3 미착수 · P4 미착수 · C1~C7 전부 미착수(정상)

**QA가 확인 못한 것**:
- #7(갤럭시Z폴드8) last-exposure "07-30" — 해당 기간 `naver_ad_daily`에 `imp>0` 행이 조회되지 않아 **독립 재현 불가(판정불능)**
- `update_bid` 194건의 GUARD_BLOCK 174/WRITE_FAILURE 20 세부 분해 — `outcome='failed'` 총합 194는 일치 확인했으나 rationale 마커 기준 분리는 재현 안 함
- D-NAO-241 hands-off 통보 유효성 — 세션도 QA도 **판정불능**
- `ops_diary_entries`의 outcome 컬럼 기준 재현 생략(action명 기준으로만 교차)
- 타 활성 체인(`sellc-원가-메뉴`) 비침범은 diff 3파일 한정 확인 — 워크트리 전수 교차오염 검사는 안 함

**목적 전환 여부**: **선언 있음** — Jino 직접 지시(00:21)를 트랙 확인줄 `91221f0a`에 원문 인용으로 선언. 앵커 `이번 세션:`·`합격:`도 그 시점에 갱신했다.

---

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: "무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야." (Jino 2026-07-19 — D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음(M 체크박스 기준).** 계약 D-NAO-247이 M4 슬라이스를 열었으나 선행 3건 중 완주 0건이다. 이번 세션의 산출은 **M4를 막고 있던 것의 정체를 코드 좌표로 특정한 것**이다
- **헤더에 남긴 확인 줄**: 12개 누적(`dc8da776`~`d5ec816e`) — 정정줄 1개 포함
- **다음 세션 후보 항목**: 계약 P2 재실행(올바른 모집단) → P3 → P4
- **트랙 종결 여부**: 미도달(2/7)

---

## 2-3. 착지

- **완료 단계**: 커밋 → push → PR **#429** → ⚠️리뷰 생략(기록물만) → **머지 완료**
- **멈춘 단계**: 없음
- **재개 명령**: 해당 없음
- **좌표**: 커밋 `dc8da776`(첫) ~ `9f2d1c0`(HANDOFF, 16건) · PR https://github.com/Jino00/ohisell/pull/429 · **머지 `01e24b8b`**
- **리뷰 판정**: `⚠️ 리뷰 생략: 기록물만 — docs/contracts/CONTRACT_pao_ignition_canary.md · docs/references/96_bid_response_curve_census_20260824.md · docs/tracks/active/track_naver-ad-optimization.md · .claude/memory/HANDOFF_pao-ignition-contract_20260825.md · .claude/memory/chains/pao-논의.jsonl` (코드 **0파일**)
- **CI**: backend(py3.10) pass 9m8s · backend(py3.14) pass 8m5s · frontend pass 52s — **3/3 실통과**, `--force` 미사용
- **검증 명령**: `gh pr view 429` · `git log origin/main --oneline -3`
- ⚠️ **L5로 「저장소를 main에 세워둔다」 생략** — 로컬 main이 공유 폴더에 체크아웃돼 있다. 다음 세션은 `git switch -c <새> origin/main`으로 원격을 명시해 갈라져라

---

## 3. 확정된 결정사항 (번복 금지)

- **D-NAO-247 — 「점화 목표」 계약 승인**(Jino 2026-08-25 00:33 *"승인해."*). 정본 `docs/contracts/CONTRACT_pao_ignition_canary.md`. 선행 P2·P3·P4, 카나리 창 C1~C7
- **북극성 §8-② 게이트는 이미 해제됨** — D-NAO-241(Jino 08-24 15:32, 통보 완료 16:18 이전). **M4에 남은 Jino 결정은 §8-① 하나**(해제 범위·시점)
- **ref 96 §2 초판 해석 철회** — 「로깅 결손 45.6%」는 결함이 아니다. 카나리 선행조건에서 제외
- **`status='off'`의 의미** — 「마지막 동기화 상태」가 아니라 **「사람이 꺼 둠」**(`user_lock`)
- **Jino 방향 지시 2건**: 00:21 *"이건 나중에 하고 일단 우리껄 빨리 켤수 있게 우리것부터 진행하자"*(대행사 원장 학습은 이월) · 00:15 *"우리 광고에서 배우는건 의도에 의한 결과값을 배우는거고, 대행사, saas의 광고에서 배우는건 이렇게 하니 저렇게 되더라 같은 현상을 학습하는거네?"*(정책 학습 vs 현상 학습의 구분 — 배선이 다르다)

---

## 4. 핵심 파일 목록

| 파일 | 역할 |
|---|---|
| `docs/contracts/CONTRACT_pao_ignition_canary.md` | ★승인된 계약 정본(D-NAO-247) |
| `docs/references/96_bid_response_curve_census_20260824.md` | ★반응 곡선 실측 + §2 정정 |
| `backend/app/services/naver_ad/auto_operator.py:1169-1172,294-298` | `reviewed=0`의 원인(`WHERE auto_operate IS True`) |
| `backend/app/services/naver_ad/auto_operator.py:868,1052,2715,2846,3807` | `dry_run=False` 하드코딩 5곳 |
| `backend/app/services/naver_ad/naver_execution_harness.py:2509-2513` | `OptimizerGuardError` — **일기 무기록** |
| `backend/app/services/naver_ad/naver_execution_harness.py:274-303,412-420` | `_guard_failure`(blocked 일기) · 성공 실쓰기(before/after 채움) |
| `backend/app/services/naver_ad/wisdom_candidates.py:23` | `HARVEST_EVENT_TYPES=("execute","blocked")` |
| `backend/app/services/naver_ad/entity_sync.py:83-95` | `_campaign_status` — off의 근거는 `user_lock` 하나뿐 |
| `backend/app/services/naver_ad/trigger_watch.py:353` | 관찰 전용 루프(`trigger_pacing` 하루 84~135건, 항상 pending, expired 누적 4,860) |

---

## 5. 알려진 이슈 / 주의사항

### 5-A. ★★모집단을 「9개」로 읽지 마라 — 이번 세션 최대 발견
`naver_campaign_settings` 9행은 **우리가 설정한 캠페인**이지 계정의 캠페인이 아니다. `naver_entity`엔 46개, **최근 7일 실집행 24개**가 있고 그중 22개는 settings 행 자체가 없어 **엔진의 검토 목록에 원리적으로 들어온 적이 없다.** 북극성 ③(*"우리가 운영하는 **모든** 광고 형태"*) 대비 **관할이 8.3%**라는 사실이 처음 수치로 드러났다. **이건 카나리 대상 선정의 문제가 아니라 관할 범위의 문제다.**

### 5-B. 집계 창이 「현재」를 가린다
30일 집계로 일평균을 내면 **08-10에 멈춘 캠페인이 「노출 1위」로 보인다.** 후보 판정엔 **최근 7일 실집행**을 함께 봐야 한다.

### 5-C. 내가 위임문에 확정 사실을 잘못 실었다
P2 위임문에 *"prod 캠페인은 9개"*를 확정 사실로 넣었고 조사원은 충실히 그 안에서만 판독했다. **인계에서 온 숫자를 위임문에 그대로 싣기 전에 모집단부터 실측할 것.**

### 5-D. 기타
- 다음 가용 번호: **D-NAO-248 · 교훈 #356**. ⚠️`next_ids.sh`는 트랙 확인줄의 「다음 가용 D-NAO-NNN」 문구를 최댓값으로 **오독**한다(이번에 248을 냈으나 실부여 최댓값은 246이었다) — grep으로 교차 확인할 것
- 워크트리에 `node_modules` 없음 → 필요 시 `npm ci`. **다른 세션 워크트리의 것을 쓰지 마라**
- ⚠️`sellc-원가-메뉴` 체인이 살아 있다(n=2, `bd4adba0`) — 그 브랜치·트랙에 쓰기 금지

---

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: Jino 2026-08-25 00:21 *"이건 나중에 하고 일단 우리껄 빨리 켤수 있게 우리것부터 진행하자"* — 승인된 계약 D-NAO-247의 선행 3건을 완주해 점화 가능 상태를 만든다.

- **남은 슬라이스**:
- [ ] **P2 재실행 — 올바른 모집단으로.** 「최근 7일 실집행 24개」를 모집단으로 memo·외부집행·발생률 판독 → 후보·배제표 → Jino §8-① 지정. **묻지 말고 진행**(읽기 전용 조사)
- [ ] **★관할 범위 문제를 Jino에게 표면화** — 실집행 24개 중 settings 2개(8.3%). 「카나리를 어디에 걸까」보다 먼저 **「엔진이 계정의 얼마를 맡을 것인가」**가 정해져야 할 수 있다. ★이건 **범위를 바꾸는 결정**이라 물어야 한다
- [ ] **P3 대조군 지정·기록** — P2 지정 직후. 코드 아님, 트랙에 D-N으로 기록. **묻지 말고 진행**
- [ ] **P4 지혜 id 조인 1개** — 성적표 부품을 지혜 id로 묶는 읽기 조인 + 콘솔 표면. **묻지 말고 진행**(코드, PR 경계 적대 리뷰 필요)
- [ ] **[미상] ⓐ hands-off 통보 유효성** — DB로 확인 불가(08-24 이후 `external_*` 0건·rationale 검색 0건). **Jino만 답할 수 있다**
- [ ] **08-24 백엔드 1시간 48분 다운의 원인** — 크론 3개 미발화. 학습을 재개하면 이런 결번이 그대로 학습 공백이 된다. **묻지 말고 진행**(조사)
- [ ] **이월(별건)**: 대행사·SaaS 원장에서 «현상» 배우기 — Jino 00:21 지시로 이월. ★설계 결론 보존: **지혜 사슬은 «정책» 학습기라 그대로 못 꽂는다. 현상 층(반응 곡선·포화점·필요 표본)이 새로 필요하고, 그 배선은 그대로 SaaS 다계정 학습의 배선이다.** 스키마를 「우리 일기」가 아니라 **「관찰된 행위 원장」**으로 일반형으로 잡을지가 그 갈림길
- [ ] **이월(관측 사각)**: `OptimizerGuardError` 일기 무기록 · 콘솔 페이지네이션(`limit:100` 고정) · `observe` 263건이 outcome 없어 harvest에 안 잡히는 구조

---

## 7. 새 세션 시작 프롬프트

```
/session-relay PAO 논의
```
