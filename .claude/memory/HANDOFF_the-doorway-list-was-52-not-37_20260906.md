# HANDOFF — 문 앞 목록은 37이 아니라 49였다 (2026-09-06, `pao-논의` n=93)

**세션**: `522b0699` · **체인**: `pao-논의` n=93 (20:24 KST 등록) · **결정**: D-NAO-295
**성격**: prod **읽기 전용** 실측 + 문서 산출. **코드 0줄 · prod 쓰기 0 · 네이버 광고 API 호출 0 · 마이그 0 · 배포 0.**
**진행률 2/7 불변.** (인계: `HANDOFF_the-bottleneck-was-not-the-decision_20260906.md` n=92)

---

## 검증 명령 (받는 쪽 어디서든 도는 한 줄)

```bash
git fetch -q origin main && git show origin/main:docs/references/140_out_of_scope_adgroup_census_20260906.md | grep -c '^## §'
```

**기대 출력**: `12` — `ref 140`이 §0~§11까지 온전히 `main`에 있다는 뜻.
(⚠️ iCloud 공유 메인 폴더는 `origin/main`보다 **1,400커밋+** 낡았다 — 그래서 `git show origin/main:`으로 읽는다.)

---

## 0. 목적 (원문 — 각색하지 않는다)

상위 목표는 그대로다: **D-NAO-59**(총이익 절대액 최대화) · 인계받은 **「1번을 자동화하자」**(Jino 2026-09-03).

이번 슬라이스는 **Jino가 n=92 HANDOFF §7의 6개 후보 중 1번을 고른 것**이다:
> ⓐ **스코프 밖 37그룹 실측표** — 광고비·전환·현 optimizer 소유(대행사/우리)·bleeding 체류일.
> 이게 카나리 후보 목록이자 **지금 당장 넓힐 수 있는 목록**이다(둘은 같은 표다).

---

## 1. 산출물

- **신설** `docs/references/140_out_of_scope_adgroup_census_20260906.md` — §0~§11(§11 = 적대 리뷰 기록)
- 트랙 `M4` 해제 조건 **ⓐ `[x]`** + 확인줄 **3줄**(착수 / 완료 QA / 적대 리뷰)
- PR **#765** 머지 `ed580a11` (2026-09-06 21:18:58 KST · **CI 3/3 실통과**: backend py3.10 13m23s · py3.14 12m12s · frontend 1m59s · `--force` 미사용) — 커밋 `d72a29a2` · `2a4fd1e1` · `9d85dd2e` · `3e5f52b9` · `e4f30711`

---

## 2. ★★ 실측이 갱신한 전제 둘

1. **「37」은 3일 창이 만든 수다.** 전 기간 **52그룹** · 현재 스코프 밖 **49**.
   7일 창과 30일 창이 같은 이유는 **사유문 「스코프 밖」이 2026-08-29 04:20:01부터만 존재**해서다 — **데이터가 9일치다.**
2. **요청된 「대행사/우리」 축은 값이 하나뿐이다** — 49그룹 전건이 `optimizer='ours'` 캠페인 2개 소속.
   소유권은 캠페인 optimizer가 아니라 `naver_adgroup_scope` 행이 표현한다.

---

## 3. ★★★ 엔진은 49 중 34에 이미 판정을 내려 놓고 그 판정을 버린다 (asof 2026-09-05)

| 군 | 엔진 판정 | 그룹 | 30일 광고비 | 무보정 RoAS |
|---|---|---:|---:|---:|
| A | `shopping_group_growth` — 올려라 | 21 | 1,886,415원 | 1.881 |
| B | `shopping_group_bep` — 내려라 | 13 | 736,570원 | 1.246 |
| C | 어느 보드에도 없음 — 판정 불능 | 15 | 709,448원 | 1.262 |

- **자를 바꾸면 A군이 21 → 7**: 보드가 쓰는 것은 **상한 자 ×1.3492**(`factor_high`). **하한 자 ×0.827**로 BEP 1.6521을 넘는 것은 **A군 7 · 전체 9그룹**(419,533원).
- **B군 5개는 최근 14 asof 전부 출혈 보드**(S26플러스·Z플립4·Z폴드2·S25플러스·S21플러스) — n=89의 「영원히 bleeding」 실물.
- **C군에 지출 1위가 있다** — S25울트라 **372,428원**이 판정 불능. 30일 광고비 **0원인데 매일 차단당하는 그룹 6개**.
- ★**「A군 전건이 목표를 넘는다」는 동어반복**이다(`shopping_group_growth` 정의 = 보정ROAS ≥ 목표, `account_diagnosis.py:1057`). §5에 자백.

---

## 4. 적대 리뷰 — 1R FAIL(P1 3건) → 2R PASS

| P1 | 무엇 | 처분 |
|---|---|---|
| 1 | 본문 「하한 자 통과 A군 6」 vs 내 데이터·표의 ✅ **7** — 문서 안에서 숫자가 갈렸다 | 7로 정정 |
| 2 | §0-2 「§8-② 소유권 분리를 기다리지 않는다」 **단정이 과했다** — 같은 `ours` 캠페인에 `external_status_change` **3건**(그중 2건이 이 49 안) | 「쓰는 행위」/「효과 귀속」으로 갈라 적고 §3에 실측표 |
| 3 | §2의 38/53/53 재현 실패 — **`GROUP BY`가 `adgroup_id` NULL 행을 그룹 하나로 셌다**(전 기간 6건) | 37/52/52 정정 + 창 경계 함수 명기 + §9에 쿼리 공개 |

★★**표면 절단 변이 2건 «둘 다 생존»** — ①`ref 140` §4 표 49행을 지워도 저장소에 이 문서를 참조하는 테스트·훅·CI가 **0건** ②트랙 `- [x] ⓐ` 줄의 경로를 없는 파일명으로 바꿔도 `track_scan.py` 출력이 **바이트 단위 동일**. ⇒ **이 문서의 «존재»를 아무도 검사하지 않는다.** 고치는 것은 이 슬라이스 범위 밖이라 §11에 적어 두기만 했다.

★**내가 내 오류를 리뷰어 오류로 적었다**(2R에서 리뷰어가 짚음) — `adgroup_id`/`created_at`으로 쿼리를 짜서 깨진 것은 **내 probe**였다. **«상대가 틀렸다»도 단정어다.**

---

## 5. 완료 QA (별도 Sonnet·읽기 전용) — 3대조 판정문 **원문 그대로**

> **판정(앵커 합격기준): 부분달성** — *「5개 중 4개 완전달성·1개(ⓐ 소유 축 칸)는 자백된 결손 포함 부분달성. 나머지 문서 내적 정합성(49행 카운트, code 좌표 `account_diagnosis.py:1057`, 커밋 diff `+276/-3` 3파일만, chain 등록부 n=93 note와 트랙 `확인:` 줄 내용 완전 일치)도 전부 라이브 재현됨」* (20:45 KST)
> **판정(D-NAO-59): 미달** — *「이번 슬라이스는 문서(ref 140) 신설과 트랙 체크박스 1건 갱신뿐이고 코드 0줄·prod 쓰기 0(라이브 확인)·네이버 광고 API 호출 0. 총이익에 대한 관측 가능한 진전은 0건이며, 세션 스스로도 확인줄에 "진행률 2/7 불변"이라 적어 이를 부인하지 않는다」* (20:46 KST)
> **판정(북극성 §1·§6): 부분달성** — *「§6 M4 해제 조건 ⓐ(3칸 중 1칸)만 채웠고 M을 닫지 않는다 … §1 5요소 중 "③총이익 방향"만 이동 … ④자동화 운영은 여전히 0」* (20:47 KST)
> **종합 판정: 부분달성**

**미달은 낮추지 않고 그대로 남긴다** — 이번 슬라이스는 「결정에 필요한 재료」였고 그게 총이익 진전으로 안 읽히는 것이 정확하다.

QA 지적 1건 채택(같은 기 재작업): §4 표에 **소유 칸 신설**. ★그 과정에서 **30일 광고비 0원인 4그룹은 원장 조인이 비어 소유가 미해결**이었는데 초판이 「전건 ours」로 뭉갠 것을 발견 — 차단 일기 `campaign_id` + 09-06 엔티티 스냅샷 두 경로로 확정하고 `†` 표시.
⚠️QA 관측 1건 정정: *「CI가 started 11:48:36Z로 3시간 이상 경과」*는 **UTC를 KST 시계와 비교한 것**(실제 2분).

---

## 6. 트랙 진행률

- **2/7 불변** (M0·M1 달성 / M2~M6 미달). M4 자체는 안 닫혔다 — **해제 조건 3칸 중 ⓐ만** 채웠다.
- ④자동화 운영 실집행 **이번 세션 0 — 연속 4세션째**(n=90·91·92·93).
- 북극성 대비 검토(D-NAO-226·227): §목차 §0~§9+부록A 훑고 **§1·§4 간극3·§6·§6-b·§7·§8** 전독. 5요소 중 **③만 이동**. §7 대칭 = 표 자체가 대칭(액셀 21 : 브레이크 13).

---

## 7. 남은 슬라이스 (고르는 건 Jino)

1. ★★**스코프 확대 결정** — `ref 140` §10이 선택지 셋을 놓았다: 하한 자로도 흑자 **9그룹**(419,533원) / 엔진 판정 그대로 **A군 21**(1,886,415원) / **대칭 확대 A+B 34**. **안 여는 것도 유효한 결정**이다. ⛔ 이 세션은 `naver_adgroup_scope`에 아무것도 쓰지 않았다.
2. **M4 해제 조건 ⓑ 대행사 개입 계수 · ⓒ 되돌림 감지율** — 3칸 중 2칸이 비어 있다. ⓑ는 §3의 `external_*` 5,270건이 출발점이다.
3. **C군 15그룹이 왜 보드에 없는지** — 표본 부족인지 부모 체인 조건인지 산출 실패인지 미규명. **지출 1위가 여기 있다.**
4. **30일 광고비 0원인데 매일 차단당하는 6그룹** 처분 — `search_term_promote` 940건과 같은 종류의 낭비.
5. n=92가 남긴 나머지: bleeding 그룹 분포(3일 23그룹은 이번에 쟀다) · B-5 24h 재측정 · `search_term_promote` 940건 처분 · **Codex 모델 핀 수정** · TODOS 3건.

⛔ **다른 세션 소관**: `발주예측` n=8 `c3b74f44` · `승인배선` n=2 `607c2c15` (둘 다 `end_kst` null ⇒ **모르면 살아 있다**).

---

## 8. ⚠️ 하네스 함정 (n=92에서 이어짐 + 이번 추가분)

1. **iCloud 공유 폴더는 워크트리를 못 만든다**(`mmap failed`) + `origin/main`보다 **1,400커밋+** stale. 로컬 **전체 클론**(비shallow) `~/ohisell-clones/pao-n93`에서 작업했다.
2. **훅 `[체인] ⛔ pao-논의 n=49`는 오탐** — 공유 폴더의 12일 묵은 사본. `origin/main` 기준 마지막 행 n=92는 닫혀 있었다.
3. ★**훅 `[트랙 기록 누락]`도 같은 사각지대** — 세션 내내 3회 발화했다. **재현 결과** 내 확인줄은 클론 계약 블록 «안»에 실재(3건)하고, 훅이 읽는 공유 폴더 트랙 파일은 **`bd7b6099`(2026-08-22)에서 정지**. **공유 폴더엔 쓰지 않았다** — 쓰면 남의 커밋에 쓸린다. PR 머지 후 `origin/main`에서 해소된다.
4. ★**파이썬 SQL에 LIKE `%`가 있으면 `% 포맷`을 쓰면 안 된다** — `"... '%스코프 밖%' ..." % CAND`가 `TypeError: not enough arguments for format string`으로 죽는다. `.replace()`나 f-string.
5. ★**`GROUP BY`와 `COUNT(DISTINCT)`는 NULL에서 갈린다** — 같은 질문을 두 방식으로 세면 모집단이 하나씩 어긋난다. 이번엔 그 1이 헤드라인 숫자였다.
6. **`naver_ad_daily`의 `__backfill__` 센티널이 실그룹 합과 거의 같다**(30일 17,129,773 vs 17,129,807) — 안 거르면 광고비가 **정확히 2배**.
7. **`naver_change_log`의 컬럼은 `changed_at`·`entity_id`**다(`created_at`·`adgroup_id` 아님).

---

## 9. 불변 확인

- **진행률 2/7 불변** · **코드 0줄 · prod 쓰기 0 · 네이버 광고 API 호출 0 · 마이그 0 · prod 배포 0**
- `naver_adgroup_scope` **9행 · `max(updated_at)=2026-09-05 12:14:50`** — 이번 세션 무접촉(완료 QA가 라이브로 재확인)
- 다음 가용 **D-NAO-296** · 교훈 **#401**(이번 세션은 295 소비, 교훈 미발급)

---

## 10. ★ 다음 세션 지시 (Jino 원문 — 각색 금지)

> **「n=94를 다음세션에서열고 /investigate로 C군 파보자」** (Jino, 2026-09-06 21:38 KST)

**다음 세션의 슬라이스는 이것으로 정해졌다** — §7의 후보 목록에서 고르는 것이 아니다.

- **대상**: `ref 140` §7의 **C군 15그룹** — 최신 소급채점(asof)에서 `shopping_group_bep`·`shopping_group_growth`
  **어느 보드에도 안 올라오는** 그룹. 30일 광고비 **709,448원**(49그룹 전체의 **21.3%**)이고
  **49그룹 지출 1위 `grp-a001-02-000000054530371` S25울트라 372,428원**이 여기 있다.
- **물음**: **왜 보드에 안 오는가** — 표본 부족인가 / 부모 체인 조건(`_on_adgroup_ids`)인가 / 산출 실패인가.
  `ref 140` §8-3이 「미규명」으로 남긴 항목이다.
- **좌표**: `backend/app/services/naver_ad/account_diagnosis.py`
  (`shopping_group_bep` `:185` · `shopping_group_growth` `:1057`) · `retro_snapshotter._BOARDS` `:21`
- **왜 이걸 먼저 하나** (2026-09-06 21:3x 대화에서 정해진 근거):
  **스코프 확대 결정(§7-1)과 독립적으로 진행할 수 있는 유일한 항목**이다. 그 결정이 열리기 전에
  ⓑ·ⓒ를 채우면 「결정 앞에 재료만 쌓는」 n=92 진단(*「일이 병목이 아닌 곳에 배치돼 있다」*)을 반복한다.
- **렌즈 선택의 근거**: 트랙 계약 구조는 n=92가 `/plan-eng-review`로 **3시간 전에** 봤고 8건 전건 처분했다.
  같은 렌즈를 반복하지 않는다. `/investigate`도 gstack 스킬이다 — **gstack을 버리는 게 아니라 렌즈를 바꾸는 것.**
  (⚠️ 내가 한 번 *「gstack이 아니라 `/investigate`」*라고 잘못 갈랐고 Jino가 그 자리에서 정정했다.)

⚠️ **선행조건 후보 1건**: 진짜 «외부» 시각이 필요하면 `~/.codex/config.toml`의 낡은 모델 핀
(`model = gpt-5.6-sol`)을 먼저 고쳐야 한다 — 안 고치면 「외부 목소리」가 다시 Claude 서브에이전트가 된다(n=92 §1 각주).
