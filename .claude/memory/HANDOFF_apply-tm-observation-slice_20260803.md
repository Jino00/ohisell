# HANDOFF — APPLY_TM 관측 슬라이스(S1) 구현 + Jino 결정 3건 (2026-08-03 밤 KST)

> 트랙: 네이버 SA 광고 최적화(우리판 MOP) · 기록: **D-NAO-137 · D-NAO-138**
> 직전 세션: `HANDOFF_ad-change-detection-cadence-probe_20260803.md`(D-NAO-136)
> 커밋: `7dfde2c`(문서) · `b1268ad`(S1 구현) — **둘 다 main, prod 배포 없음**

## §0 가장 먼저 알아야 할 것

**세션 첫 지시("08-04 07:45 크론 결과 확인")는 수행 불가였다** — 세션 시작이 08-03 20:02 KST라
그 크론은 11시간 43분 뒤다. 대신 크론과 무관하게 진행 가능한 것을 했다.

**S1은 구현했지만 배포하지 않았다.** Jino 결정("승인 · 크론 뒤 배포")대로다. 게다가 배포를
막는 선행조건이 하나 더 발견됐다(§5 마이그레이션 헤드 분기).

## §1 크론은 무장돼 있다 (내일 아침에 발견하지 않도록 미리 확인)

prod `scheduler_state`:

| 항목 | 값 |
|---|---|
| `job_name` / `cron_expression` | `shopping_ad_product_sync` / `45 7 * * *` |
| `is_enabled` | **1** |
| `last_run_at` / `last_status` | 2026-08-03 07:45:00 / **ok** |

오늘 정시 발화했으니 내일도 발화한다. 기준선(12:09 수동 sync)이 그 뒤라, 예상대로 08-03
오후 233건을 판정 대상으로 본다.

## §2 ★인계의 사실 정정 3건 — 다음 세션이 잘못된 전제로 시작하지 않게

### ①「대행사가 우리 소재 입찰을 3회 내렸다」 → **대행사가 자기 캠페인 소재를 만졌다**

D-NAO-136 §7의 표현이 오해를 부른다. 소재→캠페인을 짚으면:

| 소재 | 캠페인 | 우리 설정 |
|---|---|---|
| …455468669, …373441859 | `● [P_삭제금지]03. 아이폰_강화유리` (on) | D-NAO-92로 **우리 자동운영 정지**, 대행사 운영 중 |
| …558104404, …558107660 | `● 01. 갤럭시_지문방지_사생활` (on) | `naver_campaign_settings`에 **행 자체가 없음**(미편입) |

계정이 우리 것이니 넓게는 우리 소재지만, **운영 주체 기준으로 우리 자동운영 대상과 접점이
없었다**(우리 대상 04·15·P는 애초에 꺼져 있다). → 침해 대응이 아니라 관찰 사안.

### ②「04·15·P 노출 0」의 원인 = **캠페인 스위치가 `off`**

`naver_entity`(08-03 07:37 동기화, 신선) 실측 — `04. 아이폰_지문방지` **off** ·
`15. 갤럭시Z_폴드8플립8_사생활보호` **off** · `P. 아이패드 파워링크` **off**.
일별 실적도 07-30(정지 당일, 부분값)을 끝으로 **07-31부터 셋 다 `imp=0`**.

→ "자동운영을 껐더니 노출이 안 나온다"가 아니다. **재개는 `auto_operate`가 아니라
캠페인 on/off 축**이고, 둘은 별개 레버다.

### ③「`referenceData`는 `ids=` 배치의 추가 반환 필드」 → **현행 조회도 이미 준다**

D-NAO-136 §2 표가 그렇게 적었으나 코드 실측 결과 현행 그룹별 `GET /ncc/ads`도
`referenceData`를 돌려주고 **우리가 이미 파싱 중**이다(`naver_sa_ad_fetcher.py:719` —
`mallProductId`·`productTitle`). 즉 `APPLY_TM`은 **버려지고 있었을 뿐 안 오는 게 아니다.**

→ **관측 축적이 `ids=` 배치 전환에 종속되지 않는다.** 오늘 밤 S1을 착수할 수 있었던 이유다.

## §3 D-NAO-137 계약 (Jino 승인 1회 받고 착수)

**목표** — 「수정 사항」 화면이 오늘 하루 신호 4건을 잡음 229건에 묻어버린다. 피드 재적용을
분리해야 주기 상향이 비로소 읽을 만해진다.

**★착수 근거(가장 중요)**: 항상 **관측 축적을 판정 배선보다 먼저** 한다, 왜냐하면 판별자
표본이 n=1인데 **`APPLY_TM`은 API가 현재값만 주므로 소급 수집이 원리적으로 불가능**하다 —
**지금 적재를 시작하지 않으면 며칠 뒤에도 여전히 n=1**이다. 이 건에서 기다림은 표본을 늘리지
않는다(그래서 "며칠 더 보고 구현"이라는 인계 문구를 그대로 따르면 영원히 못 한다).

**슬라이스**: **S1 `APPLY_TM` 적재(관측만)** → *[3~5일]* → S2 판별자 배선 + 화면 분리 →
S3 `ids=` 배치 전환 + 주기 상향.
`ids=` 전환을 S3에 둔 이유: 현행 242콜/일은 지금 아프지 않고, 전환의 이득은 주기를 올릴 때만
실현된다(범위만 슬라이스).

**금지선**: S1에서 판정 로직·화면 **분류** 무변경 / **임계 120초를 코드에 굳히지 않는다** /
주기 변경은 S3까지 금지 / 자동운영 재개·캠페인 on-off는 스코프 밖.

## §4 S1 구현 (커밋 `b1268ad`, 4,469 passed)

- `get_ads`: `referenceData.APPLY_TM` 방어적 파싱. **키 이름은 정확히 `APPLY_TM`**(이웃 키는
  전부 camelCase인데 이것만 대문자+언더스코어) **값은 int**(이웃 값은 전부 문자열인데 이것만
  숫자형) — 둘 다 prod 스냅샷 실측이다. int·숫자문자열 수용, **범위 밖은 None**(초 단위
  10자리를 밀리초로 읽으면 1970년대가 나와 S2 분포를 오염시킨다).
- `naver_adgroup_product.ad_apply_tm` BigInteger nullable + 마이그레이션 `c4a7e2b91d63`.
- `shopping_ad_product_sync`가 적재. **`ad_external_change.run`에는 판정 입력으로 넘기지
  않는다** — 탐지기는 명명된 키만 읽고 키 순회를 하지 않으므로(`.get("edit_tm")` 등) 구조적으로
  무해하다.
- 테스트 15건. 그중 **계약 회귀 1건**: `apply_tm` 있이/없이 외부변경 판정이 동일함을 고정하고,
  **동시에 그 판정이 0건이 아님도 고정**한다(무변화 주장이 공허해지지 않게).

**의미의 한계는 그대로다**: `APPLY_TM`이 "피드 적용 시각"이라는 건 **실측 추론이지 문서
기재가 아니다**(공식 스웨거 `NccShoppingCollectionProduct`에 필드는 실재하나 설명문 없음).
그래서 S1은 **원문 보관만** 하고 의미에 기대는 판정을 하지 않는다.

## §5 ⚠️★배포 선행조건 — 마이그레이션 헤드가 갈라진다 (미해소)

prod `alembic_version` = **`rg9billed7c4e`**인데 **그 리비전 파일이 로컬 main에 없다.**
열린 **PR #190**(`claude/serene-agnesi-feee36`, RG 감사 S9)이 갖고 있고 prod엔 이미 배포됐다
— 이 프로젝트의 "배포 먼저·PR 나중" 관례에서 정상적으로 발생하는 상태다.

그 리비전의 부모가 내 것과 **같은 `b6e1c93f4275`** → 이대로 prod에서 `alembic upgrade head`를
돌리면 **헤드 2개로 갈라져 실패**한다.

**→ 배포 전에 PR #190 병합을 기다렸다가 재부모(re-parent)하고 `alembic heads`가 1개인지
확인할 것.** 전례: 커밋 `3130ee5` "마이그 재부모". (LESSONS #107)

배포 명령(재부모 후):
```
scripts/safe_deploy.sh backend/alembic/versions/c4a7e2b91d63_*.py backend/app/models.py \
  backend/app/services/naver_sa_ad_fetcher.py \
  backend/app/services/naver_ad/shopping_ad_product_sync.py --migrate --restart
```

**합격기준**: ①`ad_apply_tm` non-null ≥95%(1,759행) ②`editTm−APPLY_TM` 분포에서 ≤120초·>1h
군집 분리 ③배포 전후 `naver_agency_op` 행 수·`op_type` 분류 동일.

## §6 D-NAO-138 — Jino 결정 3건

### ①백필 행 = 「주석만」 → **코드 변경 0으로 처리했고 라이브 적재 완료**

기존 `naver_change_actor_override`가 "정정이 자동 판정과 **같은 값**이어도 `corrected=True`"
(= 사람이 확인함)를 **이미 의도적으로 지원**하고, 프론트도 `correction_note`를 이미 렌더한다
(`NaverAdModifications.tsx:222`). → 기존 PUT 엔드포인트로 `actor=agency`(무변경) + 주석 적재.
**원천 테이블 UPDATE 0줄.**

**★D-NAO-136 ⑦의 건수를 정정한다: 3군집 23건이 아니라 4군집 26건.**
적용 기준을 재현 가능하게 못 박았다 — **"같은 분에 2건 이상 뭉친 `ad_edit`"**:
07-30 18:21(13) · 07-31 19:21(5) · 08-01 12:27(5) · **08-01 18:21(3, 인계 누락분)**.

남은 **단건 7건**(id 105~109·130·131)은 그대로 뒀다. 그중 **`…558104404`·`…558107660`이
오늘 대행사가 입찰을 내린 바로 그 소재**라, "단건 = 실조작"이라는 읽기를 뒷받침한다.

주석 문구: `피드 재적용 의심 — 같은 분에 여러 소재가 함께 전진했고(피드 서명) 광고 설정은
무변동. 단 APPLY_TM은 현재값만 조회돼 소급 확증 불가(D-NAO-137).`

라이브 확인: `GET /api/naver/ad/modifications?date_from=2026-07-30&date_to=2026-08-01&source=agency_op`
→ 총 55행 중 **주석 26행**, 샘플 `{actor: agency, actor_auto: agency, corrected: true, correction_note: ...}`.

### ②04·15·P 재개 = 「계속 정지」 (원인 규명은 §2-②)

### ③대행사 입찰 인하 3건 = 무대응(관찰) — 성격은 §2-①

## §7 이번 세션이 하지 않은 것

- **prod 코드 배포 0**(S1은 커밋만). **네이버 API 쓰기 0.**
- **prod DB 쓰기는 주석 26행뿐**(override 테이블, 되돌리기 = PUT `actor:null`). 원천 테이블
  (`naver_agency_op`·`naver_change_log`) UPDATE 0줄.
- **codex 미실행** — S1은 배포 시점에 PR 경계 리뷰 1회 필요. **부채로 남긴다.**
- ⚠️**의도치 않은 부작용 1건**: 마이그레이션 검증을 임시 DB로 하려 했으나 이 레포의
  `alembic/env.py`는 `alembic.ini`의 고정 URL만 읽어 `DATABASE_URL`을 무시한다 → **로컬 dev
  DB(`backend/ohisell.db`)가 대신 올라갔다**(prod 아님, 무해). LESSONS #106 · failures.jsonl 기록.

## §8 다음 세션 시작 프롬프트

```
트랙: 네이버 SA 광고 최적화(우리판 MOP). 이어받는 상태:
- 자동운영 정지 유지(D-NAO-132). 04·15·P는 「계속 정지」 확정(D-NAO-138) —
  단 노출 0의 원인은 캠페인 스위치가 off인 것이지 자동운영 정지의 부작용이 아니다.
- D-NAO-137 S1(APPLY_TM 관측 적재, 판정 무변경) 구현 완료·커밋 b1268ad·**미배포**.

첫 할 일: 08-04 07:45 크론 판정 확인. 08-03 오후 233건을 어떻게 판정했는가 —
`bid_change` 3건 + `ad_edit` 약 230건 예상이고, 그 ad_edit은 대행사 조작이 아니라
네이버의 상품 피드 재적용이다(판별자 editTm−APPLY_TM ≤ 120초).

그다음: S1 배포. ⚠️선행조건 = 마이그레이션 재부모. prod alembic이 rg9billed7c4e
(PR #190, main 미병합)인데 내 c4a7e2b91d63의 부모가 같은 b6e1c93f4275라 헤드가 갈라진다.
PR #190 병합 후 재부모 → alembic heads 1개 확인 → safe_deploy --migrate --restart.
배포 후 codex PR 경계 리뷰 1회(부채).

그 뒤 3~5일 관측이 쌓이면 S2(판별자 임계를 데이터로 결정 + 화면 분리), 이어서
S3(`ids=` 배치 전환 + 주기 상향).

문서: 트랙 `docs/tracks/active/track_naver-ad-optimization.md`(D-NAO-137·138),
HANDOFF `.claude/memory/HANDOFF_apply-tm-observation-slice_20260803.md`.
```
