# 세션 인수인계: D-NAO-197 ① col7/8/9 적재 완료 (D-NAO-198·199)

> 저장일시: 2026-08-19 07:4x KST · 저장소 **main·clean** · ⚠️**origin 미푸시 6커밋**
> 트랙: **PAO(파오)** — `docs/tracks/active/track_naver-ad-optimization.md`
> 결정 **D-NAO-198 · 199** · 교훈 **#315 · #316** · 다음 번호 = **D-NAO-200 / 교훈 #317**
> prod alembic head = **`std1dim2axes3`** · PAO 정지 유지(`optimizer` 7/7 `none`)

---

# 0. ★★두 게이트 — 매번 지킨다 (D-NAO-200, Jino 2026-08-19 07:50)

> *"너가 무슨 새로운 일을 시작하기 전에 이 목표에 맞는지 판단하고 진행해. 그리고 마지막에 이 목표가 완성되었는지 QA를 통해서 판단하고 마누리 하자"*

**「이 목표」 = §1-0의 Jino 11:38 원문.**

## ① 착수 게이트 — 새 일을 시작하기 **전에**
무엇을 하든 **먼저** 목표 대조 판정을 **출력하고** 진행한다. 3값:

| 값 | 뜻 | 같이 적을 것 |
|---|---|---|
| **적합** | D-NAO-197 범위 5개 중 하나이거나 그 선행 조건 | **어느 항목인지 번호** |
| **선행조건** | 범위 항목은 아니나 없으면 못 한다(예: prod 디스크) | **왜 없으면 막히는지 한 줄** |
| **밖** | 목표 밖 | `↗️ 스코프 밖` 자백 후 진행하거나, 목표를 바꿀 일이면 **새 계약**(Jino 승인) |

★**「이왕 보인 김에」로 시작하지 않는다.** 이 트랙의 표류는 전부 국소적으로 정당한 연쇄에서 왔다.

## ② 종료 게이트 — 닫기 **전에**
**별도 서브에이전트**(Sonnet high·읽기 전용)에 완료 QA를 맡기고 판정을 **①앵커 ②HANDOFF ③트랙** 세 곳에 남긴다. 어휘 5값(달성/부분달성/미달/목적전환/판정불능).

★**QA에 선판정 2개를 먼저 시킨다**(2026-08-18에 이 형식으로 돌려 잘 작동했다):
- ⓐ **합격기준이 계약을 덮는가**
- ⓑ **계약이 지시(11:38 원문)를 덮는가** — **진도 N/5**와 **층2 분모 M/36**을 병기시킨다

★**「완료」 ≠ 「모든 데이터」를 매번 명시**하게 한다. 이 트랙은 「완료됐나」에 **세 번 잘못 답했고**, 세 번 다 잡은 것은 **Jino의 재점검**이지 모델의 절차가 아니었다.
★**미달도 유효한 종결**이다. 미달인 채 닫고 판정 원문 그대로 적는다 — 성공만 적힌 인계는 홍보다.

## 왜 결정으로 박았나
전역 하네스(§1 앵커·§2 완료 QA)에 이미 있는 규율인데 **이 작업에서 두 번 새어 나갔다**:
①「13:27 상품메타+광고3축만」을 범위 상한으로 오독(D-NAO-196) ②매트릭스 36축을 「받아올 수 있는 것」의 목록으로 오독해 **분모가 틀린 채 「완료」를 셌다**(D-NAO-197). 둘 다 **착수 시 목표 대조가 없었던 것**이 원인이다.

---

# ★ 이 파일을 읽는 순서
1. **§1 — 다음 세션이 할 일**(진도 1/5, 남은 4개). 여기만 읽어도 착수할 수 있다.
2. **§2 — 착수 전 5분**(함정·좌표·명령).
3. 나머지는 참조: §3 확정 결정 · §4 이번 세션이 한 것 · §5 완료 QA 2건 · §6 정정된 것 · §7 이월·결정대기

---

# 1. 다음 세션이 할 일

## 1-0. 목적 (원문 — 새로 발명하지 말 것)

> *"성과로 나눈 등급에 대해서 광고, 스마트스토어에서 API로 받아올 수 있는 모든 데이터를 나누자"*
> — Jino 2026-08-18 11:38

**이 문장을 그대로 앵커 `목표:`에 복사한다.**

## 1-0b. ★도착지도 확정됐다 (D-NAO-199, Jino 2026-08-18 23:1x)

> *"자료를 모두 받아서 fable이 성과 등급의 연관성을 찾아내고 스마트스토어 광고운영에 반영하자"*

**3단: ①수집(D-NAO-197 범위 5개) → ②Fable 연관성 분석 → ③광고운영 반영.**
목표 변경이 아니라 **도착지 추가**다. ②의 저자는 Fable로 규약돼 있다(Jino 08-18 12:30).
★**③은 별도 계약이다** — 이유는 §7-2.

## 1-1. 완료 정의 = D-NAO-197 (Jino 승인) — **진도 1/5**

| # | 항목 | 상태(2026-08-19 07:4x) | 다음 세션 비용 감각 |
|---|---|---|---|
| ① | **시간대·지역·매체**(col7/8/9) | ✅**완료**(D-NAO-198, QA 달성) | — |
| ② | **연령·성별·관심사**(CRITERION) | 테이블 0개, `/ncc/criterion` 코드 호출 **0건** | 코드+마이그+배포+365일 백필 |
| ③ | **매체 블랙리스트**(A5/A6) | 미착수 | ★**가장 쌈** — 추가 API 콜 0, §1-2 참조 |
| ④ | **상품 메타**(C10) | 미착수 | ⑤ 뒤에 |
| ⑤ | **커머스 미개봉 75건** 개봉 | 미착수(41/116=35.3% 그대로) | 문서 읽기 |

**진도 1/5.** 층2 분모(ref 73 §1-C)로는 교차완료 **15/36**(개정 4의 14에서 +1).
★**「몇 % 됐나」엔 반드시 어느 층의 분모인지 병기한다**(§0-A 3층 깔때기: 표면 ⊃ 축 ⊃ 교차완료).
★**「완료」 ≠ 「모든 데이터」** — 5개를 다 채워도 층1 대비 N1~N8·C2/C3/C7/C8/C9/C12가 범위 밖이다.
이건 **Jino가 승인한 명시적 축소**이지 은폐가 아니다(D-NAO-197이 스스로 열거).

★**범위 밖으로 확정된 것**(이번 완료의 조건이 **아니다**): N2 MasterReport 5종(`item=Qi`) · N3 AdExtension · N4 SharedBudget · N7 `/stats` breakdown 3종 · N8 미요청 필드 13종 · 쇼핑 수요 기준선 · C2·C4·C7·C8·C9·C11. **N5=불가(원리) · N6=판정불능**은 D-NAO-196에서 처분됨.

## 1-2. 착수 순서 — 비용이 정한다

### 1️⃣ ③ 매체 블랙리스트(A5/A6) — **가장 싸다**
- `naver_sa_writer.py`가 **`/ncc/targets` 응답을 이미 받아서 `targetTp` 필터로 즉석 폐기**한다. 응답에 `MEDIA_TARGET`·`PC_MOBILE_TARGET`이 들어 있다. **저장만 하면 열린다.**
- ★**이번 세션 실측이 인계 전제를 뒤집었다**: 「쇼핑제외 **쓰기** 때만 돈다」가 **틀렸다.**
  - `naver_sa_writer.py:237`(`get_shopping_exclusions`) → `get_live_exclusions`(:298) 경유로 **`exclusion_survival.py:214`(생존감시)** · **`search_term_execution.py:707`** 의 **읽기** 경로에서도 돈다.
  - `naver_sa_writer.py:310`(`_shopping_restrict_target`)만 쓰기 전용.
  - ⇒ 옛 HANDOFF가 「상시 커버리지를 원하면 별도 스윕이 필요한지 **먼저 판단하라**」고 남긴 질문의 답 = **별도 스윕 불필요.**
- 필요한 것: 신규 테이블 + 파싱 + 배선. **마이그레이션 있음.**

### 2️⃣ ② CRITERION 365일 백필
- 엔드포인트 **`/ncc/criterion/{ownerId}`**(adgroup grain). 코드 호출 **0건**(재확인함).
- **1년 소급 실증됨** — 연령·성별(#4 S3)과 관심사(#9 A1)가 여기서 열린다.
- ⚠️**꺼 놓은 행은 행 0**이라 365일 소급이 완전하지 않다.
- ★**벌크 경로 별존**: StatReport `CRITERION`/`CRITERION_CONVERSION` 리포트 2종 — 엔티티별 GET 스윕 없이 리포트 1건으로 전 그룹 취득 가능성(ref 75 ADS §4-4, **응답 스키마·성공 여부 미확인**). **엔티티 스윕 482콜을 짜기 전에 이 경로부터 재 볼 것.**
- 코드 + **마이그레이션** + 배포 + 백필.

### 3️⃣ ⑤ 커머스 75건 개봉 → 그 결과로 ④ C10
- ★**순서 지킬 것 — ⑤ 없이 ④ 하지 말 것.** C10 라벨 자체가 `[부분확인]`이다(커머스 116건 중 개별 문서를 연 것이 41건뿐, 공백이 상품 도메인 64 중 54에 몰림).
- 새로 연 N배송 4건에서 **라벨 어긋남이 4건** 나왔다 — 안 열면 「상품메타가 층화에 쓸 만한가」조차 못 믿는다.
- 1차 출처: `https://apicenter.commerce.naver.com/llms/llms.txt` + endpoint별 개별 `.md`.

## 1-3. 합격기준을 쓸 때
- **각 항목** ⓐprod 라이브 행(**행수·창 병기, 0행이면 미달**) ⓑ등급 교차 산출물 + 재현 SQL ⓒ매트릭스(**ref 73**) 반영.
- ⑤는 「몇 건 열었나 / 라벨 바뀐 것 몇 건」으로 잰다.
- ★**「전수 조사 완료」라고 쓰지 않는다** — 커버리지 자백 절 의무.
- ★완료 QA에 **선판정 2개**를 먼저 시킨다: 「합격기준이 계약을 덮는가」 + 「계약이 지시를 덮는가(진도 N/5·층2 M/36)」. 이번 세션이 그 형식으로 돌렸고 잘 작동했다.

---

# 2. 착수 전 5분

## 2-1. 환경·명령
- **로컬**: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 메인, **main**)
- **prod 조회**(읽기 전용):
  `ssh -o BatchMode=yes sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db" < 로컬.sql`
  ★**인라인 heredoc은 따옴표가 벗겨져 SQL이 깨진다 — 반드시 로컬 `.sql` 파일 + stdin.**
- **prod에서 배포 코드 실행**: venv `/home/ubuntu/ohisell/backend/.venv/bin/python` (시스템 `python3`엔 sqlalchemy 없음)
- ★**로컬에서 네이버 SA API를 부르려면 `.env`를 명시 로드**해야 한다 — 안 하면 **403**이 난다(자격증명 없음이 아니라 권한 없음처럼 보인다):
  ```python
  from dotenv import load_dotenv; load_dotenv(".env")
  import importlib; importlib.reload(F)   # 모듈 전역 상수를 다시 읽게
  print(bool(F.ACCESS_LICENSE))           # 확인
  ```
  (`failures.jsonl` 2026-08-18 기록)
- **배포**: `scripts/safe_deploy.sh <파일…> [--migrate] [--restart]`
- **번호**: `./scripts/next_ids.sh` — ★**결정하면 즉시 트랙에 쓴다.**
- **테스트**: `cd backend && python3 -m pytest tests/ -q -p no:randomly` (약 3분)
- ⚠️ GitHub Actions 결제 정지로 **CI 미가동**.

## 2-2. ★마이그레이션 순서 — 어기면 ingest가 통째로 침묵한다
②③은 스키마 변경이다. 이 앱은 **부팅 시 인프로세스 마이그레이션을 하지 않는다.**
→ `safe_deploy.sh <마이그파일> <코드파일…> --migrate --restart`. 이번 세션에서 정상 작동 확인
(매니페스트 관측: 14:11:53 마이그 → 14:11:57 alembic → 14:12:02 코드 → 14:12:12 재시작).
★단 **신규 테이블만 추가하는 마이그레이션은 순서 위험이 낮다**(어겨도 「no such table」만 나고 기존 테이블 SELECT는 안 죽는다) — 적대 리뷰가 이번에 그 구분을 확인했다. 그래도 금지선대로 순서는 지킨다.

## 2-3. ★함정 (이번 세션이 실제로 밟거나 확인한 것)
1. **`__backfill__` 센티널** — 공용 필터가 **없다**. 배제 관용구를 쓰는 파일은 **43개**(`app/`, 상수 `BACKFILL_SENTINEL_ADGROUP` 임포트 후 각자 인라인). ⚠️**리터럴 `"__backfill__"` 문자열로 세면 20개뿐이다** — 세는 패턴을 먼저 밝혀라. **새 집계 SQL마다 다시 적어라.** (이번 신규 두 표엔 센티널 행 0건 — 가정이 아니라 측정했다.)
2. **인계 숫자는 실측 전엔 믿지 마라** — 이번 세션에서도 **인계 8건 중 3건이 정정**됐다(§6).
3. **위임문의 「배경 사실」도 검증 대상**(교훈 #314) — 출처 좌표를 붙이고, 못 붙이면 「내 요약 — 검증 대상」으로 라벨을 바꿔라. 이번 세션은 전 위임문에 좌표를 붙였고 하위 기가 그 중 일부를 정정해 왔다.
4. ★**창 없는 distinct는 축의 정체가 아니다**(교훈 #316) — 아래 §6-3.
5. ★**grain 절삭은 로컬 결정이 아니다**(교훈 #315) — 아래 §6-4.
6. **「크론이 있다」 ≠ 「그 결정이 실행됐다」** — 대상·저장 방식이 승인 의도와 반대일 수 있다.

## 2-4. 착수 시 확인 명령 (복붙용)
```bash
git branch --show-current && git status --short && git rev-list --count origin/main..main
./scripts/next_ids.sh
ls -t docs/references/*audit* | head -1   # 주기 구조 감사 7일 경과 여부(전역 §4)
```

---

# 3. 확정된 결정사항 (번복 금지)

1. **D-NAO-198** = ① col7/8/9 적재 계약(Jino 승인, 3안 중 「①만」). 완료·QA 달성.
2. **D-NAO-199** = 도착지 3단(수집 → Fable 연관성 → 광고운영 반영). **목표 변경 아님.**
3. ★**두 표로 가른 설계**(번복하려면 근거가 필요하다): 결합 grain 전건은 180일 **3.43M행·586MB**이고 그중 **98.2%가 clk=0·cost=0**. prod 디스크 **92%**(포화로 배치 유실 전력)라 전건은 못 싣는다. → 축별 마진 전건 + 결합은 `clk>0 or cost>0`만.
4. ★**별도 크론이 아니라 기존 07:40 잡에 동승** — 두 표 커버리지가 갈라지면 원료가 180일 뒤 사라져 못 메운다.
5. ★**코드 사전을 만들지 않는다** — 지역·매체 코드의 «뜻»은 미상으로 두고 리포트가 준 코드값 그대로 저장(D-NAO-198 「안 함」). 지역엔 `-1`이 섞이는 날이 있는데 뜻 미상이다.
6. **매트릭스 정본은 ref 73**(`data/73_band_x_all_api/BAND_X_ALL_API_MATRIX_20260818.md`, 현재 **개정 5**). ref 68은 08-17 스냅샷 — 옛 HANDOFF·MEMORY가 ref 68을 가리키고 있었다.
7. **승계**: C3 반품 제외 · A2 요일·시간 제외 · 「시간대→CPC 상향」 불성립(끄는 판정만 성립) · **fan-out 배분 안 함** · 커머스 숫자엔 상한 프록시 라벨.

---

# 4. 이번 세션이 한 것

## 4-1. D-NAO-198 — ① col7/8/9 적재·배포·백필 (커밋 6건, 전부 미푸시)

| 커밋 | 내용 |
|---|---|
| `2ca8f917` | 계약 승인 + 착수 전 QA 「미달」 기록 + 실측 정정 3건 |
| `70dd2fd5` | 구현 — 모델 2 · 마이그 `std1dim2axes3` · fetcher · ingest · 07:40 동승 · 백필 스크립트 · 테스트 8종 |
| `0734ef7a` | 적대 리뷰 P2 채택 3건 |
| `df496a56` | 교차 산출물 + 재현 SQL 5본 |
| `4add4db0` | ref 76(Fable) + 매트릭스 개정 5 |
| `4fcc14b6` | 완료 QA 「달성」 + Fable이 잡은 설계 귀결 |

**신설**
- `naver_search_term_dim_daily` — 축별 마진(dim_type `h`/`r`/`m` × dim_value), **전건**
- `naver_search_term_dim_cell_daily` — 6키 결합, **`clk>0 or cost>0`인 칸만**
- `naver_sa_ad_fetcher.fetch_search_term_dimensions()` + `_num_or_flag()`(콤마 표기 0-낙하 **관측**)
- `app/services/naver_ad/search_term_dim_ingest.py`
- `backend/scripts/backfill_search_term_dims.py`(날짜별 커밋·`--skip-existing` 재개·180일 하한 클램프)
- `tests/test_naver_searchterm_dims.py` **10종**

**라이브 (2026-08-18 23:5x 관측)**
- 마진 **984,165행 · 171일(2026-02-19~2026-08-17) · 307그룹**
- 결합 **33,733칸 · 170일 · 300그룹**
- DB **1.84GB → 2.08GB(+244MB)**, 디스크 92% 유지
- 백필 **실패 9일 = 전부 2026-03**(03-03·04·05·10·11·26·27·28·29) — **D-NAO-184가 이미 판정한 「네이버에 리포트가 없다(수집 실패 아님)」와 정확히 일치**(그쪽 결측 10일 = 02-16 + 3월 9일, 02-16은 180일 창 밖). **독립 재현.**
- 파싱 경고 **0건**

**검산 3종**
1. `__backfill__` 센티널 — 신규 2표 + 대조군 `naver_search_term_daily` **전부 0행**(측정함)
2. ★**축 불변식** — h/r/m 세 축의 imp·clk·cost 총합 **완전 동일**(3,438,070 / 34,827 / **47,222,632원**). 각 축이 같은 원본 행을 분할하므로 반드시 같아야 한다. 밴드별로도 5밴드 전부 일치.
3. 밴드 조인 **307/307(100.0%)**, 전건 SHOPPING

**적대 리뷰 = PASS(P1 0)** · P2 4건 → 3채택 1기각
- 채택① **「추가 API 콜 0」 주장이 틀렸다** — `ensure_reports_built`(GET 상태)·`_list_reports_by_type`(GET 목록)도 자기 몫으로 다시 돈다. 07:40 잡에서 창 길이만큼 2배(POST만 dedup). 주석 정정.
- 채택②③ 테스트 전 케이스가 **단일 adgroup**이라 「집계 키에서 `adgroup_id` 제거」 변이가 살아남았다 → 복수 adgroup + 축 간 코드값 충돌(hour `05` vs region `05`) 2케이스 추가로 봉쇄.
- 기각④ `_num_or_flag`의 로그 상한 20 경계 — 값에 영향 없음.
- **변이 주입**: 만든 쪽 5종 + 리뷰어 5종, 살아남은 3종 봉쇄 후 **전건 검출**.
- 테스트: 신규 10 passed · 전체 **5,712 passed / 1 failed**(`test_vendor_item_axis`, 최소 08-11부터의 **기존 부채**, 다른 트랙)

## 4-2. ref 76 — 밴드 × 3축 연관성 분석 (Fable)
`docs/references/76_band_x_dim_axes_20260818.md` + `data/76_band_x_dim_axes/`(out 2 + raw 2 + SQL 5본)

★**핵심은 「가른다」가 아니라 「못 가른다」다**:
1. **시간대·지역은 밴드를 못 가른다** [산술·홀드아웃 재현] — band1↔band3 시간 블록 격차 전부 **1.5%p 이내**, 지역 02(24.8~24.9%)·09(19.4~21.0%) 사실상 동일. **잘 버는 그룹과 못 버는 그룹이 같은 시간·같은 지역에 돈을 쓴다.**
2. **매체 집중도만 생존** [상관·주의] — `8753`이 band1 비용 **57.0%** / band3 **52.1%**, top3 81.0%/77.5%. 홀드아웃 양쪽 방향 재현. **그러나 격차 ~5%p이고 band2(53.8%)가 단조성을 깬다.**
3. ★**밴드 총계의 CTR(1.253 vs 0.886%)·평균순위(4.59 vs 5.04) 격차는 홀드아웃에서 깨졌다** → **[미검증 가설]로 강등**. half1에서 CTR 1.018 vs 1.015 동률, 순위는 4.99 vs 4.60 **역전**. **ref 64 「순위 2.5~4 이익 스팟」과 같은 구조** — 홀드아웃 없었으면 거짓 발견을 또 실었다. (단 `8753` 매체 **내부** CTR 서열은 양쪽 재현 → [상관·주의] 존치)
4. **결합 칸 고유 발견 실재** — `8753`은 심야(00–06시 61.2% > 주간 53.2%), `27758`(평균순위 1.7의 최상위 지면)은 주간에 몰린다. 홀드아웃 4칸 전부 방향 재현. **마진표만으로는 안 보인다** → 결합표를 유료 칸이라도 남긴 설계가 값을 했다.

**판정 불가로 남긴 것**: 「과거로 갈수록 raw 행수 감소」(07-30 26,669행 → 05-15 7,859행) — 롤업이 날짜를 잃어 ①규모 성장 ②그룹 수 변화 ③보존 특성을 **원리적으로 못 가른다**. 시계열 절을 **아예 두지 않았고** 그 이유를 명시(+D-NAO-183 준-공선).

## 4-3. 매트릭스 개정 5 (Fable)
- **#3 승격**: 원료없음 → **교차완료(3중 한정)**. §1-C 교차완료 **14 → 15**(광고 13), 원료없음 6 → 5. §0-A 층3 12/24 → **13/24**. 분모 36 불변.
- ★**층1·층2는 안 변했다** — 새 endpoint를 연 게 아니라 **이미 받던 리포트의 버리던 컬럼을 살린 것**. → **F19 신설**(F1 「사망 지점은 호출이 아니라 적재」의 실행 단계 실증).
- **#24(hh24)와 역할 분담 명시**: #24 = adgroup·38일·**CPA 성립**(이익 질문) / #3 = 171일·지역·매체 동반·**전환 없음**(장기 분포 질문).
- ★**개정 4의 stale 정정 3곳** — #17(A9 검색량)을 「아직 미배포」라 적었는데 같은 날 16:1x D-NAO-196이 배포해 prod 1,166행 라이브였다. **자기 규약(「셀에 측정일·창을 박는다」)을 어긴 사례로 기록.**
- `/ncc/targets` 폐기분 호출 경로 정정 2곳 + F18 1곳.

---

# 5. 완료 QA — 이 세션은 **두 번** 돌렸다

## 5-1. 착수 전 (2026-08-18 22:42, 별도 Sonnet·읽기 전용) = **미달**
Jino 지시(*"handoff파일에서 제시된 목적에 대해서 마지막 QA를 통해 모두 달성되었는지 확인해"*)로 실행.
- **선판정: 「합격기준이 지시를 덮지 못함」** — 5개를 다 채워도 층1 대비 N1~N8·C2/C3/C7/C8/C9/C12가 명시적 범위 밖. **Jino가 승인한 명시적 축소이지 은폐가 아님.**
- 항목별 ①~⑤ **전건 미달**(prod 테이블 0개·저장 코드 0건·승인 이후 커밋은 문서 편집 4건뿐)
- **진도 0/5** — HANDOFF 자체 주장과 일치(**드물게 인계 숫자가 그대로 맞은 경우**)
- 층2 교차완료 14/36

## 5-2. 슬라이스 종료 (2026-08-18 23:55, 별도 Sonnet·읽기 전용) = **달성** (판정 원문 요지)
- **선판정 0-1**: 합격기준이 계약 목표를 덮는가 = **덮음**
- **선판정 0-2**: 계약이 지시를 덮는가 = **덮지 못함** — 진도 **1/5**, 층2 **15/36**. **「완료」≠「모든 데이터」이고 문서가 그 구분을 스스로 자백하고 있어 정합**
- ⓐ **달성** — 위 §4-1 라이브 수치를 판정기가 직접 재관측
- ⓑ **달성** — 판정기가 재현 SQL을 **독립 재구현**해 5밴드 원 단위 **정확 일치**(band1 71그룹/1,228,422/15,394/21,198,611 등), unmatched 0
- ⓒ **달성** — §1-C 합계 검산 36 통과
- **금지선 3종 준수**: `--migrate` 순서(매니페스트 시각 관측) · 센티널 3표 0행 · `optimizer='none'` 7/7·`auto_operate=0` 7/7
- **「안 함」 5종 위반 0건** · prod 배포 파일 sha256 = 로컬 HEAD 일치
- **종합: 달성 · 미달 0건**
- **이월 1건**: `_cell_daily` distinct 일수 **170** vs 마진 171 — 하루가 전량 노출-only였을 가능성. 수치 영향 없어 보이나 **원인 미확인**.

---

# 6. ★정정된 것 (인계·문서가 틀렸던 곳)

## 6-1. 착수 실측이 인계 8건 중 3건을 정정
| # | 인계 주장 | 실측 |
|---|---|---|
| 1 | `/ncc/targets` GET은 「쇼핑제외 **쓰기** 때만」 돈다 | **틀렸다** — `:237`은 `exclusion_survival.py:214`·`search_term_execution.py:707` **읽기** 경로에서도 돈다. `:310`만 쓰기 전용 ⇒ **상시 스윕 불필요** |
| 2 | `ST_COL_*` 상수가 `naver_sa_ad_fetcher.py:1104~1131` | 컬럼 지도 주석 **1104~1119**, 상수 **1120~1127**(1131은 뒤 EXPKEYWORD 주석) |
| 3 | `__backfill__` 배제 「약 40개 파일」 | **43개**(`app/`, 상수 임포트 기준). 리터럴 문자열로 세면 20개 — **세는 패턴을 밝혀야 한다** |

**유효 확인 5건**: col7/8/9 3축 테이블 0개(단 시간 축**만** 담는 테이블은 `naver_keyword_hourly` 등 **4개** 존재 — 「시간대조차 없다」로 읽으면 틀린다) · CRITERION 0개·grep 0줄 · `SHOPPINGKEYWORD_DETAIL` 매일 **07:40** 수집(`scheduler_service.py:1781`, `("sync_naver_search_term","40 7 * * *")`) · 180일 한도는 **day-180 BUILT 45,200행 ↔ day-181 400/10004**로 경계 실측 · prod alembic head 로컬 일치.

## 6-2. 문서 좌표 정정
**매트릭스 정본 = ref 73**(개정 5). HANDOFF·MEMORY.md가 **ref 68**(08-17 스냅샷)을 가리키고 있었다.

## 6-3. 축 코드 수는 창에 따라 다르다 (교훈 #316)
| 축 | D-NAO-182(1일 표본) | 3일 프로브 | **171일 실측** |
|---|---|---|---|
| 시간대 | 24 | 24 | **24** ✓ |
| 지역 | 19 | 18~19 | **21** |
| 매체 | 22 | 22~23 | **37** |
**주기가 닫힌 축(24시간)만 짧은 창에서 안정**하고, 열린 축은 창이 길수록 자란다. 매체를 22종으로 알고 만든 사전·분기는 37종 앞에서 조용히 15종을 흘린다.

## 6-4. ★내 설계가 원격 전제를 깼다 (교훈 #315)
용량 때문에 **검색어 텍스트를 버리고 adgroup grain으로 뭉갰는데**, 매트릭스 **개정 3**이 축 #3을 *「브랜드어 × 시간대」류 질문의 **유일한** 원료 후보(ref 74 C1)*로 지목해 뒀다. **이 적재로는 그 질문이 성립하지 않는다** — 원표 재호출만 남고 그건 **180일 한도**에 걸린다.
**Fable이 개정 5를 쓰다 `models.py:3176` docstring 실측으로 잡았다.** 절삭 검토 때 「무엇을 버리나」는 물었고 **「내가 버리는 것에 누가 기대고 있나」는 안 물었다.**
→ 브랜드어 × 시간대가 필요하면 **결합표에 검색어 축을 얹는 별도 계약**이 필요하고 **시한은 180일**이다.

## 6-5. D-NAO-182의 「45,200행 실물」이 재측정과 안 맞는다
같은 2026-08-17 리포트를 다시 받으니 **27,864행**(16열 가드 탈락 0). 무엇을 센 수인지 **확인 안 됨** — 이번 설계는 재측정값 위에 세웠다.

---

# 7. 이월 · 결정 대기

## 7-1. 이월 (다음 세션이 밟을 수 있는 것)
- ★**prod 디스크 92%**(8.4G 여유, DB 2.08GB) — MEMORY 마지막 기록은 83%. **회수 후보는 전부 다른 트랙**: `.pm2/logs` **730MB**(무로테이션, 옛 부채 627MB에서 증가) · `chroma_backup_*` 659MB · `floor1_fix_backup` 264MB · `/var/log` 1.8GB. ai-office 13G · stock 11G · ohisell 11G · meta-ads-v2 3.4G. **이 저장소는 디스크 포화로 배치를 유실한 전력이 있다(08-04).** ②③이 또 테이블을 늘리므로 **다음 계약 전에 판단이 필요하다.**
- `_cell_daily` distinct 일수 **170** vs 마진 171 — 원인 미확인(QA 이월).
- ★**「브랜드어 × 시간대」 원료 부재** — §6-4. 필요하면 별도 계약, 시한 180일.
- **`_safe_int`의 콤마 표기 0-낙하** — 고치지 않고 **관측만** 한다(같은 리포트를 읽는 `naver_search_term_daily`와 값이 갈라지는 게 더 나쁘다). 이번 백필에서 경고 **0건**. `_num_or_flag`가 로그로 표면화.
- **07:40 잡의 GET 2배** — 다운로드 결과를 공유하면 줄일 수 있다(적대 리뷰 P2-1, 기능 무해).
- **직전 세션에서 넘어온 것**(그대로 유효): 적대 리뷰 P2 5건(검색량 기준선 — ★`_parse_qc`의 `'1,234'→0` 상속 결함이 **소급 불가 시계열에선 영구 오측**) · **`/estimate` 적재 미구현**(D-NAO-186 승인분) · **`channelProductId ≡ mall_product_id` [미상]** · 2026-07-14~17 4일 **비용 2,216,906원·전환 0건·계정 전체**(원인 미상 — 38일판 CPA가 전부 조건부) · `expkeyword` 391일 전환 4종 0의 원인 · **WEB_SITE 이중 grain 정본**(`models.py:2468-2469` docstring과 데이터 어긋남) · `test_vendor_item_axis` 기존 실패 1건
- **ref 76이 [미상]으로 남긴 것**: SHOPPING 366그룹 중 **59그룹 원료 부재 원인**(측정된 특성만 기록 — 활동일 중앙값 6일 vs 307그룹 193일, 비용 4.33%) · 지역·매체 코드의 뜻 전부 · 지역 `-1`(imp 49·cost 0) · band2의 위치
- **매트릭스 「호출가능·미적재」 10축** — 이번 슬라이스에서 **변동 0**(#3은 원료없음에서 승격한 것이지 이 처분에서 온 게 아니다)

## 7-2. ★③「광고운영 반영」이 왜 별도 계약인가 (D-NAO-199)
1. **PAO 완전 정지** — `optimizer='none'` 7/7이라 집행 경로가 harness 단에서 막혀 있다. **해제 범위(전 캠페인 vs 카나리 2~3개)는 Jino 결정 대기.**
2. **대행사와 제외 칸 소유권 분리**가 선행 조건(도달 15그룹의 칸은 전건 대행사 것, 우리 몫 3건).
3. **「광고 전략 추천 금지 — 전략은 Jino 몫」**이 트랙 규약.
4. ★**②가 ③을 자동으로 정당화하지 않는다** — ref 64(홀드아웃 8/8 미재현)·D-NAO-183(출시×계절 준-공선). **연관성 발견 → 집행 사이에 홀드아웃 재현이 들어가야 한다.** 이번 ref 76이 그 규율로 발견 1건을 실제로 강등했다.
5. ★**그리고 ①은 아직 총이익에 못 닿는다** — `SHOPPINGKEYWORD_DETAIL`에 전환·매출이 없어 **ROAS·CPA·총이익 불성립**, CPC·CTR·노출·평균순위까지다. 전환이 있는 원장(`naver_keyword_hourly` 등)과 붙여야 하는데 **그레인 호환은 [미확인]**(ref 76 §7).

## 7-3. Jino 결정 대기
| # | 항목 | 배경 |
|---|---|---|
| 1 | **A3 회색 토큰 4건** | 「버디」 398,874원(갤럭시 버디4/5 vs 골프 birdie) · 일미리케이스 17,779 · A73 2,847 · 소다케이스 2,372. 총 영향 **0.54%**. 데이터로 못 가른다. **안 넣는 게 기본값**이라 방치해도 무해 |
| 2 | `optimizer` 해제 범위 · 대행사 제외 칸 소유권 · ref 65 §10 나머지 | 기존 미결 — **집행에 걸려 별도 계약감** |
| 3 | **prod 디스크 92%** | 회수 후보가 전부 다른 트랙 자산 — 손대도 되는지 |
| 4 | **N3 AdExtension** | 매트릭스 「API 생성 불가」와 공식 문서 CRUD 6+리포트 2종이 **상충**, 실계정 GET은 작동(190콜 200 OK). 「생성 불가」 진위는 **[미상]** |

---

# 8. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_dim-axes-ingested_20260818.md 읽고 이어서 작업해줘.
§1(다음 세션이 할 일)과 §2(착수 전 5분)를 먼저 볼 것.

★★§0의 두 게이트를 먼저 읽을 것(D-NAO-200): ①새 일 착수 «전»에 목표 적합성 3값 판정을 출력하고 진행
  ②닫기 «전»에 별도 Sonnet 완료 QA — 선판정 2개(진도 N/5·층2 M/36 병기) 먼저.
★목표는 §1-0의 Jino 11:38 원문을 그대로 앵커 `목표:`에 복사 — 새로 발명 금지.
★도착지는 §1-0b(D-NAO-199): 수집 → Fable 연관성 → 광고운영 반영. ③은 별도 계약(§7-2).
★완료 정의는 D-NAO-197로 확정, 현재 진도 **1/5**(①만 완료).
  「몇 % 됐나」엔 반드시 어느 층의 분모인지 병기할 것(층2 = 15/36).
  ★「완료」 ≠ 「모든 데이터」 — 5개를 다 해도 층1 대비 범위 밖이 남는다.

착수 순서는 §1-2대로 — 비용이 정한다:
  1) ③ 매체 블랙리스트(A5/A6) ★가장 쌈. 추가 API 콜 0이고 **상시 스윕도 불필요**
     (인계 전제였던 「쓰기 때만 돈다」가 이번 세션 실측으로 뒤집혔다 — §1-2 좌표 참조)
  2) ② CRITERION 365일 백필 ★엔티티 스윕 482콜 짜기 전에 StatReport 벌크 경로부터 재 볼 것
  3) ⑤ 커머스 75건 개봉 → 그 결과로 ④ C10. ★순서 지킬 것

★②③은 마이그레이션 포함 → safe_deploy.sh --migrate 순서 강제(§2-2).
★새 집계 SQL마다 `__backfill__` 배제를 다시 적을 것(공용 필터 없음, 43개 파일이 각자 인라인).
★새 테이블 grain을 정할 때 ref 73에서 그 축을 참조하는 셀을 먼저 grep할 것(교훈 #315 — 이번에 내가 밟았다).
★숫자를 적을 땐 창을 병기할 것(교훈 #316 — 매체 distinct가 22가 아니라 37이었다).
★인계 숫자는 실측 전엔 믿지 말 것 — 이 세션도 3건이 정정됐다.
★prod 디스크 92%다(§7-1). ②③이 또 테이블을 늘린다 — 착수 전에 확인할 것.
```
