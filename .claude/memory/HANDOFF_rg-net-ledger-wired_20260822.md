# 세션 인수인계 — RG 매출을 «콘솔 net 축»으로 배선 (D-CPP-47)

> 저장: 2026-08-22 15:0x KST · 세션 `a1ae61e2` · 사슬 「쿠팡-손익정합」 **n=2**
> 트랙: **`docs/tracks/active/track_coupang-rg-net-ledger.md`(신설)** · 진행률 **4/8**
> PR **#322 병합 완료** · **prod 배포 완료**(`bd5a660d`) · **백필 완료**(두 계정 축 완전 정렬)
> ⚠️ **완료 QA 재판정 3대조 = 부분달성(7/8) / 부분달성 / 미달** — 원문은 §5.
> ★**남은 미달은 ⓑ(종합조망) 하나뿐이고, 그걸 막던 유일한 이유(옵션축 커버리지)가 해소됐다.**

---

## 1. 발단

n=1(586fba5c)이 승인받아 둔 계약(§6)을 착수했다. 계약 목표 원문:
> 트랙 착수 관측 *"대시보드가 회사 매출의 34.5%만 보여준다 — RG1 2,170만원이 매출 0인데 비용 1,736만원만 들어가 오픽스가 −1,734만원"* 중 **RG 몫을 닫는다.**

세션 중 Jino 개입 4회가 설계를 크게 바꿨다(§3).

## 2. 한 것 — 커밋 5건 (전부 push 완료)

| 커밋 | 내용 |
|---|---|
| `7851cd8f` | RG 배선 본체 — 새 모듈 2개 + 대시보드 병합 + D-CPP-43 정렬 |
| `41d5ff42` | 트랙 신설 + prod 실측 기록 |
| `8d4849d1` | 적대 리뷰 1R P1 2건 수정 |
| `8e948be1` | 2R NEW P1 수정 |
| `a934572f` | 완료 QA 판정 기록 |

**새 파일**: `backend/app/services/coupang/rg_net_revenue.py`(데이터 접근층) · `rg_channel_pnl.py`(행 조립층) · `backend/tests/test_rg_net_channel_pnl.py`(22종)
**수정**: `profit_calculator.py` · `dashboard.py` · `schemas.py`

### 고친 라이브 결함 셋 (전부 대시보드 전용)
1. **RG 행이 아예 없었다** — RG 주문은 `orders`에 원리적으로 안 들어오는데 요약 엔진이 `orders` 행이 있어야만 버킷을 만들었다 → 오픽스가 창 길이에 따라 나타났다 사라졌다
2. **RG 정산 수수료가 3P 행에서 빠지고 있었다** — `get_rg_total_by_account` 키는 `account_key`(`COUPANG_WING1/2`)인데 `_channel_rows`가 `ch.code`로 매칭. RG 채널 code는 `COUPANG_RG1/2`라 안 맞는다
3. **폐기된 D-16 규칙으로 돌고 있었다** — 합산에 `ad_sales`가 섞여 있었다. 종합조망·상품손익은 이미 D-CPP-43으로 옮겨 갔는데 대시보드만 안 옮겨져 **같은 계정 RG 수수료를 두 화면이 다른 금액으로 뺐다**

### 신설: 쿠팡 광고 경로
`calculate_channel_summary`엔 쿠팡 광고 경로가 **아예 없었다**(cafe24·네이버만). 증거 = 화면 전체 광고비가 네이버+로켓1P와 **원 단위 일치**(쿠팡 몫 0).
가르는 축은 **옵션ID의 정체**(`sell_type` 라벨 금지 — D-CPP-43). 우주=`coupang_product_item` · RG표시=`rg_inventory` ∪ RG주문 ∪ 옵션축RFM · 나머지=3P.

## 3. ★Jino 개입 4회가 바꾼 것 (이게 이 세션의 핵심)

1. **"2P의 수수료 체계는 우리가 정확히 조사해놨던 내용이 있어"**(12:31) → ref 17(9종 체계)·**D-CPP-43** 발견. 내가 광고비를 발명하려던 것을 막았다. D-CPP-43은 **Jino가 2026-08-12에 같은 질문을 해서** 나온 결정이었다.
2. **"SellC의 다른 계정과 똑같은 방법으로 처리하면 되지 않아?"**(12:46) → 답이 이미 D-CPP-43에 있었다. 내가 물을 게 아니라 **찾았어야** 했다.
3. **"옵션ID는 2P와 3P에서 달라. 그래서 구분이 가능해"**(13:28, 윙 화면 캡처) → 내가 풀려던 「같은 옵션이 양쪽으로 팔려 광고비를 못 가른다」가 **존재하지 않는 문제**였다. 매출비율 안분(추정) 설계를 폐기했다. prod 실측 확인: RG 판매 옵션 132 · 3P 88 · **교집합 0**.
4. **"모든 상품은 상품관리>상품 조회/수정에 들어가면 다 있어. 너가 모를 수가 없어"**(13:29) → 「모름」을 설계 버킷으로 두던 것을 **동기화 경보**로 바꿨다.

★공통점: 네 번 다 **내가 없는 것을 만들려 할 때** 이미 있는 것을 가리켰다.

## 4. prod 읽기 전용 실측 (2026-08-22 13:46~13:52 KST)
`ssh sellc.ohitech.co.kr` · `sqlite3 "file:/home/ubuntu/ohisell/backend/ohisell.db?mode=ro"`

- **ⓐ 3,152,860원/174개/16일** — 요약축 == 옵션축(등가 재확인) · gross 3,651,030원(방향 일치)
- **ⓕ 720,069원 / 99.4348%** — `_cost_master` 규율로 **원 단위 일치**(계약이 「한 자리 차이 가능」이라 경고한 항목)
- **ⓒ 45,000원/3개**(gross 68,900원)
- **ⓓ** 08-20 오픽스 광고비 **25,436원** · 그날 3P `orders` **0건**
- **D-CPP-43 효과 실증**(07-13~08-09): 오픽스 4,711,332 → **2,227,811**, 오하이테크 **33,314 불변**(정산 광고 0인 계정) = D-CPP-43의 「같은 축」 서명 재현
- ★**PA 실제 광고비 > 정산 공제**: RG 몫 **4,536,774원** vs 공제 2,483,521원. 차액 약 **205만원**은 윙이 예고한 이월분 → **정산 공제만 보면 실제 광고비의 절반쯤만 보인다.** 3P 몫 18,072원 = RG **98.3%**
- **미배분 광고비 ≠ 0**: 26옵션 **57,787원**(4.2%). 상위 2개가 Z폴드8 필름 → 신상품이 카탈로그 동기화보다 광고에 먼저 뜬다. 「0이 정상」이라 쓴 내 초판 주석은 실측이 반증했다

## 5. 완료 QA (별도 Sonnet·읽기 전용 · 판정 원문 그대로)

> **판정(계약 합격기준 ⓐ~ⓗ): 부분달성** — 달성 4건(ⓐⓒⓕⓗ) / 미달 3건(ⓑⓓⓖ) / 판정불능 1건(ⓔ, 라이브 증거 없음) (2026-08-22 14:37 KST)
>
> **판정(Jino 2026-08-21 지시 원문): 부분달성** — ②는 달성, ①은 대시보드만 달성(종합조망 미착수라 절반), ③은 코드 구현 완료·라이브 미검증, ④는 착수 자체가 안 됨(미달) (2026-08-22 14:37 KST)
>
> **판정(트랙 궁극 목표): 미달** — 종합조망(intelligence.py) 미착수 + prod 미배포로 어느 화면에도 아직 편입되지 않았다 (2026-08-22 14:37 KST)

**QA가 독립 재현**: ⓕ를 자체 스크립트로(cost=720069.0/coverage=0.994348/net_orders=172) · ⓗ를 `origin/main` **별도 워크트리**로 대조(내 stash 방식보다 엄밀)
**QA가 확인 못한 것**: ⓓ 수치 독립 재현 안 함 · ⓔ는 유닛테스트로만(HTTP 관통 미확인) · PR #322 CI는 `steps:[]`·2~3초로 **결제정지 서명**이라 코드 신호 아님(결제 상태 직접 재확인은 안 함)

## 5-A. ★완료 QA — **재판정** (2026-08-22 15:33 KST · 배포·백필 뒤 · 판정 원문)

> **판정(계약 합격기준 ⓐ~ⓗ): 부분달성** — 달성 7건(ⓐⓒⓓⓔⓕⓖⓗ) / 미달 1건(ⓑ)
>
> **판정(Jino 2026-08-21 지시 원문): 부분달성** — ②③④는 달성, ①은 「대시보드와 종합조망 둘 다」 중 대시보드만
>
> **판정(트랙 궁극 목표): 미달** — 배포·백필 이후에도 `intelligence.py`는 트랙 시작 시점과 **diff 0**. **배포 여부와 무관하게 착수되지 않은 항목이라 배포로 뒤집힐 수 없었다**

1차 대비: ⓓ 미달→**달성** · ⓔ 판정불능→**달성** · ⓖ 미달→**달성** · **ⓑ 미달 유지** · ⓐ·ⓕ 달성 유지.
★배포·백필이 세 항목을 살렸지만 **트랙 궁극 목표는 안 움직였다.**

## 5-B. 배포·백필 (Jino 지시 15:06 *"배포와 백필 해줘"*)

- **PR #322 `--force` 병합** — CI 3잡 FAILURE지만 **steps 0개·2초·로그 부재 = 결제정지 서명**(직접 확인). 실제 코드 신호는 로컬 6016 passed. 자백은 `safe_merge.log`
- **배포** `bd5a660d` — 백엔드 5파일·마이그레이션 없음·무중단 블루그린(다운타임 0초)
- ★**표면 다섯 전부 라이브 확인**(§6-A 참조) — 이 세션에서 한 번도 못 했던 것
- **백필**: WING1 `vi_days`76 → **06-07~08-21(76일)** · WING2 71 → **06-12~08-21(71일)**. **두 계정 다 옵션축 == 요약축 완전 정렬**(WING1 +59일 · WING2 +46일). 두 상수 원복(7/7)
  - ⚠️첫 76일 시도는 **45초 `min_fetch_interval_s` 쿨다운**에 걸려 **조용히 no-op**였다(30일 실행 직후 재실행). prod을 다시 재고서야 알았다 — 로그를 안 봤으면 「했다」고 오인할 뻔했다

### ★라이브가 내 실측을 두 번 정정했다
1. **`ad_unallocated`는 0원이다.** 내가 13:5x에 잰 「26옵션 57,787원(4.2%)」은 **카탈로그만으로 3P를 판정한 임시 쿼리**의 산물이었다. 배포된 코드는 `카탈로그 ∪ orders ∪ 옵션축 NORMAL`을 써서 그 돈을 **3P로 정확히 귀속**했다(Z폴드8 `95854992864`: 카탈로그 0행인데 orders 8건·옵션축 NORMAL 11행). **배포된 코드가 내 임시 쿼리보다 정확했다.**
2. **「카탈로그가 우주」는 부정확하다** — WING1 카탈로그 413행인데 실제 3P 판매 옵션 88개 중 **67개(76%)가 카탈로그에 없다**(동기화 08-20 20:35이라 시간 문제가 아니다). **판매 원장이 실질 우주다.**

### ★★백필이 RG 정산 데이터도 끌어왔다 (예상 못 한 부수 효과)
`cmd_run`이 정산도 수집한다 — 오늘 **236행** 신규. 16일창 차감 수수료가 **18,437원 → 1,195,906원**, 오픽스 RG 순이익이 **973,510 → −96,916**으로 뒤집혔다.
**숫자가 틀린 게 아니라 정산 데이터가 그동안 비어 있었던 것이다** — 3.15M 매출에 수수료 18,437원은 애초에 말이 안 되는 값이었다(RG 수수료는 매출의 ~19.5%, ref 04). ⓐ·ⓕ는 백필 후에도 **전부 불변**.

## 6. 적대 리뷰 3라운드 (1R FAIL → 2R NEW P1 → 3R RESOLVED · 변이 누적 10종 생존 0)

- **1R P1 2건** — 둘 다 **통합 이음매**. 변이 6종 전부 KILLED였는데도 못 잡았다(테스트가 반환 dict까지만 봤다). ①`order_count`에 판매수량 ②자백 칸이 `_LEAF_PASSTHROUGH`·스키마 **두 경계**에서 드롭(교훈 #321 재발 — `schemas.py`엔 이미 그 경고가 있었다)
- **2R NEW P1** — **1R 수정이 만든 것.** `order_count`를 옵션축으로 옮기며 완전성 게이트를 그 칸에만 안 씌웠다. ★WING2는 옵션축이 07-27부터라 **기본 30일 창에서 상시 재현**되던 조건. 기존 테스트가 **바로 그 상황을 세팅해 두고도** 그 칸을 안 봐서 못 잡았다
- **3R RESOLVED**(P1=0)

## 6-A. ★표면 — 이 작업이 «어디에 닿아야» 하는가 (그리고 아직 안 닿았다)

**표면 = sellC 대시보드 `/channel-breakdown` 「회사 > 채널」 요약표.** 배포 후 화면에서 확인할 다섯:
1. 「개인회사 오픽스」 **회사 행이 창 길이와 무관하게 뜬다**(종전엔 나타났다 사라졌다)
2. leaf가 「쿠팡 3P(판매자배송)」/「쿠팡 로켓그로스」/「쿠팡 로켓배송(1P)」 **세 줄로 갈린다**
3. RG 행 매출이 **3,152,860원(net)**이고 3,651,030원(gross)이 아니다
4. 상단 KPI 「광고비」에 **오픽스 몫이 포함**된다(종전엔 통째로 샜다)
5. RG 행이 `option_axis_days`·`ad_unallocated`로 **자기 신뢰도를 실토**한다

⚠️ **이 세션에는 그 표면에 아무것도 안 닿았다** — prod 미배포. 위 다섯은 전부 「코드가 그렇게 만들 것」이지 「화면에서 봤다」가 아니다. 완료 QA의 세 번째 판정(트랙 궁극 목표 **미달**)이 정확히 이걸 잡았고, ⓓ 미달·ⓔ 판정불능도 같은 이유다.

★**다음 세션은 배포 후 이 다섯 줄을 화면에서 직접 확인하는 것으로 시작하면 된다.** 그리고 **스크린샷을 파일로 남길 것** — D-NAO-204에서 「라이브 화면」이 합격기준이었는데 증거를 안 남겨 독립 검증 불능이 된 전례가 있다.

## 7. 다음 세션이 할 일 — **ⓑ 하나뿐이고, 막던 것이 사라졌다**

### 남은 미달: ⓑ 종합조망 RG 매출 == 대시보드 RG 매출
- **무엇을**: `intelligence._agg_rg_orders`(167줄)가 gross 원장(`CoupangRgOrderItem`) 대신 **`rg_net_revenue.net_revenue_by_option`**을 읽게 한다. 반환 모양이 **이미 같게** 만들어져 있다(`{vid: {revenue, qty, order_count, name}}`) — 그러라고 그렇게 설계했다
- **★막던 것이 해소됐다**: 종전엔 옵션축이 WING1 08-05~밖에 없어 그 이전 창의 RG 매출이 0이 됐다(개악). 이제 **두 계정 다 옵션축 == 요약축 완전 정렬**(76일·71일)이라 그 위험이 없다
- **주의**: 소비처가 **셋**이다 — `compute_command_center` · `product_pnl.py:294` · `revenue_canonical.py:139`. 셋 다 옵션 grain을 요구하므로 모양은 맞지만, **각각이 gross를 전제한 계산을 하는지** 봐야 한다(특히 `_merge_rg_orders`의 unit_price 보존 주석)
- **사이클**: 두 번째 손익 엔진이라 **적대 리뷰 + 배포**가 한 번 더 필요하다. 이번 세션 실적은 3라운드(1R FAIL → 2R NEW P1 → 3R RESOLVED)였다
- 끝나면 트랙 **8/8** → §6 트랙 종결 QA 대상

### 착수 시 참고
- 트랙은 **main에 병합됐다** — 훅의 「트랙 파일을 읽을 수 없다」 경고는 이제 안 뜬다
- 백필은 **일회성**이었다. `vi_days`는 기본 7로 원복돼 있어 **롤링 창 밖은 다시 안 받는다**. 다시 넓히려면 같은 절차(config에 `vi_days` 넣고 실행 후 원복)이고, ★**연속 실행 시 45초 쿨다운**을 기다려야 한다
## 8. 이월 (스코프 밖 발견)

- **`group_summary_by_company`의 `order_count` 합산에 로켓1P 예외가 없다**(`_kpi_totals`엔 있다) → 로켓1P 판매수량이 **지금도** 회사 소계의 「주문 건수」를 오염시킨다. 이번 변경과 무관한 기존 결함 → 소관: `track_coupang-rocket-1p.md`
- **`order_count` 과소**(의도적 자백) — 옵션축이 창을 못 덮으면 0. 완전한 해법 `Optional[int]`은 스키마·두 집계층·프론트를 함께 건드려야 함 → 트랙 「안함」 등재
- **일자 추이 차트에 RG 미편입** — 요약표엔 뜨는데 추이엔 안 뜬다 → 트랙 「안함」 등재(다음에 「회귀 아닌가」로 재조사되지 않게)
- **옵션축 upsert 키에 `registration_type` 누락**(`vendor_item_sales_sync.py:108`·`:48-53`) — 같은 옵션이 하루에 양쪽이면 덮어쓴다. 다만 옵션ID 배타성(§3-3)이 실측 확인돼 **실제로는 못 일어난다** → 위험도 하향
- (인계분) 3P 독립 검산 축 소비처 0 · 갭 34건 건별 신원 · S6-auto 헛돎 · `CATEGORY_TR` 미개봉 시트

## 9. 좌표

- 트랙: `docs/tracks/active/track_coupang-rg-net-ledger.md`(**브랜치에만**) · 진행률 **4/8**
- PR: https://github.com/Jino00/ohisell/pull/322
- 앵커: `.claude/anchors/a1ae61e2-aa1e-4a2e-94ac-5949e8f14661.md`(판정 3줄 + 실측 + 이월)
- 사슬 등록부: `.claude/memory/chains/쿠팡-손익정합.jsonl` — 이 세션이 **n=2**. 다음은 `/session-relay 쿠팡-손익정합`으로 이어받고 n=3로 등록
- 새 D-N: **D-CPP-47** · 새 교훈 번호 미부여(다음 세션이 `scripts/next_ids.sh`로)
