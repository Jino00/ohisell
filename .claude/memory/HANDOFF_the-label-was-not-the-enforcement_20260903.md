# 세션 인수인계: PAO 기능 전수 목록 + 기능 #1 (b) 완주 / (a) 불가

> 저장일시: 2026-09-03 21:2x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것
> 체인: `pao-논의` n=83 (세션 e91888d5) · 목표이름: **목적관철 목표**
> **검증 명령**: `grep -c "due_untouched" backend/app/services/naver_ad/exclusion_grade.py` (기대 **5**) · `grep -c "★1-1-b" docs/references/65_paper_application_design_20260817.md` (기대 **1**)
> → 첫 값이 0이면 적대 리뷰 P1-1 수정이 되돌려진 것이다(도래한 재개방을 수리가 취소하던 결함).

> 제목의 뜻: **라벨은 집행이 아니었다.** 오늘 여섯 번 그 모양이 나왔고, 여섯 번째는 내 코드였다.

---

## 1. 프로젝트 위치 및 환경
- 로컬 경로: `~/.claude-worktrees/ohisell/pao-n83` (브랜치 `feat/pao-n83`, 머지 완료)
- 워크트리 venv 신설: `backend/.venv` (py3.14 · `requirements.txt` + `pytest` + `httpx`).
  실행: `cd backend && PYTHONPATH=. .venv/bin/pytest tests/ -q` — **`PYTHONPATH=.` 없으면 `No module named 'app'`**
- prod 조회(읽기 전용): `ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python - " < q.py`
  (첫 줄 `load_dotenv("/home/ubuntu/ohisell/backend/.env")`, SQLite·UTC)
- 아티팩트(기능 전수표 웹판): https://claude.ai/code/artifact/78839cb2-6991-459b-bb17-5e505651c58e

## 2. 이번 세션 완료 목록

- ✅ **`docs/references/131_pao_feature_census_20260903.md` 신설(352줄)** — PAO 기능 전수 목록. 북극성 5요소·5층을 **분모**로 세 목록(`ref 115` 손 31 / `ref 65` 논문 13 / 북극성 §2·§4)을 합쳐 **합집합 60행**(됐다 24·부분 10·없다 26). 3축 팬아웃(Sonnet 3기, 위임문에 북극성 해당 절 원문 동봉).
- ✅ **`backend/app/services/naver_ad/exclusion_grade.py`** — `grade_review_audit()` + `repair_grade_review()` 신설. 등급↔`next_review_at` 어긋남을 세고 **규칙값으로만** 고친다(`default_next_review_at` 단일 출처).
- ✅ **`backend/tests/test_exclusion_grade.py`** — 신규 13종(총 **32 test 함수 / 45 passed**).
- ✅ **트랙 확인줄 2개** (`docs/tracks/active/track_naver-ad-optimization.md`, TRACK-CONTRACT 블록 안).
- ✅ 착지: PR **#698** 머지 `9709a3a8` · CI **3/3 실통과** · `--force` **미사용**.
- ✅ 등록부 n=83 등록(`proc=Jinoui-MacBookPro:71009`).

**코드 변경 = 백엔드 1파일 + 테스트 1파일. prod 배포 0 · 네이버 광고계정 쓰기 0 · 마이그레이션 0.**

## 2-1. 완료 QA — 적대 리뷰 (별도 기 · Opus · 2026-09-03 20:3x~21:1x KST)

- **작업 목적(정본 원문)**: "이제 전체적으로 보자. 1번 계약, 1번 이슈를 포함해서 운영을 위해 손을 달고 쿠팡에서 잘 팔리는데도 불구하고 노출을 일으킬 수 없는 경우 어떻게 극복방안을 종합해서 계약서를 작성하자" (Jino 2026-08-30 21:45, 계약 D-NAO-280)
- **이번 세션 Jino 지시 원문**: 「PAO의 기능별로 하나씩 구현을 해나가자」(17:21) → 「**북극성 정한것에 포함된 모든 기능이 여기 다 리스팅됬어?**」(17:25) → 「1번을 자동화하자」(18:00) → 「(b) → (a) 순서로 한 슬라이스로 가자」(18:10) → 「b 먼저 마무리하자」(20:16)

**판정(적대 리뷰): 1R FAIL(P1=1, P2=6) → 2R PASS(미해소 P1=0).**

**1R P1-1 원문 요지**: *"감사가 «규칙대로 맞는 행»을 어긋남으로 세고, 수리가 도래한 재개방을 취소한다 — 점화 프리플라이트 경고가 조용히 꺼진다."*
`should`를 «오늘» 기준으로 계산했는데 `next_review_at`은 «등급이 붙던 날» 기준 값이다. `id=1`(07-22 등급 + 30일 = 08-21, **규칙에 완벽히 부합**)이 어긋남으로 떴고 수리는 **+43일 밀었다**. 그 `id=1`이 하필 이 저장소가 이미 「due 1건」으로 표면화하고 `ignition_preflight.WARN_REOPEN_DUE`가 점화 경고를 내는 바로 그 행이다 ⇒ **아무것도 해결 않고 경고만 꺼진다.**
★리뷰어가 **내 산술도 잡았다**: 헤드라인 「어긋남 20건」인데 서사는 13+4+1=**18**, **차이 2가 정확히 그 위양성**이었다.

**2R 판정 원문 요지**: 「해소 ✅」 — prod 독립 재현 `ok=3971 repairable=5 pending_decision=13 due_untouched=1 ungraded=0`(합 3,990 ✓), `id 1·2 in repairable? **False**`. 우회 사냥(5등급 × 도래 + 「정확히 오늘」 **7행**) → **수리로 바뀐 행 없음**.
- **질문②(NULL 한 방향 비교의 손실) 판정 원문**: *"받아들일 만하다. P1 남지 않는다"* — ⓐ새 규칙은 **원리적으로 기존 날짜를 옮길 수 없다** ⓑ`why` 문구가 「전부 센다」를 참칭하지 않는다 ⓒprod 손실 사례 0건.
- **질문③(`due_untouched`가 침묵과 구별되는가)**: *"코드 층에서는 구별된다. 다만 **사람이 보는 층에서는 아직 아니다** — 호출부가 여전히 0곳"* (= P2-6 이월).

**항목별**
| 합격기준 | 명령 | 관측 | 판정 |
|---|---|---|---|
| 어긋남을 «전부» 센다 | prod 감사 재현 | `repairable=5 · pending=13 · due_untouched=1` | 달성(단 **NULL 정합성 한 방향**으로 범위를 좁혔음을 문서에 명시) |
| 날짜를 발명하지 않는다 | 코드 독해 + 변이 R2 | `default_next_review_at` 단일 출처 · 앵커 발명 제거 | 달성 |
| 오컷의심을 자동 수리하지 않는다 | 변이 R6 | **3 failed**(1R 2건 → 강화) | 달성 |
| 회귀 새 실패 0 | `pytest tests/ -q` | **7,735 passed / 실패 0** (리뷰어 관측 7,734+1skip) | 달성 |
| 적대 리뷰 PASS(P1=0) | 2R | 미해소 P1 **0** | 달성 |

**미달·미판정 항목**: 없음(2R PASS). 단 아래 §5 이월 6건은 **커버리지 구멍**으로 남았다.
**목적 전환 여부**: 없음. (a)는 전환이 아니라 **실측에 의한 불가 판정**이다.

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: "무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야." (Jino 2026-07-19, D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음** — 기능 전수는 마일스톤 체크박스를 겨누지 않고, (b)는 계약 P2 계열이나 체크박스 대상이 아니다. 계약 「목적관철」 **12/38 불변** · M2 **0/8 불변**.
- **헤더에 남긴 확인 줄**: 2개 (`17:4x` ref 131 신설 · `20:2x` 기능 #1 (b)/(a))
- **다음 세션 후보 항목**: §6
- **트랙 종결 여부**: 아니오(2≠7)

## 2-3. 착지

- **완료 단계**: 커밋 → push → PR → 적대 리뷰(1R FAIL → 2R PASS) → 머지 → 정리
- **멈춘 단계**: **없음**
- **재개 명령**: 해당 없음
- **좌표**: 커밋 `12db67ea`(1R 대상) + `2716d58e`(2R 수정) · PR **#698** · 머지 **`9709a3a8`** · CI 3/3(py3.10 12m48s · py3.14 9m59s · frontend 1m47s) · `--force` **미사용**
- **리뷰 판정**: 1R **FAIL**(P1=1·P2=6) → 2R **PASS**(미해소 P1=0)

## 3. 확정된 결정사항

- **D-NAO-285** — PAO 기능 전수 목록(`ref 131`)을 북극성 5요소·5층 분모로 신설. 순서·우선순위는 **정하지 않는다**(채택은 Jino).
- **(a) 검색어 제외 자동승인 = 불가 판정** — 대상 12건이 전부 SHOPPING이고 ncc `restricted-keywords`가 SHOPPING 쓰기 불가(**HTTP 400 code 3728** 실측, SS3-A 실행기 docstring §실측-0).
- **오컷의심 13건은 자동 수리에서 분리** — `pending_decision`으로 «목록»에만. 실행은 **소유권 분리 후**(계약 §1-2 · 북극성 §8-② Jino 결정 대기).
- **감사 범위를 NULL 정합성 한 방향으로 좁힌다** — 값이 틀린 날짜는 `ok`로 센다(의도된 손실, 2R가 「받아들일 만하다」 판정).

## 4. 핵심 파일 목록

| 파일 | 무엇 |
|---|---|
| `docs/references/131_pao_feature_census_20260903.md` | ★**기능 전수 60행 + §8 진행 기록**. 다음 세션의 분모 |
| `backend/app/services/naver_ad/exclusion_grade.py` | 등급 정본 + 이번 신설 감사·수리 |
| `backend/tests/test_exclusion_grade.py` | 45 passed. `_row_at()` 헬퍼가 「과거에 규칙대로 박힌 행」·「도래한 행」을 만든다 |
| `docs/references/82_pao_north_star_20260819.md` | 북극성. ★§3 표 **L1 행이 아직 4/36**(실제 5/36) — stale |
| `docs/references/115_issue2_pao_missing_hands_20260830.md` | 손 31행. **6행이 4일 만에 낡았다** — 단독 인용 금지 |
| `docs/contracts/CONTRACT_pao_purpose_and_hands.md` | 목적관철 계약 12/38 |

## 5. 알려진 이슈·주의사항

- ★★**「됐다 24」는 부풀 수 있다** — L3 10행(#2·5·8·9·10·11·13·17·27·31)은 `ref 115` 08-30 판정을 **재확인 없이** 옮겼다 📄. 같은 표의 6행이 4일 만에 낡았다.
- ★**북극성이 계기판인데 계기판이 낡았다** — 오늘 4회 관측(L0 절이 작성 당일 저녁부터 부정확 · §3 표 L1 행 4/36 · §4 간극1 · M6 진입조건 감사 3회 「재측정 안 됨」). `ref 131` §5 신3이 그 행이다.
- **이월 6건(2R 리뷰어 발견, 라운드 안 늘림)**:
  ①**R7·R8·R10 변이 생존** — 커버리지 구멍(결함 아님). 도래 가드 `continue`는 **무관·미검증** 도래 행에만 하중 · 「정확히 오늘」 경계 무테스트 · `cycle>1` 테스트 없음(**P2-1 수정은 테스트가 아니라 독해로만 검증**).
  ②★**앵커 발명이 수리 «값»에는 남았다** — NULL이던 성과미달 4건은 `today+30 = 10-03`으로 채워진다(등급일 기준이면 09-16).
  ③★★**값 검사 부분 회복안(채택 권고)**: 정당한 만료일은 어떤 앵커(≤오늘)에서도 `today + 90`을 넘을 수 없다 ⇒ `actual > today+90`은 **앵커 없이도 틀렸다**. 포기한 손실의 일부를 되찾는다.
  ④도래 가드가 HOLD 분기 «앞»이라 도래한 오컷의심은 `pending_decision` 계수에서 빠진다(prod 사례 0).
  ⑤전수 스캔 2회(비-dry_run이 감사를 두 번 돈다 — 비용만).
  ⑥**P2-4**(어휘 밖 등급 1건이 감사 전체를 예외로 죽인다 — prod 0건·도달 불가) · **P2-6**(호출부 0 = **사람이 보는 표면 없음**).
- ⚠️**리뷰어 임시본이 남았다**(내 `rm` 권한 거부): `/tmp/review-n83`·`/tmp/review-n83b`·`/tmp/mut*.py`·`/tmp/eg2.orig.py`·`/tmp/test_zz_2r.py`·`/tmp/q*_n83.py`. **git 오염 0**(`worktree add`가 끝내 등록 안 됨).
- ⚠️**훅의 `[체인] ⛔ pao-논의 n=49`는 stale 오탐** — 공유 폴더가 ~600커밋 뒤처졌다. 생존 판정은 **origin/main**으로.
- ⛔**무접촉**: `pao-uiux` n=8(PID 15269 생존 확인 · 콘솔 10개 화면 «행선지» 설계 중 — **콘솔 화면에 손대지 말 것**) · `발주예측` n=8 · `승인배선` n=2.
- **라이브 관측(무접촉)**: 오늘 자동 집행 10건이 소재 1개에 `1,510→1,730→1,980→1,690→1,440→1,230` = **순 −18.5%**(UP 2·DOWN 3). 09-01 스코프 추가분 2소재는 **+50%에서 멈춰 이틀째 0건**(상향 상한 리셋 입구 없음). 대기 카드 899건 · 죽은 승인카드 133건(전부 08-29~30 생성, 신규 발생 멈춤).

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: 계약 D-NAO-280 발단 Jino 원문(§2-1 상단) + 이번 지시 「PAO의 기능별로 하나씩 구현을 해나가자」.
- **남은 슬라이스**:

- [ ] ★**Jino 결정 ①** — **오컷의심 13건을 열 것인가.** BEP 1.676의 **4~7배**로 돌던 검색어다(`갤럭시울트라S26지문방지` 7.829 · `오하이풀커버` 7.569 · `액정필름갤럭시S26` 5.976). 지금은 목록에만 오른다. **되돌릴 수 없지 않다**(재제외 경로 생존·일일 캡 10) 그러나 계약이 「실행은 소유권 분리 후」로 순서를 정했다 ⇒ **묻는다**(3문 검사 ②).
- [ ] ★**Jino 결정 ②** — **(a) 자리를 무엇으로.** ①못 낼 카드 생성 중지(**추천** — 지금 카드는 「할 수 있다」면서 못 한다 = 원장이 거짓말) ②사람 목록으로 표면화 ③쇼핑 제외의 다른 경로 조사(존재 여부 **[미상]**) ⇒ **범위를 바꾸므로 묻는다**(3문 검사 ①).
- [ ] **값 검사 부분 회복** — `actual > today + REVIEW_BACKOFF_MAX(90)`은 앵커와 무관하게 틀렸다. 2R 리뷰어 **채택 권고**. **묻지 말고 진행**(되돌릴 수 있고 범위 안).
- [ ] **이월 커버리지 3종**(R7·R8·R10) 테스트 보강. **묻지 말고 진행**.
- [ ] **P2-6 배선** — `grade_review_audit`를 사람이 보는 표면에 잇는다. ⚠️**콘솔 10개 화면은 `pao-uiux` 설계 대기** — 그 트랙 결과를 기다리거나 화면 아닌 표면(브리핑·CLI)을 쓴다.
- [ ] **미검증 3,970 재분류 루틴** — 모듈이 「재료 도래 시 재분류」라 적어 뒀는데 그 루틴이 없다. **별 슬라이스**(규모가 커서 끼워 넣으면 다른 게 묻힌다).
- [ ] **하한 게이트**(직전 후보, 설계까지 해 둠) — 세 곳이 1순위로 지목. ★**두 레인 모두 배선해야 한다**: 계약은 일 레인(`auto_operator.py:1149-1150`)을 지목했으나 **오늘 −18.5%를 만든 건 시간당 레인**(`run_hourly_lane` :3347·:3552)이다. 일 레인만 고치면 체크박스는 닫히고 돈은 계속 샌다.
- [x] ~~기능 전수 목록~~ 완료 — `ref 131`, 커밋 `71419d90`
- [x] ~~기능 #1 (b)~~ 완료 — 머지 `9709a3a8`

## 7. 새 세션 시작 프롬프트

/session-relay pao-논의
