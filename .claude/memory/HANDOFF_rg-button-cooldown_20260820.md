# 세션 인수인계: RG 수집 버튼이 1시간 쿨다운에 조용히 삼켜지던 것 수리
> 저장일시: 2026-08-20 11:0x (KST)
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## 1. 프로젝트 위치 및 환경
- 로컬 경로(워크트리): `/Users/jino/.claude-worktrees/Ohiselling/rg-button-cooldown`
- 브랜치: `claude/rg-button-bypasses-cooldown` — PR #314(OPEN·미병합)
- 소속: 트랙 아님. 완료된 「Wing 세션 자동화」의 후속. 정본 문서 =
  `docs/PLAN_coupang-claim-retry-lease.md` §6(이번 세션이 작성).
- 배포 대상: **Mac 로컬 도구**(`~/.ohisell/tools/wing_browser_fetcher.py`) — prod
  백엔드는 무변경. 재기동: `launchctl kickstart`.
- 백업: `~/.ohisell/tools/wing_browser_fetcher.py.bak-20260820`.

## 2. 이번 세션 완료 목록
- ✅ `wing_browser_fetcher.py` RG 폴 루프에 쿨다운 면제 로직 추가 — prod
  `refresh-status`의 `requested_at`이 마지막으로 집어본 값과 다르면 «사람이 새로 누른
  요청»으로 판별해 쿨다운을 면제한다. 면제는 새 요청 1회에만 적용되도록
  `rg_served_request_at` 기록과 `last_rg`(쿨다운 시작 시각) 갱신을 **둘 다 claim 호출
  앞**에 둔다.
- ✅ 예외 경로·`claimed=False` 경로에 짧은 백오프(`rg_retry_at`, 기본 30초) 추가 —
  claim이 실패해도 다음 정상 쿨다운(1시간)까지 통째로 막히지 않게 한다.
- ✅ 쿨다운/백오프에 막혀 스킵할 때 로그를 남긴다(최대 5분에 1회). 남은 시간(ETA)은
  실제로 막고 있는 문(백오프 30초 vs 쿨다운 3600초) 기준으로 계산한다.
- ✅ repo↔Mac 가동본 동기화 — 2026-08-13 prod Basic Auth 작업(`_basic_auth` 헬퍼 +
  호출 11곳)이 Mac 가동본에만 반영돼 있던 것을 repo에 반영. 그 결과 드러난 테스트
  22건의 좁은 스텁(`auth=`를 못 받음)을 전수 수정.
- ✅ 신규 테스트 14건(런타임 시뮬레이션 4건 포함) — 그중
  `test_claim_exception_does_not_cause_infinite_reclaim`가 P1-1(아래) 재발을 잡는다.
- ✅ 문서: `docs/PLAN_coupang-claim-retry-lease.md` §6 신설(무엇이 있었나 → Jino 결정
  → 한 것 → 라이브 증거 → 부수 → 남은 것).
- ✅ 커밋 5건: `07030022`(동기화) → `034fc0a4`(면제 로직) → `2da88b03`(1R 반영) →
  `44d5778a`(문서 §6) → `35169639`(2R P2 회귀 테스트). PR #314 OPEN·미병합.

## 2-1. 완료 QA (앵커가 있던 세션은 필수 — `~/.claude/CLAUDE.md` §2)
- **작업 목적(정본 원문)**: `docs/PLAN_coupang-claim-retry-lease.md` §6 — 「RG 수집
  버튼이 1시간 쿨다운에 조용히 삼켜지던 것을 수리한다. 버튼 요청은 쿨다운을 면제하되,
  면제는 새 요청 1회에만 적용하고 실패 재시도 폭주는 막는다.」
- **합격기준(원문)**: ①쿨다운에 걸린 상태에서 버튼을 누르면 면제되어 즉시 claim이
  시도된다(라이브 관측) ②면제는 그 요청 1회에만 적용되고 다음 자동 쿨다운은 정상
  작동한다 ③claim 실패·예외가 반복돼도 재시도가 폭주하지 않는다(백오프로 제한) ④건너뛴
  스킵은 로그로 남아 침묵하지 않는다.
- **판정**: **부분달성** — ①②④ 달성·「안 함」 위반 0건 / ③은 코드·테스트 증거로만
  뒷받침되고 합격기준이 명시한 「라이브」 실사례에는 닿지 못해 **판정불능**으로 남긴다
  (2026-08-20 QA 시점).
- **항목별**:
  - ① — 라이브 로그: `10:28:16 완주(쿨다운 시작) → 10:28:38 재요청 → 10:28:46 트리거
    (두 계정) → 10:28:55/10:29:02 완주 → requested=False`, prod
    `claimed_at=10:28:46.94·attempts=1·in_flight=True` → **달성**.
  - ② — 같은 라이브 창에서 재요청 없이 시간만 흐른 구간은 쿨다운이 그대로 유지됨을
    코드 경로로 확인(신규 테스트) → **달성**(라이브에서 「다음 자동 쿨다운」까지
    기다린 재확인은 없음, 근거는 테스트).
  - ③ — `test_claim_exception_does_not_cause_infinite_reclaim` 등 런타임 4건이
    5~20회전 시뮬레이션으로 재claim 폭주가 없음을 확인. 그러나 prod를 실제로 5xx
    상태로 만들어 라이브 재현한 사례는 없음 → **판정불능**(코드·테스트 증거는 있으나
    합격기준이 요구하는 「라이브」에는 못 닿음).
  - ④ — 라이브 배포본에서 스킵 로그 발생을 직접 관측한 사례는 세션 안에 없었음(스킵
    상황 자체가 안 재현됨) → 코드 존재는 확인, 라이브 관측은 **판정불능**.
- **미달·미판정 항목**: ③④의 라이브 실사례 — 다음 세션에서 쿨다운 구간에 버튼을 다시
  눌러 스킵 로그·백오프 동작을 직접 관측하면 판정 가능.
- **목적 전환 여부**: 없음. (※QA 시점엔 적대 리뷰 2R가 미완이었다 — 그 뒤 **2R는
  PASS**로 끝났다. 이 사실은 QA 판정 이후에 확정됐으므로 QA 원문에는 반영되지 않았고,
  여기 병기한다.)

## 2-2. 트랙 진행률
> 이 작업은 트랙 소속이 아니다 — 이 절은 통째로 뺀다.

## 3. 확정된 결정사항
- **쿨다운은 버튼 요청을 막지 않는다** — Jino 09:43 원문 이후 확정. 쿨다운의 목적은
  «실패 재시도 폭주 방지»이지 «사람이 누른 것을 막는 것»이 아니다(선택지 4개 중 이
  방식으로 확정).
- 면제 판별자는 prod `refresh-status`의 `requested_at` 값 변화다 — 다른 판별자(예:
  버튼 클릭 이벤트 자체)는 쓰지 않는다.
- 면제는 **1회성**이다 — 같은 요청에 대해 반복 면제하지 않는다.

## 4. 핵심 파일 목록
| 파일 | 역할 |
|------|------|
| `~/.ohisell/tools/wing_browser_fetcher.py` (Mac 가동본, repo에도 동기화됨) | RG 폴 루프 — 쿨다운 면제·백오프·스킵 로그 로직 |
| `docs/PLAN_coupang-claim-retry-lease.md` §6 | 이번 작업의 정본 서술(무엇이 있었나~남은 것) |
| 신규 테스트 14건(런타임 4건 포함, wing 페처 테스트 파일) | 면제·백오프·무한재claim 방지 회귀 |
| `test_wing_vi_detail.py:317` | Basic Auth 스텁 좁음 → 전수 수정 대상이었던 파일 |

## 5. 알려진 이슈 / 주의사항
- ~~`refresh-status`가 「초록인데 `last_success_at` 5일 전」~~ → **오독이었다. 결함이
  아니다** (2026-08-20 12:4x 실측 후 정정, Jino 질문 *"이건 진행하는게 좋아?"*에 답하며).
  - `status`는 **Wing 쿠키 상태**(302/401→red)이지 데이터 신선도가 아니다 — 초록은 사실이다.
  - `stale` 임계는 **30일**(`rg_settlement_sync.py` `_RG_STALE_HOURS = 30*24`)이고 그 값엔
    근거가 코드에 적혀 있다: *"RG 정산은 주 단위 → 30일 무push까지 stale 아님(주간 캐던스
    + Mac 야간 off 오탐 방지)"*. 5일은 정상 범위 한복판이다.
  - 완결성은 시계가 아니라 **결손 조회**(`layer2-gaps`)가 잰다 — 매 회차 `prod 결손 0`.
  - ★교훈: **서로 다른 것을 재는 두 계기를 나란히 놓고 모순이라 부르지 마라.** 「초록인데
    묵음」이 이 repo의 반복 실패 형태인 것은 맞지만, **이 사례는 그것이 아니다.**
  - 남는 진짜 간극 1개: RG가 `collection_watchdog` 대상이 아니다(`RESCUE_STREAMS =
    {"supplier_hub"}`). 단 RG는 **버튼-only**라 「안 도는 것」이 정상 상태여서 시간 기반
    경보는 오탐이 기본값이 된다 — 보험을 든다면 시계가 아니라 **「결손 N건」 표면화**.
    **Jino 판단(12:4x): 지금은 안 한다.**
- **합격기준 설계 교훈**: 「재시도 폭주 방지」류 항목은 라이브로 못 잰다 — 폭주를
  실제로 유도하려면 prod를 일부러 5xx로 망가뜨려야 하므로, 애초에 이런 항목은
  코드·테스트 증거로 판정하도록 계약 단계에서 합격기준을 설계해야 한다(다음엔
  「라이브」를 요구하지 말 것).
- **리뷰어에게 전용 워크트리를 줘야 한다** — 이번 적대 리뷰가 작업 워크트리를
  공유해서 돈 흔적이 있다(별도 확인 필요). 다음 적대 리뷰부터는 리뷰어 세션에 별도
  워크트리(또는 read-only 체크아웃)를 배정할 것.
- Mac 로컬 도구(`~/.ohisell/tools/`)는 배포 CAS·마이그레이션 가드가 안 닿는 영역이다
  — 직접 고치기 전 `diff <repo> <가동본>`을 항상 먼저 뜬다(교훈 #340).

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)**: 없음 — 이 작업 자체는 §6 문서·코드·테스트·배포로
  완결됐다. 다음 세션은 새 작업(트랙 재개 또는 위 이월 2건 중 택1)으로 시작한다.
- **남은 슬라이스**:
  - [x] ~~`last_success_at` 시계 문제~~ — **실측 결과 결함 아님으로 종결**(§5 참조).
    Jino가 「진행하는 게 좋냐」고 물어 재봤고, 답은 «아니오»다. 쫓지 마라.
  - [ ] ③④ 합격기준의 라이브 실사례 확보(다음 쿨다운 구간에 버튼을 눌러 스킵 로그·
    백오프 동작 직접 관측).
  - [x] ~~PR #314 병합~~ — **완료**(Jino가 `safe_merge.sh 314 --force`, 병합 커밋 `5dad812b`).
    main의 `tools/wing_browser_fetcher.py`가 Mac 가동본과 바이트 동일함을 확인했다.

## 7. 새 세션 시작 프롬프트
아래를 복사해서 새 대화 첫 메시지로 사용:

```
.claude/memory/HANDOFF_rg-button-cooldown_20260820.md 읽고 이어서 작업해줘
```
