# 세션 인수인계: 문은 열었는데 그 앞에 아무도 없었다
> 저장일시: 2026-09-06 06:35 KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것
> 체인: `pao-논의` n=89 (세션 `935aae94`) · 목표이름: **탐색소생 목표**(D-NAO-289, 종결) → **스코프확대 목표**(전환 후)
> 검증 명령: `ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && grep -c 'D-NAO-289' app/services/naver_ad/auto_operator.py; sqlite3 ohisell.db \"select count(*) from naver_adgroup_scope where enabled=1;\""` → **3** 과 **9** 가 나오면 인계가 참이다(배포분 생존 + 스코프 9행). 받는 세션은 **위임 없이 직접 실행**한다.

## 1. 프로젝트 위치 및 환경
- 작업 클론: `/Users/jino/ohisell-pao-n89` (브랜치 `feat/pao-n89`)
  ⚠️ **iCloud 공유 폴더에서 `git worktree add`가 안 된다**(`fatal: mmap failed`, 2회 재현). GitHub에서 로컬 디스크로 `--depth 1` 클론 후 fetch로 우회했다.
  ⚠️ 공유 메인 폴더(`~/Library/Mobile Documents/.../Ohiselling`)는 `origin/main`보다 **1,250커밋+ 낡았다** — 트랙·체인 훅 경고는 전부 그 stale 사본을 읽은 **오탐**이다(이번 세션에 5회 이상).
- prod: `sellc.ohitech.co.kr` · 백엔드 `/home/ubuntu/ohisell/backend` · 활성 포트 **:8001** · venv `.venv/bin/python`
- 배포: `scripts/safe_deploy.sh` · 병합: `scripts/safe_merge.sh` · 번호: `scripts/next_ids.sh`
- ★**시간대 함정(이번 세션 완료 QA가 발견)**: 같은 prod DB 안에서 **`naver_change_log.changed_at`은 KST 저장**, **`ops_diary_entries.created_at`은 UTC 저장**이다. 절대 시각으로 자르면 한쪽이 9시간 틀린다. `failures.jsonl` 기록 완료.

## 2. 이번 세션 완료 목록
- ✅ **계약 `docs/contracts/CONTRACT_exploration_revival.md` 신설·승인·종결** — D-NAO-289 「탐색소생 목표」(Fable 초안 → 코디네이터 보강 → Jino 승인 16:42)
- ✅ **`backend/app/services/naver_ad/auto_operator.py`** — 가드5 `_exploration_daily_loss_reason`에 «표본 부족» 예외 + 실효입찰 **지연 getter**. origin/main 대비 순증 **+79줄**(계약 §7 목표 80 이내)
- ✅ **`backend/tests/test_naver_exploration_revival.py`** 신설(10테스트) · `test_naver_ad_exploration_bx3.py` 픽스처 처분 · `test_naver_hold_reasons_and_inday_catchup.py` 인구조사 등재+분류 단언
- ✅ **PR #741 머지 `1eb90342`** (CI 3/3 실통과·`--force` 미사용) → **prod 무중단 배포**(CAS 통과·마이그 0·다운타임 0초·:8011→:8001)
- ✅ **적대 리뷰 1R FAIL(P1=1) → 2R PASS(P1=0)** + 2R 생존 변이 1종을 그 자리에서 수리·재확인
- ✅ **완료 QA 2회**(D-NAO-289 / 스코프확대) — 판정 원문은 §2-1
- ✅ **스코프 3행 신설**(id 7·8·9) — 감사 API `PUT /api/naver/ad/scope/adgroup` 경유, `naver_change_log action='adgroup_scope_change'` 3행
- ✅ 트랙 확인줄 **16줄** 누적 · `failures.jsonl` 1건 · 계약 §11 QA 원문

## 2-1. 완료 QA (전역 §8)

### ① D-NAO-289 「탐색소생 목표」 (2026-09-05 20:30~20:45 KST · 별도 Sonnet · 읽기 전용)
- **작업 목적(정본 원문)**: Jino *"안된다고 그냥 꺼버리면 그 광고그룹은 영원히 기회를 잃는거잖아. 이런건 어떻게 처리를 하게 되어 있어?"*(15:51) → *"A안으로 가자"*(16:21)
- **합격기준(원문)**: 계약 §4 ⓐ~ⓜ (13항목)
- **판정(Jino 지시 원문)**: **부분달성** — 구현·검증·배포는 실물 확인. 다만 「그 그룹이 실제로 기회를 되찾는」 라이브 장면 0건이고, 리뷰 이월 P2-8(예외 창 14일 vs 스톱로스 창 3/7일 사이 미접촉 구간)이 잔여를 인정한다 — 「영원히」 문제의 완전 폐쇄는 아니다 (2026-09-05 20:45 KST)
- **판정(계약 §4)**: **부분달성** — 즉시 확정 가능한 9개 전부 달성, 시간 경과형 4개(ⓘⓙⓜⓛ) 판정불능 (2026-09-05 20:45 KST)
- **판정(트랙 D-NAO-59)**: **판정불능** — 총이익에 물린 금액을 잴 라이브 원장이 아직 없다 (2026-09-05 20:45 KST)
- **판정(PAO 북극성 M4)**: **미달** — M4 합격 관측과 전제인 §8-①② 결정 2건이 이 세션에서 안 움직였다 (2026-09-05 20:45 KST)
- **종합: 부분달성**
- ★**QA 이후 ⓛ이 달성으로 바뀌었다**: 00:20 KST 사이클에서 새 사유문이 실물로 10그룹 기록됐고, 계약 §5 SQL(substr 1,48)로 **한 칸(211건)으로 모인다** — 적대 리뷰 P1-1 수정이 라이브에서 작동함을 확인. **ⓘⓙⓜ은 여전히 판정불능**(7일 창·이동 0건).
- **미달·미판정 항목**: 북극성 M4 미달 · ⓘⓙⓜ 판정불능 · P2-8 잔여

### ② 「스코프확대 목표」 (2026-09-06 06:3x KST 진행 중 — 결과는 아래 §5 참조)
- **합격기준(원문)**: ①넣은 그룹이 콘솔 PAO 스코프 화면에 뜬다 ②다음 레인에서 그 그룹 판정이 실제로 돌아 일기에 남는다 ③넣지 않은 그룹은 행위 변화 0
- **코디네이터 실측(QA 아님)**: ② **달성**(21:20:00.003678 무인 실집행 1건) · ③ **달성**(21:14 이후 입찰 쓰기 그 1건뿐) · ① **미확인**(화면 안 봄)
- ⚠️ **QA 판정은 이 HANDOFF 작성 시점에 미도착** — 받는 세션이 `qa-scope` 결과를 확인하거나 재실행할 것.
- **목적 전환 여부**: 🔁 **있음** — 「탐색 되살림 A안」 → 「스코프 확대」(2026-09-05 21:02, Jino 지시). 선언 후 앵커 갱신 완료.

## 2-2. 트랙 진행률
- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: "무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야." (Jino 2026-07-19 — D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음(체크박스 0개)** — 계약 §9-1이 스스로 "M4 합격 관측에 닿지 않는다"고 선언했고 실제로 안 닿았다. 다만 **④자동화 운영은 움직였다**: 엔진 관할이 **1그룹 → 4그룹**(30일 광고비 8.3% → 14.7%)
- **헤더에 남긴 확인 줄**: 16줄 누적, 마지막 `확인: 2026-09-06 00:2x KST [935aae94] — 🔁목적 전환 후 스코프 3행 신설 + D-NAO-289 라이브 첫 관측…`
- **다음 세션 후보 항목**: M4(스코프 추가 확대) · M2
- **트랙 종결 여부**: N≠M — 해당 없음

## 2-3. 착지
- **완료 단계**: 커밋 → push → PR #741 → 적대 리뷰(1R FAIL → 2R PASS) → **머지 `1eb90342`** → prod 배포
- **멈춘 단계**: 문서 커밋 **4개 미착지**(아래 좌표) — 이 HANDOFF 커밋과 함께 착지 예정
- **재개 명령**: `cd /Users/jino/ohisell-pao-n89 && git fetch origin && git merge origin/main && git push origin feat/pao-n89 && gh pr create`
- **좌표**: PR #741 · 머지 `1eb90342` · 미착지 `2390afff`·`af0153a1`·`be14925a`·`7e8cfa90`
- **리뷰 판정**: 1R FAIL(P1=1) → **2R PASS(P1=0)**

## 3. 확정된 결정사항 · 실측

### D-NAO-289 — 순환이 닫히지 않는 좌표(코드로 확정)
```
탐색 가드5  _exploration_daily_loss_reason (auto_operator.py:2787)
  → _bleeding_hold_reason (:895) → shopping_group_bep 보드에 있으면 제외
보드 진입: 14일 cost>0 ∧ 보정ROAS<BEP  /  탈출: 「14일 비용 0」 또는 「ROAS≥BEP」뿐
  (account_diagnosis.py:206-213)
⇒ 바닥 그룹은 소액 비용이 계속 나 비용 0이 안 되고 전환 0이라 ROAS 0 = 영구 bleeding
```
**처방**: bleeding이어도 `cost_asof < 실효입찰 × account_diagnosis.LOW_CLICK_THRESHOLD(=10)`이면 «표본 부족»으로 보고 탐색 허용. **새 상수 0개** — 스톱로스 보드 자신이 `:818-819`에서 그 미만을 «성급한 사살 금지»로 안 건드리는 바로 그 정의다.

### 라이브 실측 (2026-09-05~06)
- bleeding 보드 최신 asof **2026-09-04, 67그룹** = 표본부족 **42** + 확정손실 25
- 체류일수 상위 **59·57·56·56·56·54·54·50·49·48일**, 다수가 보드 시작일(07-08)부터
- 탐색 실집행 마지막은 **2026-07-30** (37일간 0건)
- 실효입찰 ≤70원(진짜 바닥) **0/67** — 「바닥 입찰 38그룹」은 **그룹입찰(명목) 착시**였고 실효는 150~2,190원
- **00:20 KST 사이클**: 새 사유문 **10그룹**(전부 확정손실) · §5 SQL 한 칸 **211건** · **예외로 «열린» 그룹 0개**

### ★★가장 중요한 발견 — 진짜 병목은 가드5가 아니라 스코프였다
표본부족 42그룹의 소재를 실측:
| | 그룹 | 왜 안 열리나 |
|---|---|---|
| optimizer ≠ ours | **25** | 엔진이 그 캠페인을 아예 안 봄 |
| ours ∧ auto_operate=0 (사생활) | **8** | 마스터 OFF |
| **ours ∧ auto_operate=1 (TPU)** | **9** | 전부 탐색 후보 «자격» 있고(정착 클릭 0~3 < 10) **전부 스코프 밖**, 그리고 **이번 사이클 후보 목록에 안 뽑혔다 — 원인 미규명** |

### 소유권 — 대행사
- 2026-09-04(금) **17:07:40~45, 6초 동안 93건** 외부 입찰 변경(`feed_verdict='real'`). 우리 두 `ours` 캠페인 안 **37건 전건 하향**(비율 0.143~0.505 = −50~−86%), **우리 스코프 그룹 4개 포함**
- Jino가 **분리 통보 완료**(2026-09-05 21:0x). **효과는 아직 관측 불가** — 마지막 대행사 변경이 통보 «전»이다

### 스코프 확대 집행
```
id 7  grp-…054537529  S23울트라   accel   정착창 전환 14 · RoAS 1.604 > 목표 1.511
id 8  grp-…044108257  S23플러스   accel   정착창 전환  8 · RoAS 2.544 > 목표 1.514
id 9  grp-…044108616  S24울트라   brake   정착창 전환  7 · RoAS 1.173 < 목표 1.472
```
선정 규칙(발명 아님): **정착창 전환이 라이브 하한 7 이상인 스코프 밖 TPU 그룹 전체**
→ **21:20:00.003678 무인 실집행**: `nad-…375625297`(∈S23플러스) **1,740→2,000원(+14.9%)**, `dry_run=0`, 네이버 `editTm` 갱신 확증

## 4. 핵심 파일 목록
| 파일 | 무엇 |
|---|---|
| `docs/contracts/CONTRACT_exploration_revival.md` | D-NAO-289 계약 정본 + §11 완료 QA 원문 |
| `backend/app/services/naver_ad/auto_operator.py` | 가드5 예외 + 지연 getter (`_exploration_daily_loss_reason` ~2787) |
| `backend/tests/test_naver_exploration_revival.py` | 신설 10테스트 (ⓐ~ⓕ + ⓖ-f/f2/h + 버킷 단언) |
| `backend/app/services/naver_ad/account_diagnosis.py` | `LOW_CLICK_THRESHOLD=10`(:25) · 스톱로스 정의(:818-819) · 보드 진입/탈출(:206-213) |
| `docs/references/82_pao_north_star_20260819.md` | 북극성 — M4·§7·§8-①② |
| `docs/tracks/active/track_naver-ad-optimization.md` | 트랙 (확인줄 16줄 누적) |

## 5. 알려진 이슈·주의사항
- ⚠️ **iCloud 공유 폴더에서 워크트리 생성 불가** — 로컬 클론으로 우회할 것
- ⚠️ **훅 오탐 상시**: 트랙·체인 훅이 stale 공유 폴더를 읽는다. 검증만 하고 **공유 폴더에 쓰지 말 것**
- ⚠️ **DB 시간대가 테이블마다 다르다**(§1 참조)
- ⚠️ **PR CI 체크 0건 = 초록 아님**. 원인은 대개 `mergeable: CONFLICTING` — **충돌 해소 전엔 GitHub가 워크플로를 아예 안 돌린다**(이번 세션 실측). 체크 0건을 보면 CI 설정보다 `mergeable`을 먼저 볼 것
- ⚠️ **트랙 확인줄 충돌 시 해소법**: 양쪽 보존 + 시간순, **main에 이미 있는 줄은 자리를 안 옮긴다**. (이번에 한 번 어겼다가 `origin/main` 판을 기준으로 재구성해 `diff`로 검증했다)
- ⚠️ 열린 PR 6건이 남의 것으로 보인다(#648~#654) — 건드리지 말 것
- ⛔ **살아 있을 수 있는 체인**: `발주예측` n=8 · `승인배선` n=2 (등록부 `end_kst` null, proc 키 없음)
- **이월(적대 리뷰 P2, 채택 안 함)**: 사유 리터럴 모듈 상수화 · `latest_asof` 2회 조회 1회화 · TOCTOU 창 · **P2-8 예외 창 14일 vs 스톱로스 창 3/7일 미접촉 구간** · **P2-9 `bid_up_explore` 만료 부채는 오진 가능성**(`_expire_stale_pending`이 14일 폴백을 적용하므로 카드가 어려서일 수 있다 — 생성일 분포를 잴 것) · **P2-10 대기카드의 실질 위험은 「사람이 승인하면 죽은 카드가 된다」**
- **1R 생존 변이 (i)**: 손실 제외 시 `_record_blocked` 일기 제거해도 전건 초록(물려받은 자리)

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)**: **「1번을 자동화하자」**(Jino 2026-09-03 21:5x) → 그 아래 **「스코프를 넓힌다 (M4 카나리 확대)」**(2026-09-04) · **「분리통보 했어. 이제 스코프 확대 하자」**(2026-09-05 21:02)
- **3문 검사**: ①목표·범위를 바꾸는가 → **아니오** ②되돌릴 수 없는가 → **스코프 행 추가·`auto_operate` 켜기는 예**(Jino 발의로만) ⇒ 그 둘 외에는 **묻지 말고 진행**

- [ ] ★**9그룹이 왜 탐색 후보에 안 뽑혔나 규명**(읽기 전용, 돈 안 나감) — `exploration.py`의 후보 선정 쿼리 조건을 코드로 읽고 그 9그룹(S25플러스·99.플립2·S21·S21플러스·S20·Z플립4·Z폴드2·S26·S26플러스)이 어디서 걸리는지 확정. **이걸 밝히기 전에 스코프에 넣으면 또 라벨만 붙는다**
- [ ] **D-NAO-289 ⓘⓙⓜ 재판정 1회**(§8 허용) — 7일 창(2026-09-12 이후)에 §4-C ⓗ·ⓘ·ⓙ·ⓜ을 다시 재고 계약 체크박스 갱신. ⓜ은 **대기카드 생성일 분포**를 같이 잴 것(P2-9)
- [ ] **스코프확대 완료 QA 결과 수령·기록** — `qa-scope`가 이 HANDOFF 작성 시점에 미도착. 결과가 없으면 재실행
- [ ] **앵커 합격 ① 확인** — 콘솔 PAO 스코프 화면에 id 7·8·9가 뜨는지(화면을 아직 안 봤다)
- [ ] **대행사 분리 통보의 효과 관측** — 09-06 이후 `naver_agency_op`에 우리 두 캠페인 대상 변경이 다시 나오는지. 나오면 통보가 안 지켜진 것
- [ ] **S24울트라(brake) 동작 관측** — 목표 미달이라 내리는 쪽이 나와야 정상. 21:20 사이클엔 안 걸렸다
- [ ] 이월 P2들(§5) 처분 — 특히 **P2-8은 별건 계약감**(Jino 질문의 잔여분)

## 7. 새 세션 시작 프롬프트
```
/session-relay pao-논의
```
