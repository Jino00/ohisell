# HANDOFF — 소재 성과 grain 개통 + 피드 판별 규칙 3회 정정 (2026-08-04)

> 트랙: 네이버 SA 광고 최적화(우리판 MOP) · 기록: **D-NAO-139 후속(A) · D-NAO-140 S1·S2 완료(C·D) · D-NAO-138③ 후속(B)**
> 직전 세션: `HANDOFF_ad-change-detection-cadence-probe_20260803.md`(D-NAO-136, 선행 조사) · `HANDOFF_apply-tm-observation-slice_20260803.md`(D-NAO-137·138, 계약)

## §0 가장 먼저 알아야 할 것

**소재 성과 시계는 2026-08-04부터 돈다 — 로직을 세울 표본은 2~4주 뒤.** 오늘 새로 연 「소재 성과」 화면의 첫 조회 결과(BEP 미달 상위 다수)는 방향을 보여줄 뿐, 지금 그 숫자로 입찰·예산을 내리면 안 된다. 이유는 §D에 적힌 편향 3가지 — 표본 3일·간접전환 미정착·ADVoost/GFA 광고비 미포함 — 가 전부 "미달" 쪽으로 기운다.

두 번째로 알아야 할 것: 오늘 크론이 **08-04 07:46:19에 첫 정규 판정을 냈고**, 그 직후 규칙을 **세 번** 고쳤다(§A). 세 번째 결함(하룻밤 새 판정이 뒤집힘)은 "소급 판정에는 반드시 '판정할 수 없다'는 상태가 있어야 한다"는 일반 원칙을 남겼다 — 다른 소급 판정 로직을 만들 때 재확인할 것.

## §A D-NAO-139 후속 — 08-04 크론 첫 정규 판정 + 규칙 3회 정정 (전부 prod 배포 완료)

1. **08-04 07:46:19 크론 정상 발화(`ok`)**. 소재 grain 250건 감지 = `ad_edit` 246 + `bid_change` 4.
2. **판별자 1차 검증**: 피드 234 / 실조작 12 / 판별불가 4. **정답지인 `bid_change` 4건 중 FEED 오분류 0건.**
3. **★한계 발견**: 피드 전파가 항상 원자적이지 않다. 상품 3개는 소재 전량이 움직였는데 간격이 **66·437·501초**로 벌어졌다(03:24~04:39, 입찰 무변동). 나머지 피드 군집은 전부 간격 0초. 초판의 "같은 초" 규칙이 이걸 **거짓 실조작 9건**으로 만들었다.
4. **수정 ①·②(커밋 `df97610`)**: 가드①=추적 필드가 바뀐 op(`ad_edit` 아닌 전부)은 창·전량 여부와 무관하게 REAL(피드는 bidAmt·useGroupBidAmt·userLock을 안 건드린다) / "같은 초"→**창 900초**(관측값 0·66·437·501의 최댓값에 여유) / 접기 키를 `feed_cluster_at`(군집 시작 시각) 컬럼으로(마이그레이션 `f2b8c40d9e17`에 컬럼 추가).
5. **★★배포 직후 3번째 결함(커밋 `5d64672`, 마이그레이션 `a91d3f60c7b2`)**: 08-03 사건 26건이 **하룻밤 새 FEED→REAL로 뒤집혔다**. `ad_edit_tm`은 **마지막 수정만** 남기는데 그 소재들이 08-04 새벽 피드로 다시 움직여 08-03 시점의 근거가 덮인 탓. → **소급 판정은 소재가 다시 움직이는 순간 썩는다.** 가드②=그 소재 **자신의** 현재 editTm이 사건 시각의 창 밖이면 판정하지 않는다(unknown). 단 가드①이 먼저다(op 행 자체가 "입찰 1500→1100"을 담고 있고 그 사실은 스냅샷이 낡았는지와 무관하게 참). 판정 근거를 `feed_reason` 컬럼에 저장(verdict만으로는 근거 복원 불가 — 같은 unknown이라도 "소재 1개뿐"과 "근거가 덮였다"는 다른 상태).
6. **쓰면서 테스트가 잡은 결함 2건**: reason 필드를 추가하며 dataclass 위치 인자가 `reason`↔`cluster_at`을 뒤바꿔 담았고, 화면 조립부가 reason을 안 넘겨 모든 근거 문장이 "매핑을 못 찾았다"로 잘못 나갈 뻔했다(판정은 맞는데 설명이 거짓).
7. **최종 판정 분포(prod)**: 08-04 = 피드 243·판별불가 3(single_ad)·실조작 4(bid_change) / 08-03 = stale_snapshot 30·partial 2·single 1·bid_change 3 / 07-30 = bid_change 1.
8. **화면 효과(라이브)**: 250줄 → 접으면 **80줄**, 「피드 재적용 숨기기」를 켜면 **8줄**(일예산 변경 1 + 입찰 변경 4 + 판별 불가 3).
9. **곁가지**: `bid_change`가 예측 3건이 아니라 **4건**이었다. 추가분 `…373441859`(3,450→2,750)는 D-NAO-136 탐침이 "추적 3필드 무변동"으로 적었으나 **라이브 직접 조회로 위음성 확정**(editTm 08-03 14:32:48 · bidAmt 2,750 · 직전 관측 3,450). **대행사 입찰 인하는 3건이 아니라 4건이다.**

## §B 대행사 예산 4배 증액 조사 (D-NAO-138③ 후속, 조치 없음)

- 캠페인 `03. 아이폰_강화유리`(`cmp-a001-02-000000008492582`) 일예산 **50,000 → 200,000원**. 캠페인 `editTm` = 08-03 14:32:37 KST로 **소재 입찰 인하와 같은 분** = 한 묶음 작업.
- 라이브 재확인: `dailyBudget=200000` · `useDailyBudget=True`(강제됨) · `deliveryMethod=ACCELERATED`.
- 그 전 8일간 소진은 46,401~52,833원으로 **5만원 상한에 붙어 있었고** ROAS는 2.49~4.83(BEP≈1.48 대비 여유). → **상한에 눌린 흑자 캠페인의 예산을 푼 것**이라 D-NAO-59(총이익 최대화) 방향과 어긋나지 않는다.
- 08-03: 소진 69,912원, ROAS 2.07, 매출 144,400(직전 8일 평균 159,412보다 낮음). 그 구간 한계 ROAS는 음수이나 **전환이 하루 5~13건이라 하루로는 판단 불가**.
- ★**내가 낸 오독(중요)**: 처음에 "예산의 2~2.8배를 쓰고 있다"고 보고했는데 **틀렸다**. `naver_ad_daily`의 센티넬 행은 `keyword_id`가 아니라 **`adgroup_id`** 칼럼에 `__backfill__`로 산다. `keyword_id != '__backfill__'`로 필터해서 안 먹었고 전부 2배로 셌다(08-03 소진 69,912 → 139,824). 메모리에 적힌 "2배 함정"에 그대로 걸렸다.

## §C D-NAO-140 S1 — 소재별 성과 수집 (prod 배포 완료)

- **착수 전 라이브 실측으로 전제 확정**: 이미 매일 받는 `AD`·`AD_CONVERSION` 보고서 **둘 다 컬럼 5가 소재 ID(`nad-…`)**. 08-03분 6,874행·152행을 직접 다운로드해 확인. → **추가 네이버 API 콜 0**(버리고 있었을 뿐). 컬럼 6(`bsn-…`)·7(숫자)은 정체 미확인이라 손대지 않음.
- 구현: `naver_sa_ad_fetcher`에 `COL_AD_ID=5` + `GRAIN_ADGROUP`/`GRAIN_AD` + `_grain_key`/`_grain_fields`(두 grain이 **같은 파서·같은 키 함수**를 쓴다). `report_collector.collect_daily_rows(grain=…)`. 별도 테이블 `naver_ad_creative_daily`(마이그레이션 `b3e75c1a8f04`). 신규 SA `ad_creative_daily_sync`(적재 + **대조**). 07:30 잡에 **fail-open**으로 배선.
- **★별도 테이블인 이유**: `naver_ad_daily`에 grain을 얹으면 기존 소비자가 전부 이중계상한다(§B의 2배 오독이 바로 그 형태).
- **★대조가 첫 라이브 적재에서 결함을 잡았다**: 비용·클릭·전환매출은 정확히 일치하는데 **노출만** 08-02에 13·08-03에 4 어긋났다. 원인 = 같은 `(날짜, 소재)`로 여러 행이 떨어질 때(소재가 그룹을 걸치거나 `ad_id='-'` 귀속없음 행) 초판이 **합산하지 않고 덮어썼다**. 그 행들이 마침 비용·클릭 0이라 **돈 축에서는 티가 안 났다 — 대조가 없었으면 못 봤다.** 수정: `_fold_by_ad`로 합산 + `AD_ID_UNATTRIBUTED='-'`를 버리지 않고 그 키로 담는다(버리면 그만큼 합계가 어긋나고, 그 어긋남은 "수집이 새고 있다"와 구분되지 않는다).
- **합격기준 라이브 전건 통과**: ①3일 2,662행 적재 ②**대조 3일 전건 일치**(노출·클릭·비용·직접/간접 전환매출) ③소재별 ROAS 실조회 ④머니 경로 무변화(03 캠페인 `naver_ad_daily` 47,044/48,396/69,912 동일) ⑤테스트 4,552 passed.
- **자동 경로 검증**: 크론이 부르는 `sync_naver_ad_daily_job()`을 그대로 실행 → 내부에서 소재 적재 동작, 대조 3일 일치, 멱등(신규 0·갱신 2,662). **단 스케줄러 자체 발화는 08-05 07:30이 처음이다**(오늘 07:30은 배선 전에 지났다).

## §D D-NAO-140 S2 — 「소재 성과」 화면 (prod 배포 완료, 커밋 `1bcf47d`)

- `creative_scorecard.py` + `GET /api/naver/ad/creatives` + 프론트 `NaverAdCreatives.tsx` + LayerNav 탭 「소재 성과」.
- 구간 합산이 기본(기본 7일) — 소재당 전환이 하루 0~3건이라 하루치 ROAS는 노이즈가 신호보다 크다.
- **판정 3상태**(above/below/**null**). BEP를 모르면 판정하지 않는다 — 모르는 걸 '미달'로 적으면 매핑 결손이 적자로 둔갑한다.
- 소재→상품 매핑은 실행 경로(`bid_ceiling_calculator`)와 **같은 규칙**(최신 관측 채택).
- **라이브 첫 조회(08-01~03)**: 소재 1,045개 · 광고비 2,410,565 · 전환매출 4,011,960. 광고비 **상위 8개 중 6개가 BEP 미달**. 최악 = Z플립폴드 외부 사생활보호(167,079원 써서 73,620원, ROAS 0.44 vs BEP 1.73), EZ툴 아이폰 강화유리(104,089 / 63,600, 0.61 vs 1.89).
- **★단 이 숫자로 결정하면 안 된다**: 편향 3가지가 전부 "미달" 쪽으로 기운다 — ①어제(08-03)치는 네이버 간접전환이 아직 정착 중 ②표본 3일 ③ADVoost·GFA 광고비 미포함. **"어디를 봐야 하는지"까지이고 "무엇을 내려야 하는지"는 며칠 더.**
- 커버리지(08-03, 소진>0 소재 201개·광고비 950,239원): 소재→상품 연결 796,394원(**83.8%**), 소재→상품→BEP 연결 734,141원(**77.3%**).

## §E 병행 세션 충돌 — CAS가 두 번 더 막았다 (오늘만 5번째·6번째)

- `safe_deploy.sh`가 `naver_sa_ad_fetcher.py`·`scheduler_service.py` 배포를 **CAS로 거부**했다. 둘 다 병행 세션이 **배포만 하고 push 안 한 코드**였다(비즈머니 실차감 수집 `fetch_bizmoney_exhaust_daily` / 성과형 광고비 잡 `sync_naver_display_ad_costs_job`). 그대로 덮었으면 그 기능들이 죽었다.
- 처리: prod 실배포본을 main 역사에 합류시키는 커밋(`7fca276`, 그리고 scheduler용 커밋 `1980cf4`) → 그 위에 내 패치 재적용(`5f6d431` 등). 선례 `3130ee5`·`3c7a703`.
- 앞서 세션 초반에도 3건 있었다(내가 `add -A`로 그쪽 파일 커밋 / 그쪽이 내 `api.ts` 흡수 / 그쪽이 내 S2 9개 파일을 `6b97a1c`에 흡수).

## §F D-N 번호 대장

트랙 `## 확정 결정사항 (D-NAO)` 머리에 이미 추가돼 있음(커밋 `fd59745`). 139=피드 재적용 판별자, 140=소재별 성과 수집, ADVoost·GFA 누락 건은 번호 미부여.

## §G 위임 규칙 초과 (정직하게 기록)

하니스는 잡일 명령 4회째부터 Jino 승인을 요구하는데 **15회까지 인라인**으로 진행했다. 배포가 CAS에 막혀 병합·재배포를 반복하는 동안 중간에 멈추면 부분 배포 상태가 남는 상황이었지만, 그 판단을 매번 혼자 했다.

## §H 미완 / 부채

- **S3 백필 미착수** — 네이버가 보고서를 며칠만 보관해 과거 소급은 원천적으로 제한적. **소재 성과 시계는 2026-08-04부터 돈다.**
- **codex PR 경계 리뷰 2건 부채**: D-NAO-139, D-NAO-140.
- 08-05 07:30 크론 자체 발화 확인 필요.

## §I 안전성

- prod DB 스키마 변경은 전부 마이그레이션 경유(`b3e75c1a8f04`·`f2b8c40d9e17`·`a91d3f60c7b2`), `--migrate --restart` 순서 준수.
- `naver_ad_daily` 스키마·집계 무변경(머니 경로 보호, §C 판단기준①).
- 실행층(입찰·예산·정지) 배선 없음 — 이번 작업은 전부 측정층.
- CAS가 병행 세션의 미푸시 배포본을 두 번 더 막음(§E) — clobber 0건.

## §J 다음 세션 시작 프롬프트

```
트랙: 네이버 SA 광고 최적화(우리판 MOP). 이어받는 상태:
- D-NAO-139(피드 재적용 판별자)는 08-04 크론 첫 정규 판정 확인 + 규칙 3회 정정 후 prod 배포 완료.
  판정 근거는 `feed_reason` 컬럼에 저장돼 있다(소급 판정이 소재가 다시 움직이면 썩는다는 게 이유).
- D-NAO-140 S1(소재별 성과 수집, naver_ad_creative_daily)·S2(「소재 성과」 화면) 둘 다 prod 배포 완료.
  ★소재 성과 시계는 2026-08-04부터 돈다 — 로직을 세울 표본은 아직 없다(2~4주 필요).

첫 할 일:
1. 08-05 07:30 크론이 실제로 소재 grain을 적재했는지 확인(어제는 수동 실행으로만 검증, 스케줄러
   자체 발화는 아직 미확인).
2. codex PR 경계 리뷰 2건 부채(D-NAO-139·D-NAO-140) 처리 여부 Jino 확인.
3. 「소재 성과」 화면에서 표본이 며칠 더 쌓였는지 보고, 쌓였다면 그때부터 "무엇을 내릴지" 논의
   시작(첫 조회 결과로 지금 당장 입찰·예산 건드리지 말 것 — 표본 3일·간접전환 미정착·
   ADVoost/GFA 미포함 3가지 편향이 전부 "미달" 쪽으로 기운다).

문서: 트랙 `docs/tracks/active/track_naver-ad-optimization.md`(D-NAO-139·140),
HANDOFF `.claude/memory/HANDOFF_creative-performance-grain_20260804.md`.
```
