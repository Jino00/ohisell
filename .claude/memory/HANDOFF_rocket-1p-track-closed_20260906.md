# HANDOFF — 로켓배송(1P) 트랙 종결 12/12 (체인 `1p-계산서` n=6)

- 세션: `e3a9888e` · 2026-09-06 17:28 ~ 19:1x KST (같은 세션의 두 번째 슬라이스, n=5는 16:35 종결)
- 발단(Jino 원문 17:26): **"이제 complete/ 하자"**
- 트랙: `docs/tracks/completed/track_coupang-rocket-1p.md` (**10/12 → 12/12 · completed/ 이동 완료**)

## 검증 명령: `ssh sellc.ohitech.co.kr "curl -s localhost:8001/api/overview/rocket-overview | python3 -c \"import json,sys;r=json.load(sys.stdin);c=r['cost_coverage'];print(c['coverage_pct'], c['unmapped_sku_count'], r['has_cost'])\""`
기대: `0.9982 2 True` — 화면은 이 값을 **99.8% ⚠ 부분반영**으로 그린다(100%가 아니다).

## §0 이 슬라이스가 남긴 한 문장
**「체크가 비어 있다」와 「안 됐다」는 다르고, 「번들에 문구가 있다」와 「눌러 보면 된다」도 다르다.**
이 트랙은 그 두 착각을 연달아 겪었다.

## §1 S5는 이미 돼 있었다 — 아홉 세션이 틀렸다
역대 확인줄이 **다섯 번** *「다음 트랙 후보 = 여전히 S5」*라고 적었지만 **그중 누구도 잰 적이 없다.**
별도 기(트랙 종결 QA·읽기 전용) 실측: 헤더 4항목 전부가 코드·prod 번들·라이브 API 3층 일치.
구현 시점 **2026-06-20 `bbff6f6c`** ⇒ `[x]`는 새 작업이 아니라 **정정**. 커밋 `fc0a9de9`.

## §2 그런데 그 S5가 «눌러 보면» 깨져 있었다
트랙 S6이 요구하는 **적대 리뷰를 처음 돌렸고 1R FAIL(P1=3)**.
도입 커밋이 스스로 *"이미 prod에 배포된 상태라 본 커밋은 배포된 현실을 기록한다"*고 적어
**리뷰 없이 라이브였음을 자백**해 뒀다. 전용 테스트 0건.

| P1 | 증상 | 기간 |
|---|---|---|
| 1 | 커버리지 **99.82%**가 「**100%**」 초록 — `Math.round(0.9982*100)=100` | 2개월 반 |
| 2 | 「원가 매핑 관리」 클릭 시 **페이지 백지** — 서버는 첫 커밋부터 dict, 프론트 타입은 배열 | **도입 이래** |
| 3 | 확정/제외/삭제 뒤 원가·순이익·커버리지 **미재조회** | 도입 이래 |

★P1-2의 실무 무게: 확정·제외·삭제 버튼이 **전부 그 표 안**이라 **사람이 원가를 매핑할 경로가 통째로 없었다.**
현재 확정 219건은 전부 배치가 붙인 것(`match_method:"suggested"`, `note:"auto score=0.70"`)이고
미매핑은 **105건** 남아 있다 — 이제 화면에서 처리 가능하다.

★P1-1의 자기모순: 같은 배지가 본문엔 「미매핑 20,160원 · SKU 2」라 적으면서 제목은 「100% 완전」이었다.

## §3 ★리뷰의 진짜 산출은 P1이 아니었다
표면 절단 변이 3종(배지 렌더 제거 · `<RocketView/>` 미렌더 · 순이익 value 비우기) **전부 SURVIVED**,
그때 **1,489개 테스트가 전건 초록**이었다. 🚀 탭을 «누르는» 테스트가 0건이었기 때문이다.
`staleSyncSelection.test.tsx`가 로켓 API 4개를 **영원히 pending인 promise**로 목킹해 둬서
P1-2의 모양 불일치가 구조적으로 안 보이게 돼 있었다.

⇒ `frontend/src/pages/rocketAxisReachesTheUser.test.tsx` **14종 신설**(단언은 전부 화면 텍스트).

## §4 2R는 PASS였지만 내 보강이 부족했다
리뷰어 원문: *「배지 쪽은 색·안내 문구까지 물어서 잘 닫혔지만, 같은 「모름」 상태에서
**순이익 카드가 완전함을 단정해도 안 잡힌다**. 이건 P1-1의 원형이 배지가 아니라
카드 표면에 그대로 남아 있는 자리다.」*
삭제·제외 경로는 **한 번도 눌러 보지 않아** 콜백 제거 변이가 생존했다(코드는 맞았다).
⇒ 카드 sub 단언 + 세 경로 전부 클릭 + 삭제 확인 취소까지 추가. 커밋 `779f67dd`.

★**이 화면의 병은 일관되게 「나쁠수록 조용해진다」**였다 — 덜 덮일수록 반올림이 100%로 만들고,
원가가 하나도 없을 때(최악) 배지가 아예 사라지고, 모를 때 순이익을 완전하다 단정하고,
되돌릴 수 없는 삭제만 확인을 안 물었다. 배지·숫자 카드·되돌릴 수 없는 버튼 **세 표면**에 각각 있었다.

## §5 착지 (완주)
- PR **#759** 머지 `a31f848e` — `safe_merge.sh` 경유, CI **3/3 실통과**, `--force` 미사용
- 프론트 배포 — **CAS 통과**, `--force-frontend` 미사용 · 스탬프 `a31f848e` · 번들 `assets/index-DTisrEiL.js`
- 백엔드 변경 **0** · 마이그 **0**
- 회귀: 프론트 **1,503 passed**(103 파일) · `tsc --noEmit` 0 error
- 라이브 확증(19:0x KST): `coverage_pct="0.9982"` · 미매핑 총수 **105**, `items` 리스트 정상 파싱 ·
  번들에서 옛 판정 패턴 `<100?` **0회** · 신규 문구 10종 전건 존재

## §6 다음 세션
- 이 트랙은 **닫혔다.** 재개는 Jino 발의로만.
- 미매핑 **105건**을 화면에서 처리하면 커버리지가 올라간다(이제 실제로 눌린다).
- ⚠️같은 날 n=5가 남긴 이월: P2-8 — `poSeqCopyReachesTheUser`·`riConfirmReachesTheUser` 픽스처가
  `has_invoice`·`invoice_seqs`를 안 담아 **부재에 기대어 초록**이다.
- ⚠️2R가 기각 아닌 «참고»로 남긴 것: 🚀 축 렌더가 무관한 `fetchCommandCenter`에 매여 있다(P2-9) ·
  조회 실패를 「데이터 없음」으로 위장(P2-1) · pct 규약 둘이 한 화면에 섞임(P2-5).

## §7 이월 (하네스 규율 — 주기 구조 감사)
- `chain.sh reclaim`이 **닫힌 자기 행도 재개를 거부**한다(「이미 닫혀 있다 — 소급 수정 금지」).
  n=3·n=4가 쓰던 `end_kst` 되돌리기는 이제 막힌다 ⇒ **같은 세션의 새 슬라이스는 새 행(n+1)으로 append**했다.
- `chain.sh`는 **주 워크트리 루트**(iCloud 공유 폴더, 2026-08-23 체크아웃)의 등록부에 쓴다 —
  거기 `1p-계산서.jsonl`이 없어 첫 호출이 **n=1 유령 등록부**를 만들었다. **`--dir "$PWD/.claude/memory/chains"` 필수.**
- iCloud 공유 폴더에서 `git worktree add`가 `fatal: mmap failed`(3회 중 2회 실패).
- `review-surface-mutation.sh`가 **완료 QA 위임에 오발화** — 2026-08-28 [5a99b32c]에 이어 **재현 2회째**.
