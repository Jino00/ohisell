# 세션 인수인계: 계정 전체 학습 방향 확정 + EXPKEYWORD 근본수정 + BM 벤치마크 레이어 승인

> 저장 2026-07-22 07:25 KST · 워크트리 `session-dfd814` · 브랜치 `claude/bx-exploration-ad-bid-400-fix-fb114e`
> 앞 HANDOFF `HANDOFF_ad-bid-400-fixed+backfill-1y+SS-live_20260722.md`를 잇는다(그 세션의 소재입찰 400 수정·1년 백필·SS 스프린트는 전부 완료·PR #76·77 병합됨). **이 파일은 그 이후 재개 세션의 새 방향을 담는다.**

## 0. 병렬 세션 충돌 — 해소됨 (기록만)
- task_ccd79ae0("Fix EXPKEYWORD column-parsing bug")를 Jino가 별도 세션에서 시작했으나 **07:31 삭제됨** — 이 세션이 이미 그 버그를 고쳐 배포(`278e5e1`)했으므로 중복. **충돌 위험 없음**(그 세션 폐기됨). EXPKEYWORD 수정은 이 세션 것이 유일·정본.

## 1. 환경
- prod: `sellc.ohitech.co.kr` · `/home/ubuntu/ohisell/backend`(pm2 `ohisell-backend`·uvicorn:8001·venv `.venv/bin/python`) · DB `ohisell.db`. prod는 **UTC**(로그 시각 +9=KST).
- 배포: `scripts/safe_deploy.sh`만(CAS·직접 scp 금지 D-NAO-49). prod 마이그 head=`f2a3b4c5d6e7`.
- **git 상태**: `278e5e1`이 origin/main보다 1커밋 앞섬(EXPKEYWORD 수정 — 배포는 됐으나 **PR 미생성=main!=prod**). 다음 세션이 PR로 복원.
- 조회 관례: naver_search_term_daily 전환 컬럼은 source='shopping'만·파워링크는 전환 귀속 불가. naver_ad_daily 집계는 `__backfill__`·'' sentinel 제외(2배 함정).

## 2. 이번(재개) 세션 완료
- ✅ **EXPKEYWORD(파워링크 확장검색어) 수집기 근본수정·배포**(`278e5e1`): 구축(~07-07) 이후 **0행 전량 드롭 버그** 발견 — 16컬럼(SHOPPINGKEYWORD_DETAIL)과 같은 `ST_COL_*`로 파싱해 길이가드(`≤14`)가 12컬럼 EXPKEYWORD 전 행 스킵. `EXP_COL_*` 12컬럼 전용 상수 분기(imp/clk/cost=8/9/10, AD 합계 96.7~98.8% 대조·rank_sum 없음=0). 2758 passed. failure-memory 대상(미기록 — 다음 세션 기록 권장).
- ✅ **파워링크 검색어 1년 백필 완주·검증 완료**(`/tmp/backfill_expkeyword.py`): 07:41 완료 total=**248만 행**·338일(2025-07-23~2026-07-21, 저활동일 일부 skip). **대조 검증**: 최대 캠페인(갤럭시 파워링크 cmp-a001-01-000000010206612) 07-19 클릭 61=61·비용 78,529=78,529원 **1원도 안 틀림=컬럼 매핑 확정**. 집계 총합은 expkeyword(클릭 143)가 naver_ad_daily WEB_SITE(96)보다 높은데, 최대 캠페인이 정확 일치하므로 이는 **expkeyword가 일부 소액 캠페인을 더 완전히 잡는 것**(naver_ad_daily 파워링크 커버리지 구멍을 메움 — 이 수집기 수정의 실가치). 정확 기전(확장검색 vs 수집 신선도)은 필요시 다음 세션 조사.
- ✅ **ref36 파워링크 보고서 전수 실측**(`docs/references/36_powerlink_report_data_20260722.md`): 9종 보고서 컬럼 실측·`/stats` 필드 가용성·엔티티 필드·보존 1년·수집 갭 표. **미수집 데이터 발견**: `ctr·cpc·crto·viewCnt`(valid인데 미사용)·ADEXTENSION(_CONVERSION 확장소재 성과/전환)·CRITERION(_CONVERSION, 연령AG####·성별GNM/GNF/GNU 패턴 추정)·`/ncc/ad-extensions` 엔드포인트·그룹의 useExpSearch/expSearchBudgetRatio/aiAdsOptIn 설정.

## 3. 확정 결정 (트랙 D-N, 번복 금지)
- **★D-NAO-78 (2026-07-22 Jino 승인)**: **학습 범위 = 계정 전체(대행사 캠페인 포함), 우리 4개만이 아님.** Jino 원문(트랙 파일에 보존): "너가 보는 데이터는 우리가 운영하는 캠페인이 아니고 전체의 광고를 보고 학습해. 대행사가 전체 캠페인을 어떻게 셋팅했는지 구조·성과·매일 변화작업을 파악해서 우리 캠페인 성과에도 도움… 우리가 돌리는 MOP는 성과를 정확히 모르니 저성과 캠페인만 돌리고 있는 거거든." **실측**: 계정 45캠페인 전량 수집 중(1년 백필 포함)이나 판단·학습 루프는 ours 4개만 소비. 대행사가 볼륨 운영(갤럭시 파워링크 7일 59만원·ROAS 1.76 등). ours 4개는 소액·저성과.
- **BM(벤치마크) 학습 레이어 구조 — Jino 승인(이 세션)**: 아래 §4. **관찰 전용·실쓰기 0**.
- **파워링크 자동 제외 방향 — Jino 지시**: "성과 기반 자동운영이어야지 수백 개 검색어를 하나하나 Confirm 못 한다." → SS3-A를 Confirm-only에서 **성과 기반 자동**으로 전환하되, 전환 귀속 불가(파워링크 구조적)를 **in-out 재심사 루프**로 보완(§5). 계획서 §0 방향고정 4(Confirm)와 상충 → 이 결정이 우선, 단 안전한 자동 규칙 설계 필요.

## 4. BM 벤치마크 레이어 설계 (승인됨 — 다음 세션 구현 대상)
> 원칙18 계층. 관찰 전용(실쓰기 0·리스크 없음). 아침 배치 1회.
```
[BM Harness]
 ├─ SA-1 구조 스냅샷러(매일): 전 45캠페인 구조를 날짜별 보존 —
 │    캠페인→그룹→키워드(수·bidAmt·qiGrade)·소재수·확장검색 on/off·제외키워드 목록.
 │    ※ 현재 "현재 상태"만 있고 역사 없음 → 스냅샷 테이블 신설(신규 마이그).
 ├─ SA-2 조작 감지기(어제 vs 오늘 diff): 대행사가 오늘 바꾼 것 —
 │    입찰 변경·키워드 추가/삭제·제외키워드 등록·소재 교체·예산 변경 → agency_ops_log.
 │    (03 MOP 관찰 감지기 패턴을 전 캠페인으로 일반화)
 ├─ SA-3 성과 대조기: 대행사 캠페인 구조↔성과 상관을 벤치마크化
 │    (고성과 그룹의 키워드 수·입찰 밴드·검색어 구성 vs 우리 캠페인).
 └─ 산출: ①일기 브리핑("대행사 오늘 조작 N건") ②프라이어 테이블(탐색·서보·L2 참조)
          ③★대행사 등록 키워드 셋 = "사람이 검증한 정답지" → SS4 승격 후보와 교차
            (대행사가 이미 키워드로 쓰는 검색어면 승격 확신도↑).
```
- 이미 있는 원료: 45캠페인 성과 1년치(백필)·검색어 계정 전체(쇼핑+파워링크 곧 1년). 새로 만드는 것=구조의 "역사"(스냅샷)와 "변화"(diff)와 배선.
- **★D-NAO-79 (Jino 승인) — BM 계획서에 반드시 명문화**: **주 산출물 = 예외 브리핑, 전체 리포트 = 온디맨드.** 전체 45캠페인 원본은 기계가 먹는 입력이지 사람이 매일 읽는 것 아님. 사람 소비 3단: ①매일=예외 브리핑(SA-2 산출, "오늘 볼 것 N가지"·이상치만) ②주간=벤치마크 요약(SA-3) ③온디맨드=드릴다운. 매일 전체 리포트 열람 습관 안 만듦(바이브 리뷰잉). 초기 2~3주만 Jino 전체 검증→확신 후 예외 전용. 브리핑은 상설 표면화(sellC 배너/아침 푸시 — 계획서에서 위치 결정). BM 계획서 §완료기준에 "예외 브리핑이 주 UX"를 못박을 것.
- 다음 단계: **Opus 계획서 → 구현**(설계=Fable 완료, 계획=Opus, 구현=Sonnet/Opus). 스냅샷 스키마·diff 로직·프라이어 배선 지점(어느 SA가 소비하는지)·**브리핑 표면화 위치(D-NAO-79)** 계획서에서 확정.

## 5. 파워링크 자동 제외 설계 (대기 — EXPKEYWORD 데이터 흐른 후)
- **배경(대행사도 같은 제약)**: 네이버는 파워링크 검색어별 전환을 안 줌(전환 리포트 검색어='-'). 업계 전체가 우회: ①전환 판단은 키워드 단위(네이버가 줌·우리 수집 중) ②검색어는 비용 기반 컷(전환 몰라도 클릭·비용 먹고 기여 정황 없으면 제외, 오컷은 감수) ③좋아 보이면 키워드로 승격→전환 추적 붙음(SS4). 자사몰이면 URL 파라미터+GA로 검색어 전환 추적 가능하나 우리는 스마트스토어라 불가.
- **설계(미착수)**: 파워링크 검색어 자동 제외 규칙 — N일(제안 14일) 누적 클릭·비용 충분 + 성과 없음 + 부모 키워드 순손실(프록시) → 자동 제외. **자동 재심사 루프**(제외 검색어 주기 재노출→성과 나면 자동 복귀, 잘못 자른 것 자가 교정). 봉투(일일캡·킬스위치·전건 기록) 유지. **선행 조건 = EXPKEYWORD 데이터가 실제로 흘러야 함(방금 개통·백필 중)**. 데이터 확인 후 Fable 설계→GATE+codex→배포.
- 쇼핑은 API 제외 불가(400 3728)라 영구 브리핑(변경 없음).

## 6. 미완 / 다음 작업 (우선순위)
- [ ] **EXPKEYWORD 백필 완주 확인**(모니터 be3kj5nm8) + AD 합계 대조 검증 + **PR 생성**(278e5e1, main==prod 복원 — §0 병렬 세션과 조율).
- [ ] **BM 벤치마크 레이어 구현**(§4) — Opus 계획서부터. D-NAO-78 실행층.
- [ ] **파워링크 자동 제외+재심사 루프**(§5) — EXPKEYWORD 데이터 확인 후.
- [ ] **관측(어제 SS 승계)**: 07:40 검색어+전환 크론 자연 수집 / 08:50 SS 레인 첫 자동 크론(승격 상위 20 재생성·쇼핑 브리핑) / 탐색 23:20 상향 10건 순위 반응 / SS 상수 첫 주 캘리브레이션(제외 후보 0 계속 시 clk 문턱 완화).
- [ ] **codex 소급 리뷰 07-23**(IU-R·B-X·SS·EXPKEYWORD 전 커밋 — 한도 회복일).
- [ ] (백로그) ref36 미수집 데이터 활용(확장소재·CRITERION 연령/성별·ctr/cpc/crto)·D-NAO-73 L2 키워드 예산 재분배·D-NAO-74 페이지 경계.
- [ ] failure-memory 기록: EXPKEYWORD 16→12컬럼 0행 드롭 버그(수집기 신설 시 컬럼 수 실측 필수 교훈).

## 7. 주의사항
- **라우팅**(Jino 지시): 구조/설계=Fable · 설계/구현=Opus · 단순=Sonnet · 옵션 추천안 자동. **세션 중 실수 기록**: Fable이 Sonnet 몫(단순 코딩)을 "효율" 명분으로 직접 수행 → Jino "token 물어내" 질책 → `.claude/memory/LESSONS_LEARNED.md`(프로젝트 루트) 기록. 이후 전 작업 위임 준수.
- 원칙22: "됐다"는 라이브 증거로만. EXPKEYWORD 백필도 완주+대조 전까지 "됐다" 금지.
- naver_search_term_daily의 파워링크 전환 컬럼은 여전히 0(구조적 — §5). 판단에 파워링크 전환 게이트 걸지 말 것.

## 8. 핵심 파일
| 파일 | 역할 |
|------|------|
| `docs/references/36_powerlink_report_data_20260722.md` | 파워링크 보고서 전수 실측(미수집 데이터·컬럼 표) |
| `docs/PLAN_naver-ad-searchterm-ss.md` | SS 스프린트(완료)·§난제 5 파워링크 전환 갭 |
| `docs/STRATEGY_naver-ad-v2.md` | 전략 v2(로드맵 SS→L2→L3) |
| `docs/tracks/active/track_naver-ad-optimization.md` | 트랙(D-NAO-77·78) |
| `backend/app/services/naver_sa_ad_fetcher.py` | EXP_COL_*·ST_COL_*·STCONV_COL_*(수정 완료) |
| `backend/app/services/naver_ad/search_term_*.py` | SS 수집·판단·실행·승격 |
| `/tmp/backfill_expkeyword.py`(prod) | 파워링크 검색어 1년 백필 러너 |

## 9. 새 세션 시작 프롬프트
`.claude/worktrees/session-dfd814/.claude/memory/HANDOFF_agency-wide-learning+expkeyword-fix+BM-layer_20260722.md 읽고 이어서. 핵심=D-NAO-78 계정 전체(대행사 포함) 학습 방향 확정·BM 벤치마크 레이어 승인(구조=§4)·EXPKEYWORD 0행 버그 수정 배포(278e5e1)·파워링크 검색어 1년 백필 실행. ⚠️§0 병렬 세션 충돌(task_ccd79ae0=중복) 먼저 확인. 다음=백필 완주 확인+PR→BM 레이어 Opus 계획서→구현. 라우팅 준수(단순=Sonnet).`
