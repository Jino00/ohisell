# HANDOFF — sellc-원가-메뉴 n=33 (세션 c6182398 · 2026-09-04 11:55 ~ 13:4x KST)

> 목표이름: **엑셀정합 목표** (트랙 `docs/tracks/active/track_cost-truth-ledger.md`)
> 목적 원문: n=32 인계 §7의 ⓪·① 두 건을 이어받았다. Jino가 이 세션에서 고른 것 둘 —
> 11:5x *「⓪ prod 배포 먼저」* · 13:2x *「배포한다」*(PR #707).

**검증 명령:**
```
ssh sellc.ohitech.co.kr "curl -s http://127.0.0.1:8011/api/cost/recipes" | python3 -c "import json,sys;d=json.load(sys.stdin,strict=False);b={x['id']:x for x in d['items']};print('r45',len(b[45]['anomaly_flag']),'/ r81',len(b[81]['anomaly_flag']),'/ r40',b[40]['match']['excel_total_inc_vat'],b[40]['picked']['item_total_inc_vat'])"
```
- **현재(둘 다 배포 완료)**: `r45 100 / r81 40 / r40 3102.7 3102.7`
- r45가 **40**이면 ① 미배포, r40 두 값이 **다르면** ⓪ 미배포다.
- ⚠️활성 포트가 배포마다 `:8011 ↔ :8001`로 뒤집힌다 — 현재 **`:8011`**. 둘 다 시도할 것.
- ⚠️원가 API 응답에 제어문자가 있어 `json.loads(..., strict=False)`가 필요하다(n=31·n=32 관측 **재현**).

## 0. 한 줄 — **인계가 「표시 문제」로 넘긴 40자 잘림이, 실제로는 «줄 하나를 인박스에서 지우고» 있었다.**

## 1. ⓪ — n=32가 못 댄 라이브 증거를 댔다

PR #697은 09-03에 머지됐지만 **prod 배포가 0건**이었다(n=32가 Jino 승인 대기 중 종료).
착수 검증 명령이 `매칭근거 12.20 / 고른항목 3102.70`을 내 **미배포를 확증**했고, Jino 승인(11:5x) 후 배포했다.

| | 배포 전 `:8011` | 배포 후 11:57:40 KST `:8001` |
|---|---|---|
| r40 | `12.20 / 3102.70` | **`3102.70 / 3102.70`** |
| r41 | — | **`4882.94 / 4882.94`** |
| r39 | — | **`None / None`** (엑셀 상품원가 null이 참 — 옛 19.30 거짓말 소멸) |

★**값이 도는 층도 함께 실측했다** — 사람이 읽는 `match`만 보면 절반이고, n=32의 M3 변이가 1차에서 살아남은 자리가 바로 거기다.
`/api/cost/board` **924행 중 `|excel_gap_pct| > 100%`인 행 0개**, r40·r41이 **정확히 3.17%**.
n=32가 §4에서 🧠 추론으로 예고한 「+25,000%대 → +3.17%」가 **소수점까지 맞았다**. 보드 최대는 3.81%(r44·r45, 무관한 기존치).

## 2. ① — 인계 숫자 2건이 둘 다 실측에서 커졌다

| 인계의 주장 | 실측 (2026-09-04 12:03 KST) |
|---|---|
| 절단 지점 2곳(`recipes.py:696`·`:1189`) | **3곳** (`:501`·`:719`·`:1212` — 줄번호는 n=32 수정으로 이동) |
| 이미 잘린 행 「45·97」 | **3건** — r45 · **r81** · r97 |

★그리고 셋의 성격이 다르다. 인박스 묶음은 `costHome.ts:118`의 `anomalyKinds()`가 `,`로 갈라 `:` 앞을 토큰으로 읽어 정한다:

| 행 | 저장된 kinds | 결과 |
|---|---|---|
| r45·r97 | `['price_conflict', 'price_co']` | 첫 토큰이 살아 「모순」 묶음엔 **선다** — 표시만 깨짐 |
| **r81** | `['landed_only_vat_unknown', 'needs_manual_lin']` | 원본은 `…,needs_manual_lines`. 「구성 없음」 규칙(`no_recipe_match`\|`needs_manual_lines`)에 **안 맞고** `price_conflict`도 아니다 ⇒ **어느 묶음에도 안 서서 화면에서 사라졌다** |

⇒ 40자 절단은 표시를 흐린 게 아니라 **판정을 바꿨다.** 저장소가 반복해 밟는 「라벨은 붙는데 집행이 안 됨」의 또 한 사례다.

## 3. 수리 — 불변식을 바꿨다

- 컬럼 폭을 원천 `cost_table_item.anomalies`와 **같게 200**으로(마이그 `anomw1s2a`, `down_revision: grainv1s1a` = prod 당시 head).
- **`fit_anomalies(parts, *, width)` 신설** — 폭을 아는 **유일한 자리**. 넘치면 **토큰을 통째로 뺀다**.
  ★불변식은 「폭에 맞춘다」가 아니라 **「반토막을 만들지 않는다」**다. 문자 절단이면 40에서 나던 병이 **200에서 그대로 재발한다** —
  실측: 12토큰 399자를 `[:200]`하면 마지막 조각이 `'pr'`이다.
- 값이 컬럼에 닿는 **네 자리 전부**가 그 게이트를 지난다(폭은 모델에서 읽어 상수 중복 없음).
- 마이그레이션은 **복원 가능한 행만** 되살린다 — 판별 근거는 「저장값이 픽된 항목 `anomalies`의 **접두사**」.
  ⚠️**r81은 복원하지 않는다** — `picked_item_id`가 NULL이라 원본이 DB에 없다. **회복 경로는 원가표 재업로드**(Jino 소관 파일)이고, 지어내지 않았다.

## 4. ★적대 리뷰 1R FAIL(P1 1) → 2R PASS — P1이 내 사각지대였다

`[:200]`을 남긴 근거로 **「컬럼 폭과 같아서 손실이 아니라 맞춤」**을 들었는데, 그 판단을 **세 자리 중 한 자리에만** 적용했다.
나머지 둘(`import_drafts`·`_reimport_composition_only`)은 상한을 200으로 올린 게 아니라 **지웠다**.
★그 둘은 200에서 잘린 `cost_table_item.anomalies`가 아니라 **잘리기 전 파스 결과** `match.draft.anomalies`를 읽는다 —
즉 「현재 최대 100자」라는 내 실측 논거가 **그 자리엔 애초에 적용되지 않았다.**

리뷰어 재현: **220자가 `String(200)` 칸에 조용히 들어갔다.** 조용한 이유 둘 — SQLite가 길이를 안 재고, 내 새 가드 셋이 전부 못 본다
(슬라이스가 **없으므로** AST 가드 대상 아님 · `dst(200) >= src(200)`이라 폭 가드 통과). Postgres였다면 업로드 트랜잭션이 통째로 죽는다.

2R 재현: 같은 입력이 `220 → 190자`, 6토큰 **전부 온전**, 넘친 토큰은 **통째로 탈락**. 게이트 우회 경로 backend 전수 재확인 **0건**.

**리뷰어 P2 7건 중 6건 채택**:
- **P2-1** AST 가드를 「이름에 anomal이 든 슬라이스」 → **「게이트를 거쳤는가 + 대입값에 슬라이스가 있는가」**로 재작성(이름 우회 봉쇄)
- **P2-2** `test_migration_anomaly_flag_width.py` 신설 — 복원 SQL·접두사 가드는 **load-bearing인데 커버리지 0**이었다(둘 다 지워도 전건 초록이었고, 가드 삭제는 무관한 행 2개를 덮었다)
- **P2-3** `AND anomaly_flag <> ''` (`substr(x,1,0)=''`는 **항상 참**)
- **P2-4·P2-5** 내 변경이 거짓으로 만든 프론트 주석 정정 + 날문자 span에 `truncate`·`title`
- **P2-6** 픽 테스트를 **HTTP body 단언**으로 — 표면 절단 변이가 서비스층 dict만 보면 원리적으로 안 잡힌다
- **P2-7 판정불능 이월** — 프론트 vitest 레인 미실행(클론 `node_modules` 0건). `npm ci && npm run build`는 exit 0.

**변이 12종 전건 사망** — 1R 생존 3종(이름으로 AST 우회 · 복원 SQL 제거 · 접두사 가드 제거)과 ★**표면 절단**(응답 직렬화 제거) 포함.

## 5. 라이브 (2026-09-04 13:34:42 KST · `:8011`)

- 선언 `VARCHAR(40)` → **`VARCHAR(200)`** · 리비전 **`anomw1s2a`**
- **r45·r97: 40자 → 100자**, kinds `['price_conflict','price_co']` → **`['price_conflict']`** (유령 조각 소멸)
- **r81: 무접촉** (설계대로)
- 200자 초과 **0건** · **반토막 토큰 전수 3건 → 1건**(r81뿐)
- `batch_alter_table` 재생성이 아무것도 안 잃었다: `foreign_key_check(cost_recipe)` **빈 결과** · 자식 3테이블 343/38/924행 · 인덱스 3종 · 레시피 113행
- 배포는 `--migrate` 순서 강제(①마이그 → ②원격 `alembic upgrade` → ③코드 → ④재시작) · 무중단 `:8001 → :8011` · 다운타임 0초

## 6. ⚠️ 다음 세션이 먼저 볼 것

1. ★**「모순 2건」 처분 — Jino 미답**(n=32에서 그대로 이월). ⓐ원가표 파일을 55/15/320으로 / ⓑ판정을 DB에 기록 / ⓒ문구만.
   ★단, **인박스가 말하는 문구는 이제 온전하다** — r45·r97이 100자 전문을 든다. 「끝난 판정을 다시 묻는다」는 문제는 그대로다.
2. ★**환산율 231.20 vs 238.52 (1.0317) — Jino 미답**(n=32 §4에서 이월).
3. ⚠️**r81은 원가표 재업로드 전엔 인박스에서 안 보인다.** DB만으로 복원 불가. 재업로드하면 `fit_anomalies`가 온전한 값을 넣는다.
4. ⚠️**AST 가드에 더 깊은 우회가 남아 있다** — 2R 리뷰어가 새로 만든 변이: `parts = match.draft.anomalies` 후 `anomaly = ",".join(parts)`
   (대입식에 슬라이스도 `.anomalies` 문자열도 없어 🟢 생존). P1이 아니고 이번 수정이 만든 회귀도 아니다. 근본은 데이터 흐름 추적이라 텍스트 검사로는 한계다.
5. ⚠️`anomalies` 자체의 폭 200도 **같은 모양이 한 층 위**에 있다 — 다만 이제 `fit_anomalies`를 지나므로 **반토막은 안 난다**(토큰 탈락만).
6. ⚠️**프론트 미배포 유지** — CAS 거부 중(다른 세션 미푸시 커밋에서 배포됨). 이번 프론트 수정 2건(주석·`truncate`+`title`)은 **prod에 안 갔다.**
7. ⚠️**업로드 가드는 여전히 08-07 스냅샷**(n=31부터 이월 그대로).
8. ⚠️**`[중복]` 2건**(OHI-0901 332,000원 · OHI-0905 222,600원) — n=28부터 **여섯 세션째** 미처분.

## 7. 하네스 관측

- ⚠️★**새 함정 — iCloud 공유 폴더에서 워크트리를 못 만든다.** `git worktree add`가 `fatal: mmap failed: Operation canceled`로 **3회 연속** 실패했다
  (`brctl download .git`으로 팩 전량 materialize 후에도 실패, 89%에서 끊김). 우회 = **GitHub에서 로컬 디스크로 전체 클론** `/Users/jino/ohisell-n33`.
  ★그 클론에서는 pre-commit 훅이 «공유 메인 체크아웃»으로 **오판**한다(판별자가 `git-dir ≠ git-common-dir`인데 클론은 둘이 같다)
  ⇒ 브랜치 커밋을 거부한다. **그 클론에서만** `core.hooksPath`를 unset했다(공유 폴더 훅은 그대로).
  ★그리고 클론엔 `node_modules`가 없다 — 프론트 검증하려면 `npm ci`가 먼저다.
- ⚠️**트랙 훅 오탐 4회**(n=31·n=32에 이은 **재현**). 숫자로 갈렸다: 공유 폴더 `HEAD 211decbc`가 확인줄 **6개**·체크 **2개**(2/6),
  `origin/main`은 확인줄 **115개**·체크 **3개**(3/6)이고 내 `[c6182398]` 줄이 들어 있다. **공유 폴더에 대신 쓰지 않았다** — 남의 커밋에 쓸려 들어간다.
- ⚠️`completion-qa.sh:107`이 `합격:`을 **같은 줄만** 읽어(`grep -m1 | sed`) 체크박스형 앵커에서는 되돌림 메시지의 「합격:」이 **빈칸으로 출력**된다.
  발동 조건(`grep -q '^합격:'`)은 정상이라 **표시 결함**이지 기능 결함은 아니다. (n=32는 반대 문제를 겪었다 — `합격:`이 산문이라 체크박스 N/M을 못 냈다.)
- ⚠️prod DB에 `PRAGMA integrity_check`를 걸면 **타임아웃**난다(147MB+). `foreign_key_check(<테이블>)`로 좁혀 쓸 것.
- prod에 `alembic`이 PATH에 없다 — `cd /home/ubuntu/ohisell/backend && .venv/bin/alembic current`.

## 8. 완료 QA — **종합 부분달성** (별도 Sonnet 기 · 읽기 전용 · 2026-09-04 13:4x KST)

대조 대상별 판정 **원문 그대로**:

```
판정(앵커 합격기준): 달성 — 체크박스 8/8 라이브 재현 성공(⑤는 문서 근거·나머지는 직접 관측). (2026-09-04 13:4x KST)
판정(트랙 합격기준): 부분달성 — B1·B2·A1 기존 [x] 그대로, A2·C1·C2는 이번 세션이 명시적으로 「안 함」으로 배제한 범위라 3/6 불변. 이 슬라이스가 6항목 중 새로 [x]로 만든 것은 0개다. (2026-09-04 13:4x KST)
판정(Jino 지시 원문): 달성 — ⓪(11:5x prod 배포 먼저)는 PR #705로 실행·라이브 확증, ①(13:2x PR #707 배포)는 마이그레이션 anomw1s2a가 prod head로 서고 코드 동작이 문서 그대로 재현됨. (2026-09-04 13:4x KST)
```

**종합 판정: 부분달성** — QA 원문:
> 이번 세션이 스스로 정의한 슬라이스(앵커 8항목 + Jino의 두 지시)는 라이브로 완전히 재현됐다. 다만 트랙 정본 합격기준 6항목 기준으로는 이번 세션이 A2·C1·C2 어느 것도 전진시키지 않아(명시적 범위 제외) 3/6에서 멈춰 있다 — 트랙 궁극 목표에는 닿지 않았다.

★**QA가 스스로 꼬리표를 단 것**(그대로 옮긴다):
> [①-5] … 내가 직접 재실행한 것은 회귀·AST 가드 테스트뿐이고 적대 리뷰 자체를 독립 재현하진 않음(📄 문서 기반)
> 판정: 달성 — 단, P1=0은 병합된 리뷰 기록에 근거(📄), 내 독립 재현 아님

★**QA가 독립으로 확인해 준 것 셋**(내 보고와 별개로 잰 것):
- r45·r97의 100자가 `cost_table_item.id=35/37`의 `anomalies`와 **완전 일치**(접두사 성립을 원천 대조로 확인)
- prod 백엔드(:8011) pm2 uptime = **2026-09-04 13:32:07 KST** ⇒ PR #707 배포가 세션 내 실제로 일어났고 **stale 증거가 아니다**
- 백엔드 전건 **7,763 passed**(QA 자신이 13:42에 재실행 — 내 7,749보다 크다. QA가 `docs/cost-n33-close` 병합 전 main 기준으로 돌렸고 그 사이 다른 커밋이 들어온 것으로 보인다 🧠)

★**QA가 [①-6]을 어떻게 갈랐나** — 앵커 마지막 합격항목은 조건부(「r81이 안 잡히는 상태가 아니게 된다 **또는** 못 된다는 사실을 근거와 함께 적는다」)였다.
QA 판정: **뒤 분기 성립** — r81은 여전히 어디에도 안 잡히지만 「`picked_item_id` NULL이라 원본이 DB에 없어 복원 불가, 회복 경로는 재업로드뿐」이 PR 본문·커밋에 근거와 함께 명시돼 있다.

## 9. 트랙 진행률

**시작 3/6 → 종료 3/6 (불변)** — 달성 B1·B2·A1 / 미달 A2·C1·C2.
이번 두 슬라이스는 **A2가 요구하는 플립/폴드·태블릿 구성 이식이 아니다**(⓪ 대조값 표시 수리의 라이브 확정 · ① 이상 문구 절단 수리).
`cost_drift`도 여전히 `null`.
