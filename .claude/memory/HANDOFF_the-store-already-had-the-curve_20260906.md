# HANDOFF — 곡선은 이미 적재돼 있었다 · 「없다」던 것이 「안 찾았다」였다 (2026-09-06)

> 체인 **`pao-진동차단` n=4** · 세션 `aa0fbbf2` · 결정 **D-NAO-291** · 교훈 **#398**
> 계약 `docs/contracts/CONTRACT_oscillation_damping.md`(D-NAO-288) · 정본 `docs/references/136_hh24_curve_was_already_accrued_20260906.md`
> 트랙 `docs/tracks/active/track_naver-ad-optimization.md` · **진행률 2/7 불변**
> 적대 리뷰 **1R FAIL(P1 1건) → 2R PASS(P1=0)** · 완료 QA **종합 부분달성**(트랙 D-NAO-59 **판정불능**)
> PR **#745 병합**(`ab68dc7f` · CI 3/3 · 08:14:04 KST) · **prod 배포 0건** · 마이그 0 · 네이버 쓰기 0콜

## 검증 명령 (한 줄 — 받는 세션은 이걸로 인계를 검증한다)

```bash
ssh sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && ./.venv/bin/python /tmp/aveto_n4b.py --db ohisell.db --since 2026-09-02 --until 2026-09-05 --deploy-ts '2026-09-05 14:08' --curve-source db | head -4" && \
  cd <이 repo>/backend && python3 -m pytest tests/test_naver_aveto_counterfactual.py -q
```
→ 출력에 **`네이버 /stats 읽기 콜 0건`** + **`[곡선 출처] … naver_keyword_hourly`** 가 있고 테스트 **37 passed**면 인계가 참이다.
⚠️`/tmp/aveto_n4b.py`가 없으면 `scripts/measurements/aveto_counterfactual.py`를 prod `/tmp`로 scp해서 돌린다(`backend/`를 cwd로).

---

## §0. 목적 (원문 — 발명하지 않는다)

n=1 HANDOFF §0의 Jino 지시문 원문:
> **엔진이 만든 CPC 상승이 엔진의 정지 근거가 된다.** 액셀(UP)은 정착창(과거) 보정ROAS로 올리고,
> 브레이크(DOWN)는 당일(현재) CPC 급등으로 내린다. … **액셀과 브레이크가 서로 다른 창을 본다 —
> 그게 진동의 원인이다.** … 어느 쪽이든 북극성 §7 「액셀·브레이크가 대칭인가」를 통과해야 한다.

**이번 세션의 시작**: n=3 인수 — Jino 명시 지시 2026-09-06 07:03 KST *「n=3은 끝났다, 인수해서 진행해」*.
**이번 세션 몫**: 인계 §6-1(hh24 곡선 적재)의 **전제부터 실측** + §6-3 ⓙ 재관측.

---

## §1. ★★인계의 1순위 항목이 반증됐다

`ref 135` §7 이월 원문: *"hh24 곡선을 적재하지 않으면 이 축은 **09-09에 닫힌다**. … 곡선 적재는 이 계약의 스코프 밖 → 소관: 이 트랙(PAO)."*

**적재는 이미 1년치가 돌고 있었다.**

| 표 | 채우는 잡 | 보존 | 완전성 |
|---|---|---|---|
| `naver_keyword_hourly` (`entity_type='adgroup'`) | `sweep_naver_keyword_hourly` (D-1 · 09:10 KST) | **365일** | 24시간 |
| `naver_adgroup_hourly_today` | `sweep_naver_today_hourly` (매시 :57) | **14일** | ★**hour 23 없음** |

**대조 실측**(prod 읽기 전용 · 07:1x~07:2x KST): 반사실이 훑는 (광고그룹, 날짜)에서 API 곡선 ↔ 적재 곡선이
**시간 슬롯 574/574 전필드 일치**(imp·clk·cost·conv_cnt·avg_rank · 24개 그룹×날짜).

⇒ 없던 것은 적재가 아니라 **배선**이었다. 실제로 든 것은 **함수 하나 + 플래그 하나**다.
믿고 착수했으면 **새 표·마이그레이션·크론**을 만들었을 것이다.

---

## §2. 한 것 — D-NAO-291

`stored_curve()` + `--curve-source {auto,db,api}`(기본 `auto` = 적재 먼저, 없으면 API). 출처는 실행 헤더에 찍힌다.

일부러 안 한 것 둘: ①표가 없으면 **조용히 API로 안 새고 터진다**(오타가 「폴백」으로 위장되는 걸 막는다)
②빈 응답에 출처를 안 붙인다(「없다」와 「0이다」 구분).

### 라이브 증거 (prod 읽기 전용)

| 관측 | 값 | 시각(KST) |
|---|---|---|
| api판 vs db판 출력 diff | **0줄** | 07:18 / 재검증 07:35:40 |
| db판 네이버 `/stats` 콜 | **0건** (api판 6건) | 동상 |
| 검산(순위고삐 원장 대조) | **16/16 일치** (양쪽 동일) | 동상 |
| A-veto 반사실 | **①0/8 · ②0/15** (ref 135 §3과 동일) | 동상 |

---

## §3. 계약 §4-D ⓙ 재관측 — 여전히 0건

| 관측 | 07:0x KST (나) | 08:16:00 KST (완료 QA 독립 재실행) |
|---|---|---|
| `naver_proposals`(배포 후) | 222건 · A-veto **0** · B-veto **0** | **250건** · 0 · 0 |
| `ops_diary_entries`(배포 후) | 248건 · 0 · 0 | **280건** · 0 · 0 |

배포(09-05 21:19:52) 이후 무인 회차 **10회 이상**. **0건은 「안 걸렸다」이지 「됐다」가 아니다**(계약 §4-D 규약).
「거부권이 이제 거부한다」는 **여전히 격리 재현으로만** 증명돼 있다. 놀랍지 않다 — 반사실이 **0/15**이고
A-veto의 인구는 전환 하한 `_INTRADAY_UP_MIN_CONV`가 지키는 인구와 이 창에서 겹친다.

---

## §4. 적대 리뷰 — 1R FAIL(P1 1건) → 2R PASS

### P1-1 — 「곡선을 못 얻었다」가 「A-veto 미발동」으로 조용히 집계됐다

`get_curve`가 `list[dict]`만 돌려줘 **`stored_curve`가 만든 「없음/영」 구분을 한 함수 만에 버렸다.**
빈 곡선이 그대로 `judge_row`로 가 `a_veto_fired=False`가 되고, 그 False가 헤드라인 ★줄의
**분모엔 남고 분자에서만 빠졌다** — ★**바로 다음 줄이 「모른다를 미발동으로 세지 않는다」고 보증하던 자리다.**

리뷰어 재현: 발동 조건을 갖춘 2행 × 적재 전무 → `①0/2건`(정답 `①0/0건`).
수리 = `adgroup` 미해석 행과 **같은 관례**(`judge=None` + `skip_reason`) + **사유별 판정불가 내역** 출력.

**수리 후 라이브 재검증(07:35:40 KST)**: 수치 **불변**(①0/8 ②0/15), 새 줄이 `판정불가 사유 — 없음(전건 판정됨)`
⇒ §2에서 보고한 수는 이 결함에 **오염되지 않았다**. **결함은 실재했고 수치는 무사했다 — 둘 다 적는다.**

### ★★2R에서 리뷰어가 내 자기검증 표의 두 칸을 갈랐다 (둘 다 리뷰어가 옳다)
1. **M9는 변이 «형태»에 따라 갈린다** — 같은 라벨 재계수는 사망하나 **별도 버킷을 «추가»하는 형태는 생존**(단언이 부분일치라서). ★줄·판정·분모 무영향이라 P1 아님.
2. **M11b** — `mode=rw`로 바꾸면서 가드가 찾는 리터럴을 **주석에 심으면 통과**한다. 이 저장소의 소스 텍스트 드리프트 가드 관례 **전체가 공유하는 성질**이고, 현실적 드리프트 방향(헬퍼 추출·변수명 변경)으로는 **시끄럽게 실패**하므로 안전 쪽으로 깨진다.

⇒ **자기검증의 표는 그 자체로 증거가 아니다.**

### ★★★그리고 교훈 #397의 **네 번째 재현**이 이 세션에도 났다
내가 쓴 「읽기 전용」 테스트가 **자기 픽스처가 연 접속**을 검사했다(`_mk_curve_db`가 스스로 `mode=ro`로 열었다)
— 스크립트를 `mode=rw`로 바꿔도 초록이었다. **교훈 #397을 읽고 나서 쓴 파일에서 났다.**

변이 재주입 7종(M4·M8·M9·M15·M16·M11·P1 원복) 전건 사망 · M13은 기각. 테스트 **31 → 37종**.

---

## §5. 완료 QA — 원문 그대로 (미달·판정불능 포함)

**별도 서브에이전트(Sonnet·읽기 전용) · 2026-09-06 08:2x KST · 대조 3개.**

- **판정(계약 D-NAO-288 §4): 부분달성** — *"§4-C ⓖ가 「소급 판정불능」이라 적어 둔 A-veto 반사실을 db 소스로 재현 가능하게 만들었고 §4-D ⓙ도 새 타임스탬프로 갱신했다. 그러나 계약이 요구하는 §4-C ⓗⓘ·§4-D ⓙ의 체크박스는 여전히 `[ ]`이고 `auto_operator.py`도 무변경이라 「거부권이 실제로 발동해 진동을 줄였다」는 실집행 증거는 이번에도 **0건**이다."*
- **판정(트랙 D-NAO-59): 판정불능** — *"변경 파일은 계측 스크립트·테스트·문서·트랙 확인줄·교훈 파일뿐이고 `auto_operator.py`(실행/판정 로직)는 0줄 변경이다. 즉 이 슬라이스는 **원리적으로 매매 의사결정이나 총이익에 손댈 채널이 없다.**"*
- **판정(PAO 북극성 M4): 부분달성** — *"M4의 품질·관측 축 안에서 움직였고 M을 닫지 않았다(2/7 불변). §1 5요소 중 ⑤만 움직였고 ①②③④는 0. §7 대칭 검사: `auto_operator.py` 무변경으로 판정 로직 자체는 손대지 않았음을 확인 — 대칭 불변, 침해 없음."*
- **종합: 부분달성** — *"계측 도구는 검증됐고 방향도 §7 대칭을 안 깼지만, 계약의 실집행 증거와 트랙의 총이익 증거는 여전히 0건이라 「됐다」고 말할 단계는 아니다."*

**합격기준**: ①**부분달성**(구조적 독립성은 확인, 「7일 창 «밖»」 문언 자체는 **09-09에 창이 닫혀야** 문자 그대로 시험 가능) ②**달성** ③**달성**.

### QA가 확인하지 못한 것 (원문)
- ~~백엔드 전체 회귀를 QA가 직접 재현 못 함~~ → **QA 후속 정정으로 해소**(같은 기 · 원문): *"`pytest -q` 전체 실행, exit code 0 (전건 통과), 관측 2026-09-06 KST. 다만 정확한 통과 건수(「7,935」)는 출력이 tail로 잘려 숫자 자체는 대조하지 못함 — exit code로 「신규 실패 0」만 확인."* **종합 판정 변경 없음: 부분달성.**
- 「7일 창 밖」의 문자 그대로의 시험은 2026-09-09 이전엔 원리적으로 불가 — 간접 증거만 확보.
- 적대 리뷰 1R→2R 변이 주입 로그 원본은 독립 재현 안 함(문서 서술 + 커밋 존재만 대조).
- prod `deploy-manifest.jsonl`이 해당 경로에 없어 「신규 배포 0건」을 그 파일로 교차검증 못 함 — `grep -c D-NAO-290`=4 불변과 `auto_operator.py` diff 0으로 간접 확인.
- §7 이월(UTC/KST 혼재 · 09-06 08시 버킷 36건 미래 시각)의 실체는 조사 안 함.

---

## §6. 남은 슬라이스 — 다음 세션이 할 것

1. ★**09-09 이후 「창 밖」 실측** — 그날이 오면 `--curve-source db`로 09-02 날짜를 돌려 **API가 400을 주는 창에서도 재현이 서는지**를 문자 그대로 시험한다. 이게 합격기준 ①을 부분달성 → 달성으로 옮기는 유일한 관측이다.
2. **계약 §4-C ⓗ의 「배포일 양쪽 창 제외」 규칙 채택 여부** — 09-12에 1주 창이 서면 필요하다(n=3이 제안만 해 뒀다).
3. **ⓙ 재관측 계속** — ⚠️A-veto는 반사실 0/15라 **한동안 0건일 수 있다.** 0건은 「안 걸렸다」이지 「됐다」가 아니다.
4. **이월 P2-2**(2R) — today 스윕 `rotate_for_hour(_CALL_CAP=800)` 회전으로 **판정 시각보다 이른 곡선이 언 채 남을 수 있다.** 닫는 관측 1줄: D-1 `naver_ad_daily`에서 `keyword_id=''` and `imp>0`인 distinct `adgroup_id` 수가 800을 넘는가.
5. **이월 P2-2(n=2)** — `latch_reason_census` 커버리지 테스트 동어반복(AST 대조 필요).
6. **이월 P2-10** — 계수기 `⚠️`를 읽는 주체 미정(크론·대시보드 배선 0).

---

## §7. ↗️ 스코프 밖 (기록만)

- `naver_proposals.created_at`은 **UTC**인데 `naver_change_log.changed_at`은 **KST**다(ref 135 §7 계승) → 소관: 이 트랙.
- ★**배포 후 `naver_proposals`에 KST 타임스탬프 행이 섞여 들어온다** — 회차 버킷을 KST로 환산하면 「09-06 08시」에 36건이 서는데 그 시각은 아직 오지 않았다(관측 07:0x KST). 위와 같은 뿌리로 보이나 **확인 안 됨** → 소관: 이 트랙.
- **n=3 등록부 행의 `end_kst`가 미푸시 상태로 남았다** — 자기 클론에 쓰고 커밋을 안 했다. 과거 행 불변 원칙에 따라 손대지 않았고, n=4 행을 append하며 `note`에 인수 근거를 남겼다 → 소관: 하네스.

---

## 트랙 진행률

**2/7 불변** (달성 M0·M1 / 미달 M2·M3·M4·M5·M6). M4의 **품질·관측** 축이고 M 닫기가 아니다.
트랙 `확인:` 줄 **4건** 누적(07:2x · 07:4x · 07:4x · 08:2x KST).

---

## ⚠️ 하네스 관측 (이 세션에서 겪은 것)

- **트랙 훅 「기록 누락」 오탐 — 이 세션에서만 7회**. 재현으로 기전 확인: 내 클론·`origin/main`에는 `확인:` 줄 **3건**이 있고 훅이 읽는 **공유 iCloud 폴더는 0건**(stale). 공유 폴더에 대신 쓰지 않았다 — 남의 커밋에 쓸려 들어간다.
- **훅 `[표면] 표면 파일 변경 0/1건`도 같은 뿌리** — 표면파일 해시를 공유 폴더에서 잰다.
- ★**`safe_merge.sh`가 「체크 0건」을 초록으로 오독하지 않고 거부했다**(교훈 #123 집행 관측). push 직후엔 CI가 아직 등록 전이라 정상 동작이다 — 재등록을 기다렸다 재시도하면 된다.
- **push 한 번마다 CI 전체가 재시작한다** — 문서 커밋을 나눠 push하면 backend CI(약 11분)를 그만큼 다시 기다린다. 착지 직전엔 커밋을 모아서 push하는 편이 낫다.
- **prod `.env`는 채워져 있고 prod에서 네이버 `/stats` 읽기가 된다** — 「자격증명 없음」이 뜨면 IP 허용목록이 아니라 **`load_dotenv()` 경로 문제**일 수 있다(`/tmp`에서 실행하면 `find_dotenv`가 «파일» 기준이라 `backend/.env`를 못 찾는다 ⇒ `load_dotenv(os.path.join(os.getcwd(), ".env"))`).
- **iCloud 공유 폴더는 여전히 624커밋+ stale** — 작업은 로컬 디스크 전용 클론(`/Users/jino/ohisell-vib-n4`, full clone)에서 했다.
