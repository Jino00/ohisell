# 세션 인수인계: 봉투 P1 종결 + PAO 전략 3갈래 규모 실측 → A 착수 확정
> 저장일시: 2026-08-11 18:3x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것
> 트랙: **PAO(파오)** — `docs/tracks/active/track_naver-ad-optimization.md`
> 앞 세션: `HANDOFF_adaptive-guardrail-envelope-P1_20260810.md`

## 1. 한 줄
D-NAO-172 P1을 **적대 리뷰 2R·배포·라이브 합격 4/4로 종결**했고, 이어서 「PAO에서 무엇을 할 것인가」를 세 갈래로 놓고 **라이브 실측**했다. 결론: **없어서 못 하는 게 아니라 있는데 안 쓰고 있다.** Jino 승인 → **A(검색어 ROAS 제외 리스트) 착수**.

## 2. ⚠️ 새 세션이 가장 먼저 할 일
1. **계약은 승인됐다**(Jino 2026-08-11 18:32 *"handoff하고 진행하자"*). 계약서 `docs/PLAN_search-term-exclusion-list.md`, 앵커 `.claude/anchors/49a17549-....md` 작성 완료. **착수 전 실측 넷도 끝났다(§4-⑤).** → **바로 P1 구현부터** 시작하면 된다.
2. P1 구현 순서(계약 §4): **①조치 생존 감시 → ②연속 ROAS 판정 + 리스트 생성기 → ③화면.** 감시를 먼저 놓는 이유 = 「우리가 건 게 아직 걸려 있나」를 못 보면 나머지가 무의미하다.
3. **낙관적 락 재적용이 지금 가능하다**(§5-③). 어제 막혔던 조건이 해소됐다.
4. `git fetch` — 병행 세션이 매우 활발하다(이 세션 중에도 내 커밋 위에 병합·push가 3회 있었다).

## 3. 이 세션이 완료한 것
| 커밋 | 내용 |
|---|---|
| `5cf0e55` | **적대 리뷰 P1-2 수리** — 쿨다운도 생성기가 값을 복제하고 있었다(§4-①) |
| `0a9617d` | D-NAO-172 P1 종결 원장 + 교훈 #232·#233 |
| `b287e65` → `0e2a7e1` | 낙관적 락 **구현 후 원복**(설계 결함 아님 — 배포 순서 문제, §5-③) |
| `cd432d5` | 잔여 P2ⓐ 원장 + 교훈 #244·#245 |

**라이브 합격 4/4(prod 08-10 22:43)**: ①KV 투입→`source=db`→삭제→`code` 복귀·원상복구 ②3개 값·출처·범위·근거 표시 ③신선도 `stale:false lag_days:0` ④범위 밖 PUT 400 + 거부가 아무것도 안 씀. ★엔진층 직접 관측 — prod에서 DB `cooldown_hours=3`일 때 탐색 트리거가 2.5h에 발동 안 함(상수 2h면 발동).

## 4. ★이 세션이 «측정»한 것 (전부 prod 라이브, 이게 이 인계의 핵심)

### ① 적대 리뷰 P1-2 — 쿨다운 값 복제
`exploration._EXPLORATION_COOLDOWN_HOURS = 2`가 게이트 상수를 **import하지 않고 값만 복제**해 탐색 발동을 스스로 게이트했다. 주석엔 "값 출처=`guardrail_gate._COOLDOWN_HOURS`"라고 적혀 있었다 — **사람은 연결을 선언했고 코드는 복사했다.** 그리고 그걸 막겠다던 테스트가 두 겹으로 공허했다(grep 상대경로 CWD 의존 → 루트에서 rc=2로 죽고 통과 / 패턴이 import 한 형태만). 교훈 #232·#233.

### ② B트랙 — 페이지 경계는 **3위↔4위**다 (신규 측정)
최근 60일 258,210행·`avg_rank` 보유율 100%. 순위가 4위 이상 움직인 광고그룹 **288개**로 유닛 고정 대조:

| 컷 | 유닛 | CTR 낙차 | 클릭당 전환 낙차 | CPC 낙차 |
|---|---|---|---|---|
| 3위 | 264 | **−8.4%** | −2.3% | −9.7% |
| **4위** | 277 | **−67.6%** | −5.5% | −17.2% |
| 5위 | 263 | −75.3% | −7.2% | −21.2% |
| 6위 | 257 | −78.3% | −8.4% | −24.3% |

- **낙차가 전부 CTR에 있고 클릭당 전환은 순위와 거의 무관하다** — 순위는 「클릭을 얻느냐」를 좌우하지 「클릭의 질」은 안 건드린다. 아래로 가면 CPC는 17~26% 싸지는데 클릭이 사라진다.
- ★**우리 이익 스팟 밴드 상단이 4.0**(`vitality_signal._RANK_BAND_TOP`) — **밴드 안이 이미 절벽 아래다.**
- 대행사 팀장 증언(ref 35 §7 「3등→4등이면 페이지가 넘어간다, MOP는 이걸 못 본다」)이 우리 데이터로 재현됐다.
- 정직 단서: `avg_rank`는 시간당 평균이라 경계가 뭉개져 있다(평균이 절벽을 만들지는 않는다) · 유닛 «안»의 시간 교란은 남음 · 쇼핑(adgroup) 축만 · 전환은 D+1 성숙도 이슈(양 구간 동일 적용).
- 재현 스크립트: 스크래치패드 `probe_B.py`·`probe_B2.py`·`probe_B3.py` 패턴(원격 `ssh sellc... .venv/bin/python -` 로 stdin 주입).

### ③ A트랙 — 검색어 전환 데이터는 **이미 완비돼 있다** (내 전제가 틀렸다)
ref 34(07-20 대행사 증언)의 「최대 갭 = 검색어 전환 미수집」을 현재로 읽었는데, **그 갭은 이틀 뒤 D-NAO-77(07-22)로 해소**됐다. 3주 묵은 문서를 현재로 말했다.

쇼핑 검색어 30일: 302,296행 · 비용 **15,434,086원** · 전환 1,565건 · 전환매출 25,755,530원(ROAS 1.67). **검색어별 ROAS가 지금 당장 계산된다.**

비용 발생 고유 검색어 **4,090개** 분포:

| 구간 | 검색어 | 30일 광고비 |
|---|---|---|
| **전환 0** | **3,178개** | **6,858,086원** |
| 0~1.0 | 37개 | 1,436,046원 |
| 1.0~1.7 (BEP 미만) | 69개 | 2,281,903원 |
| 1.7~3.0 | 130개 | 2,286,796원 |
| 3.0+ | 676개 | 2,571,255원 |

→ **BEP 아래에 10,576,035원 = 쇼핑 검색어 광고비의 68.5%.** 그런데 **제외 실집행은 19일간 1건**(`naver_search_term_exclusion` 1행), 승격 제안 **256건 pending 방치**.

빠진 조각 셋: ⓐ판정이 연속 ROAS가 아니라 **「전환 유무」 이진 게이트**(`conv_purchase_amt/cost` 나눗셈이 코드에 없음) ⓑ쇼핑 제외키워드 **API가 HTTP 400(code 3728)으로 거부** → 실제 제외는 콘솔 수동 ⓒ게이트 임계값 캘리브레이션 미완(19일 1건이 그 증상).
⚠️**파워링크 5,208,430원은 검색어별 전환을 네이버 API가 안 준다** — 미구현 갭이 아니라 **근본 한계**(실측 확정). 뚫을 수 없다.

### ④ 03 강화유리 캠페인 실태 (「PAO로 03을 켜면?」 질문의 답)
- 03 = `cmp-a001-02-000000008492582` · status on · `auto_operate=0`·`optimizer=none` · 그룹 24개 전부 on · **소재 엔티티는 DB에 0행**(계정 전체에 `entity_type='ad'` 행이 없다 — 실효입찰 판별은 라이브 API로 한다).
- 30일 광고비 2,155,047원(계정의 5.1%) · ROAS 2.80 · 7일 3.42 · 일예산 **200,000원**(원복 미결).
- **미집행 approved 0건**(계정 전체도 0) — 묵은 제안 재점화 걱정은 없다. pending 33건은 전부 검색어 승격.
- 첫 회차 판정 대상: 03만 **13그룹**(적자 down 3 + 성장 up 10).
- **대행사가 03을 돌리고 있다** — `naver_agency_op` 최근 14일 25건(ad_edit 17·bid_change 8), 최신 08-06 소재 입찰 −15~−37% 인하. ⚠️`change_log`의 `external_*`만 보면 0건으로 보인다(대행사 조작은 **`naver_agency_op` 테이블**에 산다).
- 같은 상품이 다른 캠페인에도: 「10. 컨텐츠매체」에 **22/24개**(우리가 08-10 정지, 유지 확인) · 「11. 벌크」에 15/24개(on).
- ★**03은 A/B의 MOP 팔이 아니다** — D-NAO-61(07-19)로 이미 우리 관리로 전환됐다. A/B는 DEAD.

### ⑤ 착수 전 실측 넷 — **완료**(계약 §7, 2026-08-11 18:3x)

**③ 조치 생존 감시 검체 — ✅ 라이브 생존 확인**
`naver_search_term_exclusion` 유일 1행: `아이패드종이필름` · group `grp-a001-01-000000060841583` · `restrict_kwd_id=rst-a001-00-000001865606760` · 2026-07-22 제외 · 재심 예정 08-21 · `cost_at_exclusion=22,854`. 라이브에 **그대로 걸려 있다.**
- **감시 수단**: `naver_sa_writer.get_restricted_keywords(adgroup_id)` (= `GET /ncc/adgroups/{id}/restricted-keywords`, 읽기 전용).
- **응답 실물**: `{"nccAdgroupRestrictKwdId","nccAdgroupId","keyword","type","delFlag","regTm"}`
- ★**대조 키는 `nccAdgroupRestrictKwdId`이고, `delFlag`(소프트 삭제)까지 봐야 한다** — 존재 여부만 보면 delFlag=true를 「살아 있음」으로 오독한다.
- ⚠️이 1건은 **파워링크** 캠페인이다(쇼핑은 제외 API가 400으로 막혀 있으니 당연). 쇼핑 검체는 P2에서 Jino 수동 실행으로 처음 생긴다.

**② 상품별 BEP 붙이기 — 커버리지 97.5%**
비용 발생 광고그룹 197개(15,434,086원) 중 **192개(15,049,413원 = 97.5%)**가 상품 매핑→BEP까지 닿는다. 못 닿는 5개(384,673원, 2.5%)는 fail-closed로 후보 제외.
- ★**93개 그룹(6,209,695원 = 40%)에 상품이 2개 이상** 붙어 BEP를 하나로 못 정한다.
- 적용 가능 BEP ROAS: 중앙값 **1.649** · min 1.223 · max 5.810. **「1.7 일괄」로 재면 판정이 달라진다.**

**① 전환 성숙도 — 이 표로는 못 잰다**
일자별 ROAS D+1 2.28 / D+2 1.81 / D+3 1.93 / … / D+14 1.35 — 오래된 날이 체계적으로 높지 않다(늦게 잡히는 전환이 크면 반대여야 한다). 다만 `naver_search_term_daily`는 **upsert라 수집 시점별 이력이 없어** 성숙 곡선 자체를 그릴 수 없다. 기록된 사실(D+1 이후 불변·재수집 D+3까지)에 맞춘다.

**④ 캠페인별 BEP 미만 분포(쇼핑 30일, BEP 1.7 일괄 기준 — 상품별로 재면 바뀜)**

| 캠페인 | 총 광고비 | BEP 미만 | 비중 | BEP미만 검색어 |
|---|---|---|---|---|
| ● 01. 갤럭시_지문방지_TPU | 9,292,589 | **6,617,439** | 71% | 1,856개 |
| ● 01. 갤럭시_지문방지_사생활 | 1,713,028 | 1,229,966 | 72% | 606개 |
| ● [P]03. 아이폰_강화유리 | 1,077,527 | 527,788 | 49% | 175개 |
| ● 02. 갤럭시_보급형_M | 875,062 | 521,744 | 60% | 188개 |
| ● [P]맥세이프카드케이스_쇼검 | 354,216 | **352,525** | **100%** | 47/48개 |

**★모델이 정하고 간 것 둘 (routine 판단, 근거 보존)**
- **상품 2개 이상 그룹의 BEP = 그중 «가장 낮은 값».** 계약 판단기준 7(fail-closed)의 적용 — 가장 넘기 쉬운 기준을 통과하면 자르지 않는다. max는 자르는 쪽으로 기운다.
- **성숙도 컷오프 D+3.** 최근 3일은 판정에서 뺀다.

**첫 라운드 대상 캠페인은 아직 미정** — 리스트 실물을 보고 Jino가 정한다. 후보 감각: 맥세이프(우리 지정·48개 중 47개 BEP 미만·규모 35만원)가 작아 검체로 안전해 보이나 **결정 아님**. 01 TGLASS는 규모가 압도적(6.6M)이지만 우리 지정 밖이라 대행사 통보 문제가 같이 걸린다.

## 5. 남은 일 / 이월
### ① ★A트랙 착수 (이 세션의 결론 — Jino 승인)
Fable 기획 파트너 판단 + Jino 승인: **A 먼저, B는 반나절로 마감만, C는 안 한다.**
- **안1(채택)** — A를 「자동 실행」이 아니라 **주간 제외 리스트 생성기**로. PAO가 리스트를 만들고 실행은 Jino가 콘솔에서.
- 근거 셋: ⓐA는 지금 돈이 새는 곳(BEP 미만 30일 1,057만원)이고 재개 개방 순서 1번(제외키워드)과 일치 ⓑAPI가 막혀 수동인 게 오히려 기회 — *"맡기면 망하겠다"* 이후 신뢰를 쌓는 경로는 **추천→사람 실행→성적표** 반복이지 자동화 복구가 아니다 ⓒC는 「우리 판단이 돈을 벌었다」는 증거 한 장 전에 열면 17프로 사고의 재판.
- **비목표**: PAO 재개·입찰 자동화·B-4 end-to-end 검증 / 제외키워드 브라우저 자동화(수동 20~50건으로 가치 실측 전엔 안 만든다) / C 전부 / 파워링크 검색어 전환 재도전 / 「전략 30~40개 발굴」.
- **선행조건(파트너가 짚은 축)**: **조치 생존 감시가 A의 1단계다.** 대행사가 우리 조치를 되돌린 게 2회이고 잠금 해제는 원인도 모른다 — 「우리가 건 것이 아직 걸려 있는가」를 하루 1회 대조해 배너로 띄우지 않으면 성과가 조용히 증발해도 모른다.
- **가장 큰 리스크**: 「전환 0 = 죽은 검색어」 등식. 3,178개엔 전환이 늦게 잡히는 것과 롱테일이 섞여 있다. 한 번에 자르면 17프로 사고의 반대 방향(볼륨 절멸). 광고비 큰 순 소량씩 + 되돌림 경로 + 합격기준을 착수 전에 박을 것.
- **캠페인 주인 문제**: `[P_삭제금지]` 5개(03·04·15·P·맥세이프)가 우리 몫 지정인데 **03은 대행사가 되살려 몰고 있다.** 첫 라운드 대상 선정은 그 사실 위에서.

### ② B트랙 마감 (반나절)
분 단위 순위로 경계 날카롭게 확인 + 파워링크 축 따로 측정. **결과는 밴드 상단(4.0) 재검토 자료로만 두고 입찰은 안 건드린다**(입찰 잠김 상태에선 잠자는 파라미터).

### ③ ★낙관적 락 재적용 — **지금 가능하다**
`0e2a7e1`을 revert하면 코드가 돌아온다(구현·테스트·변이 6종 KILLED 전부 완료돼 있다). 어제 막혔던 이유는 **prod 프론트가 main 미병합 브랜치 빌드**여서였는데, 병행 세션이 `6b0f495`로 해소해 **지금 prod 스탬프 = `cd432d5`·branch=main**이다. 단 prod 번들에 `If-Match`가 아직 0건이므로 **백엔드와 프론트를 반드시 같이** 올릴 것(백엔드만 먼저 = 저장 버튼 즉사, 교훈 #244).

### ④ 잔여 P2 ⓑⓒⓓ (D-NAO-172)
int에 `4.7` 넣으면 조용히 `4`로 절삭 / `updated_at` UTC / `guardrailParamsSave.ts`의 `!p.rejected`가 도달 불가 분기(가드는 두되 테스트를 백엔드 불변식 `source=="db" ⟹ not rejected`로 옮기는 게 맞다).

### ⑤ 이월 (앞 세션에서 넘어온 것)
`update_keyword_bid`가 `useGroupBidAmt: False`를 항상 전송(`naver_sa_writer.py:316`·`:348`) — 소재 금지선의 키워드판일 수 있는데 코드에 검토 흔적 없음 · `next_ids.sh`가 원장 미기재 번호를 재발급 · `[LEVER_MISMATCH]` 상시 표면 없음 · B-2 집계 정본 헬퍼 · B-3 BEP 기준선 표면화 · 03 일예산 200,000 원복(13일째 미결) · 01 갤럭시_지문방지_TPU(계정 광고비 **43.6%**, 18,591,280원/30일) 미조치 · 대행사 통보 여부.

## 6. 상태·환경
- prod: `sellc.ohitech.co.kr` · pm2 **`ohisell-backend-8001`**(항상 `pm2 list` 확인) · 프론트 번들 `index-BbHjjvEy.js`
- ⚠️**Mac IP가 회전한다** — 08-11 06:2x에 `116.84.110.196`으로 바뀌어 nginx 허용목록(`/etc/nginx/snippets/ohisell-allowlist.conf`)에서 빠졌다. 그 결과 **무중단 배포의 vhost 검증이 막혀 `--restart-legacy`(다운타임 ~50초)로 마무리**했다. 병행 세션이 `59e743c`로 「IP 회전 확정」을 기록했다. 허용목록은 prod 보안 설정이라 **모델이 건드리지 않는다.**
- 원격 조회 관용구(이 세션에서 계속 쓴 것):
  `ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python -" < 스크립트.py`
- 테스트: `cd backend && python3 -m pytest -q`(~3분, 현재 5272 passed) · `cd frontend && npx vitest run`(306)
- ★**변이 주입은 커밋 «후»에만** — 이 세션에서 `git checkout --` 원복이 미커밋 구현을 지웠다(교훈 #245). 스크립트 첫 줄에 `test -z "$(git status --porcelain)"` 게이트를 둘 것.
- ★**공유 메인 폴더에서 `--amend` 금지** — 병행 세션의 스테이징 변경을 삼킨다(이번에 발생, 손실은 없었음).

## 7. 새 세션 시작 프롬프트
```
.claude/memory/HANDOFF_guardrail-P1-closed+strategy-sizing_20260811.md 읽고 이어서 작업해줘
```
