# HANDOFF — 계약이 자기 `[미상]`에 적어 둔 「못 잰다」가 다음 세션의 작업 지시가 됐다 (2026-09-05)

> 체인 **`pao-진동차단` n=2** · 세션 `84263fc6` · 결정 **D-NAO-289**
> 계약 `docs/contracts/CONTRACT_oscillation_damping.md` §4-C ⓘ (D-NAO-288, n=1이 승인받은 계약)
> 트랙 `docs/tracks/active/track_naver-ad-optimization.md` · **진행률 2/7 불변**
> **완료 QA 종합: 부분달성** (계약·앵커 **부분달성** / 트랙 D-NAO-59 **판정불능** / 북극성 **부분달성**)
> **마이그레이션 0 · prod 배포 0 · 자동 쓰기 0 · `backend/app/**` diff 0줄**

## 검증 명령 (한 줄 — 받는 세션은 이걸로 인계를 검증한다)

```bash
cd <이 repo>/backend && python3 -m pytest tests/test_naver_latch_reason_census.py -q && \
  ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell && ls scripts/measurements/latch_reason_census.py 2>/dev/null || echo 'prod에 없음(정상 — 배포 안 함)'"
```
→ 테스트가 **33 passed**면 드리프트 가드가 살아 있다. prod 쪽은 **「없음」이 정상**이다(이 슬라이스는 prod 배포 0건 — 계수기는 `/tmp`로 scp 해서 돌린다).

---

## §0. 목적 (원문 — 발명하지 않는다)

n=1 HANDOFF §0의 Jino 지시문 원문을 그대로 옮긴다:
> **엔진이 만든 CPC 상승이 엔진의 정지 근거가 된다.** 액셀(UP)은 정착창(과거) 보정ROAS로 올리고,
> 브레이크(DOWN)는 당일(현재) CPC 급등으로 내린다. … **액셀과 브레이크가 서로 다른 창을 본다 —
> 그게 진동의 원인이다.** … 어느 쪽이든 북극성 §7 「액셀·브레이크가 대칭인가」를 통과해야 한다.

**이번 세션 몫**: n=1이 못 닫은 계약 §4-C **ⓘ**(래치 사유). Jino 선택 = **「계수 스크립트 고정 + 드리프트 가드」**(15:5x KST, 선택지 3개 중).

## §1. ★★착수 실측이 계약의 전제를 반증했다 — 이게 이 세션의 시작이다

계약 §10 [미상]과 §8 스코프밖 기록:
> *"`naver_change_log`에 「어느 가드레일이 실쓰기를 막았나」 사유 열이 없어 `·->·` 27/54건의 내역을 못 잰다"*

**틀렸다** (prod 읽기 전용, as-of 2026-09-05 15:5x KST · 7일 창 · `ad` grain):

| 잰 것 | 값 |
|---|---|
| 무쓰기 행 중 `[실행 불가]` 차단 마커 보유 | **37 / 37건** |
| rationale이 비었거나 사유가 없는 행 | **0건** |

사유는 `naver_execution_harness._guard_failure`(:300)가 **처음부터** rationale에 원문으로 싣고 있었다.
★**열이 없는 게 아니라 «키»가 없었다.** ⇒ 마이그레이션·컬럼 신설이 **지워졌다**(실측 10분).

⚠️**단 「37/37 전건」도 창의 산물이다**(적대 리뷰가 잡음): **90일 창에선 188 중 20건이 마커가 없다**.

## §2. 한 것

- `scripts/measurements/latch_reason_census.py` — 읽기 전용 계수기. **stdlib 전용**(앱 임포트 0 · 네이버 API 호출 0)이라 `.venv` 없이 prod에서 돈다.
- `backend/tests/test_naver_latch_reason_census.py` — **드리프트 가드 33종**. 진짜 `guardrail_gate.check()`를 **15종 차단 조건으로 구동**해 나온 실문자열을 규칙표에 때린다.
- `docs/references/134_latch_reason_census_20260905.md` · 계약 §0·§10·§4-C 정정 주석(**원문 무변조 — 삭제 줄 0건**) · 트랙 확인줄 **3건** · README 등재 · **교훈 #392~#394**.
- PR **#736** — 커밋 4(`d442a32` 초판 · `a6ea889` P1 수리 · `decba46` as-of · `cca4a7b` 교훈 · +트랙). **머지 대기**.

## §3. ⓘ 실측 (★as-of를 떼면 거짓이 된다 — 매시 :20 크론마다 움직인다)

**as-of 16:0x** 전체 57 · 무쓰기 37(64.9%) · 쿨다운 2h **28** · 자동 하향 일일 상한 **9** · 레인 **17:11:9**
**as-of 16:21/16:31**(적대 리뷰·QA 독립 재현, 일치) 전체 **58** · 무쓰기 **38** · +건수상한 1 · 레인 **18:11:9**

★§0이 *"진동을 지금 가드레일이 눌러 주고 있다"*고 한 그 가드레일 = **이 창에서는 쿨다운 2h(75.7%)**.
⚠️**「경제성이 아니라 박자 게이트」로 일반화하지 않는다** — **90일 창은 BEP 미달 55 = 쿨다운 55 동률**(북극성 §7).
★북극성 §7 대칭 소급 재확인: **액셀 18 : 브레이크 20**.

## §4. ★★이 세션의 최대 산출물 — 「내가 세운 원칙을 내가 어겼다」 3회

적대 리뷰 **1R FAIL(P1 2건) → 2R PASS(P1=0)**. 변이 누적 **14종 + 재주입·역행 10종 전건 사망**.

| | 무엇 |
|---|---|
| **P1-1** | 같은 커밋에서 §0·§10은 원문 무변조 + 정정 «부착»(그 주석에 「원문은 남기고 정정만 붙인다」라고 **자기 원칙을 명시**)해 놓고, **§4-C ⓘ 에서만 원문을 삭제·교체하고 `[x]`를 찍었다.** 그 항목 본문이 *"배포 후 변화는 아직 못 잰다"*고 **스스로 자백**하고 있었다 ⇒ **자백은 방어가 아니다** |
| **P1-2** | **「모름」을 「차단됨」으로 세고 있었다** — harness :267이 대문자로 금지한 것. (a)`[실행 불가]`인데 게이트 밖 사전 가드인 행을 「마커 없음」으로 라벨 (b)레인 표가 쓰기 실패 행까지 세어 **북극성 §7 대칭 수의 분모가 오염**. **그 상수를 import 하면서 그 주석은 안 읽었다** |
| **자기검증 M5** | ⚠️ 드리프트 자백 출력 제거가 **1차 생존** → 표면 3종 신설. 그런데 **리뷰어가 「ℹ️ 줄」 하나가 더 남은 걸 잡았다**(M10) ⇒ 표면은 «종류»가 아니라 **«줄 단위»**로 센다 |

★**7일 창 수는 P1-2 수리 후에도 완전히 불변**이다(37건 전부 `[실행 불가]`라 「모름」 0). **안 바뀐 게 사소했다는 뜻이 아니다** — 90일 창엔 「모름」 20건이 실재하고, 그게 다른 밴드였던 건 우연이다(교훈 #123).

## §5. 완료 QA — 원문 그대로 (미달·판정불능 포함)

- **판정(계약·앵커 합격 4항): 부분달성** — *"①②③ 라이브·격리 재현 모두 달성, ④는 체크박스 자체가 (규율에 따라) 미체크로 남아 문자 그대로는 미충족"*
- **판정(트랙 목표 D-NAO-59): 판정불능** — *"읽기 전용 진단 스크립트일 뿐 어떤 입찰·집행 코드도 바꾸지 않았다(`git diff origin/main --stat -- backend/app`가 빈 출력으로 확인). 「ROAS를 감수하고 매출을 늘려 총이익을 늘렸는가」를 재는 관측치가 이 산출물엔 원천적으로 없고, §10도 스스로 「총이익에 얼마를 물렸는지 이번 창에선 못 잰다」고 자백한다."*
- **판정(북극성 §1·§6): 부분달성** — *"M4(품질 축) 안에서 움직였고 M 체크박스는 불변(2/7). **5요소 중 이 addendum 자체가 움직인 것은 0/5다** … 다만 §10의 문서화된 오류 전제를 실측으로 반증하고 §7 대칭을 소급 재확인한 것은 진단적 정합성 확보로서 실질 가치가 있다."*
- **종합: 부분달성**

### ★QA가 잡은 것 — 만든 쪽 보고에 없던 것
1. **내 북극성 검토가 틀렸다.** 나는 앵커에 「④자동화 운영이 움직인다」고 적었는데 **실측은 0/5**다. 계약 §9의 「③④ 이동」은 **D-NAO-288(전 세션)** 것이고 이번 3커밋의 `backend/app` diff는 **0줄** — **§9만 보면 이번 것도 ③④를 움직인 것으로 오독하기 쉽다.**
2. `git diff --stat`에 `.claude/memory/chains/pao-진동차단.jsonl` +1줄이 섞여 있다(체인 등록부 — 하네스 부산물).

### QA가 확인하지 못한 것 (원문)
- 적대 리뷰 1R FAIL의 **변이 주입 로그 원본을 못 봤다** — 문서에 적힌 결과만 대조했다.
- **90일 창은 직접 안 돌렸다**(7일만 라이브 재확인).
- §4-C ⓗ·§4-D ⓙ는 **원리적으로 지금 재판정 불가능**하다.
- 일기 4,543 vs census 38의 「120배」는 **문서 인용만 확인**했다.

## §6. 남은 슬라이스 — 다음 세션이 할 것

1. ★**ⓘ는 여전히 미달이다.** 계약 원문이 요구한 것은 「비율의 **배포 후** 변화」인데 배포 3시간이라 「후」가 없었다.
   ★★**그리고 구조적으로 이 도구로는 영영 못 낸다** — 기준선 27/54는 **소재 1개(`nad-…554755092`) 4일** 수치인데
   계수기에 **`entity_id` 필터가 없다.** ⇒ 다음 세션은 **필터를 넣거나, 기준을 새 계약으로 바꾸거나** 둘 중 하나다.
2. **ⓗ** — 같은 소재·같은 날 UP∧DOWN 날 수. 배포 전 창 실측 **2일**(09-03·09-04). 1주 창이 서면 재측정.
3. **ⓙ** — 두 거부권 일기. **이 계수기엔 원리적으로 안 잡힌다**(둘 다 `hold`라 change_log에 행이 없다) — `ops_diary_entries` 정본.
4. **이월 P2-2(적대 리뷰)** — 커버리지 테스트가 **동어반복**이다. `REASON_RULES` ↔ `GATE_CASES` 두 «손으로 쓴 목록»끼리만 비교하고 `guardrail_gate`의 실제 반환 자리(**33곳**)를 안 본다. 양쪽에서 같이 지우면 초록. **현재 15/33** ⇒ AST 대조 필요(D-NAO-265 S4-a 형식).
5. **이월 P2-10** — 계수기의 `⚠️`를 **누가 읽는가는 미정**(크론·대시보드 배선 0). 사람이 손으로 돌릴 때만 갱신된다.

## §7. ↗️ 스코프 밖 (기록만)

- 소재 `nad-…554755092`가 오늘 **1010→1520원(+50.5%) 단조 상승**, DOWN 0건. 자동 상향 누적 상한은 `auto_up_base_bid` 대비 ×2.0이라 기준점 위치에 따라 곧 닿을 수 있다 → 소관: **상한 리셋 입구(B1 세션·PR #725)**
- 일기 `blocked` 7일 **4,543** vs change_log 무쓰기 **38** = 약 **120배**. **두 표의 상위 사유가 아예 안 겹친다**(일기 1·2위 `daily 손실상태 제외` 1,779+907, 3위부터 `자동운영 스코프 밖`) — 셋 다 harness에 닿기 전에 걸러진다 ⇒ **「누락」이 아니라 설계상 다른 층**. 나란히 놓으면 오독 → 소관: 이 트랙

## 트랙 진행률

**2/7 불변** (달성 M0·M1 / 미달 M2·M3·M4·M5·M6). M4의 **품질·관측** 축이고 M 닫기가 아니다.
트랙 `확인:` 줄 **3건** 누적(16:1x · 16:3x · 16:4x KST).

## ⚠️ 하네스 관측 (이 세션에서 겪은 것)

- **트랙 확인줄 훅 오탐 4회** — 공유 폴더가 `211decbc`로 **624커밋 stale**. 알려진 함정. 공유 폴더에 대신 쓰지 않았다.
- **체인 생존 훅 `⛔ pao-논의 n=49` 오탐** — 같은 원인(공유 폴더 등록부 stale). origin/main에서 재확인했다.
- **적대 리뷰 표면 변이 훅이 «완료 QA» 위임에 오탐 1회** — 안내대로 그대로 재전송해 통과.
- **iCloud 공유 폴더에서 워크트리 불가**는 이번에도 유효 — 로컬 디스크 `--depth 1` 클론(`/Users/jino/ohisell-vib-n2`)으로 우회.
- **`--days` 없이 돌린 pytest 전건 회귀는 약 5~6분** (7,886 passed).
