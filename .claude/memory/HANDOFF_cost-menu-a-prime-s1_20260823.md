# HANDOFF — 계약 A′ 승인 + S1(원가 메뉴 · 부자재 층) prod 배포·병합
2026-08-23 (KST) · 세션 `bd4adba0` · 체인 **「SellC 원가 메뉴」 n=2**
워크트리 `/Users/jino/.claude-worktrees/ohiselling/cost-menu-aprime` (브랜치 `feat/cost-menu-aprime`, **PR #339 병합 완료** `aab20a2a`)

---

## 0. 다음 세션이 30초 안에 알아야 할 것

★**이 트랙의 목표에는 이름이 있다 — 「SellC 원가 메뉴 목표」**(Jino 명명 2026-08-23).
**정본은 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md`다.** 목표를 다시 쓰지 말고 그 파일을 열어
앵커 `목표:`에 원문을 옮긴다. 트랙 헤더와 갈라지면 **카드가 이긴다.**

- **트랙 궁극 목표는 「정확한 원가 산출」**이고 4갈래다: ①SellC 원가 메뉴 ②계산 과정이 보이는 것 ③수입건 금액 → 회계적 원가 ④재고·ecount. 진행률 **2/6 불변**.
- 이번 세션이 한 것 = **계약 A′ 승인(D-CPP-53) + 그 첫 슬라이스 S1**. ①과 ③의 «입력층»까지 라이브다.
- **다음은 S2** — 레시피 탭(엑셀 → 상품명×폼팩터 초안 → Jino 승인 → 표준원가 계산).
- ★**S2 착수 전 Jino 답이 필요한 것 1건**은 §6-1에 있다. 답 없이도 착수 가능한 형태로 계약이 설계돼 있으나, 그 답이 A2의 정밀도를 정한다.

---

## 0-A. ★다음 세션의 운영 원칙 — Jino 지시 (2026-08-23 13:00)

> ***"다음 세션에서부터는 SellC 원가 메뉴 목표를 달성하는 위주로 진행되게 목표잡기와,
> 다른 트랙의 일을 집어와서 여기에 섞지 말라"***

**두 가지 명령이다. 둘 다 지켜야 한다.**

### ① 목표잡기 — 세션의 슬라이스는 「SellC 원가 메뉴 목표」에서 «역산»해 잡는다

착수 시 세울 질문은 「무엇을 할까」가 아니라 **「6항목 중 어느 것을 [x]로 만들 수 있는가」**다.
- 앵커 `이번 세션:` 줄에 **겨냥한 합격항목(A1/A2/C1/C2)을 반드시 적는다.** 어디에도 안 걸리면
  그 자체가 표류 신호다 — 착수 전에 자백하고 Jino에게 확인한다.
- **진행률을 못 올리는 작업(조사·수리·리팩터)이라면 「왜 이것이 다음 [x]로 가는 최단 경로인가」를
  한 줄로 적는다.** 못 적으면 그 작업은 이번 세션 몫이 아니다.
- 진행률 N/M은 **증거 좌표(커밋·PR·라이브 관측) 없이 올리지 않는다.** 체크박스는 «주장»이고
  판정은 종결 QA가 한다.

### ② 섞기 금지 — 다른 트랙의 일을 이 트랙으로 끌고 오지 않는다

이 트랙의 «안 함» 목록은 **경계이지 할 일 목록이 아니다.** 아래는 **소관이 다른 트랙**이고,
발견해도 **고치지 말고 적기만** 한다(앵커 `## 이월`에 **소관 트랙 파일명을 지목해** 한 줄):

| 끌어오지 말 것 | 소관 |
|---|---|
| 프로모션 분담금·즉시할인 손익 | `track_coupang-promo-pnl.md` |
| 광고비 배분·광고 최적화(PAO) | `track_naver-ad-optimization.md` |
| 상품↔채널 매핑의 커버리지·충돌 판정 | `track_product-connection-map.md` |
| 쿠팡 2P 손익 정합 | `track_coupang-2p-parity.md` |
| 부가세 축 변경(D-NAO-150 뒤집기) | **새 계약**(Jino 승인 지점) |
| 수입건 원장 자체의 결함·소급 적재·자금정산서 파서 | 계약 B(같은 트랙이나 **다른 계약**) |
| AI_office 수입원가 자산과의 통합·폐기 | **Jino 결정 대기** — 어느 쪽도 상대를 전제하지 않는다 |
| 배포 스크립트·훅 등 하네스 결함 | 하네스(별건) |

★**「이왕 보인 김에」가 이 병의 이름이다.** 각 단계가 국소적으로 정당해서 금지문이 안 걸린다
(A를 고치려면 B가 필요해 보이고, B엔 C가…). 그래서 **자백을 먼저 출력하는 것**이 규율이다:
`↗️ 스코프 밖: <이유> / 목표: <「SellC 원가 메뉴 목표」>`.
자백 후 진행은 허용된다 — 차단이 아니라 **가시화**다.

★**이 지시가 나온 배경**: 전역 규율이 Jino 원문으로 이렇게 적고 있다 —
*"처음에 목표한 일이 끝나도 프로젝트 내의 다른 트랙의 업무를 끌고와서 마치 자기일인것처럼
일을 하고 있고"*. 이번 지시는 그 규율을 **이 트랙에 명시적으로 못박은 것**이다.

### 목표의 정본
`.claude/memory/chains/sellc-원가-메뉴.GOAL.md` — **목표를 다시 쓰지 말고 그 파일을 열어
앵커 `목표:`에 원문을 옮긴다.** 트랙 헤더와 갈라지면 **카드가 이긴다.**

---

## 1. 작업 목적 (원문 — 발명 금지)

Jino 2026-08-22:
> "SellC에 원가 메뉴를 하나 붙이자. 여기에는 우리가 현재 알고있는 모든 원가가 있잖아? 그 원가를 가지고
> 계산되는 방법이 첨부된 파일처럼 나오고 그 계산이 되면 되지 않을까? 그리고 수입건에 대해서는 두번째,
> 세번째 첨부자료처럼 수입한 금액을 알려주면 너가 이런것들을 모아서 회계적으로 계산되는 원가를 만들면
> 되잖아. 그리고 그런 원가를 만들려면 재고사항이 필요할꺼고, 그건 ecount와 연결하면 될꺼고."
> "목표는 정확한 원가 산출이니까?"

이번 세션 몫: **계약 A′(층1 표준원가) 승인 + 슬라이스 S1**.

## 2. Jino 결정 5건 (전부 이 세션에서 받음 · 원문 보존)

| 결정 | 원문 | 번호 |
|---|---|---|
| A′ 착수 | "A로 하자" (21:13) | — |
| **단가 출처** | *"우리가 강화유리 정보에서 읽는 수입서류에 나와있는 부자제 단가를 참고해"* (21:13) | — |
| **부가세** | *"부가세는 우리 기준에서는 비용으로 넣자. 나중에 환급을 받게 되긴 하지만, 수입때의 부가세는 그냥 우리 비용으로 잡자"* (21:41) | **D-CPP-51** |
| **환율** | *"환율도 그 표를 만들때의 기준이었어. … 고정된 환율을 쓰지 말고 수입시의 환율이 적용된 정확한 원가를 만들자"* (21:4x) | **D-CPP-52** |
| **레시피 키** | *"두번째 첨부된 엑셀파일에 있는 상품명의 구조로 만들어주는건 어떨까? 상품명이 같으면 들어가는 부품도 같거든"* + *"플립, 폴드 제품은 하나의 상품명에서 단가가 달라져"* (21:5x) | 계약 §0-B |
| 계약 승인 | "진행하자" (22:22) | **D-CPP-53** |

부수 결정: 필름 원단은 조아테크 대량 매입 → 1장/2장 제품 둘 다 만든다(「태블릿 표 2벌」 이상치가 **이상이 아님**으로 닫힘) · 레시피 입력 = 「엑셀 읽어 초안 → 화면 승인」.

## 3. 규범 근거 — 1차 출처 대조 완료 (§3 금지선 준수)

Jino 지시 *"이걸 회계적으로 어떻게 처리하는지 확인을 해서 강화유리와 같은 기준으로 원가 기준을 세우라고"*에 대한 답. **웹검색 요약이 아니라 원문을 직접 받아 확인했다.**

- **일반기업회계기준 제7장 7.6**(한국회계기준원 공식 PDF): "매입원가는 매입금액에 매입운임, 하역료 및 보험료 등 … 부대원가를 가산한 금액이다. … **성격이 상이한 재고자산을 일괄하여 구입한 경우에는 총매입원가를 각 재고자산의 공정가치 비율에 따라 배분하여** 개별 재고자산의 매입원가를 결정한다."
  ⇒ **계약 B의 배부 로직이 곧 이 조문의 구현**이다. 「강화유리 기준」은 **새로 세울 게 아니라 이미 서 있고**, 부자재도 같은 원장을 탄다.
- **7.14 표준원가법**: "실제 원가와 유사한 경우에 편의상 사용할 수 있다. … **정기적으로 검토하여야 하며 필요한 경우 현재 상황에 맞게 조정하여야 한다.**"
  ⇒ ★**A′의 회계적 명분이자 제약.** 현 엑셀 표준 2,820.64 고정 vs 실제 2,800.10~3,291.70(평균 **+7.3% 과소**)은 **이 조건 미달 상태**다.
- **7.13 / 실7.10**: 단위원가 = 선입선출·가중평균(총평균/이동평균)·후입선출. 적용방법의 **일관성 유지**.
- **법인세법 시행령 §72②1호**: 취득가액 = "매입가액에 취득세 … **그 밖의 부대비용**을 가산한 금액" — 조문에 「관세」·「부가가치세」·「매입세액」이라는 **단어 자체가 없다**.
- **§74**: ①평가는 **관할세무서장에게 신고한 방법** ②자산별·종류별로 다른 방법 가능 ③변경은 적용 사업연도 종료일 **3개월 전** **④무신고 시 「선입선출법」**.
  ⇒ ★**「어느 로트 단가를 쓰는가」는 모델이 고를 수 없다** — 회사가 무엇을 신고했는지가 정한다.

★**Jino 지시(21:4x)**: *"세무사 확인을 하지 말고, 온라인에서 권위있는 곳에서 확인을 해보자. 국세청이나 관련 지식을 알 수 있는 국가 웹사이트등에서"* ⇒ 확인 경로는 세무사가 아니라 **우리 법인세 신고서의 「재고자산 평가조정명세서」(시행규칙 별지 제39호서식) 「③신고방법」 칸**(홈택스 → My홈택스 → 신고내역). §74⑦이 그 서식 제출을 의무화한다. **미확인 1건**(계약 §9-1).

## 4. 완료 QA — 판정 원문 그대로 (앵커 `.claude/anchors/bd4adba0-2ada-416e-9c6b-3f9c98b982a3.md`)

```
판정(계약 A′ §7 합격기준): 달성 — S1 판정 대상 3항목(합격 1·2·9) 전부 prod 라이브 관측.
  합격 1 = 부자재 탭에 cleaning kit 로트 2건이 서로 다른 값으로(id=1 190.82원 / id=2 178.78원,
  VAT 포함 209.9 / 196.66) + 양쪽 「원장과 일치」·lot_count:2·stale_count:0
  합격 2 = product_master 963행·SUM 3642052.09999999·AVG 3781.98556593976 배포 전후 완전 동일,
  health 16키·cost_drift: null 동일
  합격 9 = 「원가=부가세 포함(D-CPP-51)」 배지 실재·기본 표시 inc_vat
  합격 3~8은 S2·S3 몫이라 판정 대상 아님 (2026-08-23 11:4x KST)

판정(Jino 지시 원문): 부분달성 — 달성: 「SellC에 원가 메뉴를 하나 붙이자」·수입금액 입력층·
  21:13 ②·21:41 부가세·21:4x 환율·21:5x 상품명×폼팩터 키.
  미달(미착수): 「계산되는 방법이 첨부된 파일처럼 나오고 그 계산이 되면」(S2) ·
  「재고사항 … ecount와 연결」(계약 C) (2026-08-23 11:4x KST)

판정(트랙 궁극 목표): 부분달성 — 진행률 2/6 불변(B1·B2만 [x]). A1으로 가는 선행 인프라를
  prod 라이브에 올렸으나 «정확한 원가 산출»엔 아직 못 닿았다 — A1은 S2 이후다 (2026-08-23 11:4x KST)
```

> **QA가 확인 못한 것(원문 그대로)**: `product_master` **행 단위 완전 다이제스트 미확인**(배포 전 풀스냅샷이 없어 집계 3종 일치로만 검증 — 서로 다른 두 행이 합·평균을 보존한 채 바뀌었을 가능성은 원리적으로 배제 못 함) · PR #339 OPEN 상태를 `gh`로 직접 재확인 안 함 · 레시피/보드 탭 렌더 미확인(S1 판정엔 불필요).
> ★**QA의 「prod 무인증 노출」 관찰은 오해로 판명**: QA가 *"현재도 외부 무인증 접근 가능"*이라 적었으나, 실측 결과 우리 egress IP `115.23.234.145`가 `/etc/nginx/snippets/ohisell-allowlist.conf:11`(「Jino 노트북」)에 **허용목록 첫 줄**이고 vhost는 2026-08-13 결정대로 `satisfy any` + `auth_basic`이다 — **허용 IP라 통과, 새 노출 아님.** 원인은 내가 위임문에 「BasicAuth라 못 볼 수 있다」는 **잘못된 전제**를 실은 것.

## 5. 트랙 진행률

- 트랙: `docs/tracks/active/track_cost-truth-ledger.md`
- **시작 2/6 → 종료 2/6 (불변)**
- 달성 유지: B1(수입건 원장 스키마+3중 검산 prod 존재) · B2(8/18 실건 라이브)
- 미달: **A1**(구성 계산 결과가 prod 관측 — **S2 몫**) · **A2**(폼팩터 이식 — S3) · C1(재고 로트 원장) · C2(평가방법 확정 + `cost_price` 컷오버)
- 이번 세션이 움직인 것: **체크박스 0개**. A1으로 가는 선행 인프라(스키마 7테이블·부자재 층·원장 단가 연결·화면)를 prod에 올렸다. 증거: PR #339 `aab20a2a` · prod alembic `cst2snap53b` · 라이브 `lot_count:2`
- 트랙 목표 원문: 위 §1과 동일
- 다음 세션 후보: §6

## 6. 다음 세션이 먼저 볼 것

### 6-1. ★Jino 답 대기 1건 — S2/S3의 정밀도를 정한다
상품명 하나에 옵션이 **최대 163종**(갤럭시Z 56 · 아이폰 45 · 갤럭시S 34 · 갤럭시A 17 · LG V40까지) 붙는다. **부품 «종류»는 같아도 필름 «면적»은 기종마다 다르다.** 계약은 **폼팩터**(플립/폴드/트라이폴드/태블릿 3구간 등 9종)로 가르기로 했으나, **같은 폼팩터 안의 기종 차이**는 열려 있다. 이건 D-CPP-27의 「공통 2,350.7 vs 기종별 2,616」 논쟁의 재출현이다.
※ 답이 없어도 S2는 «상품명당 원가 1개»로 착수 가능하다. 답은 A2의 정밀도를 정한다.

### 6-2. S2 착수 (계약 §6)
> **S2 — 레시피 파싱·승인 + 조립형 표준원가 (트랙 A1)**: 두 엑셀 업로드 → 상품명×폼팩터 구성 초안 → 계열별 승인 화면, 순수 SA 계산, 일반(`bar`) 폼팩터 표준원가 보드 관측.

합격 3·4·5(계약 §7). 기대값 **2,350.7원**(지문방지 TPU 3매, D-CPP-31 정본 대조값)이 **링크된 서로 다른 SKU 2건 이상에서 같은 값으로** 관측돼야 한다.

### 6-3. 이월 (고치지 않고 운반)
- ★**`scripts/safe_deploy.sh`가 prod에 없는 신규 디렉터리를 만들지 않는다** — 이번 배포에서 `backend/app/services/cost_menu/`가 없어 scp 4건이 실패했는데 **그 시점에 `main.py`는 이미 그 패키지를 import하도록 배포된 상태**였다(재시작됐으면 앱이 죽는다). 스크립트가 재시작 전에 멈춰 구 프로세스가 살아 있었고(HTTP 200 확인), `mkdir -p` 후 재배포로 완주. **다음에 새 패키지를 만드는 슬라이스는 이 함정을 다시 밟는다.** 소관: 배포 스크립트(별건).
- ★**훅 사각**: `track-progress-sync.sh`가 앵커의 `트랙:` 상대경로를 **공유 메인 폴더 기준**으로 풀어, 워크트리에서 트랙에 `확인:` 줄을 정상적으로 남겨도 **누락으로 판정**한다(이 세션 내내 발동). 워크트리 작업이 표준이 된 지금 «워크트리 우선 조회»가 필요하다. 소관: 하네스(전역 훅).
- **AI_office 수입원가 트랙 중복**(`track_import_cost.md`·`import_payable_ledger`·`compute_landed_cost`·`hs_reference`·ECOUNT 기표 harness 2종) — SellC 원장과 데이터 흐름 0건. **Jino 결정 전엔 어느 쪽도 상대를 전제하지 않는다.**
- **`scripts/next_ids.sh`가 D-CPP를 발급하지 않는다**(D-NAO·교훈만) — 번호 충돌의 구조적 원인. 이번엔 origin/main 대조로 수동 부여(51·52·53).
- 계약 §9 이월: **§9-10 전역 `PRAGMA foreign_keys` 검토**(SQLite가 FK를 강제 안 해 CASCADE가 선언만 되고 안 돎) · **§9-11** 단가 없는 라인 거부 무테스트(현재 도달 불가) · **§9-12 prod 데이터 결함(계약 B 소관)** — 수입건 id=1의 관세 비용줄에 `is_duty` 표시가 없어 무관세여야 할 cleaning kit이 관세를 배부받는다(현행 **190.82** vs 올바른 입력 시 **182.17**, 격차 6.7% 중 약 4.5%p가 입력 산물) · **§9-13** 수동 단가를 `ex_vat`만 입력 시 「최신 단가」가 「—」.
- **cleaning kit 168원 ↔ 엑셀 부자재 항목 대응 불명** — 화면이 「엑셀 대응: 미확정」으로 스스로 말하고 있다. 계산은 원장 값으로 도므로 막히지 않는다.
- 자금정산서 파서 미구현(해상운임 누락 동반) · 13개월 소급 적재 — 둘 다 새 계약 필요.
- Nora(AI 회계) Slack 문의 미발송 · Jino 언급 「첨부 이미지」 미수신.
- 훅 오탐 1건(`review-surface-mutation.sh`가 리뷰 아닌 수정 위임문에 발동 — 계약 §6 계수 대상). 그대로 재전송하지 않고 **표면 변이 요구를 실제로 추가해** 통과시켰다.

## 7. 적대 리뷰 — 1R FAIL → 2R PASS

**1R P1 2건은 뿌리가 하나**였다: `cost_material_price`의 `source='ledger'` 행이 **연결 시점 값을 복사해 두고 그 뒤 재검사하지 않는다.** 네 갈래로 터졌다(전부 리뷰어가 재현):
1. reopen → 원장은 단가를 지우는데 화면은 낡은 값을 「최신 확정 로트 단가」로 표시
2. 환율 정정 후 원장 198.91인데 화면 190.82, **재연결은 409로 막힘**
3. 수입건 삭제 → `ondelete="CASCADE"` 선언에도 **고아 잔존**(`PRAGMA foreign_keys` 미설정, prod도 SQLite)
4. **라인 순서 정정 시 SQLite rowid 재사용** → cleaning kit의 근거가 `Glass_iP12promax`로 바뀜

**수정**: 근거 보존은 유지하되 **조회 시점 재검사**(`ledger_check.py` 순수 SA, 6값 `ok/manual/missing/unconfirmed/changed/item_mismatch`) + **스냅샷 2칸**(`linked_item_name`·`linked_shipment_id`, 마이그 `cst2snap53b`) + `refresh` 엔드포인트(409 해소, `item_mismatch`는 거부). **어긋난 행은 이력에 남되 「최신」 자격만 잃는다.**

**2R P2 채택 2건**(`2fc2144a`): 수입건 신원도 대조(다른 서류가 rowid를 물려받는 경우 → `item_mismatch`로 흡수, 값 증식 안 함) · 구 연결(스냅샷 NULL)은 **「대조 못 했다」를 자백하고 갱신 거부**.

### ★★표면 절단 변이 — 다섯 번째 재현, 처음으로 브레이크가 생겼다
1R에서 리뷰어가 **좌측 「💰 원가」 메뉴를 통째로 지웠는데 프론트 테스트 21건이 전부 초록**이었다. 호출부 제거·라우트 제거·메뉴 제거 **4종 전부 SURVIVED**. 저장소가 **다섯 번째**로 밟은 병이다.
⇒ `frontend/src/pages/costPageReachesTheUser.test.tsx` 신설(`App`을 `/cost`에서 **통째로** 렌더 — 라우팅·레이아웃·페이지·호출부가 한 줄로 이어져야만 통과). 2R에서 **13종 전부 KILLED**(새 자백 표면 5종 포함). **경고가 사라져도 초록인 케이스 0건.**

## 8. 산출물 좌표

- **PR #339** https://github.com/Jino00/ohisell/pull/339 — 병합 `aab20a2a` (2026-08-23T02:51:28Z)
  ⚠️ `safe_merge.sh 339 --force` — CI 3잡이 2~3초 fail이나 **`steps: 0` + 로그 blob 부재**로 GitHub Actions 결제 정지 서명을 **직접 확인**한 뒤 강제. 자백은 `$TMPDIR/safe_merge.log`.
- 커밋 8개: `a72fb8c7`(S1 구현) → `6b9a7302`(1R P1) → `767acdd6`(트랙) → `2fc2144a`(2R P2) → `3be8caa6`(origin/main 병합) → `f3ba27af`(트랙) → `1c6e9900`(QA 판정) → 병합
- **prod 배포**: 백엔드 2026-08-22T22:52:35Z 무중단(:8011→:8001, 다운타임 0초) · 프론트 22:54:08Z(CAS 통과 1회) · alembic `duty50attrib → cst1menu53a → cst2snap53b`
- 계약 정본: `docs/PLAN_cost-menu-standard-cost.md` (464줄+)
- 신설 파일: `backend/app/routers/cost_menu.py` · `backend/app/services/cost_menu/{__init__,matcher,materials,ledger_check}.py` · `frontend/src/pages/CostPage.tsx` · 테스트 5종
- 테이블 7개: `cost_material` / `cost_material_price` / `cost_recipe` / `cost_recipe_line` / `cost_recipe_link` / `cost_standard` / `cost_setting`
- 테스트: 백엔드 **2 failed / 6,244 passed**(실패 2건 사전 존재 — `test_health_partial_sync`·`test_vendor_item_axis`, 신규 0) · 프론트 540 · `tsc -b` 통과

## 9. ★배포 직전에 피한 사고

배포 파일 목록을 뽑다가 **내가 안 건드린 `naver_ad/reflection_*`가 섞여 있는 것**을 발견했다. 확인하니 브랜치가 `origin/main`보다 **17커밋 뒤**였고 병행 세션이 D-NAO-228을 푸시한 상태였다. **그대로 배포했으면 그 세션 작업을 prod에서 되돌렸다.**
⇒ 병합(충돌 1건 `docs/TRACKS.md` — 양쪽 트랙 항목 **둘 다 보존**) → 회귀 재실행 → 배포.
**교훈**: 배포 전 `git diff --name-only origin/main..HEAD`에 **내가 모르는 파일이 있으면 그것이 곧 뒤처짐 신호**다.

## 10. 못 한 것

- **브라우저 화면을 내가 직접 못 봤다** — Jino가 스크린샷으로 확인해 줬고, 완료 QA가 라이브 DOM을 직독해 대체했다.
- `product_master` 행 단위 다이제스트(§4 QA 원문).
- S2·S3(레시피·계산·폼팩터 이식) — 계약상 이번 슬라이스 밖.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
