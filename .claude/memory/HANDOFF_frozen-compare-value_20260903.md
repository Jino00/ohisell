# HANDOFF — sellc-원가-메뉴 n=32 (세션 d68149be · 2026-09-03 16:30 ~ 09-04 10:5x KST)

> 목표이름: **엑셀정합 목표** (트랙 `docs/tracks/active/track_cost-truth-ledger.md`)
> 목적 원문(Jino 2026-09-03 16:08): *"다음 세션에서 전체적으로 부자재와 레시피의 가격을 검토해볼 수 있게 방법을 알려줘."*
> 이어서(17:01): *"그럼 이제 우리 직원이 부자재와 레시피 가격을 확인하기 위해 어떻게 확인하면 돼?"*
> ⇒ 검토 주체는 **내가 아니라 직원**이고, 수단은 **화면**이다. 이 한 줄이 세션의 축을 정했다.

**검증 명령:**
```
ssh sellc.ohitech.co.kr "curl -s http://127.0.0.1:8011/api/cost/recipes" | python3 -c "import json,sys;d=json.load(sys.stdin,strict=False);r=[x for x in d['items'] if x['id']==40][0];print('매칭근거',r['match']['excel_total_inc_vat'],'/ 고른항목',r['picked']['item_total_inc_vat'])"
```
- **현재(prod 미배포 상태)**: `매칭근거 12.20 / 고른항목 3102.70` ← **두 값이 다르면 아직 배포 전이다**
- **배포 후 기대**: `매칭근거 3102.70 / 고른항목 3102.70` (두 값이 같아진다)
- ⚠️활성 포트가 배포마다 **:8011 ↔ :8001**로 뒤집힌다 — 둘 다 시도할 것.
- 착수 시 판별표(16:33 실측, 인계 n=31과 일치): `계산값 529 · 보류 11 · 정본없음 40 · 컷오버대상 0`

## 0. 한 줄 — **인계가 「진짜 이상 2건」이라 넘긴 것은 둘 다 이미 판정된 것이었고, 진짜 결함은 그 옆에 있었다.**

ref 130 §6-F가 1·2순위로 지목한 두 건(`패키지 (fold)` 320 vs 370 · EZ툴 CNY)은 **판정 완료분**이다.
그 자리에서 실제로 고장 나 있던 것은 **「얼린 대조값이 재업로드를 못 따라간다」**였다.

## 1. ★Jino가 내 오독을 잡았다 — 이 세션의 가장 큰 산출물

착수 보고에서 나는 ref 130을 **그대로 옮겨** 「미판정 2건」이라고 말씀드렸다. Jino의 반문:

> *"위의 2가지는 이미 결정된거 아니야?"*

실측 결과 **맞았다**:

| 건 | 판정 시점·원문 | 좌표 |
|---|---|---|
| `패키지 (fold)` 320 | Jino 화면 확인 **2026-08-28 18:59** — *"여기에 나오는 320원이 맞는거야"* | `HANDOFF_cost-purchased-goods-axis_20260828.md:109` |
| EZ툴 CNY | **2026-08-27 실측 확정**(추론 아님) — 「단위 오표시」로 코드 좌표까지 특정 | `HANDOFF_glass-cost-live_20260827.md:120·124` |

★그리고 **09-02 인계에 「EZ툴은 Jino가 확인해야 한다 → 실제: 셋 다 원장에서 계산값이 나온다 / 잡은 쪽: Jino」**가
**이미 표로 적혀 있었다**(`HANDOFF_cost-cutover-executed_20260902.md:241`). 같은 오독이 **두 번째**다.

⇒ **교훈**: 인계 문서의 「미해결」 목록은 그 자체가 실측 대상이다. n=31이 성실히 쓴 플레이북조차
**판정된 것을 미판정으로 되돌렸고**, 나는 그걸 검증 없이 Jino께 옮겼다. 잡은 건 내 규율이 아니라 **Jino의 반문**이다.

## 2. 진짜 결함 — 얼린 대조값 (PR #697)

`pick_cost_table_item`이 픽 **시점**의 `item.total_inc_vat`를 note에 **얼려** 넣는다.
그런데 원가표 파서는 **2026-09-01에 고쳐졌다** — 수입 섹션에서 D열 「제품원가(CNY)」가 아니라
I열 「상품원가」를 읽도록(`recipe_parser.py:316·392-395`). 08-27에 픽된 레시피는 그 **외화 단가**를
note에 든 채 남았고, 화면 **한 자리에서 같은 항목이 두 값**으로 섰다.

```
원가표 「오타오_투명 강화유리 (New Ver.)」 · 제품원가(+VAT)  12.2원    ← 매칭 근거 (얼린 값)
고른 항목 「오타오_강화유리필름/오타오_투명 강화유리」 · 제품원가(+VAT)  3,102.7원  ← 고른 항목 (지금 값)
```

보드의 `excel_gap_pct`도 그 얼린 값으로 나눠 **「격차 5,000원대」**를 만들었다 — ref 130이 「진짜 이상」이라 부른 그 숫자다.

**영향 범위 실측(전건)**: 픽된 38개 중 **어긋남 정확히 2개**(r40·r41) + **r39 1개**(다른 이유, §3). 36개는 일치.

**수리** — `_excel_total_inc_vat(recipe, saved)` 하나를 신설해 **두 소비처가 같이** 쓴다:
①`recipe_payload`의 `match`(사람이 읽는 층) ②`board()`의 `excel_gap_pct`(값이 도는 층).
얼린 값을 **고쳐 쓰지 않고 읽을 때 파생**한다 — 정본은 엑셀이고 재업로드가 항목 값을 바꾸므로,
파생이면 저절로 따라간다. **마이그레이션 0 · 프론트 0줄**(타입이 이미 `null` 허용).

## 3. r39는 다른 이유였다 — Jino가 준 사실이 확인해 줬다

> Jino(17:29): *"r39 EZ툴 먼지제로 제품은 Otao에서 수입하는 삼성 갤럭시 강화유리야."*

셋 다 `recipe_kind=imported_goods`이고 원가 라인의 **`price_source = ledger`**다 —
**원가는 이미 수입 원장에서 나오고 원가표가 아니다.** r39의 픽된 항목은 `total_inc_vat`가 **null**이다
(엑셀이 그 행의 환산·부대비를 아직 안 채웠다). ⇒ 「엑셀 총액 없음」이 **참**이고,
옛 19.30을 그리는 것이 거짓말이었다. 같은 수리가 세 건을 다 옳게 만든다.

## 4. ★덤 — 얼린 값을 걷어내니 상수가 드러났다 (미처분, Jino 판단 대기)

|  | CNY | 원장 (ex-VAT) | 원/CNY | 엑셀 (ex-VAT) | 원/CNY |
|---|---|---|---|---|---|
| r39 갤럭시 먼지제로 | 19.3 | 4,584.75 | 237.55 | — | — |
| r40 아이폰 맥스 클리어 | 12.2 | 2,909.96 | **238.52** | 2,820.64 | **231.20** |
| r41 아이폰 사생활보호 | 19.2 | 4,579.62 | **238.52** | 4,439.04 | **231.20** |

**엑셀 231.20원/CNY vs 원장 238.52원/CNY — 두 건이 소수점까지 같은 비(1.0317).**
행마다 다른 게 아니라 **환산율 하나가 다르다**(환율 또는 부대비율). 원장이 실적이므로 원장이 맞고
엑셀이 **3.17% 낮게** 잡혀 있는 것으로 보인다(🧠 추론 — 엑셀 파일을 직접 안 열었다).
⇒ **원가표 파일 소관이라 Jino 판단 대기.** 배포 후 보드 「엑셀 대비」는 r40·r41에서 **+25,000%대 → +3.17%**로 바뀐다.

## 5. 적대 리뷰 — 1R FAIL(P1 1) → 2R PASS

**P1**: `confirm_cost_table_absent`가 note의 흔적을 안 거둔다. 핀이 끊긴 뒤(`pin_lost`) 사람이
**화면이 시키는 대로** 「원가표에 없음」을 확인하면 `picked`는 `state=absent`인데 `match`는
**얼린 옛 값을 그대로** 들고 있었다 — 내가 세운 원칙(「얼린 값으로 메우면 다시 거짓말한다」)이
`picked_item is None`인 **모든 갈래에 닿아야 하는데 이 갈래 하나가 빠졌다.**
`unpick_cost_table_item`은 이미 거두는데 **여기만 짝이 없었다.**

★**지적보다 한 칸 넓게 고쳤다** — 남은 건 값만이 아니라 `cost_table_item`·`cost_table_section`도였다.
즉 「원가표에 **없다**」고 방금 판정한 화면이 그 **항목 이름까지** 계속 말하고 있었다.
2R 리뷰어가 그 판단을 지지: *"값만 지웠으면 「원가표 「죽은 항목명」」이라는 이름만 남은 반쪽 유령 문장이 남았을 것"*.

- 2R가 **자기 1R 재현 스크립트를 그대로 재실행**해 12.20 소멸을 확인(이 라운드의 핵심 증거).
- **변이 누적 7종 전건 사망** — M1~M6(리뷰어) + M7(P1 수정). 표면 절단 3종 포함.
- ★**M3이 1차에서 살아남았다** — `board()`의 `excel_gap_pct` 경로가 테스트에 없었다. 저장소가
  반복해 밟는 자리(「값이 도는 층과 사람이 읽는 층」 중 한쪽만 지킴)라 **테스트를 그 층에 추가해 사살**했다.
- 리뷰어 P2 2건은 **기각**(malformed note 경로 동작 불변 · `picked_item`은 `lazy="selectin"`이라 N+1 없음).

## 6. 직원용 산출물 — Jino 지시 「파일로 만들어줘」

- **PDF**: `원가확인안내_20260903.pdf` (A4 2장 · 헤드리스 Chrome 인쇄용 HTML로 별도 조판)
- **웹 페이지**: https://claude.ai/code/artifact/163bd2bd-7854-43ba-9052-0eb708755a1c
- 원본 HTML 2벌은 세션 스크래치패드에 있다(repo에 안 넣었다) — 재생성 필요하면 이 HANDOFF의 수치로 다시 짜면 된다.

담은 것: ①홈 인박스가 곧 확인 목록 ②부자재 「단가 출처」 3등급(**원장 4 / 등록가 94 / 없음 41**)
③레시피 「계산 내역」 ④⛔ 눌러선 안 되는 것 둘 ⑤⚠️ 지금 화면이 잘못 말하는 것 둘.

★**문구를 한 번 정정했다**: 초판은 12.2를 「위안(CNY)」이라 적었는데 조사 결과 **위안이 아니라 옛 값**이었다.
PDF·웹 둘 다 「옛 값 / 맞는 값」으로 맞췄다.

## 7. ⚠️ 다음 세션이 먼저 볼 것

0. ★★**prod 배포가 «안 됐다»** — PR #697은 머지됐으나 **백엔드 배포 0건**이다(Jino 승인 대기 중 세션 종료).
   위 검증 명령이 `12.20 / 3102.70`을 내면 아직 미배포다. 배포하면 세 값이 한 번에 바뀐다.
   `scripts/safe_deploy.sh backend/app/services/cost_menu/recipes.py --restart` · 마이그레이션 **없음**.
1. ★★**②(a) `anomaly_flag` 40자 잘림 — 미수리.** `CostRecipe.anomaly_flag`가 `String(40)`이고
   `recipes.py:696`·`:1189`가 **명시적으로 `[:40]`으로 자른다.** 실측: 온전한 문구는 **100자·모순 3건**인데
   레시피 사본은 **둘을 통째로 버리고** `price_co` 조각을 남긴다.
   ```
   온전(CostTableItem.anomalies, String(200)):
     price_conflict:부착 안내문:55.0≠30.0,price_conflict:비닐 (16*23+4):15.0≠10.0,price_conflict:패키지:320.0≠171.0
   잘림(CostRecipe.anomaly_flag, String(40)):
     price_conflict:부착 안내문:55.0≠30.0,price_co
   ```
   ⇒ 컬럼을 `String(200)`으로 넓히고 `[:40]` 제거(마이그레이션 필요 · prod alembic은 `grainv1s1a (head)`로 **깨끗**).
   ★단, **이미 잘려 저장된 45·97은 다음 원가표 업로드 때 채워진다** — 마이그레이션이 지난 문자열을 되살리진 못한다.
2. ★★**②(b) 「모순 2건」 처분 — Jino 미답.** 셋 다 이미 판정된 값(55/15/320)이고 **적용값은 이미 맞다.**
   틀린 건 인박스가 「사람이 고른다」며 **끝난 판정을 다시 묻는 것**이다. 선택지 셋을 드렸으나 답을 못 받았다:
   - **ⓐ** 원가표 파일을 55/15/320으로 맞춘다 (근본 해결 · 파일은 Jino 소관)
   - **ⓑ** 판정을 DB에 기록해 접는다 (컬럼 추가 + 마이그레이션)
   - **ⓒ** 문구만 「파일이 두 값 — 판정 끝, 적용값 55/15/320」으로 바꾼다 (코드 최소)
3. ★**환산율 231.20 vs 238.52 (§4)** — Jino 미답. 확인할지 기록만 할지.
4. **적대 리뷰 이월 1건**: `picked_by_human`이 absent 확인 후에도 `true`로 남는다.
   `unpick`엔 짝이 있는데 여기만 없다 — 사소한 불일치, 범위 밖이라 이월.
5. ⚠️**프론트 미배포 유지** — prod가 다른 세션의 미푸시 커밋 `9a28224e`에서 배포돼 CAS 거부 중.
   이번 수리는 **백엔드만이라 프론트를 안 건드린다**(그래서 이 순서가 안전했다).
6. ⚠️**업로드 가드는 여전히 08-07 스냅샷** (n=31에서 이월 그대로).
7. ⚠️**`[중복]` 2건**(OHI-0901 332,000원 · OHI-0905 222,600원) — n=28부터 **다섯 세션째** 미처분.

## 8. 완료 QA

**해당 없음** — 이 세션은 **M 규모**다(2파일·한 세션·계약 없음). 전역 §1대로 **적대 리뷰 1회**로 충족했고,
§8은 완료 QA를 **L 종결 시 1회**로 못 박는다. 대신 **내가 검증하지 못한 것을 여기 적는다**:

- **prod 라이브 증거 0건** — 코드·테스트·리뷰는 통과했으나 **배포를 안 했으므로 화면은 아직 옛 값을 말한다.**
  「됐다」라고 말할 수 있는 것은 **격리 성공까지**이고, 라이브 증거는 다음 세션 몫이다.
- 직원 PDF의 수치는 **09-03 17:0x 관측값**이다 — 원가표·매입가가 갱신되면 달라진다.
- §4의 환산율 3.17%는 **🧠 추론**이다(엑셀 파일 미개봉).

## 9. 트랙 진행률

**시작 3/6 → 종료 3/6 (불변)** — 달성 B1·B2·A1 / 미달 A2·C1·C2.
이번 작업은 **A2가 요구하는 플립/폴드·태블릿 구성 이식이 아니다**(대조값 표시 수리다). `cost_drift`도 여전히 `null`.

## 10. 하네스 관측

- ⚠️**착수 훅 `[체인] ⛔ pao-논의` 줄은 오탐이다** — 공유 메인 폴더가 `origin/main`보다 **1,000커밋+** 뒤진다.
  실제로 공유 폴더의 `sellc-원가-메뉴.jsonl`은 **2줄**이었고 `origin/main`은 **31줄**이었다.
  체인 정본은 반드시 `git show origin/main:<경로>`로 읽을 것. (n=31도 같은 관측을 남겼다 — **재현**.)
- ⚠️**내 앵커의 `합격:`이 체크박스가 아니라 한 줄 산문이었다** — 그래서 종료 블록의 「앵커 합격 체크박스 기준
  N/M」을 못 냈고 트랙 기준으로 냈다. 다음 세션은 앵커 `합격:`을 **체크박스 M개로** 쓸 것.
- prod에 `alembic`이 PATH에 없다 — `cd /home/ubuntu/ohisell/backend && .venv/bin/alembic current`.
- 원가 API 응답에 제어문자가 있어 `json.loads(..., strict=False)`가 필요하다(n=31 관측 **재현**).
