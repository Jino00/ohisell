# HANDOFF — PAO 화면 목표 · §7½ 3단계 완주 (pao-uiux n=9)

**검증 명령:** `curl -s "https://sellc.ohitech.co.kr/api/naver/ad/performance/day" | python3 -c "import json,sys; t=json.load(sys.stdin)['totals']; print(t['gross_profit_today'], t['gross_profit_known_campaigns'], t['gross_profit_lens_note'][:12])"` → 숫자 2개 + `보정 전 값입니다`가 나와야 한다. 마지막 값이 빠지면 이 인계는 낡았다(누가 되돌렸거나 재배포됐다).

날짜: 2026-09-04 11:58 ~ 14:5x (KST) · 세션 `3ac88848` · 체인 `pao-uiux` **n=9**
트랙: `docs/tracks/active/track_pao-ui-ux.md` (**10/22 불변**)
워크트리: `~/.claude-worktrees/ohisell/pao-uiux-n8` · 브랜치 `feat/pao-uiux-n9`
계약: `docs/contracts/CONTRACT_pao_console_uiux.md`(계약 2) · 설계서 `docs/references/122_pao_console_uiux_design_20260902.md`

---

## §0 목적 — 원문 그대로

Jino(2026-08-29): *"이 작업은 PAO의 UI/UX만 다루는 트랙으로 독립운영할꺼야"*
계약 2 승인(2026-09-02): *"일단 그렇게 진행을 하자. 그리고, 불편하면 수정하자"*
이번 세션 지시(2026-09-04 11:5x): **「회수하고 n=9 시작」 + 「§7½ 3단계」** / 자 선택 **「그날 카드의 자 그대로」** / **「머지 후 배포」**

## §1 한 것 — §7½ 3단계 5항목 전건

| 설계서 항목 | PR |
|---|---|
| 총이익 첫 칸(§4-1) | #709 |
| 결과 칸 총이익 델타 「+N원/−N원」, RPC 자는 접힘(§4-3) | #706 |
| 「채점 전 · D+14 · …부터」(§4-4) | #706 |
| 쓰기없음 배지(§4-4 — 문구는 실측으로 개정) | #706 |
| 실집행/연습 분리(§4-4) + 「(Ava 미분리)」(§4-2) | #706 |

**09-15 «전»이 완료 조건이었고 11일 여유로 닫혔다.**

PR **#706** 머지 `e8001d9f` · **#709** 머지 `c3e4c08e` · CI 각 3/3 · **적대 리뷰 두 PR 다 1R FAIL → 2R PASS**.

## §2 ★인계·설계서를 뒤집은 실측 3건

1. **n=8이 「선행 조건」이라 적은 것이 없었다.** *「총이익 델타 금액 미저장 → 컬럼 가산 선행」*은 틀렸다 — `actual_json`이 `before/after{clk,conv_amt,cost}`와 `lens{bep,gamma,cf}`를 이미 적고 `_gross_profit`이 그 넷의 순수 함수라, **스키마 변경 0**으로 채점 당시와 같은 숫자가 나온다.

2. **설계서 §4-4의 「456건 전건 dry_run」 전제가 반증됐다**(prod 읽기 전용, 창 08-24~30):
   `dry_run=1` → **`flight_pacing` 456** · `optimizer_change` 3 · `adgroup_scope_change` 1 / `dry_run=0` → `external_*` 256(전부 대행사 관측).
   그 456은 **정확히 한 종류**이고 `flight_loop.py:312`가 *"진단 기록 — 아무것도 바꾸지 않았다"*라 적어 둔 것이다 — **「연습 집행」이 아니라 「진단 기록」**. 그리고 `improvement_events` 모듈 헤더가 *"dry_run=False만 세면 03 자동운영 재가동 같은 가장 중요한 이벤트가 통째로 사라진다"*고 **「라이브 실측 함정」**이라 못 박아 뒀다.
   ⇒ 「연습(dry_run) — 계정에 안 나감」을 문자 그대로 쓰면 **한 일을 안 했다고** 말하게 된다 ⇒ 셋 모두에 참인 **「네이버 광고 API 쓰기 없음」**으로 바꿨다.
   ↗️ **설계서 §4-4 문단 재측정이 남았다** — 그 숫자가 3단계 존재 이유의 절반이다. 소급 수정 금지라 본문은 안 고쳤다.

3. **자(尺)를 두 번 잘못 잡았다가 되돌렸다** — 둘 다 「한 절을 더 읽어서」 잡혔지 내 규율이 아니다.
   - 결과 칸이 **상한 하나만** 썼다. `pao_scope_roster._profit_band`가 「있는 그대로」를 기본값으로 두고 **ref 93 §1 행 9**가 표시 전용 소비처를 「구간 양끝 병기」 **1순위**로 지정한다(Jino 2026-08-24 *"있는 그대로를 보여줘야 하는거 아니야?"*). 근거: 계정 30일 총이익 **보정 +5,963,568원 ↔ 미적용 −234,545원 — 부호가 갈린다.**
   - 성과 첫 칸은 **반대 방향**이었다: 「보정을 안 쓴다」의 근거가 프록시 매출에만 성립하는데 과거 날짜 입력은 `conv_amt`(그 계수의 정의역)라 다른 총이익과 **44% 갈렸다**(477,599 vs 687,654). 값은 Jino 선택대로 두고 **침묵만 없앴다**.

## §3 적대 리뷰 P1 4건 — 하나도 내가 미리 못 잡았다

전부 「값은 그럴듯한데 화면이 거짓말한다」 종류다.
1. 대행사 `external_*` 행이 「채점 전」이라 말했다 — 그 행은 `verify_date`가 없어 채점기가 **영영 안 본다**(같은 사건이 `agency_op`으로 잡히면 「채점 대상 아님」이라 원천에 따라 두 말을 했다).
2. `dry_run`을 「연습」이라 불렀다(위 §2-2).
3. 첫 칸이 **오늘 0원 쓴 정지 광고**의 프록시 매출을 더했다 — 리뷰 재현: 헤드라인 308,341원 중 **85%(262,231원)**. 같은 카드의 ROAS는 `spend<=0`에서 계산을 **거부**하는데 총이익만 값을 냈다(한 카드 안에서 자가 갈렸다).
4. 보정 여부를 침묵했다.

★**생존 변이가 최대 산출물**: 픽스처의 «아는 캠페인»이 **하나뿐**이라 `sum`을 `max`로 바꿔도 전건 초록이었다 — 합계를 잰다고 믿었는데 **합계를 안 재고 있었다**. 캠페인 둘로 늘려 2R에서 사망 확인.
변이 누적 **24종 · 최종 생존 0**(표면 절단 5종 포함 — 결과 칸 렌더 제거 · 요약 배선 제거 · 첫 칸 렌더 제거 · `sub` 배선 제거 · 호출부 제거).

## §4 배포 · 라이브 증거 (Jino 승인 「머지 후 배포」)

백엔드 3파일 **CAS 3/3 · 마이그레이션 없음 · 무중단 다운타임 0초**(:8011→:8001, 스케줄러 리더 승격 확인) · 프론트 dist(백업 `dist_backup_20260904_1437`) · 배포 커밋 `c3e4c08e`(main 머지 커밋이라 다음 세션 CAS 조상 조건 충족).

**라이브 3계약**(14:3x~14:4x KST):
- 첫 칸 총이익 **28,297원** · 아는 광고 **12** / 뺀 광고 **35** · 「보정 전 …」 실림 · **known(12) ≤ 돌아간 광고(18)** 성립
- 결과 칸 상태가 실제로 갈린다: `no_api_write` **180**(예산 페이싱 관찰) · `pending` **13**(입찰가 변경 — 「채점 전 · D+14 · **2026-09-18**부터」) · `not_ours` **7**
  ★설계서가 하드코딩하라던 **09-15가 실제론 09-18**이다(행의 `verify_date`에서 읽게 한 것이 값을 냈다)
- `by_execution = {scope: ours, **api_write: 15**, no_api_write: 466}` — PAO 실제 네이버 쓰기 15건이 466건의 「쓰기 없음」과 **더는 안 섞인다**
- prod 번들 새 문자열 전건 실림 · `/naver-ad/performance` **200**

## §5 남은 슬라이스 · 이월

- ↗️ **설계서 §4-4 재측정**(위 §2-2) → 소관: 이 트랙
- ↗️ **전 구간 보정 적용 여부** — 성과 첫 칸을 보정 전으로 둘지 과거 날짜만 보정할지는 **Jino 결정**
- ↗️ **「하한으로도 흑자인가」** — `actual_json.lens`에 `factor_low`가 없어 못 잼 → 소관: `track_naver-ad-optimization.md`(처분 계약 `docs/PLAN_naver-profit-yardstick-review.md` 안3)
- ↗️ **계약 2의 10칸 처분** — 미체크 12 중 10이 「설계서에 무엇이 적혀 있는가」를 재는 항목이고 설계서는 n=3에서 완성·QA까지 끝났다. 구현 슬라이스는 원리적으로 그 칸을 못 켠다 ⇒ 계약 2 종결 판정 때 한 번에 처분할 자리. **그때까지 10/22는 실제보다 낮게 읽힌다.**
- ↗️ P2 이월 5건: 수집 공백일의 「0원」 단언 · 카드별 사유 미렌더 · 반올림 방식(`round` vs `ROUND_HALF_UP`) · 옛 표면 픽스처가 새 필드를 안 실어 `undefined` 렌더 · `sub`가 `lens_note`를 무조건 붙임
- **다음 슬라이스 1순위**: 설계서 §7½ **4단계**(계정 수준 두 면 + 탭 3개). **선행 소관 하나** — 「지금 PAO가 돌리는가」 표시값이 진리표 값이어야 하는데 그 계산 결함(`pao_scope_roster.py:351`) 수리는 **다른 트랙 소관**이다. 수리 전이면 그 값 옆에 「(진리표 미적용 — 행 값)」을 붙인다.

## §6 함정 (다음 세션이 밟을 것)

- ⚠️ **iCloud 공유폴더에서 `git worktree add`가 3회 연속 `fatal: mmap failed`** — 89%까지 갔다 죽기도 한다. 회수된 워크트리를 새 브랜치로 재사용하는 것이 우회로였다.
- ⚠️ **eslint 경고가 상한 96에 정확히 걸쳐 있다**(여유 0) — 경고 1건만 늘어도 CI가 빨개진다.
- ⚠️ 훅의 `[체인] ⛔ pao-논의 n=49`는 이 세션 내내 **오탐**이었다(공유 폴더 stale). 생존 판정은 `origin/main`의 등록부로.
- ⚠️ 병합 diff에 **다른 트랙 파일**(`models.py`·`cost_menu/recipes.py`)이 섞여 들어온다 — 배포 목록에서 빼야 한다.
