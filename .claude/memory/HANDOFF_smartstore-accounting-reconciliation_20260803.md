# HANDOFF — 스마트스토어 회계 정합 (2026-08-03, PR #170 병합 완료)

> Jino 발단: "sellc 대시보드에 나오는 판매·광고·이익을 믿을 수가 없어. 스마트스토어부터 정리해보자."
> 결과: 회계 오류 6건을 찾아 고쳤고, 최근 1주일 순이익이 **21% 부풀려져** 있었다.
> **PR #170 병합 = main `d05d358`, prod 배포 완료.**

## §0 가장 먼저 알아야 할 것

1. **대시보드 산술 자체는 틀린 적이 없다.** 매출−원가−수수료−배송비−광고비 계산은 매번
   원 단위로 맞았다. 틀린 것은 **입력**이었다 — 안 물어본 API, 잘못 매핑한 상태, 복사된 필드.
2. **수수료는 주문 API = 정산 실측, 오차 0원**(delivered 617건 476,380 = 476,380). 이 축은 믿어도 된다.
3. **정산 성숙 = D+12.** 최근 1주일은 커버리지 0~46%라 정산 대조가 **원리적으로 불가능**하다.
   대조창은 항상 `오늘−14일` 이전.
4. 살아있는 사실은 메모리 토픽 2개에 있다:
   `naver-settlement-maturity-and-cost-model.md` / `naver-commerce-api-settlement-surface.md`

## §1 고친 것 (전부 prod 배포 + 라이브 검증)

| # | 항목 | 규모 | 커밋 |
|---|---|---:|---|
| 1 | 택배비 정액 1,900 → 배송방식별 실단가(N배송 3,020) | 배송비 +400,960 | `1a693c1` |
| 2 | 배송수입 이중계상 제거 | 매출 −45,000 | `ad3d714` |
| 3 | 배송비 수취분 주문관리 수수료 반영 | 수수료 +29,876 | `ad3d714` |
| 4 | 정산 수집 3대 결함 | 누락 129건 → 2건 | `2f9097d` |
| 5 | 교환을 매출로 인정 | 매출 +826,500 | `b407855` |
| 6 | 미입금취소를 매출에서 제외 | 매출 −163,000 | `b407855` |
| 7 | 정산 catch-up이 아침배치를 막던 결합 해소 | (사고 예방) | `7ccf6f1` |

**07-27~08-02 순이익 2,230,668 → 1,754,832 (−21%).** 배포 전에 예측을 못 박고 배포 후 원 단위
일치를 확인하는 방식으로 두 라운드 모두 검증했다.

## §2 N배송 배송비 구조 (Jino 확인 + API 실증)

`deliveryFeeAmount`는 N배송 전 건 **3,000원 고정**이고, `deliveryDiscountAmount`가 갈린다:

| 할인 | 건수 | 비중 | 누가 내나 | 우리 수입 |
|---:|---:|---:|---|---:|
| 3,000 | 270 | 61.6% | 네이버가 멤버십 회원에게 면제 → **네이버가 우리에게 지급** | 3,000 |
| 0 | 165 | 37.7% | 고객이 우리에게 지급 | 3,000 |

**어느 쪽이든 3,000원**이라 코드는 할인 필드를 볼 필요가 없다. 정산 `DELIVERY` 행 3,000원
109건으로 실증. 우리가 품고(PG)에 내는 단가는 **3,020원**(Jino 확인).

```
N배송   +3,000(수취) −81(수수료 2.724% 절사) −3,020(품고) = −101원/건
일반         0(무료배송)  0                  −1,900(한진) = −1,900원/건
                                      → N배송이 건당 1,799원 유리
```

★배송비 수수료 원 단위는 **절사(ROUND_DOWN)**. 라이브 1,652건 전수 대조에서 절사 99.5% vs
반올림 92.1%이고, **N배송의 유일한 금액 3,000원은 절사(81)만 맞다**(반올림이면 매 건 +1원 과대).

## §3 codex 리뷰

| 라운드 | 결과 |
|---|---|
| 1R | P1 2건(SQL 폴백 불일치 + `json_extract`가 잘린 JSON에서 예외) · P2 1건(순서 의존) — 전건 대응 |
| 2R | P1 0건 · P2 2건(반올림이 실측과 어긋남 + first-wins 순서 의존) — 전건 대응 |
| 3R | **사용 한도 초과로 실행 불가**(복구 08-09). focus 5개를 자체 점검 → **P1급 1건 자체 발견·수정**(`7ccf6f1`) |

⚠️ **부채**: 마지막 3커밋(`2f9097d`·`b407855`·`7ccf6f1`)은 codex 미검토. 08-09 후 재실행하거나
Jino 판단으로 면제.

## §3.5 ★★세션 막판 정정 — orderId 조회는 periodType·searchDate와 상호 배타

**커머스 API 실측(2026-08-03, raw HTTP로 확인):**
```
orderId + periodType        → 400 "periodType 값은 orderId, productOrderId 값과 같이 입력될 수 없습니다"
orderId + searchDate        → 400 "searchDate 값은 orderId, productOrderId 값과 같이 입력될 수 없습니다"
orderId 단독(+page/size)    → 200 ✅
```
→ **orderId 단건 조회에서는 `settleDecisionType`을 쓸 수 없다**(그건 `periodType=PAY_DATE`일 때만
의미가 있는데 periodType 자체를 못 준다). 대신 그 주문의 정산 행이 유형 구분 없이 전부 나온다.

### 이 때문에 세션 중반 결론 하나가 틀렸다 — 정정
"N배송 반품 2건은 정산 3유형 전부에서 0행" → **틀렸다.** `(r or {}).get('elements', [])`로 읽어
**400 실패를 '없음'으로 오독**했다. 올바른 조회 결과:

```
주문 2026072553216341(결제 07-25) / 2026072852172181(결제 07-28) — 각 2행, 둘 다 비멤버십
  PROD_ORDER  NORMAL_SETTLE_BEFORE_CANCEL  gross=16,800  mgmt=−458  expect=15,586
  DELIVERY    NORMAL_SETTLE_BEFORE_CANCEL  gross= 3,000  mgmt= −81  expect= 2,919
```

### ★Jino 가설이 데이터로 지지된다 — N배송과 비N배송의 반품 처리가 다르다

| | 반품 시 DELIVERY 행 | 의미 |
|---|---|---|
| **비N배송** | 5,000원 `NORMAL_SETTLE_ORIGINAL` | 반품비를 **정산받는다**(수입) |
| **N배송** | 3,000원 `NORMAL_SETTLE_BEFORE_CANCEL` | 원 배송비가 **취소된다**(수입 없음) |

즉 N배송 반품은 "반품비를 받는" 구조가 아니라 "받았던 배송비를 되돌리는" 구조로 보인다.
표본 2건(둘 다 비멤버십)이라 확정은 이르다. **멤버십 N배송 반품은 여전히 0건**이라
"멤버십이면 네이버가 보상"은 미검증.

## §3.6 미결 — 감시 프로브(B)가 잘못된 파라미터로 배포됨, **잡은 꺼둠**

서브에이전트가 만든 `naver_claim_settlement_probe`(커밋 `15a7dba`, 테스트 19건 통과)가
**prod에 배포돼 있으나 라이브에서 실패**한다 — `fetch_case_settlement_by_order`가
`periodType`+`settleDecisionType`+`orderId`를 함께 보내 400을 받는다(§3.5).
서브에이전트가 커밋 메시지에 "라이브 미검증(크레덴셜 없음)"이라고 정확히 경고했고, 그게 터졌다.

**조치**: prod `scheduler_state`에서 `run_naver_nbaesong_return_probe`를 **`is_enabled=0`으로 꺼뒀다.**
코드·테이블(마이그 `d4f6a8c0b2e5`)은 배포된 채 남아 있다.

**다음 세션이 할 일**:
1. `fetch_case_settlement_by_order`에서 `periodType`·`settleDecisionType` 제거 → `{orderId, pageNumber, pageSize}`만
2. 프로브의 3유형 루프 제거(주문당 1회 조회로 충분) + 테이블 `settle_decision_type` 컬럼 의미 재검토
   (API가 안 주므로 `settleType`으로 대체하거나 컬럼 삭제)
3. 테스트 19건 갱신 → 라이브 1회 실행으로 검증 → `is_enabled=1`

★내 커밋 `7ccf6f1`에 서브에이전트의 스케줄러 배선이 섞여 들어가 prod에 반쪽 배포된 상태였다
(지연 import라 즉사는 면했고, 뒤늦게 마이그+서비스까지 배포해 봉합). **같은 워크트리에서
서브에이전트를 돌리면 `git add <파일>`이 남의 작업을 집어간다** — 다음엔 별도 워크트리로.

## §4 미결 — 반품 배송비 (4번 항목)

수입은 실측 확정: 반품 주문의 DELIVERY 정산행 **50건 247,500원**(5,000원 45건이 지배적).

**막힌 곳: 반품 회수비 단가를 모른다.** 시스템에 택배 계약·청구 데이터가 없다. 부호가 뒤집힌다:

| 회수비 가정 | 87건 지출 | 순 |
|---:|---:|---:|
| 2,500원 | 217,500 | +30,000 |
| 3,000원 | 261,000 | −13,500 |
| 4,000원 | 348,000 | −100,500 |

**한쪽만 넣으면 대시보드가 반품을 이익 나는 일로 보이게 만든다** → 수입·비용을 짝으로 넣어야 한다.

- **A (Jino 확인 대기)**: 스마트스토어센터 → N배송 관리 → **`N배송 물류비 청구서`**
  (도움말 categoryId=11103)에 반품 회수비 단가가 있을 것. 숫자를 받으면 즉시 배선 가능.
- **B (서브에이전트 진행 중)**: N배송 반품 정산 감시 프로브. N배송 반품 2건(07-25·07-28)이
  **08-06·08-09에 익으면** 구조가 드러난다. 지금은 3개 `settleDecisionType` 전부에서 0행.
  ★그 2건은 둘 다 **비멤버십**이라, Jino가 말한 "멤버십 반품 보상"은 사례 0건이라 미검증.
  일별 정산의 보상성 필드(`returnCareSettleAmount` 반품안심케어·`preferentialCommissionAmount`
  우대수수료환급 등)는 전부 0 = 반품안심케어 미가입으로 보임.
  ⚠️ B가 이 워크트리에서 `naver.py`·`models.py`·alembic 마이그레이션·프로브 서비스를
  **미커밋 상태로** 작업 중이었다. 결과는 별도 PR로 올려야 한다.

## §5 다음 세션이 알아야 할 것

- **백엔드가 08-03 03:45 KST에 왜 죽었는지 미규명.** 로그에 라인이 잘린 채
  `INFO: Started server process [2257565]`가 이어붙어 있다. 서버 재부팅은 아니다(uptime 40일).
  정산 크론 유실의 뿌리이고, 캐치업을 넣었어도 **캐치업 목록 밖 잡들은 계속 샌다.**
- **트랙 파일에 D-N을 못 적었다.** 메인 체크아웃에 다른 세션의 **미커밋** D-NAO-131·132가 있어
  충돌 회피로 미뤘다. ⚠️ **그 131·132는 아직 main에 없어 유실 위험** — 확인 필요.
- 이어서 할 후보: 쿠팡·cafe24도 같은 전수 대사(오늘은 스마트스토어만) / 배송방식별 P&L 상설 뷰
  (08-11이면 N배송 표본이 익음) / `BEFORE_CANCEL` 상시 대사(정산 취소 ↔ 주문 상태 어긋남 감시).

## §6 방법론 (재사용)

14일 전수 대사 스크립트: `/tmp/recon14.py`(prod). **API가 정본, DB가 대조 대상**이라는 방향이
핵심이다. 검사 8종 — 주문 수집 완전성 / 주문 필드 8종 / 정산 수집 완전성 / 정산 내부 폐쇄
(`gross + 수수료 − 정산예정액 = 0`) / 주문↔정산 교차 / 취소·반품 정합 / 배송비 정합 / daily vs case.
★`fetch_orders`는 **변경일** 기준이라 결제일로 재그룹해야 한다.

교훈은 `LESSONS_LEARNED.md` #76~79, 에러는 `failures.jsonl` 5건에 기록.
