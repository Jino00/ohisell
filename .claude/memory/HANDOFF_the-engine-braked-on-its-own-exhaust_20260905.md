# HANDOFF — 엔진이 자기가 만든 배기가스를 보고 브레이크를 밟고 있었다 (2026-09-05)

> 체인 **`pao-진동차단` n=1**(신설) · 세션 `a833394c` · 계약 **D-NAO-287** 「진동차단 목표」
> 트랙 `docs/tracks/active/track_naver-ad-optimization.md` · **진행률 2/7 불변**
> **완료 QA 종합: 부분달성**(계약 §4 부분달성 / 트랙 D-NAO-59 **판정불능** / 북극성 부분달성)

## 검증 명령 (한 줄 — 받는 세션은 이걸로 인계를 검증한다)

```bash
ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell && grep -c '_gate_baseline_window\|_own_bid_multiple_today\|오늘 증거 거부권' backend/app/services/naver_ad/auto_operator.py"
```
→ **0이 아니면** 배포분이 prod에 살아 있다. 0이면 이 인계의 「배포됨」 주장이 죽은 것이다.

---

## §0. 목적 (원문 — 발명하지 않는다)

Jino 지시문 원문:
> **엔진이 만든 CPC 상승이 엔진의 정지 근거가 된다.** 액셀(UP)은 정착창(과거) 보정ROAS로 올리고,
> 브레이크(DOWN)는 당일(현재) CPC 급등으로 내린다. … **액셀과 브레이크가 서로 다른 창을 본다 —
> 그게 진동의 원인이다.** 액셀·브레이크가 **같은 창**을 보게 하거나, 다른 창을 볼 수밖에 없다면
> **진동을 감쇠**하는 장치를 만든다. 어느 쪽이든 북극성 §7 「액셀·브레이크가 대칭인가」를 통과해야 한다.

계약 정본: `docs/contracts/CONTRACT_oscillation_damping.md`(Jino 승인 11:5x KST).

## §1. 착수 실측이 인계를 확대 정정했다

인계문은 「1,320 → 1,980 → 1,440」이라 했다. 7일 전수를 다시 재니 **나흘짜리 톱니의 하루 조각**이었다.
소재 `nad-…554755092`: **4일 · 54회 판정 · 16회 실쓰기 → 1,320원에서 1,330원**(순변위 **+0.8%**).

ref 129가 못 본 것 셋:
1. **브레이크는 둘이다** — CPC급등 14연속 + **순위고삐 12연속**(ref 129는 앞만 봤다).
2. **둘 다 래치다** — 입력이 전부 당일 «누적»이라 한 번 걸리면 자정까지 안 풀린다
   (정착창 기준 794.7원이 14시간 부동 · 당일소진 11,397→14,583 단조).
3. ★**§7 비대칭의 좌표** — 09-04 11:20에 고삐는 추정ROAS<BEP를 **이미 알았지만** 소진 문턱 미달로
   침묵했고, **같은 시각 액셀이 정착창만 보고 올렸다.** 오늘 나쁘다는 증거가 손에 있는데
   **그걸 보는 눈이 액셀 쪽에만 없었다.**

## §2. 한 것 (배포까지 완주)

- **S1 `_HOURLY_DECISION_WINDOWS`** — 네 게이트의 창·자기오염 여부를 한 자리에 선언하고
  `_gate_baseline_window()`로 **실제 집행**시킨다(표를 거짓으로 고치면 테스트가 죽는다).
- **S2 미러 거부권 2개**(한 커밋) — 둘 다 `hold`만 만들고 새 액션을 만들지 않는다.
  - **A-veto**: 정착창 ok ∧ 오늘 추정ROAS < BEP ∧ **당일 전환 ≥ 2** → 상향만 멈춘다.
  - **B-veto**: CPC급등 문턱에서 **우리 자신의 당일 상향 배수**를 벗긴다(1.0 하한 = 완화만, 강화 안 함).
- PR **#728** 머지 `7177a19b`(CI 3/3) · **prod 배포**(CAS 통과·무중단·다운타임 0초·**마이그 0건**).
- 테스트 **24종** · 회귀 **7,852 passed** · **표본 하한 게이트(D-NAO-286) 43개 유지**.

## §3. ★★이 세션의 최대 산출물 — 「내가 세운 방어가 나 자신에게 없었다」 3회

적대 리뷰 **1R FAIL(P1 6건)**. 그중 셋이 같은 병이다:

| P1 | 무엇 |
|---|---|
| **P1-1** | **판정 grain(adgroup) ≠ 실쓰기 grain(ad)** → B-veto가 **최근 30일 실집행의 100%에서 무동작**. 그러면 살아 있는 거부권이 A-veto 하나 = **내 계약 §3이 금지한 「액셀만 조이는 수정」을 내 코드가 하고 있었다** |
| **P1-2** | `_record_blocked` 2개를 지워도 **235건 전건 초록** — 계약 §4-D가 지목한 **유일한 사람 표면**이 무보호 |
| **P1-6** | `_HOURLY_DECISION_WINDOWS`를 읽는 **프로덕션 코드 0줄** — 「선언만 있는 표는 조용히 거짓이 된다」고 §2-3에 쓰면서 그 방어를 그 표에 안 걸었다 |

나머지 셋: P1-3(하향 집행하면서 「하향 보류」 일기) · P1-4(A-veto가 계약보다 넓게 액셀을 조임) ·
P1-5(계수기가 `bid_up_servo`를 DOWN으로 셈).
★잡은 건 내 규율이 아니라 **독립 재현**이다.

★**구현 중 자기 발견 1건**: A-veto 초판에 전환 하한이 없어 **매일 아침 액셀이 통째로 눌릴** 뻔했다
(`intraday_roas` 정직 경계②가 「구조적 과소추정」이라 이미 적어 뒀다). 하한 없이 배포했으면
**진동을 고치려다 D-NAO-85(ROAS +7%·매출 −52%)를 재현**했다. 하한은 액셀 자신의 기준
`_INTRADAY_UP_MIN_CONV`(=2)를 거울로 적용 — 새 매직넘버 0개.
⇒ **일반형: 완화 장치는 자기 입력의 «정직 경계»를 안 읽으면 반대편 극단이 된다.**

## §4. §7 대칭 검사 — 수 (D-NAO-234 §9-2 형식)

| 방향 | 종전 → 새 | 정확도 |
|---|---|---|
| 액셀 UP | **28 → 23건**(−5) | 🧠 **대리 지표 — 측정 아님** |
| 브레이크 DOWN | **26 → 24건**(−2) | 소급 정확 |

둘 다 움직였다 = 한쪽만 조이지 않았다. **자격이 다르다**:
- B-veto −2는 소급 정확이고 **궤적 효과의 하한**(억제 2건에 실제 1,980→1,690 쓰기가 들어 있어
  라이브였다면 뒤 12건의 입력 자체가 달랐다).
- ★**A-veto −5는 소급 판정불능** — hh24 곡선을 **네이버가 7일만 보존하고 우리는 저장하지 않는다.**

## §5. 완료 QA — 원문 그대로 (미달·판정불능 포함)

- **판정(계약 §4): 부분달성** — *"만들어 붙였다"는 확인됐고 "붙인 것이 실제로 작동해 진동을 줄였다"는
  아직 증거가 없다.*
- **판정(트랙 목표 D-NAO-59): 판정불능** — 총이익을 «전진»시키는 처방이 아니라 **위생 조치**이고,
  진동이 총이익에 물린 금액을 잴 원장이 없다.
- **판정(북극성 §1·§6): 부분달성** — M4 **품질** 축. 5요소 중 ③④만 이동, ①②⑤는 0.
  §7 대칭은 구조·수치 양쪽으로 통과.
- **종합: 부분달성**

★QA가 잡은 내 누락 2건: **§7 예산 초과**(순증 150줄 목표 → 실측 **315줄**) ·
§4-B ⓕ 체크박스가 미체크인 채 통과 상태였음.

## §6. 라이브 (배포 후)

```
2026-09-05 13:20:00.034511 | nad-…554755092 | bid_up | 1330→1520 | ROAS-UP(정착창 2.9602 ≥ 1.87)
```
★**무인 발화 확증** — as_of 소수점이 크론 슬롯과 일치(수동은 임의 시각).
★**정상 경로 생존 = 회귀 0의 라이브 증거.**
⚠️**두 거부권 0건** — 오늘 아직 CPC급등도 BEP 하회 확인도 없다(붕괴 국면 미도래).
**0건은 「안 걸렸다」이지 「됐다」가 아니다.**

## §7. 남은 슬라이스 — 다음 세션이 할 것

1. **§4-C ⓗⓘⓙ 재측정**(1주 창). 특히 **ⓙ가 A-veto의 유일한 정확한 계수**다:
   ```sql
   select action, count(*), substr(min(rationale),1,80) from ops_diary_entries
   where event_type='blocked' and (rationale like '%오늘 증거 거부권%' or rationale like '%자기유발분%')
   group by action;
   ```
   그리고 **ⓗ** = 같은 소재·같은 날 UP과 DOWN이 함께 난 날 수(**배포 전 창 09-01~09-05에 2일**:
   09-03·09-04). **ⓘ** = `·->·` 재발화 비율(배포 전 27/54).
2. **미달 항목 재판정 1회**(§8 규약 — 같은 기가 낸다).
3. 이월: **계수기와 라이브의 배수 기준이 완전히 같지 않다**(P2-3·P2-4) — 소급 수를 라이브 수처럼 쓰지 말 것.

## §8. ↗️ 스코프 밖 (기록만)

- `naver_change_log`에 「어느 가드레일이 실쓰기를 막았나」 사유 열이 없어 `·->·` 27/54건의 내역을
  못 잰다 → 소관: `docs/tracks/active/track_naver-ad-optimization.md`

## 트랙 진행률

**2/7 불변** (달성 M0·M1 / 미달 M2·M3·M4·M5·M6). M4의 **품질** 축 수리이지 M 닫기가 아니다.
트랙 `확인:` 줄 1개 누적(2026-09-05 13:3x KST).

## 착지

커밋 `cd53594`(구현) · `7662b5a`(P1 수정) · `0381ee6`(main 병합) · `9fe2c58`(QA 판정) ·
PR **#728 MERGED** `7177a19b` · **prod 배포 완료 13:0x KST**.
QA 판정 커밋은 별도 PR로 올린다.

## ⚠️ 하네스 관측 (이 세션에서 겪은 것)

- **트랙 확인줄 훅 오탐 4회** — 공유 폴더가 stale(`211decbc` vs origin/main `a6d3603`). 알려진 함정.
- **적대 리뷰 표면 변이 훅이 완료 QA 위임에 오탐 1회** — 안내대로 그대로 재전송해 통과.
- **iCloud 공유 폴더에서 워크트리 불가**는 이번에도 유효 — 로컬 디스크 `--depth 1` 클론으로 우회했다.
