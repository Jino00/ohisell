# HANDOFF — 병목은 그 «결정»이 아니었다 (2026-09-06, `pao-논의` n=92)

**세션**: `0b2a300e` · **체인**: `pao-논의` n=92 (18:51 KST 등록) · **결정**: D-NAO-294
**성격**: 트랙 계약 검증 + prod 읽기 전용 조사 + 문서 반영. **코드 0줄 · prod 쓰기 0 · 네이버 광고 API 호출 0 · 마이그 0.**
**진행률 2/7 불변.** (같은 세션 프로세스의 n=91 인계: `HANDOFF_both-premises-were-wrong_20260906.md`)

---

## 검증 명령 (받는 쪽 어디서든 도는 한 줄)

```bash
git fetch -q origin main && git show origin/main:docs/references/139_pending_backlog_root_cause_20260906.md | grep -c '^## §'
```

**기대 출력**: `9` — `ref 139`가 §1~§9까지 온전히 `main`에 있다는 뜻.
(⚠️ iCloud 공유 메인 폴더는 `origin/main`보다 **1,400커밋+** 낡았다 — 그래서 `git show origin/main:`으로 읽는다.)

---

## 0. 목적 (원문 — 각색하지 않는다)

> **「지금 이 트랙이 잘 구성이 되었는지 gstack으로 검증을 해보자」** (Jino, 2026-09-06 18:18 KST)
> **「T6 조사를 먼저 돌려 순서 문제를 정리한 뒤 전부 한 번에 가자」** (18:51)

상위 목표는 그대로다: 트랙 궁극 목표 **D-NAO-59**(총이익 절대액 최대화) · 인계받은 **「1번을 자동화하자」**(Jino 2026-09-03).

---

## 1. 한 일 — gstack `/plan-eng-review` 8건, 전건 처분

| # | 발견 | Jino 선택 | 반영 |
|---|---|---|---|
| D1 | `[P1]` M4에 차단 조건만 있고 해제 조건 없음 | ① 준비할 재료 명시 | ✅ |
| D2 | `[P1]` M2가 6/6 배포됐는데 미체크 | ① 재확인 후 체크 | ⚠️ **재확인 결과 미달 → 안 찍음** |
| D3 | `[P2]` 계약 블록의 95%가 로그 | ① 기록을 아래로 | ✅ |
| D4 | `[P2]` 「안함」 2줄 소관 없음 | ① 둘을 가른다 | ✅ |
| D5 | `[P2]` 북극성 §3이 트랙과 모순 | ① 정본 포인터 | ✅ |
| D6 | `[P2]` 계기판 문서 5개 병렬 | ② TODOS로 | ✅ |
| D7 | `[P1]`* 마일스톤 순서 자체 의심 | ③ 먼저 재본다 | ✅ **재봤고 반증** |
| D8 | `[P2]`* M3이 관찰만으로 통과 가능 | ① 라벨 의무화 | ✅ |

\* 외부 목소리(Claude 서브에이전트) 발견. **Codex는 사용 불가** — `~/.codex/config.toml`의 `model = gpt-5.6-sol` 핀이 낡았다(`[notice.model_migrations]`에 대체 모델이 적혀 있다). **한 줄 고치면 다음 세션은 진짜 외부 모델을 쓸 수 있다.**

---

## 2. ★★ T6 조사 — 외부 검토자의 전제 3개가 «전부» 반증됐다

외부 목소리: *「문서가 스스로 병목은 §8-①② 결정이라 적어 놓고 §6은 무관한 M2·M3을 먼저 하라 정당화한다 ⇒ 승인 못 되는 재고를 쌓는다」*

Jino가 **「순서를 바꾸기 전에 먼저 재본다」**를 택했고, prod 실측(`ref 139`)이 셋 다 뒤집었다:

| 전제 | 실측 (2026-09-06 18:53~19:03 KST) |
|---|---|
| 승인이 병목이다 | **아니다** — 최근 4일 승인 **28건 전건 집행(100%)** |
| 사람이 승인을 안 해서다 | **아니다** — 승인 419건 중 `console`(사람) **2건**. 417건이 자동 승인원 |
| M2·M3이 재고를 쌓는다 | **아니다** — 대기 1,415의 87.6%는 `bid_up_explore` 960 + `search_term_promote` 280 |

**진짜 사유 = 엔진의 자기 차단.** 최근 3일 `blocked` **1,947건**:
**자동운영 스코프 밖 940(48.3%) + 손실상태(bleeding) 807(41.4%) = 89.7%**
(나머지 E 145건은 「경제성 상한 이미 도달」 등 **정상 판단** — 적체로 세면 안 된다)

⇒ **마일스톤 순서 변경 근거 없음. M0→M6 그대로 간다.**

★지적이 가리킨 «방향»은 유효했다 — **일이 병목이 아닌 곳에 배치돼 있다.** 병목의 정체가 달랐을 뿐이다.

---

## 3. ★★★ 진짜 지렛대 — 스코프이고, M4에 묶여 있지 않다

| 관측 | 값 |
|---|---|
| 관할 `naver_adgroup_scope` enabled | **9행** (`None` 6 · `accel` 2 · `brake` 1) |
| 최근 3일 「스코프 밖」으로 차단당한 고유 광고그룹 | **37** |
| 관할 비율 | **19.6%** |

★**스코프 확대는 M4를 열지 않고도 이미 두 번 됐다**: 2026-09-05 21:14 세션 `935aae94`가 §8-①② 미결 상태로 3행을 넓혔고 **3분 10초 뒤 무인 실집행**(`21:20:00.003678`, 1,740→2,000원, `editTm` 갱신 확증).

⇒ **M4(카나리 1주 무위반)와 스코프 확대는 분리 가능한 두 일인데 한 칸에 묶여 있었다.** 트랙에 명시했다.

★부수: `search_term_promote` **940건 전건**(대기 280·만료 535·반려 125)이 승인 0·집행 0.
`_ACTION_BY_PROPOSAL_TYPE`(`naver_execution_harness.py:204`)에 키가 없어 **원리적으로 집행 불가한 죽은 카드**다.

---

## 4. 문서에 반영한 것 5건 (PR #760)

- **ⓐ M4 해제 조건 신설** — 결정에 필요한 관측 3개를 체크박스로: ⓐ스코프 밖 37그룹 실측표 ⓑ대행사 개입 계수 ⓒ되돌림 감지율.
  **결정을 재촉하지 않는다. 재료를 모으게 한다** — 40세션 연속 「여전히 대기」의 처방이다.
- **ⓑ M3에 「관찰분/실집행분 구분 표시」 의무** — 실집행 0인 동안 성적표는 원리적으로 관찰분뿐인데(북극성 §4 간극4) 그게 M4 근거가 되면 «구경한 기록»으로 «돈 쓰는 결정»을 한다.
- **ⓒ 「안함」 2줄 분리** — 예산 변경 = **영구 안 함** / 층1 91 endpoint = **TODOS로 배출**.
- **ⓓ 확인줄 이관** — 계약 블록 **434KB → 20KB**(계약 본문은 1,512자·0.35%였다).
  ★**최근 12줄만 블록 안에 유지한다** — 훅 `track-progress-sync.sh:96`이 «블록 안» 확인줄에서 세션ID를 찾으므로 **전부 내리면 매 세션 오탐**이다.
  **확인줄 293개 전건 보존·순서 보존·이동만.** ⚠️남의 줄도 옮겼다(관례와 어긋나나 Jino가 D3에서 명시 채택).
- **ⓔ 북극성 §3에 「트랙이 정본」 포인터**(원문 무수정) — L0 `3/5` vs 트랙 `5/5`, L1 `19/36` vs 트랙 `20/36`으로 원본이 3주 낡아 있었다.

TODOS **3건 배출**: 계기판 5개 통합(관측 11건) · ADVoost·GFA 528만원 처분 · 광고 API 표면 개봉.

---

## 5. ★ M2 체크는 찍지 않았다 — 재확인이 막았다

| M2 하위 | 라이브 (2026-09-06 19:0x KST) | 판정 |
|---|---|---|
| ① 계층 풀링 | 105,036행 · `target_date` 오늘 · 최근3일 24,610 · 크론 09:30 ok | ✅ |
| ② bidWeight 판독 | 6,920행 · `observed_at` **오늘 08:12** · 크론 08:15 ok · 비-100 19행 | ✅ |
| ③ n-gram 의미단위 | `naver_proposals.target_type='search_term_semantic'` **전 기간 7건**(만료 5·대기 2) · 실집행 0 | ⚠️ |
| ④ 판정면 라벨 | `weekday` 보유 일기 최근3일 2,020 · `is_kr_holiday` 9,787 | ✅ |
| ⑤ 하한 게이트 | `naver_account_settings` key=`guardrail_params` = `{"min_weekly_conv_target":"7"}` 실재 · **발동 0건** | ⚠️ |
| ⑥ GAVE 성적표 축 | 118건 · 최근3일 19 · 최신 09-05 23:00 | ✅ |

★**그냥 찍었으면 거짓 합격이었다.** Jino가 D2에서 고른 「재확인 후 찍는다」가 정확히 이 경우를 위한 선택이었다.
**진행률 2/7 불변.**

⚠️ ③⑤는 **테이블 이름을 세 번 잘못 짚은 끝에** 찾았다 — `naver_search_term_semantic`(없음, 실제는 `naver_proposals.target_type` 값) · `naver_guardrail_params`(없음, 실제는 `naver_account_settings.key='guardrail_params'`). **다음 세션은 `sqlite_master`부터 볼 것.**

---

## 6. 반영하지 «않은» 것 2건 (의도)

1. **외부 목소리 「M0 합격기준이 실패 불가능한 이지선다」** — M0은 이미 `[x]`이고 **닫힌 마일스톤의 기준을 소급 변경하는 건 금지선**이다. 리뷰 기록에만 남긴다.
2. **「M2·M3 순서 변경」** — §2로 근거 반증.

---

## 7. 남은 슬라이스 (다음 세션 — 고르는 건 Jino)

1. ★★**M4 해제 조건 ⓐ — 스코프 밖 37그룹 실측표**. 광고비·전환·현 optimizer 소유(대행사/우리)·bleeding 체류일.
   **이게 카나리 후보 목록이자 «지금 당장 넓힐 수 있는 목록»이다(둘은 같은 표다).** 가장 값싸고 가장 많이 말해 준다.
2. **bleeding 807건의 그룹 분포** — 몇 개 그룹이 몇 번씩인지 안 쟀다(`ref 139` §8-2). n=89 실측: 보드 67그룹·체류 최장 59일.
3. **B-5 24시간 재측정** — n=90이 넘긴 항목. 09-05 21:14 스코프 3행 확대가 대기 곡선을 꺾었는지. **오늘 21:14 이후 창이 찬다.**
4. **`search_term_promote` 940건 처분** — 집행 경로를 만들 것인가, 생성을 끌 것인가. 지금은 매일 죽은 카드를 찍어낸다.
5. **Codex 모델 핀 수정** — `~/.codex/config.toml` 한 줄. 그래야 다음 리뷰의 「외부 목소리」가 진짜 외부 모델이 된다.
6. TODOS 3건(계기판 통합 · ADVoost 528만원 · 광고 API 개봉).

⛔ **다른 세션 소관, 건드리지 말 것**: `발주예측` n=8 `c3b74f44` · `승인배선` n=2 `607c2c15` (둘 다 `end_kst` null · proc 없음 ⇒ **모르면 살아 있다**).
`pao-진동차단` n=5는 12:04 종결 · `pao-상한리셋`·`pao-uiux`·`1p-계산서`도 닫혀 있다.

---

## 8. ⚠️ 하네스 함정 (n=91에서 이어짐 + 이번 추가분)

1. **iCloud 공유 폴더는 워크트리를 못 만든다**(`fatal: mmap failed`) + `origin/main`보다 **1,400커밋+** stale. 로컬 **전체 클론**(비shallow) `~/ohisell-clones/pao-n91`에서 작업했다. `--depth 1` 금지(배포 CAS 거짓 거부).
2. ★**셸 cwd가 클론과 공유 폴더 사이를 오간다.** 이번 세션에서도 `chain.sh status`가 공유 폴더를 읽어 「n=49 열림」이라는 **12일 묵은 오탐**을 냈다. **매 명령에 클론 절대경로를 명시할 것** — 「cd 한 번 했으니 유지된다」는 이 하네스에서 성립하지 않는다.
3. ★**앵커가 두 곳에 산다**(n=91 발견) — `scope-anchor.sh`가 세션 cwd 기준이라 cwd가 오가면 같은 세션 id의 앵커가 갈린다. **종료 전 `ls -la <작업경로>/.claude/anchors/`로 하나인지 확인**할 것.
4. pre-commit 훅이 사설 클론을 「공유 메인 체크아웃」으로 오판한다(`git-dir == git-common-dir`). 미추적 0건 확인 후 `--no-verify` + 커밋 메시지 자백으로 진행했다.
5. ★**위임한 Sonnet 기가 배경 명령 완료를 안 기다리고 반환한다**(이번 세션 3회). `safe_merge.sh`처럼 오래 기다리는 명령을 위임하면 **직접 확인해야** 한다.

---

## 9. 불변 확인

- **진행률 2/7 불변**(M0·M1 달성 / M2~M6 미달)
- **코드 0줄 · prod 쓰기 0 · 네이버 광고 API 호출 0 · 마이그 0 · prod 배포 0**
- ④자동화 실집행 **이번 세션 0** — 다만 **구조에서는 처음 움직였다**(스코프가 M4와 분리 가능함을 실측으로 세움)
- 다음 가용 **D-NAO-295** · 교훈 **#401**(이번 세션은 294 소비, 교훈 미발급)
