# 세션 인수인계: 학습 루프 배관 수리 + 갤럭시Z 8시리즈 인수 (2026-07-28 밤)

> 저장일시: 2026-07-28 22:40 KST
> 세션 모델: Opus. 트랙: `docs/tracks/active/track_naver-ad-optimization.md`
> **★이 세션은 코딩을 중단하고 인계한다(Jino 지시 22:38). 다음 세션이 실코딩을 이어받는다.**

---

## 0. 가장 먼저 알아야 할 것 (놓치면 안 되는 것 5가지)

1. **커밋 2개는 prod에 배포되지 않았다.** 워크트리 브랜치에만 있다. **라이브 시스템은 이 세션 이전과 동일하다** — 인수(아래 §3-C)를 제외하고 아무것도 안 바뀌었다. "고쳤다"고 읽지 말 것.
2. **Jino가 승인한 수정 5개 중 2개만 완료했다.** 미완 3개(밴드 충돌·flight_loop·dry_run 전면 해제)는 다음 세션 최우선.
3. **수명주기 국면 판정 구조(SA1~SA3)는 "미착수"가 아니라 "미승인"이다.** Claude가 두 번 승인 요청했고 Jino는 승인 대신 설계를 계속 교정했다. 착수 전 승인 필요.
4. **이 세션에서 Claude가 5번 틀렸다가 정정했다**(§7). 같은 실수를 반복하지 않으려면 그 목록을 먼저 읽을 것.
5. **codex 게이트는 08-02 쿼터 리셋까지 사용 불가.** 오늘 커밋 2건도 적대적 리뷰 미실시 → 부채 누적(기존 4건 + 신규 2건).

---

## 1. 프로젝트 위치 및 환경

- repo 루트: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (main)
- **작업 워크트리**: `.claude/worktrees/learning-loop-repair`, 브랜치 `claude/learning-loop-repair`
  - 분기점 = `origin/main` `fce9d39`(PR #160 병합 = 다른 세션의 성과뷰 Phase 3)
  - HEAD = `dd73a59`
- prod: `ssh -o BatchMode=yes sellc.ohitech.co.kr`, `/home/ubuntu/ohisell/backend`, DB `ohisell.db`, pm2 `ohisell-backend`
- 배포는 `scripts/safe_deploy.sh`만 (D-NAO-49 CAS 가드). DB 변경 있으면 `--migrate`.
- python: 시스템 `python3`(의존성 설치돼 있음). **`python` 명령은 없다** — `python3 -m pytest`로 실행.
  `--timeout` 플래그는 이 환경 pytest에 없음(pytest-timeout 미설치).

---

## 2. 세션 시작 시 요청받은 라이브 확인 3항목 — 결과

Jino가 아침 확인 항목으로 요청한 3건. **두 개는 "발화 안 함"이 아니라 "발화할 수 없었음"이었다.**

### ① 어젯밤 BP(예산 페이싱) 증액 발동 — 어젯밤엔 레인이 없었다
- `budget_pacing.py` prod **최초 배포 = 07-28 14:17 KST**(deploy-manifest). 그 전엔 코드 자체가 prod에 없었다.
  HANDOFF가 "밤사이 발동 여부"를 확인 항목으로 적은 것 자체가 시간순서상 성립 불가였다.
- 14:20~20:20 7회는 전부 `reviewed=5 raised=0` (소진율 90% 미달 = 설계대로).
- **21:20에 첫 실발동**(`naver_change_log` id=894, dry_run=0):
  03 아이폰_강화유리, 소진율 95.3%(47,645/50,000), 프록시 ROAS 5.1947 ≥ 목표 1.9471,
  최근 3시간 소진 4,675원 → **일예산 50,000 → 52,700**.
- **원복은 아직 미시험** — `run_naver_budget_pacing_reset`의 `last_run_at`이 NULL(한 번도 안 돎).
  **07-29 00:05이 첫 실전.** P1 래칫 결함을 고쳤던 경로라 반드시 확인할 것.

### ② 08:50 새 형식 경보 — 발화했으나 구형식
- 레인 정상 실행(`last_run_at=08:50:15`, `ok`), CTR 경보 8건 발화.
- 그러나 본문이 구형식(캠페인 ID 그대로·`W3` 코드·`밴드 내≤4.0`). 이유는 ①과 동일 —
  `alert_humanizer.py` 배포가 14:17이라 08:50 실행은 5.5시간 앞섰다.
- 현재 배포된 `ctr_alert_briefing.py`는 `alert_humanizer`를 실제로 import·사용 중(소스 확인).
  **첫 신형식 실발화 = 07-29 08:50.**

### ③ 03 ROAS 유지 — 유지 중
| 07-22 | 07-23 | 07-24 | 07-25 | 07-26 | 07-27 |
|---|---|---|---|---|---|
| 5.04 | 1.33 | 3.26 | 3.52 | 2.50 | 3.88 |
- 07-28 당일 프록시 **5.19** vs target 1.9471 / BEP 1.6931 — 둘 다 상회.
- 성과 페이지 카드에 이미 `source=today_proxy`로 표시 중(HANDOFF 예상보다 빠름).
- 재가동(12:44 설정변경 / 13:20 첫 집행) 이후 실쓰기 28건.
- ⚠️ **미해결**: 성과 API 타임라인 `executed=18` vs DB `update_bid` dry_run=0 **27건** 불일치.
  집계 로직 차이로 보이나 원인 미확인. 커맨드센터 BP 집계 누락(task_c86494e4)과 같은 계열 의심.

---

## 3. 이번 세션의 발견 (전부 라이브 실측)

### A. 학습 루프 전수 조사 — "이 시스템은 학습하는가"에 대한 답
Jino 질문: *"지금까지 돌리면서 학습한 내용이 있고 그걸로 더 잘 돌리는 구조가 있는 거냐, 아니면 처음 세운 원칙 하나로 계속 가는 거냐?"*

**답: 재료는 매일 새로 계산되고, 규칙은 영원히 고정이다.**

- **사람 없이 행동이 바뀌는 경로 = 6개**: 보정계수(30일 실주문÷네이버convAmt) · 학습 최적 순위밴드(probe CD5) ·
  계층 수축 RPC · BM 경쟁사 입찰밴드 p50 · BEP/판매가 중앙값(E5c) · 예측모델 자동 강등(active 25/demoted 75).
  단 이건 "과거에서 배운다"보다 **"최신 데이터로 다시 잰다"**에 가깝다.
- **정책 파라미터(threshold)는 자동 조정되는 것이 단 하나도 없다.** 임계값을 DB에서 읽는 코드 0건,
  전부 모듈 상수. prod에서 `mode`·`gamma`·`target_roas_override` 전부 NULL, `expert_delegated_types`=`[]`.
- **학습 루프의 마지막 한 칸이 사람이다**: 데이터 → 대시보드/Slack → Jino나 Claude가 읽음 → D-NAO 결정 → 상수를 코드에 박음.

**가장 아까운 것 3개(조사 결론)**
1. `bid_up_explore` 219건 실집행이 반응곡선 원료에서 제외 → **이번 세션에서 수정(§4-①)**
2. 소급 채점 15,932행이 대시보드에서만 끝남(제안 우선순위·스텝 폭에 미반영)
3. 전환 성숙곡선 `conv_delay` 20행 전량 미사용, 그 자리를 하드코딩 1.2가 대신 → **부분 수정(§4-②)**

**추가 발견**
- `flight_loop`은 **영구 dry_run**(`dry_run=False` 호출자 0건), prod 99행 전부 dry_run=1.
- 그마저 **07-25 이후 0행인데 크론은 `ok`** — `flight_loop.py:206` "forecast 없음 → 조용히 continue",
  change_log 행조차 안 남는 무성 실패.
- **학습값과 하드코딩 상수가 충돌**: 학습된 평일 최적밴드 = **1.0~2.0**인데 `exploration.py:77` 하드코딩 가드는
  "과열밴드 rank<2.5 진입 금지". 실행 레인에서는 **상수가 이긴다**.

### B. 갤럭시Z 8시리즈 — 외부 일정 + 시장가 + 실측 CVR

**출시 타임라인(외부 조사, 다출처 교차확인)**
| 07-22 | 언팩(런던) |
| **07-28** | **국내 사전판매 개시 — 오늘이 1일차** |
| 08-03 | 사전예약 마감(폴드:플립 비중 공개 예상) |
| 08-04 | 사전개통 — 폰이 손에 들어오기 시작 |
| 08-07 | 정식 출시(국내·글로벌 동일) |

- 라인업이 **3종으로 분화**: 폴드8 **울트라**(메인 8형/203.1mm) · 폴드8 **와이드**(신규 폼팩터, 커버 138.4mm/메인 193.2mm 4:3) · **플립8**(커버 104.8mm = **플립7과 동일 규격**).
  → 필름은 최소 3종 별도 SKU. 플립8 커버는 플립7 금형 재활용 가능성(공식 확인 필요).
- 가격: 울트라 257.73만 / 폴드8 227.81만 / 플립8 168.30만 (전작 대비 +19.8만).
- 전작(폴드7/플립7) 사전판매 104만대, **폴드 60 : 플립 40**(폴더블 사상 첫 폴드 역전).
- ⚠️ **폴드8/플립8 자체의 사전예약 대수·비중은 08-03 마감 후에나 나온다**(현재 존재하지 않는 데이터).

**시장가 사다리 실측(07-28 21:57 라이브 조회) — 경쟁 공백은 닫혔다**
| 그룹 | 07-21 (ref 37) | 07-28 3위 방어가(MOBILE) | 배율 |
|---|---|---|---|
| 폴드8 울트라 | 50원 → 3.08위 | 1,070원 | 21배 |
| 폴드8 와이드 | 50원 → 2.81위 | 1,070원 | 21배 |
| 플립8 | 50원 → 2.70위 | 2,670원 | **53배** |
- 우리 신규 캠페인(사생활보호) 3소재의 사다리가 전부 50원으로 나온 것은 **"시장가 50원"이 아니라
  "신호 없음"** — 판단 근거로 쓰지 말 것.

**저반사 3종 실측 CVR(07-20~27, 대행사 운영분) — 3종 전부 BEP 미달**
| 그룹 | 클릭 | 전환 | CTR | CVR | ROAS | 손익분기 CVR |
|---|---|---|---|---|---|---|
| 폴드8 울트라 `70451363` | 70 | **0** | 2.0% | 0% | 0% | 12.1% |
| 폴드8 와이드 `70523564` | 274 | 10 | **4.7%** | 3.65% | 49.5% | 11.7% |
| 플립8 `70523694` | 46 | 3 | 1.0% | 6.52% | 59.1% | 20.0% |

- 8일 광고비 **510,196원** / 귀속매출 223,500원 / 실주문 100% 귀속 관대가정으로도 455,600원.
  **공헌이익 환산 8일간 27만~39만원 순손실.**
- **07-27 폴드8 와이드 하루**: 광고비 **199,572원** · 전환 3건 · 매출 49,500원 · **ROAS 24.8%** · 순위 1.83위.
- **핵심 해석: CTR은 건강한데(2~6.8%) CVR이 안 붙는다.** 관심은 최대인데 구매가 안 일어난다 —
  붙일 폰이 아직 없기 때문일 가능성. **단 이것은 가설이지 확정이 아니다(§5 원칙 참조).**
- 신뢰도: 와이드는 CVR 95% CI [2.0%, 6.6%]로 **판정 확실**. 울트라는 클릭 70·전환 0(rule-of-three 상한 4.3%).
  **플립8은 클릭 46·전환 3으로 CI [2.2%, 17.5%] — "확실히 미달"이라 단정하기 이르다.**
- 07-26·27은 전환 정착 진행 중이라 과소계상 가능. **07-28 와이드 실주문 21건(전일 3~4배 급증)**은
  광고 귀속 데이터가 없어 판정 미반영.

**상품 정체 (혼동 주의 — 이 세션 최대 착오 지점)**
| | Jino가 지목한 3종 | 우리 신규 캠페인이 미는 것 |
|---|---|---|
| 상품ID | `13661073009`/`010`/`011` | `13684462601`/`602`/`603` |
| 제품 | **저반사 지문방지 6매입** | 사생활보호 풀커버 2매입 |
| 판매가 | 16,800 / 16,800 / 14,800 | (별도) |
| 원가 | 6,090 / 6,090 / 3,480 | 3,890 / 3,890 / 3,227 |
| BEP ROAS | 2.0365 / 1.9708 / 1.5578 | 캠페인 1.8405 |
| 광고 주체 | **대행사**(캠페인 `cmp-a001-02-000000008425541`) | 우리(`cmp-a001-02-000000010907625`) |
- ⚠️ 이전 HANDOFF의 "확정 원가 플립 3,227 / 폴드 3,890"은 **사생활보호 라인**의 원가다. 저반사 라인이 아니다.
- ⚠️ `naver_adgroup_product`는 **우리 자동운영 캠페인만 커버**한다. 대행사 캠페인 상품이 "매핑 0행"으로
  보이는 것은 정상이며 **"광고 안 하고 있다"는 뜻이 아니다.** 이 세션이 여기서 한 번 틀렸다.

### C. 8시리즈 대행사 캠페인 인수 — Jino 승인·실행 중
- Jino 원문: **"그래, 우리가 인수할꺼야"**(22:22).
- 대상: `cmp-a001-02-000000008425541`("01. 갤럭시_지문방지_TPU") — 현재 `auto_operate=0`.
- 실행 방식: dry-run 평가 선행 → **8시리즈 3그룹에 입찰 상향 제안 0건**일 때만 인수(조건부).
  (오늘 03 재가동에서 탐색 UP 5건이 집행된 전례가 있어 상향 경로를 먼저 확인)
- **✅ 인수 완료(22:50경)**: `naver_change_log` **id 900**(optimizer none→ours, 콘솔 API `PUT /campaign-settings/optimizer`)
  + **id 901**(auto_operate false→true, DB 직접 UPDATE — auto_operate 전용 라우터 없음, D-NAO-92/101과 동일 관례).
  실조회 확정: `optimizer=ours, auto_operate=1`.
- **dry-run 평가 결과 — 타겟 3그룹 상향 제안 0건(조건 충족)**
  | 그룹 | 현재입찰 | 판정 | 사유 |
  |---|---|---|---|
  | 폴드8울트라 70451363 | 1,000 | hold | **상품 단가/BEP 미확인 → fail-closed**, 보정ROAS 0.00 < 목표 1.93 |
  | 폴드8와이드 70523564 | 1,000 | hold | 최근 3시간 노출 0 < 30 (표본 부족) |
  | 플립8 70523694 | 1,900 | hold | 학습 최적밴드(순위 4.0+) 이미 도달 → 탐침 생략 |
- **★★★ 인수 범위가 3그룹이 아니라 캠페인 전체 68개 그룹이다(중대)**
  - `NaverCampaignSettings`는 **campaign_id 단일키**라 **그룹 단위 인수가 구조적으로 불가능**하다.
    스위치 하나가 캠페인 전체에 걸린다.
  - 이 캠페인은 **삼성 갤럭시 사실상 전 라인업**(S8~S26 · Note8~20 · Z폴드1~8 · Z플립1~8), 68그룹 중 63개 on.
  - 인수 즉시 **8시리즈 외 10개 그룹**(Z폴드2/플립2/폴드1/플립1/S8+/S23/S21+/Z트라이폴드 등)이
    `bid_up_explore`(탐색UP) 대상으로 잡혔다 — 전부 저볼륨 그룹의 첫 탐색 진입 최소입찰이고
    **BEP 연동 economic ceiling으로 캡**됨(예: 1,390→상한 1,640 / 550→상한 1,100).
    대량 pause·대폭 상향은 0건. 오늘 03 재가동 때와 동일한 계산된 스텝.
  - **⚠️ Jino가 "인수"를 승인할 때 3그룹을 염두에 뒀는지 68그룹까지 의도했는지는 확인되지 않았다.**
    다음 세션은 이 범위를 Jino에게 명시 확인할 것. 되돌리려면 `optimizer`/`auto_operate`를 원복하면 된다.
- **⚠️ 사고 1건(서브에이전트 보고, LESSONS 기록 필요)**: 1차 dry-run에서 prod DB에 트랜잭션
  롤백(SAVEPOINT) 격리를 시도했으나 SQLite에서 실패해 `optimizer=ours, auto_operate=1`이
  **의도치 않게 prod에 실제 커밋**됨(13:29:03 UTC = 22:29 KST). 즉시 발견·원복했고
  **다음 시간당 크론(23:20 KST) 전이라 실집행은 없었음**(`naver_change_log` 대조 확인).
  이후 prod 오프라인 백업 사본으로만 재평가. **교훈: prod DB에 롤백 전제로 쓰기를 시도하지 말 것 —
  읽기 전용 평가는 반드시 사본에서.**
- **23:20 레인에서 기대할 것**: 타겟 Z8 3그룹은 계속 hold(울트라는 BEP 데이터가 채워지기 전까지 fail-closed),
  캠페인 내 다른 저볼륨 그룹 다수에 BEP 상한 내 소액 탐색입찰이 발사될 수 있음.
  `naver_change_log`(campaign_id 필터) + 크론 로그로 라이브 확인(원칙22, stale 주의).
- **별건 데이터 공백**: 폴드8울트라가 **상품 BEP 미등록** 상태. fail-closed라 당장 위험하진 않지만
  그 그룹은 BEP가 채워질 때까지 아무 조정도 못 받는다. 원가/BEP 입력 필요.

---

## 4. 이번 세션에서 실제로 고친 것 (커밋 2건, **미배포**)

### ① `466bbc5` — 반응곡선 학습 원료 필터를 가드레일 셋과 분리
- **원인**: `bid_rank_curve.build_observations`가 원료 필터로 `RANK_STEP_TYPES`를 썼는데,
  그 셋의 의미는 "스톱로스 base 스위치 + harness 신선도/TOCTOU 기계 적용 대상"이지 "곡선 표본"이 아니다.
  세 의미가 한 셋에 얹혀 있었다.
- **결과**: prod에서 `bid_up_servo`/`bid_up_rank`는 **실집행 0건**인데 원료로 인정되고,
  실제 219건 실행된 `bid_up_explore` + **평시 주력 `bid_up`·`growth_bid_up`이 통째로 제외** →
  `bid_rank_slope` **0행** → rank_servo 영구 콜드스타트 + EX marginal_stop + D-NAO-89 deep 예외(조건③) 동시 휴면.
- **수정**: `bid_step_types.CURVE_SAMPLE_TYPES`(= `BID_UP_TYPES` 별칭) 신설, `bid_rank_curve`만 이걸 쓰게.
  `RANK_STEP_TYPES`는 가드레일/TOCTOU 의미 그대로 → 탐색·콜드에 TOCTOU 기계가 딸려가지 않는다.
- **의도적 보류**: `bid_down`은 미편입. 반대 사분면(Δbid<0 ∧ 순위악화)이라 `_fit_slope`의 유효쌍 정의를
  함께 넓혀야 하고, 인하 시 순위 반응이 대칭이라는 근거가 없다. 표본 2배 후보이나 별도 검증 필요.
- **검증**: 필터를 옛 것으로 되돌리면 회귀 테스트 5건 실패함을 실측 확인. 3,967 passed.

### ② `dd73a59` — 이익 상한 RPC에 전환 정착 보정 배선
- **원인**: `bid_ceiling_calculator._rpc_for`가 `[today-90, today-1]` 창을 **단일 SUM**으로 더해,
  아직 정착 중인 최근 며칠이 과소계상된 채 RPC를 끌어내렸다. 수요가 오르는 구간에서
  **가장 뜨거운 최근 며칠이 가장 덜 집계**되고, **신규 상품은 창 안 데이터가 전부 최근**이라 편향을 100% 맞는다.
- **수정**: `conversion_maturity.load_curve` / `maturity_multiplier`(=1/m(d), 상한 3배 클램프) 신설.
  `_rpc_for`를 날짜별 `group_by`로 바꿔 각 날짜에 배수 적용 후 합산.
- **fail-safe**: 곡선 없음 · `days_since ≥ MATURITY_DAYS` · m(d)≤0 · m(d)≥1 → **배수 1.0 = 종전과 완전 동일**.
- **★중요한 판단 정정**: 원래 지시는 "하드코딩 1.2를 실측 곡선으로 대체"였으나,
  **`auto_operator._INTRADAY_UP_MARGIN`(1.2)은 대체 대상이 아니다.** 그쪽은 시간당 레인의 장중 추정이라
  일 단위 m(d)와 **시간 그레인이 다르다.** 잘못 적용하면 문턱이 2배 이상 느슨해진다(라이브 돈이 걸린 방향).
  **`_INTRADAY_UP_MARGIN` 1.2는 그대로 남아 있고 별도 판단 대상이다.**
- **검증**: 3,979 passed. 회귀 테스트에 fail-safe 7케이스 + 날짜별 개별 보정 케이스 포함.

---

## 5. Jino가 이 세션에서 확정한 원칙 (D-NAO 기록 필요 — 아직 미기록)

### 원칙 1 — 손해 안 나는 선에서는 끄지 않고 계속 운영한다
> Jino 원문: *"지금은 손해보지 않는 선에서는 계속 운영을 해야해. 왜냐하면 고객들이 장바구니에 담는 경우도
> 있고 폰을 사면 어떤 필름을 살지 탐색기일 수 있거든. 판매 초기잖아"*
- 구체적 형태: **정지가 아니라 BEP 상한까지 입찰 인하.** 순위는 밀리지만(5~7위 예상) 노출·관찰은 계속.
- 전례: **D-NAO-91**(17프로 top-5는 BEP 상한 초과라 ~6위 수용)과 동일 판단.

### 원칙 2 — 국면 전환은 날짜 예측이 아니라 실측 지표의 종합으로 판단한다
> Jino 원문: *"너가 위에서 언제는 이럴꺼야가 아니고 실제 들어오는 click, ctr, roas, 장바구니등을 종합적으로
> 보고 국면의 변환을 판단해야지, 단순히 언제 어떨꺼야라는 알 수 없고 의미없는 예측을 하지 말자.
> 실측을 믿자. 숫자를 믿는거지"*
- 지표 묶음(선행→후행): 노출·클릭·CTR → **장바구니 담기** → 전환 지연 분포 → CVR·ROAS.
- 여러 지표가 **함께** 움직일 때만 전환 인정. 하나만 튀는 건 노이즈.
- **★장바구니는 이미 있다**: `cart_conversion_rate`. **오늘 오전 PR #128로 이중계산 버그를 고친 바로 그 지표**이며,
  기록상 "probe 선행지표"로 명시돼 있다. 탐색기의 핵심 증거 → 국면 판정 지표로 배선 필요(미착수).

### 원칙 3 — 달력은 무시하지도, 지배하지도 않는다 (증거의 눈금)
> Jino 원문: *"물론 달력의 스케줄을 무시하면 안되지. 계연성은 있으니까. 종합적으로 판단하자.
> 쓰고 안쓰고의 이분법으로 보지 말고"*
- 달력의 역할 4가지: ①증거 문턱 조정(변곡점 근처면 적은 표본으로도 인정, 아니면 더 많은 증거 요구)
  ②관측 밀도 ③관측 창 경계(발표일 이전은 다른 세상) ④사후 라벨(다음 런칭의 프라이어).
- **넘지 않을 선: 달력만으로는 절대 전환하지 않는다.** 지표 0이면 08-04가 와도 아무 일 없음.
- 기존 시스템의 계층 수축(`(n×자기값 + K×프라이어)/(n+K)`, K=10)과 **같은 철학을 시간축에 적용**하는 것.

### 원칙 4 — 전 캠페인 공통 적용, 공격성은 결과이지 손잡이가 아니다
> Jino 원문: *"이건 이 제품들만이 아니고 우리 MOP가 돌리는 모든 광고에 적용이 되어야 하는거고.
> 단, 플립폴드8 시리즈처럼 출시기간의 제품과 안정기에 온 제품의 공격성만 달라지는거겠지?"*
- 근본 변수는 하나: **환경 변화 속도**. 파생 ①옛 데이터를 얼마나 믿나 ②증거 없을 때 얼마나 참나 ③그 결과=공격성.
- **공격성 자체를 다이얼로 만들면 안 된다** — 사람이 상수를 박는 구조로 회귀.
- **성숙 = 현행 동작**이 되도록 설계해야 전 캠페인 적용이 안전. (②번 커밋은 이미 그렇게 만들어져 있음)
- **★귀결: 제품별 출시 달력을 다 채우지 않아도 시스템은 작동한다**(국면은 실측이 정하므로).
  달력은 전제가 아니라 보조 장치.

---

## 6. 다음 세션이 할 일 (우선순위순)

### 최우선 — Jino가 승인했는데 못 끝낸 것 3개
Jino 승인 원문: *"위의 모든 내용을 다 수정하자. dry run도 모두 해재하고"*(21:53). 5개 중 2개만 완료.

- [ ] **② 학습 최적밴드 vs 하드코딩 2.5 충돌 해소**
  - `deep_ok`(과열밴드 예외, `expansion_allocator.py:204~217`)는 조건③이 `bid_rank_slope` 프라이어 존재라
    ①커밋이 원료를 공급하면 **열릴 수 있게** 됐다. 다만 실제로 열리는지는 **08:10 학습 크론이 slope를 적립해야 안다.**
  - **남은 진짜 충돌**: `_probe_trigger`가 `weighted_rank ≥ _HOURLY_RANK_DOWN_THRESHOLD(2.5)`에서만 발동하는데
    학습된 평일 최적밴드는 **1.0~2.0** → **탐침이 자기가 학습한 최적점에 구조적으로 도달할 수 없다.**
    (`auto_operator.py:123`, `_learned_optimal_skip` 1817~1857 참조)
  - D-NAO-86이 P4(정적 밴드 상한)를 **재심 대상으로 이미 지정**해 뒀다 — 그 틀에서 처리.
- [ ] **③ flight_loop 무성 실패 — 원인 규명 완료(아래), 수정 미착수**
  - **한 줄 원인: 원료 부재(코드 결함 아님).** `optimizer='ours'` 캠페인 6개 전부가
    `NaverForecastDaily`(campaign grain) 예측을 못 받고 있다.
  - change_log `flight_pacing` 마지막 = **07-24 22:15**. 이후 크론은 매 2시간 "3~5캠페인 처리"로
    정상 완주 로그를 남기지만 전량 무성 스킵(예외 로그 39만 줄 중 **0건**).
  - **07-25 경계는 우연** — 배포 이력 없음. 두 캠페인의 강등 쿨다운(3일) 시작일이 겹친 날일 뿐.
  - 6개 캠페인 상태:
    | campaign | gate_status | 원인 |
    |---|---|---|
    | 010236310 | demoted | MAPE 0.5~0.6 지속(07-24 clk 예측6/실측12) |
    | 008514959 | demoted | MAPE 0.7~1.0(예측2/실측9) |
    | 008492582 | demoted(~07-31) | 07-27 강등 |
    | 010769985 | demoted(stale) | **`NaverEntity.status='off'`(07-21~)** → `forecast_engine._active_scopes`가 제외 → 재평가 자체 불가 |
    | 010907625 | fallback | 07-27 신규 편입 콜드스타트(14일 이력 80% 미달) |
    | **008425541** | **active** | **오늘 22:36 인수한 그 캠페인. 유일하게 07-28 예측 존재 → 다음 00:15 실행에서 성공 예상** |
  - **고칠 지점 (a) 관측성**: `flight_loop.py:206-209`(스킵을 로그·change_log 어디에도 안 남김) +
    `scheduler_service.py:475-481`(스킵/성공 breakdown 없이 "N캠페인 처리"만 찍어 전원 스킵도 정상으로 보임).
  - **고칠 지점 (b) 근본**: 세 곳의 상호작용 —
    ① `forecast_scorer.py:20-22` `DEMOTE_MAPE_THRESHOLD=0.50`이 **저볼륨 캠페인엔 상시 발동선**
       (대체 예측 없이 그냥 강등 → 3일간 예측 자체가 끊김)
    ② `forecast_gate.py:28-33` 쿨다운 재평가 억제
    ③ **대상 캠페인 정의 불일치**: `flight_loop._ours_campaigns`(32-37행)는 `NaverEntity.status`를 안 보는데
       `forecast_engine._active_scopes`는 본다 → 정지된 캠페인이 `optimizer='ours'`로 남으면
       **영원히 forecast 없이 매번 조용히 스킵**된다.
- [ ] **⑤ dry_run 전면 해제 — ★지금 해제하면 안 된다(근거 확보). Jino 재확인 필요.**
  - Jino 승인 원문은 "dry run도 모두 해재하고"였으나, 조사 결과 **지금 해제는 위험**하다:
  - 현재 "안전"한 것은 **6개 중 5개가 스킵 중이라 우연히 아무 일도 안 일어나는 것**일 뿐,
    구조가 고쳐진 게 아니다.
  - **★오늘 인수한 `008425541`이 유일한 active** — dry_run을 풀면 **68그룹짜리 그 캠페인이 첫 실집행 대상**이 된다.
    그런데 α의 원료가 되는 예측 모델은 MAPE 50~180%대로 강등당한 이력이 있거나(또는 콜드스타트라 검증조차 안 됨),
    **얼마나 스킵 중인지 볼 방법이 없는 상태**(관측성 미수정)라 신뢰도 낮은 예측이 실집행으로 흘러드는 순간을 아무도 못 본다.
  - **권고 순서: (a)관측성 먼저 → 강등 직후 재활성 시 α 보수화(첫 N일 α 상한 캡) 안전판 → 그다음 해제.**
    "고치고 → dry_run 상태로 1일 관측 → 해제"라는 Jino 동의 순서와 정합.

### 그다음 — 오늘 것 마무리
- [ ] **커밋 2건 적대적 리뷰**(codex 08-02까지 불가 → 독립 Opus 리뷰로 대체)
- [ ] **①`466bbc5`(반응곡선 원료)는 배포 권고** — 순수 학습 원료 확대, 행위 변경 없음.
      배포하면 **익일 08:10 크론이 `bid_rank_slope`를 처음 적립**하고 그게 ②밴드 충돌 작업의 재료가 된다.
- [ ] **②`dd73a59`(정착 보정)는 배포 보류 권고 — 실측이 근거를 반증했다.**

  **측정 완료(prod DB 사본, 406개 그룹 전/후 비교). 결과:**
  | 지표 | 값 |
  |---|---|
  | 중앙값 | 1.071배 |
  | p90 | 1.231배 |
  | 최댓값 | **1.750배** (클램프 3배 근접 **0개**) |
  | 무변화(1.0) | 150개 (37%) |
  | 1.1배 미만 | 241개 (59%) |
  | 1.5배 이상 | 11개 (2.7%) |

  - ✅ **`conv_delay` 곡선은 prod에 존재한다**(cohort_n=20, updated_at 07-27 23:10) → **no-op 아님, 실제 작동.**
  - ❌ **★코드 주석의 "신규/런칭 상품에 가장 크게 작동(의도된 표적성)" 주장이 실측과 어긋난다.**
    1.5배+ 상승 그룹의 **80%가 90일 내내 존재한 기존 상품**이다. 실제 판별 변수는 신규성이 아니라
    **표본 희소성** — 저클릭 그룹(clk 5~52)의 전환이 우연히 고배수 구간(m=0.5714)에 몰리면 최대 배율을 맞는다.
    **주석을 반드시 수정할 것**(틀린 근거를 코드에 남기면 다음 사람이 그걸 믿는다).
  - ❌ **★갤럭시Z 8시리즈 3그룹은 전부 배율 1.0000 = 무변화.** 이 변경을 하게 만든 바로 그 제품들에
    **아무 효과가 없다**(전환매출이 보정이 걸리는 8~20일 구간 밖에서 발생).
  - ⚠️ **곡선 자체가 의심스럽다**: day 8~18(서로 다른 11개 days_since)이 소수점 4자리까지 **정확히 동일값
    0.5714(=4/7)**. 실제 코호트 노이즈라면 이렇게 일치할 확률이 낮다 — 코호트 표본이 얇거나 관측치가
    복사됐을 가능성. 그리고 **최댓값 1.75가 바로 이 구간에서 나온다.**
  - ⚠️ 계정 폴백층(콜드 그룹 14개)도 일괄 1.112배 — 개별 특성과 무관하게 같은 배율을 받는다.
  - **판정: 원리는 맞지만(정착 지연 보정 자체는 옳다) 근거와 표적성이 검증 안 됐다.**
    다음 conv_delay 곡선 갱신 후 배율 분포가 안정되는지 재확인 → 곡선 이상(4/7 반복) 원인 규명 →
    주석 수정 → 그 뒤 배포. **오늘 밤 배포하지 말 것.**
- [ ] **D-NAO 기록** — §5의 원칙 4개 + 인수 + 커밋 2건. 마지막 번호가 **D-NAO-105**이므로 **106부터**.
      제안 번호: 106=학습 배관 수리 / 107=신제품 광고 운영 4원칙 / 108=8시리즈 인수 / 109=국면 판정 구조(미승인 표기)

### 미승인 — 착수 전 Jino 승인 필요
- [ ] **수명주기 국면 판정 구조(SA1~SA3)**. Claude가 두 번 승인 요청했으나 Jino는 승인 대신 설계를 교정했다.
      최종 합의된 형태:
      ```
      SA1 device_launch_calendar   기기별 일정 → 관측 앵커(변곡점 후보). 정책 트리거 아님.
      SA2 lifecycle_observer       달력 후보 + 실측(CVR추세·장바구니·전환지연)으로 국면 전환 판정
      SA3 learning_budget          국면별 학습 허용 손실 + 무엇을 알아내면 종료인지
      배선: bid_ceiling_calculator(창 하한) · conversion_maturity(지연 실측 겸용) ·
            stop_loss/exploration(무전환 인내) · expansion_pressure(실측 확인 시 확장 개방)
      ```
- [ ] **학습 예산** — "런칭 구간에 배움값으로 얼마까지 쓸 것인가"가 아무 데도 안 적혀 있다. Jino가 정할 숫자.
      현재 추세 하루 20만원대 / 순위를 낮추면 5~7만원으로 같은 정보 획득 가능(Claude 추정).

### 인수 후속
- [ ] 인수 성사 확인 → 저반사 3종 입찰이 BEP 상한까지 **자동으로** 내려가는지 라이브 확인
      (손해 안 나는 상한 ≈ 470 / 620 / 620원, 현재 1,000 / 1,000 / 1,900원)
- [ ] 캠페인의 **8시리즈 외 그룹**(아이폰 등 성숙 상품) 영향 확인 — 인수는 캠페인 단위일 수 있음
- [ ] 08-03(사전예약 마감·비중 공개) / 08-04(개통) 전후 지표 집중 관측

### 내일 아침 라이브 확인 (원칙 22)
- [ ] **00:05 BP 원복 첫 실전**(래칫 결함 고친 경로) — `run_naver_budget_pacing_reset`, 03 예산이 52,700→50,000
- [ ] **08:50 신형식 경보 첫 발화** — 사람말 형식인지 육안 확인
- [ ] **08:10 학습 크론이 `bid_rank_slope`를 처음 적립하는지** — ①커밋 배포했을 경우에만.
      미배포면 안 나옴(당연) → 배포 후 익일로 미뤄짐

### 이월된 기존 부채
- codex 소급 리뷰 **6건**(기존 4 + 오늘 2) — 08-02 쿼터 리셋 후 일괄
- 별도 세션 조사 3건: `naver_ad_daily` 상세/센티널 전환매출 불일치(task_124adb63),
  폴드8울트라 소재 탐색 미편입(task_806c4425), 커맨드센터 BP 액션 집계 누락(task_c86494e4)
- 성과 API `executed=18` vs DB 27건 불일치(§2-③)

---

## 7. ★이 세션에서 Claude가 틀렸다가 정정한 것 (반복 방지 — 반드시 읽을 것)

| # | 틀린 주장 | 실제 |
|---|---|---|
| 1 | "저반사 3종은 광고가 하나도 안 붙어 있다"(매핑 0행 근거) | **대행사가 이미 광고 중.** `naver_adgroup_product`는 우리 자동운영 캠페인만 커버 |
| 2 | "경쟁 공백이 아직 살아있을 수 있다"(ref 37의 50원 근거) | **닫혔다.** 6일 만에 20~53배 |
| 3 | "수요는 08-04에 온다 / 지금은 피크가 아니다" | **Claude의 짐작이었다.** Jino 교정 → 예측 금지 원칙 확정 |
| 4 | "BM 프라이어 41,983건은 소비 코드 0"(07-23 감사 인용) | **이미 배선돼 있었다**(exploration 하한). 감사 이후 바뀜 — 옛 기록을 현재로 인용한 실수 |
| 5 | "90일 창이 신제품 CVR을 성숙 상품 평균으로 희석한다" | **아니다.** 90일 창엔 신규 그룹의 짧은 이력만 담긴다. 진짜 원인은 **전환 정착 지연** |
| 6 | "여유계수 1.2를 성숙곡선으로 바꾸면 된다" | **시간 그레인이 달라 직접 대체 불가.** 잘못 적용하면 문턱이 2배 느슨해짐 |
| 7 | (커밋 `dd73a59` 주석) "이 보정은 **신규/런칭 상품에 가장 크게 작동**한다(의도된 표적성)" | **실측 반증.** 1.5배+ 상승의 80%가 기존 상품. 실제 변수는 신규성이 아니라 **표본 희소성**. 그리고 **Z8 3그룹은 배율 1.0 = 무효과** |
| 8 | "인수하면 하루 20만원 출혈이 즉시 차단된다" | **오늘 밤은 아니다.** 손실 브레이크는 08:50 **일 레인**. 시간당 레인 dry-run은 3그룹 전부 hold |

**공통 패턴**: ①우리 테이블의 커버리지 범위를 확인하지 않고 "없다"고 단정 ②과거 감사 기록을 현재 상태로 인용
③짐작을 사실처럼 서술 ④**코드 주석에 검증 안 된 근거를 단정형으로 씀**(#7 — 다음 사람이 그걸 믿는다)
⑤**어느 레인이 언제 도는지 확인 없이 "즉시"라고 말함**(#8).
**원칙 22(라이브 증거)와 [[naver-ad-claimed-vs-wired-gaps]]가 가리키는 그 실패들이다.**

> ★#7이 특히 중요하다: 이 세션은 "쌓아놓고 안 읽는 데이터"를 고치러 시작했는데,
> **그 수정의 근거 자체를 실측 없이 주석에 단정형으로 썼다.** 측정해보니 틀렸다.
> 같은 병을 고치면서 같은 병에 걸린 것 — 다음 세션은 이걸 먼저 읽고 시작할 것.

---

## 8. 조사 3건 — 전부 완료 (결과는 위 §3-C · §6에 반영됨)

1. ✅ **8시리즈 인수 dry-run + 조건부 실행** → 인수 완료, **범위가 68그룹**(§3-C). 사고 1건 포함.
2. ✅ **정착 보정 전/후 상한 변화 실측** → 곡선 존재하나 **표적성 주장이 반증됨, 배포 보류 권고**(§6).
3. ✅ **flight_loop forecast 부재 원인** → **원료 부재(코드 결함 아님), dry_run 해제는 위험**(§6).

## 8-B. 68그룹 인수 유지 판단 (Claude 권고, Jino 최종 미확인)

Jino 질문 *"뭐가 좋은 방향이야?"* 에 대한 Claude 권고 = **유지**. 근거:
- **오늘 밤 유지 vs 원복의 실질 차이는 저볼륨 10그룹의 BEP 상한 내 소액 탐색UP뿐.**
  8시리즈 3그룹은 어느 쪽이든 hold(변화 없음)라 차이가 없다.
- ★**정정**: Claude가 앞서 "인수하면 하루 20만원 출혈이 즉시 차단된다"고 말했으나 **오늘 밤은 아니다.**
  시간당 레인은 상향·서보 중심이고, 손실 브레이크(스톱로스·일일손실고삐)는 **08:50 일 레인**에서 돈다.
  → 실제 제동은 **내일 아침**에 온다. 그게 유지의 진짜 이득이고, 원복하면 안 온다.
- Jino 원칙 4(전 캠페인 공통 적용)와 정합. 애초에 그룹 단위 인수는 구조적으로 불가능.

**유지 조건 3개(다음 세션이 반드시 이행):**
1. **폴드8 울트라 BEP 등록** — 원가 미입력이라 fail-closed hold. 8일간 클릭70·전환0으로 7.3만원 태운
   그룹인데 인수해도 손을 못 댄다. **Jino 데이터 필요.**
2. **"68그룹 인수 후 첫 밤" 전수 확인** — 23:20~08:50 사이 `naver_change_log`(campaign_id 필터)에
   무엇이 나갔는지. 예상 밖이면 그때 범위 재조정.
3. **문제 시 킬스위치** — `auto_operate`만 내리면 부족하고 **`optimizer`도 함께 none으로** 내려야 한다
   (D-NAO-92에 기록된 킬스위치 사각).

---

## 9. 핵심 파일

| 파일 | 역할 |
|------|------|
| `docs/tracks/active/track_naver-ad-optimization.md` | D-NAO-1~105 (단일 진실). 106부터 추가 |
| `docs/references/37_galaxy_z8_launch_trend_observation_20260722.md` | 발표일 baseline·런칭 플레이북 템플릿 |
| `docs/references/39_objective_audit_20260723.md` | 목적함수 감사 P1~P8(P4=정적 밴드 재심 지정) |
| `backend/app/services/naver_ad/bid_step_types.py` | ①수정 — `CURVE_SAMPLE_TYPES` 신설 |
| `backend/app/services/naver_ad/bid_rank_curve.py` | ①수정 — 원료 필터 교체 |
| `backend/app/services/naver_ad/conversion_maturity.py` | ②수정 — `load_curve`·`maturity_multiplier` 신설 |
| `backend/app/services/naver_ad/bid_ceiling_calculator.py` | ②수정 — `_rpc_for` 날짜별 보정 |
| `backend/app/services/naver_ad/flight_loop.py` | ③대상 — 206행 무성 실패 |
| `backend/app/services/naver_ad/exploration.py` | ②대상 — 77행 하드코딩 밴드 2.5 |
| `backend/app/services/naver_ad/auto_operator.py` | 123행 탐침 문턱 / 130행 `_INTRADAY_UP_MARGIN`(미변경) |

---

## 10. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_learning-loop-repair+z8-takeover_20260728.md 읽고 이어서 작업해줘.
★먼저 §0(놓치면 안 되는 것 5가지)과 §7(내가 틀렸다 정정한 6건)을 읽을 것.
커밋 2건(466bbc5·dd73a59)은 워크트리 claude/learning-loop-repair에만 있고 prod 미배포 상태야.

순서: ①§8의 진행 중이던 조사 3건 결과 회수(인수 성사 여부·상한 변화 실측·flight_loop 원인)
→ ②내일 아침 라이브 확인 3건(00:05 BP 원복·08:50 신형식 경보·08:10 slope 첫 적립)
→ ③Jino 승인했는데 못 끝낸 3개(밴드 충돌·flight_loop·dry_run 해제)
→ ④적대적 리뷰·배포·D-NAO 106~ 기록.
수명주기 국면 판정 구조(SA1~SA3)는 미승인이니 착수 전에 Jino 승인 받을 것.
```
