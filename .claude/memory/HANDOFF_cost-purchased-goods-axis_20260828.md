# 세션 인수인계: 원가를 «믿을 수 있게» — 오류 교정 → 근거 연결 → 매입품 축 계약

> 저장일시: 2026-08-28 19:1x KST · 세션 `dc51780e` · 체인 **「SellC 원가 메뉴」 n=18**
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## 0. 목표 — 다시 쓰지 마라

정본 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md`. 목표이름 **「SellC 원가 메뉴 목표」**.

> ***"SellC에 원가 메뉴를 하나 붙이자… 목표는 정확한 원가 산출이니까?"*** (Jino 2026-08-22)

이 세션의 하위 계약: **「매입품 근거 목표」**(`D-CPP-63`, `docs/contracts/CONTRACT_purchased_goods_cost.md`, Jino 2026-08-28 18:27 승인).

## 1. ★이 세션의 한 문장

**「믿을 수 있는 원가」가 77개에서 450개가 됐다 — 그리고 나머지 513개가 왜 없는지가 처음으로 좌표로 나왔다.**

세션은 Jino의 질문 하나로 시작했다: *"지금 원가에 올라가있는 원가들은 믿어도 되는거야?"*(16:11). 인계 숫자를 옮기지 않고 prod를 직접 잰 것이 이날 모든 작업의 출발점이다.

## 2. 프로젝트 위치 및 환경

- 워크트리: `/Users/jino/.claude-worktrees/ohiselling/cost-menu-n18` · 브랜치 `feat/cost-menu-n18`
- prod: `sellc.ohitech.co.kr` · 활성 포트 **8011**(블루-그린이라 그때그때 `pm2 jlist`로 재라) · DB `/home/ubuntu/ohisell/backend/ohisell.db`
- prod API는 외부에서 401 — 관측은 `ssh` 후 `http://127.0.0.1:8011/api/cost/...`
- 백엔드 테스트: `cd backend && PYTHONPATH=. .venv/bin/python -m pytest tests/ -q` (venv는 이번에 새로 만들었다)

## 2-1. 완료 QA (별도 Sonnet · 읽기 전용 · 판정 원문 그대로)

- **작업 목적(정본 원문)**: 앵커 `.claude/anchors/dc51780e.md` — *"SellC에 원가 메뉴를 하나 붙이자 … 목표는 정확한 원가 산출이니까?"*(Jino 2026-08-22)
- **대조 3건 각각 판정**:

> **판정(계약 §4 합격기준 = 앵커 합격 체크박스): 달성** — *"라이브 재현 가능한 항목(슬라이스①③④⑤ 전부, ②⑥ 대부분)이 앵커 주장과 숫자·타임스탬프까지 정확히 일치. 미체크 2건(S1 2/3·3/3)도 실제로 파일 부재로 확인되어 체크박스가 정직하다. 이력성 수치 일부(②의 20→10, 448 전후)와 원본 엑셀 대조(⑥)는 판정불능이나 현재 상태와 모순되는 증거는 없음."* (2026-08-28 KST)

> **판정(Jino 지시 원문): 달성** — *"8개 지시 전부가 시간순으로 커밋에 대응한다: 16:40→`bdb6ea31`(16:47) · 16:52→`85f63cf6`(16:58) · 17:45→`e8ee7633`(17:53) · 17:54→`8eebaf18`(18:05) · 18:17(매입완제품만 참조)→계약 §0-D·§2·§3에 원문 인용 + `CostPurchasedPrice`가 조립품 테이블과 분리된 구조로 실제 집행 · 18:22(정본 질문)→계약 §0-B에 그 사고 자체를 근거로 기록 · 18:27 답 3건→계약 §7 결정표에 1:1 반영 + `ba1b4eb5`(18:29) · 18:59(320원 확인)→슬라이스⑥의 파일↔시스템 대조로 응답(구체 수치 재현은 판정불능이나 방향은 일치)."* (2026-08-28 KST)

> **판정(트랙 궁극 목표): 부분달성** — *"트랙 헤더 체크박스 진행률 **3/6 불변**(B1·B2·A1 유지, A2·C1·C2 미달) … 궁극 목표("정확한 원가 산출", ecount 연결 포함) 중 오류 교정·근거투명성은 실질 진전이나, 재고(C1)·평가방법 컷오버(C2)·플립/폴드-태블릿 계열 확장(A2)은 이번 세션에서 착수 0. 추가로 「매입 완제품은 레시피가 아니라 SKU 그레인」이라는 구조적 발견(A2 선행조건)이 나왔으나 아직 계약 §4 체크박스 승격 전."* (2026-08-28 KST)

> **종합: 부분달성** — *"세션 자체의 좁은 산출물(계약 §4 = 앵커)과 Jino 지시 원문 대비로는 「달성」이나, 트랙 전체 궁극 목표 대비로는 진행률 불변인 「부분달성」이다."*

- **QA가 확인하지 못한 것**(원문): ①슬라이스②의 시점별 이력 수치(20→10, 448 전후, 구성 줄 수 불변, 픽 무결성) — 과거 스냅샷 부재로 재현 불가, 현재 상태와 모순 없음 ②슬라이스⑥ 「종 135개 중 134 일치」·「원가표 자기정합 완벽」 — **원본 엑셀에 접근하지 않아 재현 불가**(OneDrive 플레이스홀더) ③Jino 18:59 응답의 구체 화면 ④브라우저 렌더 스크린샷(프론트 diff 0건이라 API 값으로 대체)
- **목적 전환 여부**: 없음

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_cost-truth-ledger.md`
- **트랙 목표 원문**: *"SellC에 원가 메뉴를 하나 붙이자. 여기에는 우리가 현재 알고있는 모든 원가가 있잖아? … 그리고 그런 원가를 만들려면 재고사항이 필요할꺼고, 그건 ecount와 연결하면 될꺼고."* / *"목표는 정확한 원가 산출이니까?"*
- **진행률**: 시작 **3/6** → 종료 **3/6** — 달성 B1·B2·A1 / 미달 **A2·C1·C2**
- **이번 세션이 움직인 항목**: **없음** — A2는 「계열 이식 «완료» + cost_drift 악화 0」을 요구하는데 이번은 기존 계열 내 2건 교정 + 근거 연결이다. 착수 «전»에 그렇게 예고했다.
- **헤더에 남긴 확인 줄**: 4건 (16:5x · 17:0x · 17:5x · 19:1x, 전부 `[dc51780e]`)
- **다음 세션 후보**: S1 2/3(서비스) — 계약 D-CPP-63이 이미 승인돼 있어 묻지 말고 진행
- **트랙 종결 여부**: 미도달

## 2-3. 착지

- **완료 단계**: 커밋 → push → PR #551 → **적대 리뷰 PASS(P1 0)** → **머지 완주**
- **멈춘 단계**: **없음**
- **재개 명령**: 해당 없음
- **좌표**: 커밋 10개(`bdb6ea31` `85f63cf6` `e8ee7633` `8eebaf18` `2b9acabd` `ba1b4eb5` `a8d35c9f` `74077113` `a034264a` + origin/main 병합 2) · PR **#551** · **머지 `d0336214`**
- **CI**: backend py3.10 **12m21s pass** · py3.14 **8m2s pass** · frontend **1m29s pass** — `safe_merge.sh` 경유 · `--force` 미사용
- **리뷰 판정**: **PASS(P1 0)** — 변이 6종 중 1 SURVIVED(`_find_column` 정확 일치를 지키는 테스트 없음) → **P2-1 채택**하여 `a034264a`로 닫음. 변이 재주입으로 사살 확인. 원복 후 전체 회귀 **7,244 passed**
- ⚠️**착지 전제 검사**: L1 «예»(ALIVE는 `발주예측` n=8 하나이나 내 대상과 무관 — 브랜치 커밋 8개 전부 이번 세션 소산) · L2 경로 지정 커밋 · L3 내 워크트리 단독 · L4 origin/main 5커밋 앞서 병합 후 진행 · **L5로 「main에 세워둔다」 생략**(로컬 `main`이 공유 메인 폴더에 체크아웃)
- ⚠️1차 `safe_merge.sh`가 **체크 0건**으로 거부됐다(CI 미시작). 「0건은 초록이 아니라 «모름»」 가드가 정상 작동한 것이다 — CI 완료 대기 후 재실행으로 통과. **이 저장소 backend CI는 8~12분**이니 `MERGE_WAIT`를 넉넉히 줄 것
- ⚠️**prod 배포는 하지 않았다** — S1 코드(모델·마이그·파서)는 머지만 됐다. 마이그 `pgprice1s1a`는 prod 미적용이고, 배포는 S1 3/3(화면)까지 간 뒤가 자연스럽다

## 3. 이번 세션 완료 목록

| # | 무엇 | 좌표 |
|---|---|---|
| 1 | **레시피 45·97 종 교체** — SKU 8개 각 196.9원 과대 해소 | `bdb6ea31` |
| 2 | **근거 미연결 승인 10건에 원가표 항목 픽** — 363 SKU | `85f63cf6` |
| 3 | **초안 80건 진단 + 즉시 가능분 3건** (버디 2 승인·도어락 1 픽) | `e8ee7633` |
| 4 | 계약 초안 v1 (Fable) | `8eebaf18` |
| 5 | 계약 v3 — 매입 완제품만·SKU 단위 축 | `2b9acabd` |
| 6 | **D-CPP-63 승인** — Jino 답 3건 반영 | `ba1b4eb5` |
| 7 | **S1 1/3** — 모델·마이그·파서·테스트 11 | `a8d35c9f` |

## 4. ★다음 세션이 알아야 할 사실

### 4-1. ★★파일 업로드 ≠ 연결 — 이것이 513개가 없는 진짜 이유다

Jino의 마지막 질문이 정확히 이것이었다: *"513개의 원가는 그런데 왜 없는거야? 다 업데이트 했잖아"*.

**원가표를 올리면 «항목»이 들어오지만, 「이 판매 상품이 그 항목이다」는 사람이 지목해야 한다.** 원가표 60항목 중 **37개가 아직 안 쓰이고 있다.**

| 이유 | 레시피 | SKU | 무엇이 막나 |
|---|---:|---:|---|
| **A. 매입품 — 원가표에 재료가 없다** | 50 | **318** | 사서 파는 물건이라 구성이 없다 → D-CPP-63이 이걸 푼다 |
| **B. 항목은 남았는데 지목을 안 했다** | 22 | **149** | 태블릿 75(칸 나누기 선행) / r66·r36 33(원단 단가 2개 공백) / 나머지 41(지목만 하면 될 가능성) |
| **C. 픽했는데 부자재 단가 없음** | 1 | 1 | 도어락 9줄 중 8줄 공백 |
| **D. 원가표에 그 칸이 없다** | 5 | 6 | 버디 30매 · 트라이폴드 |
| **레시피에 안 붙음** | — | **39** | 채널코드 미매칭(`[중복]` 포함) |

### 4-2. ★★증명하고 픽한다 — 이름으로 고르면 틀린다

근거 미연결 10건(363 SKU)을 **구성 줄 집합(종 이름 × 수량 multiset)이 일치하는 항목이 유일한가**로 증명해 이었다. **10/10 유일 일치 · 애매 0 · 원가 변화 0.**

**이름으로 골랐으면 틀렸다**: r68 「빛반사, 지문방지 매트 필름 3매」의 정답이 「지문방지필름 TPU 3매」였다 — 안 닮았다.

독립 2차 근거: 이미 픽된 7건이 전부 「실계산 = 엑셀합 **+33.90**」(cleaning kit이 엑셀 160.00 대신 원장 190.82를 쓰는 차 30.82×1.1)인데 미연결 10건도 **전부 같은 상수**를 만족했다.

⚠️**초안 78건엔 이 증명법을 못 쓴다** — 구성이 0줄이라 대조할 집합이 없다.

### 4-3. ★★원가표 파일은 믿을 만하다 — 실측으로 확인됐다

Jino 확인(18:50) *"가장 최근에 올린 원가표 파일의 내용은 맞다"* → 그래서 잰 것은 **「시스템이 그 값을 그대로 쓰고 있나」**다.

- **종 135개 중 134개가 파일과 일치** (유일 불일치 = 종 54 「패키지 (fold)」 참고값 **370**)
- **원가표 자기정합 완벽** — 구성 있는 항목 전건에서 「제품원가 = 구성합 × 1.1」 성립(어긋남 **0건**)
- ★**오늘 고친 8 SKU가 파일과 일치함이 확증됨**: 파일이 `힌지 2매`·`사생활_외부2매` **두 행만** 30/10/**171**로 적었고(나머지 폴드 11행은 55/15/**320**), 그 두 행이 정확히 레시피 45·97이다. Jino 화면 확인(18:59) *"여기에 나오는 320원이 맞는거야"*
- 검산: 힌지 2매 = 364+30+60+8+10+171+6+160 = 809 ex × 1.1 = **889.9** = 파일의 890

### 4-4. ★시스템이 어제 이미 잡아 뒀는데 화면에 안 뜬다

`cost_table_item.anomalies`의 item 34·36에 **08-27 13:30 업로드 시점부터** 이렇게 있었다:

```
price_conflict:부착 안내문:55.0≠30.0, 비닐 (16*23+4):15.0≠10.0, 패키지:320.0≠171.0
```

**파서가 정확히 잡았고 아무도 못 봤다.** 내가 오늘 prod를 뒤져 «발견»한 것이 실은 어제부터 DB에 있었다. 원가표 이상 표시 항목 **10건** 전체가 화면에 없다 — 표면화만 하면 되는 건이라 값이 크다.

### 4-5. 등록된 단가의 «등급» (승인 레시피가 실제로 쓰는 171줄)

| 출처 | 줄 | |
|---|---:|---|
| 원장 파생 (ledger) | **20** (12%) | 실제 통관 전표. 고아 0·링크 끊김 0 |
| 엑셀 참고값 채택 | **126** (74%) | 파일 값 그대로 |
| 부가세 ×1.1 파생 | **25** (15%) | 세전만 있어 시스템이 곱함 — **세율 10% 아니거나 면세면 틀린다. 아무도 안 본다** |

로트가 여러 개인 종 **11개** — 최신 로트를 쓴다(정책이지 사실이 아니다). cleaning kit 166.35~190.82(폭 **14.7%**).

### 4-6. 조인 경로 — 이름이 주, 채널코드가 대조

매핑 파일 944행 → `internal_sku`: **이름 944/944(100%)** · 채널코드 930/944(98%) · **둘 다 있는 930 중 22행 불일치**. ⇒ 이름을 주 경로로, 채널코드는 대조로 쓰고 **불일치 22행은 「확인 필요」로 세운다**(시스템이 안 고른다).

### 4-7. ★두 매핑 파일의 열 구성이 다르다 — 위치로 읽으면 틀린다

| | 08-07판 | 08-22판 |
|---|---|---|
| A열 | `상품명`(제품+옵션 통합, 고유 940) | `상품명`(제품만, 고유 88) |
| **B열** | **`원가`** | **`옵션명`** |
| C열~ | 채널 코드 | **동일**(채움율까지 같다) |

**08-22판은 「원가가 빠진 판」이 아니라 상품명/옵션명을 분리한 다른 목적의 판이다.** 08-07판의 원가도 **정본이 아니라 과거 스냅샷**이다 — 원가의 정본은 우리 시스템(Jino 18:17).

⚠️저장소의 기존 `mapping_parser.py`는 **08-22판 레이아웃 전제**로 쓰여 있다(`PRODUCT_NAME_COL=0, OPTION_NAME_COL=1`). 08-07판을 그 파서에 넣으면 **원가 1956을 옵션명으로 읽는다.**

## 5. 알려진 이슈 / 주의사항

- ⚠️**S1 코드는 prod에 배포 안 됐다** — 워크트리에만 있다.
- ⚠️**`alembic.ini`가 `sqlalchemy.url`을 하드코딩해 `DATABASE_URL`을 무시한다.** 1차 마이그 테스트가 엉뚱한 db를 보고 **거짓 통과**했다. 격리 테스트는 `sed`로 ini를 복사해 `-c /tmp/alembic_test.ini`로 실행하라.
- ⚠️**빌드·테스트 검증에 파이프 금지** — `| tail`이 exit code를 삼킨다(인계 3연속 재발).
- ⚠️`파일 원가는 SKU(옵션) 단위다` — r84 51 SKU가 922×29/2,400×22 · r88 26 SKU가 1,000/2,000/2,694. 현재 `cost_standard`는 레시피당 1행이라 담을 수 없다(D-CPP-63 §0-F가 이걸 푼다).
- ⚠️종 54 「패키지 (fold)」 참고값 **370**이 파일 어디에도 없다(라인값은 171·320). 단가는 이미 320이 있어 「채택」이 건너뛰므로 **계산엔 영향 없다** — 표시만 틀리다.
- ⚠️**trifold 종 9개**(76~85)가 참고값만 있고 원가표 라인 근거가 없다. 단가 전부 0행. 이전 업로드 잔재로 보인다.
- ⚠️`cost_setting.valuation_method='fifo'`가 `confirmed=0` — **결산엔 아직 못 쓴다**.
- ⚠️`product_master.cost_price`가 347 SKU에서 파일·우리 계산과 어긋난다(대부분 필름 계열, 어긋난 쪽은 시스템 옛 값). **컷오버는 이 계약 밖**이다.
- ⚠️**체인 「발주예측」 n=8(`c3b74f44`)이 살아 있다** — `otao_*`는 읽기만 할 것(D-CPP-63 금지선).

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: 계약 D-CPP-63 §1 — *"원가 메뉴에서 매입품 SKU 한 줄마다 「이 단가는 … 어느 행에서 왔다」가 보이고, SKU별로 제 값이 서고, 파일 값과 기존 `cost_price`의 차이가 화면에 서서 Jino가 고른다."*
- **남은 슬라이스**:
- [ ] **S1 2/3 — 서비스**: 대상 판별(사람의 확인 클릭이 곧 분류) · 이름↔채널코드 대조 · **레시피 × 단가** 묶음 diff · 1원 36행 공백 분리 · 조립품 「대상 아님」
- [ ] **S1 3/3 — 라우터 + 화면**: 업로드 미리보기(쓰기 없음) / 그룹 확정 / 행 표시 / 보드 카운트. 그 뒤 적대 리뷰 + prod 배포 + 완료 QA
- [ ] S2 — 렌즈 39 SKU 교차검증(파일 원화 ↔ 발주 CNY). **부대비 포함이라 파일 > CNY×환율이 정상**(Jino 18:27)
- [ ] S3 — 공백 34(시스루 케이스 6색 28 + 하이톡 6). **화면 직접 입력**만, ECOUNT 없음
- [ ] (묻지 말고 진행) **B그룹 41 SKU 지목** — 오늘 363개 이은 것과 같은 작업이나 구성이 0줄이라 이름 판단이 필요하다
- [ ] (묻지 말고 진행) **원단 단가 2개**(매트 2매 600 · 오픽스 사생활 1,000 자리)를 넣으면 **33 SKU**가 열린다
- [ ] (묻지 말고 진행) **원가표 이상 표시 10건을 화면에 세운다** — 이미 DB에 있는데 안 보인다(§4-4)
- [ ] 태블릿 75 SKU — 「한 레시피 = 원가표 여러 칸」 칸 나누기(트랙 A2 소관)

## 7. 새 세션 시작 프롬프트

```
/session-relay sellc-원가-메뉴
```
