# 세션 인수인계: M2-z 입력 확정 — 합격기준 16항목 전수 잔여 인벤토리 (D-NAO-221)
> 저장일시: 2026-08-21 19:5x KST · 체인 「PAO 논의 **30**」 (세션 `f67f21b7`)
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## 1. 프로젝트 위치 및 환경
- 로컬: `~/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (**main**)
- prod: **`sellc.ohitech.co.kr`** — ★ssh 별칭은 **반드시 FQDN**
- prod DB: `ssh sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db \"<SQL>\""`
- 배포: `scripts/safe_deploy.sh <파일…> [--migrate] --restart` / 병합: `scripts/safe_merge.sh <PR> [--force]`
- prod 마이그 head = **`m2b2devw1eight`**(불변 — 이번 세션 스키마 변경 0건) · **디스크 95%**(여유 5.8G, 94%→95%로 계속 차는 중)
- ★★**테스트를 돌릴 수 있는 인터프리터는 저장소 전체에 하나뿐이다**(이번 세션 실측):
  `.claude/worktrees/naver-ad-execution-loop-6cc75b/backend/.venv/bin/python3` (Python 3.14.3 / pytest 9.1.1)
  ```bash
  cd backend && "<repo>/.claude/worktrees/naver-ad-execution-loop-6cc75b/backend/.venv/bin/python3" -m pytest tests -q
  ```
  공유 메인은 `backend/.venv.broken-py314`(깨짐) · 워크트리 23개 중 22개 venv 없음 · **prod엔 `tests/` 미배포**.
  ⚠️`--timeout` 옵션은 `pytest-timeout` 미설치라 **인자 오류로 즉시 종료**한다. 옵션 없이 돌릴 것.
  ⚠️이 venv는 **다른 브랜치 워크트리 것**이라 main의 `requirements*.txt`와 갈라질 수 있다(2026-08-21엔 전건이 돌았다).

## 2. 이번 세션 완료 목록
- ✅ 체인 등록부 `n=30` append
- ✅ **착수 필독 실측**(Sonnet·읽기 전용) — 유령 **0건** / 부분오류 2건(§5-1)
- ✅ **D-NAO-221 — M2-z 입력 확정**: 합격기준 **16항목** 전수 잔여 인벤토리. 근거 `docs/references/88_m2_s1s2_residual_census_20260821.md`
- ✅ **S1⑥ 해소** — 두 번 연속 판정불능이던 항목을 전건 재실행으로 닫음
- ✅ 완료 QA **3대조 = 판정 3줄**(§2-1) · 커밋 2건(`f362a4a0`·`794d301a`)
- ✅ **코드 변경 0건 · 배포 0건 · prod 쓰기 0건**(전건 `-readonly`)

## 2-1. 완료 QA (판정 원문 그대로 — 미달 포함)

### 작업 목적(정본 원문 — 트랙 계약 헤더 `목표:`)
*"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."* (Jino 2026-07-19 · D-NAO-59)

- **판정(트랙 궁극 목표 D-NAO-59): 미달** — QA 원문: *"이 세션은 읽기 전용 조사·문서 1건이고 코드·배포·prod 쓰기 0건이라 총이익에 관측 가능한 기여가 없다(양수도 음수도 아닌 순수 0). 직전 세션(크론 정리)이 같은 성격의 순수 관측 세션에 「판정불능이 아니라 미달」을 적용한 전례와 동일 기준 적용."*
  ★**이 미달은 낮추지 않는다** — 「조사·인프라 작업을 목표 진전으로 착각」하는 경로가 열리기 때문이다.
- **판정(M2 계약 §4 합격기준): 부분달성** — 달성 **S1③④⑥**(⑥은 이 세션이 직접 재현해 확정) / 미해소 **S1②** / 판정불능 **S1⑤** / 부분달성 **S1①** / 미착수 **S2 ①~⑤**. QA 원문: *"문서가 재확인한 모든 수치는 라이브 조회와 정확히 일치했다."*
- **판정(앵커 합격 ⓐ~ⓔ): 달성** — 다섯 기준 전부. **추정 등재 0건**(전 주장이 좌표 보유).
- ★★**QA가 전건 테스트를 독립 재실행**(19:32~19:36, `2 failed, 5994 passed` — 코디네이터 관측과 동일) + prod 6항목 재조회.
- 「안 함」·금지선 침범 **0건**(`git show --stat f362a4a0` = 178 insertions·**0 deletions** · `- [ ] M2` 미체크 유지 · 기존 확인 줄 삭제·수정 없음) · **목적 전환 없음**.
- ★**QA 지적 경계선 1건**: S1②의 원 어휘 「달성⚠️캐벗」을 내 문서가 「미해소」로 재서술한 것. 원 판정문 무변경이라 침범은 아니나 경계선 ⇒ ref 88 §3-1에 **어휘 구분 각주** 부착(「미해소」는 판정 어휘가 아니라 캐벗의 «상태» 기술 · 처분은 M2-z가 정한다).
- **QA 미확인 4건**: BRAND_SEARCH 라벨 컬럼 부재 · 08-22 관측 3건 시각 미도래 · 서브에이전트 조사 «과정» · 디스크·bak 경위.

## 2-2. 트랙 진행률
- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: §2-1과 동일(D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음**(M2 «판정 준비»이지 슬라이스가 아니다). 단 **S1⑥이 판정불능→달성으로 이동**했다. 증거: 커밋 `f362a4a0`·`794d301a` · ref 88
- **확인 줄**: 3건 추가(19:2x 착수 / 19:3x 완료 / 19:4x QA) — 누적 **22건**
- **트랙 종결 여부**: **미도달**(2/7)

## 3. 확정된 결정사항
- **D-NAO-221** — M2-z 입력 확정. 읽기 전용 조사(새 승인 지점 아님).
  - ★★**「담당은 완료가 아니다」** — 인계의 「S1 ①②④⑥이 남았다」는 «미달 목록»이 아니라 «M2-c가 담당하지 않은 항목»이었다. 그래서 **④는 거짓 양성**(달성 확정)이고 **⑤는 누락**(판정불능인데 목록에 없다)이었다. 목록이 «슬라이스 담당»으로 만들어지면 **자기 슬라이스가 담당한 항목은 판정이 「판정불능」이어도 «닫힘»으로 세어진다.**

## 4. 핵심 파일 목록
| 파일 | 역할 |
|------|------|
| `docs/references/88_m2_s1s2_residual_census_20260821.md` | ★**M2-z의 입력 정본** — 16항목 전수표 · 「①②④⑥」 오류 해부 · **지뢰 3개** · 실행 사양(시각·명령·분모) |
| `docs/PLAN_naver-m2-l2-wiring.md` | M2 계약 정본(D-NAO-214). §4 합격기준 = ref 65 원문 · §6 슬라이스 a/b/b2/c/d/z · §8 Q5 미결 |
| `.claude/anchors/f67f21b7-….md` | 이번 세션 앵커(판정 3줄 + 이월) |
| `docs/references/87_m2c_semantic_units_live_20260821.md` | M2-c 라이브 실측 + 내일 08:50 기준선 |

## 5. 알려진 이슈 / 주의사항
1. **인계 주장 부분오류 2건**(착수 실측): ①prod 디스크 94%→**95%**(5.8G) ②워크트리 「7개」는 최근분만이고 **총 24개**. **유령 0건.**
2. ✅**인계 ⑬ 해소** — 워크트리 7개 브랜치 **전부 origin/main 병합 완료**(디렉터리만 잔존). 나머지 17개는 어느 인계에도 미등재.
3. ★★**M2-z는 08-22 09:30 이전엔 원리적으로 돌 수 없다** — 그 전에 돌리면 최소 5항목이 판정불능으로 나온다.
4. ★★**M2-z 분모는 16항목**(S1 6 + S2 5 + **M2-b2 신설 5**)이고 **b2 5항목은 ref 65 원문이 아니다**. 계약 §6 원문: *"합격기준은 ref 65 원문이 아니라 «신설»이다 — S1 ①~⑥에 「소비 배선」 항목 자체가 없다(**인용으로 착각하면 재규정 금지가 무너진다**)."* **같은 표에 섞지 말 것.**
5. ★**S1⑤ 분모 함정** — `target_type='search_term_semantic'`은 **0행**(원장에 한 번도 존재한 적 없다)인데 `search_term`∧`pending`은 **280행**이고 `created_at` 전건이 08-07~08-20으로 **M2-c 배포보다 앞선다**. 원 카운트면 「≥1건 달성」이 나온다. `ss_exclude`∧approved **1행**·`change_log` 실행 **1건**도 둘 다 2026-07-22 **선행 파워링크 건**이다.
6. ★**S1②의 「달성」은 조건부** — QA 캐벗(무인 크론 발화)이 미해소다. `write_naver_pooled_estimates.last_run_at=2026-08-21 10:59:17`은 **수동 트리거 시각 그대로**이고 `next_run=08-22 09:30`. `target_date`는 **min=max=2026-08-21 단일일**(누적 히스토리 하루치뿐 — M2-d 진입 조건 「추정치 지속 생성 관측」에 직접 걸린다).
7. ★**S2③ 함정 예고** — `gave_score_d3/d7`은 `models.py`에 있고 내부 계산에 쓰이는데 `routers/*.py`·`schemas.py`엔 **0건**이다. M2-d가 「API 노출」을 만들 때 교훈 #321(`response_model`이 키를 지운다)의 **네 번째 재발** 자리.
8. ★**S1①의 콘솔 캡처는 Jino 개입이 필요한 유일한 항목**(계약 §8-Q5 미결). 대조 좌표는 확정돼 있다 — 캠페인「아이폰 17프로 필름」/그룹「아이폰필름」(`grp-a001-02-000000063093697`) 연령 탭, `AG5054=130`.
9. **CI 빨강은 결제 정지**(`steps=0`·2~4초)지 코드 신호가 아니다. ⚠️확인 없이 `--force` 습관이 붙으면 진짜 빨간불도 같은 손짓으로 지나간다.
10. **다음 구조 감사 트리거 = 08-25 이후**(마지막 §4 감사 = `docs/references/69_audit_pao_drift_20260818.md`, 08-18).

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)** — 트랙 계약 헤더 `목표:` 줄 그대로:
  *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."*
  이번 칸 = **M2 = L2 배선**(ref 65 S1+S2), 계약 `docs/PLAN_naver-m2-l2-wiring.md`(승인됨).
- **남은 슬라이스**: **M2-d**(08-28 이후) · **M2-z**(08-22 09:30 이후)

- [ ] ①**★내일 아침 관측 4건 — 묻지 말고 진행.** 시각이 와야 판정 가능하다. **명령·기대값은 ref 88 §4 표에 있다.**
      **07:35** `SELECT COUNT(*) FROM naver_entity WHERE pc_bid_weight IS NOT NULL;`(기대 ≈871 / 전체 92,235 · 오늘 **0**) → M2-b2 ① 판정불능 해소
      **08:12** `sweep_naver_adgroup_criterion` `last_run_at`·`last_status`(★**무인** 첫 발화 — 오늘도 13:36:49 수동값 그대로)
      **08:25** `SELECT COUNT(*) FROM naver_search_term_exclusion WHERE match_type IS NOT NULL;` + 분포(오늘 **0/3,990**) → S1①
      **08:50** ★S1⑤ — §5-5의 **좁힌** 카운트로 셀 것(원 카운트면 거짓 달성)
      **09:30** ★S1② — `write_naver_pooled_estimates.last_run_at`이 **09:30대**로 바뀌었는지 + `target_date` distinct 증가
- [ ] ②**M2-z** — M2 종결 QA(별도 Sonnet·읽기 전용, **16항목** 라이브 대조). **입력 정본 = ref 88.** 트랙 M2 체크박스는 이 판정으로만. ★**09:30 이전 착수 금지**
- [ ] ③**M2-d** — S2 전체. 진입 조건 = M2-a 배포 +7일(**2026-08-28 이후**) & 추정치 지속 생성 관측(§5-6 — `target_date`가 하루치뿐이라 이 조건이 실제로 차는지 봐야 한다)
- [ ] ④**계약 §8-Q5 — 콘솔 캡처 1장**(§5-8 좌표). S1①이 콘솔 대조 판정불능을 벗어나는 유일한 경로
- [ ] ⑤**크론 정리 2차(선택)** — `run_naver_learning_loops` 4루프 잠정 조사(이번 세션 부수): `bid_rank_curve`·`estimate_calibrator`는 **소비처 있음**(단 `optimizer='none'` 게이트로 실행 막힘) · `hourly_pattern`은 `flight_loop`(**관측 전용**) · **`proposal_scoreboard`는 소비처 미발견**. 확정 조사 필요
- [ ] ⑥**이미 쌓인 496 MB의 처분**(DELETE+VACUUM) — 크론 정지는 순증만 멈춘다. **디스크 95%**
- [ ] ⑦**`conversion_maturity`가 쌓기만 하고 안 쓴다** — 유일 소비처가 `MATURITY_CORRECTION_ENABLED=False`(2026-07-29 보류). 소급 불가라 안 껐으나 부채
- [ ] ⑧**토큰화 규칙 중복 정의 해소** — `_build_whitelist`(search_term_judge)와 `_tokens_from`(semantic_units)이 규칙을 두 파일에 중복 정의. **잔여 no-op 불변식이 이 둘의 동기화에 의존**한다
- [ ] ⑨**사전 구멍 「액정」 처분** — 잔여 상위. 화이트리스트 정책 재심과 같은 층
- [ ] ⑩**「관련어인데 적자」의 처분**(제외/하향/구제 레인) — 계약 §8-Q4 미결. 후보가 실재하므로 결정 대상이 생겼다
- [ ] ⑪**`search_term` rejected 141건** — ★**정정**: `created_at`이 **2026-07-19~07-29 전건 옛것**이라 인계의 「18:3x→18:5x에 125→141 증가」와 **창이 안 맞는다**. 증가 관측 자체를 재확인할 것. 좌표 `auto_operator.py:1077`
- [ ] ⑫신설 카운터(`shopping_over_cap`·`semantic_*`)가 크론 `log.info`(`scheduler_service.py:909-917`)에 안 실린다
- [ ] ⑬**워크트리 24개** — 최근 PAO 7개는 **전부 병합 확인**(디렉터리만 잔존), 나머지 17개 미등재. 정리는 잡일
- [ ] ⑭**`.venv.broken-py314`** — 공유 메인 venv가 깨진 채 방치. **이게 QA 판정불능의 구조적 원인**이다(§1)
- [ ] ⑮**CLAUDE.md 미커밋 11줄**(「트랙 목표 규율」 절) — 어느 HANDOFF에도 기록 없이 누적. Jino 판단
- [x] ~~S1⑥ 재실행~~ **완료**(5,994 passed / 실패 2 = 기준선 ⇒ 새 실패 0, QA 독립 재현)

## 7. 새 세션 시작 프롬프트
```
.claude/memory/HANDOFF_m2z-input-census_20260821.md 읽고 이어서 작업해줘
```
(체인을 이어받으려면: `/session-relay PAO 논의` — 이번이 **30**번이었다)
