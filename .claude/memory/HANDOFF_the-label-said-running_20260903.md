# HANDOFF — pao-논의 n=82 (세션 14873038, 2026-09-03 15:40~16:1x KST)

> 제목의 뜻: **라벨은 「가동중」이라고 했다.** 크론은 오늘도 돌았고, 실집행은 35일 전에 끊겨 있었다.

## §0. 작업 목적 (원문 — 발명하지 말 것)

계약 `docs/contracts/CONTRACT_pao_purpose_and_hands.md`(D-NAO-280) 발단 Jino 원문:
> "이제 전체적으로 보자. 1번 계약, 1번 이슈를 포함해서 운영을 위해 손을 달고 쿠팡에서 잘 팔리는데도
> 불구하고 노출을 일으킬 수 없는 경우 어떻게 극복방안을 종합해서 계약서를 작성하자" (2026-08-30 21:45)

이번 세션 발단: Jino **「PAO가 구현하려는 기능을 점검해보자」**(15:26) → **「재측정해줘」**(15:39).

## §1. 이 세션이 한 것 — 한 가지

`ref 65` §1-1의 **13기법 「현재 상태」가 2026-08-17판이라 17일 낡은 것**을 재측정했다.
**읽기 전용 · 코드 0줄 · prod 쓰기 0 · 네이버 광고계정 쓰기 0.**

**왜 필요했나**: 북극성 §3의 L2 행(「논문 기법 실배선」)이 이 문서를 인용하는데, 오늘 감사(ref 121 §1)가
그 값을 **「재측정 안 됨 — 미상」**으로 처리했다. 계기판의 한 칸이 비어 있었다.

**산출물**: `ref 65` **§1-1-b 신설** — `git diff --stat` **41줄 순수 추가 · 삭제 0줄**(08-17 원문 무변경, 소급 수정 금지 준수).

## §2. ★결과 — 셋이 문서 모르게 움직였다

| # | 기법 | 08-17 | 09-03 |
|---|---|---|---|
| ②/[9] | 계층 EB 축소추정 | 부분구현(죽은 코드) | ✅ **가동중** — `pool_all_with_priors`가 크론 `30 9 * * *` 배선(D-NAO-214) · prod `naver_pooled_estimate_daily` **86,577행**(08-21~09-03, 키워드 6,943) |
| ⑤ | GAVE 점수 | 배선·정지중 | ✅ **가동중** — prod `/api/naver/ad/proposals` 응답 `rationale`에 `[GAVE사전: 기대점수 21409.3500·유형실적 51200.4(n=3423)]` **15건 실측** |
| [10] | 168슬롯 시간대 풀링 | 구현·**미배선** | ⚠️ **부분구현**(배선 해소) — 크론 `10 8 * * *` · `naver_hourly_pattern_history` **168칸 전건**(09-02) · `expected_cost_fraction`이 페이싱에 실소비. ★단 **`hourly_pattern.py`는 여전히 네이버 `bidWeight`를 안 읽는다**(08-17 핵심 지적 잔존) |

**나머지 열은 그대로**: [8] n-gram 미구현 · [1][6] **하한 게이트 여전히 없음** · [4] 한계ROAS 계산 0건 · [0][2] 슬랙 0건 · ① 관측기 · [3]·[7] 부분/가동중이나 실집행 공백 · **③④[5] 제어수학 4종 전부 미구현**(④는 `scipy`·`ortools`·`pulp`가 **의존성에 아예 없다**).

## §3. ★★다음 세션이 잃으면 안 되는 관측 셋

1. **이름으로 찾으면 오판한다** — ②/[9]는 `pool_all()`로 grep하면 「호출부 0건 = 죽은 코드」인데,
   **형제 함수 `pool_all_with_priors()`가 크론에 붙어 매일 6,943 키워드를 돌린다.**
   ★같은 함정을 **2026-09-03 하루에 네 번** 밟았다: 계약 P2 재개방 dispatch(가칭 `open_exclusions`) ·
   M2 S2 두 건(주말·공휴일 라벨·램프업 라벨) · 여기. **기능은 이름이 아니라 «경로»로 찾는다.**
2. **「가동중」은 크론이 돈다는 뜻이지 일이 일어난다는 뜻이 아니다** — [3] 증액 트리거는 크론이
   오늘 15:20:31 ok인데 **90일 실집행이 07-28~07-30 4건뿐**이고 이후 **35일간 0건**이다.
   그 4건도 «증액 → 몇 시간 뒤 날짜 기준 무조건 원복»이었다. [7] explore_op도 07-30 끊김 →
   08-30 하루 30건 → 다시 4일째 0건. **07-30 = D-NAO-132 정지 시점.**
   ⇒ **ref 65의 상태 어휘가 «코드 존재»와 «실제 발화»를 구분하지 못한다.**
3. **데이터 하한 게이트는 17일 뒤에도 없다** — `min_weekly`·`data_floor` grep **0건**,
   `guardrail_params` SPECS 9키에도 없음. 있는 건 `cost<=0 → unknown`이라는 «존재 여부» 게이트다.
   그 부재가 **같은 날 라이브에서 값을 치렀다**(하루 전환 0~2건 그룹에서 입찰 `1,320→1,980→1,440` ±50% — `ref 129` §1).

## §4. 부수 — ① 예측 정확도 재료는 실제로 쌓이고 있다

`naver_forecast_model` **32,307행 중 `recent_mape` non-null 183행**, `MAX(trained_at)` **오늘 07:50:19**
(gate_status active 25 · demoted 66 · fallback 32,216).
ref 65가 *「①관측기의 예측 정확도가 ③ 채택 판단의 입력」*이라 한 그 재료다.
⚠️ **다만 그 값이 ③에 실제로 소비되는지는 안 쟀다**(축적 사실만 확인).

## §5. 완료 QA — 판정 원문 (별도 Sonnet · 읽기 전용 · 2026-09-03 16:1x KST)

> **판정(ref 65 13기법 상태 열): 부분달성** — 13행 전부 재측정·5값 어휘 순수·이동 3건 정확히 식별, 프로덕션 실측치 12/13행이 소수점까지 정확히 재현됨. 단 **[8]행의 grep 건수 인용 오류**를 발견 — `n_?gram` grep을 「0건」이라 적었으나 실측 6건. 결론(미구현·그대로)은 실체와 부합하지만 병기하기로 약속한 「grep 건수」자체가 틀렸다.
>
> **판정(Jino 지시 원문 «재측정해줘»): 달성** — 13기법 전부에 대해 명령→관측값→판정을 남겼고, 그중 12건은 파일:줄·prod 행수·grep 건수까지 소수점 단위로 재현됐다.
>
> **판정(북극성 §3 L2 미상 해소): 부분달성** — 근본 데이터는 ref 65 §1-1-b에서 해소됐다. 그러나 **북극성 문서(ref 82) 자신은 이번 세션이 손대지 않았다** — §3 L2 행과 부록A가 지금도 08-17 집계값(「가동중 1」)을 그대로 인용한다. **ref 82만 읽는 독자에게는 미상이 «해소된 것으로 보이지 않는다»** — 원료는 새로 만들어졌지만 그 원료를 인용하는 표는 갱신되지 않았다.
>
> **판정(종합): 부분달성**

### ★QA 지적 3건 — 전부 그 자리에서 수리했다 (다음 세션으로 안 넘긴다)

넘겼으면 오늘 감사(ref 121)가 진단한 **「발견이 처분 없이 이월만 된다」를 내가 재현**하는 것이었다.

1. **[8]행 판정이 뒤집혔다** — QA는 「grep 건수 오류(0건 → 6건)」로만 봤는데, 내가 직접 재니 **7건**이고 그중 `semantic_units.py`가 나왔다. 추적하니 **기법 자체가 살아 있었다**: `judge_semantic_units`가 `judge_search_terms`(:275)에서 실제 호출되고 `search_term_ss_lane.py:917`이 산출을 소비한다. ⇒ **미구현 → 부분구현으로 정정.**
   ★★**grain이 바뀌어 있었다** — 「단어 n-gram」이 아니라 **의미 단위 최장일치 분절**(D-NAO-191. 한국어 검색어의 **97.4%가 토큰 1개**라 단어 n-gram은 산출 0). `n_?gram`이라는 **이름으로 grep해서 놓쳤다.**
2. **[4]행 파일 나열 정정** — `marginal_roas` 17건의 파일이 5개(`expansion_allocator`·`exploration`·`expansion_pressure`·`auto_operator`·`proposal_pipeline`)인데 초판이 `expansion_pressure`를 빠뜨리고 `budget_allocator`·`proposal_writer`를 잘못 넣었다.
3. **북극성 §3 L2 행 갱신** — 재측정 줄을 **4줄 순수 추가·삭제 0**으로 덧붙였다(08-19 원문 무변경). ⚠️「가동중 3」을 그대로 읽지 말라는 경고를 같이 적었다.

### ★★그리고 이게 이 세션의 진짜 결말이다

**「이름으로 찾으면 오판한다」를 교훈으로 적은 그 표 안에서, 바로 다음 행이 같은 함정을 밟았다.** 오늘 하루 **다섯 번째**다.
**규칙을 아는 것과 그 순간 적용하는 것은 다른 일이다.** 잡은 것은 내 규율이 아니라 **완료 QA의 독립 재현**이었다.

## §6. 트랙 진행률

- **트랙 2/7 불변** · 계약 「목적관철」 **12/38 불변** · M2 계약 **0/8 불변**
- 이 세션은 **진단**이라 체크박스를 겨냥하지 않았다
- ★단 **북극성 §3 L2 행의 「미상」이 해소**된다: 「논문 기법 실배선 = 가동중 1/13」(08-17) → **가동중 3/13**(09-03).
  정확히는 **「가동중 3이되 그중 [3]은 라벨과 실제가 어긋난다」**가 이번 재측정의 답이다.

## §7. 착지

| 단계 | 결과 |
|---|---|
| 커밋 | ref 65 §1-1-b + 등록부 / 트랙 확인줄 |
| PR **#693** | 머지 `42904f2b` · CI **3/3**(py3.10 13m34s · py3.14 11m15s · frontend 1m55s) |
| `--force` | **미사용** |
| 멈춘 단계 | **없음** |
| 코드 변경 | **0줄** · prod 쓰기 **0** |

## §8. 다음 세션 후보 (묻지 말고 이어서 — §1 승인 지점 아님)

1. ★**M2 착수** — 재측정으로 잔여가 더 좁아졌다. S2 5항목 중 ⓐ(주말·공휴일 라벨)·④(램프업)는 이미 있고,
   **ⓑ 데이터 하한 게이트**가 오늘 **세 곳에서 전부 1순위**로 나왔다(계기판 · ref 129 · 이 재측정).
2. **[3]이 왜 35일간 안 돌았나** — 이번엔 사실만 확인했고 **원인은 안 봤다**(트리거 미충족/대상 축소/스코프).
   「가동중인데 0건」의 인과.
3. **되먹임 진동 처방**(ref 129 §1) — 액셀은 정착창 ROAS, 브레이크는 당일 CPC를 본다.
   Fable 권고와 합치면 「하한 게이트에 시계를 붙이고 브레이크에도 UP과 같은 전환 하한을 준다」가 한 슬라이스.
4. **ref 121 감사 안건 A1~A9** — 결정란 여전히 빈 칸.

## §9. ⚠️ 주의 / ⛔ 무접촉

- ⛔ **무접촉**: `발주예측` n=8 · `승인배선` n=2 (origin/main `end_kst: null`)
- ⚠️ **하루 전환 0~2건 위에서 입찰이 ±50% 움직인다.** 하한 게이트가 없다.
- ⚠️ **자동인 것은 입찰뿐** — 실집행 전부 `update_bid`, 예산·제외·정지 0건. 소재 3개·캠페인 2개, 사람 승인 없이 매시 :20.
- ⚠️ 훅의 `[체인] ⛔ pao-논의 n=49`는 **stale 오탐**(공유 폴더). `chain.sh status`도 같은 폴더를 읽는다.
  생존 판정은 **origin/main**으로.
- ⚠️ ref 65 §1-1-b 말미의 **「확인 못 한 것」 6건**을 지우지 말 것 — 특히 prod 관측과 워크트리의 **배포 커밋 대조를 안 했다**.

## §10. 검증 명령

```
grep -c "★1-1-b" docs/references/65_paper_application_design_20260817.md
```
→ **1**이면 이 세션의 재측정이 살아 있다(0이면 누군가 그 절을 지운 것 — 그러면 북극성 L2 행이 다시 미상으로 돌아간다).
