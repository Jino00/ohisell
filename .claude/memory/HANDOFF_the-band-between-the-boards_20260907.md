# HANDOFF — 보드 사이의 띠 (2026-09-07, `pao-논의` n=94)

**세션**: `fc6fa804` · **체인**: `pao-논의` n=94 (2026-09-06 21:43 KST 등록) · **결정**: D-NAO-296
**성격**: prod **읽기 전용** 조사 + 문서 산출. **코드 0줄 · prod 쓰기 0 · 네이버 광고 API 호출 0 · 마이그 0 · 배포 0.**
**진행률 2/7 불변.** (인계: `HANDOFF_the-doorway-list-was-52-not-37_20260906.md` n=93)

---

## 검증 명령 (받는 쪽 어디서든 도는 한 줄)

```bash
git fetch -q origin main && git show origin/main:docs/references/141_c_group_board_gap_20260906.md | grep -c '^## §'
```

**기대 출력**: `12` — `ref 141`이 §0~§11까지 온전히 `main`에 있다는 뜻.
(⚠️ iCloud 공유 메인 폴더는 `origin/main`보다 **1,458커밋+** 낡았다 — 그래서 `git show origin/main:`으로 읽는다.)

---

## 0. 목적 (원문 — 각색하지 않는다)

상위 목표는 그대로다: **D-NAO-59**(총이익 절대액 최대화) · 인계받은 **「1번을 자동화하자」**(Jino 2026-09-03).

이번 슬라이스는 n=93 HANDOFF §10이 실어 둔 Jino 지시다:
> **「n=94를 다음세션에서열고 /investigate로 C군 파보자」** (Jino, 2026-09-06 21:38 KST)

대상은 `ref 140` §7의 **C군 15그룹** — `shopping_group_bep`·`shopping_group_growth` 어느 보드에도 안 오는 그룹. 물음은 `ref 140` §8-3이 「미규명」으로 남긴 **「표본 부족인가 / 부모 체인 조건인가 / 산출 실패인가」**.

---

## 1. 산출물

- **신설** `docs/references/141_c_group_board_gap_20260906.md` — §0~§11(§11 = 적대 리뷰 1R·2R 기록)
- 트랙 `확인:` **2줄**(착수 1 + 정정 1) — 진행률은 **2/7 불변**, 체크박스 **0개** 새로 안 찍음
- PR **#768** (커밋 `f88b593b` · `9a95d153` · `14a6aa10`)

---

## 2. ★★ 세 가설이 전부 반증됐다

| 가설 | 처분 | 근거 |
|---|---|---|
| 표본 부족 | **원리적으로 불가** | 두 보드 어디에도 클릭·전환 하한이 없다. `shopping_group_bep:185`는 `cost>0`+`roas_c<BEP`뿐이고, `shopping_group_growth:1057` docstring이 *"저클릭 필터는 두지 않는다"* |
| 부모 체인 | **불가** | `_on_adgroup_ids`(`:368`)는 **growth에만** 걸린다 → BEP 미달이면 bep 보드에 그대로 뜬다. 라이브: 목표 초과 71 vs growth 보드 69 = 필터 3종이 계정 전체에서 걷어내는 건 **2그룹**, C군엔 0건 |
| 산출 실패 | **0건** | 15/15 전건 설명. 스냅샷 크론은 08-24~09-05 **13일 연속 매일 800~899행** |

**실제 사유는 둘이고, C군은 하나의 군이 아니었다.**

1. **띠 5그룹** — `BEP 1.6522 ≤ 보정ROAS < 캠페인목표 1.8692`. 두 보드 정의의 **여집합**이다. 30일 **701,369원** = C군 총 732,308원의 **95.8%**.
2. **14일 창 비용 0 인 10그룹** — `ref 140`은 30일 비용으로 골랐는데 보드 창은 `_LOOKBACK_DAYS = 14`. 이 10개를 매일 차단하는 것은 보드가 아니라 **탐색 레인**(`actor='explore'` · `bid_up_explore`)이다. 보드에 없는 게 정상이다.

---

## 3. ★띠의 규모 — 상시로 존재한다

창 2026-08-22~09-05 · SHOPPING 광고그룹 cost>0 **149개** · 14일 비용 **5,664,602원**:

| 구간 | 상한 자 ×1.3492 | 하한 자 ×0.827 |
|---|---|---|
| BEP 미달(브레이크) | 67그룹 · 1,364,796원(24.1%) | **96그룹 · 4,376,718원(77.3%)** |
| **띠(무액션)** | **11그룹 · 630,367원(11.1%)** | 9그룹 · 481,929원(8.5%) |
| 목표 이상(액셀) | 71그룹 · 3,669,439원(64.8%) | 44그룹 · 805,955원(14.2%) |

13일 시계열에서 **무보드가 비어 본 적이 한 번도 없다**(5~13그룹 · SHOPPING 광고비의 2.9~22.5%). 오염을 걷어낸 「띠만」은 **7·9·7·10·8·8·5·5·8·5·5·9·11**.

**S25울트라는 「나았다」도 「빠졌다」도 아니다** — 08-24~26 bep → 08-27~09-01 띠 → 09-02~03 growth → 09-04~05 띠. 원시 ROAS 1.2161→1.5133→1.3092에 자 1.3492를 곱하면 폭 13%의 띠를 그대로 통과한다. `retro_signal` 8일 공백은 크론 누락이 아니다.

---

## 4. ★★가장 무거운 것 — 비대칭

| | 무액션 집합 | 관찰 표면 |
|---|---|---|
| **손실(브레이크)** | at-floor·스톱로스 도달인데 더 못 내리는 유닛 | **있다** — `floor_wait_units`(`account_diagnosis.py:888`, D-NAO-65 UI3). `shopping_pause_candidates`와 여집합 정합까지 계약(`diagnosis.py:279-285`) |
| **이익(액셀)** | BEP는 넘지만 목표 미달(띠) | **찾지 못했다** — 훑은 범위에서 0건 |

엔진은 **「내릴 수 없어서 가만히 있는 것」은 세면서 「올릴 이유가 부족해서 가만히 있는 것」은 안 센다.** `ref 140` §7이 이걸 「판정 불능」이라 부른 것은 오독이다 — 엔진은 **「무액션」이라고 판정했고 그 판정을 아무 데도 안 적었다.** 북극성 §7 「액셀·브레이크 대칭」 검사에 걸린다.

⚠️「없다」가 아니라 **「훑은 범위에서 0건」**이다. 훑은 것: `accel_gate_view.py`(보드에 오른 행 위에서만 센다) · `NaverAdDiagnosisBoard.tsx` · `무액션`/`no_action` 전수 grep. **콘솔 API 라이브 호출은 안 했다.**

---

## 5. 적대 리뷰 — 1R **FAIL(P1 3건)** → 2R **PASS(P1 0)**

| P1 | 무엇 | 처분 |
|---|---|---|
| 1 | 「띠 5그룹 599,369원 · 84.5%」가 **문서 자신의 표와 어긋난다**(표 합은 701,369) + 84.5%의 분모가 `ref 140`의 **다른 30일 창** | 701,369원 · 95.8%로 정정. **초판은 과소평가였다** |
| 2 | 0원 6그룹을 **n=89(D-NAO-289)에 귀속**시킨 것이 오독 — `bid_up_explore`는 D-NAO-70·71 도입 · **머지 이전이 316건**(전체 358 기준 88.3%) · `cost=0`이라 n=89 모집단에 원리적으로 못 든다 | 귀속 삭제. **n=89의 「열어준 그룹 0개」는 흔들리지 않는다** |
| 3 | §8-4의 자기 흠 고백이 헐거웠다 — 오염 「하루 최대 2」가 실제 **4**, 미고백 오염원 1종(`shopping_pause_candidates` 겹침 3일) | 최대 4 + 겹침 명기, 「띠만」 시계열 추가 |

★★★**3R — 완료 QA 재판정이 2R의 수치를 반증했다**: 1R·2R이 통과시킨 「338건(94.9%)」은 **일자 단위 근사**였다. `ops_diary_entries.created_at`은 **UTC**인데(`SELECT datetime('now')`=로컬 UTC 일치 확인) 머지 시각 20:05:31 KST = **11:05:31 UTC**로 정밀히 세면 **316건(88.3%)**이다. 09-05 발화는 02:20~03:20에만 있지 않다(00시 6 · 07시 6 · **15~23시 22건**). **작성자·2R 리뷰어·1차 QA 세 층이 전부 같은 시간대 함정에 걸렸고 재판정 한 기만 시각까지 내려가 봤다** — 알려진 반복 함정(`naver_change_log`=KST · `ops_diary_entries`=UTC)의 재현이다. 결론 방향은 불변.

P2 8건 **전건 채택**. ★★**2R이 내 정정이 만든 새 오류를 또 잡았다** — §4의 「나머지 날은 1만원 미만」이 거짓(08-25 오염 42,385원)이고 같은 문장의 21.9%와 자기모순. **P1-3(흠 고백이 헐거웠다)을 고치면서 같은 자리에 새 축소를 넣었다.**

### ★★★변이 7종 전건 SURVIVED — 두 번째 재현이다

`ref 141`을 **통째로 지우고 트랙 확인줄 2개까지 지워도** backend **7,983 passed** · `check_pao_canon.py` exit 0 · 참조하는 테스트·CI **0건**. §2의 코드 사실을 **정반대로 뒤집어도**(「`_on_adgroup_ids`가 bep에만 걸린다」) 아무도 안 잡는다. §4 합계를 바꿔 백분율 3종이 서로 모순돼도 안 잡는다. **`ref 140` §11이 같은 것을 이미 보고했고 이번이 두 번째다** — 「같은 결함 두 번이면 모양을 고쳐라」에 걸린 자리.

---

## 6. ⚠️ 하네스 함정 — 훅 「트랙 기록 누락」의 진짜 원인이 잡혔다

**내가 23:33에 「iCloud 공유 폴더 stale 사본 오탐」이라 진단한 것은 부분적으로 틀렸다.** 적대 리뷰 변이 6이 `~/.claude/hooks/lib/track_scan.py`를 직접 돌려 보니 **클론에서도** 확인줄 유무와 무관하게 `{"parsable": false, "has_record": false, "n": 0, "m": 0}`이다.

원인: `docs/tracks/active/track_naver-ad-optimization.md`에 `<!-- TRACK-CONTRACT` **2개**(3행·33행) · `/TRACK-CONTRACT -->` **2개**(38행·2506행)가 있어 `len(opens) != 1`로 파싱을 포기한다. 33행은 2026-09-06 12:1x 세션(`531204ca`)이 **그 고장을 설명하려고 마커를 원문 인용**하면서 생겼고, 그 줄 자신이 이미 예고해 뒀다.

⇒ **이 트랙을 여는 모든 세션이 확인줄을 아무리 잘 써도 이 훅을 맞는다.** 마커 정리는 33행 확인줄의 **소급 수정**이라 이 세션은 안 했다. 부채로 넘긴다.
(공유 폴더 stale도 «동시에» 참이다 — 두 원인이 겹쳐 있었고 나는 하나만 보고 닫았다.)

기존 함정 유지: iCloud 공유 폴더는 워크트리를 못 만든다 → 로컬 전체 클론 `~/ohisell-clones/pao-n94`에서 작업했다. 훅 `⛔ 체인 n=49`도 같은 stale 사본이 만든 오탐(`origin/main` 기준 마지막 행은 n=93, `end_kst 21:19`로 닫혀 있었다).

---

## 7. 완료 QA (별도 Sonnet·읽기 전용) — 판정문 **원문 그대로**

### 재판정 1회 (같은 기 · 2026-09-07 00:31 KST) — 미달 항목 재작업 후

> **판정(앵커 합격기준): 달성** — *「1차 QA를 부분달성으로 만든 유일한 결함(701,369/95.8% 재현 실패)이 이번 재검산에서 문서 표·라이브 DB·직접 덧셈 3자가 완전히 일치하는 것으로 확인됨」*
> **판정(D-NAO-59): 미달** — *「코드 0줄·prod 쓰기 0·광고 API 호출 0(git diff·scope·change_log로 직접 확인), 총이익 절대액에 관측 가능한 변화 0건」*
> **판정(북극성 §1·§6): 부분달성** — *「§7 대칭 검사에 해당하는 구체적 비대칭을 실측 특정했고 M4 ⓐ의 잔여 모호성을 좁혔으나, 5요소 중 ③에 한정된 이동이고 ④는 이 세션도 0 — M4 자체는 닫히지 않음」*
> **종합 판정: 부분달성** — *「앵커 기준 자체는 달성으로 올라갔지만, D-NAO-59 미달과 북극성 부분달성이 종합을 끌어내린다」*

재판정이 남긴 판정불능 1건: §6의 「338건(94.9%)」 정밀 재현 실패 → 위 3R로 **316건(88.3%)**으로 정정했다.

### 1차 판정 (2026-09-06 23:41 KST) — 원문 보존

> **판정(앵커 합격기준): 부분달성** — *「4항목 중 3항목(2·3·4)은 라이브 증거로 완전 재현, 1항목(1)은 분류·좌표는 재현되나 핵심 집계 수치(599,369원, 정확값 701,369원)가 재현 실패」* (2026-09-06 23:41 KST)
> **판정(D-NAO-59): 미달** — *「코드 0줄·prod 쓰기 0·광고 API 호출 0이고 총이익 절대액에 관측 가능한 변화가 0건이다. 발견(액셀 쪽 무관찰 밴드)은 총이익 개선의 «전제조건 규명»일 뿐 총이익 자체의 이동이 아니다」* (23:41 KST)
> **판정(북극성 §1·§6): 부분달성** — *「§6 M4 해제 조건 ⓐ의 잔여 미규명 항목을 실제로 좁혔고 §7 대칭 검사에 해당하는 구체적 비대칭을 실측으로 특정했으나, 5요소 중 ③에 한정된 이동이고 ④는 여전히 0이며 M4 자체는 닫히지 않았다」* (23:41 KST)
> **종합 판정: 부분달성**

**미달은 낮추지 않고 그대로 남긴다.** QA가 잡은 부분달성 사유(합산 오류)는 적대 리뷰 P1-1과 **같은 결함**이고 정정됐다.

**「안 함」 침범: 없음**(QA 실측 — `naver_adgroup_scope` 9행 · `max(updated_at) 2026-09-05 12:14:50`(착수 이전) · 커밋 diff에 `backend/`·`frontend/` 0줄).

---

## 8. 트랙 진행률

- **2/7 불변**(M0·M1 달성 / M2~M6 미달). 체크박스 **0개** 새로 안 찍었다 — M4 해제 조건 ⓐ는 n=93이 이미 `[x]`이고 이번은 그 **잔여 흠**을 메운 것이라 새로 찍을 칸이 없다.
- ④자동화 운영 실집행 **이번 세션 0 — 연속 5세션째**(n=90·91·92·93·94).
- 북극성 대비 검토(D-NAO-226·227): §목차(§0~§9+부록A) 훑고 **§1·§4 간극3·간극4·§6·§6-b·§7·§8** 전독. 5요소 중 **③만 이동**. §7 대칭 = **이번 발견 자체가 비대칭의 특정**이다.

---

## 9. 남은 슬라이스 (고르는 건 Jino)

1. ★★**띠에 관찰 표면을 줄 것인가** — `floor_wait_units`의 액셀 쪽 대칭물. 코드 변경이라 **계약 1장**. 북극성 §7이 요구하는 대칭 검사에 직접 걸린다.
2. ★**`ref 140` §10의 확대 선택지가 이 결과로 바뀐다** — C군 15는 「판정 불능」이 아니라 **띠 5 + 지출 없음 10**이고, 띠 5그룹은 **지금도 BEP를 넘겨 돈을 벌고 있다**(목표에만 못 미친다). 확대 대상으로서의 성질이 다르다.
3. **탐색 레인이 스코프 밖에서 매일 만드는 후보의 처분** — 지금은 생성 → 차단이 매일 반복(356건/8일). 스코프를 넓히거나 탐색 후보 생성을 스코프로 먼저 거르거나 둘 중 하나.
4. **띠에 관찰 보드가 없는 것이 설계인지 누락인지** — `ref 141` §8-1이 흠으로 남겼다. `floor_wait_units`는 D-NAO-65에서 «명시적으로» 만들어졌는데 대칭물 부재가 결정으로 남은 기록을 못 찾았다.
5. **M4 해제 조건 ⓑ 대행사 개입 계수 · ⓒ 되돌림 감지율** — 3칸 중 2칸이 그대로 비어 있다.
6. **부채 둘**: 이 저장소는 `docs/references/*`의 존재·내부 정합을 검사하는 장치가 **0건**(두 번째 재현) · `track_scan.py`가 이 트랙 파일에서 원리적으로 작동 안 함(마커 2쌍).

⛔ **다른 세션 소관**: 착수 시점 기준 `origin/main` 등록부에서 열린 체인 없음(n=93 `end_kst 21:19`). 다만 n=93 프로세스(PID 66495)는 21:38까지 커밋을 더 썼다 — **등록부가 닫혀도 프로세스는 살아 있을 수 있다**(2단계 닫기의 그 구간).

---

## 10. 불변 확인

- **진행률 2/7 불변** · **코드 0줄 · prod 쓰기 0 · 네이버 광고 API 호출 0 · 마이그 0 · prod 배포 0 · 보드 로직 무접촉**
- `naver_adgroup_scope` **9행 · `max(updated_at)=2026-09-05 12:14:50`** — 이 세션 무접촉(완료 QA가 라이브로 재확인)
- 다음 가용 **D-NAO-297** · 교훈 **#401**(이번 세션은 296 소비, 교훈 미발급)
