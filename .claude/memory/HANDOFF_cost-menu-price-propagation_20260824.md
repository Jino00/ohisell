# HANDOFF — S5 착지: 단가 변경이 모든 표준원가에 전파된다 (D-CPP-55 · 합격 14~16)

2026-08-24 (KST) · 세션 `7bbf57c9` · 체인 **「SellC 원가 메뉴」 n=7**
워크트리 `/Users/jino/.claude-worktrees/ohiselling/cost-menu-n7` (브랜치 `feat/cost-menu-n7`)
**PR #402 병합 · prod 배포 완료** (백엔드 무중단 재시작 · 프론트 `index-BWks4jUh.js`)

---

## 0. 목표 — 다시 쓰지 마라

★**이 트랙의 목표에는 이름이 있다 — 「SellC 원가 메뉴 목표」.**
**정본은 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md`다**(실재 확인: 11,144바이트, `origin/main`에도 있다 — 이번 완료 QA가 「없다」고 적은 것은 낡은 사본을 읽은 오탐이다). 목표를 다시 쓰지 말고 그 파일을 열어 앵커 `목표:`에 원문을 옮긴다. 트랙 헤더와 갈라지면 **카드가 이긴다.**

> ***"SellC에 원가 메뉴를 하나 붙이자. 여기에는 우리가 현재 알고있는 모든 원가가 있잖아? 그 원가를
> 가지고 계산되는 방법이 첨부된 파일처럼 나오고 그 계산이 되면 되지 않을까? 그리고 수입건에 대해서는
> 두번째, 세번째 첨부자료처럼 수입한 금액을 알려주면 너가 이런것들을 모아서 회계적으로 계산되는
> 원가를 만들면 되잖아. 그리고 그런 원가를 만들려면 재고사항이 필요할꺼고, 그건 ecount와 연결하면
> 될꺼고."*** / ***"목표는 정확한 원가 산출이니까?"*** (Jino 2026-08-22)

## 0-A. ★운영 원칙 — Jino 지시 (2026-08-23 13:00, n=2→n=7 승계)

> ***"다음 세션에서부터는 SellC 원가 메뉴 목표를 달성하는 위주로 진행되게 목표잡기와,
> 다른 트랙의 일을 집어와서 여기에 섞지 말라"***

**① 목표잡기는 역산이다** — 착수 질문은 「무엇을 할까」가 아니라 **「합격 항목 중 어느 것을 [x]로 만드는가」**다.
**② 다른 트랙 일을 끌고 오지 않는다** — **같은 트랙 안의 계약 B도 남의 일이다.**

---

## 1. 30초 요약

**진행률 2/6 불변.** A1은 **여섯 세션째** Jino의 승인 클릭을 기다린다 — 이번엔 그것이 **태만도 슬라이스 설계도 아니고**, 계약 §2-2가 그 클릭을 사람 몫으로 못 박았기 때문이다(교훈 #274).

이번 세션이 한 것은 **Jino의 질문에서 시작해 계약 위반 하나를 찾아 닫은 것**이다.

| 무엇 | 상태 |
|---|---|
| 계약 A′ 개정 2 (D-CPP-55 · §0-D · §6 S5 · 합격 14~16) | Jino 승인(09:34) · 반영 완료 |
| 단가 변경 → 표준원가 전파 (6경로) | **prod 배포·병합 완료** |
| 「+ 수동 단가 입력」이 발효일을 함께 보낸다 | 배포 완료 |
| 합격 16 (무해성) | **달성** |
| 합격 14·15 (전파의 라이브 관측) | **판정불능 — Jino의 A1 클릭이 선행조건** |

---

## 2. ★다음 세션이 «가장 먼저» 할 일

### 2-A. A1 라이브 관측 — 이제 **두 가지를 동시에 연다** (n=3→n=7 승계, 여섯 세션째)

`https://sellc.ohitech.co.kr/cost` → **레시피 탭**
1. 제품 검색 **「빛반사」** → 「…매트 필름 3매」 → 폼팩터 **`bar`** (레시피 **id=68**)
2. 구성 **9줄** + 참고값(600·30·22·60·8·13·98·6·100) 확인 · **합계는 「—」여야 한다**
3. **「이 구성을 승인한다」** → **「엑셀 참고값을 단가로 채택」**
4. **표준원가 보드** → 같은 제품 → **2,350.70원**

★**산수는 이미 검산돼 있다**(n=7 실측): `3×600 + 337 = 2,137`(부가세 제외) `× 1.1 = 2,350.70` — 레시피의 `match.cost_price_mode`와 소수점까지 일치한다. 즉 **클릭 하나 뒤에 정확히 그 숫자가 나오도록 서 있다.**

⇒ 관측되면 트랙 `- [ ] A1` → `- [x]`(증거 좌표 병기) → **진행률 3/6**
⇒ **동시에 S5 합격 14·15가 관측 가능해진다**(아래 2-B).

### 2-B. 합격 14·15 라이브 관측 (A1 직후, 같은 화면)

A1으로 승인된 레시피가 생기면 그때 관측한다:
- **합격 14**: 부자재 탭에서 공용 종(예 「패키지 (bar)」) 단가를 바꾸면 → **표준원가 보드**에서 그 종을 쓰는 승인 레시피 **전부**가 새 값으로. prod 실측상 bar 필름 레시피 **8개**(48·49·50·66·**68**·72·83·100)가 부자재 **8종**을 공유한다.
- **합격 15**: 그 단가를 삭제하거나 종을 미승인으로 되돌리면 → 보드가 **「—」+사유**로(0이 아니다).

★**단가를 넣을 때 발효일을 반드시 확인**한다 — 화면이 오늘 날짜를 미리 채워 묻는다. 비우면 그 값은 「최신」이 못 되어 **바꿔도 아무 일이 안 일어난다**(§4 참조).

### 2-C. 합격 10의 라이브 눈 확인 (n=6 승계, 여전히 열림)

레시피 탭 왼쪽 목록을 **마우스로 스크롤**했을 때 오른쪽 상세 패널이 화면에 남는가. 계약 §7-10이 *"jsdom이 원리적으로 못 잰다 — 라이브 눈 확인이 판정"*이라고 스스로 적어 둔 항목이다.

### 2-D. S3 (합격 5~8) — A2로 가는 경로

**착수 전 Jino 답 1건**이 정밀도를 정한다(§8 참조). A1·합격 14·15엔 이 답이 필요 없다.

---

## 3. 무엇을 만들었나

### 3-1. ★발단 — Jino의 질문이 계약 위반을 드러냈다

Jino가 화면을 보며 두 가지를 물었다: *"이게 단가와 금액으로 숫자가 들어가지는건가?"* → *"만약 부자재중 하나의 단가가 변경되면 다른 제품에서 그 부자재가 사용되는 단가는 같이 변경되는거지?"*

코드를 읽어 답하다 **갈라짐**을 찾았다:
- `recompute()` 호출부가 저장소 전체에 **3곳뿐**(승인·승인취소·엑셀채택) — **단가를 쓰는 경로엔 한 곳도 없었다.**
- 보드는 계산하지 않고 **저장 행**을 읽는다(`recipes.py` `board()` → `std.std_cost_inc_vat`).
- ⇒ **레시피 상세(조회 시점 계산)는 즉시 새 단가로 바뀌는데 보드는 옛 값이 남고, 「계산 후 단가가 바뀌었다」고 말하지도 않는다.**

★★**이건 새 요구가 아니라 계약 §2 판단기준 5를 어긴 상태였다** — 계약이 이미 *"표준원가의 갱신 주체는 항상 원장이다 — 사람이 아니다 … 로트 확정 이벤트가 단가 이력을 만들고 표준원가가 재계산된다"*고 적어 뒀다. **그런데 합격기준 1~13 어디에도 그것을 재는 항목이 없었다.** 적대 리뷰는 「코드가 옳은가」를, 완료 QA는 「합격기준을 충족했나」를 묻는데 **기준에 자리가 없으면 둘 다 통과한다.** 아무도 거짓말하지 않았는데 결함만 살아남았다 — 쿠팡 손익정합 체인 n=8이 같은 형태를 기록했고 **이것이 두 번째 실측**이다.

★같은 병의 흔적이 이미 코드에 있었다: `adopt_excel_prices` 주석이 *"상세에는 2,350.70이 뜨는데 **보드만 빈 칸**"*이라고 **정확히 이 병을 적고 채택 경로 하나만 닫아** 뒀다.

### 3-2. 처분 — 트리거 한 자리 + 6경로 (`1a216626`)

`recipes.recompute_for_material(s)` **하나**가 「그 종을 쓰는 **승인된** 레시피를 다시 계산한다」의 유일한 구현이다. 경로마다 사본을 두지 않았다 — 사본을 두면 7번째 경로가 또 한쪽만 고쳐진다(S4의 `LIST_COLUMN_SCROLL_CLASS`와 같은 처분).

| # | 경로 | 위치 |
|---|---|---|
| 1 | 원장 라인 연결 | `materials.link_ledger_line` → `_propagate` |
| 2 | 연결값 refresh | `materials.refresh_ledger_price` → `_propagate` |
| 3 | 수동 단가 추가 | `materials.add_manual_price` → `_propagate` |
| 4 | 단가 삭제 | `materials.delete_price` → `_propagate` |
| 5 | 종 승인/해제 | `materials.update_material`(`status`일 때만) → `_propagate` |
| 6 | 엑셀 채택 | `recipes.adopt_excel_prices` → 채택 종 전체를 한 번에 |

★**6번은 «세면서» 찾았다** — 「채택 경로는 이미 닫혔다」가 **절반만 참**이었다. 채택은 *종*의 단가를 만드는데 자기 레시피만 재계산해, 그 종을 공유하는 다른 승인 레시피는 안 따라왔다.

### 3-3. 구현 중 실측이 잡은 것 2건

**① `cost_standard` 중복 INSERT** — 한 레시피가 9종을 쓰니 트리거가 같은 레시피에 아홉 번 닿는데, `autoflush=False` 세션에서 방금 만든 행이 다음 조회에 안 보여 두 번째 행을 만들고 유일 제약에 걸렸다. `recompute()` 끝에 `flush`를 넣어 닫고 테스트로 잠갔다. **픽스처가 prod 세션과 같은 설정이라 잡혔다** — 달랐으면 prod에서 터졌을 자리다.

**② ★화면까지 가서야 나온 것 — 전파를 다 고쳐도 화면에선 안 움직였을 것이다.**
「+ 수동 단가 입력」이 `effective_date`를 **안 보냈다**(`CostPage.tsx`). 「최신 단가」는 `(effective_date, id)` 내림차순이고 `null`은 맨 뒤로 가므로, 채택분(오늘 날짜)이 있는 종에 날짜 없는 단가를 넣으면 **이력에만 남고 계산엔 영영 안 쓰인다.** 합격 14는 화면 표면으로 판정되니 이 한 칸이 빠지면 슬라이스가 「API로만 되는」 상태가 된다.
⇒ 발효일을 **오늘로 미리 채워 묻고** 함께 보낸다(서버가 몰래 채우면 §2-7 위반이라 사람에게 보여 주고 고칠 수 있게).

### 3-4. 테스트 — 전부 HTTP + 보드 경유

`backend/tests/test_cost_menu_price_propagation.py` **12종 신설.** `recompute_for_material`을 직접 부르지 않고 **사람이 화면에서 하는 일**을 API로 하고 **보드가 무엇을 말하는지**만 본다 ⇒ `_propagate` 호출 한 줄만 지워도 깨진다. 픽스처는 **prod 모양을 축소 재현**(레시피 2개가 부자재 8종 공유).
프론트 앱 경로 2종 추가(발효일 동봉 · 날짜 물음 취소 시 저장 안 함).

---

## 4. 적대 리뷰 — 1R FAIL(P1 1건) → 2R PASS(P1 0건) · 변이 누적 14종

### ★★1R P1 — **이 슬라이스가 고치려던 병이 이 슬라이스 안에서 재생산돼 있었다**

**재현(리뷰어)**: `link_ledger_line`·`refresh_ledger_price`의 `_propagate` 한 줄을 각각 지워도 cost_menu 스위트 **91 passed, 0 failed**. 즉 내 커밋 메시지의 *"호출부 한 줄만 지워도 깨진다"*가 **6경로 중 4경로에서만** 참이었다.

**뿌리**: 나는 경로를 세어 **코드는 6곳 다 배선**했는데 **«테스트가 그 경로를 밟는가»는 안 셌다.** 구조가 나머지 4경로와 같아 작동할 개연성은 높았으나 그건 **추정**이고, 격리 성공은 충분조건이 아니다.

**처분(`ea99075a`)**: 경로 1·2용 테스트 2종 신설 후 **같은 변이를 내가 재주입해 닫힘 확인** — 경로 1 제거 → 1 failed · 경로 2 제거 → 1 failed(직전엔 91 passed). 둘 다 원복·워킹트리 클린.

★단언을 값 하드코딩이 아니라 **「보드(저장 행) == 상세(조회 시점 계산)」 등식**으로 잡았다 — 이 계약이 고친 병이 정확히 그 둘의 갈라짐이라, 반올림 규칙이 바뀌어도 유효하다.
★로트는 prod 실측 `LOT_JUL`을 **임포트**해 쓴다(복사하면 한 벌이 낡는다). **통관일만 오늘로** 두는데, 과거 통관일이면 채택분(오늘)을 못 이겨 «전파가 돌아도 화면이 안 바뀌는» 무효 시나리오가 되기 때문이다.

### 1R 변이 10종 결과

| 변이 | 대상 | 결과 |
|---|---|---|
| M2·M3 | 경로 1·2 `_propagate` | **SURVIVED** → P1-1 |
| M1·M4·M5·M6 | 경로 5·3·4·6 | KILLED |
| M7 | `db.expire(material, ["prices"])` | KILLED (3건) |
| M8 | `recompute`의 트레일링 `db.flush()` | KILLED (**10건 전부 IntegrityError**) |
| FM1·FM2 | 프론트 `effective_date` / 취소 가드 | KILLED |

### 2R — «새 테스트가 가짜로 통과하지 않는가»를 검사했다

- 1R P1 **해소 확인**: 같은 변이 재주입 → 둘 다 KILLED
- `recompute` 저장값에 `+1` 주입 → **12건 전부 실패**(등식이 항등식이 아님을 직접 확인)
- `db.expire` 제거 → `test_linking_...`이 `!= {"2350.70"}` 줄에서 실패
- 보드(저장 행)와 상세(조회 시점 계산)가 **실제로 다른 코드 경로**임을 코드로 확인

**P2 트리아지**: P2-1(N+1 재계산) **채택** → `recompute_for_materials`로 종 집합을 한 번에 · P2-2(발효일 기본값이 클라이언트 타임존 의존) **기각** — 자동 저장이 아니라 `prompt` 기본값으로 사람에게 보여 주고 고칠 수 있어 §2-7에 부합(리뷰어도 「실질 위험 낮다」로 동의).

**2R 이월 권고 1건**: `test_refreshing_a_ledger_price_reaches_the_board`가 stale-collection류 변이에 상대적으로 덜 민감하다 — 왜 경로 2가 그 변이에서 안 잡혔는지 **미확인(INCONCLUSIVE)**. 1R P1이 지정한 변이는 두 경로 다 KILLED이므로 합격 기준 자체는 충족.

---

## 5. 완료 QA — 판정 원문 그대로 (미달·판정불능 포함)

별도 서브에이전트(Sonnet·읽기 전용)가 **대조 3건을 각각 따로** 판정했다. **원문 그대로 옮긴다.**

> **판정(계약 A′ §7 합격 14·15·16): 부분달성 — 합격16 달성(무해성 확인) / 합격14·15 판정불능(전파 메커니즘은 코드·격리 테스트로 강하게 뒷받침되나, prod에 승인 레시피가 0건이라 관측 대상 자체가 없음). ★단, 2R 미완료 정황이 사실이면 계약 자체 규칙상 §3 금지선 위반="미달"에 해당할 수 있어 이 판정은 그 전제 위에서만 유효함을 병기. (2026-08-24 10:2x KST)**

> **판정(Jino 지시 원문): 부분달성 — "같은 부자재를 어디서 바꾸든 모든 원가에 같이 적용"의 메커니즘은 6개 쓰기 경로가 단일 트리거(recompute_for_material)로 수렴하도록 코드에 구현됨(구조적으로 지시를 충족). 그러나 지시가 뜻하는 "적용되어야지"의 실제 관측 가능한 사례는 prod에서 아직 0건 발생 — 승인된 레시피가 하나도 없어 "모든 원가"에 해당하는 대상이 라이브에 존재하지 않는다. (2026-08-24 10:2x KST)**

> **판정(트랙 궁극 목표): 판정불능 — 트랙 체크박스는 이번 슬라이스로 변화 없음(2/6, A1·A2·C1·C2 전부 미착수). 이 세션의 작업(D-CPP-55·S5)은 계약 A′ 안의 정합성 보강 슬라이스로, 트랙의 궁극 목표(정확한 원가 산출·재고/ecount 연동)에 닿았는지를 잴 수 있는 최소 표면(A1: 승인된 레시피 1건이 라이브에서 관측되는 것)조차 아직 존재하지 않아 기여도를 라이브로 판정할 수 없다. (2026-08-24 10:2x KST)**

### ★★QA가 잡은 내 기록 결함 — 지적이 옳다 (다음 세션이 반복하기 쉽다)

QA 원문: *"**2R이 실제로 완주됐다는 기록이 어디에도 없는 채로 슬라이스가 닫혔다(배포+병합)**"*

**사실관계**: 2R은 **실제로 돌았고 PASS(P1 0건·변이 누적 14종)**였다. 그러나 나는 **그 판정을 기록에 남기기 전에 배포·병합**했고, QA가 읽은 `origin/main` 트랙 파일에는 「2R 진행 중」까지만 있었다. ⇒ **그 시점의 기록만으로는 「PR 경계 리뷰 1회」(계약 §3 금지선) 충족을 검증할 수 없었다.**

★**교훈의 일반형**: 문제는 리뷰를 «했는가»가 아니라 **«했음을 남겼는가»**다. 판정은 **배포 전에 기록한다** — 안 그러면 「받았는데 받은 것처럼 안 보이는」 상태가 되고, 그건 다음 판정기에게 「안 받았다」와 구별되지 않는다.

### QA의 오탐 1건 — 실측으로 반증

QA: *"`.claude/memory/chains/sellc-원가-메뉴.GOAL.md` — 파일이 존재하지 않는다(`ls` 확인)"*
**실측**: 워크트리·`origin/main` 둘 다 **실재**(11,144바이트). QA가 **209커밋 뒤진 공유 메인 폴더 사본**을 읽었다.
★★**같은 함정의 여덟 번째**이고, 이번엔 **QA가 트랙 파일에서는 그 함정을 알아채 배제했는데 이 파일에서는 밟았다** — 한 세션 안에서 같은 주체가 한쪽만 방어한 것이다. 소관: 하네스.

### QA가 확인하지 못한 것 (원문 요약)

1. 2R이 (기록에 안 남긴 채) 실제로 수행됐는지 — 트랜스크립트 접근 불가
2. 합격 14·15의 **실브라우저 렌더링** — API/DB/코드 텍스트만 확인
3. CI FAILURE 2건이 이 PR과 무관한 사전 결함인지 — **직접 재검증 안 함**(구현자는 재현했다 — §7)
4. `GOAL.md` 실재 — 낡은 사본을 읽었다(위 오탐)
5. §9 리스크 항목 — 이번 범위 밖

---

## 6. 트랙 진행률

- 트랙: `docs/tracks/active/track_cost-truth-ledger.md`
- **시작 2/6 → 종료 2/6 (불변)**
- 달성 유지: **B1**(원장 스키마+3중 검산) · **B2**(8/18 실건 라이브)
- 미달: **A1**(승인 클릭 대기 — **여섯 세션째**) · A2(S3) · C1 · C2
- 이번 세션이 움직인 것: **체크박스 0개.** 계약 A′ **합격 16을 라이브로 닫았고** 14·15는 배포까지 갔다(관측은 A1 대기) — 증거: PR #402 · prod 스탬프 `commit=3b6a577b0115…`
- 트랙 목표 원문: §0과 동일
- 다음 세션 후보: **§2-A(A1) → §2-B(합격 14·15) → §2-C(합격 10) → §2-D(S3)**

---

## 7. 산출물 좌표

- `1a216626`(S5 구현) → `ea99075a`(1R P1 수정) → 트랙 기록 커밋 → **PR #402 병합**
- prod 배포: 백엔드 2파일 CAS 통과 · **무중단 재시작 다운타임 0초**(활성 :8011)
  프론트 `index-BWks4jUh.js`(로컬 dist와 해시 동일) · 스탬프 `commit=3b6a577b0115… branch=feat/cost-menu-n7 ts=2026-08-24T01:09:30Z`
- **무해성**: `product_master` **963행 · SUM 3,642,052.099999993** — 배포 전후 **완전 동일**
- **승인 0건 불변**: `cost_recipe` 100건 전부 `draft` · `cost_standard` **0행** · `cost_material` 129종 전부 `unconfirmed` — **모델이 승인·채택을 대신 누르지 않았다는 증거**
- 테스트: cost_menu **93 passed** · 프론트 **800 passed / 58 files** · `tsc -b` 통과
- ⚠️ **`safe_merge.sh --force` 1회** — CI 3건 FAILURE 상태로 병합. 근거는 주장이 아니라 **대조**다:
  `origin/main cc4cb458`에 기준선 워크트리를 띄워 돌리니 **백엔드 같은 2건**(`test_health_partial_sync` · `test_vendor_item_axis`)**과 lint `96 problems (0 errors, 96 warnings)`가 개수까지 동일하게 재현** ⇒ 내 diff가 만든 CI 실패 **0건**.
  ★참고: main 쪽 최근 런들은 **3잡 모두 2초·로그 없음(결제정지 서명)**이라 「원래 빨갛다」의 **종류가 다르다** — 그걸 근거로 삼지 않고 직접 재현했다. 자백 로그 `$TMPDIR/safe_merge.log`
- 계약 개정: `docs/PLAN_cost-menu-standard-cost.md` §0-D · §6 S5 · §7 합격 14~16 · §8 — **순수 추가**(§1 목표·§3 금지선·S1~S4·합격 1~13 불변)

---

## 8. 이월 (고치지 않고 운반 — 소관 지목)

**이번 세션이 새로 만든 것 / 찾은 것**
- **2R 이월 권고**: `test_refreshing_a_ledger_price_reaches_the_board`가 stale-collection류 변이에 덜 민감한 이유 미확인(INCONCLUSIVE) → 소관: 이 계약
- ★**원장 재확정이 표준원가를 흔드는데 전파가 없다**: 수입건을 reopen→재확정하면 연결 단가가 「어긋남」이 되어 `latest_price`가 사라지는데, **보드는 옛 표준원가를 계속 보여준다**(사람이 「갱신」을 눌러야 따라온다). 같은 계열의 병이지만 트리거가 계약 B 도메인(confirm/reopen)에 있다 → 소관: **계약 B ↔ A′ 경계**(새 슬라이스 필요)
- ★**판정을 배포 전에 기록하지 않았다** — §5 참조. 절차 결함이고 다음 세션이 반복하기 쉽다 → 소관: 이 체인의 작업 순서

**하네스 (실측됨)**
- ★★**공유 메인 폴더 낡은 사본 함정 — 이번 세션에서 2주체가 밟음**(착수 훅 `⛔ n=2 살아 있음` 오탐 · 완료 QA의 `GOAL.md` 오탐). **누적 여덟 번째.** 이번 실측 **209커밋** 뒤짐 → 소관: 하네스(전역 훅 + 위임문 규격에 「정본은 `origin/main`」)
- `track-progress-sync.sh`가 워크트리 트랙 파일을 못 본다 — **6세션 연속** → 소관: 하네스
- 기존 CI 부채 2건(`test_health_partial_sync` · `test_vendor_item_axis`) + 프론트 lint 96 warnings(exit 1) — **origin/main에서 재현 확인** → 소관: 수집 파이프라인 헬스 / 프론트 lint 정책(이 계약 밖)

**n=3~n=6에서 승계 (여전히 유효)**
- `scripts/safe_deploy.sh`가 **prod에 없는 신규 디렉터리를 만들지 않는다** → 소관: 배포 스크립트
- `scripts/next_ids.sh`가 **D-CPP를 발급하지 않는다** → 소관: 스크립트(별건)
- 레시피 탭 왼쪽 칼럼 폭 `320px` 고정(부자재 탭은 `minmax(22rem,28rem)`) → 소관: 이 계약
- `unreachableReason`의 종 미발견 분기 문구 → 소관: 이 계약
- **계약 B 소관**(고치지 않음): 수입건 id=1 관세 `is_duty` 미표시 · 자금정산서 파서 미구현 · 13개월 소급 적재
- `cost_drift` 배포 전 스냅샷 부재 · 전역 `PRAGMA foreign_keys` 미설정 · M16 SURVIVED · P2 3건
- AI_office 수입원가 트랙 중복 → **Jino 결정 대기**

---

## 9. Jino 답 대기 1건 (n=2→n=7 승계, 여전히 열림)

상품명 하나에 옵션이 최대 **163종**(갤럭시Z 56 · 아이폰 45 · 갤럭시S 34 · 갤럭시A 17 · LG V40).
**부품 «종류»는 같아도 필름 «면적»은 기종마다 다르다.** 계약은 **폼팩터 9종**으로 가르기로 했으나 **같은 폼팩터 안의 기종 차이**는 열려 있다. 답은 **A2(S3)의 정밀도**를 정한다(A1·합격 14·15는 답 없이 진행 가능).

---

## 10. 못 한 것

- **A1** — 승인·채택은 사람 몫이라 누르지 않았다. **여섯 세션째** 열려 있다.
- **합격 14·15의 라이브 관측** — 승인된 레시피가 0건이라 **원리적으로 불가**했다. 모델이 prod에 수동 단가를 넣어 증거를 만드는 것은 계약 §3 금지선이다(교훈 #274와 같은 자리).
- **합격 10의 라이브 눈 확인** — n=6에서 승계, 그대로 열림.
- **S3(합격 5~8)** — 착수 안 함. Jino 답 1건이 정밀도를 정한다(§9).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
