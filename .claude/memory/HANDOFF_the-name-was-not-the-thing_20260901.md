# HANDOFF — pao-논의 n=80 (세션 14873038, 2026-09-01 14:36~17:3x KST)

> 제목의 뜻: **이름으로 기능을 찾으면 틀린다.** 계약이 가칭으로 적어 둔 것을 그 이름으로 grep해
> 「미구현」으로 볼 뻔했고(실제로는 완비), 반대로 콘솔 파일에 grep 0건인 문구가 화면엔 떠 있었다.

## §0. 작업 목적 (원문 — 발명하지 말고 이어받을 것)

계약 `docs/contracts/CONTRACT_pao_purpose_and_hands.md` (**D-NAO-280**, 승인 2026-08-31 09:24) 발단 Jino 원문:

> "이제 전체적으로 보자. 1번 계약, 1번 이슈를 포함해서 운영을 위해 손을 달고 쿠팡에서 잘 팔리는데도
> 불구하고 노출을 일으킬 수 없는 경우 어떻게 극복방안을 종합해서 계약서를 작성하자" (2026-08-30 21:45)

**목표이름: 목적관철 목표** · 트랙 `docs/tracks/active/track_naver-ad-optimization.md`

## §1. 이번 세션이 한 것 — 슬라이스 둘

Jino 지시 두 건으로 갈렸다.

### 슬라이스 ① prod 배포 (Jino 14:4x «prod 배포 먼저»)
n=77·n=78이 main에 머지해 두고 **prod에 안 올린** PAO 코드를 배포했다.
- `safe_deploy.sh` 백엔드 **20파일 · CAS 20/20 통과**(거부 0 · `--force` 미사용)
- 마이그 **148/148 완전 평형**(prod DB 이미 head `shipyard1s1a`) ⇒ `--migrate` 불요
- **무중단 재시작 다운타임 0초**(:8011 → :8001, 05:53:51Z 전환)
- 프론트: 스탬프 CAS 통과 → `npm run build` 재빌드 → 배포

**★배포 전 사전검사가 지뢰 둘을 잡았다** (이 슬라이스의 실질):
1. **심볼 개명** — n=77이 `AD_BID_CANARY_CAMPAIGNS`를 개명했는데 prod의 다른 파일이 그걸 **심볼로** import한다. 부분 배포면 부팅이 깨진다. prod 심볼 **33개 전수 대조** → 사라진 건 그 하나뿐이고 **쓰는 파일 4개가 전부 배포 집합 안**임을 확인 후 한 묶음으로 배포.
2. **스위치 폴백** — `scheduler_service.py`가 `cs_dry_run`을 env→**DB 우선**으로 바꾼다. prod `backend/.env:53`에 **`NAVER_CS_DRY_RUN=0`(실쓰기 허용)**이 실재해서, DB에 값이 있었으면 콜드스타트 레인이 배포 순간 뒤집혔을 자리다. **DB `guardrail_params` 행이 없음을 직접 조회**해 동작 불변 확인(코드 주석의 주장을 믿지 않고 DB를 봤다).

### 슬라이스 ② 계약 §6 P2 잔여 (Jino 15:14 «P2 잔여부터 이어서 진행해줘»)
계약 진행률 **9/38 → 12/38** · 트랙 **2/7 불변**.

**P2 7항목 전수 실측이 그림을 바꿨다** — 「없다」고 본 넷 중 하나가 이미 완비였다:

| # | 항목 | 결과 |
|---|---|---|
| 1 | H1 `auto_operate` 버튼+API+감사행 | ✅ 이미 있음(n=77) → **체크** · 라이브 「엔진 끄기」 확인 |
| 2 | 킬스위치 런타임화 + 「OFF=카나리 복귀」 화면 | ✅ 이미 있음(n=77) → **체크** · 라이브 문구 확인 |
| 3 | H2 발의→승인→실행 change_log | 입구 완성 · 계약이 `optimizer='ours'` 복귀 후로 **명시 이월** |
| 4 | 쇼핑 재개방 유형별 dispatch | ✅ **D-NAO-271로 이미 완비**(적대 리뷰 3회 상환) — 라이브 검증만 남음 |
| 5 | 「복원 발의」 버튼 | ❌ 없음 — **백엔드 선행 필요** |
| 6 | void 프론트 배선 + H5 일괄 지정 | ❌ 없었음 → **이번에 구현**(PR #633) |
| 7 | 카나리 상수 개명 | ✅ 이미 있음(n=77) → **체크** |

**★①②⑦은 n=77이 한 일이 계약에 안 옮겨져 있던 것**이다(일이 안 된 게 아니다).

## §2. 완료 QA — 판정 원문 그대로 (별도 Sonnet · 읽기 전용)

> **판정(계약 §6 P2): 부분달성** — 3항목 실측 정합(달성) · 2항목 신규 구현했으나 계약 증거문 미갱신(부분) · 2항목 명시 이월(합리적, 미달 아님) (2026-09-01 17:2x KST)
>
> **판정(Jino 지시 원문): 부분달성** — 「prod 배포 먼저」는 완전 달성 · 「P2 잔여부터 이어서 진행해줘」는 실행됐으나 산출은 "재고+2건 구현"이지 "잔여 완결"이 아님
>
> **판정(북극성 5요소): 부분달성** — ④자동화의 "전제"만 움직임(정직하게 자기 한정), 트랙 M 진행률 2/7 불변, 표류 없음
>
> **판정(종합): 부분달성**

**★QA가 잡은 실결함 1건 — 그 자리에서 수리했다**: 내가 16:28에 계약에 「⑥ 셋 중 하나도 없음」이라 적고, 16:33·16:56에 **실제로 구현한 뒤 그 증거문을 안 고쳤다.** 다음 세션이 grep하면 「코드도 없다」고 오판해 같은 것을 다시 만들 뻔했다. → 커밋 `<이 세션 종료 커밋>`에서 「갱신(n=80 종료 시점)」 문단 추가(체크박스는 미체크 유지 — §14 레시피 관측이 아직 없다).

**QA가 확인 못 한 것 (그대로 보존)**: 마이그 148개의 그래프 전수 일치(단일 head 여부) · 콘솔 스크린샷 재관측 · 적대 리뷰 변이 5종의 재실행 재현 · `safe_deploy.sh` CAS 로그 원문.

**QA 이월 3건**: ⑥ 증거문 낡음(수리 완료) · 앵커의 프론트 스탬프 인용 해시 부정확(`1e7318d3` → 실제 `bdcea3a8`, 둘 다 HEAD 조상이라 판정 불변) · 「change_log 최대 id 7920 불변」이 시간이 지나며 깨짐(7934 — **다른 세션의 자동 집행** 때문이고 n=80 쓰기는 0이 맞다. 판정 근거를 「고정 스냅샷」이 아니라 「시간에 취약한 절대값」으로 쓴 사례).

## §3. 적대 리뷰 — 1R FAIL(P1 1) → 상환 → 2R PASS(미해소 0)

**P1-1 — 일괄 버튼이 «역할·메모»를 조용히 지웠고 확인 문구는 정반대를 단언했다.**
역할 셀렉트를 안 건드리고 「전부 끄기」를 누르면 `role:null`이 나가 라벨 3건·메모 3건이 소멸하는데, 대화상자는 *「행은 남고 꺼지기만 합니다」*라고 적혀 있었다. 메모 소멸은 원장에 기록조차 안 됐다.
→ **「안 보냄」과 「명시 null」을 값으로 가른다**(`adgroup_scope.KEEP` sentinel + pydantic `model_fields_set`). 화면 기본값을 **「역할 유지」**로, 「역할 지우기」는 별도 선택지로.

**★★생존 변이 5종이 이번 리뷰의 진짜 산출물이다.** 내가 위임문에 「표면 절단 변이를 반드시 1개 넣어라」를 못 박아 보냈는데 **그 표면 절단 변이 2개가 둘 다 살아남았다** — 내 테스트가 못 막았다:

| 변이 | 왜 안 잡혔었나 |
|---|---|
| MB-1 `changed_at` 제거 → UTC 낙하 | 주석이 「같은 함정 세 번」이라 못 박은 KST 규약을 **아무 테스트도 안 지켰다** |
| MB-10 행마다 commit → 원자성 파기 | 계약·docstring·주석에 **세 번** 적힌 성질인데 테스트 0건 |
| MF-1 일괄 후 `onChanged()` 제거 | 표면 절단 — 「1건 바뀜」을 읽는데 표는 옛 값 |
| MF-2 void 후 `setReloadKey` 제거 | 표면 절단 — 무효화한 행이 목록에 남는다 |
| MF-3 역할 셀렉트 미전달 | P1-1이 무방비였던 이유 |

각각 테스트를 세워 재주입 사망 확인. MB-11(죽은 `rows` 필드)은 필드 자체를 제거해 구조로 해소.
**P2 트리아지**: 채택 4(창 밖 그룹 경고 · 원소 길이 제약 · `rows` 제거 · `adgroup_scope_change` 등록) / **이월 1**(`prompt`·`confirm` 억제 환경 — 「취소」와 「억제」를 구분할 수 없는 구조적 한계).

## §4. 교훈 #382 — 변이는 「넣었다」가 아니라 「실제로 실행됐나」까지 봐야 한다

같은 변이(MB-10)를 **두 번 연속 잘못된 자리에 넣어** 「생존」으로 오독할 뻔했다. ①갱신 분기에 넣었는데 테스트가 만드는 행이 전부 신규라 **early return** ②자리를 옮겼는데 이번엔 수정 «전»이라 **no-op**. 세 번째로 수정 «뒤»에 넣고서야 죽었다.

**처방 셋**: ①변이 줄에 `raise`를 잠깐 넣어 **도달 여부부터** 확인 ②분기가 둘이면 변이도 둘(2R 리뷰어가 독립 재현 — 갱신 경로에만 심으면 신규 테스트 22 passed 그대로) ③변이 표에 **「어디에 넣었나」**를 적기.

## §5. 트랙 진행률

- 트랙: `docs/tracks/active/track_naver-ad-optimization.md`
- **시작 2/7 → 종료 2/7 (불변)** — 달성 M0·M1 / 미달 M2~M6
- 계약: **시작 9/38 → 종료 12/38**
- 이번 세션이 움직인 항목: 계약 P2 ①②⑦ 체크(증거 좌표 병기) · ⑥ 코드 구현(체크는 §14 관측 대기)
- **트랙 목표 원문**은 트랙 파일 `<!-- TRACK-CONTRACT v1 -->` 블록 참조
- **북극성 대비**: ④자동화의 «전제»(끄는 손·되돌림 손)만 이동. 실집행은 이 슬라이스가 아니다.

## §6. 착지

| 단계 | 결과 |
|---|---|
| 커밋 | `729e4128`(계약 실측) · `5870fe8d`(구현) · `1b2451de`(1R 상환) · `0ff2ae75`(트랙 확인줄) |
| PR **#633** | 머지 `8531391f` · CI **3/3**(py3.10 11m24s · py3.14 10m36s · frontend 1m44s) |
| PR **#634**(기록물) | 머지 `1acfa0f2` · CI 3/3 |
| 적대 리뷰 | 1R FAIL → 2R **PASS** |
| `--force` | **미사용** |
| 멈춘 단계 | **없음** |
| prod 배포 | 슬라이스 ①만(백엔드 20파일 + 프론트). **슬라이스 ②는 prod 미배포** |
| 네이버 광고계정 쓰기 | **0**(n=80 귀속) |

## §7. 다음 세션 후보 (묻지 말고 이어서 — §1 승인 지점 아님)

1. ★**슬라이스 ② prod 배포** — void·H5가 main에만 있고 prod엔 없다. 배포해야 §14 레시피 관측이 가능하고, 그래야 계약 ⑥ 체크박스가 닫힌다.
2. **계약 P2 ④ 라이브 검증** — WEB_SITE 1건 + SHOPPING 1건 재개방 + 「나머지 제외 생존」. ⚠️**네이버 계정 쓰기**이고 대상 캠페인 `auto_operate`가 켜져 있어야 한다. 착수 전 그 스위치 상태를 먼저 잴 것.
3. **계약 P2 ⑤ 복원 발의** — 백엔드 선행 필요(정본이 복원용 필드를 내려줘야 함. 화면이 표시 문자열을 파싱하면 「베껴 적기」다). 실효 캠페인이 1개뿐이라 우선순위는 낮다.
4. **주기 구조 감사** — 마지막 산출물 08-21로 **11일 경과**, 전역 §4 트리거 발동 상태. 이번 세션은 Jino가 P2를 택해 안 했다.

## §8. ⚠️ 주의 / ⛔ 무접촉

- ⛔ **무접촉**: `발주예측` n=8 · `승인배선` n=2 (둘 다 origin/main `end_kst: null` = 살아 있음)
- ★**관측(다른 세션 소관)**: `d02f7c89`가 **PAO 첫 자동 집행을 라이브 관측**했다 — 16:20:35 KST, 소재 입찰 910→1,040원, **33일 만의 첫 `update_bid` execute**. 그 세션이 16:03:12에 `auto_operate`를 켰고 그 뒤 시간당 레인이 자동으로 돈다. **북극성 ④자동화가 0에서 처음 움직인 기록**이다. 손대지 않았다.
- ⚠️ **훅 stale 오탐 상습**: `[체인] ⛔ pao-논의 n=49` / `sellc-원가-메뉴 n=2` — 공유 메인 폴더가 origin/main보다 크게 뒤처진 탓. **생존 판정은 반드시 origin/main으로.** 이번 세션은 원격 브랜치 25개를 전수 스캔해 확정했다.
- ⚠️ **반착지 세션은 등록부에서 안 보인다**(n=79가 남긴 구조적 구멍): PR까지만 하고 끝난 세션의 등록부 행은 **머지 안 된 브랜치에만** 있어 origin/main에서 원리적으로 안 보인다. 브랜치 전수 스캔이 유일한 방어다.
- ⚠️ 머지 대기가 길다 — `MERGE_WAIT=1200` 필요(기본 600초는 백엔드 CI 11분에 부족).
- ⚠️ eslint **96/96**으로 잠겨 있다 — 경고 1건만 늘어도 CI가 깨진다.
- ⚠️ prod `backend/.env:53`에 **`NAVER_CS_DRY_RUN=0`**(콜드스타트 레인 실쓰기 허용)이 실재한다. DB `guardrail_params` 행은 **비어 있다** — 넣는 순간 env를 이긴다.
