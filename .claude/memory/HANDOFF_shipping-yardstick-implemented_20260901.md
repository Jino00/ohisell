# HANDOFF — 배송비 자(尺) 정합 · 구현·배포 (2026-09-01)

> 체인 `배송비-자` **n=2** · 세션 `d02f7c89` · 12:1x ~ 14:0x KST
> 목표이름: **배송비 자(尺) 정합 목표** · **D-NAO-283**
> 계약: `docs/contracts/CONTRACT_shipping_yardstick.md` (Jino 승인 2026-09-01 12:2x, 4지선다 중 「ⓐⓑⓒ 전부」)
> 트랙: `docs/tracks/active/track_naver-ad-optimization.md` (PAO)

## 0. 작업 목적 — 원문 (다음 세션은 이걸 발명하지 말고 옮길 것)

> **Jino 원문 (2026-08-31 21:32 KST)**: *"이건 폴드 세븐 플립 세븐 다 똑같이 우리가 엔배송으로 진행을 하면은 네이버가 자기들 멤버십 가지고 있는 사람들한테는 배송비를 제해주고 우리한테 그 배송비를 주는 거. 그런데 내일 배송 같은 경우는 우리가 배송을 하는 경우야. 우리가 엔배송의 재고가 없을 때 내일 배송으로 돌리게 되거든. 내일 배송으로 돌리게 되면은 우리가 한진택배를 통해서 물건을 납품하게 돼. 물건을 배송하는데 우리가 한진택배에 내는 비용은 1900원이지. 그런데 고객한테 삼천원을 받으니깐 우리는 1100원이 남는 거야. 이런 구조로 배송은 진행이 돼."*

> **발단 (2026-08-31)**: *"그룹상품번호 52308509 제품을 pao광고 운영하고 싶어"*

## 1. 한 것

| | 변경 | 좌표 |
|---|---|---|
| ⓐ | **클램프 폐지** — `max(0, 지불−수취)` → `order_delivery.net_shipping_burden` | `bep_calculator._avg_qty_and_logistics` |
| ⓑ | 주문 0건 폴백 = 같은 `group_product_no` **형제 실측** | `_group_logistics` · `logistics_by_product` |
| ⓒ | 판매가 폴백 ③ = `naver_product_meta_current.discounted_price` | `calculate_bep` |
| + | `price_basis` · `logistics_basis` 컬럼 | 마이그 `shipyard1s1a` |
| + | `bep_from_parts()` · `select_best_mappings()` 순수 함수 추출 | 시뮬레이터가 규칙을 베끼지 않게 |
| + | `scripts/simulate_bep_shift.py` — 배포 전 읽기 전용 시뮬레이터 | D-NAO-236 선례 |

★**ⓑ가 산식을 새로 만들지 않는다** — 형제 주문을 pooled 표본으로 넘겨 `_avg_qty_and_logistics`를 **그대로** 돌린다.
★`order_delivery.net_shipping_burden`의 docstring이 「BEP는 별도로 클램프한다」고 적고 있어 **함께 고쳤다** — 안 고치면 그 문장이 배포와 함께 조용히 거짓이 된다.

## 2. 완료 QA 판정 — 원문 그대로 (별도 Sonnet · 읽기 전용 · 13:5x)

```
판정(계약 §4 합격기준): 부분달성 — ②③④⑤ 달성(⑥ 판정불능: 리뷰를 다시 돌지 않고
  기록만 확인). ①(헤드라인 항목) 미달 — 계산값은 옳으나 계약이 지목한 «표면»에 안 뜬다.
판정(Jino 지시 원문): 부분달성 — 「1,100원이 남는다」는 코드·라이브로 성립.
  그러나 08-31 발단은 종료 시점에도 진전 0. 무단 축소는 아니다(계약 §1 명시 배제).
판정(트랙 궁극 목표): 부분달성 — 진행률 2/7·§7 대칭 액셀 쪽 확인. 발단 상품은 여전히 광고 밖.
판정(종합): 부분달성
```

### ★★합격기준 ① 미달 — 내가 기준을 잘못 썼다 (다음 세션이 반드시 읽을 것)

**「값이 API에 실린다」를 확인하고 「화면에 뜬다」로 체크했다.**
두 화면(`NaverAdPerformance.tsx BepBreakdownSection` · `NaverAdReport.tsx`)이 모두 `only_actionable=True`로 호출하고(하나는 백엔드 기본값 `Query(True,…)`, 하나는 프론트 명시), 발단 상품은 `naver_adgroup_product` **0건**(광고 미연결)이라 **두 호출 모두에서 0건**이다. 화면엔 「전체 보기」 토글이 없다. `only_actionable=false`를 직접 넘기면 응답에 실린다.

★**그리고 이 기준은 애초에 자기모순이었다** — 그 표는 «광고에 연결된 상품»만 보여주는데, 계약 §1 «안 함»이 광고 소재 등록을 스코프 밖으로 뺐다. **계약이 배제한 행위를 해야만 통과하는 기준**을 합격기준에 넣은 것이다.

★**기준을 낮추지 않았다**(전역 §2). ①은 미달로 닫았다. 「비광고 상품도 화면에서 볼 수 있게 하는가」는 이 계약이 아니라 별도 판단이다 — **다음 세션 후보 1순위**.

### ★QA 신규 발견 — 내 자기평가보다 실효과가 넓다
광고에 실제로 연결된 **524행** 중 **sibling 74 · meta 76 = 150행**의 값이 이번 배선으로 바뀌어, **지금 돌고 있는 광고**의 입찰 상한 계산에 반영되고 있다. 가상 사례가 아니다.

### QA가 확인 못 한 것 (그대로 보존)
- 클램프 81행의 정확한 −6.15%·−5.49% — 배포 전 스냅샷이 덮여 **사후 독립 재계산 불가**. 방향·건수는 `logistics_cost=0` **81→0**, `<0` **0→86**으로 구조적 재현
- 적대 리뷰 변이의 생존/사망 — 리뷰를 다시 돌지 않음(기록·diff 정합성만 확인)
- PAO 21일 무집행 원인 — 스코프 밖

## 3. 라이브 증거 (prod, 재계산 `calculated_at 2026-09-01 13:23:02`)

| 관측 | 값 |
|---|---|
| `bep_roas IS NULL` | **202 → 36** |
| `logistics_cost = 0` / `< 0` | **81 → 0** / **0 → 86** |
| 출처 분포 | 물류비 `{orders 557, sibling 161, default 40}` · 판매가 `{orders 592, meta 166}` — **시뮬레이션 예측과 전건 일치** |
| 발단 상품 | `13687558209` sp 19,900(meta) · 물류비 **−208.73**(sibling) · **BEP 1.7267** / `13687558210` **BEP 1.504** (둘 다 배포 전 NULL) |
| 표면 | `/api/naver/ad/bep`·`/performance/bep-breakdown` 둘 다 출처 키 실림 · 프론트 번들에 「형제 상품 실측」·「실측 없음(가정)」·「배송 마진」·「스토어 할인가」 4문구 전부 · 스탬프 `commit=1e7318d3` |

### ★시뮬레이션과 라이브가 갈린 1건 — 원인 확정 (다음에 이 스크립트 쓸 때 필독)
발단 상품 BEP 예측 **1.7026** vs 실제 **1.7267**(+1.4%).
시뮬레이터가 `commission_rate`를 **저장 스냅샷에서 얼려** 쓰는데, `calculate_bep`은 상품별 요율을 **현재 nb_share와 함께** 다시 뽑는다(N배송 프리미엄 1.5%p × nb_share). 형제 실측이 nb_share를 0 → ≈0.6으로 올려 요율 **0.042510 → 0.0515**(역산으로 확인: 1.7026을 만드는 요율이 정확히 재계산 로그의 계정 요율 0.042510).
⇒ **ⓑ·ⓒ로 새로 생기는 BEP는 하한으로 읽어라.** ⓐ(클램프) 행은 자기 주문이 있어 nb_share 불변 → **③의 −6.15%는 영향 없다.** 한계는 스크립트 docstring에 못 박았다.

## 4. 적대 리뷰

**1R FAIL(P1 1) → 상환 → 2R P1 미해소 0 (게이트 PASS)**

★1R P1 = **교훈 #380의 세 번째 재발**. `price_basis`·`logistics_basis`가 DB에서 실제 JSON까지 가는 **배선을 아무도 안 지켰다** — 백엔드 테스트는 DB 컬럼을, 프론트 표면 테스트는 `vi.mock`한 손수 fixture를 봤다. 리뷰어가 직렬화 줄을 지우자 **1,346건 전건 초록**. ⇒ HTTP 왕복 테스트 3개 신설 후 **리뷰어 변이 2종 재주입해 각각 사망** 확인.
★그 교훈을 **커밋 메시지에 직접 인용하면서** 같은 구멍을 남겼다.
★2R P2-2: 시뮬레이터가 dedupe를 `max(판매가)`로 **베껴 적어** 본체와 어긋나 있었다 → `select_best_mappings()`로 공유. 재실행 **숫자 전건 불변** ⇒ **잠복 결함**이었다.
★리뷰어가 짚은 옛 산식 주석은 :533이었는데 **:280도 같은 결함**이었다(내가 바로 아래 :296에 「클램프 없음」을 적으면서 위 산식 줄은 안 고쳤다).

변이 누적 **7종** — 표면 절단 4종(리뷰어 2 + 내 2). 1R 생존 2종 → 2R 전건 사망.

## 5. 트랙 진행률

- 트랙: `docs/tracks/active/track_naver-ad-optimization.md`
- **시작 2/7 → 종료 2/7** (불변). 이 슬라이스는 §6 표의 어느 M 합격 관측도 아니다.
- 움직인 것: 북극성 §3 L2 표의 **「상품 단위 BEP 확보」**(202행이 BEP를 못 갖던 것 중 166행 구제)와 **「★총이익의 자」**(원가 쪽). **M4·M5의 전제**다.
- 트랙 목표 원문: *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야."* (Jino 2026-07-19, D-NAO-59)
- **북극성 대비(D-NAO-226·227)**: ①M 귀속 없음(M4·M5 전제) ②5요소 중 **③총이익 방향**만, **④자동화 38세션째 0** ③**§7 대칭 = 액셀 쪽**(직전 n=77이 브레이크였고 「액셀 대응물을 다음에」라 적었다. D-NAO-234·236이 **매출 쪽 자**에서 고친 병의 **원가 쪽 판본**) ④`PLAN_naver-profit-yardstick-review.md`는 다른 자(매출 귀속 보정계수) — 중복 아님

## 6. 다음 세션 후보

1. ★**합격기준 ① 미달 상환** — 비광고 상품의 BEP를 사람이 볼 수 있게 할 것인가. 선택지: ⓐ화면에 「광고 미연결 포함」 토글 ⓑ`only_actionable` 기본값 변경(파급 확인 필요) ⓒ안 한다(광고 붙으면 자동으로 보인다). **Jino 판단이 필요한 건 아니다** — §1 승인 지점 세 검사 중 ③(목표 변경 아님·되돌릴 수 있음)이라 다음 세션이 **묻지 말고 이어서** 하면 된다.
2. Jino 콘솔 작업 3건(내 손이 안 닿음): 4매입 5개 **소재 등록**(캠페인 15 재활용 시 2매입과 섞이므로 **광고그룹 분리** 권고) · 캠페인 **정지 해제**(`manual_emergency_stop`) · 일예산 확인
3. ↗️ PAO 승인→집행 단절(21일 무집행) → **소관: `pao-논의`**. 가져오지 않는다

## 7. 이월 (전역 §7 — 기록만)

- ↗️ **RG 판매일 축 테스트 2건이 저장소 전체 머지를 막는다** — `test_rg_sales_date_fees.py`가 `date(2026,8,31)` 하드코딩. 09-01 실측 **2 failed / 28 passed**. 재현: `cd backend && python3 -m pytest tests/test_rg_sales_date_fees.py -q` → 소관: **RG 판매일 축 트랙**
- ↗️ 최근 10건 표본의 하루 단위 진동 → 소관: 이 계약의 「안 함」
- ↗️ 타채널(1·2·7) 배송방식 판별 불가 100% — BEP엔 무해 → 소관: 미지정
- ↗️ 시뮬레이터의 「요율 동결」 한계 — 고치려면 `product_commission` SA까지 얹어야 해 `calculate_bep`의 절반을 재구성하게 된다. docstring에 명문화하는 것으로 갈음 → 소관: 이 스크립트

## 8. 착지

- 커밋 `87c916a5`(구현) → `fda1b465`(리뷰 1R 상환) → `1e7318d3`(2R P2-2 상환) → `06b8ab30`(라이브 기록) → 본 커밋(QA 판정)
- **PR #623** — 열림. **머지 안 함**: CI backend가 위 RG 2건으로 빨갛다(py3.10 2 failed/7,505 passed · py3.14 2 failed/7,504+1skip · frontend pass). **새 실패 0건**이 3경로(로컬 7,478 passed · CI 2종)에서 동일하게 확인됨
- **prod 배포는 이미 끝났다** — 마이그 `cphist1s1a → shipyard1s1a` 정순서 · CAS 5/5 · 무중단 0초 · 프론트 dist. 재계산도 실행됨. 즉 **머지 여부와 무관하게 라이브는 새 코드로 돌고 있다**
- 정정 경로: `git revert -m 1 <머지해시>` (아직 머지 안 됨) · prod는 이전 커밋으로 `safe_deploy.sh` 재배포 + `calculate_bep` 재실행

## 9. 하네스 관측

- 훅 `[체인] ⛔` 2건(`pao-논의 n=49` · `sellc-원가-메뉴 n=2`) **오탐** — 공유 메인 폴더가 `211decbc`로 origin/main(`5e189af9`)보다 약 400커밋 stale. origin/main 정본 대조 시 각각 n=77·n=22 종결
- 훅 `[트랙 기록 누락]` **오탐** — 확인줄은 워크트리에 실재(`grep -c d02f7c89` = 1). 같은 stale 원인. [[track-confirm-line-hook-reads-shared-folder]]대로 검증만 하고 공유 폴더엔 쓰지 않았다
- 훅 `review-surface-mutation.sh` **오탐 1건** — **완료 QA** 위임문을 적대 리뷰로 오인해 1회 deny(재전송 통과). → `CONTRACT_surface_rule_enforcement.md` §6 계수 대상
- prod 포트가 배포 중 **8001 → 8011로 되뒤집혔다**(다른 세션 추정, uptime 52초 관측). 내 코드는 안 덮였다(심볼 5종 생존·클램프 잔존 0 확인). **활성 포트를 스스로 찾는 습관이 필요하다**
- `frontend/dist/.deploy-stamp`는 **레거시 경로**다 — 정본은 `$REMOTE_REPO/.frontend-deploy-stamp`. 레거시 경로만 보면 「스탬프 없음」 오판
- ⛔ 무접촉: `발주예측` n=8(`c3b74f44`) · `승인배선` n=2(`607c2c15`) — 둘 다 `end_kst: null`
