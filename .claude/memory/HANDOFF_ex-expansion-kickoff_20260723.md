# HANDOFF: EX 확장 압력 스프린트 킥오프 (D-NAO-85)

- 일시: 2026-07-23 07:35 KST 작성 / **09:30 갱신 — ref39 구조 감사(P1~P8)·원칙23·D-NAO-86/87/88 반영**
- 워크트리: `vt34-deploy-visibility-first-7c103b`
- 브랜치: `claude/vt34-deploy-visibility-first-6b0e91` (전 세션 PR #85는 이미 main 병합됨 — 이 세션의 docs 변경은 별도 소형 PR로 main에 병합)
- 이 세션 = **기록 전용**. 코드 변경 0건. 다음 세션이 실제 EX 착수.

## 0. 어젯밤 세션 요약 (1줄 + 포인터)

VF(가시성 우선, D-NAO-83) 구현·배포·라이브 합격 + D-NAO-84 N배송 훅 완결 — ghost_hold 2건 실포착·17프로 상한 2,290→3,010 해방 검증·N배송 판별자(`deliveryAttributeType=="ARRIVAL_GUARANTEE"`) 확정·배선. 상세는 전 HANDOFF `.claude/memory/HANDOFF_vf-visibility-live+nbaesong-hook_20260722.md`.

## 1. 오늘 아침 대화 — D-NAO-85 EX 확장 압력 스프린트 확정

### 발단 — Jino 질책 (원문)

> **"이것만 봐도 우리의 문제가 딱 나오는게, RoAS 7% 올리면서 매출이 52%가 줄어들었어. 내가 재일 처음에 말했던 RoAS가 좀 낮아지더라도 매출을 극대화하자는 전제는 어떻게 없어져버린거야?"**

근거 표본 = 03캠페인(강화유리) 6월 평균 대비 MOP 운영 후(07-19~21) 비교. 정확한 표는 트랜스크립트에 없어 **약(근사)** 수치로 기록:

| 지표 | 6월 일평균 | MOP 후 일평균(07-19~21) | 변화 |
|---|---|---|---|
| 노출 | 약 4,566 | 약 3,059 | −33% |
| 클릭 | 약 133 | 약 46 | −65% |
| 전환 | 약 4.4 | 약 2.3 | −47% |
| 매출 | 약 39.6k | 약 18.9k | **−52%** |
| 광고비 | 약 16.5k | 약 7.4k | −55% |
| ROAS | 2.40 | 2.57 | **+7%** |

### Claude 진단 — 동의

D-NAO-59(총이익 절대액 최대화 — ROAS 최대화 아님, 한계 ROAS ≥ BEP 구간에선 볼륨 확장) 기준으로 **목적함수 위반**이 실측으로 드러남. 실행층 구조를 보면:
- **브레이크(하향 압력)**: 스톱로스·손실고삐(RL3/RL4/DL)·게이트(BEP 미달 차단)·쿨다운(2h)·다수 상한(경제성 상한·일일 캡 다수) — 강하고 즉각적, 캠페인 전역에 상시 작동.
- **액셀(상향 압력)**: 탐색 스텝(B-X)·IU(장중 상향)·VT4(신수요 우선순위)·VF 증거창(D-NAO-83) — 소폭·조건부·**개별 그룹 단위**뿐.
- 결과: **"캠페인 보정 ROAS가 BEP를 크게 웃돌면(2.57 vs 1.55) 한계 ROAS가 BEP에 수렴할 때까지 볼륨을 밀어 올리는 상설 확장 압력 메커니즘"이 부재.** 그룹별 개별 판정만 쌓여 있고, 캠페인 전체가 "여유가 있다"는 신호를 확장 행동으로 조직적으로 번역하는 층이 없었음.

### 귀속 주의 (반드시 기록해야 하는 이유)

3일 표본의 매출 하락 자체는 **07-14~18 구 MOP 말기 스파이럴에서 이미 진행 중**이었음(광고비 31k→7.5k, 트랙 D-NAO-81 03 스파이럴 해부 참조). 우리 시스템은 그 바닥에서 캠페인을 인수했다. 따라서 정확한 서술은 **"우리가 매출을 줄였다"가 아니라 "회복이 구조적으로 느리다"**. 그러나 이 귀속 정정과는 **독립적으로** 확장 압력 메커니즘의 부재는 실재한다 — 회복이 느린 이유 자체가 액셀 부재이기 때문. 두 사실을 혼동하지 말 것.

### 확정 구조 — Q1 (Jino "Q1만 진행")

EX(확장 압력, Expansion pressure)는 계층 구조로 확정:

- **압력의 존재 판정 = 캠페인 단위.** 캠페인 보정 ROAS와 BEP의 갭이 임계 이상이면(2.57 vs 1.55처럼) 그 캠페인에 "확장 모드"를 개방하고 확장 여력(볼륨을 얼마나 밀 수 있는지)을 산출한다. 판단 근거는 캠페인 전체의 통계적 질량(표본이 캠페인 단위라 충분) — 그룹 단위로는 이 판정을 할 표본이 부족한 경우가 많다는 점이 이 계층 분리의 이유.
- **압력의 배분 = 그룹 단위.** 캠페인이 확장 모드일 때, 그 압력을 어느 그룹에 얼마나 배분할지는 그룹 단위로 정한다. 우선순위 후보 3가지:
  1. 밴드 내 + 순위 여유(2.5 방향으로 이동 여지가 있는 그룹)
  2. 고수요·밴드 밖(수요는 있는데 순위가 이익 밴드 밖인 그룹)
  3. 증거창(D-NAO-83 VF) 그룹의 순 랭킹
  - 자기 표본이 있는 그룹은 자기 ROAS로 판단, 표본이 없으면 캠페인 프라이어를 쓴다(수축/shrinkage 철학 — hierarchical_pooling.shrink 등 기존 EB 수축 자산 재사용 방향).
- **정지 조건**: 그룹별 한계 반응(bid_rank_slope, 이미 IU-R R3에서 관측 중인 반응곡선 재사용)을 보면서, 그 그룹의 한계 ROAS가 BEP에 접근하면 그 그룹의 확장을 정지한다 — D-NAO-59의 "한계 ROAS=BEP=총이익 꼭짓점" 수렴 조건 그대로.

### 보류 — Q2 (Jino 지시로 진행 안 함)

OG(Objective Guardian) — Claude가 스스로 목적함수 미달성을 감지하고 자율적으로 개선 루프를 도는 구조(Tier 0/1/2 자율 등급 설계, 예: Tier 0=관찰만/Tier 1=제안/Tier 2=일부 자율 실행 같은 단계적 위임)는 이번엔 다루지 않는다.

> Jino: **"일단 Q2는 하지 말고"**

백로그로만 보관. **재제안은 Jino가 먼저 꺼낼 때만** — Claude가 먼저 다시 제안하지 않는다.

### 착수 순서 (Claude 추천, Jino 수용)

1. **③ 먼저**: 07:50 데이터 동기화 완료 후, **07-22를 포함한 4일치**로 03/04/17프로 재판정. 07-22는 가격 인하(D-NAO-84)·N배송 첫 주문·17프로 밴드 진입이 겹친 날이라 이 날을 포함해야 최신 실태를 정확히 볼 수 있음.
2. **② 다음**: GAVE 목적함수 배선 실측. D-NAO-72에서 1차(소급 채점)만 배선됐고 2차(flight_loop α 선택 통합)는 미배선 — [[naver-ad-claimed-vs-wired-gaps]] 전례(mode 다이얼 저장만·gave_score 정의만 호출 0)가 있으므로 "있으니 된다" 단정 금지, 코드 실측으로 확인.
3. **① 마지막**: EX 설계(Fable)·구현(라우팅 관례대로: 구조=Fable, 하위 구현=Opus/Sonnet).

## 1.5 제1회 구조 감사 (ref 39) — EX 세션 필독 ★09:30 추가

07-23 아침, Jino가 Fable에 **주기적 재심 제안 권한**을 부여하는 대화("① 풀면 실제로 답이 좋아지는 제약: 스코프 잠금·재심 금지" → 승인) → **전역 CLAUDE.md 원칙 23 신설**(주 1회 주기로 Fable이 트랙 전체를 재심 제안, 결정권은 항상 Jino 불변, 재제안 금지로 봉인된 항목은 예외, 안전장치 변경은 제안 단계에서 멈춤). 이 권한으로 시행한 **제1회 전체 구조 감사 = `docs/references/39_objective_audit_20260723.md`**(D-NAO-59 목적함수 대비, 문제 인벤토리 P1~P8). 트랙 D-NAO-86에 요약 기록됨. EX 세션은 이 감사 결과를 **반드시 편입**해야 한다 — 아래 표가 각 항목의 처리 방침.

| # | 항목 | 요지 (증거 병기) | 처리 방침 | 관련 D-N |
|---|---|---|---|---|
| P1 | KX — 수요 표면(키워드·그룹) 확장 레버 부재 | 최대 공백. 자동화는 기존 그룹 입찰 조정뿐, 신규 그룹·키워드 생성 0건. 실측: 대행사가 신모델 그룹6+키워드85 제값 입찰 추가 → 같은 날 저녁 전환 +27~62%(07-22 관찰). BM 프라이어 41,983건 소비 코드 없음(관찰 전용) | **EX 후 별도 스프린트**(승인 완료, 착수는 EX 완료 후) | D-NAO-88 |
| P2 | EX — 확장 압력 메커니즘 부재 | D-NAO-85로 이미 확정·설계 착수 중. P1(수요 표면 자체 없음)·P4(정적 상한)가 안 풀리면 EX가 밀어올릴 대상·천장이 막혀 결과가 안 나올 수 있음 — 설계 시 상호작용 확인 필수 | **EX 본체 그 자체**(이 스프린트의 핵심) | D-NAO-85 |
| P3 | UP 게이트 "데이터 없음=0" 숨은 브레이크 | 실사례: change_log 1720 "보정 ROAS 0.0<목표 1.82"로 UP 차단. 전환 D+1~7 정착·귀속 클릭 과소계상 2회 실측과 충돌 — 진짜 "아직 안 옴"과 "반응 없음"을 게이트가 구분 못 하고 후자로 읽어 확장을 계통적으로 막음. DOWN 방향은 문제 아님(보수성이 손실방지 축에선 올바른 비대칭) | **EX 설계 범위 편입** — 표본 부족 시 캠페인 프라이어로 폴백(수축 철학, EX 그룹배분 로직과 패턴 공유). DOWN 게이트는 현행 유지 | 재심 지정 |
| P4 | 이익 스팟 밴드(2.5~4) 정적 상한 변질 | 원설계(D-NAO-66)는 횡단면 관찰이었으나 실행층에서 전 그룹 공통 고정 천장으로 굳음 — 한계ROAS 여력 큰 그룹도 밴드 밖 진입이 차단됨. 올바른 규칙은 "밴드"가 아니라 그룹별 "한계ROAS≥BEP인 동안 계속 확장" | **재심 지정, EX 설계에서 밴드 동적화로 자연 흡수**(bid_rank_slope 재료, EX가 이미 쓰기로 확정된 재료라 별도 착수 불요) | D-NAO-66 계열 재심 |
| P5 | 브레이크 발화 vs 재진입 비대칭 | 03 스파이럴(07-14~18): 순위밀림+스톱로스 겹쳐 광고비 31k→7.5k, 회복 구조적으로 느림. VT(시간당 즉시복원) 별도 스프린트 필요했던 사실 자체가 "퇴출은 빠르고 재진입은 느린" 비대칭의 증거 | **EX 이후 과제** — 액셀 축(EX) 먼저 세운 뒤 브레이크 축 대칭화가 순서상 맞음. 이번 스프린트 스코프 아님 | EX 이후 |
| P6 | 예산 레버 잠금 | `cost_today>=daily_budget`가 UP 하드차단, 자동 확대 경로는 카나리 단계 정지. EX가 압력 걸어도 예산 천장이 막으면 무의미 | **확정·EX 스코프 포함**: 캠페인 일예산 = max(과거30일 일평균지출×1.5, 50,000원), auto_operate=True만 실쓰기 | D-NAO-87 |
| P7 | 목적함수(총이익 절대액) 일일 비가시성 | 일 레인 브리핑에 캠페인별 총이익 절대액이 안 보임 — ROAS·매출·클릭만 보임. 이번 52% 매출하락도 3일 지나서야 Jino가 발견 = 이 비가시성의 직접 결과 | **우선순위 1, EX 세션 첫 작업**(새 계산 로직 없이 기존 raw_data 조합, 반나절 규모) | — |
| P8 | GAVE 배선 부분 | D-NAO-72에서 1차(소급채점)만 배선 확인, 2차(flight_loop α 선택) 미확인 | **새 발견 아님** — D-NAO-85 착수순서 ②(실측)가 이미 처리 예정, 중복 작업 방지용 인벤토리 등재만 | D-NAO-72/85 |

**Jino 결정 완료 2건(더 이상 대기 아님)**: P6→D-NAO-87 확정(예산 봉투 규칙 위 표 참조), P1→D-NAO-88 확정(KX 착수 승인, 순서는 EX 뒤). **재심 지정 1건**: P4(스팟 밴드 정적 상한) — EX 설계 내 자연 흡수로 처리, 별도 승인 불요.

**원칙23 메모**: 마지막 구조 감사 = 2026-07-23 (ref 39), 차기 목표 ~07-30(주 1회 주기). 세션 시작 시 트랙 파일 상단 "마지막 구조 감사" 라인으로 다음 감사 주기 여부를 확인할 것.

## 1.6 EX 스프린트 확장 스코프 (ref39 반영 후 확정) ★09:30 추가

EX 세션이 실제로 할 일은 D-NAO-85 원안(캠페인 압력판정→그룹배분)에 ref39가 추가한 4개 항목을 더해 **5갈래**로 확장됨. 순서:

1. **P7 일일 이익 스코어카드 (첫 작업)** — 캠페인별 총이익 절대액, 어제/7일 이동평균/6월 대비. 새 계산 로직 불필요, raw_data 조합만. 반나절 규모. 목적함수 비가시 해소가 목표(이번 사고 재발 방지).
2. **EX 본체** — 캠페인 단위 압력 판정(보정ROAS−BEP 갭) → 그룹 단위 배분(밴드내 순위여유 → 고수요·밴드밖 → 증거창 랭킹) → bid_rank_slope 기반 정지 조건.
3. **P4 밴드 동적화** — 정적 2.5~4 상한을 그룹별 한계ROAS≥BEP 조건으로 대체, EX 본체 설계에 자연 흡수(별도 단계 아님).
4. **P3 UP 게이트 프라이어 폴백** — 표본 부족(clk 미달 등) 시 그룹 자기 표본 대신 캠페인 프라이어로 판단, EX 그룹배분 로직과 패턴 공유.
5. **D-NAO-87 예산 봉투 레버** — 캠페인 일예산 = max(과거30일 일평균지출×1.5, 50,000원), auto_operate=True만 실쓰기, 재산정 주기·기존 가드레일(+100%/회·라운드 캡)과의 램프 정합을 EX 설계에서 확정.

**EX 스코프 밖 (명시적 제외)**:
- **KX(D-NAO-88, 신규 키워드·그룹 생성)** — 승인은 완료됐으나 착수는 EX 완료 후 별도 스프린트.
- **P5(브레이크 발화/재진입 비대칭 대칭화)** — EX(액셀 축) 완료 후 순서.
- **Q2 OG(자율 개선 루프)** — Jino 보류 지시 유지, **재제안 금지**(Jino가 먼저 꺼낼 때만).

## 2. 승계 항목 (EX 착수와 병행 가능, 순서 무관)

- **아침 자동 3건 관문**: 07:45 BM 벤치마크 레이어 관문 4개 자동 검증 / 08:50 PX(파워링크 자동 제외) 레인 + VF 유령 관측 첫 브리핑 + 04 W1 CTR 경보 / 09:30 codex 소급 리뷰(VT3·4·VF는 이미 challenge 완료라 스코프 중복 주의).
- **Z8(갤럭시 폴드8/플립8) 발표일 확정 데이터 관찰**: ref 37 baseline 대비, 관찰 전용(실쓰기 0).
- **03/04/17프로 판정**: 위 착수 순서 ③에서 겸해서 처리.
- **BEP 13,900 반영 관찰**: 17프로강화유리 새 가격(13,900+배송3,000)이 selling_price 주문 중앙값 소스에 반영되는 추이 관찰.
- **N배송 배송비 정산 라인 확인**: `expectedSettlementAmount` 13,174에 배송비 3,000이 미포함되어 있음 — 실정산 도착 시 확인 필요(D-NAO-84 잔류).
- **관측 갭 2건 chip (task_9f4ea74c)**: ①시간당 레인 INFO 카운터 로그 전 기간 미출력(main.py 로깅 미설정) ②stop_observe/step-capped 분기 diary 미기록(침묵 분기). 코드 수정은 스코프 밖 — 이미 백로그 chip으로 발행됨, 별도 세션에서 처리 가능.
- **B+C 스프린트(수명주기 인식+런칭 플레이북)**: D-NAO-84에서 방향 승인됐으나 EX 스프린트 뒤로 순연.

## 3. 새 세션 시작 프롬프트 (복사용)

```
HANDOFF `.claude/memory/HANDOFF_ex-expansion-kickoff_20260723.md` 읽고 이어서 진행해줘.

핵심: D-NAO-85 EX 확장 압력 스프린트 + ref39 구조 감사(P1~P8) 확장 스코프.
- EX = 캠페인 단위 압력 판정(보정ROAS−BEP 갭) → 그룹 단위 배분(밴드내 순위여유→고수요→증거창 랭킹), Q1만(Q2 OG는 보류 — 재제안 금지, Jino가 먼저 꺼낼 때만).
- 착수 순서: ③07-22 포함 4일 재판정(03/04/17프로) → ②GAVE 목적함수 배선 실측(1차만 배선·2차 flight_loop 미배선, "있으니 된다" 단정 금지) → ①EX 설계·구현.
- EX 확장 스코프(ref39 반영, 위 §1.6 참조): P7 일일 이익 스코어카드(첫 작업, 반나절)+EX본체+P4 밴드 동적화(자연 흡수)+P3 UP게이트 프라이어 폴백+D-NAO-87 예산 봉투(캠페인 일예산=max(과거30일 일평균지출×1.5, 50,000원), auto_operate=True만).
- 스코프 밖: KX(D-NAO-88, 승인 완료·EX 뒤 별도 스프린트)·P5(브레이크 대칭화, EX 뒤)·Q2 OG(보류 유지).
- 원칙23(주기 구조 감사·재심 권한, 2026-07-23 신설) 확인: 마지막 감사=07-23 ref39, 차기 목표 ~07-30.
- git log 확인해서 병행 백로그 칩 2건(관측갭 task_9f4ea74c·수집기 UTC창 task_192ad8f6 있으면) 반영 여부 체크.
- 라우팅: 설계=Fable, 구현은 모델 라우팅 관례대로(구조 아래 하위작업=Opus, 단순/코딩 기본=Sonnet), 옵션은 Claude 추천안 자동 진행.
```
