# 세션 인수인계 — 다음 세션 1순위: 로켓 1P 원가 매핑 4건

> 저장일시: 2026-08-04 19:2x KST · main `08195f8` · prod 배포 완료 · PR #198 push됨
> **Jino 지시(19:23)**: "다음 세션에서 로켓 1P 손익의 1순위(옵션↔마스터 연결 4건) 일순위로 진행하자"
> 앞 세션 둘: `HANDOFF_rocket-sku-pnl+cost-source_20260804.md`(로켓 손익) ·
> `HANDOFF_coupang-ad-change-log_20260804.md`(오늘 한 쿠팡 광고 이력 — 완료됨)

---

# 1. ★최우선 — 옵션↔마스터 연결 4건 (Jino 입력 대기)

**2026-08-04 19:2x prod 재확인: 4건 전부 아직 미입력.**
연결 키는 상품번호가 아니라 **옵션ID**. 화면: `/product-connection-map`

| 옵션ID | → 마스터 | 원가 | 상품 |
|---|---|---|---|
| `93373791456` | `OHI-0736` | **3,300 또는 3,400 (아래 ★)** | 아이폰17프로 강화유리 2p+EZ툴 |
| `95752961189` | `OHI-Z-LOWREF6-FOLD8-WIDE` | 6,090 | Z폴드8 무광택 |
| `95752961188` | `OHI-Z-LOWREF6-FOLD8-ULTRA` | 6,090 | Z폴드8울트라 무광택 |
| `95752961187` | `OHI-Z-LOWREF6-FLIP8` | 3,480 | Z플립8 무광택 |

### ★1번이 압도적으로 크다 — 자동매핑 잔여의 86.4%
`93373791456`은 SKU `62178970`의 옵션이고, 그 SKU 하나가 **자동매핑 원가 2,165,476원 중
1,870,000원(86.4%)**을 차지한다. 이 한 건만 넣으면 자동추정 의존이 7.0% → 약 1% 아래로 떨어진다.

### ★근거는 앞 인계보다 강해졌다 (2026-08-04 오전 실측)
앞 인계는 "형제가 0734·0735·0737, 연번 빈칸"이라고만 적었는데, **실제로는 형제 3개 모두
쿠팡 로켓배송(channel 5) 옵션ID가 붙어 있고 `OHI-0736`만 비어 있다**:

| 마스터 | 로켓 옵션ID |
|---|---|
| `OHI-0734`(에어) | 93373791467 |
| `OHI-0735`(17프로맥스) | 93373791473 |
| `OHI-0737`(17) | 93373791457 |
| **`OHI-0736`(17프로)** | **없음** ← 여기에 `93373791456`이 들어갈 자리 |

### ★★넣기 전 Jino가 정할 것 — 원가가 3,300이냐 3,400이냐
같은 물건에 **마스터가 둘** 있다:

| 마스터 | 등록일 | 원가 | 채널 매핑 |
|---|---|---|---|
| `OHI-0736` | 2026-04-15 | **3,300** | 자사몰·스마트스토어 (로켓 자리 비어 있음) |
| `OHI-TGLASS-IP17PRO` | 2026-06-16 | **3,400** | **쿠팡_오픽스(3P) 전용 2건** |

현재는 자동매핑이 `OHI-TGLASS-IP17PRO`(3,400)를 물고 있다. 발주 550개 기준 **55,000원 차이**.
Jino 원문 참고: "39017747 이것도 원가가 3400원이야" / "환율로 인해서 조금 바뀌었을 수 있어"
(단 sellc 등록원가는 39017747도 3,300이다.)

### 넣은 뒤 검증할 것 (라이브 합격기준)
- 커버리지: **현재 sellc 79.77% · 자동추정 7.00%** → sellc 90%대로 올라가는지
- 계정 순이익: **현재 33,967,085원** → 변화분이 새 원가와 **원 단위로 일치**하는지
- 4개 상품이 흑자/적자 어느 쪽으로 뜨는지
- 조회: `GET /api/overview/rocket-overview?from=2026-07-05&to=2026-08-03` → `sku_pnl.coverage`

---

# 2. 그다음 — 자동매핑 잔여 14건 (2,165,476원)

2026-08-04 오전 실측. 앞 인계의 "sellc에 등록하면 자연 해소"는 **14건 중 2건에만 성립한다** —
원가 경로가 `상품번호 =브리지= 옵션ID → 채널매핑`이라 **브리지가 없으면 sellc 등록이 닿지 않는다**.

| 갈래 | 건수 | 금액 | 처방 |
|---|---|---|---|
| A. 브리지 있음 | 2 | 1,882,000 (86.9%) | 옵션ID를 sellc에 등록하면 해소 (그중 1,870,000이 위 1번) |
| B. 브리지 없음 | 12 | 283,476 (13.1%) | **등록만으로는 안 됨** — 판매분석 관측이 있어야 브리지가 생긴다 |

### ★오매핑 의심 3건 — 증거 있음, **내가 고치지 않았다**(LESSONS #118)

| SKU | 금액 | 발주 이름 | 붙은 마스터 | 문제 |
|---|---|---|---|---|
| `63408012` | 122,364 | 일미리 맥세이프 **1mm 투명** 16프로 | `OHI-0171` 하이브리드 **소다** 16프로 (4,532) | 다른 제품군. 게다가 OHI-0171의 진짜 로켓 옵션 `94723055372`는 브리지상 **`69592738`**(소다 16프로) 것 — 이미 임자가 있다 |
| `69592740` | 6,666 | **맥세이프** 하이브리드 소다 16 | `OHI-0133` 하이브리드 소다 16 (2,222) | 형제 `69592738`(프로)는 맥세이프 마스터 4,532에 붙음 → 맥세이프판이면 2,222는 과소 |
| `41848991` | 2,222 | 갤럭시 **S24+** | `OHI-0158` 갤럭시 **S24** (2,222) | 기종 불일치 |

나머지 11건(일미리 5·프라이버시 3·EZ툴 12미니·S25·3D TPU)은 기종까지 일치해 그럴듯하다.
`57316036`만 발주명에 "맥세이프" 언급이 없는데 "맥세이프 유" 마스터에 붙어 있다(2,400원, 미세).

### 해소 확인된 것
`39017749`·`39017751`·`39017754`(+`39017747`)는 **sellc 연결이 있고 원가 3,300으로 잡힌다** —
어제 문제였던 자동매핑 2,516원이 아니다. 앞 인계 체크리스트 4번 완료.

---

# 3. 환경·명령

- prod 배포는 `scripts/safe_deploy.sh`만 (DB 변경 시 `--migrate`) · 프론트 `--frontend`
- 테스트 `cd backend && python3 -m pytest tests/ -q` (**4,635 passed**, ~2분)
- prod DB 조회 `ssh sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && sqlite3 ohisell.db \"...\""`
- ★`alembic/env.py`는 `DATABASE_URL`을 무시하고 `alembic.ini`를 읽는다(LESSONS #109)
- ★**병행 세션이 같은 폴더 main에 커밋·push한다.** `git add -A` 금지, 내 파일만 스테이징.
  오늘 alembic 헤드 분기 1회·`models.py` CAS 차단 1회 — 둘 다 가드가 막았고 재부모·합집합으로 해소.

---

# 4. 오늘 완료한 것 (참고 — 이어서 할 일 아님)

**쿠팡 광고 변경 이력 「수정 사항」 쿠팡판** — `/coupang-ad-changes`, prod 가동 중.
상세는 `HANDOFF_coupang-ad-change-log_20260804.md` · 트랙 `track_coupang-ad-change-log.md`(D-CAC-1~7).

| | 오픽스 | 오하이테크 |
|---|---|---|
| 감시 캠페인 | 20 (SALES 16 + NCA 4) | 573 (SALES 525 + NCA 48) |
| 활성(세부 추적) | 5 | 19 |
| 소재 | 543 | 499 |

- 원천 둘: 쿠팡 `change-history/events-simple`(전/후 값·실행 시각·**90일 소급**) + 우리 스냅샷 diff
- 90일 274건 백필 완료. 겹치면 쿠팡이 이긴다(초 절삭 후 병합).
- **D-CAC-6** 변경 주체 = Jino 단일(쿠팡은 대행사 없음) · **D-CAC-7** 미확인 축은 Jino가 운영 시 알려줌
- ★오늘 두 번 틀렸다: "비-PA는 디스플레이일 것"(→NCA였다) / "goalType 필터는 안 숨긴다"
  (→`null`은 필터 해제가 아니라 기본값 SALES). **공통 원인: "바꿔봤는데 결과가 같더라"를
  "영향이 없다"로 읽었다.** 트랙에 교훈으로 남김.

### 그쪽 미결
- 소재 옵션ID 목록 실증 — 소재를 한 번 넣거나 빼고 「광고비 갱신」을 눌러야 붙는다(소급 불가)
- codex PR 경계 리뷰 — 쿼터 리셋 **08-09**, 이 트랙 **전체 범위**로 (오늘 작업 대부분이
  병행 세션의 main push에 실려 PR 리뷰 없이 들어갔다)

---

# 5. 그 밖 미결

- 브리지 없는 190개 옵션(광고비 308,434원) — 판매분석 미관측분 처리 방침
- 0.02% 옵션합계 vs 계정총액 차이 원인
- 판매분석 "Basic 무료체험" **08-20 종료**(D-CPP-5) — 브리지 신규 관측이 멈춘다
- 오픽스 광고 세션: keycloak까지 만료되면 자가 복구 불가 → `ad_cost_browser_fetcher.py login`

---

# 6. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_next-rocket-1p-cost-mapping_20260804.md 읽고 §1 로켓 1P 원가 매핑 4건부터 진행
```
