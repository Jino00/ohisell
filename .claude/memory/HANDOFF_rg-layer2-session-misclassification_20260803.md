# HANDOFF — 쿠팡 RG 정산 층2 수집이 주기 전환마다 죽던 결함 — 업스트림 500을 로그아웃으로 오분류 (2026-08-03, 워크트리 `youthful-albattani-54e1e0`, 브랜치 `claude/rg-probe-delta`)

## §0 가장 먼저 알아야 할 것

**PR #178, 라이브 합격.** 세션 문제가 아니었다 — `wing.coupang.com`의 `status/api`가 상시 HTTP 500을
뱉는 업스트림 장애였고, 그 500의 본문은 로그인 페이지가 아니라 **로그인된 Wing 셸 HTML**
(`<title>Coupang Wing - 김진오, 오픽스</title>`, `__GLOBAL_DATA__ activeProfile:'production'`,
`server: istio-envoy`)이었다. 판정 함수가 그 둘을 똑같이 "로그아웃"으로 접었고, lease 계약상
`login_required`는 재시도 없이 요청을 소멸시켜 **버튼 1회 = 주기 1개**가 됐다 — RG 166시간 침묵의
실제 기전.

## §1 근본 원인

`_rg_json`이 "status != 200"과 "본문에 `<html`"을 똑같이 `None`으로 접었고, 호출자는 그 `None`을
"로그아웃"으로 번역했다. 실제로는 로그인된 세션이 멀쩡히 살아있는데 `status/api` 자체가 상시
장애였다. lease 계약(재시도 없이 요청 소멸)이 이 오분류를 치명적으로 만들었다 — 매 버튼 클릭이
`login_required`로 즉시 소멸하니 층2 결손이 절대 안 메워졌다.

## §2 라이브 계측 (2026-08-03 13:10~13:11, 같은 페이지·같은 쿠키·32회)

| 엔드포인트 | 결과 |
|---|---|
| `status/api` | 1/32 성공. 창 폭 35/21/7/1일 전부 동일 결과(질의 무게와 무관). 실패는 0.55초 fast-fail |
| `download-list/api` | 8/8 성공 (0.49초) |

## §3 "주기 전환에서만 죽는다"로 보였던 이유

그 지점이 특별해서가 아니라 재시도 예산 차이였다. 진입 프로브 `_rg_login_wait`(5초×180초)도 매
회차 똑같이 실패했지만 22~46초 두들겨 통과시켰다. 주기 전환 프로브는 2회·5초뿐이라 97% 실패율
앞에서 항상 죽었다 — 같은 코드, 다른 재시도 예산.

## §4 수정 (PR #178, 커밋 4건 = 병행 세션 2 + 이 세션 2)

**병행 세션(`fix/rg-session-probe-false-negative`, 로컬 전용)**:
- 3상태 판정(ok/auth/unknown) 도입
- 판정 불가를 로그아웃으로 승격 금지
- AUTH 사유에서 body 제거(토큰 노출 방지)

**이 세션 델타**:
1. 프로브 엔드포인트를 `status/api` → `download-list/api`로 교체
2. `_rg_fetch_status_raw`에 5xx 재시도(120초 예산, 반환을 (판정, raw) 튜플로 변경)
3. 오리진 이탈(xauth)을 로그아웃 확증으로 인정
4. `redirect:'manual'` — JSON API가 302로 로그인 페이지에 튕기면 `follow`는 크로스 오리진에서
   `TypeError: Failed to fetch`로 터져 명백한 로그아웃이 '판정 불가'로 뭉개진다

**판단기준**: 오분류 비용은 비대칭 — 정상→로그아웃 오판은 요청 영구 소멸, 로그아웃→정상 오판은
재시도 가능한 실패. 확증 없으면 AUTH로 부르지 않는다.

**테스트**: 4278 passed. 회귀 16건 추가.

## §5 라이브 합격 (2026-08-03 14:12~14:26, prod Mac WING1)

1. 500 3건이 전부 재시도로 처리·통과
   - `14:13:30 status/api 2회 재시도 끝에 성공`
   - `14:21:05 3회 재시도 끝에 성공`
2. 한 회차 **3개 주기 완주**, 주기 전환 2회 통과 (14:25:11 회차 rc=0, push 성공 4/실패 0).
   대상 = 08-01~08-02·07-27~07-31·07-20~07-26
3. `layer2-gaps` STORAGE_FEE 결손 **7주기 → 4주기**, 보관비 205행 적재

배포 전(12:00~14:12) vs 후 비교:
- `층2 루프 중 세션 만료` 3건 → **0건**
- `세션 만료 의심` 오보 8건 → **0건**

## §6 검증 방법 기록

설정된 2종의 결손이 0이라 그대로는 주기 전환을 못 지나가서, `rg_report_types`에 `STORAGE_FEE`를
임시로 추가해 3개 주기를 만들었고 **검증 후 원복**했다(현재 `['WAREHOUSING_SHIPPING',
'PRODUCT_SIZE_COMPARISON']`). `rg_status_days`도 임시로 150/200을 시도했다가 원복(현재 키
없음=기본 35).

## §7 미결/후속 3건

1. **PR #178 미병합** — 병합 전엔 `install_local_runtime.sh`가 Mac의 수정을 또 되돌린다(오늘
   13:34:41·13:45:03·14:01:47 세 번 실제 발생). 병합이 사실상의 보호 장치.
2. **`STORAGE_FEE`는 상시 편입 후보** — 실제 시트명이 `'보관비'`로 백엔드
   `_SHEET_FEE_TYPE_MAP`과 일치함이 라이브로 확인됐다(추정으로 넣어두고 아무도 검증한 적 없던
   매핑). 켜면 남은 4주기도 자동으로 메워진다. 단 `CATEGORY_TR`은 실제 시트명이 `'주문내역,
   판매수수료'`라 매핑에 없어 켜면 422다.
3. **3월 `WAREHOUSING_SHIPPING` 결손 2건(03-02~03-08, 03-09~03-15)은 이 경로로 영구히 못
   메운다** — 그 결손은 150일창부터 보이는데 `status/api` 열거는 150·200일창에서 **빈 배열**을
   돌려준다(400이 아니라 조용한 0건, 라이브 확인). 결손 구간과 열거 구간이 겹치지 않는다.

## §8 구조 결함 (칩으로 분리, `task_d14eb7cc`)

`install_local_runtime.sh`가 페처 5종+`coupang_auth.py`를 한 워크트리 기준으로 통째로 덮어, 다른
세션의 배포를 조용히 되돌린다. `safe_deploy.sh`의 CAS가 prod 경로에서 막는 바로 그 사고인데 Mac
페처 경로엔 가드가 없다.

## §9 codex

쿼터 소진(리셋 2026-08-09 16:16)으로 Jino가 이 건 한정 면제(2026-08-03). 부채 아님.
