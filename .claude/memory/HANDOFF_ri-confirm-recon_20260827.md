# 세션 인수인계: 1P계산서 목표 — S3 쓰기 정찰 + S2-3 라이브 재관측

> 저장일시: 2026-08-27 22:5x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것
> 체인 `1p-계산서` **n=2** · 세션 `63b2dfa4`

## 1. 프로젝트 위치 및 환경
- 저장소: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 메인 폴더 — **이 세션은 미접촉**)
- 작업 위치: 워크트리 `~/.claude-worktrees/Ohiselling/1p-invoice-n2` (브랜치 `feat/1p-invoice-n2`, `origin/main` 704f5cfe 기준)
- prod 백엔드: **포트를 매번 재라.** 이 세션 중 `:8011 → :8001 → :8011`로 **두 번 뒤집혔다**(다른 세션의 블루그린). `ssh sellc.ohitech.co.kr "ss -lntp | grep -E ':(8001|8011)'"`
- prod 화면은 이 Mac에서 브라우저로 직접 열린다(IP 허용목록): `https://sellc.ohitech.co.kr/rocket-recon`. 단 브라우저 팬 뷰포트가 0x0이라 `read_page`가 빈 트리를 준다 — `get_page_text` + `javascript_tool`(클릭)로 우회했다.
- prod 조회는 파일+scp: `scp -q q.py sellc...:/tmp/q.py && ssh sellc... "cd /home/ubuntu/ohisell/backend && .venv/bin/python - < /tmp/q.py"`
- ★**Mac 페처 런타임은 저장소가 아니다** — 아래 §2 참조.

## 2. 이번 세션 완료 목록 (저장소 코드 **0줄**)
- ✅ **S3 쓰기 정찰 완주** — 산출 `docs/references/106_ri_confirm_recon_20260827.md`
- ✅ **S2-3 라이브 재관측 완주** — prod 화면에서 굳음 8건 → 0건 실관측
- ✅ Mac 런타임에 재훑기 코드 배포 + 로켓 데몬 재기동
- ✅ 교훈 **#371** 추가 · 트랙 `확인:` 줄 1개 추가 · 체인 등록부 n=2 행

### ★★가장 중요한 발견 1 — 「배선했다」가 「라이브에 깔렸다」가 아니었다 (교훈 #371)
n=1의 완료 QA는 S2-3 미달의 원인을 「Mac 페처가 그 세션 중 실행되지 않아서」로 적었다. **틀렸다.**
데몬이 실행하는 파일은 `/Users/jino/.ohisell/tools/rocket_supplier_fetcher.py`라는 **별도 사본**이고,
설치본은 **2026-08-22**판이라 `_collect_stale_open_po_pages`가 **0건**이었다(저장소 최신 4건, diff 113줄).
그 상태에선 갱신 버튼을 몇 번 누르든 굳은 8건은 영원히 안 갱신된다.
- 고침: `bash tools/install_local_runtime.sh --files-only` (CAS 판정 `ancestor` 통과, 매니페스트 기록)
  → `launchctl kickstart -k gui/$(id -u)/com.ohisell.rocket`
- ⚠️ CAS가 `ad_settings_collect.py`를 건너뛰었다 — 어느 브랜치·매니페스트에도 없는 **다른 세션의 미커밋 배포본**이다. 건드리지 않았다.

### ★★가장 중요한 발견 2 — 「굳은 8건 = 죽은 유령」은 틀렸다
재훑기 첫 회전(22:24:28, `날짜 22개 → 22페이지 · 발주 41건`, 발주 push **522/11 → 753/33**) 뒤:
- 8건 전부 `synced_date` 2026-08-05 → **2026-08-27** 갱신, `stale_count` 8 → **0**
- **그런데 8건 전부 여전히 `RI`다** — CI로 전이되지 않았다. 그리고 **발주상세에 확인 버튼이 살아 있다.**
- ⇒ 지급은 끝났지만(지급일 2025-12-19 ~ 2026-04-22, 전건 경과) **죽은 줄이 아니라 «아직 안 누른 줄»**이다. 최고(最古) 2025-10-15.
- RI 총계 **12건 → 15건**(481,085원 → **7,299,040원**). 기존 4건 중 2건(140045435·140045255)은 CI로 전이돼 빠졌고 신규 6건이 들어왔다.
- 화면은 오독을 막고 있다 — 오래된 8건에 **「지급일 경과」 배지**가 실제로 렌더된다(prod 스크린샷 확인).

### ★S3 판정 — **벤더 버튼이다**
- `#btnConfirmInvoice` → **`POST /scm/purchase/order/confirmInvoice?purchaseOrderSeq={seq}`**
- **바디 없음**(`$.post(url, callback)` — 2번째 인자가 함수라 jQuery가 data를 생략), **CSRF 토큰 없음**, 응답 `{success: bool}`, 실패 시 `alert(data)`(구조화 에러 없음), 사람 게이트는 네이티브 `confirm()` 1회
- 핸들러 원문: `detail.js:215-227` (`GET /scm/resources/20260821172025/supplier-hub/js/app/po/detail.js`)
- ★**대조군이 결정적이었다**: 버튼은 **RI 3/3 존재 · CI·PA·RP 0/3**. 서버측 상태 게이트 ⇒ **버튼 존재 = 「지금 확인 가능」의 정본**으로 쓸 수 있다.
- 상태 모델(진행바 `data-status` 원문): `REQUEST_CONFIRM_PARTNER`(RP) → `PURCHASE_ORDER_ACCEPT`(PA) → `REQUEST_PARTNER_CONFIRM_INVOICE`(**RI**) → `PARTNER_CONFIRM_INVOICE`(**CI**). prod 분포: CI 2,582 · PA 71 · RP 32 · RI 15.
- ★**정찰은 GET만 했다.** 확인 버튼을 누르지 않았고 supplier에 상태 변경 요청을 보내지 않았다(계약 §3 금지선 준수).

## 2-1. 완료 QA
> 별도 서브에이전트(Sonnet, 읽기 전용). **판정 원문 그대로** — 미달·판정불능 포함.

- **작업 목적(정본 원문)**: 계약 §1 — *"…supplier의 「거래명세서확인요청」(RI) 건을 SellC에서 모아 볼 수 없고… 되면: `/rocket-recon`에서 ①발주가 돈이 되기까지의 열린 파이프라인 전 구간을 상시 확인하고 ②살아 있는 RI 건만 골라 볼 수 있다."*
- **합격기준(원문)**: 계약 §4 S3 3항목 + S2 4번째 항목(미종결 PO 재수집)

### 판정 — 대조 3건 각각
- `판정(계약 §4 S3+S2-3): **부분달성** — S3-1 달성 / S3-2 **판정불능**(QA 에이전트가 채팅 표면에 접근 불가) / S3-3 달성 / S2-3 달성 (2026-08-27 KST)`
- `판정(Jino 지시 원문): **부분달성** — 「모아서 보기」 달성, 「눌러서 처리」 미착수(계약 §1이 스코프에서 뺀 의도적 이월) (2026-08-27 KST)`
- `판정(트랙 궁극 목표): **미달** — 진행률 10/12 불변, 애플리케이션 코드 diff 0줄, 종합조망 파일 무변경. QA 원문: "판정불능이 아닌 이유: 증거가 불충분한 게 아니라 «0 diff·체크박스 불변»이라는 확정적 증거가 있다."*`
- **종합 판정: 부분달성**

### 항목별 (QA 원문 요약)
1. **주체·요청 판정 → ref 산출 — 달성.** `grep -c 'id="btnConfirmInvoice"'` RI 1/1/1 · 대조군 0/0/0, `detail.js:218` 원문 확인. 정찰 스크립트 전문 검토 — **GET fetch만 존재, POST·클릭·submit 없음**(금지선 준수 독립 확인).
2. **Jino 채팅 화면 보고 — 판정불능.** QA는 그 대화 스트림에 접근 권한이 없다. 「발견 0건」이 아니라 「증거에 못 닿음」.
3. **전제(Mac·로그인) — 달성.** 단 launchd 로그에 남은 복구는 22:24 **1회뿐**이고 나머지 3회는 스크래치 스크립트 stdout에만 있다. 로그인 생존의 독립 증거는 정찰 6건 전부 200·수만 바이트 실HTML.
4. **S2-3 — 달성.** 로그·API·화면 **3층 정합**: `22:24:28 재훑기 22페이지/41건` → `push {ingested:753, pages:33}` → API 15행 전부 `is_stale=false` → 화면 「지금 확인이 필요한 건 15건 · 7,299,040원」/「⚠️ 상태가 굳은 건 0건」. 계약이 지목한 예시행 139791428(발주 08-18·입고 08-26·계산서 30871641·230,235원)이 **원문과 정확히 일치**.

- **미달·미판정**: S3-2 판정불능 · Jino 원문의 「눌러서 처리」 미착수 · 트랙 S5·S6 미달
- **목적 전환 여부**: 없음(`🔁 목적 전환` 0건). n=1이 남긴 슬라이스를 그대로 수행.
- **QA가 확인 못 한 것(원문)**: ①S3-2 실제 이행 여부 ②「4회 로그인 복구」의 정확한 횟수 ③RI 3건을 가져온 시점의 스크립트 원본이 스크래치패드에 안 남음(같은 파일을 TARGETS만 바꿔 두 번 돌렸다 — **ref 106 §1에 그 한계를 명기했다**) ④supplier 서버측 access log 열람 불가 ⑤착지(커밋·PR·머지) 결과는 QA 범위 밖

★**QA 지적 2건을 반영해 고쳤다**: ref 106 §1에 「같은 스크립트를 두 번 돌려 RI 회차 TARGETS가 파일로 재현되지 않는다 — 다만 요청 방식은 `FETCH_JS` 상수가 정하고 두 회차 동일」을 명기 · 앵커의 「자동 로그인 복구 4회」를 출처별로 갈라 적음.

## 2-2. 트랙 진행률
- **트랙**: `docs/tracks/active/track_coupang-rocket-1p.md`
- **트랙 목표 원문**: `"우리가 지금까지 Ofix에서 한 일을 OhiTech에서도 구현할 수 있어? 물론 OhiTech는 로켓배송이 추가되지" / "발주한 금액, 납품한 공급가, 정산 금액을 모두 봐야지. 매출은 쿠팡이 발주한 금액이 될꺼고" / "광고비용이 빠지겠지?"`
- **진행률**: 시작 **10/12** → 종료 **10/12** (불변)
  - 미달: **S5**(종합조망 로켓배송 뷰/축 + 갱신 버튼 + 원가 매핑 관리 UI + 커버리지% 배지) · **S6**(prod 라이브 self-verify + 적대 리뷰 + 배포)
- **이번 세션이 움직인 항목**: **없음.** 이 슬라이스는 트랙 항목이 아니다(체인 슬라이스). 진전 없음도 유효한 기록이다.
- ★단 트랙 「안함」의 *"supplier 쓰기 → CONTRACT_1p_invoice_gap.md S3 정찰 후 별도 계약"* 의 **전제 조건이 충족됐다** — 이제 그 별도 계약을 초안할 수 있다(Jino 승인 지점).
- **다음 세션 후보(트랙 기준)**: S5.

## 2-3. 착지
- **완료 단계**: 커밋 → push → PR → (적대 리뷰 **생략**) → 머지 → **완주**
- **멈춘 단계**: 없음
- **재개 명령**: 해당 없음
- **좌표**: 커밋 `4886af3a` → PR **#513** → 머지 **`abe50a25`**  ·  종결 기록(이 절·`end_kst`)은 별도 PR
- **CI**: **3/3 실통과** (backend py3.10 11m50s · py3.14 9m20s · frontend 51s), `--force` **미사용**
  - ⚠️ 1차 `safe_merge` 시도는 `MERGE_WAIT` 기본 600초 안에 py3.10이 안 끝나 **거부**됐다(정상 동작). `--force`를 쓰지 않고 `MERGE_WAIT=900`으로 재시도해 통과.
- **적대 리뷰**: ⚠️ **리뷰 생략: 기록물만** — 7파일 전부 문서·기록물(HANDOFF·LESSONS_LEARNED·체인 jsonl·claude-progress.txt·TRACKS.md·트랙 파일·ref 106), **코드 0파일**
- **배포**: 없음(저장소 코드 0줄). 단 **Mac 런타임 배포 1건**은 했다 — `~/.ohisell/tools/rocket_supplier_fetcher.py` (CAS `ancestor` 통과, 매니페스트 `blob 2ed20509…` · `commit 704f5cfe` · `2026-08-27 22:22:08 KST`) + `launchctl kickstart -k gui/$(id -u)/com.ohisell.rocket`
- **정정 경로**: 이 PR은 revert 커밋으로 되돌릴 수 있다. Mac 런타임은 `install_local_runtime.sh --force=rocket_supplier_fetcher.py`로 되돌린다(설치 시 백업 생성).
- **L5**: 로컬 `main`이 공유 메인 폴더에 잡혀 있어 「main에 세워둔다」 **생략**

## 2-4. 등록부
- 체인 `1p-계산서` n=2 행은 `handoff`를 먼저 채우고 **착지가 끝난 뒤** `end_kst`를 채웠다(2단계 — 그 사이 구간에 커밋을 더 썼기 때문이다).
- ⚠️ 이 절을 쓰는 중 실수 1건: `git switch -c <새브랜치>` 로 옮긴 뒤 `git checkout <옛브랜치> -- <파일>` 을 실행해 **방금 쓴 워킹트리 편집을 커밋본으로 덮었다**. 재작성해 복구했다. `checkout -- <경로>`는 그 경로의 미커밋 변경을 조용히 버린다.

## 3. 확정된 결정사항
- **「거래명세서확인」은 벤더가 눌러 도달하는 종착 상태다**(CI). 쿠팡 자동이 아니다.
- **확인은 지급의 선행조건이 아니다** — 안 눌러도 돈은 들어온다(8건 실증). 기한도 없다.
- **버튼 렌더는 서버측 RI 게이트다** — 「눌러도 되는가」를 우리가 지어낼 필요가 없다.
- **Mac 페처 런타임은 저장소와 별도 사본이다** — 머지 ≠ 배포(교훈 #371).

## 4. 핵심 파일 목록
| 파일 | 역할 |
|---|---|
| `docs/contracts/CONTRACT_1p_invoice_gap.md` | **정본 계약** — §4 합격기준, §7 미상 |
| `docs/references/106_ri_confirm_recon_20260827.md` | **이번 세션 산출** — S3 정찰 전문 |
| `tools/install_local_runtime.sh` | Mac 런타임 배포(CAS 가드·매니페스트). `--dry-run`이 「지금 누구 코드가 깔려 있나」를 답한다 |
| `/Users/jino/.ohisell/tools/rocket_supplier_fetcher.py` | **실제로 도는 페처**(저장소 아님) |
| `backend/app/services/coupang/rocket_pipeline.py` · `routers/overview.py` · `routers/coupang_ops.py` | RI 큐·재훑기 날짜 API |
| `frontend/src/pages/rocketPipelineTabs.tsx` | 확인요청함 탭 화면 |

## 5. 알려진 이슈 / 주의사항
- ★**prod 백엔드 활성 포트가 세션 중 두 번 뒤집혔다**(8011→8001→8011). 인계에 적힌 포트를 믿지 말고 `ss -lntp`로 매번 잴 것.
- ★`~/.ohisell/tools/ad_settings_collect.py`는 어느 브랜치·매니페스트에도 없는 미커밋 배포본이다(CAS가 건너뜀) — 소관: 광고 수집 트랙.
- supplier 세션이 Chrome 종료 후 매번 끊겨 run마다 Keychain 자동 로그인을 다시 한다(이 세션에 4회). 전부 성공했으나 run당 ~1분을 쓴다 — 소관: `CONTRACT_collection_stability_s1`.
- 브라우저 팬 뷰포트 0x0 — `read_page`가 빈 트리를 준다. `get_page_text` + `javascript_tool` 클릭으로 우회.
- `_collect_stale_open_po_pages` 후에도 `stale-open-po-dates`는 **날짜 18개·발주 33건**을 계속 준다(RP 24·PA 9 계열). RI만 0이 됐다 — 정상이다(비-CI 미종결은 계속 생긴다).

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)**: Jino 2026-08-27 — *"그리고 거래명세서확인요청 내용을 SellC에서 모아서 볼 수 있나? **이걸 모아서 내가 누를 수 있게 해줘**"* / *"딥링크는 뭐야? **내가 원하는건 SellC에서 눌러서 처리하는거야**"*
- **남은 슬라이스**:
- [ ] ★**쓰기 실행 계약 초안 — 이제 성립 가능하다(S3가 답했다).** ⚠️ **§1 승인 지점이다**(되돌릴 수 없는 회계 확정 + 계약 「안 함」 변경) — Jino 승인 없이 착수하지 말 것. 초안에 반드시 못 박을 것:
  - **재시도 금지**가 금지선 — 멱등성이 `[미상]`이다(호출한 적 없음). `refresh_contract`의 lease 재시도는 **읽기용 설계**라 그대로 쓰면 같은 확인을 두 번 누른다.
  - 백엔드 `_write_guard.guarded_write` 패턴(dry_run 기본 + `CONFIRM_LIVE_WRITE` 토큰 + WARNING 감사 로그)을 게이트로, Mac 페처는 실행기로만.
  - 되돌리기 경로가 화면에 **없다** — 되돌릴 수 없다고 가정할 것.
  - 부분 실패의 표면이 `[미상]` — 응답이 구조화돼 있지 않다(`alert(data)`).
- [ ] (트랙 별건) **S5** — 종합조망 로켓배송 뷰/축 + 갱신 버튼 + 원가 매핑 관리 UI + 커버리지% 배지. 트랙 10/12를 움직이는 유일한 남은 기능 슬라이스. ★체인 슬라이스와 **다른 것**이니 섞지 말 것.
- [ ] (선택) 굳은 RI 8건(2025-10 ~ 2026-01, 4,870,435원)을 **수동으로** supplier에서 확인할 것인가 — 지급은 끝났으므로 회계상 잔여 효과는 미상. 쓰기 계약이 서면 자동으로 처리된다.

★**3문 검사 결과**: 쓰기 실행 계약은 ①계약 「안 함」을 바꾸고 ②되돌릴 수 없다 ⇒ **§1 승인 지점**. 나머지(S5)는 트랙 기존 항목이라 묻지 말고 진행 가능.

## 7. 새 세션 시작 프롬프트
```
/session-relay 1p-계산서
```
