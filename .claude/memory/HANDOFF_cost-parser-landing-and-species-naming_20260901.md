# HANDOFF — sellc-원가-메뉴 n=23 «후반» (세션 8560ea1e · 2026-09-01 13:5x~22:5x KST)

> ⚠️ **이 세션은 13:5x에 이미 한 번 종결 등록됐다**(등록부 n=23 `end_kst: 2026-09-01 13:5x`,
> 인계 `HANDOFF_cost-truth-source-layer-s2_20260901.md`). 그 뒤로 **9시간을 더 진행했다.**
> 이 파일은 그 «후반»의 인계다. 앞부분(S2 정본 판별층 착지)은 위 파일이 정본이다.
> ★규약이 경고하는 「닫힌 뒤에도 쓰는」 구간이 이번엔 **9시간**이었다 — 역대 최장이다.

## 0. 한 줄

Jino 질문 하나(*"1번부터 가자고"*)에서 시작해 **내 오경보 하나가 세 갈래를 열었다** —
엑셀 전수 대조 · 파서 결함 착지 · 부자재 종 이름 규칙. `cost_price`는 **한 건도 안 바꿨다.**

## 1. 완료 QA — 판정 원문 (별도 Sonnet · 읽기 전용 · 22:5x KST)

**판정(계약 C §4 S2): 달성** — prod `https://sellc.ohitech.co.kr/cost`의 「정본 판별」탭에서 963 SKU 표·보류 사유+소관 문장·정본없음 소관별 분류 세 항목 전부가 지금 직접 브라우저로 관측된다. 단 이 배포는 이 세션(n=23)의 조치가 아니라 **다른 세션(`pao-n80`, 커밋 `bdcea3a8`, 14:55 KST)의 프론트 배포가 이미 병합돼 있던 이 세션 코드(PR #624)를 함께 실어 올린 부수효과**다 — 앵커의 「미달」 판정(13:3x)은 그 시점엔 정확했으나 지금은 stale하다.

**판정(Jino 지시 원문): 부분달성** — 달성: 12개 지시 전부가 커밋·prod DB·라이브 코드로 재현됨. **미달: 앵커에 기록된 19:43 지시(「G2 269건 컷오버, 방식 b」)가 전혀 집행되지 않았다** — `cost_price_history` 행수 0(전건), `product_master.cost_price` 합계 3,642,052.1 불변. 세션은 이 슬라이스를 포기하면서 `🔁 목적 전환` 선언도 없이 다른 작업으로 넘어갔다(무단전환에 해당할 소지).

**판정(트랙 궁극 목표): 부분달성** — 체크박스 3/6 불변(B1·B2·A1 [x] / A2·C1·C2 [ ]). 오늘 작업은 「정확한 원가 산출」에 실질 기여하나 어느 체크박스도 옮기지 않았다 — 계약 §6이 착수 전 예고한 대로이므로 표류는 아니다.

**판정(종합): 부분달성** — 목적 전환 선언 없음. S2가 「미달→달성」으로 뒤집힌 것은 이 세션의 공로가 아니라 우연한 병합 순서의 산물이라는 점, Jino가 명시 승인한 대규모 쓰기(G2 269 컷오버)가 그대로 증발했다는 점 — 둘 다 「무엇이 안 움직였나」다.

### ★19:43 슬라이스가 왜 증발했나 (사후 기록)

Jino가 「b」(오늘 일괄 컷오버)를 고른 **직후 본인이 되물었다**:
> *"가격을 너가 임의대로 덮어버리면 나중에 또 문제되는거 아니야?"*

⇒ **Jino 본인이 일괄 덮어쓰기를 보류**시켰고, 대화는 「그럼 지금 원가가 정확한가」로 옮겨갔다.
**전환 자체는 정당했다.** 잘못은 그 보류를 `🔁 목적 전환`으로 **선언·기록하지 않은 것**이다 —
그래서 슬라이스 2 체크박스 4개가 사유 없이 비었고 QA가 「무단전환 소지」로 읽었다.
판정은 약속 대비만 재므로 **미달 기록을 그대로 둔다.**

## 2. 한 것

### 2-1. 파서 착지 — PR #641 머지 `33537c9d` · prod 배포 완료

**고친 것 셋:**
1. 총액 열을 **라벨로** 찾는다(`_TotalSpec`) — 열 위치를 안 박는다
2. 「데이터 행인가」와 「총액이 얼마인가」를 분리 — 섹션 제목은 **구조로** 판정
3. `recipe_kind` 세 번째 값 **`purchased`**(국내 매입 완제품)

**prod에서 정정된 값:**
| 품목 | 전 | 후 |
|---|---:|---:|
| 오타오_투명 강화유리 | 12.2 (CNY) | 3,102.70 |
| 맥세이프 일미리 케이스 | 0.99 (USD) | 2,676.38 |
| 오픽스 거치대 | 11 | 19,582.64 |
| 맥세이프 셀카봉 | 4.5 | 8,011.08 |

**적대 리뷰 1R FAIL(P1 4) → 2R PASS.** 리뷰어가 **변이 5종을 살려냈고 그때 관련 테스트 110개가 전건 초록**이었다. 다섯 다 「값은 맞는데 사람이 그걸 못 본다」 계열이고 **넷은 코드 주석이 위험을 이미 자백해 둔 자리**였다(*"여기서 먼저 가른다"*·*"섹션 끝 상품 행이 먹힌다"*). **적어 두는 것으로는 안 막힌다.**

★**P1-4는 진짜 코드 결함**: `truth_source.classify_group`이 매입품에 「부자재 보강이 선행이다」라는 **틀린 지시**를 화면에 냈다(1줄이 정상 모양인데 보강하라고). 수리 후 **변이 10종 전건 사살**, 2R 리뷰어가 직접 재주입해 재현 확인.

⚠️**내가 그 자리에서 같은 병을 또 밟았다** — P1-4를 **코드만 고치고 테스트를 안 써서** 변이 주입에서 살아남아 되돌아왔다(42 passed). 그리고 변이 되돌리기에 `git checkout --`를 써 **커밋 안 된 내 수정을 한 번 날렸다**.

회귀 백엔드 **7,615** · 프론트 1,334 · lint 0e/96w · build exit 0 · CI **3/3**.

### 2-2. 원가표 재업로드 — prod DB 변경

`POST /api/cost/recipes/import` · 사전 백업 `ohisell.db.bak-costreupload-20260901-221119`(2.8GB) · HTTP 200.

- **원가표 항목 60 → 68** (사라졌던 8행 복구 — 그 8행이 **케이스 165 SKU**의 원가였다)
- `recipe_kind`: assembly 55 → **assembly 52 · imported_goods 5 · purchased 11**
- 자백 문구가 DB에 실제로 저장됨: `foreign_only_no_landed:19.3` · `landed_only_vat_unknown` ×2 · `excel_total_mismatch:1014.20≠922.0`

**★안 바뀐 것 (전건 실측 대조):** 단가 102 · 계산값 22 · 구성 180줄 · 링크 924 · 승인 22 · 종 139 **전부 불변** · 표준원가 합계 **85,373.47 → 85,373.47** · **`cost_price` 합계 3,642,052.1 불변** · 핀 relinked 23 / lost 0.

⚠️**`패키지 (fold)` 참고값 370은 안 고쳐졌고 그게 옳다** — `conflicted: values [171.00, 320.00] → kept 370.00`. 엑셀 폴드 섹션에 패키지가 두 값이라 「값이 둘이면 안 고른다」가 거부했다. 계산 영향 0(적용단가 320), 화면 참고값만 틀리다.

### 2-3. 부자재 종 이름 규칙 — 커밋 `74a96c9b` · **PR #648 ⛔머지금지**

**등급을 «값»이 정한다**(`_GRADE_BY_VALUE`): 안내문 30→소·55→대·40→태블릿 / 패키지 171→소·320→대·550→태블릿. 표에 없는 값은 **고르지 않고** `unknown_grade:` 자백.

★**초판은 등급을 «폼팩터»로 정했고, 내가 넣은 가드가 배포 전에 반증했다** — 폴드인데 소포장을 쓰는 제품 둘 때문에 「대」 아래에 171과 320이 같이 들어갔다. ⇒ **이름에서 폼팩터를 뗐다면 등급에도 넣으면 안 된다.**

**비닐 무접촉** — Jino: *"비닐은 사이즈로 구분해야 해서 지금처럼 두는게 맞을거 같은데?"*

실측(옛 파서 ↔ 새 파서, 같은 엑셀): 종 **124 → 108** · 부자재 30 사라지고 14 생김 · **필름류 변화 0건** · 미지 등급 0건.

## 3. 오늘의 실측 — 다음 세션이 쓸 사실들

- **139종 중 64종이 「참고값은 있는데 단가 미채택」**. 계산을 실제로 막는 것은 **r10 도어락(draft·SKU 1) 하나뿐**
- 도어락은 **엑셀에 값이 다 있다**(내가 「빈칸」이라 한 건 오독 — 75행은 섹션 제목 행) · 채택하면 3,196.4원, 엑셀 3,162.5와 차이 **+33.9 = cleaning kit 환율 상수**
- cleaning kit: **원장이 정본**(ex 190.82 / inc **209.90** · ledger · 2026-08-18), 레시피 18개 중 17개 적용 · buddy 6레시피는 kit 줄 없음(엑셀과 일치)
- 「비닐 (12\*22+4)」이 **엑셀 안에서 두 값**(bar·doorlock·buddy 13 / **flip 10**) — Jino 확인: 의도된 값
- 엑셀 51·53행(폴드 힌지 2매·사생활_외부2매)이 플립 포장을 쓰는 것은 **정상**(Jino 확인)

## 4. 착지

| 무엇 | 상태 |
|---|---|
| PR **#641** 파서 수정 | ✅ 머지 `33537c9d` · CI 3/3 · prod 배포(CAS 6/6·무중단 0초·마이그 없음) |
| PR **#648** 종 이름 규칙 | ⛔ **머지 금지** — 마이그레이션과 한 슬라이스. 파서만 배포되면 다음 업로드에 옛 30종 + 새 14종 공존 |
| 트랙 확인줄 2건 | 분리 브랜치 `docs/cost-n23-track-records` |
| prod DB | 재업로드로 변경(백업 있음) · `cost_price` 무변경 |

## 5. 다음 세션 후보

1. **부자재 종 병합 마이그레이션** — PR #648과 한 슬라이스. 종 30개 병합 + 구성줄 repoint + 되돌림 매핑 보존. 계약 초안 `docs/contracts/CONTRACT_material_species_consolidation.md`(D-CPP-65, 승인 면제) 참조
2. **G2 269건 컷오버**(계약 S3) — 미집행. ★**「임의로 덮어쓰지 않는다」는 Jino 우려가 설계 입력**이다(방식 b 재검토 사유)
3. 도어락 8종 「채택」 — Jino 클릭
4. `74a96c9b`에 테스트 0건 — 마이그레이션과 같이 쓸 것

## 6. 트랙 진행률

- 트랙: `docs/tracks/active/track_cost-truth-ledger.md`
- **시작 3/6 → 종료 3/6** (달성 B1·B2·A1 / 미달 A2·C1·C2)
- **계약 D-CPP-64: 1/13 → 4/13** (S2 세 항목 체크 — 근거는 QA 판정 원문)
- 이번 세션이 움직인 항목: 트랙 체크박스는 **없음**. 계약 S2 3항목이 라이브 성립(단 배포는 남의 세션 부수효과)
- 트랙 목표 원문: *"SellC에 원가 메뉴를 하나 붙이자 … 목표는 정확한 원가 산출이니까?"* (Jino 2026-08-22)

## 7. 이월 (QA 원문 + 내 것)

- ↗️ **G2 269건 컷오버 미집행 · 목적 전환 선언 없음** → 소관: 계약 D-CPP-64 S3
- ↗️ **S2 라이브 성립이 남의 배포 순서에 얹힌 결과라 재현성이 취약** — 더 오래된 커밋에서 프론트 재배포 시 화면이 사라질 수 있다. `frontend/dist/.deploy-stamp` 부재도 확인 가치 → 소관: D-CPP-64 S4
- ↗️ **`74a96c9b`(+124줄)에 테스트 0건** — prod 미배포라 즉시 위험 없음 → 소관: 마이그레이션 세션
- ↗️ **`truth_board()` 층에 배선 테스트 없음** — 2R에서 표적 표면 절단 변이 생존(cause 하나만 `reason=None`으로 끊으면 46건 전건 초록) → 소관: D-CPP-64 S4
- ↗️ (승계) `internal_sku` 자유 변경으로 이력 끊김 · `Product.cost_price` 타입 거짓말 · `alembic.ini`가 `DATABASE_URL` 무시

## 8. 하네스 관측

- ⚠️**트랙 확인줄 훅 오탐 이 세션에서만 38회** — 공유 메인 폴더가 **1,031커밋** stale. 정본(워크트리)엔 확인줄 4건 누적. 기록된 처방대로 공유 폴더에 대신 쓰지 않았다
- ⚠️**등록부 n=23이 13:5x에 닫힌 뒤 9시간을 더 썼다** — 「모르면 살아 있다」가 그 구간 통째로 뚫린다. 새 행으로 정직하게 기록한다
- ✅ `review-surface-mutation.sh`가 2R 위임문의 표면 변이 누락을 잡았다 — **오탐이 아니라 내 누락**이었다(1R엔 넣고 2R엔 빠뜨림)
