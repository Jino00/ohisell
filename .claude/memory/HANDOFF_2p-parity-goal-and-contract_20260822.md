# 세션 인수인계 — 「쿠팡 손익정합 목표」 명명 + 판매일 축 계약 승인 (구현 0건)

> 저장: 2026-08-22 22:2x KST · 세션 `4ab640b3`(전반부 `f0fe421f`) · 사슬 「쿠팡-손익정합」 **n=4**
> 트랙: **`docs/tracks/active/track_coupang-2p-parity.md`** — **재개 · 8/16**
> 계약: **`docs/contracts/CONTRACT_rg_sales_date_axis.md`** — **승인됨 2026-08-22 22:00**
> ⚠️ **이번 세션은 조사·계약 세션이다 — 코드 변경 0건 · PR 없음 · prod 배포 없음.**
> 완료 QA 3대조 = **부분달성 / 미달 / 미달** (원문 §5)

---

# 0. 「쿠팡 손익정합 목표」 ★매 HANDOFF에 이 블록을 복사한다 (Jino 지시)

> 정본: `.claude/memory/chains/쿠팡-손익정합.GOAL.md` — 트랙 헤더와 갈라지면 **정본이 이긴다**

**발단 (2026-08-21)**
> ***"sellC 에 보면 어디에도 오픽스의 자료가 안나오고 있어"***
> *"오픽스 2P도 나오게 한다는거지?"*
> *"이렇게 진행하면 우리가 1P, 3P진행하면서 겪었던 모든 과정(원가매칭, 광고비 매칭등 모든 과정)을 다시 진행해야 하는거야?"*

**확정 (2026-08-22)**
> *"지금 문제는 2P에 대한 내용이 https://sellc.ohitech.co.kr/의 데시보드와 서브메뉴에 전혀 안보이는게 문제인거잖아."*
> *"그리고 2P의 순이익을 내기 위한 모든 계산이 1P, 3P, 스마트스토어에 했던 것과 같이 정확히 나와야 하고."*
> *"어제 어떤 제품이 몇개가 팔리고 그 판매분의 정산공제, 원가, 세금, 기타비용등을 빼고 남는 이익이 있잖아. 다른 판매와 같이 2P도 그걸 보자는거지"*

**⇒ 2P(오픽스 로켓그로스)가 1P·3P·스마트스토어와 「같은 대접」을 받는다.**
**① 자기 메뉴와 자기 화면을 갖는다   ② 그 날 팔린 것의 손익을 그 날 자리에서 본다(판매일 축).**

**★표류 경보 (이번 세션에 실제로 밟은 것)**
1. **「대시보드가 회사 매출의 34.5%만」을 목표로 읽지 마라** — 그건 **1P 트랙**의 2026-08-05 계약 문장이다.
2. **로켓1P는 손대지 마라** — 매출의 절반이 넘어 어떤 실측을 해도 먼저 튀어나오지만 소관이 다르다.
   Jino 원문: *"로켓은 이미 잘 돌아가고 있으니까 너가 건들지마. 우리의 원래 목표가 아니잖아."*
3. **트랙이 「종결」이어도 목표 달성이 아니다** — 08-22 17:54의 「8/8 종결(달성)」은 남의 목표 기준이었다.

---

## 1. 발단 — 트랙이 «남의 목표»로 닫혀 있었다

n=3이 트랙을 「8/8 종결(달성)」으로 닫았는데, 그때 판정한 목표가 **1P 트랙의 계약 문장**이었다.
이 사슬의 발단(*"sellC 에 보면 어디에도 오픽스의 자료가 안나오고 있어"*)이 아니었다.
헤더가 그렇게 적혀 있어서 **이번 세션도 착수 30분간 1P 진단으로 통째로 샜다** — Jino가 두 번
되밀어서(*"로켓은 건들지마"* → *"세션 쿠팡 손익정합의 제일 처음을 다시 뒤돌아봐"*) 목표를 되찾았다.

⇒ 목표에 **이름을 붙였다**(Jino 명명): **「쿠팡 손익정합 목표」**. 앞으로 앵커·HANDOFF·계약·QA 위임문은
목표를 다시 쓰지 않고 이 이름으로 부르고 정본 파일을 가리킨다.

## 2. 실측 3건 (이번 세션의 본체)

### 2-1. 채널 4종 순이익 항목 인벤토리 (`.claude/memory/chains/쿠팡-손익정합.INVENTORY.md`)
기준점 = `dashboard._channel_rows()` — **4채널이 같은 행 모양으로 만나는 유일한 지점**.
- **2P에만 없는 것 2건**: 교환 배송손익 · 3PL 고정비(`rg_channel_pnl.py:134` `"fixed_cost": "0"` 하드코딩)
- **2P·3P가 같이 없는 것 2건**: 비-PA 광고비 · 매출 정산화 보정(종합조망 전용) — **2P만의 미달로 세면 안 된다**
- ★**2P가 이미 3P·네이버보다 «나은» 것 1건**: 원가 커버리지 게이트. 3P·네이버는 원가를 몰라도 순이익을
  그냥 낸다(`profit_calculator.py:344-350`은 `unmapped_revenue`로 자백만 한다).
  ⇒ **「3P와 똑같이」를 합격기준으로 쓰면 후퇴다.**

### 2-2. 표면 실측 (`frontend/src/components/Layout.tsx:244-282`)
| 채널 | 전용 메뉴 | 전용 화면 |
|---|---|---|
| 로켓배송 1P | 접이식 그룹 1 | **4** |
| 네이버 광고 | 항목 1 | **9** |
| **로켓그로스 2P** | **0** | **0** |

2P는 남의 화면에 세 들어 있다 — 종합조망 14곳 · **재고 관리 8곳**(`RgReplenishmentTable`·`RgFeeAuditView`) ·
쿠팡 운영 5곳. ★**`Dashboard.tsx`는 RG를 0회 언급한다.**

### 2-3. ★정산공제 그레인 불일치 (INVENTORY.md **부록 A** — 이번 세션 최대 산출)
Jino: *"어제 187,120원을 팔았는데 정산공제액이 153,058원이었어. 이게 말이돼?"* → **안 된다.**

- 화면 −32,673.09원을 재현했다. **네 항 중 셋(매출·원가·광고비)은 이미 판매일/집행일 축**이고
  `commission` 한 항만 **08-17~23 한 주 통짜**다(overlap 필터에 안분 없음 — 그 주기를 덮는 어느 날을
  물어도 같은 값). 그 한 항이 그날 매출의 **81.8%**라 부호를 뒤집는다. **08-18은 625%.**
- 정상 비율 = **29.45%**(완결 주기 08-10~16 실측). 옵션 단가에서 올라온 값과 교차 일치
  (개당 물류비 2,921원 ÷ 객단가 15,593원 = 18.7% + 판매수수료 ~10%).
- **08-21 판매일 축 완전 분해 = +51,375 ~ +57,460원.** 감도 하한(요율 상단+결손 19% 전부 얹음)에서도
  **+41,000원** — **범위 전체가 흑자다.** 원가 39,352.40원은 화면값과 **정확히 재현**됐다.

## 3. 계약 (승인됨 2026-08-22 22:00 — Jino *"그러자"*)

`docs/contracts/CONTRACT_rg_sales_date_axis.md`
**범위**: `commission` 한 항을 정산 인식일 축 주간 통짜 → **판매일 축**으로. 산출물 4가지
(옵션 단가 SA · 요율 SA · `rg_channel_pnl` 교체+자백 필드 · 트랙 재개).
**안 함**: ①2P 전용 메뉴·화면(**다음 계약** — 숫자가 틀린 채 화면을 늘리면 틀린 숫자를 더 많은
자리에 뿌린다) · 교환 배송손익 · 3PL 고정비 · 옵션 단위 «수집» 개선(별건, 단 **화면 자백은 이번 몫**).
**합격기준 ⓐ~ⓗ는 전부 sellC 화면에서 판정한다.**

## 4. 착수 실측 결과 (계약 §8 — 다음 세션은 여기서 바로 구현에 들어간다)

### 4-1. 소비처는 셋이고 «같은 일을 하는 구현이 둘»이다
| 소비처 | 경로 | 처분 |
|---|---|---|
| 대시보드 | `dashboard.py:383` → `rg_channel_pnl.py:86` | **교체(본체)** |
| 종합조망 | `intelligence.py:593` `_agg_rg_settlement_fees` → `:1209-1210` | **같이 옮긴다** — 안 옮기면 또 두 화면이 다른 말을 한다(D-CPP-47이 고친 그 병) |
| 상품손익 | `product_pnl.py:360-361` · `:341` | 동반 이동. **보존식 깨지면 SKU 손익 표가 렌더 자체가 안 된다** → 회귀 검사 필수 |

### 4-2. ★베낄 설계는 3P가 아니라 `product_pnl`이다
`product_pnl.py:349-375`가 **이미** 옵션 단위 읽기 + **VAT gross-up ×1.1** + **잔차 2종 분리**
(`rg_account_only_fees`·`rg_vat_grossup_gap` — 「단일 plug는 오류를 숨긴다」) + **보존식 정확 성립**을 한다.
⇒ **계약 §4 ⓒ의 설계가 이미 존재한다. 새로 만들 것은 「날짜 배분」 한 겹뿐이다**(product_pnl은 창 단위).

### 4-3. 판매수수료 요율 — 네 가설 중 **둘 배제**
| 가설 | 판정 |
|---|---|
| 옵션별 실측 요율이 이미 있다 | **배제** — `coupang_revenue_fee`에 RG 옵션 **0건**(3P 대조군은 384·266건 겹침 = 조인 정상). **그 테이블은 3P 전용** |
| 정산 인식 지연 | **배제** — 1주 밀면 편차가 **26배**(0.699%p → 18.10%p) |
| 카테고리 믹스 | **배제 안 됨** — 요율 낮던 주기는 아이폰 강화유리 지배, 오른 뒤는 골프필름·Z플립폴드 사생활필름. 집중도 63.5%→82.1%. **인과는 확인 안 됨** |
| 단일 정률+VAT | **확인 안 됨 유지** — ÷1.1 해도 1.65%p 흔들림 |

⇒ **요율은 추정할 수밖에 없다** → `basis` 표시는 필수. **단일 상수 금지** → 최근 완결 주기에서 실측 갱신.
부수: 옵션 row=VAT前 · 계정 row=VAT後(`intelligence.py:606-612`) — 역산 요율 8.42~10.23%는 VAT 포함.

### 4-4. 보관비 처분 **확정: 판매일 귀속 금지**
★결정적 관측 — **WING2의 6개 주기에서 매출이 정확히 0인데 storage는 1,988~7,259원 발생**했고,
같은 주기 `sale_fee`·`delivery`·`warehousing`은 **전부 정확히 0**이었다. 재고 보유 비용이 맞다는 실증.
WING2 비중 71%는 storage가 커서가 아니라 **매출 연동 비용만 0으로 떨어지기 때문**(10주기 전부 57.8~60.7%, 이상치 아님).
⇒ **계정 기간비용으로 분리 표시**하고 §4 ⓒ 보존식이 검사한다(조용히 사라지면 보존식이 터진다).

---

## 5. 완료 QA (별도 기 · Sonnet · 읽기 전용 · 22:1x KST · **판정 원문 그대로**)

> 판정(계약 §4 합격 ⓐ~ⓗ): **부분달성** — 달성 2건(ⓖ·ⓗ) / 미달 6건(ⓐ·ⓑ·ⓒ·ⓓ·ⓔ·ⓕ) (2026-08-22 KST)
>
> 판정(「쿠팡 손익정합 목표」 ②): **미달** — 08-21 순이익이 여전히 −32,673.09원으로 계약 이전 버그값과
> 정확히 일치, 판매일 축 배선이 코드에 존재하지 않음(diff 0건) (2026-08-22 KST)
>
> 판정(「쿠팡 손익정합 목표」 전체): **미달** — ①(메뉴·화면)은 다음 계약 소관이라 애초 범위 밖이고,
> ②(판매일 축 손익)도 이번 세션은 계약 초안·착수 실측만 했을 뿐 구현 커밋이 0건이라 라이브에
> 반영된 것이 없다. 「부분달성이 정상」(계약 §5)이라는 전제는 ②가 구현됐을 때 성립하는 것인데,
> 이 세션 자체가 「구현 0건 — 조사·계약 세션이다」라고 트랙 확인줄에서 스스로 밝히고 있다 (2026-08-22 KST)

**QA가 확인 못 한 것**: ⓑ의 raw `commission` 필드값(API 미노출 — ⓐ의 비트 단위 일치와 코드 diff 0건으로
추론) · 브라우저 픽셀 렌더(전부 curl) · WING2 동일 항목 · 계약 §7 3항목 완결 여부(★**QA 실행 22:1x과
§8-4·8-5 커밋 `d23acd6b` 22:12가 겹친 stale read로 보인다** — 판정은 이에 의존하지 않는다).

**stale 제외 증거: 없음.**

---

## 6. 트랙 진행률

- 트랙: **`docs/tracks/active/track_coupang-2p-parity.md`** (구 `completed/track_coupang-rg-net-ledger.md`)
- **시작 8/8(종결 상태) → 종료 8/16(재개)**. 체크박스는 **하나도 안 찍었다** — 구현 0건이므로.
- 달성 항목(1단계 ⓐ~ⓗ, net 원장 배선): 전건 이전 세션 산물, 불변.
- 미달 항목(2단계 ⓘ~ⓟ, 판매일 축): **전건 미착수**.
- 이번 세션이 움직인 것: **트랙 자체의 목표**(ⓟ에 해당하나 QA가 계약 §4 ⓗ로 판정 = 달성. 체크박스는
  다음 세션이 증거와 함께 찍는다) · 커밋 `5bf21311`.
- **트랙 목표 원문**: §0 참조.
- **★①메뉴·화면의 합격기준은 아직 M에 없다** — 다음 계약이 정의할 때 추가한다(자리표시자 금지).
  그러므로 **16/16이 되어도 목표 전체 달성이 아니다.**
- 다음 세션 후보: 계약 §1 산출물 1~3(옵션 단가 SA · 요율 SA · `rg_channel_pnl` 교체).

---

## 7. 다음 세션이 할 일 (묻지 말고 이어서 한다)

**계약 §1의 산출물 1~3을 구현한다.** 착수 실측(§4)이 다 끝나 있으므로 바로 코드로 들어간다.

1. **`product_pnl.py:349-375` 패턴을 읽는 것부터** — 옵션 단위 읽기·VAT gross-up·잔차 2종·보존식이
   이미 있다. 거기에 **날짜 배분 한 겹**을 얹는 것이 이번 작업의 전부다.
2. **소비처 셋을 같이 옮긴다**(§4-1). 상품손익 보존식 회귀 검사 필수.
3. **요율은 최근 완결 주기에서 실측해 캐시**하고 `basis`를 행에 싣는다. 3P `option_fee_rate.py:85`의
   폴백·`basis` 패턴을 참고하되 **식은 다르다**(RG는 물류비=수량×정액, 판매수수료만 요율).
4. **보관비는 판매일에 붙이지 않는다**(§4-4 확정).
5. 적대 리뷰 1회(변이 중 하나는 **최종 산출물까지 가는 경로**를 끊는 것) → 배포 → **완료 QA 3대조**.

⚠️ **착수 시 인계를 실측으로 검증할 것** — 이 저장소의 인계는 유령을 실어 나른 전례가 많다.
특히 §4-1의 소비처 셋은 **다시 grep**하라(D-CPP-49 때 인계가 셋이라 했는데 실제로는 넷이었다).

## 8. 좌표

- 커밋 10건(전부 main·push 완료, **backend·frontend 0건**):
  `c260a801` 목표 카드 · `992e2497` 목표 명명 · `945b4c01` 인벤토리 · `f8446e7d` 목표 ② 확정 ·
  `1d893af2` 부록 A · `2717bc3a` 계약 초안 · `5bf21311` 트랙 재개 · `e3ce8274` 계약 §8-1~3 ·
  `d23acd6b` 계약 §8-4·8-5
- PR: **없음** · prod 배포: **없음**
- 앵커: `.claude/anchors/4ab640b3-e5c6-44ef-aefa-92d2ea62cd00.md`(판정 3줄 + QA 미확인 목록)
- 새 D-N: **없음**(설계 결정은 계약 §8에 있고, D-CPP 번호는 구현 세션이 받는다.
  ★`scripts/next_ids.sh`는 **D-CPP를 안 다룬다** — 네이버 트랙 경로 하드코딩이라 grep로 최댓값 확인)
- 사슬 등록부: `.claude/memory/chains/쿠팡-손익정합.jsonl` — 이 세션이 **n=4**

## 9. 이월 (스코프 밖 — 고치지 않고 적는다)

- **1P 기본축(계산서)은 원가·프로모션이 `ZERO` 하드코딩** — 이 항목에선 1P가 2P보다 못하다
  → 소관: `docs/tracks/active/track_coupang-rocket-1p.md`
- **3P·네이버엔 원가 커버리지 게이트가 없다** — 원가 미상 매출이 있어도 순이익을 그냥 낸다
  → 소관: 별건(3P 손익 엔진)
- **`/api/dashboard/channel-breakdown`이 `commission`을 노출하지 않는다** — 그래서 완료 QA가 ⓑ를
  직접 관측하지 못했다. 구현 시 이 필드를 노출하면 다음 QA가 직접 잰다 → 소관: **이 계약**(설계 반영)
- **같은 창을 15분 간격으로 두 번 부르면 값이 달라진다**(30일 순이익 −397,297 → −396,666).
  조회가 재동기화를 부르는 구조로 보인다 → 소관: 별건(운영)
- **공유 메인 폴더가 origin/main과 27커밋 뒤처져 있었다**(세션 착수 시 fast-forward로 복구).
  n=3도 같은 함정을 12커밋에서 보고했다 — **반복 재발** → 소관: 별건(운영 절차)
