# 세션 인수인계: API 표면 전수성 검증 + 검색량 기준선 개통 (D-NAO-195·196·197)

> 저장일시: 2026-08-18 22:3x KST · **origin/main push 완료(미푸시 0)** · 저장소 **main·clean**
> 트랙: **PAO(파오)** — `docs/tracks/active/track_naver-ad-optimization.md`
> 결정 **D-NAO-195 · 196 · 197** · 교훈 **#313 · #314** · 다음 번호 = **D-NAO-198 / 교훈 #315**
> prod alembic head = **`kv1vol2daily3`** · PAO 정지 유지(`optimizer` 7/7 `none`)

---

# ★ 이 파일을 읽는 순서

1. **§1 — 다음 세션이 할 일**(D-NAO-197 확정 범위 5개). 여기만 읽어도 착수할 수 있게 썼다.
2. **§2 — 착수 전 5분**(함정·좌표·명령). 이걸 안 읽으면 오늘 겪은 걸 다시 겪는다.
3. 나머지는 필요할 때 참조: §3 확정 결정 · §4 이번 세션이 한 것 · §5 완료 QA · §6 이월·결정대기

---

# 1. 다음 세션이 할 일 — D-NAO-197 (2026-08-18 19:0x **Jino 승인**)

## 1-0. 목적 (원문 — 새로 발명하지 말 것)

> *"성과로 나눈 등급에 대해서 광고, 스마트스토어에서 API로 받아올 수 있는 모든 데이터를 나누자"*
> — Jino 2026-08-18 11:38

**이 문장을 그대로 앵커 `목표:`에 복사한다.**

## 1-1. ★「완료」의 정의 (D-NAO-197로 확정 — 이제 분모가 고정됐다)

Jino가 **「13:34 지목분 + 커머스 목록 확정」**을 선택했다. 아래 **5개를 다 하면 이 작업은 완료**다.

| # | 항목 | 현재 상태(2026-08-18 22:2x 라이브 확인) |
|---|---|---|
| ① | **시간대·지역·매체**(col7/8/9) | 테이블 **0개** |
| ② | **연령·성별·관심사**(CRITERION) | 테이블 **0개**, 코드 호출 **0건** |
| ③ | **매체 블랙리스트**(A5/A6) | 미착수 |
| ④ | **상품 메타**(C10) | 미착수 |
| ⑤ | **커머스 미개봉 75건** 개별 문서 개봉 | 미착수 |

**진도: 0 / 5.** 교차완료 **14/36(층2 기준)** → 5개 완료 시 **약 20/36**. 예상 2~3세션.

★**「몇 % 됐나」를 말할 때 반드시 어느 층의 분모인지 병기한다**(매트릭스 §0-A 3층 깔때기: 표면 ⊃ 축 ⊃ 교차완료). 이 범위는 **층2 기준**이다.
★**범위 밖으로도 확정됐다**(이번 완료의 조건이 **아니다** — 다음 계약감): N2 MasterReport 5종(`item=Qi`) · N3 AdExtension(개통 판정·482콜) · N4 SharedBudget · N7 `/stats` breakdown 3종 · N8 미요청 필드 13종 · 쇼핑 수요 기준선 · C2·C4·C7·C8·C9·C11. **N5=불가(원리) · N6=판정불능**은 D-NAO-196에서 이미 처분됐다.

## 1-2. 착수 순서 — **시한과 비용이 정한다**(분석 가치가 아니다)

### 1️⃣ ① col7/8/9 — 가장 급하다
- **왜 1순위**: `SHOPPINGKEYWORD_DETAIL` **재생성 한도가 정확히 180일**이다(2026-08-17 실측, `scripts/probe_report_retro_limit.py`). **매일 창이 굴러가 앞이 사라진다.**
- **컬럼 정체는 확정돼 있다**(D-NAO-182, 45,200행 실물의 distinct로 검증):
  `col7` = **시간대**(distinct 24, `00`~`23`) · `col8` = **지역**(19) · `col9` = **매체**(22, MEDIA_TARGET의 media id와 자릿수 일치)
  ⚠️「지역」 코드 사전과 「매체」 id 사전은 **아직 미상**이다 — 축의 정체만 확정됐다.
- **좌표**: `backend/app/services/naver_sa_ad_fetcher.py:1104~1131`(컬럼 지도 주석 + `ST_COL_*` 상수). **리포트는 이미 매일 받고 있다** — 파싱에서 버릴 뿐이다.
- ★**별도 테이블이 필요하다**(같은 주석이 이유를 적어 뒀다): 기존 `naver_search_term_daily`에 축을 더하면 **행이 수십 배**가 되고, 성적표·SS레인·후보생성 등 **여러 소비자가 읽는 테이블의 의미가 바뀐다.**
- 코드 + **마이그레이션** + 배포 + 180일 백필.

### 2️⃣ ③ 매체 블랙리스트(A5/A6) — **가장 싸다: 추가 API 콜 0**
- `naver_sa_writer.py`가 **매 쇼핑제외 쓰기마다** `fetcher._get("/ncc/targets", {"ownerId": adgroup_id})`를 이미 호출한다(**`:237`과 `:310` 두 곳**). 응답에 `MEDIA_TARGET`·`PC_MOBILE_TARGET`이 들어 있는데 **`targetTp` 필터로 즉석 폐기**한다.
- 즉 **저장만 하면 열린다.** 교훈 #312(「신호가 없다고 판단하기 전에 이미 받아 놓은 자료를 먼저 뒤져라」)의 재현이다.
- ⚠️단 이 경로는 **쇼핑제외 쓰기 때만** 돈다 — 상시 커버리지를 원하면 별도 스윕이 필요하다. **그 판단을 먼저 할 것.**

### 3️⃣ ② CRITERION 365일 백필
- **소급이 되니 ①③보다 덜 급하지만 가치는 크다** — 연령·성별(#4 S3)과 관심사(#9 A1)가 여기서 열리고, 둘 다 **1년 소급이 실증**됐다.
- 엔드포인트 **`/ncc/criterion/{ownerId}`**(adgroup grain, D-NAO-196에서 **코드 호출 0건** 확인). 상세는 `docs/references/data/75_api_surface_census/ADS_CENSUS_20260818.md`.
- ⚠️**꺼 놓은 행은 행 0**이라 365일 소급이 완전하지는 않다(D-NAO-186 원문).
- 코드 + **마이그레이션** + 배포 + 백필.

### 4️⃣ ⑤ 커머스 75건 개봉 → 그 결과로 ④ C10
- ★**순서가 중요하다. ⑤ 없이 ④를 하면 안 된다.** C10 라벨 자체가 `[부분확인]`이다 — 커머스 **116건 중 개별 문서를 연 것이 41건(35.3%)**뿐이고 공백이 **상품 도메인(64 중 54 미확인)**에 몰려 있다.
- ★그리고 **새로 연 4건에서 라벨 어긋남이 4건** 나왔다(N배송). **안 열면 「상품메타가 정말 층화에 쓸 만한가」조차 못 믿는다.**
- 1차 출처: `https://apicenter.commerce.naver.com/llms/llms.txt` + endpoint별 개별 `.md`.
- C10 = `products/search` + 정적 62종(매트릭스 `1-B` C10 행). 정적 속성(카테고리 등)이 **등급의 층화 후보**다.

## 1-3. 합격기준을 쓸 때 (지난 세션의 실패를 반복하지 않도록)
- **①~⑤ 각각** ⓐprod 테이블에 **라이브 행**(행수·창 병기, **0행이면 미달**) ⓑ**등급 교차 산출물 + 재현 SQL** ⓒ매트릭스 반영.
- ⑤는 「몇 건을 열었나 / 라벨이 바뀐 것은 몇 건인가」로 잰다.
- ★**「전수 조사 완료」라고 쓰지 않는다** — 커버리지 자백 절을 반드시 둔다(§3-4).
- ★완료 QA에 **「합격기준이 지시를 덮는가」를 먼저 판정**하게 한다.

---

# 2. 착수 전 5분 — 이걸 안 읽으면 오늘 겪은 걸 다시 겪는다

## 2-1. 환경·명령
- **로컬**: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 메인, **main**)
- **prod 조회**(읽기 전용):
  `ssh -o BatchMode=yes sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db" < 로컬.sql`
  ★**인라인 heredoc은 따옴표가 벗겨져 SQL이 깨진다 — 반드시 로컬 `.sql` 파일 + stdin.**
- ★**prod에서 배포된 코드를 직접 실행**하려면 venv: `/home/ubuntu/ohisell/backend/.venv/bin/python`
  (시스템 `python3`엔 sqlalchemy가 없다 — 이 세션에서 한 번 걸렸다)
- **배포**: `scripts/safe_deploy.sh <파일…> [--migrate] [--restart]`
- **번호**: `./scripts/next_ids.sh` — ★**결정하면 즉시 트랙에 쓴다.** 안 쓰면 도구가 같은 번호를 재발급한다(D-NAO-194가 실제로 그랬다).
- **테스트**: `cd backend && python3 -m pytest tests/ -q -p no:randomly` (약 3분)
- ⚠️ GitHub Actions 결제 정지로 **CI 미가동** — 빨강도 초록도 코드 신호가 아니다.

## 2-2. ★마이그레이션 순서 — 어기면 ingest가 통째로 침묵한다
①②는 **스키마 변경**이다. 이 앱은 **부팅 시 인프로세스 마이그레이션을 하지 않는다.**
`models.py`를 마이그레이션보다 먼저 배포하면 nullable 컬럼 추가라도 ORM이 엔티티를 통째로 SELECT 하다 `OperationalError: no such column` → **그 테이블 ingest 경로가 통째로 침묵**한다.
→ `safe_deploy.sh <마이그파일> <코드파일…> --migrate --restart`. 스크립트가 ①마이그 선배포 ②원격 `alembic upgrade head` ③코드 ④재시작 순서를 **구조로 강제**한다(이 세션에서 정상 작동 확인).

## 2-3. ★이번 세션이 실제로 밟은 함정 6개
1. **`__backfill__` 센티널** — 공용 필터가 **없다**(약 40개 파일이 각자 인라인 배제). **새 집계 SQL마다 다시 적어라.** 잊으면 **에러 없이 조용히 틀린다**(08-18 하루 2회 발생, 비용 점유율 60.0% vs 정답 96.7%).
2. **인계 숫자는 실측 전엔 믿지 마라** — 이 세션에서 인계·문서 주장 **4건이 정정**됐다(미푸시 커밋 수 · `hourly_pattern` 「죽음」 · band가 DB에 있다는 전제 · ref 71 줄번호).
3. ★**위임문의 「배경 사실」도 검증 대상이다**(교훈 #314) — 하루에 **3회** 미검증 숫자를 「이미 확정된 사실」 절에 실어 보냈다. **출처 좌표를 붙이고, 못 붙이면 「내 요약 — 검증 대상」으로 라벨을 바꿔라.** 하위 기 프롬프트에 「새 사실 창작 금지·추정 금지」를 넣어라 — 세 번 다 그게 잡았다.
4. ★**「크론이 있다」 ≠ 「그 결정이 실행됐다」** — D-NAO-186 ①은 크론이 있었지만 대상(저클릭만)·저장(덮어쓰기)이 승인 의도와 반대였다.
5. ★**규율은 주 경로에만 붙고 「지름길 분기」는 검사를 안 받는다**(교훈 #313) — 커밋 메시지에 「벽시계로 읽지 마라」고 써 놓고 같은 함수의 지름길 분기에서 그걸 했다. **역변이**(옳은 수정을 넣어도 테스트가 통과하는가)로 잡았다.
6. **`entity_type='adgroup'` 행은 `adgroup_id`가 전건 빈 문자열**(`naver_keyword_hourly` 153,694건) — grain 키는 `entity_id`다.

## 2-4. 착수 시 확인 명령 (복붙용)
```bash
git branch --show-current && git status --short && git rev-list --count origin/main..main
./scripts/next_ids.sh
ls -t docs/references/*audit* | head -1   # 주기 구조 감사 7일 경과 여부(전역 §4)
```

---

# 3. 확정된 결정사항 (번복 금지)

1. ★**D-NAO-197 = 완료 정의**(§1-1). 범위 5개, 범위 밖도 명시.
2. ★**「13:27 상품메타+광고3축만」은 범위 상한이 아니다** — 그건 **모델의 실패 보고(「10축을 안 가져왔다」) 안에서 Jino가 한 분류**였지 목표 재정의가 아니다(Jino 15:29 확인). 목표는 원문 그대로 「모든 데이터」다.
3. ★**매트릭스의 분모는 3층이다** — 표면(층1) ⊃ 축(층2) ⊃ 교차완료(층3). **1-C의 36은 층2**이고 Jino 원문의 분모는 **층1**이다. **층1→층2 낙차가 층2→층3 낙차보다 크다.**
4. ★**「전수 조사 완료」라고 쓰지 않는다** — 광고는 Guides 절(리포트 컬럼 스키마 prose)을 못 열었고, 커머스는 64.7%가 인덱스 설명문 근거다.
5. **검색량 기준선은 기존 `keyword_volume_sync`와 별개로 둔다** — 대상(돈이 닿은 vs 저클릭)·저장(시계열 vs 덮어쓰기)·목적이 반대다. **합치지 마라.**
6. ★**그 기준선은 원리적으로 파워링크만 본다**(§6-2).
7. **잘림은 비용 내림차순으로 남기고 반드시 로그**(`truncated`). 상한은 없애지 않는다(원장 이상 시 콜 폭주 방어).
8. **승계**: C3 반품 제외(Jino 13:33) · A2 요일·시간 제외 · 「시간대→CPC 상향」 **불성립**(끄는 판정만 성립) · **fan-out 배분 안 함**(밴드 내 dedupe, 밴드 간 합산 금지) · 커머스 숫자엔 **상한 프록시 라벨** · 전환 금액이 hh24에 없어 **CPA만 성립**.

---

# 4. 이번 세션이 한 것

## 4-1. D-NAO-195 (오후) — 시간대 축 + A3 브랜드어 사전
- **시간 해상도 층 신설·배포**: `today_proxy_revenue.revenue_by_hour`·`last_complete_hour`·`HourlyRevenue`. 라이브 검산 등식 성립(`total == 일단위 프록시`).
- **168칸 CPA 4주 창 확보** — 원료를 `naver_keyword_hourly`로 바꿔서. 겹치는 45,674행에서 `cost`·`conv_cnt` **100% 일치**로 신뢰 근거를 세웠다.
- **A3 브랜드어 사전 97토큰**(전건 출처 좌표, **추정 등재 0건**). 회색 4건은 Jino 확인 대기.
- 커밋 `974795cc`·`b0de7099`·`547afe36`·`38b8fd4d`·`ad61975f`·`5eec43c0`·`27166fe9`·`21456ffb`·`22eaea53`·`6a3f3240`·`3211e3e3`·`7db8561a`·`353818d6`

## 4-2. D-NAO-196 (저녁) — 전수성 검증 + 검색량 기준선
- **ref 75 신설**(`data/75_api_surface_census/`): 광고 endpoint **110**·리포트 **42종**·`/stats` fields **19**(코드 실사용 19/110 · **MasterReport 0/29** · **criterion 0/7**) · 커머스 **116**. **미배정 endpoint 91**, 등급 교차 가능 **26**.
- **매트릭스 개정 4·4-a·4-b**: §0-A **3층 깔때기** 신설 + 커버리지 자백 + 후보축 N1~N8 + 서술 정정(제목 「전 자료」·#19 B1·C11).
- **D-NAO-186 ① 검색량 기준선 개통·배포** — prod **1,166행** 라이브(창 1일), PC·모바일 분리 전건, **하한 sentinel 729행(62.5%)**.
- **N3·N5·N6 실계정 측정 190 GET**(쓰기 0): **N3=개통**(grain이 정확히 광고그룹) · **N5=불가(원리)** · **N6=측정 후 판정불능**.
- 커밋 `882cea9c`·`c31a1000`·`b7b2b752`·`6e93b4f5`·`fe3d46f6`·`5e528afb`·`651e63af`

## 4-3. D-NAO-197 (밤) — 완료 정의 확정
§1-1. Jino 승인. 커밋 `4f349e69`.

---

# 5. 완료 QA (판정 원문 그대로 — 미달 포함)

- **작업 목적(정본·Jino 11:38)**: *"성과로 나눈 등급에 대해서 광고, 스마트스토어에서 API로 받아올 수 있는 모든 데이터를 나누자"*
- **판정: 부분달성** (2026-08-18 17:2x KST, 별도 Sonnet·읽기 전용, **재개 후 전건 재판정**)
- **선판정(기준이 지시를 덮는가)**: **덮음**
- **달성 ①③④⑤⑥⑧** / **부분달성 ②⑦**
  - ② — 검색량 기준선만 라이브. **col7/8/9·CRITERION 테이블 0개**
  - ③ — 재개 후 **달성**. QA 자기 제한 원문: *"이전 라운드는 3건 전부가 미측정 punt였고, 이번은 7/8이 깨끗한 3값 안착 + 1/8만 진짜 측정 후 정직한 미결이라는 점에서 **질적으로 다르다**고 판단해 달성으로 매겼다 — 그러나 이건 **재량 판단이지 문언의 기계적 충족은 아니다**"*
  - ④ — 재작업 후 **달성**. QA가 조인 퍼널·밴드 집계·baseline **3종 독립 재실행해 전항목 일치**, fan-out **1.682배** 재현
  - ⑦ — 적대 리뷰 **P1=0 달성** / 테스트 **5,704 passed / 1 failed**(`test_vendor_item_axis`, 최소 08-11부터의 **기존 부채**, 신규 회귀 아님 → 문언상 미충족)
- **「안 함」 위반 0건** — ★「13:27을 범위 상한으로 썼는가」 grep **0건** · PROBE 쓰기 0건을 `naver_campaign_settings.updated_at` 전건 07-20/07-30으로 **라이브 교차검증**
- **목적 전환**: 없음

---

# 6. 알아야 할 것 · 이월 · 결정 대기

## 6-1. ★왜 「완료」가 세 번이나 잘못 보고됐나
매트릭스 36축은 **ref 68 머리말이 스스로 밝히듯 「이미 있던 값을 옮겨 조립한 것」**이지 API 표면에서 연역한 것이 아니다. 즉 **「우리가 이미 본 것」의 목록**인데 제목이 「전 자료」를 주장해 다음 독자를 속였다. 그 위에서 「몇 축 완료」를 세면 **분모가 이미 틀렸다.**
→ 개정 4가 3층 깔때기로 층위를 갈랐고, **D-NAO-197이 분모를 못 박았다.**
→ 세 번 다 잡은 것은 **Jino의 재점검**이지 모델의 절차가 아니었다.

## 6-2. ★검색량 기준선이 쇼핑을 원리적으로 못 잰다 (새 제약)
`naver_ad_daily.keyword_id`가 SHOPPING 캠페인 **66,695행 전건에서 빈 문자열**(QA가 100.0% 직접 확인)이고 `naver_entity(entity_type='keyword')`는 **전량 WEB_SITE**다. 쇼핑검색광고는 등록 키워드가 아니라 상품 기반이라 구조상 그렇다.
⇒ **밴드 비용의 92.5%인 쇼핑의 수요를 이 기준선이 못 본다.**
**해결 경로**: keywordstool은 **임의 텍스트 조회가 되므로** `naver_search_term_daily.search_term`을 시드로 쓰면 쇼핑도 잴 수 있다(**범위 밖 — 다음 계약감**).

## 6-3. Jino 결정 대기 (2건 — 둘 다 지금 안 정해도 진행에 지장 없다)
| # | 항목 | 배경 |
|---|---|---|
| 1 | **A3 회색 토큰 4건** | 「버디」 398,874원(갤럭시 버디4/5 vs 골프 birdie — 골프필름 그룹 `grp-a001-01-000000043935093`의 「버디필름」이 증거) · 일미리케이스 17,779 · A73 2,847 · 소다케이스 2,372. **총 영향 0.54%**. 데이터로 못 가른다. **안 넣는 게 기본값**이라 방치해도 무해 |
| 2 | `optimizer` 해제 범위 · 대행사 제외 칸 소유권 · ref 65 §10 나머지 | 기존 미결 — **집행에 걸려 별도 계약감** |

## 6-4. 이월 (다음 세션이 밟을 수 있는 것)
- **적대 리뷰 P2 5건**(검색량 기준선): ★`_parse_qc`의 `'1,234'→0`·`'abc'→0` 상속 결함이 **소급 불가 시계열에선 영구 오측**이 된다(가장 무겁다) · lookback 경계 미잠금 · 모델 `UniqueConstraint` 미잠금 · `entity_type` 필터 미잠금 · `is_below_threshold`가 PC·모바일 미분리
- **`/estimate` 적재 미구현** — D-NAO-186 승인분인데 안 했다. 붙으면 콜 예산(약 2,200콜) **재검산 필요**(기준선이 이미 최대 800콜)
- **`channelProductId ≡ mall_product_id`** `[미상]` — 확정되면 C11(N배송 SKU)이 등급에 닿는다
- **D-NAO-195분**: 2026-07-14~17 4일이 **비용 2,216,906원·전환 0건·계정 전체**(원인 미상 — 38일판 CPA가 전부 조건부다) · `expkeyword` 391일 전환 4종 0의 원인 · WEB_SITE 이중 grain의 정본(`models.py:2468-2469` docstring과 데이터가 어긋남)
- **`__backfill__` 공용 필터층 부재** · **`test_vendor_item_axis` 기존 실패 1건**(다른 트랙)

---

# 7. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_api-surface-census+volume-baseline_20260818.md 읽고 이어서 작업해줘.
§1(다음 세션이 할 일)과 §2(착수 전 5분)를 먼저 볼 것.

★목표는 §1-0의 Jino 11:38 원문을 그대로 앵커 `목표:`에 복사 — 새로 발명 금지.
★완료 정의는 D-NAO-197로 확정됐다(§1-1). 범위 5개, 현재 0/5.
  「몇 % 됐나」엔 반드시 어느 층의 분모인지 병기할 것(층2 기준).

착수 순서는 §1-2대로 — 시한과 비용이 정한다:
  1) col7/8/9 180일 백필   ★재생성 한도 정확히 180일, 매일 앞이 사라진다. 별도 테이블 필요.
  2) 매체 블랙리스트(A5/A6) ★추가 API 콜 0 — naver_sa_writer.py:237·310이 이미 받아서 버린다.
  3) CRITERION 365일 백필   /ncc/criterion/{ownerId}, 코드 호출 0건.
  4) 커머스 75건 개봉 → 그 결과로 C10 상품메타.  ★순서 지킬 것 — ⑤ 없이 ④ 하지 말 것.

★1)3)은 마이그레이션 포함 → safe_deploy.sh --migrate 순서 강제(§2-2).
  이 앱은 부팅 시 인프로세스 마이그레이션을 안 한다 — 어기면 ingest가 통째로 침묵한다.
★새 집계 SQL마다 `__backfill__` 배제를 다시 적을 것(공용 필터 없음).
★인계 숫자는 실측 전엔 믿지 말 것 — 이 세션도 4건이 정정됐다.
★위임문의 「배경 사실」에도 출처 좌표를 붙일 것(교훈 #314).
★Jino 결정 대기 2건은 §6-3 — 둘 다 지금 안 정해도 진행에 지장 없다.
```
