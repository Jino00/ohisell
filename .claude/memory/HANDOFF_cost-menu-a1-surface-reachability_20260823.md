# HANDOFF — A1의 «표면»을 사람이 쓸 수 있게 (엑셀 업로드 UI + 제품·옵션 필터)

2026-08-23 (KST) · 세션 `5432a577` · 체인 **「SellC 원가 메뉴」 n=4**
워크트리 `/Users/jino/.claude-worktrees/ohiselling/cost-menu-s3` (브랜치 `feat/cost-menu-s3`)
**PR #366 `b91dc21f` · PR #374 `9cf6a366` · PR #375 `0ab864a2`** → `origin/main`

---

## 0. 목표 — 다시 쓰지 마라

★**이 트랙의 목표에는 이름이 있다 — 「SellC 원가 메뉴 목표」.**
**정본은 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md`다.** 목표를 다시 쓰지 말고 그 파일을 열어
앵커 `목표:`에 원문을 옮긴다. 트랙 헤더와 갈라지면 **카드가 이긴다.**

> ***"SellC에 원가 메뉴를 하나 붙이자. 여기에는 우리가 현재 알고있는 모든 원가가 있잖아? 그 원가를
> 가지고 계산되는 방법이 첨부된 파일처럼 나오고 그 계산이 되면 되지 않을까? 그리고 수입건에 대해서는
> 두번째, 세번째 첨부자료처럼 수입한 금액을 알려주면 너가 이런것들을 모아서 회계적으로 계산되는
> 원가를 만들면 되잖아. 그리고 그런 원가를 만들려면 재고사항이 필요할꺼고, 그건 ecount와 연결하면
> 될꺼고."*** / ***"목표는 정확한 원가 산출이니까?"*** (Jino 2026-08-22)

---

## 0-A. ★운영 원칙 — Jino 지시 (2026-08-23 13:00, n=2→n=3→n=4 승계)

> ***"다음 세션에서부터는 SellC 원가 메뉴 목표를 달성하는 위주로 진행되게 목표잡기와,
> 다른 트랙의 일을 집어와서 여기에 섞지 말라"***

**① 목표잡기는 역산이다** — 착수 질문은 「무엇을 할까」가 아니라 **「합격 6항목 중 어느 것을
[x]로 만드는가」**다. 앵커 `이번 세션:`에 겨냥 항목을 적는다.

**② 다른 트랙 일을 끌고 오지 않는다** — 목표 카드의 「목표의 «옆면»」 표가 경계다. 발견해도
**고치지 말고 적기만** 한다(앵커 `## 이월`에 소관 지목). **같은 트랙 안의 계약 B도 남의 일이다.**

---

## 1. 30초 요약 — 이번 세션이 무엇을 바꿨나

**A1은 열리지 않았다. 진행률 2/6 불변.** 그런데 **A1까지 가는 길이 세 번 막혀 있었고 세 번 다 뚫었다.**

| 무엇이 막고 있었나 | 어떻게 뚫었나 |
|---|---|
| 엑셀을 올릴 방법이 화면에서 안 보였다 (벌거벗은 `<input type="file">` 2개) | 카드형 드롭존 — PR #366 |
| 올리고 나니 **레시피 100건·SKU 924행에서 목표를 못 찾았다** | 제품 → 옵션 2단 필터 — PR #374 |
| 그 필터가 **레시피 탭에서 옆 패널을 덮었다** | 고정폭 → 접히는 폭 — PR #375 |

★**세 번 다 「값은 맞는데 사람이 못 본다/못 찾는다」였다.** 계산 파이프라인은 S2에서 이미 prod에
있었고 지금도 정확하다 — 사람이 거기 **닿지** 못했을 뿐이다. **이 화면의 합격은 계산이 아니라 도달이다.**

### ★그리고 Jino가 엑셀을 올렸다 — 파싱이 실측과 맞았다

prod: `cost_recipe` **100건** · `cost_recipe_line` 156줄 · `cost_material` 129종 · `cost_recipe_link` **924건**

목표 레시피 **`id=68`**(「오하이 빛반사, 지문방지 매트 필름 3매」 × `bar`) = 구성 **9줄** · SKU **107건**
— 인계 예측(부자재 9종 · SKU 106건)과 일치하고 **라인 실측이 엑셀 검산과 정확히 같다**:

```
필름 600×3=1800 + 부착안내문 30 + 부자재(밀대외) 22 + 알콜솜 2EA 60
+ 비닐(9*18) 8 + 비닐(12*22+4) 13 + 패키지 98 + 폼텍 스티커 6 + 부착 지그 100
= ex 2,137  ⇒  ×1.1 = inc 2,350.70
```

부자재 9종 전부 `unconfirmed` · 단가 행 **0건** = **설계대로**(엑셀 참고값은 `excel_ref_price`에만
있고 계산 경로에 원리적으로 안 닿는다). 승인 레시피 **0/100**.

구성 0줄 **82건**은 매입 완제품(케이스·애플워치 스트랩)·강화유리·태블릿 = 원가표에서 열이 부자재가
아니라 «계수»인 섹션 → **S3 몫**(인계 예고와 일치).

---

## 2. ★다음 세션이 «가장 먼저» 할 일 — A1 라이브 관측 (n=3에서 승계, 여전히 열림)

`https://sellc.ohitech.co.kr/cost` → **레시피 탭**

1. 필터 바 검색칸에 **「빛반사」** → 제품 **「…매트 필름 3매」** → 폼팩터 **`bar`**
   (이제 100건 중 1건으로 좁혀진다 — 이번 세션이 만든 것이 이 조작이다)
2. 레시피 클릭 → 오른쪽에 구성 9줄 확인 → **「이 구성을 승인한다」**
3. **「엑셀 참고값을 단가로 채택」**
4. **표준원가 보드** 탭 → 같은 제품 필터 → **2,350.70원**이 여러 SKU에 뜨는지

관측되면 → 트랙 `- [ ] A1` → `- [x]`(증거 좌표 병기) → **진행률 3/6**.
안 되면 → 무엇이 어떻게 다른지 그대로 적는다. **기준을 낮추지 않는다.**

★**승인 클릭은 계약 §2-2가 사람 몫으로 못 박은 자리다.** 모델이 대신 누르면 판정하러 간 기가
증거를 만드는 것이 된다(교훈 #274). 이번 세션도 누르지 않았다.

---

## 3. 무엇을 만들었나

### 3-1. 엑셀 업로드 = 카드형 드롭존 (PR #366)

Jino: *"파일을 그냥 드롭하면 되나?"* → *"이 부분을 좀 더 선택이 쉽게 직관적으로 바꾸자."*

- 카드 전체가 클릭·드래그·키보드(Enter/Space) 대상
- 선택 **전**에 기대 시트 이름(「제품 원가표」·「원가 매핑」)·예시 파일명을 화면이 말한다
- 선택 **후** 파일명·크기 + 「바꾸기」/「지우기」
- `.xlsx`가 아니면 그 자리에서 거부 + 사유에 **할 일** 병기 (서버 400까지 안 간다)
- 「초안 만들기」 비활성 **이유**를 문장으로
- 패널 밖 드롭은 `preventDefault`로 조용히 무시

### 3-2. 제품 → 옵션 2단 필터 (PR #374)

Jino: *"제품명, 옵션명을 불러올 수 있게 드롭버튼을 … 제품만 선택하면 제품에 속하는 옵션들이 쭉
나오기도 하고 옵션까지 선택하면 딱 그 제품만 나오고. 지금 「오하이 빛반사, 지문방지 매트 필름 3매」
제품을 찾는게 쉽지 않네"*

공용 `ProductOptionPicker`: 제품 검색칸 + 제품 셀렉트(건수 병기) + 2단 셀렉트 + 초기화
**보드 = 제품 → 옵션(SKU)** · **레시피 = 제품 → 폼팩터**(레시피엔 옵션 개념이 없다)

★필터가 걸려도 **「N건 중 M건 표시 중 — 필터: …」**를 반드시 말한다. 「미승인·미계산 SKU도 빠짐없이
싣는다」가 계약 조항이고, 지금 **924건 중 924건이 미계산**이라 조용한 필터는 곧 커버리지 착시다.

### 3-3. 레이아웃 수정 (PR #375)

Jino: *"레시피탭은 왜 화면 구성이 안맞지?"* / *"칸이 옆으로 나오고 그러잖아?"*

필터 바가 **넓은 보드 탭과 320px 레시피 탭 둘 다**에 놓이는데 초판이 보드 폭만 보고 `w-56`·
`min-w-[16rem]` 고정폭을 박았다. 그리드/플렉스 자식의 기본 `min-width: auto`가 축소를 막아
**컨트롤이 왼쪽 칸을 뚫고 오른쪽 패널을 덮었다.** → `w-full min-w-0` + `flex-1 basis-*` + `min-w-0`.

백엔드·API는 **세 PR 모두 무변경**이다.

---

## 4. ★이번 슬라이스의 교훈 — 같은 병이 여덟 번

| # | 무엇 | 누가 잡았나 |
|---|---|---|
| 1 | 위임 산출물의 테스트가 **드롭 경로를 한 줄도 안 지킴** (핸들러 절단해도 13건 초록) | 리드 변이 |
| 2 | 카드가 Enter/Space를 삼켜 **「지우기」가 키보드로 영영 안 됨** | 적대 리뷰 1R |
| 3 | **리드가 쓴 P1 회귀 테스트가 허수** — `fireEvent.click`을 뒤에 붙여 결함을 덮음 | 리드 변이 |
| 4 | 옵션 셀렉트가 제품에 종속되지 않아도 초록 (드롭다운 «자신의 내용»을 안 잼) | 위임 자체 발견 |
| 5·6 | 제품 변경 시 옵션 리셋이 **무방비** (보드·레시피 각각) | 적대 리뷰 1R |
| 7 | **0건 안내 렌더 분기를 통째로 지워도 초록** — 기존 테스트가 0건 경로를 «일부러 피해감» | 적대 리뷰 1R |
| 8 | 필터 바가 **옆 패널을 덮음** (테스트 736건 전부 초록) | **Jino의 눈** |

★**#8은 jsdom이 원리적으로 못 잡는다** — 레이아웃을 계산하지 않으므로 「겹쳤는가」를 잴 수 없다.
그래서 가드를 「겹침」이 아니라 **«그 원인이 된 클래스가 돌아오는가»**로 세웠다(약한 가드지만
아무것도 안 지키는 것보다 낫고, 다음 사람에게 「여기 고정폭을 박으면 안 된다」를 전달한다).

★**#3이 특히 중요하다 — 단언의 «자리»가 결함을 덮는다.** 뒤에 붙인 조작(`fireEvent.click`)이
파일을 지워 버려서, 결함이 있어도 초록이었다. jsdom은 keydown 뒤 네이티브 버튼 활성화를
대신해 주지 않으므로 「브라우저가 그 활성화를 **할 수 있는 상태인가**」(부모가 `preventDefault`로
죽이지 않았는가)를 직접 재도록 다시 썼다.

★**레이아웃 가드 자체도 두 번 틀렸다** — `\bw-\d`가 `min-w-0`의 `w-0`을 잡았고(클래스 경계는
공백이다), 테스트를 헬퍼가 없는 `describe`에 넣었다. 둘 다 재주입 변이로 잡았다.

---

## 5. 적대 리뷰 — 2회, 각각 1R FAIL → 2R PASS

### PR #366 (드롭존)
**1R P1**: 카드가 `role="button"`이라 Enter/Space를 **무조건** 삼켜, 중첩된 「지우기」가 키보드로
영영 작동하지 않고 대신 파일 선택창이 열렸다. **「바꾸기」는 목적이 우연히 같아 증상이 안 보였고 —
그 우연이 결함 절반을 가렸다.** 고침: `e.target !== e.currentTarget`이면 카드가 키를 안 받는다.
**P2 채택 2**(고른 파일 위에 잘못된 파일 → 앞 선택이 «경고 없이» 사라지던 것 / 여러 파일 드롭 시
나머지를 조용히 버리던 것). 2R 변이 4/4 KILLED.

### PR #374 (필터)
**1R P1 2건** — 둘 다 **라이브 결함이 아니라 무방비**였다(위 #5·#6).
**P2 채택 4**(0건 안내 미검증 / 「전체 (N건)」 숫자가 거짓말 가능 / `form_factor: null` 픽스처 부재로
sentinel 경로 전량 미검증 / **코드 수정** — 검색어가 선택된 제품을 밀어내면 `<select>`가 빈 것처럼
보이던 «유령 선택» → 핀 고정).
2R 변이 **6/7 KILLED**. **M7(핀 가드 완화)은 SURVIVED** — 라이브 결함은 아니지만
「핀 고정된 제품이 중복 렌더되지 않는다」는 속성이 안 지켜진다 → **이월**.

### PR #375 (레이아웃)
⚠️ **적대 리뷰 생략** — CSS 클래스 교체 + 가드 테스트 1건이고, 이 브랜치의 기능 변경분은
#366·#374에서 이미 리뷰를 받았다. PR 본문에 자백했다.

---

## 6. 완료 QA — 판정 원문 그대로

> ⚠️ **판정 «미수신»이다.** Jino 지시(2026-08-23 20:58 *"이번 handoff는 QA판정 기다리지 말고 바로 해"*)로
> 판정기(별도 Sonnet·읽기 전용)를 띄운 채 인계를 닫았다. **판정 결과는 이 파일에 없다.**
>
> ★**이것을 「달성」으로 읽지 마라.** 판정이 없는 것은 판정이 좋은 것과 다르다.
> 다음 세션은 아래를 **직접 다시 돌려서** 판정을 받고, 그 원문을 이 자리에 채워라.

**판정: 판정불능(미수신) — 판정기를 띄웠으나 결과 도착 전에 인계를 닫았다 (2026-08-23 20:58 KST)**

### 다음 세션이 판정을 받는 법
- 대조 대상은 **셋**이고 **각각 따로** 판정한다(하나가 나머지를 덮으면 「달성」만 남고 무엇이
  안 움직였는지가 기록에서 사라진다):
  ① **계약 A′ §7 합격기준** — `docs/PLAN_cost-menu-standard-cost.md`
  ② **Jino 지시 원문** — §0 목표 원문 + §0-A 운영 지시 + 이번 세션 지시 3건(§3-1·3-2·3-3 인용)
  ③ **트랙 궁극 목표** — `.claude/memory/chains/sellc-원가-메뉴.GOAL.md` 합격 6항목
- 판정기는 **읽기 전용**이다. 특히 **화면에서 「승인」·「채택」을 대신 누르지 마라** — 그게 이
  판정의 핵심 미달 항목이고, 누르면 판정이 무의미해진다(교훈 #274).
- 산출물 좌표·라이브 증거 명령은 §8에 다 있다. 기준선: `product_master` 963행 · SUM 3642052.1

### 판정 전에도 확실한 사실 (판정 «대신»이 아니라 재료다)
- **진행률 2/6 불변** — A1 체크박스는 이번 세션도 열리지 않았다(승인 클릭 대기)
- prod `cost_recipe`=100 · `cost_recipe_link`=924 · **승인 0/100** · 부자재 단가 행 0건
- 무해성: `product_master` 963행 · SUM 3642052.1 — 3회 배포 전후 불변
- 적대 리뷰 2회 전부 1R FAIL → 2R PASS · 변이 SURVIVED 1종(M7, §9 이월)

---

## 7. 트랙 진행률

- 트랙: `docs/tracks/active/track_cost-truth-ledger.md`
- **시작 2/6 → 종료 2/6 (불변)**
- 달성 유지: **B1**(원장 스키마+3중 검산) · **B2**(8/18 실건 라이브)
- 미달: **A1**(구성 계산 결과가 prod 관측 — **승인 클릭 대기**) · A2(S3) · C1 · C2
- 이번 세션이 움직인 것: **체크박스 0개.** 대신 **A1까지 가는 경로 3건을 뚫었고**, Jino가 실제로
  엑셀을 올려 **레시피 100건·SKU 924건이 prod에 실렸다**(승인 전 단계까지 완주)
  — 증거: PR #366 `b91dc21f` · #374 `9cf6a366` · #375 `0ab864a2` · prod `cost_recipe`=100
- 트랙 목표 원문: §0과 동일
- 다음 세션 후보: **§2(A1 관측)** → 그 다음 S3

---

## 8. 산출물 좌표

- 커밋 흐름: `0c8c88b8`(드롭존) → `46cd396d`(리뷰 P1·P2) → `3c7733a9`(트랙) → **PR #366 `b91dc21f`**
  → `bd49ad2b`(필터) → `ae4ca263`(리뷰 P1 2건·P2 4건) → `5d971ab3`(트랙) → **PR #374 `9cf6a366`**
  → `9b2414c7`(레이아웃) → `974d435a`(병합) → **PR #375 `0ab864a2`**
- **prod 배포 3회** — 전부 `safe_deploy.sh --frontend` CAS 통과.
  마지막 빌드 스탬프 `9b2414c7b744 dirty=0`
- ⚠️ **세 PR 모두 `safe_merge.sh --force`.** 매번 **가정이 아니라 실측으로** 결제 정지 서명을 확인했다:
  3잡 전부 `steps: 0` · 2~5초 종료 · 로그 blob **404(BlobNotFound)**. 자백 로그 `$TMPDIR/safe_merge.log`
- ★**배포 전 뒤처짐이 세 번 다 잡혔다**: **29커밋 → 32커밋 → 1커밋.** 프론트는 `dist`를 통짜 rsync
  하므로 병합 없이 올리면 **그 창에 배포한 세션들 작업이 prod에서 사라진다**(교훈 #350). 매번 병합
  후 재빌드했고 충돌 0건.
- 라이브 문자열: prod 번들에 「…명으로 찾기」·「먼저」·「해당 조건에 맞는」 실재
- **무해성**: `product_master` **963행 · SUM 3642052.1** — S1·S2 기준선과 동일 (3회 배포 전후 불변)
- 테스트: 프론트 **736 passed / 58 files**(다른 세션 신규 포함) · `tsc -b` 통과 · 백엔드 무변경

---

## 9. 이월 (고치지 않고 운반 — 소관 지목)

**이번 세션이 새로 만든 것**
- **M7**: 「핀 고정된 제품이 검색 결과와 중복 렌더되지 않는다」가 테스트로 안 지켜진다(핀 가드
  `!base.some(...)` 완화 변이 SURVIVED). 코드는 맞고 커버리지만 비었다. 소관: 이 계약(다음 슬라이스)
- **P2 이월 3건**(리뷰 1R): 드래그 플리커(`onDragLeave`가 `relatedTarget` 미확인) · 정적
  `aria-label`이 상태 변화를 안 알림 · `role="button"` 안에 `<button>` 중첩 패턴 자체.
  **셋 다 실브라우저·AT 확인이 선행**이라 jsdom에서 판정 불가. 소관: 이 계약
- **레이아웃은 자동 테스트가 원리적으로 못 잡는다** — 이번엔 Jino 눈이 잡았다. 화면 폭이 다른
  두 곳에 같은 컴포넌트를 놓는 변경은 **라이브를 눈으로 봐야 한다.** 소관: 이 계약(작업 방식)

**n=3에서 승계 (여전히 유효)**
- `scripts/safe_deploy.sh`가 **prod에 없는 신규 디렉터리를 만들지 않는다** → 소관: 배포 스크립트
- `track-progress-sync.sh`·`scope-anchor.sh`가 **공유 메인 폴더**를 읽어 워크트리 기록을 못 본다
  → 소관: 하네스(전역 훅). ★이번 세션 내내 울었고 **전부 오탐**이었다(실측: `origin/main`엔
  확인줄 3건, 공유 메인 폴더 사본엔 0건 · 그 사본이 **107커밋** 뒤). 훅을 달래려 줄을 더 쓰지 않았다.
- `scripts/next_ids.sh`가 **D-CPP를 발급하지 않는다** → 소관: 스크립트(별건)
- **P2-2**: `board()`가 레시피마다 재계산 — S3에서 재검토
- **계약 B 소관**(고치지 않음): 수입건 id=1 관세 `is_duty` 미표시 · 자금정산서 파서 미구현 · 13개월 소급 적재
- **계약 §9 이월**: 전역 `PRAGMA foreign_keys` 미설정 · 단가 없는 라인 거부 무테스트 · `ex_vat`만 입력 시 「최신 단가」가 「—」
- AI_office 수입원가 트랙 중복 → **Jino 결정 대기**
- ★훅 오탐 1건 기록(계약 §6 계수 대상): `review-surface-mutation.sh`가 **수정 위임**(리뷰 위임이
  아님)에서 발동했다. 표면 변이 문단을 실어 다시 보내니 통과. 소관: 하네스

---

## 10. ★Jino 답 대기 1건 — S3의 정밀도를 정한다 (n=2→n=3→n=4 승계, 여전히 열림)

상품명 하나에 옵션이 최대 **163종**(갤럭시Z 56 · 아이폰 45 · 갤럭시S 34 · 갤럭시A 17 · LG V40).
**부품 «종류»는 같아도 필름 «면적»은 기종마다 다르다.** 계약은 **폼팩터 9종**으로 가르기로 했으나
**같은 폼팩터 안의 기종 차이**는 열려 있다(D-CPP-27의 「공통 2,350.7 vs 기종별 2,616」 재출현).

※ 답이 없어도 S2는 «상품명당 원가 1개»로 이미 갔고, 답은 **A2(S3)의 정밀도**를 정한다.
※ 이번 세션 보드 실측이 이 질문을 눈에 보이게 만들었다 — 「…3매」 한 제품에 **163 SKU**가 달려
있고 전부 `cost_price` 2,350.7이다.

---

## 11. 못 한 것

- **A1 관측** — 승인·채택은 사람 몫이라 누르지 않았다(§2). 이번 세션도 열지 못했다.
- **브라우저 화면을 모델이 직접 못 봤다** — BasicAuth 자격증명 미시도. prod 번들 문자열·DB로만
  확인했다. ★그 공백이 **레이아웃 결함이 라이브에 나간 이유**다(§4 #8).
- S3(수입 완제품 파생 + 폼팩터 갈림 계열 이식) — 계약상 이번 슬라이스 밖.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
