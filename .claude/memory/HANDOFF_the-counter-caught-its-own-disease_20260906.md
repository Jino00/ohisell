# HANDOFF — 계수기가 자기 병을 잡았다 · 좌표를 잃은 인용이 한 세션에 세 번 (2026-09-06)

> 체인 **`pao-진동차단` n=5** · 세션 `531204ca` · 결정 **D-NAO-293**(292에서 재번호) · 교훈 **#400**
> 계약 `docs/contracts/CONTRACT_oscillation_damping.md`(D-NAO-288) §4-C ⓗ·ⓘ · 정본 `docs/references/138_oscillation_daycount_and_grain_20260906.md`
> 트랙 `docs/tracks/active/track_naver-ad-optimization.md` · **진행률 2/7 불변**
> 적대 리뷰 **1R FAIL(P1 2건) → 2R PASS(P1=0)** · PR **#751** · CI **3/3 pass**
> **prod 배포 0건 · 마이그 0 · 네이버 API 0콜 · 판정 로직 0줄 변경** · 회귀 **7,975 passed**

## 검증 명령 (한 줄 — 받는 세션은 이걸로 인계를 검증한다)

```bash
cd <이 repo> && scp scripts/measurements/oscillation_daycount.py sellc.ohitech.co.kr:/tmp/vf.py && \
  ssh sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && python3 /tmp/vf.py --db ohisell.db --before-days 7 --after-days 7" | grep -E "양방향을 낸 소재|분모 둘" && \
  cd backend && python3 -m pytest tests/test_naver_oscillation_daycount.py tests/test_naver_latch_reason_census.py -q
```
→ 출력에 **`배포 후 구간에 양방향을 낸 소재: 0개`** 와 **`★분모 둘: …`** 이 있고 테스트 **70 passed**면 인계가 참이다.

---

## §0. 목적 (원문 — 발명하지 않는다)

n=1 HANDOFF §0의 Jino 지시문 원문:
> **엔진이 만든 CPC 상승이 엔진의 정지 근거가 된다.** 액셀(UP)은 정착창(과거) 보정ROAS로 올리고,
> 브레이크(DOWN)는 당일(현재) CPC 급등으로 내린다. … **액셀과 브레이크가 서로 다른 창을 본다 —
> 그게 진동의 원인이다.** … 어느 쪽이든 북극성 §7 「액셀·브레이크가 대칭인가」를 통과해야 한다.

**이번 세션 몫**: 계약이 «다음 세션»에 남긴 좌표 둘을 집행한다. **1주 창은 09-12에 선다** —
이번 것은 **판정이 아니라 판정 가능성**을 만드는 슬라이스다.
- §4-C ⓘ 원문: *"계수기에는 `entity_id` 필터가 없다 — 일주일 뒤에 돌려도 이 도구로는 그 전후 비교를 낼 수 없다. **다음 세션은 여기서 시작한다.**"*
- §4-C ⓗ 부기: *"**제안 규칙(채택은 다음 세션)**: 배포일은 양쪽 창에서 제외하고, 제외 사실과 그 날의 수를 항상 병기한다."*

**착수 검증**: n=4의 `검증 명령:`을 그대로 실행 — prod `--curve-source db` 출력에 `네이버 /stats 읽기 콜 0건`·`[곡선 출처] naver_keyword_hourly` 확인 + `test_naver_aveto_counterfactual.py` **37 passed** ⇒ **인계 참**.

---

## §1. ★★★이 세션의 진짜 이야기 — 진단한 병을 세 번 저질렀다

진단한 병: **«좌표를 잃은 인용»** — 다른 모집단에서 나온 두 수를 하나의 사실로 읽는 것.

| # | 어디서 | 무엇을 | 잡은 것 |
|---|---|---|---|
| ① | 계약 §0의 「27/54」 | 다른 **창**의 두 수를 한 비율로 | 나(착수 실측) — 다만 **원인 귀속을 틀렸다**(②로 이어짐) |
| ② | 내가 쓴 §1의 원인 귀속 | 「grain이 다르다」 — 실제론 **창**이 다르다 | **적대 리뷰 1R P1-2** |
| ③ | 내가 만든 계수기 출력 + 그걸 인용한 문장 3곳 | **전 소재 합계**를 한 소재의 사실로 | **적대 리뷰 1R P1-1** |
| ④ | 내가 쓴 §5-2의 「세 번 배포」 | **잘린 표본**(`tail -3`)을 전수로 | **적대 리뷰 2R 신규 #5** |

★**자기검증은 네 번 다 통과시켰다.** 막은 것은 전부 **다른 기의 재현**이다.
★**규율을 «알고 있다»가 방어가 아니다** — ②③④는 전부 그 병을 **진단하는 문서를 쓰는 도중에** 났다.
⇒ 그래서 집행을 문장이 아니라 **출력의 «모양»**으로 옮겼다(§3).

---

## §2. 착수 실측 — 「27/54」의 좌표 (prod 읽기 전용 · `--as-of '2026-09-05 12:30'`)

| 소재 필터 | 창 | 전체 | 무쓰기 |
|---|---|---|---|
| `nad-…554755092` | 09-02~09-05 | 40 | **27** |
| **(전 소재)** | 09-02~09-05 | **40** | **27** |
| `nad-…554755092` | 09-01~09-05 | 40 | 27 |
| (전 소재) | 09-01~09-05 | **54** | 35 |
| (전 소재) | **09-01 하루** | **14** | **8** |

- **살아남는 결론**: 27과 54는 **한 비율이 아니다.** 27은 창 09-02~09-05의 무쓰기, 54는 **창 09-01~09-05(하루 더 길다)**의 전체다. §0이 그 54에 「4일」이라 붙인 것이 어긋난 자리다(40+14=54 · 27+8=35).
- **철회한 결론**(1R P1-2): *"grain이 다르다"* · *"n=2의 정정은 분자에만 맞다"* — **같은 창에서 소재 필터는 no-op**이고 소재와 창이 **교락**돼 분리 불가다. 「grain이 다르다」 → **「좌표(창·소재·컷오프)가 다르다」**로 낮췄다.

---

## §3. 한 것 — D-NAO-293

**신설 `scripts/measurements/oscillation_daycount.py`**(ⓗ 계수기) — 구조로 지키는 것:
1. ★배포일은 **양쪽 창에서 제외**하되 그 날의 수와 **배포 시각 기준 «소재별» 전/후 분해**를 항상 병기
2. ★★**진행 중인 날은 분모에서 뺀다** — 「UP만 났고 DOWN은 아직」인 날을 완결된 날과 같은 분모에 넣으면 배포 후 창이 **구조적으로** 좋아 보인다
3. ★**분모를 둘 낸다**(완결된 날 / 발화가 있었던 날) — 엔진이 안 깨어난 날을 「진동 없던 날」로 세면 창을 늘릴수록 비율이 좋아진다
4. ★**「배포 후 구간에 양방향을 낸 소재: N개」를 스스로 세어 말한다** + 합계줄에 *"이 줄만으로는 「같은 소재에서」를 말할 수 없다"*(1R P1-1 수리 — 문장이 아니라 출력에 했다)
5. 조인에서 빠진 `change_log` 행을 센다(실측 0건이지만 안 세면 그 사실도 안 보인다)

**확장 `latch_reason_census.py`**(ⓘ) — `--entity-id` · `--since/--until` · `--as-of` + **셋을 항상 헤더에 인쇄**. `dry_run <> 0` 행이 있으면 자백(필터는 «더하지» 않는다 — 이미 낸 기준선의 인구를 소급으로 바꾸므로).

테스트 **38종**(신설 23 · latch D절 확장). ★sqlite 읽기 전용 가드를 **리터럴 존재가 아니라 AST 호출 검사**로(교훈 #397의 4번째 재현 방지 — 주석에 심어도 통과하던 형태).

---

## §4. 라이브 (prod 읽기 전용 · 쓰기 0 · 네이버 API 0콜)

**ⓗ**(2026-09-06 09:46:44 KST · `--before-days 7 --after-days 7`)

| 창 | 완결된 날 | 진동일 | 완결 기준 | **발화일 기준** |
|---|---|---|---|---|
| 배포 전 08-29~09-04 | 7일 | **2일**(09-03·09-04) | 28.6% | **50.0%**(발화 4일) |
| 배포 후 09-06~09-12 | **0일** | — | **판정 불가**(09-06 진행 중) | 판정 불가 |
| ★제외한 배포일 09-05 | (양쪽에서 제외) | UP3·DOWN2 | — | — |

배포일 **소재별** 분해: `554755092` 배포 전 UP3·DOWN0 / 배포 후 UP0·DOWN2 · `375625297` 배포 후 UP1 · `414877237` 배포 후 UP1
⇒ **배포 후 구간에 양방향을 낸 소재: 0개.** n=3의 관측은 갱신되지 않는다.

**ⓙ**(09:4x KST) — `naver_proposals` 배포 후 **426건** · `ops_diary_entries` **457건**, A-veto **0** · B-veto **0**.
n=4의 08:16 재관측(250·280) 대비 **분모만 커졌다** ⇒ **세 세션 연속 0건**(n=3 17:26 · n=4 08:16 · n=5 09:4x). 규약대로 **「안 걸렸다」**.

**이월 P2-2(n=4) 해소** — 닫는 관측 원문대로 D-1 `naver_ad_daily` `keyword_id=''`·`imp>0` distinct adgroup = **187·187·190·181 ≪ `_CALL_CAP` 800** ⇒ today 스윕은 이 창에서 **회전하지 않는다**. ⚠️상한이 아니라 **모집단**이 작아 성립하는 결론이다.

**신규 — 배포일 제외 규칙의 근거가 하나 더**: prod `deploy-manifest.jsonl` 실측, 09-05에 `auto_operator.py`를 포함한 배포가 **5건**이다(09:11:02 `e7924a7` `deploy/pao-n87` · 13:02:07 `7177a19` main · 14:08:42 `85bdee8` D-NAO-288 · 20:05:58 `1eb90342` `feat/pao-n89` · 21:19:56 `0b4ad0dd` D-NAO-290). 계약이 경계로 잡은 「배포 = 14:08」은 그날의 **다섯 중 하나**였고, **배포 후 창의 첫 «깨끗한» 날은 09-06**임이 확정된다. 부수로 n=4 완료 QA의 *"deploy-manifest.jsonl이 해당 경로에 없다"*도 반증(168KB 실재).

---

## §5. 적대 리뷰 — 1R FAIL(P1 2건) → 2R PASS

- **P1-1** 배포일 분해가 «전 소재 합계»인데 소재의 사실로 읽혔다 → 소재별 분해 + 양방향 소재 수 자백 + 합계 경고줄
- **P1-2** 「27=소재 / 54=전 소재」의 원인 귀속이 재현되지 않는다 → 살아남는 결론/철회하는 결론을 갈라 적음
- **P2 채택 10 · 기각 1 · 이월 3.** 채택 중 셋은 **2R 신규**였다: ①`two_way`가 「배포 후만」 본다는 것이 무테스트(회귀하면 **P1-1의 거짓 문장이 정확히 부활**) ②P2-5가 「채택」으로 적혔으나 실제로는 **주석만** 달림 ③P2-4의 새 픽스처가 배타 경계를 **실제로는 안 세웠다**(초 없는 `--as-of`와 문자열 «길이»로 빠졌다). 셋 다 변이 재주입으로 사망 확인.
- **기각 1**(P2-8): latch에 `dry_run` 필터를 «더하면» 이미 낸 ⓘ 기준선의 인구를 소급으로 바꾼다 — 실측상 이 창은 전건 `dry_run=0`이라 차이 0이므로 필터 대신 **발산 시 자백**.
- ★**변이 29종 중 1R 생존 7건이 최대 산출**이었고 전부 한 자리를 가리켰다 — **테스트가 자기 픽스처가 세운 조건만 검사한다**(픽스처에 `dry_run=1`·다른 grain·배포일 무쓰기·경계 당일 행·`--now-kst` 없는 실행이 **한 번도 없었다**).

---

## §6. 완료 QA — 원문 그대로 (미달·판정불능 포함)

**별도 서브에이전트(Sonnet · 읽기 전용) · 2026-09-06 11:2x~11:32 KST · 대조 3개.** ★원문 그대로 옮긴다.

### 합격기준 항목별 (QA 원문)
- **①** *"배포 전 창(08-29~09-04) 완결 7일·진동 2일(28.6%/발화일 기준 50.0%), 배포 후 창(09-06~09-12) 완결 0일→「판정 불가」(진행 중 09-06만 존재), **제외한 배포일 09-05는 UP3·DOWN2로 별도 출력**되고 소재별 분해에서 「배포 후 구간에 양방향을 낸 소재: 0개」까지 자체적으로 세어 병기함 → **판정: 달성**. 배포 후 창 자체의 최종 진동 판정은 09-12에나 가능하다는 것도 계수기가 스스로 밝힘 — 이는 결함이 아니라 계약이 원래 요구한 설계"* (관측 11:25:13 KST)
- **②** *"헤더에 창·소재 필터·컷오프가 모두 인쇄되고 「전체 40건·무쓰기 27건(67.5%)」로 §0의 원 기준선과 같은 좌표(소재 1개·창 09-02~09-05)를 정확히 재현 → **판정: 달성**. 다만 「배포 후」 쪽의 실제 전후 비교 수치는 아직 09-12 전이라 이번 관측에는 없음 — 계약도 이를 요구하지 않음"*
- **③** *"위 두 명령 모두 prod에서 `mode=ro` 접속으로 실제 실행했고 관측 시각이 출력에 명시됨. 소스 코드 확인(`sqlite3.connect(f"file:{args.db}?mode=ro", uri=True)`, INSERT/UPDATE/DELETE grep 0건)으로 쓰기 0건도 직접 확인 → **판정: 달성**"*

### 대조 대상별 판정 (QA 원문)
- **판정(계약 D-NAO-288 §4): 부분달성** — *"§4-A·B·C(ⓐ~ⓖ)는 이전 세션에서 이미 달성 확인됐고, 이번 세션(n=5)이 맡은 몫인 ⓗ·ⓘ의 「09-12 판정 가능하게 만들기」는 라이브로 검증됨. 다만 ⓗ·ⓘ·ⓙ 세 체크박스는 계약 자신이 「아직 판정 시점이 아니다」라며 `[ ]`로 남겨두었고 나도 그대로 관측했다(ⓙ: `naver_proposals` **481건**·`ops_diary_entries` **541건**, A-veto **0**·B-veto **0** — 2026-09-06 11:25 KST)."*
- **판정(트랙 D-NAO-59): 판정불능** — *"이번 슬라이스는 계수기 두 개를 만들어 「09-12에 판정할 수 있게」 했을 뿐, 총이익을 직접 재는 새 원장이나 액셀·브레이크 판정 로직 변경은 0줄(`git diff origin/main...HEAD --stat`로 확인). 「ROAS 방어가 아니라 총이익을 늘렸는가」를 이번 산출물에서 직접 잴 근거가 없다."*
- **판정(PAO 북극성 M4): 부분달성** — *"M4의 「품질·관측」 축 안에서 움직였고 M을 닫지 않음(진행률 2/7 불변, 직접 확인). §7 액셀·브레이크 대칭은 판정 로직 무변경으로 불변, 자동화 운영(④)은 앵커 자백대로 「3세션 연속 없음」. M4 합격 관측(카나리 실집행 1주 연속) 자체를 겨냥한 슬라이스가 아니라 그 주변의 관측 인프라를 만든 것."*
- **판정(종합): 부분달성** — *"「만들었다」(계수기 2종·테스트 다수·문서·정정 이력)는 라이브로 확인됐고, 「그것이 09-12 판정에 실제로 쓰일 수 있다」도 확인됐다. 다만 그 자체가 최종 판정(ⓗⓘⓙ 체크)이나 총이익 개선의 증거는 아니며, 계약도 처음부터 그걸 요구하지 않았다."* (11:32 KST)

### QA가 확인하지 못한 것 (원문)
- *"PR #751 현재 HEAD(`a7cecf6a`)의 CI가 관측 시점까지 완료되지 않았다. 11:24·11:31 KST 두 차례 확인했으나 `backend`가 계속 `IN_PROGRESS`(run `34006312599`)였다. 인용된 「CI 3/3 pass(run 34004042815)」는 확인했으나 이는 병합 «전» 커밋 `bdbfbd25`에 대한 것이고, 현재 HEAD인 origin/main 병합 커밋 자체의 CI는 관측 시점 기준 판정불능이다."*
- *"로컬 전체 회귀를 직접 돌려 **7,978 passed, exit 0**(11:31 KST, 312.19s)를 관측했다 — 구현자가 보고한 「7,975 passed」와 3건 차이난다. 실패 0건은 확인했으나 정확히 왜 3건 차이나는지는 조사하지 않았다."*
- *"적대 리뷰(1R FAIL→2R PASS)의 변이 주입 로그 원본은 이 저장소에 없어 커밋 메시지·diff로 간접 확인했을 뿐, 리뷰 세션 자체를 재현하지 않았다."*
- *"ⓘ의 「배포 전/후 같은 grain 전후 비교」 자체는 배포 후 완결 창이 아직 없어 이번 QA에서도 실제 전후 비교치를 낼 수 없었다 — 계수기의 능력만 확인했고, 이는 계약이 09-12로 명시적으로 미룬 것과 일치한다."*
- *"문서에 적힌 「09-05 배포 5회」 등 prod `deploy-manifest.jsonl` 세부 수치는 이번 QA 범위에 없어 별도로 재검증하지 않았다."*

### ⚠️ QA 지적에 대한 구현자 응답 (판정은 바꾸지 않는다)
- **회귀 7,975 vs 7,978의 3건 차는 내 보고 시점 문제다.** 내가 7,975를 잰 것은 **2R 신규 가드 3종을 넣기 «전»**이고, QA는 넣은 «뒤»를 쟀다. 7,975 + 3(`test_two_way_means_after_deploy_only` · `test_nowrite_confession_counts_rows_not_cells` · `test_as_of_boundary_is_exclusive_at_second_precision`) = **7,978**이 맞다. **QA 수가 정본이다.**
- **HEAD `a7cecf6a`의 CI**는 QA 관측 이후 완료된 것을 착지 시점에 별도로 확인한다 — QA의 「판정불능」은 그 시각 기준으로 옳다.

---

## §7. 남은 슬라이스 — 다음 세션이 할 것

1. ★**09-09 이후 「창 밖」 실측**(n=4 §6-1 계승) — `--curve-source db`로 09-02를 돌려 **API가 400을 주는 창에서도 재현이 서는지** 문자 그대로 시험한다.
2. ★★**09-12에 ⓗ·ⓘ를 판정한다** — 이번에 세운 두 계수기로. 계약 §5에 실행 명령 2줄이 있다.
   ⚠️**ⓘ를 옛 「27/54」와 «직접» 비교하지 말 것** — 같은 자로 양쪽을 다시 재고 비교한다(§2).
   ⚠️배포 후 창의 첫 깨끗한 날은 **09-06**이고, 09-12에도 **진행 중인 날은 분모에서 빠진다.**
3. **ⓙ 재관측 계속** — 세 세션 연속 0건이다. A-veto는 반사실 0/15라 한동안 0일 수 있다.
4. **이월(2R)** — N6·N7·N8: P2-9(분류 불가 타입만 난 날) · P2-6(`--basis all` 무쓰기 비중) · `dry_run` 자백. **코드는 옳고 가드만 없다.**
5. **이월 P2-2(n=2)** — `latch_reason_census` 커버리지 테스트 동어반복(AST 대조 필요).
6. **이월 P2-10** — 계수기 `⚠️`를 읽는 주체 미정(크론·대시보드 배선 0).

---

## §8. ↗️ 스코프 밖 (기록만)

- `naver_proposals.created_at`은 **UTC**인데 `naver_change_log.changed_at`은 **KST**다 → 소관: 이 트랙.
  ★이번 계수기는 **`naver_change_log`(KST)에서만 시각을 취하므로** 이 오염 경로를 밟지 않는다.
- 배포 후 `naver_proposals`에 KST 타임스탬프 행이 섞여 들어온다(n=4 관측: 「09-06 08시」 36건 미래 시각) — **이번에 조사 안 함** → 소관: 이 트랙.
- ⚠️**결정 번호 경합 5회째** — PR #752(`pao-논의` n=91)가 01:38:01Z에 먼저 병합돼 292를 가졌고, 규칙대로(main이 트렁크) **내 것을 293으로 재번호**했다(본문 불변·번호 15곳). 둘 다 `next_ids.sh`를 썼다 — **도구는 예약을 못 한다** → 소관: 하네스.
- ⚠️**훅 오탐 2건** — `review-surface-mutation.sh`가 **완료 QA 위임문**(읽기 전용)을 적대 리뷰로 오인해 2회 deny. 프로세스 재시작으로 「세션당 1회」 카운터가 초기화된 것으로 보인다 → `CONTRACT_surface_rule_enforcement.md` §6 계수 대상.
- ★★**트랙 훅 「기록 누락」은 오탐이 아니었다 — 트랙 파일이 «파싱 불가» 상태다**(12:1x KST 확진).
  `track_scan.py`는 `OPEN`/`CLOSE` 마커가 **정확히 1개씩**일 때만 파싱하는데, 08:4x 확인줄이
  「블록 밖 사고」를 설명하려고 본문에 **종료 마커를 원문 인용**해서 CLOSE가 **2개**(299·309행)가 됐다.
  실측: `parse()` → `parsable=False · has_record=False · n=0 · m=0` / 인용 줄만 빼면 `True · True · 2/7`.
  ⚠️**지금부터 이 트랙을 여는 모든 세션이** 「기록 누락」을 맞고 진행률이 0/0으로 읽힌다.
  ★**내 1차 진단도 틀렸다** — 같은 `awk` 범위로 재현하려다 **같은 함정**에 빠져 「내 줄이 블록 밖」이라
  결론냈다(실제 내 줄 303~308행은 블록 3~309 **안**). **도구를 깨는 리터럴은 그 도구를 흉내 낸 검산도 깨뜨린다.**
  손대지 않는다 — 남의 확인줄은 **소급 수정 금지**, 스캐너는 **하네스 소관**(수리 지점 1곳:
  `~/.claude/hooks/lib/track_scan.py:33-34`의 부분일치를 **줄 전체 일치**로) → 소관: 하네스.
- ⚠️(구판 기록) **트랙 훅 오탐이라 적었던 것** — 내 클론·`origin/main`에는 `확인:` 줄 4건이 블록 «안»에 있다. 훅이 읽는 **공유 iCloud 폴더가 stale**(그 폴더 HEAD `211decbc`). 공유 폴더에 대신 쓰지 않았다 — 남의 커밋에 쓸려 들어간다.

---

## 트랙 진행률

**2/7 불변** (달성 M0·M1 / 미달 M2·M3·M4·M5·M6). M4의 **품질·관측** 축이고 M 닫기가 아니다.
트랙 `확인:` 줄 **4건** 누적(09:5x · 10:3x · 10:3x · 11:2x KST).
**북극성 5요소**: ⑤학습·상시개선만 이동 · **④자동화 운영은 n=3·n=4·n=5 3세션 연속 「없음」**.
**§7 대칭**: `auto_operator.py`·`guardrail_gate.py`·`bid_simulator.py`·`exploration.py` **0줄 변경** ⇒ 불변.
