# 세션 인수인계: 판정기가 소재를 못 보고 있었다

> 저장일시: 2026-09-04 (KST)
> 새 대화 시작 시 이 파일을 먼저 읽을 것
> 체인: **`pao-논의` n=85** (세션 `deb55836`, 계정 dgfrty00@gmail.com)
> 검증 명령(**어느 폴더에서든 돈다** — 로컬 체크아웃이 낡아도 무관):
> `git fetch -q origin main && git show origin/main:scripts/floor_gate_check.py | ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python -"`
> ★★초판은 `scp scripts/floor_gate_check.py …`였는데 **공유 메인 폴더에서 실패한다**(2026-09-05 11:12 Jino 실행 실측: `scp: stat local "scripts/floor_gate_check.py": No such file or directory`). 그 폴더가 `origin/main`보다 **1,250커밋+ stale**이라 파일 자체가 없기 때문이다. **인계의 검증 명령은 «받는 쪽이 앉아 있는 위치»에서 돌아야 한다** — 내 작업 클론 기준으로 쓰면 안 된다. 기대: **배포 상태 줄이 «⛔미배포»면 아직 안 올라간 것**(SPECS 9종), «★배포됨»이면 SPECS 11종 + 소재 3개가 전부 차단으로 뜬다. 어느 쪽이든 그 줄이 정본이다.

---

## 0. 한 줄

**「1번을 자동화하자」의 전제가 실측으로 반증됐고, 그 자리를 파다가 «판정기가 소재 grain을 원리적으로 못 읽고 있었다»는 것이 나왔다.** 표본 하한 게이트(D-NAO-286)를 계약·구현·리뷰까지 마쳤다. **prod 배포는 안 했다** — 배포하는 순간 PAO 자동 집행이 멈추므로 그 시점은 Jino가 고를 자리다.

---

## 1. 프로젝트 위치 및 환경

- 로컬 경로: `~/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 메인 폴더는 main 고정)
- ⚠️ **이 세션의 작업 공간은 워크트리가 아니라 «로컬 디스크 클론»이다**: `…/scratchpad/ohisell-n85` (브랜치 `feat/pao-n85`). 공유 폴더에서 `git worktree add`가 **또** `fatal: mmap failed: Operation canceled`로 죽었다(n=84에 이어 2회째) — GitHub에서 `--depth 1` 클론으로 우회했다.
- 트랙: `docs/tracks/active/track_naver-ad-optimization.md` (PAO) · 계약: `docs/contracts/CONTRACT_sample_floor_gate.md`
- 북극성: `docs/references/82_pao_north_star_20260819.md` · 설계 원문: `docs/references/65_paper_application_design_20260817.md` §6 S2-ⓑ

---

## 2. 이번 세션 완료 목록

- ✅ **릴레이·생존 판정** — 훅의 `[체인] ⛔ n=49`는 공유 폴더 **1,226커밋 stale** 오탐. `origin/main` 등록부 마지막 행 n=84 `end_kst` 채워짐 = 닫힘 ⇒ n=85로 등록.
- ✅ **인계 `검증 명령` 3건 직접 실행(위임 없이) — 전부 통과**
- ✅ **착수 실측 — 인계의 전제를 반증**(§3-①)
- ✅ **계약 「표본하한 목표」 D-NAO-286 작성 → Jino 승인 → 구현 → 자기 변이 12종 → 적대 리뷰 → PR #711**
- ✅ CI **3/3 SUCCESS**(py3.10 · py3.14 · frontend) · 회귀 **7,773 passed 실패 0** · 신규 테스트 **33**
- ✅ **자기 변이 12종** — 11 즉사 · **M9 생존 → 상환 → 재주입 사망**(커밋 `33f1bff`)
- ✅ **완료 QA**(별도 기·읽기 전용) — 3대조 전부 **부분달성**(§3-5, 원문 그대로)
- ✅ **적대 리뷰**(다른 기·Opus) — **1R FAIL(P1=1) → 상환 → 2R PASS**. ★P1-1이 내 사각지대였다(§2-3)
- ✅ 기억 2건·failure DB 1건 기록

**네이버 광고계정 쓰기 0 · prod 배포 0 · 마이그레이션 0 · 스코프 행 무접촉 · `auto_operate` 무접촉 · 남의 트랙 파일 무접촉.**

---

## 3. 확정된 결정사항 · 실측

### ① 「승인할 사람이 없다」는 반증됐다 (2026-09-04 12:0x KST, prod 읽기 전용)

| 카드 | 인계 📄 | 실측 | 진짜 원인 |
|---|---|---|---|
| 제외 `negative_keyword` | 12 | **12** | SHOPPING API 거부(HTTP 400 code 3728) |
| 승격 `search_term_promote` | 280 | **280** + 만료 **495**(인계 320) | **executor가 의도적으로 없다** |
| 탐색 `bid_up_explore` | 500 | **628** | **전건 스코프 밖** |

- 대기 카드 **1,000건 중 스코프 «안»은 27건뿐**
- 스코프 «안» 그룹 `grp-…70523564`는 **오늘도 자동 승인·집행**(`bid_up` 3건 `auto_op_hr`) ⇒ 승인원은 살아 있다
- 막는 것은 `engine_approve` → `_scope_hold_reason` → `blocked_by_scope`이고, 스코프는 **Jino가 08-28·09-01에 일부러 좁힌 것**(`naver_adgroup_scope` 6행, memo에 지시 원문)

### ② Jino 결정 5건 (이 세션에서 받은 것 — 원문)

1. 「스코프를 넓힌다 (M4 카나리 확대)」
2. 「켜되, 먼저 왜 꺼졌는지 밝히고」 → 밝혀 보니 껐던 사유가 살아 있어 **안 켰다**
3. 「하한 게이트를 먼저 만든다」
4. 「캠페인 + 대상그룹 둘 다, 양방향 차단」
5. 「탐색·콜드는 면제」
6. 계약 승인 — 「승인 — 그대로 가자」

### ③ ★`auto_operate`가 꺼진 이유 — 사고가 아니라 결정이었다

`naver_campaign_settings.memo` 원문(`cmp-a001-02-000000010164717`):
> ‖ 2026-09-01 22:56 KST auto_operate 1→0 (Jino 지시 「그럼 돌려」). 사유: PAO가 이틀·14클릭 표본으로 입찰을 910→1,360원(+49%) 올렸는데 한 달 전체(ROAS 1.503)·광고그룹 전체(1.537) 둘 다 BEP 1.894 아래다. optimizer=ours는 유지 = 보되 집행 안 함. 되돌리기: auto_operate=1.

**감사 흔적은 `change_log`가 아니라 `memo`에 있다** — 내가 「흔적 없음」이라 단정한 것은 틀렸고 정정했다.

그 사유는 **지금도 성립한다**(30일, BEP 1.894): 폴드8 1.74 · 폴드8울트라 2.24 · 플립8 1.20 · 플립7 1.11 · 폴드7 2.60(클릭 8·전환 1 = 표본 없음). **5개 중 표본과 BEP를 둘 다 만족하는 건 폴드8울트라 하나뿐이다.**

### ④ ★★기계적 원인 — 판정기가 소재를 못 봤다

`auto_operator._settlement_agg`가 `keyword`/`else` 두 갈래뿐이라 `target_type='ad'`가 `NaverAdDaily.adgroup_id == <소재id>`를 조회 = **원리적으로 0행**. 그래서 소재 grain에서 ①정착창 실측 검증 ②CPC 급등 DOWN ③손실 고삐가 **전부 죽어 있었다**. 그런데 **최근 30일 실집행 15건이 전부 `ad` grain**이다.

소재 원장은 있었다 — `naver_ad_creative_daily` 23,353행. **자료가 없던 게 아니라 판정기가 그 테이블을 안 봤다.**
09-01 재현(창 08-24~30): 폴드8 그룹 소재 둘이 클릭 113 / **클릭 14** — memo의 「14클릭」이 그 자리다.

---

## 3-5. 완료 QA (별도 기 · Sonnet · 읽기 전용 · 2026-09-04 14:52 KST) — **원문 그대로**

**대조 3개 전부 부분달성.**

판정(계약 §4 합격기준): 부분달성 — ⓔⓕⓗ 달성(테스트·CI 층) / ⓐⓑ 미달(구현은 계약 문구와 정확히 일치하나 prod 미배포로 지목된 표면에 안 뜸) / ⓒⓓⓖ 판정불능(배포 전제 미충족으로 관측창 자체가 안 열림) (2026-09-04 14:52 KST 기준)

판정(Jino 지시 원문): 부분달성 — 「캠페인+대상그룹 둘 다 양방향 차단」·「탐색·콜드는 면제」는 코드(`_FLOOR_GATED_TYPES = _BID_TYPES - explore - cold`)와 테스트로 확인됨 / 「하한 게이트를 먼저 만든다」는 코드로는 만들어졌으나 prod에서 라이브로 서 있는 상태는 아직 아님(⛔미배포, 오늘도 게이트 없이 소재 3건 실집행) / 「스코프를 넓힌다(M4)」는 계약 §1에서 명시적으로 다음 슬라이스로 이월(이번엔 안 함) — 무단 축소가 아니라 앵커에 선언된 순서 조정 (2026-09-04 14:52 KST)

판정(북극성 M2 S2-ⓑ): 부분달성 — 「§4 판정표를 코드 상수로: fail-closed 게이트」는 `guardrail_gate._check_data_floor`로 코드화·테스트 완료 / 그러나 ref65 S2 합격기준 ②가 요구하는 「하한 미달 캠페인 5개에 확장 제안 0건을 **크론 로그로 확인**」은 미배포라 관측 불가 (2026-09-04 14:52 KST)

### QA 항목별 (요지)
| 항목 | 판정 | 근거 |
|---|---|---|
| ⓐ SPECS 2키 prod 노출 | **미달** | prod SPECS **9종**, 두 키 「describe()에 없음」 |
| ⓑ 소재 grain 집계 실호출 | **미달** | 숫자(conv 9·2·7)는 원자료로 일치하나 `_settlement_agg(db,"ad",…)` 실호출은 prod에 함수가 없어 미실행 |
| ⓒ 다음 레인 0건 + blocked 일기 | **판정불능** | 배포 전제 미성립 — 오늘 소재 실집행 **3건**·blocked 일기 **0행** |
| ⓓ 사유에 실수 | **판정불능** | 〃 |
| ⓔ 양방향 | **달성** | `test_증액도_감액도_똑같이_막힌다[bid_up,bid_down]` (14:52:53) |
| ⓕ 사람 승인 회귀 0 | **달성** | 로컬 + CI py3.10 7,807 passed |
| ⓖ 캠페인 게이트 크론 로그 | **판정불능** | 새 게이트 반영 회차 없음 |
| ⓗ 회귀 전건 | **달성** | CI 3/3(py3.10 7,807 · py3.14 7,806+1skip · frontend 1,468·eslint 0 errors) + 로컬 7,773 passed 0 failed |

### QA가 확인 못한 것 (원문)
- ⓐⓑ의 prod-live 함수 호출 자체(배포 전이라 원리적으로 불가)
- ⓒⓓⓖ 전부 — 배포·크론 회차 전제 미성립
- 백엔드 전체 스위트 로컬 재실행이 QA 종료 시점까지 미완주(다른 백그라운드 pytest와 경합) → 이후 완주해 **7,773 passed 0 failed**로 교차 확인, 판정 불변
- PR #711은 OPEN·MERGEABLE — 아직 머지 안 됨(prod 미배포와 정합)

★**미달·판정불능은 유효한 종결이다.** 합격기준 ⓐ~ⓖ가 요구하는 라이브 증거는 **배포 후에만** 생기고, 배포는 PAO 자동 집행을 멈추는 행위라 Jino가 시점을 고를 자리로 남겼다. 다음 세션이 배포하면 **미달 항목만 같은 기준으로 1회 재판정**하면 된다(합격기준을 낮추지 말 것 — §8).

---

## 2-3. 착지

- **완료 단계**: 커밋 → push → PR → CI 3/3 → 완료 QA → **적대 리뷰 1R FAIL(P1=1) → 상환 → 2R PASS(미해소 P1=0)** → **머지 완주**.
- **머지**: `ab777c03` (2026-09-04T14:54:58Z) · `scripts/safe_merge.sh 711` · CI 3/3 pass(py3.10 8m47s · py3.14 9m2s · frontend 6m56s) · **`--force` 미사용** · 멈춘 단계 없음
- **적대 리뷰 판정(원문)**: 1R **FAIL · P1 1건 · P2 12건** → 2R **PASS · 미해소 P1 0건**. 리뷰어 변이 1R 19종(15 사망·4 생존) → 상환 후 **생존 4종 전부 사망**. 2R 추가 변이 4종 중 3종 사망, MX3(DL 면제가 방향을 안 보는 변이) 생존 — **출고 코드는 `and _ptype in BID_DOWN_TYPES`로 정확**하고 인구조사 표가 그 성질을 확인했으므로 결함이 아니라 **잠금 누락**(이월).
- **좌표**: 브랜치 `feat/pao-n85` · PR **#711** · 커밋 `6016da6`(구현) → `33f1bff`(변이 M9 상환 + 트랙 확인줄) → `5d00fb7`(체인 등록) → `70dbb0a`(§5 실행명령 수리)
- **재개 명령**: `gh pr view 711 --json state,mergedAt --jq '.state, .mergedAt'`
- **머지 전 필수**: 적대 리뷰 **P1=0**. `scripts/safe_merge.sh 711` 사용(`gh pr merge` 직접 호출 금지 — 이 repo는 서버측 required checks를 못 걸어서 스크립트가 그 자리를 대신한다).
- **⛔ prod 배포는 이 세션 범위 밖**: 배포 즉시 PAO 자동 집행이 멈춘다(실집행 소재 3개 전부 하한 미달). 시점은 Jino가 고른다.
- **특기**: 변이 되돌리기를 `git checkout HEAD -- <path>`로 하다가 **커밋 전이던 구현 3파일이 소실**됐다. 패치 스크립트 재적용으로 복구(정확한 문자열 치환으로 짜 둔 덕에 결정적으로 재생). **변이 주입은 반드시 커밋 «후»에.**

## 4. 핵심 파일 목록

| 좌표 | 무엇 |
|---|---|
| `docs/contracts/CONTRACT_sample_floor_gate.md` | 계약 8요소 — 합격기준 ⓐ~ⓗ가 정본 |
| `backend/app/services/naver_ad/guardrail_gate.py` | `_check_data_floor` · `_FLOOR_GATED_TYPES` · 상수 2 |
| `backend/app/services/naver_ad/auto_operator.py` | `_settlement_agg` ad 분기 · `settlement_conv_counts` |
| `backend/app/services/naver_ad/naver_execution_harness.py` | 컨텍스트 배선 — **게이트의 마지막 표면** |
| `backend/tests/test_naver_guardrail_floor_gate.py` | 신규 33 |
| `scripts/floor_gate_check.py` | 계약 §5 `실행:` — **배포 전에도 돈다** |

---

## 5. 알려진 이슈·주의사항

- ⚠️ **배포하면 PAO 자동 집행이 전부 멈춘다** — 실집행 소재 3개가 전부 정착창 전환 15 미만(9·2·7, 09-04 14:28 관측). **의도된 결과**이고, 재개 경로는 ①표본 축적 ②화면에서 SPECS 값 조정 둘뿐이다.
- ⚠️ **`_settlement_agg` 수리는 액셀도 연다** — 정착창 ROAS가 UP의 «허용» 근거이기도 해서(`intraday_ok OR settle ok`), 하한을 넘긴 소재는 종전보다 UP이 쉬워질 수 있다. 표본 하한이 앞에 서 있지만 이게 이 PR의 가장 위험한 자리다.
- ⚠️ **[미상] — 끄고 나서 광고비가 1/5로 줄었다**: 폴드8 09-01 35,171 → 09-02 8,384 → 09-03 6,344. 스위치를 꺼도 올려둔 입찰은 안 돌아가는데 원장에 되돌림 기록이 **0건**이다(external 포함). 누가 내렸는지 설명이 안 된다.
- ⚠️ **`naver_entity`에 `entity_type='ad'` 행이 0건** — 소재 이름·현재 입찰가를 이 저장소가 못 읽는다. 대기 카드 511건의 `target_type='ad'` 대상들이 그래서 현재가 조회가 안 된다.
- ⚠️ 내 실수 — 변이 되돌리기를 `git checkout --`로 했는데 커밋 전이라 3파일 구현이 소실됐다. 패치 재적용으로 복구. **변이는 커밋 후에.** `failures.jsonl` 기록 완료.
- ★★⚠️ **main CI가 자정 넘김으로 빨갛다 — 내 것이 아니다**(2026-09-05 00:0x KST 관측).
  머지 `ab777c03` «직후» 실행(14:55:01Z 시작)이 **15:00:00Z = KST 자정**을 가로질러
  `'2026-09-04'` vs `'2026-09-05'` 서명으로 무너졌다. 실패 파일 **14개**가 원가·대시보드·
  KPI·매핑·상품·RG 등 **여러 트랙에 걸쳐** 있고 **내가 만진 파일은 하나도 없다**.
  내 PR #711의 CI는 자정 «전» **3/3 SUCCESS**였고 로컬 전체도 **7,783 passed**였다.
  ⇒ n=79가 문서화한 그 모드(「‘오늘 초록’은 시각을 안 잰다」)의 **재발**이고, 그날 도는
  **모든 브랜치**가 같이 빨개진다. 소관은 그 테스트들의 트랙이지 이 트랙이 아니다.
  ↗️ 스코프 밖(기록만): 날짜 시드 의존 테스트 14파일 → 소관: 각 파일의 트랙(n=79가
  `test_rg_sales_date_fees.py`에 같은 수리를 한 전례가 있다 — 「시계를 +10년 옮기는 가드」).
- ⛔ 무접촉: `pao-uiux` n=8 · `발주예측` n=8 · `승인배선` n=2

---

## 5-9. ★정정 — 「배포하면 PAO가 전부 멈춘다」는 과장이었다 (2026-09-05 09:03 KST 실측, n=87)

> 발단: Jino 반문 *「표본 하한 게이트를 prod에 올릴 시점(올리면 지금 도는 자동 집행이 멈춘다): 무슨 소리야?」*
> 이 문서 §5 첫 줄과 §0이 그렇게 적었는데, **범위를 좁혀 말했어야 했다.** 원문은 지우지 않고 여기서 정정한다.

**정확히는: 게이트는 «성과 판단 입찰 레인»만 막고, 지금 PAO가 자동으로 하는 일이 그것 하나뿐이라 「전부」처럼 보인다.**

최근 7일 PAO 자동 실집행 = **18건**(09-01 6 · 09-02 1 · 09-03 5 · 09-04 5 · 09-05 1) —
**전부 `update_bid` · 소재(ad) grain · 승인원 `auto_op_hr`**(bid_up 12 · bid_down 6).
그 외 자동 실집행(예산·정지·제외·탐색)은 **0건**이다. 스코프가 좁아서 이미 안 돈다.

**막는 것은 «캠페인» 하한이 아니라 «소재» 하한이다** (정착창 08-28~09-03):

| 소재 | 클릭 | 비용 | 전환 | RoAS | 캠페인 전환 | 판정 |
|---|---|---|---|---|---|---|
| `…558104404` | 82 | 101,865 | **7** | **0.90** | 42 | ★차단 |
| `…558107660` | 42 | 40,478 | **4** | 1.97 | 42 | ★차단 |
| `…554755092` | 60 | 69,488 | **8** | 2.20 | 89 | ★차단 |

캠페인 전환 42·89는 하한 15를 넉넉히 넘는다. ★`…104404`는 **RoAS 0.90**(BEP 1.894 한참 아래)인데
전환 **7건** 표본으로 입찰이 매일 움직이고 있다 — 2026-09-01에 Jino가 스위치를 끈 그 모양 그대로다.

**배포해도 계속 도는 것**: 사람 콘솔 승인 · DL 일일 손실 고삐 · 탐침 되돌림 · 탐색 · 콜드스타트 ·
킬스위치 · 예산/정지/제외 레인 · **수집 크론 전부** · 제안 «생성»(게이트는 «실행»만 본다).

### ★★배포는 되돌릴 수 있다 — 지금 상태가 오히려 되돌리기 어렵다

| | 하한 | 결과 |
|---|---|---|
| A | 15(기본) | 자동 입찰 변경 **0** — 관찰만 |
| B | **7** | 소재 **2개 통과**(전환 7·8) · 가장 얇은 4건짜리만 차단 |
| C | 배포 안 함 | 지금처럼 **전환 4건짜리도 움직인다** |

A↔B는 **화면에서 SPECS 값 하나**(봉투 5~20)로 오간다 — 재배포 불필요. 즉 배포 자체는
「멈춤/안 멈춤」의 이분법이 아니라 **「어디부터 멈출지」를 화면에서 고를 수 있게 되는 것**이다.
하한 10에선 통과 0개, 7에선 2개 — 실질 선택지는 **15 / 7~8 / 미배포** 셋이다.

---

## 5-10. ★★prod 배포 완료 + 하한 7 — 그리고 «오늘 실효는 0»이다 (2026-09-05 09:1x KST)

> Jino 지시 **「배포하고 하한 7로 가자」**(09-05 09:09 KST). §2 승인 지점 — 지시로 집행.

**배포**: `scripts/safe_deploy.sh` 6파일 + `--restart` · **CAS 6/6 통과** · 무중단 블루-그린
(:8001 → :8011, **다운타임 0초**) · **마이그레이션 0** · 커밋 `e7924a7`.

**하한 적용**: `PUT /api/naver/ad/settings/guardrail-params` 로 `min_weekly_conv_target: 7`.
★이 API는 **전체 치환**인데, 적용 전 DB 유래 키가 **0개**(전부 code/env)라 이 한 키만 실어도
나머지 10키가 그대로 유지된다 — 실제로 그렇게 됐다:
`min_weekly_conv_target=7.0 **source=db**` · `min_weekly_conv_campaign=15.0 source=code` · 나머지 불변.

### 라이브 판정 (계약 §5 `실행:` 명령, 09:12:34 KST)
```
배포 상태: ★배포됨(새 판정기 실재)        SPECS 전체 키 11종
…104404  clk 82 cost 101,865 conv 7  캠페인conv 42 → 통과
…107660  clk 42 cost  40,478 conv 4  캠페인conv 42 → ★차단
   사유: 표본 하한 — 대상 정착창 전환 4건 < 하한 7건(D-NAO-286, ref 33 [6] 주 15전환)
…554755092 clk 60 cost 69,488 conv 8 캠페인conv 89 → 통과
```
⇒ **합격기준 ⓐ 달성**(2키가 값·범위·출처·`why`와 함께 prod 화면 재료에 실림) ·
**ⓑ 달성**(`_settlement_agg(ad,…)` 실호출이 clk 82/42/60 반환 — 종전엔 원리적으로 전건 0).

### ★★그러나 «오늘 실효는 0»이다 — 적대 리뷰 P2-12가 라이브로 확인됐다

| 소재 | 그룹 | `auto_operate` | 정착창 전환 | 최근 실집행 | 하한 7 |
|---|---|---|---|---|---|
| `…554755092` | …70523564 | **1** | **8** | 09-02~05 **12건** | **통과** → 계속 돈다 |
| `…104404` | …70991863 | **0** | 7 | 09-01 이후 **0건** | (통과) — 애초에 안 돌음 |
| `…107660` | …70991863 | **0** | 4 | 09-01 이후 **0건** | (차단) — 애초에 안 돌음 |

**유일하게 도는 소재가 전환 8건이라 통과하고, 4건짜리는 09-01에 꺼진 마스터 스위치 때문에
이미 멈춰 있었다.** 「하한 7 → 가장 얇은 것만 차단」은 이론상 맞고 **실효는 없다** — 그 소재는
게이트까지 가지도 않는다(스코프가 아니라 캠페인 `auto_operate=0`이 먼저 막는다).

★**그게 이번 배포의 좋은 결과다**: 행위 변화 **0** · 다운타임 **0초** · 게이트 **무장 완료**.
스코프를 넓히거나 사생활 스위치를 켜는 순간부터 물기 시작한다.

⇒ **ⓒⓓ는 여전히 관측 불가**(막힐 대상이 지금 안 돈다). 「차단 0건」을 달성으로 읽으면
«미가동»을 «차단»으로 오독하는 것이다 — 그게 P2-12가 경고한 자리다.

### ⚠️ 내 측정 실수 1건
「스코프 행이 6→1로 줄었다」고 놀랐으나 **오탐**이었다 — ssh 출력을 `head`로 잘라 5줄이 안 보였다.
6행 전부 `enabled=1`로 그대로다.

---

## 5-11. ★완료 QA 재판정 1회 (배포 후, 2026-09-05 09:43 KST) — 원문 그대로

§8 「미달 항목만 재작업 후 같은 기에 1회 재판정」. **ⓐ 미달→달성 · ⓑ 미달→달성 · ⓒⓓⓖ 판정불능 유지 · 미달 0건.**

판정(계약 §4 합격기준): 부분달성 — ⓐⓑⓔⓕⓗ **5개 달성**·ⓒⓓⓖ **3개 판정불능**(미달 0건). 배포·값 반영(하한 7)까지 라이브로 확인됐으나, 게이트가 실제 제안을 막는 순간은 이번 재판정 시점까지 라이브로 한 번도 관측되지 않았다 (2026-09-05 09:43 KST)

판정(Jino 지시 원문): 부분달성 — 「배포하고 하한 7로 가자」는 prod SPECS에서 `min_weekly_conv_target=7.0(source=db)`로 라이브 확인돼 **이 지시 자체는 달성**. 다만 최초 지시 「스코프를 넓힌다(M4)」는 다음 슬라이스로 이월된 채 변화 없음 (2026-09-05 09:43 KST)

판정(북극성 M2 S2-ⓑ): 부분달성 — 코드화·배포·SPECS 값(7) 라이브 반영 완료. ref65 S2 합격기준②는 미확인 — 그 5개 캠페인이 `optimizer='none'`이라 **게이트가 서기 전부터 이미 별도 사유로 닫혀 있었다**는 것만 새로 드러났다 (2026-09-05 09:43 KST)

### ⚠️ QA 근거 1건 정정 — UTC/KST 혼동 (판정값은 불변)
QA가 ⓒ 근거로 든 **「09:19 이후 제안 생성 0건」은 틀렸다.** `naver_proposals.created_at`은 **UTC 저장**이라 KST로 필터하면 9시간 어긋난다.
**실측(09:45 KST)**: 09:20 KST 레인(=00:20 UTC)에 제안 **16건 생성**(`bid_up_explore` 15 · `bid_up` 1) — 전부 `pending`·`approval_source` NULL.
⇒ 실제 사유는 「아무것도 안 돌았다」가 아니라 **「레인은 정상 작동했고 16건 전부 «스코프 밖»에서 `engine_approve`에 홀드돼 게이트까지 못 갔다」**(일기: 「자동운영 스코프 밖 광고그룹 — D-NAO-244」).
★★**스코프가 게이트 앞에 서 있다** — 이 세션이 처음부터 발견한 그 구조가 배포 후에도 그대로 관측된다.

### ★ⓖ에 대해 새로 드러난 것
합격기준 ⓖ가 지목한 5개 캠페인(아이패드 파워링크·아이폰_지문방지·컨텐츠매체·맥세이프카드케이스·갤럭시Z_폴드8플립8_사생활보호)이 **전부 `optimizer='none' · auto_operate=0`**이다.
즉 「확장 제안 0건」은 게이트 덕이 아니라 **PAO 관리 대상이 아니어서** 이미 0이었다. ⓖ는 원래부터 «게이트를 시험할 수 없는 기준»이었다 — 다음 세션이 이걸 달성으로 읽지 말 것.

---

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: 「1번을 자동화하자」 → 실측 반증 후 Jino가 고른 길 = **「스코프를 넓힌다(M4 카나리 확대)」**. 하한 게이트는 그 **선행조건**이었고 이번에 끝냈다.
- **3문 검사**: ①목표·범위를 바꾸는가 → 아니오 ②되돌릴 수 없는가 → **배포·스위치·스코프 확대는 예** ⇒ 그 셋은 Jino 발의로만.
- **남은 슬라이스**
  - [ ] **prod 배포** — Jino가 시점을 고른다(배포 즉시 자동 집행 정지). `scripts/safe_deploy.sh` 사용, 마이그레이션 없음
  - [ ] 배포 후 계약 §4 ⓐ~ⓖ 라이브 판정 + **완료 QA 1회**(대조 3: 계약 §4 / Jino 지시 원문 / 북극성 M2 S2-ⓑ)
  - [ ] 플립/폴드 5그룹 `auto_operate` 재점화 — 게이트가 라이브로 확인된 뒤, Jino 발의로
  - [ ] 스코프 확대 — 후보 20그룹 실측표가 §7에 있다. **어느 그룹인지는 Jino가 정한다**
  - [ ] [미상] 광고비 1/5 급락의 원인
- **함께 살아 있는 후보**(n=84 이월): 제외 원장 3,882건이 「제외됨」인데 라이브가 계속 돈을 썼다

---

## 7. 스코프 확대 후보 20그룹 (30일 창, BEP RoAS 1.711 · 다음 세션이 재실측할 것)

전부 a001-02 SHOPPING. **TPU 17 · 사생활 3**(S21·S23플러스·S24울트라 — 이 셋은 바형이라 성격이 다르다).

| 등급 | 그룹 | 광고비 | 전환매출 | RoAS |
|---|---|---|---|---|
| BEP 초과 7 | Z플립6 4.69 · Z플립5 4.63 · Z플립7 2.75 · S23플러스 2.48 · 노트9 2.24 · S22 2.18 · Z폴드6 1.87 | 222,788원 | 564,600원 | 2.53 |
| BEP 미달 7 | 노트20울트라 1.67 · S24울트라 1.57 · S23 1.49 · S24플러스 1.08 · Z폴드4 0.92 · Z플립2 0 · S21 0 | 363,605원 | 518,900원 | 1.43 |
| 비용 0, 6 | Z트라이폴드 · Z플립1 · Z폴드1 · Z폴드SE · 노트10플러스 · S8플러스 | 0 | 0 | — |

- 대행사 접촉 30일 **1건** ⇒ 북극성 §8-② 소유권 분리가 이 후보군에선 사실상 성립(대행사 external 691건은 전부 a001-01 파워링크에 있다)
- ⚠️ 비용 0인 6그룹은 창 안 집행이 없어 **화면 목록에 안 뜬다** — 일괄 지정으로는 못 넣고 단건 API가 필요하다
- 대기 628건은 **inert** — 일 레인이 같은 날 생성분만 집고 `bid_up_explore`는 재수거 대상이 아니다. 스코프를 열어도 소급 발사는 없다

---

## 7-5. 트랙 진행률

**진행률 2/7 — 불변**(달성 M0·M1 / 미달 M2·M3·M4·M5·M6). **체크박스를 하나도 찍지 않았다.**

- 이 슬라이스는 **M2의 일부**(S2-ⓑ 하한 게이트)이지 M2 전체가 아니다 — S1(ⓐ풀링·ⓑn-gram·ⓒbidWeight 판독)과 S2 나머지(ⓐ주말·공휴일 라벨 · ⓒGAVE 성적표 축 · ⓓ[8] WEB_SITE · ⓔ조치×환경 채점)가 남는다.
- M2 체크박스는 `ref 65` S1 ①~⑥ + S2 ①~⑤를 **라이브로** 요구한다. 완료 QA가 S2 ②(크론 로그)를 **판정불능(미배포)**으로 판정했으므로, 지금 찍으면 증거 없는 주장이 된다.
- 트랙 확인줄: `확인: 2026-09-04 13:5x KST [deb55836] — …` 1줄 누적(커밋 `33f1bff`, TRACK-CONTRACT 블록 «안»).
  ⚠️ Stop 훅이 「트랙 기록 누락」이라 했으나 **오탐**이다 — 훅이 읽는 공유 메인 폴더의 트랙 파일은 확인줄 **54줄**뿐이고 origin/main 기준은 **230줄**이다(1,200커밋+ stale). 공유 폴더엔 쓰지 않았다(남의 커밋에 쓸려 들어간다).

---

## 8. PAO 북극성 대비 검토 (D-NAO-226·227)

1. **어느 M** — §6 **M2 S2-ⓑ**(하한 게이트). 애초 방향은 M4였고 이건 그 선행조건이다.
2. **5요소** — ④자동화 운영. ★기억·인계의 「④ 39세션째 0」은 **낡았다**: 스코프 안 그룹은 지금도 자동 집행 중이다.
3. **§7 대칭** — 이 게이트는 **브레이크**다. 그래서 Jino가 「양방향」을 골랐고(증액만 막으면 D-NAO-85형 표류), 「탐색·콜드 면제」로 액셀을 살렸다. 대칭 검사 통과.
4. **관련 절 전수** — §목차 훑고 §1·§2·§4-3·§5-3·§6 M2/M4·§6-b·§7·§8-①②③ 읽음. `ref 65` §1-2·§4·§5-b·§6 S2 원문 인용.

---

## 9. 새 세션 시작 프롬프트

```
/session-relay pao-논의
```
