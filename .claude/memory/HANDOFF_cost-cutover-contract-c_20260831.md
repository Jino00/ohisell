# 세션 인수인계: 배선이 «애초에 없었다» — 「계약 C」 승인까지

> 저장일시: 2026-08-31 20:2x KST · 세션 `0ef4ee1d` · 체인 **「SellC 원가 메뉴」 n=21**
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## 0. 목표 — 다시 쓰지 마라

정본 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md`. 목표이름 **「SellC 원가 메뉴 목표」**.

> ***"SellC에 원가 메뉴를 하나 붙이자. 여기에는 우리가 현재 알고있는 모든 원가가 있잖아? 그 원가를 가지고 계산되는 방법이 첨부된 파일처럼 나오고 그 계산이 되면 되지 않을까? 그리고 수입건에 대해서는 두번째, 세번째 첨부자료처럼 수입한 금액을 알려주면 너가 이런것들을 모아서 회계적으로 계산되는 원가를 만들면 되잖아. 그리고 그런 원가를 만들려면 재고사항이 필요할꺼고, 그건 ecount와 연결하면 될꺼고."*** (Jino 2026-08-22) / ***"목표는 정확한 원가 산출이니까?"***

★**Jino가 2026-08-31 17:26에 궁극 목표를 «상태»로 다시 말했다** — 이게 앞으로의 조준점이다:

> ***"우리의 궁극적인 목표는 원가 메뉴에 올라오는 원가가 항상 정확하게 올라오는거고, 수정이 있을때 실수없이 잘 수정되어서 최신 원가가 유지되게 하는거 맞지?"***

세 요소 **①항상 정확 ②수정 시 실수 없이 ③최신 유지**. 대조해 보니 **셋 다 이미 기록된 목표 안에 있었다** — 새 목표가 아니라 같은 목표를 결과로 말한 것이다.

이 세션의 하위 계약: **「원가 컷오버 목표」**(`D-CPP-64`, `docs/contracts/CONTRACT_cost_truth_cutover.md`, Jino 승인 2026-08-31 19:51).

## 1. ★이 세션의 한 문장

**20일째 원가가 안 맞던 이유는 «드리프트»가 아니라 «배선 부재»였다 — 계산한 값이 장부로 흘러가는 경로가 코드에 한 줄도 없었고, 그건 버그가 아니라 우리가 8월에 선언한 설계였다.**

## 2. 프로젝트 위치 및 환경

- 워크트리 `/Users/jino/.claude-worktrees/ohiselling/cost-menu-n21` · 브랜치 `feat/cost-menu-n21`
- prod `sellc.ohitech.co.kr` · **활성 포트는 배포마다 8001↔8011로 뒤집힌다 — `pm2 jlist`로 매번 재라**(이번 세션 관측값 **8001**, pm2 이름 `ohisell-backend-8001`)
- prod DB는 SQLite: `/home/ubuntu/ohisell/backend/ohisell.db`. **조회는 반드시 읽기 전용으로**:
  `sqlite3.connect("file:/home/ubuntu/ohisell/backend/ohisell.db?mode=ro", uri=True)`
- prod 조회 관례: 스크립트를 `/tmp`에 쓰고 `scp` 후 `ssh ... 'python3 /tmp/x.py'` (heredoc은 따옴표가 깨진다)
- **헬스 엔드포인트는 `/api/scheduler/health`다** — `/api/health/scheduler`는 **404**(이번 세션에 한 번 헛짚었다)
- 백엔드 테스트: `cd backend && PYTHONPATH=. python3 -m pytest tests/ -q` · 프론트 타입체크는 **`npm run build`로만**
- **검증에 파이프 금지** — `| tail`이 exit code를 삼킨다(인계 5연속 경고)

## 2-1. 완료 QA (별도 Sonnet · 읽기 전용 · 판정 원문 그대로)

- **작업 목적(정본 원문)**: 위 §0 트랙 목표
- **합격기준(원문)**: 앵커 `합격:` 4항목 — ①513의 정본이 집단별로 갈라진다 ②474가 draft에 묶인 이유가 갈린다 ③「수정」의 정식 경로가 실측으로 열거된다 ④셋이 「목표 완성 계약」의 §0으로 쓸 수 있는 형태로 산출물 1장에 실린다

### 슬라이스 1 — 격차 진단 (16:4x 판정)

> **판정(앵커 합격기준 4항목): 달성** — *"4항목 전건. QA가 prod를 `mode=ro`로 독립 재현했고 문서의 숫자 30개 이상이 소수점까지 전건 일치"*
> **판정(Jino 16:3x 지시 원문 「격차 진단부터」): 달성**
> **판정(트랙 궁극 목표 대비): 부분달성** — *"진행률 3/6 불변·코드 0줄·`cost_price` 미변경"*
> **종합: 부분달성**

★QA가 내 오류 1건을 잡았고 **이월하지 않고 고쳤다**: 「쿠팡 1P 맵이 963 중 **279**만 덮는다」가 틀렸다 — **279는 행수이고 distinct `internal_sku`는 177**(커버리지 18.4%). 직접 재확인 후 정정 문단과 함께 고쳤다. 결론(「연결 0건 ≠ 안 판다」)은 그대로이고 **근거가 강해진다.**

### 슬라이스 2 — 계약 준비도 실측 (19:3x 1차 · 20:11 재판정)

> **판정(앵커 합격기준 4항목): 달성** — *"QA가 prod `mode=ro` 직접 SQL로 완전 독립 재구성해 513=474+39 · 39=9+4+26 · `recipe_kind` 4행 분포 · 474의 form_factor 7분류(레시피 수까지)를 전부 정수 단위로 정확 재현했고, 드리프트 가드 호출부(쓰기차단 2·읽기 1·`cost_menu` 0)와 무가드 2경로·가드 2경로를 코드 직접 열람으로 확인"*
> **판정(Jino 19:0x 지시 원문 「나를 지금 재고 가자」): 달성** — *"「계약 없이 재는 일」이라는 주장도 `git show --stat 18a97c6e`가 코드 0건으로 확인"*
> **판정(트랙 궁극 목표 대비): 부분달성** — *"진행률 3/6 불변 · `cost_price` 값 변경 0건 · 코드 0줄이라 「항상 정확·최신 유지」 상태 자체는 이번에도 전진하지 않았다. 다만 진단을 넘어 D-CPP-64 실제 승인까지 도달해 C2를 향한 필수 선행 결정이 확정됐고, 앵커가 착수 «전» 「이번 범위 아님」을 선언했으므로 표류가 아니라 계약 §6이 스스로 예고한 정직한 다음 단계"*
> **종합: 부분달성** · **목적 전환: 없음**

★**QA를 두 번 돌렸다.** 1차(19:3x)가 ③**「PR #607이 판정 시점에 아직 OPEN — origin/main 안착 미확정」**을 미해소로 남겨서, 착지가 끝난 뒤 재판정했다. **두 판정 모두 앵커에 보존**했다 — 나중 것이 앞의 것을 지우지 않는다.
★재판정이 해소한 것: PR #607 머지 확인 · 계약 #610 머지 `eeaef983` · 합격기준 4번의 *"Jino가 읽고 계약을 승인할지 고를 수 있다"*가 **실제 승인까지** 일어나 **「고를 수 있다」를 넘었다.**

**QA가 확인 못한 것**(원문): ①나머지 18개 원가메뉴 쓰기 경로의 개별 검사 — **문서·앵커가 스스로 「서브에이전트 조사분·재현 안 함」으로 자백한 범위와 동일** ②드리프트 가드 정본 스냅샷 파일의 실재·최신 여부 ③「22개」 총계의 전수 카운트 ④474 중 매입품/조립품 비율 — **문서도 [미상] 명시, QA도 잴 방법 없음** ⑤프론트 UI 가드

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_cost-truth-ledger.md`
- **트랙 목표 원문**: §0과 동일(헤더 `목표:` 줄)
- **진행률**: 시작 **3/6** → 종료 **3/6** — 달성 B1·B2·A1 / 미달 **A2·C1·C2**
- **이번 세션이 움직인 항목**: **없음(조사·계약만)** — 계약 §6이 착수 «전»에 *"S3가 서면 C2 [x] 후보"*로 예고했다. 무단 정체가 아니다.
- **헤더에 남긴 확인 줄**: 4건, 전부 `[0ef4ee1d]` (16:5x · 16:4x · 19:2x · 19:5x)
- **다음 세션 후보 항목**: **C2** — 계약 D-CPP-64의 S3가 서면 체크 후보(3/6 → 4/6)
- **트랙 종결 여부**: **미도달** (C1이 남는다)

## 2-3. 착지

- **완료 단계**: 커밋 → push → PR **3건** → 리뷰(기록물 예외) → **머지 3건 전부 완료**
- **멈춘 단계**: **없음**
- **재개 명령**: 해당 없음
- **좌표**:
  | PR | 내용 | 머지 |
  |---|---|---|
  | **#604** | ref 118 격차 진단 + 완료 QA 판정 | `4fa94e03` |
  | **#607** | ref 119 배선 부재 census | `3c646f3e` |
  | **#610** | 계약 C 초안 + 승인 확정 | `eeaef983` |
  커밋 `f2058bb9` `6eaa90e8` `18a97c6e` `ed93c74c` `06c386b4`
- **리뷰 판정**: **생략(기록물만)** — `⚠️ 리뷰 생략: 기록물만` 자백을 세 PR 모두에 남겼다. **코드 0파일**이라 §6 기록물 예외에 해당한다.
- CI: 세 PR 전건 **3/3 pass** · `safe_merge.sh` 경유 · **`--force` 미사용**
- ⚠️ **L5로 「main에 세워둔다」 생략**(로컬 `main`이 공유 메인 폴더에 체크아웃)

## 3. 확정된 결정사항 (번복 금지)

- **D-CPP-64 「원가 컷오버 목표」 승인** (Jino 2026-08-31 19:51) — `docs/contracts/CONTRACT_cost_truth_cutover.md`가 **정본**이다.
- ★★**계약 §2-0 — Jino 원문**: ***"원가는 무조건 sellC의 원가 메뉴를 참고해"***. 이 조항이 §2의 나머지 전부보다 **앞선다**. ⇒ `product_master.cost_price`는 원가 메뉴 정본의 **사본**이지 독립 사실이 아니다.
- **계약 A′(D-CPP-53) §3 첫 금지선을 «조건부로» 푼다** — 쓰기는 **컷오버 경로 한 벌 + Jino 클릭 후 + 이력 append + 근거 좌표**일 때만. **나머지 21개 문은 오히려 더 잠근다.** 소비처 14곳(읽는 쪽)은 **여전히 불가침**. A′ §3의 **나머지 9개 금지선은 전부 상속**(계약 §3-A 표).
- **Q3 = 자동 추종** (Jino *"그래"*) — 승인 대상은 «값»이 아니라 **「이 SKU는 이 정본을 따른다」는 결정**.
- **Q1 = C1(재고+ECOUNT) 이번엔 안 함** (Jino *"아니, 안해도 돼"*) — ⚠️ **미해소 1건은 §5에.**

## 4. 핵심 파일 목록

| 파일 | 역할 |
|---|---|
| `docs/contracts/CONTRACT_cost_truth_cutover.md` | **계약 C = D-CPP-64 정본** (200줄 · 슬라이스 4 · 합격 13항목 · **0/13**) |
| `docs/references/118_cost_price_vs_computed_gap_20260831.md` | 447건 원인별 진단 · 분류 SQL 병기 |
| `docs/references/119_cost_truth_wiring_census_20260831.md` | **배선 부재** + 513 집단 + 쓰기 경로 22개 census |
| `docs/tracks/active/track_cost-truth-ledger.md` | 트랙 정본 (3/6) |
| `backend/app/routers/cost_menu.py:14-15` | **금지선의 집행 지점** — *"읽지도 쓰지도 않는다"* |
| `backend/app/services/cost_truth_audit.py` | 감시기 — **정본이 08-07 엑셀**이라 재조준 대상(S4) |
| `backend/app/routers/products.py:86·97·459·468` | `cost_price` 4경로 중 **앞 둘이 무가드** |
| `backend/app/services/product_mapping_ingest.py:181·284` | 나머지 쓰기 + 가드 |

## 5. 알려진 이슈 / 주의사항

- ⚠️⚠️ **Q1 미해소**: *"안해도 돼"*를 **「이 계약에서 뺀다」**로 읽었다(질문이 *"이번에 같이 할까요"*였으므로). 그런데 **트랙 합격항목 C1은 그대로 남아 있고** Jino 원문 ③(*"재고사항이 필요할꺼고, 그건 ecount와 연결하면 될꺼고"*)도 트랙 목표에 살아 있다. **「이번에 안 함」인지 「트랙에서 아예 내림」인지 아직 안 정해졌다.** 후자라면 **트랙 목표 변경이라 새 계약**이다. **이 계약 종결에는 영향 없다 — 물어야 할 때가 오면 그때 묻는다.**
- ⚠️ **새 하네스 함정 — iCloud가 git ref 파일을 복제한다**: `.git/refs/remotes/origin/feat/cost-menu-n21 2`가 생겨 `git fetch`가 `fatal: bad object` + `did not send all necessary objects`로 실패했다. **push·PR·`ls-remote`는 정상**이라 작업엔 지장 없다. 공유 폴더 `.git`이라 **지우지 않고 남겨 뒀다.** 판정은 항상 `git ls-remote`로 — 로컬 ref를 믿지 마라.
- ⚠️ 헬스 엔드포인트 `/api/scheduler/health` (앞에 `/api/health/scheduler`로 헛짚어 404). `healthy: false`는 **실패 잡 7건** 때문이고 원가와 무관하다.
- ⚠️ **`product_master.category`는 분류에 못 쓴다** — 963 중 **940이 NULL**.
- ⚠️ 트랙 확인줄 훅 오탐 다수(공유 폴더 **918커밋** stale — 진행률을 2/6·확인줄 0건으로 읽는다). **공유 폴더에 대신 쓰지 마라.**
- ⚠️ 살아 있는 세션 3건 미접촉: `발주예측` n=8(`c3b74f44`) · `승인배선` n=2(`607c2c15`) · **`pao-논의` n=75(`67c50dc0` — 등록부에 행이 없는 미등록 라이브)**.
- ⚠️ `alembic.ini`가 `DATABASE_URL`을 무시한다(마이그 테스트 거짓 통과) — 손대지 않았다.

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: 계약 D-CPP-64 §1 — *"원가 메뉴에서 SKU마다 「이 SKU의 정본은 무엇이고, 현재 `cost_price`와 얼마나 어긋나며, 컷오버는 언제 누가 했나」가 보이고, Jino의 확인 클릭 한 벌의 문으로만 `cost_price`가 정본을 따라가며, 그 뒤로는 어긋남이 생기는 순간 감시기가 빨갛게 말한다."*

- **남은 슬라이스** (계약 §4 — **0/13**):
- [ ] **S1 — 이력과 문** (합격 3항목 · 마이그 1건). **여기서 시작한다.** ①`cost_price` 변경 이력 테이블(append-only) 신설 + 원가 메뉴 「변경 이력」 패널 ②무가드 `PUT /api/products/{id}`·`POST /api/products`의 `cost_price` **전면 거부**(사유 문장: 「원가는 원가 메뉴가 정본이다 — 정정은 원가 메뉴에서」) ③가드 fail-open을 health 배너가 자백
- [ ] **S2 — 정본 판별층** (합격 3항목). 963 전 SKU에 「정본 유형·정본값·현재값·격차」 + **보류는 사유와 소관을 문장으로** + 정본 없음 513이 소관별로 갈라 보임
- [ ] **S3 — 컷오버 실행** (합격 4항목). G2 269 + G3-2 9 = **278 SKU / +111,367.8원** + 평가방법 확정(`confirmed=1`). ★**「클릭 없인 한 건도 안 움직인다」가 합격기준이다** — 배포 후 Jino 클릭 «전» 이력 0건을 화면에서 확인
- [ ] **S4 — 감시기 재조준** (합격 3항목). `cost_drift`가 «SKU별 정본 ↔ `cost_price`» 대조 + 컷오버 SKU 자동 추종(`auto` 행) + 마지막 변경 시각·경로 표시
- [ ] (묻지 말고 진행) 계약 D-CPP-63 §4 체크박스 **0/8** — n=19가 S1을 완주하고 QA가 「4항목 라이브 실질 달성」이라 판정했는데 계약에 안 옮겨져 있다. 증거 좌표를 달아 옮길 것

## 7. 새 세션 시작 프롬프트

```
/session-relay sellc-원가-메뉴
```
