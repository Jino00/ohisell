# HANDOFF — 선행 P3 대조군 지정 + 쇼핑 제외 «재개방» 개통 (2026-08-28, 세션 5a30df63 / 체인 PAO 논의 66)

## 0. 작업 목적 (원문 — 발명하지 말고 그대로 옮길 것)

계약 `docs/contracts/CONTRACT_pao_ignition_canary.md` (**D-NAO-247**, Jino 승인 2026-08-25 00:33 「승인해.」). **목표이름: 점화 목표**. 북극성 **M4** 슬라이스.

> **목표**: 07-30 10:48 이후 **26일째 0건**인 우리 실집행을 재개한다. … 이 계약이 끝나면: **Jino가 승인한 카나리 캠페인에서 우리 엔진이 실집행하고, 그 행위·결과가 일기→후보→지혜 사슬에 다시 실리며, 성적이 구간 자 병기로 화면에 보인다.**

이번 세션 몫: 선행 **P3**(대조군 지정) + 쇼핑 제외 해제 배선 수리. **점화는 하지 않았다.**

---

## 1. 완료 QA — 판정 원문 그대로 (별도 Sonnet · 읽기 전용 · 4대조)

**종합 판정: 부분달성**

- **판정(계약 §4 선행): 부분달성** — P3 달성 / P2·P4 미달
- **판정(Jino 지시 원문): 부분달성** — 절차성 지시(대조군·처치군 지정, 제외해제 배선 수리)는 이행·기록됨. 「그래, 추가해줘」 승인분은 실행되지 않음(정당성은 §1-A). 「운영해보자」의 실집행 자체는 아직 도달 전(§8-① 미결로 계약이 이미 상정한 순서)
- **판정(트랙 헤더 합격 M항목): 부분달성** — 진행률 **2/7 불변**. M4 선행요건은 실질 전진했으나 체크박스 이동 0건. QA 표현: *「정직하게 불변 기록됨 — 허위 체크 없음」*
- **판정(북극성 궁극 목표 5요소): 부분달성** — **④자동화 여전히 0**(직접 확인). §7 대칭 검사는 실결함 1건을 발견·수리해 실질 기여

**QA 「저자가 주장했으나 재현 안 된 것: 없음」** — P3 숫자·제외 원장 카운트·코드 구조·PR/CI·전체 테스트 전부 독립 재현.

**QA 판정불능 2건**: ①라이브 네이버 콘솔 상태(읽기 전용 조건상 API 미접근 — 「계정 32건」은 이 세션의 이전 실측 인용으로만 존재) ②「쿠팡 ROAS 8.58」 수치가 **파일에 미기록**(대화 안에만 존재).

### §1-A. QA가 남긴 관찰 (★처분 아님 — 다음 세션이 읽고 판단할 것)

> *「승인받은 지시를 세션이 스스로 판단해 미실행한 것 자체는 이례적 권한 행사다. 되돌린 근거가 코드 사실관계 검증이라는 점에서 "불복종"이 아니라 **"표면 대 의도 불일치의 정정"**에 가깝지만, 이런 case가 반복되면 **"Jino가 승인한 것도 세션이 재해석해 뒤집을 수 있다"는 선례가 될 위험**은 있다 — 이건 처분이 아니라 관찰로만 남긴다.」*

QA는 이번 건을 **정당**으로 판정했다(근거: ①구조적 재현 ②코드 주석의 명시적 금지와 8일 미이행 사고 이력 ③§2 목적 전환 선언으로 자백+기록 ④되돌려 보고 = 은폐도 임의 진행도 아님).

---

## 2. 트랙 진행률 (전역 §6 의무)

- 트랙: `docs/tracks/active/track_naver-ad-optimization.md`
- **시작 2/7 → 종료 2/7 (불변)**
- 달성: M0 · M1 / 미달: M2 · M3 · M4 · M5 · M6
- **이번 세션이 움직인 항목: 없음.** P3·D-NAO-271은 M4의 **선행**이고, M4 체크는 카나리 창 7일 연속 관측으로만 처분된다
- **트랙 목표 원문** (Jino 2026-07-19, D-NAO-59):
  > "무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."
- 확인줄 2건 누적 (착수 21:0x · 종결 23:3x, 둘 다 `[5a30df63]`)

---

## 3. 한 것

### 3-1. D-NAO-270 — 선행 P3 대조군 지정 (코드 0줄)

| | 대상 | 7일(08-21~27) |
|---|---|---|
| **처치** | `Z폴드8와이드` **1그룹** (`grp-a001-02-000000070523564`) | 214,932원 · 182클릭 · 16전환 · 268,800원 · ROAS 1.251 |
| **대조** | TPU 나머지 **57그룹 집합** | 841,841원 · 567클릭 · 90전환 · 1,287,830원 · ROAS 1.530 |

★**「깨끗한 대조군」이 원리적으로 없다**: TPU 비용 상위 21그룹의 `ad_edit_tm`이 **전건** 8/24 08:56Z(연속 4초)·8/26·8/27 세 스윕 중 하나 ⇒ **대행사는 그룹을 골라 만지지 않고 캠페인 전체를 일괄 스윕한다.** 그래서 대조군 기준을 「같은 시장」이 아니라 **「같은 외생 충격을 맞는 것」**으로 정의하고, 판정을 **전주 대비 변화의 차**(처치 Δ − 대조 Δ)로 잡았다.

**배제 2건**: ①**울트라** — 상품코드·판매가·원가·BEP가 사실상 동일해 초안이 「동족 1순위」로 올렸으나 **Jino가 반증**(*"와이드랑 울트라는 다른 제품이야. 고객의 선호도가 완전 다르지. 쿠팡 광고리포트에서도 확인해보면 알 수 있잖아"*) → 쿠팡 검색 노출 **4,838 vs 199(24배)** · 주문 122 vs 6 · CVR 17.48% vs 12.00% · ROAS 3.908 vs 2.255 ⇒ **같은 BEP라도 다른 시장** ②**폴드7** — 소재 20개·상품 20종·BEP 1.53~2.11 혼재로 구조 상이, 규모는 처치의 1/7.

### 3-2. D-NAO-271 — 쇼핑 제외 «재개방»(자동 해제) 개통

**발단은 Jino 질문 둘**: *"제외했던 키워드를 다시 살릴 수도 있는거잖아?"* → *"우리 자동해제도 만들어놨잖아"*. **둘 다 맞았고 내가 틀렸다** — 기계는 완성돼 있었고(D-NAO-259 상태기계·등급 백오프·probation·채점기·학습 사슬 진입) **막힌 건 마지막 한 줄**이었다.

`_open_exclusion`이 파워링크 전용 `delete_restricted_keywords`를 **직접** 호출 → 쇼핑은 `restrict_kwd_id`가 NULL(리소스에 키워드 단위 id가 없는 게 의도된 설계)이라 첫 가드에서 전건 거부 ⇒ **쇼핑 재개방 원리적 0건**. prod 실측 NULL **3,884** / 있음 **106**(97.3%).

**수리**: `rollback_exclusions(..., adgroup_type=명시)` 라우팅 + **유형 판정을 클레임 «전»으로** + 실패 로그에 `term` 병기.

★★**적대 리뷰 1R FAIL(P1 1) → 2R PASS**. P1-1 = 초판이 함수 머리에 둔 **캠페인 축** 조기 필터가 `return False`로 즉시 끝내 **「캠페인 축은 비쇼핑인데 광고그룹 축은 SHOPPING」인 조합이 권위 있는 축을 묻지도 못한 채 영구 차단**됐다 — **고치려던 결함이 축 불일치 부분집합에서 재발하는 모양**이고, 내가 그 자리 주석에 D-NAO-179 경고를 써 놓고 그 옆에서 같은 병을 재현했다. ⇒ **필터 통째 제거, 유형 판정을 광고그룹 축 한 곳으로 단일화.**

★**변이 M3이 1차에 생존**: None 가드가 없어도 클레임 후 라이브 403으로 실패해 `False`/`status=excluded`가 **우연히 같게** 관측됐다. `change_log(_RESTORE_ACTION) 카운트==0` 단언 추가로 재주입 사살 ⇒ **관측값이 같다고 같은 일이 일어난 게 아니다.**

---

## 4. ★★이 세션의 본체 — 내가 틀린 것을 Jino가 다섯 번 잡았다

| # | 내 주장 | 실제 | 잡은 이 |
|---|---|---|---|
| 1 | 그룹 단위 점화는 계약 범위 밖, 개정 필요 | D-NAO-244가 **이미 승인**(«캠페인 마스터 ∧ 그룹 제한»), 계약 §2·C1이 이미 그 구조 | **Jino** (*"우리 광고 단위를 캠페인과 광고 그룹 모두 진행 가능하게 만들어놓지 않았나?"*) |
| 2 | 입찰은 `bid_down`만 열림 (Fable 보고 릴레이) | `_AD_BID_CANARY_PROPOSAL_TYPES = {"bid_down","bid_up"}` — **양방향**(D-NAO-129·130) | 코드 직독 |
| 3 | **`AD_BID_CANARY_CAMPAIGNS`에 TPU 추가하면 입찰이 열린다** | **정반대.** `AD_BID_ROUTING_ENABLED=True`라 이미 전 캠페인 개방이고, 그 상수의 현재 소비처는 **«제외» 의미**(`delegation_gate:115` 위임 자동승인 거부 · `expert_briefing_builder:96` 브리핑 제외). 코드가 *"★건드리지 않는다"*로 이미 금지, 8일 미이행으로 4캠페인·92그룹·256소재 hold 사고 이력 | 코드 직독 — ⚠️**Jino 승인(*"그래, 추가해줘"*)을 받은 뒤였다.** 실행하지 않고 되돌려 보고 |
| 4 | 입찰만 켜면 제외 검증 불요 | `OPEN_ACTIONS` 다섯 액션 **전건 개방**, 액션별 스위치 없음 | 코드 직독 |
| 5 | 제외 쓰기 32건 덮어쓰기 위험 | D-NAO-180·181이 이미 라우팅·보존 3겹으로 해결 | **같은 세션 안에서 자체 반증** |

★**일반형**: 다섯 다 **메모리·주석·상수 «이름»을 읽고 실값과 소비처를 안 읽어서** 났다. 「이 레버가 열려 있나」는 **실값과 호출부**로만 답이 나온다.

---

## 5. ★되살리기 계획을 데이터로 자체 철회

잠긴 11건 중 성과 이력이 있는 4건이 **13개월 전건 전환 0**(합계 29,688원):

| 검색어 | 막은 날 | 클릭 | 광고비 | 전환 |
|---|---|--:|--:|--:|
| `폴드보호필름` | 7/28 | 6 | 11,345원 | **0** |
| `갤럭시폴드액정필름` | 7/28 | 5 | 8,555원 | **0** |
| `폴드8정품필름` | 7/24 | 4 | 6,153원 | **0** |
| `폴드와이드프라이버시필름` | 7/28 | 2 | 3,635원 | **0** |

⇒ **Jino가 7월에 콘솔로 막으신 판단이 옳았다.** 쿠팡 유사 검색어 ROAS를 근거로 「되살릴 만하다」던 내 초판을 철회한다 — **채널이 다르면 같은 낱말도 다른 시장이다.**

★**패턴**: **「폴드8」이 붙으면 팔리고**(`폴드8필름` 190클릭 22전환 · `폴드8지문방지필름` 84클릭 14전환) **기종 미특정이면 안 팔린다**.

⇒ 카나리 액셀은 **입찰 상향 · 그룹 재개** 둘로 간다. 제외 해제는 배선만 열어 두고, 재료는 **엔진이 스스로 막은 것**부터 생긴다.

---

## 6. ★Jino 결정 — 「엔진이 켜진 뒤 스스로 하게 두자」

풀려 있는 제외 7건(`live_state=missing`, 그중 `폴드보호필름`·`갤럭시폴드액정필름`은 **8/26~27에도 노출 관측** = 실제로 돈이 나감)을 **사람이 다시 막지 않는다.**

⇒ **엔진이 Jino와 같은 판단을 하는지가 첫 주의 시험**이 된다.

**시험 성립 확인** — 쇼핑 제외 게이트(클릭 ≥ 10 · 비용 ≥ 공헌이익 8,506원 · 구매 전환 0 · rolling 14일)에 걸리는 후보가 **QA 독립 실측 4건**:
`갤럭시폴드와이드`(78클릭) · `갤럭시와이드`(27) · `갤럭시폴드와이드8`(21) · `Z폴드와이드`(11) — **전부 기종 미특정 패턴**으로 Jino가 막으신 것과 같은 모양.

⚠️ **게이트는 기계적 조건이라 같은 결론이 나와도 같은 «이유»는 아닐 수 있다.** 「방향이 맞는가」까지만 잰다.

---

## 7. 다음 세션이 할 일 (순서 중요)

| | 무엇 | 행위 변화 |
|---|---|---|
| 1 | **prod 배포** — ⛔ 남의 트랙 미배포 마이그(`pgprice1s1a_cost_purchased_price.py`, 커밋 `a8d35c9f` D-CPP-63 S1 1/3, 소관 `sellc-원가-메뉴` n=18 `dc51780e` **종결**)가 `safe_deploy`에 막힘. **Jino ⓐ 결정으로 이번엔 건너뜀.** 대신 배포하지 않았다 | 0 |
| 2 | **제외 원장 정비** — 계정 32건 vs 원장 11건(21건 미귀속) · 쓰레기 행 4건(`4`·`5`·`6`·`7`, console_import 파싱 오류 추정) · `live_state=missing` 7건 | 0 |
| 3 | **`naver_adgroup_scope`에 폴드8와이드 1행** | 0 |
| 4 | **Jino §8-① 지정** — `auto_operate=1` · `optimizer='ours'` | **여기서부터 엔진이 움직인다** |

★**3을 4보다 «먼저»** — 순서가 뒤바뀌면 스코프 0행이라 `blocked_by_scope`가 아무것도 안 막아 **58그룹이 전부 열린다**.

**P4(지혜 성적표)**: 이미 prod 라이브(`wisdom_scorecard.py` + API + 콘솔 섹션4). 남은 건 표면 지정·프론트 렌더 확인. ⚠️병행 세션 소관 가능성 있어 **무접촉**.

---

## 8. ⚠️ 이번 세션의 실수·함정

1. ★**오케스트레이션 실수** — 서브에이전트 둘이 같은 파일에 변이를 **병행 주입**(첫 기가 다른 기에 넘긴 것을 모르고 둘째를 띄웠다). 최종 `grep MUTATION` 0건·스위트 그린·리뷰어 잔재 0건으로 수습. ⇒ **같은 파일을 만지는 위임은 하나만 띄운다.**
2. ★**위임이 「전달했다」로 끝났다** — 첫 에이전트가 결과 없이 「다른 에이전트에 넘겼다」고 보고. **「하겠다」는 「했다」가 아니다** — 직접 재서 확인했다(n=65 교훈의 재현).
3. ⚠️**훅 오탐 1건**(계약 §6 계수 대상) — `review-surface-mutation.sh`가 **완료 QA 위임문**을 적대 리뷰로 오인(위임문에 실린 「적대 리뷰 1R→2R」 좌표 문자열에 걸림). 재전송으로 통과.
4. ⚠️**UTC 창 함정** — `date('now','-28 day')`로 검색어를 조회했다가 「게이트 통과 후보 0건」이 나왔다. 리터럴 날짜로 다시 재니 12건 이상. **`now`는 UTC다.**
5. ★**자정 경계 flaky 발견**(↗️ 스코프 밖 — 기록만) — `backend/tests/test_naver_ops_date_range.py`가 **00:06 KST에 돈 CI에서 실패**했다(`{'from': '2026-08-28'} != {'from': '2026-08-29'}`). 원인: 모듈 상수 `TODAY`는 **임포트 시점**에 고정되는데 `sales_summary(days=0)`은 **호출 시점**에 「오늘」을 다시 계산한다 ⇒ 그 사이 자정을 넘으면 갈라진다. **제 변경과 무관**하다는 근거 셋: ①같은 코드로 #560(23:1x)·#561(23:4x)이 CI 3/3 통과 ②#562 diff는 등록부 JSONL **1줄** ③실패 시각이 자정 6분 뒤. **`--force` 쓰지 않고 재실행** → 3/3 통과로 진단 확인(자정 지난 뒤라 통과). ⚠️**매일 자정 전후 CI는 재발한다.** 소관: 그 테스트 파일의 원 작성 트랙. ★n=64가 잡은 「픽스처의 잠복 **요일** 의존」(주말에 돌리면 전건 램프업)의 사촌이다 — 이건 **시각** 의존.
6. ⚠️**낡은 주석이 코드 판단을 다섯 번 틀리게 했다**(§4) — 2R 리뷰어가 마지막 nit으로 **또 낡은 주석 하나**를 잡았다(제거된 조기 필터를 가리킴). 고쳤다.

---

## 9. 착지

- 커밋 5건: `5e0e408e`(D-NAO-270 P3) · `1b495681`(제외 쓰기 경고 자체 정정) · `794d6780`(D-NAO-271 코드) · `bdb5d46f`(리뷰 P1-1 상환) · `d3f6e057`(2R nit)
- **PR #560 머지** — CI backend py3.10 **12m15s** · py3.14 **10m39s** · frontend **1m21s** 전건 pass, **`--force` 미사용**
- **prod 배포 없음** (Jino ⓐ 결정 — §7-1)
- **광고계정 쓰기 0 · `auto_operate`/`optimizer` 변경 0 · 스코프 행 0** — QA 라이브 확증(착수 18:21 이후 `naver_change_log`에 `flight_pacing` 24행뿐이고 **전건 `dry_run=1`**)
- 백엔드 스위트 **7,251 passed / 0 failed**(QA 독립 재실행 일치)
- L5로 「main에 세워둔다」 생략 — 다음 세션은 `git switch -c <새> origin/main`

## 10. 다음 가용 번호

**D-NAO-272** · 교훈 **#376**
