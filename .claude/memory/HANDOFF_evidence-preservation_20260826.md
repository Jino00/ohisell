# 세션 인수인계: 학습 사슬 증거보전 (D-NAO-251) — 「PAO 논의 54」
> 저장일시: 2026-08-26 10:1x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## §0. 궁극 목표 — 원문 (발명 금지)

**PAO 북극성 §1**: PAO의 궁극 목표는, 네이버 광고·스마트스토어 API가 주는 모든 지표와 4등급 성과등급 사이의 «개연성(연관)»을 홀드아웃 검증까지 통과한 지식으로 확정하고(①), 논문에서 채택한 기법과 이 트랙이 이미 세운 실행 구조(레인·가드레일·고삐·확장압력) 위에서(②), 우리가 운영하는 모든 광고 형태를 총이익 절대액이 최대가 되는 방향으로(③, D-NAO-59) 자동화 운영하며(④), 운영의 매 행위와 결과를 일기→반성→지혜 승격의 학습 사슬에 태워 어느 지점을 배우면 성능이 더 오르는지를 스스로 찾아, 신호가 허락하는 최단 주기로 성능개선을 반복 시도하는 것(⑤)이다.

**이어지는 작업의 목적 (Jino 원문 2026-08-25 09:20)**:
> *"나는 우리의 자료가 옵시디언, LLM wiki를 통해 학습이 되고 지혜로 올라간 뒤에 다시 그 학습된 지혜가 우리의 광고에 적용이 되었으면 해. 물론 광고에는 한가지 방법만 있는게 아니기 때문에 다양한 시도를 해봐야 해서 꼭 지혜로 올라온 방법만 사용되는건 안되겠지만, 최소한 우리가 지혜는 얻어야 발전이 있고 로직계선이 될거잖아?"*

## 1. 프로젝트 위치 및 환경
- 워크트리: `/Users/jino/.claude-worktrees/ohiselling/pao-n54` (브랜치 `feat/pao-n54`)
- prod: `sellc.ohitech.co.kr` · DB `/home/ubuntu/ohisell/backend/ohisell.db` · venv `/home/ubuntu/ohisell/backend/.venv/bin/python3`
- **API 활성 포트 `8011`** (이번 세션에 8011→8001→8011로 두 번 무중단 전환)
- 배포: `scripts/safe_deploy.sh <파일…> [--migrate] [--restart]` / 프론트 `--frontend`
- 병합: `scripts/safe_merge.sh <PR번호>` (CI 대기 상한 `MERGE_WAIT`)
- ⚠️ 이 워크트리의 `frontend/node_modules`는 `pao-n52` 워크트리로의 **심볼릭 링크**(정상, gitignore 대상)

## 2. 이번 세션 완료 목록
- ✅ **계약 D-NAO-251 「증거보전 목표」** — `docs/contracts/CONTRACT_evidence_preservation.md` (Jino 06:43 «승인»)
- ✅ `backend/app/models.py` — `OpsWisdomCandidate`에 4컬럼(`judged_at`·`judged_occurrences`·`rejudge_count`·`prior_judgments_json`)
- ✅ `backend/alembic/versions/ev1preserve51_*.py` — 마이그 + 백필 2건
- ✅ `backend/app/services/naver_ad/wisdom_candidates.py` — `_TERMINAL_STATUSES` 분리(promoted만) · `_reopen_ready()` · 신규 카운터 4종
- ✅ `backend/app/services/naver_ad/wisdom_judge.py` — `no_action` 카운터 · 대기열 제외 · 적응형 상한 · 판정 스냅샷·이력 · `_prior_judgments_view()`
- ✅ `backend/app/services/naver_ad/wisdom_scorecard.py` — `_judge_backlog()` · `_no_action_status()` · 후보 행 재개방 6필드
- ✅ `backend/app/services/scheduler_service.py` — 크론이 harvest 7키·judge 5키 로깅
- ✅ `frontend/src/lib/api.ts` · `NaverAdOptimizationConsole.tsx` — 타입 3종 + 렌더 블록 3개
- ✅ 테스트 신규 **+84건**(백엔드 6,614→**6,698** · 프론트 887→**901**) — 최종값은 완료 QA가 직접 실행해 확인(6,698 passed / 0 failed, 271.92초)
- ✅ 적대 리뷰 보고서 `docs/reviews/REVIEW_evidence_preservation_20260826.md` (1R·2R·#462 3라운드 전부)

## 2-1. 완료 QA (판정 원문 그대로 — 미달·판정불능 포함)

- **작업 목적(정본 원문)**: §0 참조. 계약 `docs/contracts/CONTRACT_evidence_preservation.md` (D-NAO-251)
- **판정(계약 §5)**: **부분달성** — 8항목 중 **달성 6 · 판정불능 2 · 미달 0**. 닫히지 않은 둘은 결함이 아니라 **관측 시점**이다 (2026-08-26 10:0x KST)
- **판정(Jino 지시 원문)**: **달성** — 23:33 «2번부터 하자» · 06:43 «승인» · 08:58 «08:45 지나면 관측하고 배포해» 세 지시가 시간순으로 이행됨 (10:0x KST)
- **판정(북극성 §1·§6)**: **달성** — 진행률 2/7 불변, ④자동화 불변, §7 무접촉 자기판정이 실측과 일치 (10:0x KST)
- **종합 판정: 부분달성**

### 항목별 (재판정 반영)
| # | 항목 | 5값 |
|---|---|---|
| ①-a | rejected 재개방 로직 테스트 | 달성 |
| **①-b** | 카운터가 harvest totals·크론 로그에 실재 | **판정불능** — 코드·테스트는 잠겼으나 **라이브 크론이 아직 안 돌았다**(pm2 전수 확인, 다음 발화 08-27 08:45) |
| **①-c** | 재심 판정문 이력에 이전 판정·표본 변화 | **판정불능** — 재심 이벤트 자체가 없어 **관측 대상이 존재하지 않는다** |
| ②-a | `no_action`·`skipped_no_action` 테스트 | 달성 |
| ②-b | id 45 hidden+사유 + `no_action` 키 응답 존재 | **달성**(1차 부분달성 → 상환 후 재판정) |
| ③-a | 적응형 상한 테스트 | 달성 |
| ③-b | 백로그 지표 관측 | 달성 (단서: `pending_ripe=2`라 「ripe 6건 이상 처리」 분기는 당분간 트리거 불가 — «또는» 분기가 약한 기준임을 병기) |
| 공통 | `--migrate` 순서 · 광고계정 쓰기 0 · 리뷰 PASS | 달성 |

### ★QA가 잡은 것 (이 세션 최대 발견)
계약 §5 ②-b가 「`no_action` 키가 **0이어도 응답에 존재**」라 명시했는데, 그 카운터는 `_sibling_buckets`(판사 프롬프트)와 `harvest_candidates()`의 **휘발성 totals**에만 있었다. **응답엔 없었다.**
**적대 리뷰 2R도 못 잡았다** — SUR-2가 `_sibling_buckets` **단위**만 겨눴고 그 값이 응답까지 닿는지는 검사하지 않았다.
★그리고 **같은 세션에서 같은 모양을 두 번** 밟았다: 1R P1도 계약 §5 ③-b가 콘솔을 표면으로 지목했는데 PR에 프론트엔드 파일이 **0개**였다.
⇒ 교훈 **#362** 「만드는 층과 닿는 층은 다른 층이다 — 합격기준이 지목한 것은 늘 닿는 층이다」

- **목적 전환 여부**: **없음**. 계약 §1 「안 함」 경계 침범 0건(점화·생성층 수리·promoted 강등·observe outcome 배선·판사 판정 로직 개정·크론 주기 변경 전부 미접촉)

## 2-2. 트랙 진행률
- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: "무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야." (Jino 2026-07-19, D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음(M 단위라 이 계약이 닫혀도 체크박스가 안 움직인다)**. 이 계약은 M3 **인접**(재는 층의 무결성)이지 M3 자체가 아니다.
- ⚠️★**진행률 계수 함정** — 순진하게 `- [` 를 세면 **11**이 나온다. `확인:` 줄 본문에 그 문자열이 **4회** 섞여 있기 때문이다. **줄머리(`line.startswith('- [')`) 기준으로 세야 7**이다. 그대로 쓰면 2/11로 보고되고 **트랙이 원리적으로 종결 불가**가 된다.
- **헤더에 남긴 확인 줄**: `확인: 2026-08-26 10:0x KST [0df93448] — …` (전문은 트랙 파일)
- **다음 세션 후보 항목**: M3(§5-3 ②는 M2-d 게이트 **2026-08-28** 이후) / M4(§8-① Jino 결정)
- **트랙 종결 여부**: **미도달**(2/7)

### 북극성 대조 (D-NAO-226·227)
① M: **M3 인접**. 새 M 안 엶 ② 5요소: **⑤만** 움직임 — **④자동화 26세션째 0** ③ §7 대칭: 판정면·집행면 **무접촉**(지혜 2건 여전히 둘 다 브레이크 방향) ④ 관련 절 정독: §목차 → §5-1·5-2·5-3·§6 M3/M5행·§6-b M3/M5·§7·§8

## 2-3. 착지
(Step 6에서 채움)

## 3. 확정된 결정사항
- **D-NAO-251** 「증거보전 목표」 계약 승인(Jino 2026-08-26 06:43 «승인»)
- 재개방 조건 = `occurrences ≥ judged_occurrences × 2.0 ∧ ≥ +5`, 재심 상한 **2회**. **promoted는 재개방 대상 아님**(승격↔기각 플립플롭이 브리핑 주입을 흔든다)
- `action=None`을 형제 매칭 **값으로 쓰지 않는다**(미상끼리 묶으면 가짜 대조군 → 학습 오염). 카운터만
- 판사 **주기는 불변**(08:45 1일 1회) — 적체 시 회차 상한만 5→15(북극성 §5-2 「주기를 부풀리는 것은 5,403배 오류와 같은 부류」)
- 새 상수 4개(2배·+5·캡 15·재심 2회)는 **무근거 초깃값**임을 계약 §2-6·§8-3이 자백 — 주기 감사 재심 안건
- `judge_verdict_json`의 **형태는 안 바꾼다**(`wisdom_writer.py:51`·`wisdom_apply.py:72`가 의존) — 이력은 별도 컬럼

## 4. 핵심 파일 목록
| 파일 | 역할 |
|---|---|
| `docs/contracts/CONTRACT_evidence_preservation.md` | 계약 정본(D-NAO-251) |
| `docs/reviews/REVIEW_evidence_preservation_20260826.md` | 적대 리뷰 1R·2R·#462 |
| `backend/app/services/naver_ad/wisdom_candidates.py` | 수확·재개방(`_reopen_ready`) |
| `backend/app/services/naver_ad/wisdom_judge.py` | 판사·적응형 상한·판정 스냅샷 |
| `backend/app/services/naver_ad/wisdom_scorecard.py` | 관측 표면(`_judge_backlog`·`_no_action_status`) |
| `frontend/src/pages/NaverAdOptimizationConsole.tsx` | 콘솔 렌더(판사 대기열·재개방·action 미상) |
| `docs/references/82_pao_north_star_20260819.md` | 북극성(§5-2·§5-3·§6-b·§7·§8) |

## 5. 알려진 이슈 / 주의사항
- ★★**재료 소멸 시계**: `_HARVEST_LOOKBACK_DAYS=90`(`wisdom_candidates.py:69`) + `blocked` 일기 최종 **2026-07-30** ⇒ **2026-10-28에 3,863건 전량이 수확 창 이탈**(08-26 기준 **64일**). 이 수리가 여는 문으로 들어올 **재고 자체가 시한부**다.
- ★**08:45 A3 관측**(구코드로 실행 — 배포는 09:00:37이라 무오염):
  - **id 30** 판정문이 대조군을 실제 인용(*"주말 승률 0.824는 평일 대조군 0.803과 거의 동일"* · *"holiday 대조군(0/4=0%)"*) ⇒ **D-NAO-250 수리 라이브 작동**
  - **id 45**(11건 전승) *"액션이 null(미상)이므로"* 기각 ⇒ 이 계약 ②가 겨눈 결함이 판사 슬롯을 태우고 있었다
  - **id 44(19/0)·38(16/0)·46(7/0)** *"days_since_first_seen=0이므로 시간적 재현성도 확인되지 않았다"* 기각 ⇒ ①의 함정 **오늘 5번 더 발생**
  - 배포 후 재개방 문턱: id **30→34** · **38→32** · **44→38** · **45→22** · **46→14**회
- **머지 ≠ 배포** — PR #461 머지 `2026-08-25T22:43:26Z`(=07:43 KST)이나 prod `wisdom_judge.py` mtime `2026-08-26 00:00:37 UTC`(=**09:00:37 KST**). 08:45 크론은 구코드로 돌았고 그게 의도한 순서였다.
- **제안 #6073 여전히 pending**(08-25 07:45:46부터) — B1~B4·B6 다섯 항목이 이 클릭 하나에 매달려 있다
- ⚠️ 훅 오탐: `[체인] ⛔` 2건(pao n=49·sellc n=2) — 공유 메인 폴더가 origin/main보다 **426커밋** stale
- ⚠️★**훅이 못 본 반대 사례**: n=53(세션 `56a183b0`)은 등록부 행이 **미커밋**이라 origin/main에도 훅에도 안 보였다. **워크트리 직접 확인으로만** 잡혔다(Jino 지시로 인수)

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)**: §0의 Jino 2026-08-25 09:20 원문. 계약 D-NAO-251은 이 세션에 닫혔고, 남은 슬라이스는 아래.
- **남은 슬라이스**:
- [ ] **①-b·①-c 익일 관측** — **2026-08-27 08:45** 크론 이후. 볼 것: pm2 로그에 `harvest scanned=… rejected_tally_resumed=… reopened=…` 줄이 실재하는가 / 재심이 발생했으면 `prior_judgments_json`에 이전 판정이 쌓였는가. **묻지 말고 진행**(관측만, 상태 변경 0)
- [ ] **제안 #6073** — 여전히 pending. 계약 §5가 「캘린더 시간은 Jino 가용성 종속(계약이 재촉하지 않는다)」으로 처분한 자리다. 착수 시 `SELECT status FROM naver_proposals WHERE id=6073` 확인하고, 여전히 pending이면 **안내 1줄만** 하고 다른 슬라이스로. **묻지 말고 진행**
- [ ] **M3-c(확정 지식 주입)** — M2-d 게이트 **2026-08-28** 이후 열린다. 계약은 **이미 승인**돼 있다(D-NAO-224, `docs/PLAN_naver-m3-wisdom-scorecard.md`). Fable 권고: A/B 5건을 **코드 상수 레지스트리**(`guardrail_params.SPECS` 전례)로, A#8을 첫 행. 문서 런타임 파싱(안A)·DB 테이블(안B)은 기각. **묻지 말고 진행**
- [ ] **점화(D-NAO-247)** — 게이트는 북극성 **§8-① Jino 결정**(optimizer 해제 범위·시점) 하나. **돈이 나가는 결정이라 물어야 한다**(전역 §1 승인 지점 ②)
- [ ] **이월 P2-6** — 한 요청에서 `OpsWisdomCandidate`를 3회 `.all()` 스캔(`_candidate_status`·`_no_action_status`·`_judge_backlog`). 현재 규모(수십~수백 행) 무해, 패널 추가 시 누적. 소관: 주기 감사

### ★볼트 학습 입력 3안 (Jino 08-25 23:30 지시로 산출 — 별건, 구현 0)
Fable 권고 **안2 «가설 카드»**: Jino가 볼트 `inbox/`에 규격 frontmatter(`type: hypothesis`·`grain`·`action`·`condition`) 노트를 적으면 `ops_wisdom_candidates`에 `h|` 접두 pending 후보로 등록되고, **승격 판정은 여전히 일기 증거와 판사 1기가 한다**. 사람은 «어디를 잴지»를 등록하고 답은 증거가 낸다 — 자(尺) 충돌을 원리로 해소.
- 기각: 안1(관측 노트 — 기록층까지라 지혜로 못 올라감) · 안3(지식 버스 — decree가 판사 우회 문을 열고 무승률 지혜가 승률 지혜와 같은 큐를 점유)
- 공통 신설 부품: **Mac→VM push 채널**(`inbox/` 존만, 0바이트 push 거부). ★**볼트 정본은 Mac이 아니라 VM**(`scripts/ohisell_vault_pull.py:31`)
- ★Fable 자평: *"세 안 전부 사슬의 «입력»을 늘릴 뿐, 실집행 0이라 가설 카드조차 증거를 못 먹는다"* — 가장 함정에 가까운 것은 안3

### 낡음 검사 결과 (Step 2-B)
- 인계받은 n=52의 「다음 가용 교훈 #358」은 **오기**였다(#358은 n=52 자신이 실부여). 실측 정답은 **#362**였다 — ★착수 시점 실측은 #359였으나 그 사이 **발주예측 체인 n=4·n=5가 #359·#360·#361을 가져갔다**(같은 날 병행 세션). 기록 직전 `next_ids.sh` 재실행이 잡았다 — **번호 함정 7회째 회피**. 이 세션 부여 = **교훈 #362**. **다음 가용 D-NAO-253 · 교훈 #363**
- QA 이월 3건 중 2건은 **이미 상환됨** → 유령으로 넘기지 않는다:
  - [x] ~~P2-5 `unresolved` 비대칭 픽스처~~ **완료** — 커밋 `3be2a642`, 변이 재주입 1 failed → 원복 3 passed
  - [x] ~~「#461이 07:43 머지됐는데 왜 45가 08:45 판사에게 갔나」~~ **해소** — 머지 ≠ 배포(prod mtime 09:00:37 KST)
- 열린 PR: `gh pr list --state open` 확인 결과 이 세션 소산 0건(#461·#462 둘 다 머지 완료)

## 7. 새 세션 시작 프롬프트
```
/session-relay PAO 논의
```
