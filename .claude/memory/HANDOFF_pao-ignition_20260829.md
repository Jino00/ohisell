# HANDOFF — PAO 카나리 «점화» 실행 + 흑자 산술 규명 (2026-08-29, 세션 4ef8503e / 체인 PAO 논의 68)

## 0. 작업 목적 (원문 — 발명하지 말고 그대로 옮길 것)

계약 `docs/contracts/CONTRACT_pao_ignition_canary.md` (**D-NAO-247**, Jino 승인 2026-08-25 00:33 「승인해.」). **목표이름: 점화 목표**. 북극성 **M4** 슬라이스.

> **목표**: 07-30 10:48 이후 **26일째 0건**인 우리 실집행을 재개한다. 지금은 `auto_operate=0`(9캠페인 전건)·`optimizer='none'`이라 auto-lane 순회가 0회 돌고(`auto_operator.py:1169-1172`), 일기 사슬도 `blocked`가 07-30에서 끊겨 학습이 멈춰 있다. 이 계약이 끝나면: **Jino가 승인한 카나리 캠페인에서 우리 엔진이 실집행하고, 그 행위·결과가 일기→후보→지혜 사슬에 다시 실리며, 성적이 구간 자 병기로 화면에 보인다.**

이번 세션 몫: 인계 §7이 남긴 **Jino 답변 대기 2건**을 실측으로 답하고 처분 → **점화 실행 + 직후 관측**.

---

## 1-0. 완료 QA — 판정 원문 그대로 (별도 Sonnet · 읽기 전용 · 4대조 · 2026-08-29 14:3x KST)

<!-- QA-VERDICT-START -->
**종합 판정: 부분달성**

```
판정(계약 §4): 달성 — 선행 P2 최종 지정·카나리 C1(점화 상태) 체크박스가 라이브 DB로 전건 재현됨(campaign_settings 9행 대조, scope 1행, 되돌리기 경로 존재). C2~C7은 계약이 요구하는 7일 창 미도래로 정직하게 미체크 상태이며 이는 계약 §4 자체가 예정한 정상 상태다. 다만 C3 보조 서술 중 "change_log 새 행 1개뿐"은 실측(10개, 그중 9개는 무관한 flight_pacing)과 어긋난다 — 안전 결론(스코프 밖 실쓰기 0)은 유지되나 증거 진술의 정확도에는 흠이 있다. (2026-08-29 KST)
판정(Jino 지시 원문): 달성 — ①「켜라」는 auto_operate/optimizer 스위치가 정확히 카나리 1개에만, 다른 8개는 불변으로 실행됨(DB 전건 대조). ②「보류한다」는 naver_search_term_exclusion(source='console_import') 표본 전건의 updated_at이 08-28 이전이며 이번 세션 중 어떤 변경도 없음을 확인 — 지시대로 손대지 않았다. ③④ 질문에는 BEP(8502.88)·factor(0.827)·14일 비용(538,477≈538,478) 등 핵심 입력값이 실측과 정확히 일치하는 근거로 답변이 제출됐다. (2026-08-29 KST)
판정(트랙 헤더 합격 M항목): 부분달성 — 실제 진행률 「2/7」은 TRACK-CONTRACT 블록 내용 대조 및 실제 설치된 track-progress-sync.sh의 재실행으로 정확히 재현됨(달성 항목). 그러나 이월에 적힌 「기계 계수 4/19 오염, M0~M6 다 닫아도 7/19에 못 닿는다」는 실제 프로덕션 훅(줄머리 앵커 사용)을 직접 재실행한 결과 재현되지 않았다 — 진행률 자체 보고는 정확하나, 그에 곁들인 구조적 결함 진단(이월 항목)은 과장/오독이다. (2026-08-29 KST)
판정(북극성 5요소): 부분달성 — ④자동화 운영이 2026-07-30 이후 처음 움직였다는 핵심 주장은 naver_campaign_settings.updated_at(2026-08-29 03:53:11 UTC) 및 최초 입찰 제안 17건(07-30 이후 최초, DB 재현 완료)으로 명확히 검증된다. ⑤학습은 저자 스스로 "아직 0(재료는 실집행 뒤)"이라 인정했고, blocked 일기 16건이 그 재료라는 서술도 정합적이다. ①②③은 이번 세션에서 실질적으로 이동하지 않았다 — 저자 자신의 자백과 일치하는 정직한 이동. (2026-08-29 KST)
```

**저자가 주장했으나 재현 안 된 것 — 2건, 둘 다 이 세션에서 정정 완료**
1. 「점화 이후 새 change_log는 #7669 하나뿐」 → 실측 10행. 원인은 **시점을 안 밝힌 단정형 서술**(내 관측은 13:21). 안전 결론은 유지.
2. 이월 「진행률 계수기 4/19 오염」 → **훅은 줄머리 앵커라 정확히 2/7**. 반증 수용·이월 취소(§5).

**QA가 확인하지 못한 것**: ①C5 창별 필요 칸 수(189/71/19/5) 산식 재현 — 기반 입력값(BEP·factor·14일 비용)은 독립 재현했으나 **산식 좌표를 위임문에 안 줘서** 미재현(판정불능. ★내 위임문 작성 실패다) ②콘솔 **프론트 표면 렌더**(DB·백엔드 함수만 확인, 브라우저 미확인) ③보류한 4행의 개별 신원 대조 ④번호 부여·훅 오탐(대조 대상 밖)
<!-- QA-VERDICT-END -->

★**QA가 잡은 것이 이 세션 최대 산출물 중 하나다** — 내가 「구조적 결함 발견」이라 적은 것이 **내 계수 방식의 오류**였고, 그걸 잡은 건 훅도 자기검증도 아니라 **별도 기의 재현**이었다.

---

## 1. ★★ 이번 세션의 본체 — 점화했다

**2026-08-29 12:53 KST. 07-30 10:48 이후 30일 만의 실집행 재개.**

Jino 지시 원문(13:0x, 선택지 제시 후): ***"켜라 (`auto_operate=1`·`optimizer='ours'`)"***

| 스위치 | 경로 | 감사 기록 |
|---|---|---|
| `optimizer='ours'` | API `PUT /api/naver/ad/campaign-settings/optimizer` (HTTP 200) | ✅ `optimizer_change` change_log |
| `auto_operate=1` | **API 경로 부재 → 직접 UPDATE** (`routers/naver_ad.py:908`이 명시) | ❌ 없음 — `settings.updated_at` + 이 문서 |

- 영향 행 수 **1** 확인 후 커밋(≠1이면 롤백하도록 작성)
- 검산: `auto_operate=1` **1개** · `optimizer≠'none'` **1개** · 나머지 **8개 불변**(before/after 9행 전건 대조) · 스코프 행 **1행**(초과 0)
- 대상: `cmp-a001-02-000000008425541`(지문방지필름 폴드8) / 그룹 `grp-a001-02-000000070523564`
- **되돌리기 1줄**: `UPDATE naver_campaign_settings SET auto_operate=0 WHERE campaign_id='cmp-a001-02-000000008425541';`

**사전 게이트 (점화 전 실측)**
- `ignition_preflight.check` 9캠페인 중 **카나리만 `safe_to_ignite: true`** — 나머지 8은 전건 `scope_empty`(*"「행이 없음」은 「아무것도 안 열림」이 아니라 전부 열림"*)
- §3 금지선 `memo = None`(실험 배치 표기 없음)
- 부품 생존 `scripts/ignition_parts_alive.py` **prod에서** 실행(로컬은 4KB 미끼 DB라 못 돈다) — S2 원장 3,990 · S4 SPECS 7종 · S5 소재 692 · S6 groups 1013 전항 정상

**표면 확인**(앵커 `표면:` 항목): roster API가 `auto_operate=True`·`optimizer=ours`·`in_scope=True`를 실제로 실어 보낸다.

---

## 2. ★★ Jino 질문 — 「적자를 흑자로 돌릴 수 있어? 돈을 안 쓰는 게 아니고 광고 운영을 잘해서」

### 2-1. 인계 결론이 뒤집혔다 (숫자 셋)

인계(n=67)는 *「제외만으로는 흑자 불가 — 59칸 다 써도 −48,874원」*이었다. **셋 다 정정된다.**

| 인계의 주장 | 라이브 실측 |
|---|---|
| 제외 **59칸 여유** | ❌ 그건 **원장** 기준. 라이브 `restrict_keyword_count` = **33/70** ⇒ **여유 37칸**. 원장 11 ↔ 라이브 33의 차 **22칸이 「우리가 모르는 남의 칸」** |
| 전환 0 검색어 381개 | 창 차이(1일)로 재현. 07-30~08-28 기준 **354개(비용>0)·947,026원** |
| **흑자 불가** | ❌ **창에 전적으로 달렸다** — 아래 표 |

### 2-2. ★ 창을 안 밝히면 답이 뒤집히는 질문이었다

흑자에 필요한 제외 칸 수:

| 창 | 있는 그대로 | **하한(C5의 자)** |
|---|---:|---:|
| n=67의 창 (07-29~08-27) | 78칸 (불가) | — |
| 30일 (07-30~08-28) | **37칸** | **189칸 — 70칸으로도 불가** |
| 최근 15일 | **28칸** | **71칸 — 70칸으로도 불가**(아슬) |
| 최근 7일 | **9칸** | **19칸 ✅** |
| 8/24 대행사 인하 이후 | **3칸** | **5칸 ✅** |

**갈린 원인은 단 하나** — 폴드8 출시 직후 **07-27~29 사흘에 광고비 741,798원**(전환 25건)이 몰렸고, 그 사흘이 창에 드느냐 나가느냐다. 일별 추이: 07-27 199,568 / 07-28 300,484 / 07-29 241,746 → 08-25 이후 1~2만원대.

⇒ **n=67도 나도 계산 실수를 한 게 아니다.** memory `coverage-claims-need-their-window`의 교과서 사례.

### 2-3. ⚠️★ 내가 Jino께 처음 드린 답을 정정했다

처음 제시한 「9~28칸이면 흑자」는 **있는 그대로** 자였다. **C5·D-NAO-230 각주가 요구하는 하한 자로는 30일·15일 창에서 70칸으로도 불가**다. Jino 결정 «뒤»에 스스로 발견해 즉시 정정 보고했다(결정을 되돌릴 근거는 아니다 — M4 합격에 총이익 항이 없다).

**정직한 결론**: **「하한 자로 흑자」는 «최근 7일 국면이 유지될 때»에만 성립**하고, 그 국면의 낮은 비용 자체가 **8/24 대행사 입찰 −41.7% 인하의 결과**다(=이미 브레이크가 걸린 뒤의 숫자). 볼륨이 7월 말 수준으로 돌아오면 **제외만으로는 하한 자 흑자가 불가능**하다.

### 2-4. 레버 셋과 관할

| 레버 | 크기 | 엔진 관할 |
|---|---|---|
| 검색어 제외 | 최근 국면 하한 자 **19칸** | ✅ 자동 발사 |
| **상품명** | 「와이드」 있고 「8」 없는 전환 0 검색어 **32개 · 234,534원** = 전환 0 비용의 **24.8%**. 최상위 `갤럭시폴드와이드` 119,323원(84클릭·전환 0) | ❌ 커머스 축 — **1주 «후»**(동시 변경 시 효과 교락) |
| 입찰 | 대행사가 쥐고 있음 — **08-29 07:37에도 만짐**(`external_bid_change` #7641) | ⚠️ 아래 §3 |

### 2-5. Jino 「진행하면서 운영해야 하나」에 대한 답

신규 전환 0 검색어 유입: 주당 **195 → 140 → 77 → 30 → 13**. 제외는 **트레드밀이 맞다**(칸은 저절로 안 비고 새 쓰레기는 온다). 단 속도가 줄어 지금은 70칸으로 감당 가능. ⚠️**이 감소는 광고비 급감과 교락**돼 있다(클릭이 줄어 검색어가 덜 보이는 것일 수 있다).

**답: 켜야만 하는 건 아니다 — 그러나 지금은 같이 하는 게 이득이다.** ①사람이 먼저 제외를 다 걸면 **카나리의 첫 주 시험(「엔진이 Jino가 7월에 막은 것과 같은 판단을 하는가」)이 성립하지 않는다** ②「사람이 하자」는 지난 30일에 이미 시험됐고 결과가 −495,438원이다.

---

## 3. ★★ 점화 «전»에 코드로 확인한 액셀·브레이크 비대칭 (북극성 §7 검사)

두 층을 갈라야 답이 맞다:

- **생성(카드를 만드는가) = 대칭.** `AD_BID_ROUTING_ENABLED = True`(`auto_operator.py:204`)라 `_ad_bid_routing_open()`이 전 캠페인 True · `_AD_BID_CANARY_PROPOSAL_TYPES = {"bid_down","bid_up"}` ⇒ 상·하향 카드 둘 다 생성.
- **자동 발사(사람 없이 쏘는가) = 비대칭.** `delegation_gate`가 `target_type == "ad"`를 **무조건** 거른다(`ad_confirm_only`, D-NAO-5). 쇼핑은 키워드 엔티티가 원리적으로 없어 입찰이 **소재(`ad`) 단위**(D-NAO-255) ⇒ **입찰은 상·하향 통째로 콘솔 Confirm 대기.** 검색어 제외는 `target_type='search_term_semantic'`이라 걸리지 않아 **자동 발사된다.**

⇒ **엔진이 스스로 쏘는 것은 제외(브레이크)뿐.** 첫 주 행위 분포는 **브레이크 100%** 예상.

★**단 이번 대상에 한해 브레이크가 곧 정답이다**(흑자 경로가 제외라고 산술이 말한다). **「비대칭이라 위험」과 「이번엔 그 방향이 맞다」가 동시에 참**이므로 C6에서 섞지 않는다 — 분포는 사실대로 적고 「대칭이 회복됐다」고는 쓰지 않는다.

⚠️ **M4 합격 관측에 총이익 항이 없어 제외만 돌아도 M4는 통과한다** — D-NAO-230 각주가 경계한 「자가 부풀면 통과」의 **사촌**이다(「브레이크만 돌아도 통과」). 그 구멍을 메우는 유일한 항이 **C5**다.

---

## 4. 안 한 것과 그 근거

- **쓰레기 제외 4칸 회수 — Jino 「보류한다」.** 승인을 받아 대상을 특정하다 **전제 둘이 무너진 것**을 발견해 되돌려 보고했다: ①이유 소멸(여유 37칸이면 4칸 불요) ②그 4행은 `source='console_import'` = **대행사 축적분**이고 `exclusion_slot_usage.py:60` `RECLAIM_NOTE`가 *「대행사 칸은 우리가 반납하지 않는다 — 소유권 분리 협의(Jino 몫) 전엔 금지선」*으로 이미 금지.
- **제외 원장 «전면» 정비** — 계약 선행 항목이 아니고 창 미확정(n=67 판정 그대로 유지).
- **상품명** — 커머스 축·계약 범위 밖. 그리고 **카나리와 동시에 바꾸면 엔진 효과와 교락**(D-NAO-270 대조군 설계와 같은 이유).
- **prod 코드 배포 0** — 앱 코드 0줄.

---

## 5. 트랙 진행률 (전역 §6 의무)

- 트랙: `docs/tracks/active/track_naver-ad-optimization.md`
- **시작 2/7 → 종료 2/7 (불변)** — M4는 카나리 창 **7일 연속** 관측으로만 처분된다. 점화 당일 체크 이동 0이 **정상**이다.
- **계약 §4 선행 2/3 → 3/3** (P2 체크 = Jino §8-① 지정) · **카나리 창 C1 체크**
- 트랙 목표 원문(Jino 2026-07-19, D-NAO-59):
  > "무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."

### ⚠️★ 「진행률 계수기 4/19 오염」은 **내가 틀린 것**이다 — 완료 QA가 반증, 재현으로 확인 (이월 취소)

세션 도중 「기계 계수가 4/19라 트랙이 원리적으로 종결 불가」라고 적었다. **거짓이다.**

- 실제 설치된 훅 `~/.claude/hooks/track-progress-sync.sh:72-73`은 **줄머리 앵커**로 센다: `grep -c '^[[:space:]]*-[[:space:]]*\[[xX]\]'`. 산문 속 `- [x]` 인용은 **안 걸린다.** 재현 결과 **N=2 · M=7** — 정확히 **2/7**.
- 내 `4/19`는 python `str.count('- [')`로 **문자열을 아무데서나** 센 값이고 **어떤 집행 메커니즘도 내지 않는 숫자**다.
- ★**교훈 — 같은 함정 재발**: 메모리 `compare-line-must-not-contain-paths`가 이미 *"판정을 지어내기 전에 **훅 계산을 재현해** 오탐부터 재라"*고 적어 뒀는데, 나는 **훅 파일을 안 열고 CLAUDE.md §6 문언을 순진하게 적용**했다. **「규칙 문언」과 「집행 코드」는 갈라질 수 있고 집행하는 쪽이 정본이다.**

**남는 진짜 함정(잠재적·소관: 하네스 문언)**: 블록 안 과거 `확인:` 줄 산문에 `- [ ]`·`- [x]` 문자열이 실제로 **12개** 있다. 훅은 안전하지만 **§6 문언은 「N = `- [x]` 개수」로만 적혀 있어** 그걸 문자 그대로 구현하는 다음 도구·다음 세션이 같은 오답을 낸다(내가 그랬다). 문언에 「줄머리」를 명시하면 사라진다.

---

## 6. 북극성 대비 검토 (D-NAO-226·227)

1. **M 귀속** = **M4**(L3 재개). 선행 P2·P3·P4 전건 종결 ⇒ **점화가 M4의 시작점**이다.
2. **5요소 중 이동**: ★**④자동화 운영이 29세션 연속 0에서 처음 움직였다** — 점화가 곧 ④의 착수다. ⑤학습은 아직 0(재료는 실집행 뒤에 생긴다 — C7 사슬).
3. **§7 대칭 검사**: ⚠️**비대칭 확인·기록**(§3). 이번 대상에선 브레이크가 정답이나 **일반화 금지**.
4. **관련 절 전독**: §목차(§0~§9+부록 A) 훑고 **§1·§4 간극3·§6(M4 행 + 6-b M4 안 함 + D-NAO-230 각주)·§7·§8-①②** 원문 확인.

---

## 5-a. ★★ 점화 직후 라이브 관측 — 엔진이 깨어났다 (13:20·14:20 두 레인)

**무인 발화 확인**: `run_naver_auto_operator_hourly` `last_run_at 2026-08-29 13:20:20.963993` / `14:20:19.994209` — 크론 슬롯 :20과 소수점까지 일치(n=63이 세운 무인 판정 기준) · `last_status: ok`.

**★엔진이 07-30 이후 처음으로 입찰 제안을 냈다 — 17건**(13:20에 9 · 14:20에 8). `bid_up 14 : bid_down 3`.

★★**시간대 판독을 두 번 뒤집은 끝에 확정**: `naver_proposals.created_at`은 **UTC**다. 결정적 증거는 이 캠페인의 입찰 제안 이력이 **`2026-07-30 01`시에서 곧장 `2026-08-29 04`시로 건너뛴다**는 것 — `07-30 01 UTC = 10:00 KST`이고 **PAO 실집행이 멈춘 시각이 07-30 10:48 KST**다. ⇒ `04`·`05`시 = **13:20·14:20 KST** = 점화 후 두 레인. (⚠️같은 테이블 `max(created_at) = 13:07`은 **KST로 쓰는 다른 writer**가 있다는 뜻 — 이 저장소의 혼재 관례가 또 나왔다. memory `sqlite-server-default-now-is-utc`.)

### ★안전 검증 — C3 성립

| 검사 | 관측 |
|---|---|
| `blocked_by_scope` 실측 | 스코프 그룹 `…070523564` → **False**(허용) / 나머지 전건 → **True** / `None`(캠페인 레벨=예산) → **True** |
| **스코프 밖 실쓰기** | **0건** — 점화 이후 새 `change_log` 중 쓰기성 행 0. ⚠️**정정(완료 QA 지적)**: 내가 「`optimizer_change` #7669 **하나뿐**」이라 단정했는데 그건 **관측 시점(13:21) 기준**이었고, QA 시점엔 **10행**(7669~7678)이었다. 늘어난 9행은 `run_naver_flight_loop` 예산 페이싱 텔레메트리(전건 `dry_run=1`·`before_value=None`)로 **스코프·입찰과 무관**하다 — 안전 결론은 그대로지만 **시점을 안 밝히고 단정형으로 쓴 것이 틀렸다** |
| `blocked` 일기 | **16건** — *「자동운영 스코프 밖(쓰기 직전 재확인)」*, `actor='hourly'`, `action='update_bid'` |

⇒ **C3(스코프 밖 쓰기 0) 성립.** 그리고 이 16건이 곧 **C7의 재료**다 — 계약이 *「`blocked`·`execute` 일기가 harvest에 집힌다」*고 한 그것이고, **학습 사슬이 07-30 이후 처음 재개**됐다.

### ⚠️★ 발견한 결함 — 「죽은 카드 방지」 가드가 안 먹는다

`auto_operator.py:363` 주석이 그 가드의 목적을 스스로 이렇게 밝힌다: *"레인이 스코프 밖 제안을 approved로 커밋한 뒤 harness가 거부해 status=approved인 채 실행 실패로 남는 걸 피하려는 것뿐이다."*
**정확히 그 상황이 16건 생겼다** — `status='approved'`·`approval_source='auto_op_hr'`인데 영원히 실행되지 않는다.

더 나쁜 것: **`real_write_blocker(p)`가 전건 `None`(차단 사유 없음)을 돌려준다.** 콘솔의 `executable` 표시가 이 값을 진실로 삼으므로(`naver_execution_harness.py` 주석) **화면은 이 16건을 「실행 가능」으로 보여준다.** 실제로는 실행 게이트(`harness.execute()` → `blocked_by_scope`)가 막는다.

- **손해는 0**(실쓰기 0·안전장치 정상) — 그러나 **값이 도는 층과 사람이 읽는 층이 갈렸다**(이 저장소의 반복 병).
- **누적 속도**: 시간당 8~9건 ⇒ 1주면 **약 1,400건**의 blocked 일기·죽은 approved 카드. **C7의 학습 재료를 희석할 수 있다** — 다음 세션이 harvest 결과를 볼 때 이 노이즈를 분리해야 한다.
- 이번 세션이 고치지 않은 이유: 코드 변경이고 점화 당일 스코프 밖이다. **다음 세션 1순위 후보.**

### 카나리 그룹 자신은 아직 0건

스코프 그룹 `…070523564`에 대한 제안은 **0건**이다. 정상이다 — 그 그룹의 레버는 **검색어 제외**인데 그건 시간당 레인이 아니라 **08:50 일 레인**이다(§6-a).

---

## 5-b. ★ 별건 — PAO 스코프 화면 표 정렬 결함 수리 (Jino 지적, 세션 후반)

Jino 원문: **「이거 칸 안맞잖아」**(prod `/naver-ad/scope` 캡처 첨부). 점화와 **별건**이고 **진행률 2/7 불변**.

**원인**: `AdgroupTable`이 `head`를 `<tr>`로 감싸 넘겼는데 `Table`(`components/ui/Table.tsx:28`)이 이미 `<thead><tr>{head}</tr></thead>`로 감싼다 ⇒ **`<tr><tr><th>…</th></tr></tr>` 중첩**.

★**깨지는 메커니즘 = CSS 익명 테이블 박스 생성 규칙**: `table-row`의 자식이 `table-cell`이 아니면 익명 셀로 감싸이므로 안쪽 `<tr>`의 `<th>`가 **본문과 같은 열 그리드에 참여하지 못한다** — 헤더는 왼쪽에 몰리고 숫자는 오른쪽으로 밀린다.

⚠️★**내 초판 설명 「브라우저 table fixup」은 틀렸고 적대 리뷰가 정정했다** — React는 `createElement`+`appendChild`로 DOM을 만들어 **HTML 문자열 파서를 안 타므로** 파서 fixup은 애초에 안 걸린다(리뷰어 실측: 스크립트로 만든 중첩 `<tr>`은 `outerHTML`에 **그대로 보존**). 결론·수리는 같지만 **다음 사람이 「React니까 안 걸리네」로 오판할 문장**이었다.

**범위 = 1곳뿐**: `Table head={...}` 호출부 **39곳 중 38곳은 이미 프래그먼트** `<>…</>`를 쓴다(리뷰어 전수 독립 재검증). **관례는 맞았고 이 화면만 어긋나 있었다.**

### ★★기존 표면 테스트 19개가 전부 초록인 채로 화면이 깨져 있었다

`paoScopeReachesTheUser.test.tsx`는 「**사용자에게 닿는가**」를 재려고 만든 파일인데도 **텍스트 존재만 묻고 구조를 안 봐서** 못 잡았다.
⇒ **「값이 화면에 있다」와 「사람이 읽을 수 있게 배치됐다」는 다른 질문이다.**

회귀 테스트 **SUR-11 2건** 추가 + **변이 주입 검증**(중첩 `<tr>` 복원 → 첫 테스트 사망).
⚠️ **둘째 테스트(칸 수 대조)는 이 변이에서 생존**한다 — jsdom은 개수가 여전히 9:9다. **지우지 않고 「무엇을 못 지키는지」를 테스트 파일에 그대로 적었다**(다른 실패 모드는 지키므로).

### 적대 리뷰 1R **PASS** (P1 0건)

리뷰어가 **실제 Chromium에 중첩 `<tr>` DOM을 만들어 렌더**해 증상을 독립 재현하고 수정본의 정렬 복구까지 확인했다 — 하니스 레벨에서 끝내지 않았다.

| 변이 | 결과 |
|---|---|
| M1 수리 되돌리기 | **죽음**(테스트1) / 테스트2 **생존** — 내 자백과 일치, 재현 확인 |
| M2 `<Th>` 1개 삭제 | **죽음**(테스트2) |
| M3 `Table.tsx` 래핑 제거 | **죽음** — 단 79파일 중 **이 1파일만** |
| M4 `<Td>` 1개 삭제 | **죽음**(테스트2) |

**P2 트리아지**: ①용어 부정확 → **채택**(커밋 `8fc42df3`) ②**이월** ↓

⚠️★**이월 — M3가 드러낸 것**: `Table.tsx`의 `<tr>` 래핑을 제거하면 **39개 화면이 전부 깨지는데 테스트는 79파일 중 1개만** 죽는다 ⇒ **나머지 38개 표엔 「헤더·본문이 같은 열에 서는가」 검사가 0건**이다. **`Table` 컴포넌트 레벨 공용 테스트 1개면 39곳을 한 번에 덮는다** — 이번 금지선(「다른 화면 건드리지 않음」)에 걸려 미착수. **다음 세션 후보.**

**검증**: tsc **0** · 프론트 **1,187 passed / 79 파일** · 커밋 `59299201`·`8fc42df3` · PR **#567**
**배포 전제**: prod 프론트 스탬프 = `a84906e8`(**원가메뉴 트랙 분**, 어제 22:07). **origin/main에 있고 내 HEAD의 조상** ⇒ **CAS 통과 예상**, 남의 작업이 지워지지 않는다. `--force-frontend` **미사용**.
⚠️ 스탬프 정본 경로는 `$REMOTE_REPO/.frontend-deploy-stamp`다 — `frontend/dist/.deploy-stamp`는 **legacy**이고 비어 있어서, 그걸 읽고 「스탬프 없음」이라 오판할 뻔했다.

---

## 6-a. ★ 엔진의 첫 실집행은 «내일 08:50»이다 — 그리고 무엇을 할지 미리 쟀다

**타이밍**: `run_search_term_ss_lane`(검색어 제외)은 **`run_naver_auto_operator_daily_job` 안**에 있고(`scheduler_service.py:991`) 그 잡은 **08:50**에 돈다. 시간당 레인(`run_naver_auto_operator_hourly`, 매시 :20)이 아니다.
⇒ **점화 당일(08-29) 시간당 레인에서는 제외가 나오지 않는다.** 첫 실집행 기회는 **2026-08-30 08:50 KST**.

**게이트 실값(정본)**: `search_term_judge._ss_params(db)` = **`(10, 14)`** = (min_click, window_days). ⚠️`_ss_params`는 `search_term_ss_lane`이 아니라 **`search_term_judge`**에 있다 — 모듈을 잘못 짚어 「prod가 배포보다 뒤인가」로 오해할 뻔했다(**아니다**, D-NAO-265 정상 반영).

**★내일 08:50 게이트 통과 예상 후보 4건**(rolling 14일 08-15~28 · 클릭≥10 · 전환 0 · 비용≥공헌이익 8,506.37 · 원장에 아직 없음):

| 검색어 | 클릭 | 14일 비용 | 전환 |
|---|---:|---:|---:|
| 갤럭시폴드와이드 | 58 | 81,000원 | 0 |
| 갤럭시폴드8와이드필름 | 28 | 36,020원 | 0 |
| 갤럭시와이드 | 21 | 29,402원 | 0 |
| 갤럭시폴드와이드8 | 20 | 25,599원 | 0 |

**4건 중 3건이 「기종 미특정」 패턴** = Jino가 7월에 콘솔에서 막으신 것과 같은 모양 ⇒ **첫 주의 시험이 성립한다.**
⚠️ 단 게이트는 **기계적 조건**이라 같은 결론이 나와도 같은 «이유»는 아닐 수 있다(n=66 QA가 남긴 단서 그대로).
⚠️ `갤럭시폴드8와이드필름`은 **가장 정확한 질의인데 28클릭 전환 0**이다(반면 `폴드8와이드필름`은 10전환). 「기종 미특정이라 안 팔린다」로 다 설명되지 않는 잔여가 있다 — 다음 세션의 관측 항목.

---

## 6-b. ★★ 판정 «후» 실측 부록 — 카나리의 실제 집행 밀도는 «하루 1회»다

Jino 질문(16:2x): ***「우리 광고에 대한 스크리닝은 내일 아침 한번 하고 그전이나 그 후에는 또 하루종일 아무것도 안하는거야?」***
⚠️ **코드·상태 변경 0 · 조회만** — 완료 QA 판정(§1-0)의 대상이 아니고 그 판정을 바꾸지 않는다.

**답: 엔진은 매시 «일»하지만, 이 그룹의 «자동 집행»은 하루 1회다. 그건 우연이 아니라 구조다.**

**① 엔진은 매시 돈다** (pm2 로그 실측):

| 시각(KST) | reviewed | approved | **executed** | held | failed |
|---|---:|---:|---:|---:|---:|
| 11:20 (점화 전) | **0** | 0 | 0 | 0 | 0 |
| 13:20 (점화 후 첫 발화) | **21** | 8 | **0** | 15 | 8 |
| 14:20 | 21 | 8 | **0** | 16 | 8 |
| 15:20 | 21 | 7 | **0** | 16 | 7 |
| 16:20 | 21 | 8 | **0** | 13 | 8 |

`held_by_reason`이 구체적이다(`roas_below=5` · `band_reached=1` · `other`). **점화가 `reviewed=0 → 21`을 만든 것이 엔진 각성의 추가 증거**다.

**② 그런데 4회 전부 `executed=0`** — 매시 승인 8건이 전부 스코프 밖이라 `blocked_by_scope`가 막는다(C3 반복 확인).

**③ 카나리 그룹 자신은 시간당 레인 제안 0건**이다. 소재 `nad-a001-02-000000554755092`는 후보 풀에 실재한다(`naver_ad_creative_daily` 28행 · 일 9~19클릭). **설령 제안이 나도 못 쏜다** — 쇼핑은 키워드 엔티티가 없어 입찰 레버가 **소재(`ad`) 단위**인데(D-NAO-255) `delegation_gate`가 `target_type=='ad'`를 **무조건** 거른다(`ad_confirm_only`, **D-NAO-5 영구 사람 전용**).

⇒ **자동 발사되는 유일한 레버 = 검색어 제외**, 그건 `run_naver_auto_operator_daily_job`(**08:50**) 안에 있다.

**④ SS 레인을 시간당으로 올려도 실익이 거의 없다** — `sync_naver_search_term`이 **하루 1회(07:40)** 동기화라 여러 번 돌아도 **볼 재료가 그대로**다. **08:50이 07:40 바로 뒤인 것이 그 설계다.**

### ⚠️ M4 판정에 대한 함의 (다음 세션이 반드시 볼 것)

M4 합격 관측은 「실집행 diary 행 생성 + 가드레일 위반 0 + 되돌림 0을 **7일 연속**」인데, **실집행 기회가 하루 1회**라 **표본이 최대 7건**이다. 계약 **C4**의 「표본 하한 ≈**52건**」과 **자릿수가 다르다** ⇒ **창 연장이 필요할 수 있다.** C4 원문이 *"미달이면 합격 선언 대신 창 연장"*이라 규정해 둔 그 경우다.

### Jino께 제시한 선택지 4개 (미결 — 답변 대기)

| | 무엇 | 대가 |
|---|---|---|
| ⓐ | 그대로 1주 관측 | 가장 안전. 「하루 1회」가 M4 관측의 실제 밀도가 된다 |
| **ⓑ** | **소재 입찰 제안을 Jino가 콘솔에서 Confirm** | **세션 권고** — 지금 가능·금지선 무접촉·엔진 판단을 사람이 보며 방아쇠 |
| ⓒ | 카나리 한정 `ad_confirm_only` 해제 | 밀도가 매시로. **D-NAO-5 금지선 ⇒ 새 계약 사안.** 첫 주 관측 «후»가 순서(지금 열면 「하루 1회 제외만」과 「매시 입찰까지」를 못 가른다) |
| ⓓ | 검색어 수집 주기 상향 | 재료 회전 상승. API 콜·네이버 갱신 주기 확인 선행 |

⚠️ **로그 함정 재확인**: 블루-그린 배포가 pm2 로그 파일을 옮겨 `-8011-*.log`만 보면 08-28에서 끊긴 것처럼 보인다. **전 로그 파일을 훑어야** 오늘 것이 나온다([[blue-green-deploy-moves-the-log-file]]).

---

## 7. 다음 세션이 할 일

| | 무엇 | 비고 |
|---|---|---|
| 0-a | ★**`Table` 컴포넌트 레벨 표 정렬 공용 테스트 1개** — 39개 호출부를 한 번에 덮는다(§5-b 이월) | 값이 크고 싸다. 변이 M3가 「39곳이 깨지는데 1파일만 죽는다」를 실증했다 |
| 0 | ★**「죽은 카드 방지」 가드 수리** — 스코프 밖 제안이 `approved`로 커밋되고 콘솔이 「실행 가능」으로 표시하는 건(§5-a) | **1순위.** 손해는 0이나 시간당 8~9건 누적이 **C7 재료를 희석**한다. 코드 변경이라 적대 리뷰 대상 |
| 1 | **카나리 1주 관측 (C2~C7)** — 엔진이 실제로 무엇을 했나. **첫 관측 시점 = 08-30 08:50 이후** | ★첫 주의 시험 = *「엔진이 Jino가 7월에 막은 것과 같은 판단을 하는가」*. 예상 후보 4건은 §6-a |
| 2 | **C5 하한 자 관측 — 창 병기 의무** | 가장 어려운 항. 「제외가 잘 돌았다」로는 못 닫는다(§2-3) |
| 3 | **C6 행위 분포** — 브레이크 100%로 나올 것 | 사실대로 적되 「대칭 회복」이라 쓰지 않는다 |
| 4 | **1주 «후» 상품명** | 카나리와 **동시 변경 금지**(효과 교락) |
| 5 | prod 코드 배포(밀린 것 있으면) | 이번 세션 앱 코드 0줄 |

⚠️ **자정 경계 CI flaky**는 이 트랙 소관이 아니다 — 그 테스트 파일의 원 작성 트랙으로 이월(n=67 규명분).

---

## 8. ⚠️ 함정·실수

0. ★★**「규칙 문언」을 「집행 코드」로 착각했다 — 완료 QA가 반증**(§5). 훅을 안 열고 CLAUDE.md §6 문언을 순진하게 적용해 「트랙이 원리적으로 종결 불가」라는 **없는 구조적 결함을 만들어 냈다.** 메모리가 이미 *"판정을 지어내기 전에 훅 계산을 재현해 오탐부터 재라"*고 적어 뒀는데 안 했다. ⇒ **집행층이 있는 규칙은 문언이 아니라 코드를 읽는다.**
0-b. ★**시점 없는 단정형 서술**: 「change_log 새 행은 #7669 하나뿐」이 13:21 관측인데 시점을 안 붙여 QA 시점(10행)과 어긋났다. **라이브 수치엔 관측 시각을 붙인다.**
0-c. ⚠️**위임문 작성 실패 1건**: QA에 C5 창별 산식의 **좌표를 안 줘서** 그 항목이 판정불능으로 남았다. 재현시킬 것이면 **재현 명령까지** 줘야 한다.
1. ★**인계의 「다음 가용 D-NAO-274」가 이미 소진**돼 있었다. `scripts/next_ids.sh`가 **275**를 줬다 — 안 돌렸으면 번호가 겹쳤을 자리다(그 스크립트가 존재하는 이유 그대로).
2. ★**내가 「라이브 칸 = 4」라고 결론낼 뻔했다.** 원장 `live_state='alive'`가 4건이라 그렇게 읽었는데, 코드가 이미 *「원장만 보면 70/70 그룹이 43칸으로 보인다」*고 경고하고 있었다. 실제 라이브는 **33칸**. **원장은 «우리가 아는 것»이지 «있는 것»이 아니다.**
3. ★**하한 자를 빼먹고 Jino께 답을 드렸다**(§2-3). 계약이 요구하는 자가 명시돼 있는데 점 추정으로 먼저 답했다.
4. `naver_search_term_daily` 컬럼이 `conv_purchase_cnt`·`clk`(`conversions`·`clicks` 아님) · 제외 원장 테이블은 `naver_search_term_exclusion`(`naver_excluded_keyword` 아님) — 이름을 추측해 세 번 깨졌다.
5. `naver_adgroup_scope.created_at`은 **UTC**인데 `naver_change_log.changed_at`은 **KST**다(같은 조작의 두 기록이 9시간 차). memory `sqlite-server-default-now-is-utc`.
6. ⚠️ 훅 오탐 `[체인] ⛔ pao-논의 n=49` — 공유 메인 폴더 등록부 stale. **생존 판정은 `origin/main`으로만.**

⛔ 무접촉: `발주예측` n=8(`c3b74f44`, `end_kst: null`).

---

## 9. 다음 가용 번호

**D-NAO-276** · 교훈 **#376** (이번 세션이 D-NAO-275 사용)
