# 세션 인수인계: 「쿠팡 손익정합」 n=7 — 계약 §1-A-3 미이행분 이행
> 저장일시: 2026-08-23 KST · 세션 `de1a7694` · 사슬 `쿠팡-손익정합` n=7
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## 0. 한 줄 요약
**n=6의 완료 QA가 「미이행 1건」으로 남긴 계약 §1-A-3을 닫았다** — 화면 B가 종합 조망의
`RgSettlementCard`를 **같은 컴포넌트로 재사용**해 주기별 정산 내역을 싣는다. Jino가 함께
지시한 화면 A의 「0원 행 접기」도 얹었다. PR #368 병합 · prod 프론트 `9d83b23a` 배포 완료.

## 1. 프로젝트 위치 및 환경
- 로컬 경로: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 main 폴더 — 착수 시 **102커밋 뒤처져 있었다**. n=3 12 · n=4 27 · n=5 26 · n=6 30 · **n=7 102** — 다섯 번째 재발이고 폭이 3배가 됐다)
- **이번 작업 워크트리**: `/Users/jino/.claude-worktrees/ohiselling/rg-settlement-history` (브랜치 `feat/rg-settlement-history`)
  - ⚠️ 새 워크트리엔 `node_modules`가 없다 — `cd frontend && npm install`(3초)
- 실행: 프론트 `cd frontend && npm test`(인구조사 래퍼) · `npx tsc -b --noEmit` · `npm run build`
- prod: `ssh -o BatchMode=yes sellc.ohitech.co.kr` · ★**활성 백엔드 포트는 블루-그린으로 8001↔8011을 오간다** — `tail /home/ubuntu/ohisell/deploy-manifest.jsonl`의 마지막 `zero-downtime-restart`의 `to_port`를 읽어라(**현재 8011** — n=6 인계의 「8001」은 그새 바뀌었다)
- 화면: `https://sellc.ohitech.co.kr/rocket-growth` · `/rocket-growth/settlement` · `/command-center`
- 배포 **반드시** `scripts/safe_deploy.sh` / 병합 **반드시** `scripts/safe_merge.sh`

## 2. 이번 세션 완료 목록

### 계약 §1-A-3 (정본 `docs/contracts/CONTRACT_2p_own_screens.md`, D-CPP-54)
계약 원문: *"기존 `RgSettlementCard`(CommandCenter) 컴포넌트를 재사용해 주기별 정산 내역을 함께 싣는다."*

- ✅ `frontend/src/components/RgSettlementCard.tsx` **신규** — 정의를 페이지 밖으로 «옮겼다».
  **종합 조망의 렌더 자리는 불변**(§1-B-1·§3 「제거·이동하지 않는다」) — 옮긴 것은 함수의
  주소이지 표면이 아니다. 사본을 떴으면 두 화면이 갈라진다(D-CPP-47이 고친 병).
- ✅ 갱신 props를 **선택**으로. 화면 B는 안 넘긴다 — 「RG 정산 갱신」은 Mac Wing 데몬을 깨우는
  실동작이라 두 화면이 같은 데몬을 경쟁적으로 부르면 서로의 폴링을 인질로 잡는다.
- ✅ 화면 B가 **네 갈래**로 말한다: 조회 실패 / 아직 로딩 / 응답에 원장 없음 / 계정 row 0건.
- ✅ 화면 A **0원 행 기본 접기** + 접힌 수 자백 + 펼치기(Jino 지시). 소계는 백엔드
  `conservation.options_net_sum`이라 **접기와 무관**하다. 판매수량 0이어도 **매출이 음수(반품)
  이거나 광고비가 있으면 접지 않는다** — 그 행이 손익에서 가장 봐야 할 행이다.
- ✅ 프론트 테스트 **23종 신설**(`rgSettlementCardReuse.test.tsx`) · 전체 **718 passed / 58 files**
- ✅ **새 계산 0 · 백엔드 변경 0파일** — 전부 프론트 표시층이다.

## 2-1. 적대 리뷰 (PR #368, 별도 기·읽기 전용)

| 라운드 | 판정 | P1 | 무엇이었나 |
|---|---|---|---|
| 1R | **FAIL** | 1 | 가드를 **백엔드가 결코 내지 않는** 분기(`rg_settlement == null`)에 걸어 아무것도 못 막았다. 실제 도달 결손은 `by_account: []`인데 카드가 ✅ 녹색으로 「정산총액 0원」을 단정 |
| 2R | **FAIL** | 1 (NEW) | **1R 수정이 만든 회귀** — `setData(null)`로 옛 값을 버렸더니 ①②③이 in-flight 내내 「요율 미상」·「순이익을 내지 않는다」를 거짓 단정 |
| 3R | **RESOLVED** | 0 | 전/후 DOM 대조로 해소 확인 · 회귀 0건 |
| 3R 델타 | **RESOLVED** | 0 | 3R P2 2건 채택 후 같은 리뷰어가 재확인 · 변이 5/5 사망 |

- 변이 누계 **사망 46 / 생존 0**(근등가 1 제외). ★표면 변이(사용자에게 닿는 마지막 경로 절단)를
  매 라운드 포함 — 종합 조망 렌더 자리 제거 · 화면 B 카드 호출 제거 · 값만 지우고 라벨 유지 ·
  「위 사유 참조」가 가리키는 빨간 배너만 제거.
- P2 **채택 8건 / 이월 3건**(아래 §5).

## 2-2. 완료 QA (판정 원문 그대로 — 미달·판정불능 포함)
- **작업 목적(정본 원문)**: 계약 §1-A-3 — *"기존 `RgSettlementCard`(CommandCenter) 컴포넌트를
  재사용해 주기별 정산 내역을 함께 싣는다."*
- **대조 대상 2개**(앵커 `대조:`): ①계약 §1-A-3 원문 ②Jino 2026-08-23 선택 원문
  (「§1-A-3 미이행분 마무리」, 0원 행 접기 포함)

### 1차 판정 (배포 전, 2026-08-23 19:4x KST)
```
판정(계약 §1-A-3 원문): 판정불능 — 화면 근거(㉠~㉣)에 도달 불가(prod 미배포), ㉤만 판정 가능
  ㉤ 달성 (적대 리뷰 P1=0 + 표면 변이 — PR 기록이 근거라 배포와 무관)
판정(Jino 선택 원문): 판정불능 — 같은 이유로 화면 근거 전무
목적 전환 선언: 없음
```
★1차가 판정불능인 것은 **정확한 판정이다** — 계약 §4 머리말이 *"「컴포넌트가 존재한다」는
합격이 아니다"*라고 못 박았고, 그 시점 prod엔 이 변경이 없었다.

### 재판정 (배포 후, 2026-08-23 19:44~19:50 KST · 별도 기·읽기 전용·**실제 브라우저 조작**)
QA가 전제를 **직접** 재확인했다(내 보고를 근거로 안 씀): prod 스탬프 `commit=9d83b23a…`
`built_at=2026-08-23T10:42:08.609Z` · `git merge-base --is-ancestor dce319f7 9d83b23a` = **참**.
아래 관측은 전부 그 빌드 기준이다.

```
판정(계약 §1-A-3 원문): 달성 — 화면 B가 종합조망과 같은 컴포넌트를 재사용하며 값이 원 단위 일치, 후퇴 없음
  ㉠ /rocket-growth/settlement (19:46) → "✅ RG 정산 비용 — 순이익 반영됨" 카드 렌더.
     COUPANG_WING1: 판매수수료 53,739 · 풀필먼트 127,621 · 반품비 1,650 · 광고비 179,134(취소선)
     · 정산 총액 362,144 · 순이익 차감액 183,010. 같은 시각 /command-center의 같은 카드와
     **여섯 숫자 전부 원 단위 일치** → 같은 컴포넌트·같은 데이터 소스임을 «값으로» 확증.
     → 달성
  ㉡ /command-center (19:44) → 종전과 같은 위치·같은 6필드로 렌더. 헤더 우측에 「🔄 RG 정산 갱신」
     버튼 **존재**(화면 B엔 없음 — 스크린샷 대조). 인접 카드도 정상 렌더.
     → 달성 (후퇴 0 · 갱신 버튼의 선택적 배선도 취지대로 갈림)
  ㉢ /rocket-growth (19:47~19:49) → 기본 상태 "판매도 광고도 없는 행 35개를 접었다 — 펼치기".
     클릭 후 "…35개를 펼쳤다 — 다시 접기"로 토글, 35행이 나타남. ★접힌 상태에서도
     **매출 0이지만 광고비>0인 행**(456·31,203·335·842·1,341·16,422·1,121원)은 계속 노출 —
     코드 로직과 실측 일치.
     → 달성
  ㉣ 펼치기 전/후 "상품 행 소계" 대조 → 접기 전 "—" / 펼친 후 "—" — 값 불변. 단 이 계정·날짜는
     전 행이 「원가 미상」이라 소계가 처음부터 계산 불가한 "—"였다.
     → 부분달성 — 「접기 전후 불변」이라는 성질 자체는 두 관측 모두에서 참이었으나,
       **실수(원 단위) 값이 접기 전후 동일하게 유지되는 사례는 이 관측 창에서 못 얻었다**
       (원가 미상·판매 0 데이터 조건 탓이지 기능 결함 정황은 없음)
  ㉤ 달성 (1차 판정 유지 — 재론 안 함)
  종합: ㉠㉡㉢ 달성, ㉣ 부분달성 — 부분달성

판정(Jino 2026-08-23 선택 원문 「§1-A-3 미이행분 마무리」, 0원 행 접기 포함): 부분달성 —
  §1-A-3 이행분(㉠㉡)과 0원 행 접기 기능(㉢)은 화면에서 확증됐고, 접기의 계산 불변성(㉣)은
  이번 관측 창의 데이터 조건상 「—」→「—」로만 확인돼 더 강한 증거(실수값 불변)를 못 얻었다

목적 전환 선언: 없음

QA가 확인 못 한 것:
  · ㉣의 «실수(원 단위) 소계가 접기 전후 동일»한 구체 사례 — 관측한 두 계정×날짜 조합
    (오픽스 08-22 · 오하이테크 08-22)이 전부 원가 미상 또는 무거래라 소계가 처음부터 "—"였다.
  · 백엔드 응답 바디(`conservation.options_net_sum` 원값) 대조는 안 했다 — 화면 표시값만 봤다.
이월 후보: 없음
```

★**판정문 자체에 내부 모순이 하나 있다** — 머리줄은 「달성」인데 같은 블록의 종합줄은
「부분달성」이다. **원문을 고치지 않고 그대로 싣고, 기록은 보수적인 쪽(부분달성)으로 잡는다.**
재판정은 §2가 허용하는 1회를 이미 썼고, 항목별 판정이 더 구체적이므로 그쪽을 정본으로 본다.

★**㉣의 미확보는 결함이 아니라 «합격기준이 데이터에 의존»한 결과다.** 「접기 전후 소계 불변」은
그 창에 **원가가 매핑된 실수 소계가 있어야** 강하게 잴 수 있는데, 관측 시점 두 계정 모두
원가 미상이었다. n=6이 남긴 *"증거 좌표가 세 번 썩었다 — 합격기준 자체가 재검증 불가능한
형태로 쓰였다"*와 **같은 결의 문제**다(그건 절대값 의존, 이건 데이터 조건 의존).
다음에 이 성질을 다시 재려면 **원가 커버리지가 있는 날짜**를 먼저 고르고 판정한다.

## 2-3. 착지
- **완료 단계**: 커밋 → push → PR **#368** → 적대 리뷰 → `safe_merge.sh 368 --force` 병합 →
  프론트 빌드 → `safe_deploy.sh --frontend` 배포. **멈춘 단계: 없음.**
- **prod 프론트**: `commit=9d83b23a05d808c81504c0f4c81c58d0e73b6e8a` · `built_at=2026-08-23T10:42:08Z`
  · 매니페스트 `{"kind":"frontend","branch":"feat/rg-settlement-history","commit":"9d83b23a","forced":false}`
  · 백업 `dist_backup_20260823_1942`
- **⚠️ `safe_merge --force` 자백**: CI 서명 3요소를 직접 실측(`steps=0` · **2초** ·
  로그 `BlobNotFound`) = 결제정지로 러너 미부착이지 테스트 실패 아님. 기록 `$TMPDIR/safe_merge.log`.
- **★배포가 한 번 멈췄다 풀렸다**: 19:40 시점 prod dist는 다른 살아 있는 세션의
  **미병합·PR도 없던** 브랜치 `feat/kpi-evidence`(스탬프 `17d8ece6`) 것이라
  `--is-ancestor 17d8ece6 HEAD` = **NO**, CAS가 거부할 상태였다. 강제하면 그 세션의 프론트가
  prod에서 사라진다(2026-08-06 clobber·n=5가 같은 이유로 멈춘 것과 같은 구조) ⇒
  **`--force-frontend` 쓰지 않고 반착지로 닫으려던 참에** 그쪽 PR #367이 병합돼 조상 관계가
  성립했다. 정상 경로로 배포했다. **강제 프론트 배포 0회.**
- **검증 명령**(다음 세션이 그대로 실행):
  ```
  ssh sellc.ohitech.co.kr 'cat /home/ubuntu/ohisell/frontend/dist/.build-stamp'   # → 9d83b23a…
  gh pr view 368 --json state                                                      # → MERGED
  ```
- **정정 경로**: prod 백업 `dist_backup_20260823_1942` · PR revert(`git revert -m 1`).
  **force-push 금지**(정정 경로를 파괴하는 유일한 행위).

## 2-4. 트랙 진행률
- **트랙**: `docs/tracks/completed/track_coupang-2p-parity.md` — **n=6이 20/20으로 종결했다.
  이번 세션은 M을 늘리지 않았다.**
- 이유: 이 작업은 트랙의 새 합격 항목이 아니라 **승인된 계약 §1 산출물의 잔여분**이다.
  종결된 트랙을 되열면 「끝났다」의 정의가 사후에 흔들린다.
- 기록 위치는 **계약 파일 §11**(신설)과 이 HANDOFF다.

## 3. 확정된 결정사항
- **이 작업은 새 계약이 아니다** — 목표·범위를 안 바꾸고 되돌릴 수 있으므로 전역 §1의 승인
  지점에 안 걸린다. n=6 HANDOFF도 이 항목만 「묻지 말고 진행 가능」으로 분류했다.
- **컴포넌트 «정의» 이동은 «표면» 이동이 아니다** — 계약 §3의 「제거·이동하지 않는다」가
  지키는 것은 렌더 자리이고, 재사용하려면 정의는 페이지 밖에 있어야 한다.
- **결손 표현은 종류마다 다른 문장을 쓴다** — 실패 / 아직 로딩 / 응답에 없음 / 원장 row 0건.
  얼굴을 갈라야 할 것은 «결손의 종류»이고, **결론(미상·미달·0원)은 어느 결손에서도 말하지
  않는다.**
- **접기는 표시이지 계산이 아니다** — 소계의 진실 원천은 백엔드 하나뿐이라 접기와 무관하다.

## 4. 핵심 파일 목록
| 파일 | 역할 |
|------|------|
| `docs/contracts/CONTRACT_2p_own_screens.md` | **정본 계약**(D-CPP-54) · **§11 = 이번 이행 기록** |
| `frontend/src/components/RgSettlementCard.tsx` | RG 계정별 정산 카드 — **두 화면의 단일 원천**(신규) |
| `frontend/src/pages/RocketGrowthSettlement.tsx` | 화면 B — ①요율 출처 ②커버리지 ③장부대조 **④주기별 정산 내역(신규)** |
| `frontend/src/pages/RocketGrowthPnl.tsx` | 화면 A — 0원 행 접기(`isQuiet`·`quietCount`·`visibleRows`) |
| `frontend/src/pages/CommandCenter.tsx` | 종합 조망 — **렌더 자리 불변**, import만 바뀜 |
| `frontend/src/pages/rgSettlementCardReuse.test.tsx` | 신규 23종 + **변이 주입 기록이 파일 하단 각주** |
| `.claude/memory/LESSONS_LEARNED.md` | **교훈 #352** |
| `.claude/anchors/de1a7694.md` | 이번 세션 앵커(합격 ㉠~㉤ · 대조 2 · 착지 · 이월) |

## 5. 알려진 이슈 / 주의사항

### ★★이 세션이 남기는 가장 값진 것 — 같은 병의 다섯 번째와 여섯 번째, 그리고 «여섯 번째는 다섯 번째를 고치는 커밋이 만들었다»
n=6이 「모름」이 「0 / 아니다」로 접히는 자리를 **네 번** 밟고 *"앞의 셋을 고치면서도 넷째는
안 보였다"*고 적어 뒀다. 이번에:

5. **1R P1** — 원장 계정 row 0건일 때 카드가 ✅ 녹색으로 「정산총액 0원」을 단정. `has_data`가
   «판매일 축 차감액»만으로도 참이 되어 **원장 row와 독립**이기 때문. RG 정산 성숙도 **D+12** ·
   화면 B는 창을 「어제」 고정 ⇒ **거의 매일이 그 상태**. ★그리고 초판 가드는
   **백엔드가 결코 내지 않는 분기**에 걸려 있었다 — **가드를 도달 불가능한 자리에 세우면
   지킨 것처럼 보이면서 아무것도 안 지킨다.**
6. **2R NEW P1** — **다섯 번째를 고치는 커밋이 만들었다.** 계정 전환 시 옛 값을 버리게 했더니
   ①②③이 in-flight 내내 새 계정에 대해 「요율 미상」·「원가 커버리지 미달 — 순이익을 내지
   않는다」를 거짓 단정. 같은 순간 ④만 정직해서 **한 화면이 스스로 비대칭**이었다.

⇒ **국소 수리는 병의 «모양»을 못 없앤다.** 한 PR 안에서 증거를 다시 얻었다. 교훈 #352.

### ★공허 단언의 새 얼굴 (변이가 아니었으면 못 봤다)
1R P2-6을 지킨다고 **주석에 선언한** 테스트가, 실패 경로에선 다른 분기가 카드를 어차피 가려
**리셋을 통째로 지워도 초록**이었다. 진짜 위험 구간은 «성공하는 전환의 in-flight»이라 조회를
**매달아** 그 구간의 DOM을 직접 재도록 다시 썼다.

### 이월 (고치지 않음 — 소관 지목)
- ★**종합 조망은 원장 row 0건에서도 「✅ 순이익 반영됨 / 정산총액 0원」을 그린다**
  (2R NEW P2①, origin/main부터의 기존 동작). 화면 B는 이번에 막았으나 같은 응답으로 두 화면이
  **«결손 표현»에서 갈린다**. 맞추려면 가드를 공용 `RgSettlementCard` 안으로 옮겨야 하고
  그건 종합 조망의 동작을 바꾸는 일이라 계약 §1-A-3 범위 밖 → **이 트랙의 후속 계약**
- `has_data=false` 배너가 무보호(origin/main 코드, 이 PR 무관) → 별건
- `msg`/`refreshing` 선택화로 호출부 누락이 타입에 안 잡힌다(오늘은 `noUnusedLocals`가 **우연히**
  잡는다) → 별건
- 3P 전용 메뉴·라우트 0개 → **별도 발의**(계약 §1-C가 명시 제외)
- 「요율 미상」(`BASIS_RATE_UNKNOWN`) 분기 렌더를 이 트랙 내내 **한 번도 못 봤다**(두 계정 다
  완결 주기가 있어서) → 이 트랙의 후속 계약
- main 빨간불 2건(`test_health_partial_sync`·`test_vendor_item_axis`) = 「썩은 테스트」로 이미
  규명됨, 이 diff 무관 → 별건

### 기타
- ⚠️ **훅 오탐 4번째**: `review-surface-mutation.sh`가 「적대 리뷰가 아닌」 위임에도 발동한다
  (n=3 완료 QA · n=5 테스트 보강 · n=6 구현 · **n=7**). 계약 `CONTRACT_surface_rule_enforcement.md`
  §6이 「오탐이 실측되면 재심 안건」이라 했고 이제 n=4다.
- ⚠️ **공유 main 폴더 뒤처짐 다섯 번째 재발 — 폭이 102커밋으로 3배**가 됐다. 파일 하나씩
  맞추는 건 증상 치료다.
- ⚠️ 커밋 `9c59fed0`의 메시지는 zsh가 백틱 식별자를 먹어 일부 단어가 비었다. 내용은 정상이고
  정본 기록은 `rgSettlementCardReuse.test.tsx` 하단 각주와 PR #368 본문이다.
  **force-push로 amend하지 않았다** — 정정 경로를 파괴하기 때문이다.
- ⚠️ **prod 활성 백엔드 포트가 8011로 바뀌어 있다**(n=6 인계는 8001). 매니페스트를 읽어라.

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)**: 계약 §1-A-3은 이번에 닫혔다. 트랙은 n=6이 20/20으로 종결했고
  궁극 목표 ①+②도 「달성」 판정을 받았다. **이 사슬의 원래 목적은 닫혀 있다** — 이어간다면
  **새 계약이 필요하다**(전역 §1 승인 지점 ①).
- **남은 슬라이스**: 없음.

**후속 후보 (전부 새 계약 사안 — 착수 전 Jino 승인 필요)**
- [ ] ★**종합 조망의 결손 표현을 화면 B와 맞추기**(위 이월 첫 줄) — 이번 세션이 실측으로
      드러낸 유일한 «현재 화면이 거짓을 말하는» 자리다. 가드를 공용 카드로 옮기면 닫힌다.
- [ ] 「요율 미상」 분기의 실제 렌더 검증 경로 마련 (이 트랙 내내 한 번도 못 봤다)
- [ ] 3P 전용 메뉴·화면 필요성 판단 (별도 발의)
- [ ] 이월분: 교환 배송손익 · 3PL 고정비 · 갭 34건 신원 · `order_count` Optional화 ·
      일자 추이 차트 RG 편입

## 7. 새 세션 시작 프롬프트
```
/session-relay 쿠팡 손익정합
```
