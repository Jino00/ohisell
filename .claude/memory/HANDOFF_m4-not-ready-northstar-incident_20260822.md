# 세션 인수인계: **M4 착수 보류 + ★북극성 사고(교훈 #346)** — 쓰기 0건 세션

> 저장일시: 2026-08-22 **22:1x** KST · 체인 「PAO 논의 **36**」 (세션 `a3343a99`)
> 새 대화 시작 시 이 파일을 먼저 읽을 것. 체인 이어받기: `/session-relay PAO 논의` — 이번이 **36**번이었다(다음은 37).
> **이 세션은 코드·prod 쓰기 0건이다**(전건 `sqlite3 -readonly`). 산출은 문서 5커밋뿐.

## 0. ★먼저 — PAO 북극성 대비 검토 (D-NAO-226 + **D-NAO-227 개정 반영**)

> ⚠️**이 세션이 바로 이 조항을 좁게 지켜서 사고를 냈다.** 개정된 검토는 **네 줄**이다 — 4번이 새로 생긴 칸이다.

1. **어느 M인가** — 이번 슬라이스 = §6 **M4**(L3 재개의 선행 결정).
2. **5요소 중 움직인 것** — **④자동화·⑤상시개선 = 착수 0이 연속 10세션째.** ①②③도 이번엔 안 움직였다(진단만).
3. **§7 대칭 검사** — 「가동 0」이라 발동 표면이 없다. 다만 ★**BEP 커버리지 조인 결함 때문에 «대칭 검사 자체가 데이터에 못 닿는다»**는 것을 확인했다.
4. ★**이번 M이 걸린 절을 전부 읽었는가** (D-NAO-227 신설) — **이 세션은 못 읽었고 그게 사고다**(§2 참조). M4를 다루는 세션은 §5(학습 루프)·§7(금지선)·§8(결정 표)을 **반드시 함께** 읽는다.

### 22:0x 검토 결과 — 다음 세션의 출발점
| | 실측(2026-08-22, prod `-readonly`) |
|---|---|
| `optimizer` | **`none` 9건** — 켜진 캠페인 0 (변화 없음) |
| `outcome_profit` | **6,957행 전건 NULL** — 첫 채점은 **08-23 08:10** 크론 |
| 기존 `outcome` 358건 분포 | **완전 불변**(§8-Q1 준수) |
| 최근 30일 광고비 | ★**21,098,946원** — 인계에 있던 「4,220만원」은 **2배 중복**(§3-1) |
| 학습 사슬 | 실행 일기 **08-11 이후 멈춤** · 반성 크론 **결번 20일** |

## 1. 프로젝트 위치 및 환경
- 로컬: `~/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (**main**, push 완료·미푸시 0)
- prod: **`sellc.ohitech.co.kr`** · prod DB는 항상 `sqlite3 -readonly`
- ⚠️**prod alembic head = `duty50attrib`**(D-CPP-50 쿠팡 관세). 세션 시작 시 `m3bprofitscore`였는데 **병행 세션이 올렸다** — PAO와 무관.
- ⚠️**병행 세션이 이 저장소에서 활발히 작업 중**이었다: 커밋 `2717bc3a`(2P 순이익 계약 초안·승인 대기) · `track_coupang-rg-net-ledger.md`→`track_coupang-2p-parity.md` rename 스테이지 · 새 체인 `sellc-원가-메뉴.jsonl` · 미추적 `HANDOFF_rg-net-ledger-wired_20260822.md`. **전건 미접촉 유지.**
- ✅**인계 정정**: 「`CLAUDE.md` 미커밋 11줄, 8세션째 Jino 판단 대기」는 **유령이다** — 직전 세션이 이미 커밋했고 `git diff`엔 내 편집만 있었다. 이 항목은 종결.
- 테스트 인터프리터: `.claude/worktrees/naver-ad-execution-loop-6cc75b/backend/.venv/bin/python3` (이번 세션은 코드 변경 0이라 미실행)

## 2. ★★이 세션의 본체 — 사고 하나와 발견 셋

### 2-1. 사고 (교훈 #346 · D-NAO-227)
**Jino 판정 원문**: *"이번건을 사고로 기록하자. 너가 Pao 북극성을 제대로 보지 않고 진행을 한건 사고네"* (21:46)

착수 때 D-NAO-226을 돌렸으나 **그 조항이 지목한 §1·§6만** 읽고, 방향이 M4로 정해진 뒤에도 **§5·§7·§8을 안 읽은 채** 3시간 진행했다. 손해 넷:
1. **§5-3 ③이 08-19에 이미 적어 둔 결론을 실측으로 재발견** — *"학습 루프의 회전 속도는 결국 L3 재개 폭이 정한다 — 기술 문제가 아니라 §8-①②의 결정 문제다"*
2. **§5-1의 「승격 실적 [미상]」을 문서 인용 없이 새 사실처럼 제시** — [미상] 채우기와 새 발견은 보고 가치가 다르다
3. **Fable 위임문에 §5를 안 실어** 파트너도 §5 없이 A/B/C 3안을 짰다
4. **Jino의 질문이 잡았다**(*"이 내용이 PAO 북극성에 들어있지 않았어?"*) — 훅도 자기검증도 아니었다

★**원인의 절반은 규율의 구멍**: 조항이 「§1·§6과 대조하라」로 **절을 못 박아** 써 있었고 나는 그걸 **좁게·문자 그대로** 지켰다. **집행 지점을 좁게 쓰면 규칙을 지켜도 목적이 샌다** — 조항 신설 **하루 만에** 드러났다.
⇒ **D-NAO-227**(Jino 승인 *"그래, 그러자"*): 검토 3줄 → **4줄**. 규율 본문은 프로젝트 `CLAUDE.md` 반영 완료(`b0c70532`).

### 2-2. ★발견 A — 총이익 «자» 자체가 의심스럽다 (M4·M5에 직결)
- `profit_scorecard`의 **보정계수 1.2975** 재현: 실주문매출 46,304,960 ÷ 광고 전환매출 35,698,990 (창 07-23~08-21). **QA 독립 재현 일치.**
- ★**분자가 `actual_revenue.naver_order_revenue`(`actual_revenue.py:27-32`) = 네이버 채널 «전체» 주문매출**(2,812건, **광고 경유 여부 무관 — 귀속 조인 없음**, QA가 코드로 확인). ⇒ **광고 밖 매출까지 광고 성과에 얹힌다.**
- ★코드 주석(`diagnosis.py:22`)은 *"convAmt가 실주문 대비 ~2.6배 과대 → 곱해 보정"*인데 **실값이 1보다 커 방향이 반대**다.
- 영향(계정 30일, 계정 BEP 1.711 가정): 보정 적용 **+5,963,568원** vs 미적용 **−234,545원** ⇒ **흑자의 거의 전부가 이 계수 산물.** 캠페인 «상대 순위»는 불변(같은 계수)이나 **절대 금액은 못 믿는다.**
- ⇒ **카나리 1주 성적도 같은 자로 잰다.** 자가 부풀면 「PAO가 총이익을 늘렸다」도 부풀어진다.

### 2-3. ★발견 B — 학습 사슬이 절반만 돈다 (§5-1 [미상] 확정 + 문서에 없던 결함)
| 칸 | 실측 (QA 독립 재현 전건 일치) |
|---|---|
| 일기 | 관찰 253(08-22까지) · **실행 250은 08-11 이후 멈춤** · 차단 3,863은 07-30 이후 멈춤 ⇒ **배울 재료가 07-30에 끊겼다** |
| 반성 | ⚠️**결번** — 08:35 일 1회인데 07-17~08-18 중 **17일만**, 마지막 **08-18**. **북극성 어디에도 없는 새 결함, 원인 미상** |
| 후보 | 27건(기각 22·숨김 3·**pending 1이 08-12부터 대기**·승격 1) |
| 지혜 | **1건**(07-26) = §5-1 「승격 실적 [미상]」의 확정값 |
| 반영 | 제안 `2314`(param_change) = **rejected** ⇒ 「지혜가 로직을 바꾼다」 **0/1** |

★그 유일한 지혜 원문: *"주말·여름·아이폰 비시즌 조건에서 캠페인 …008514959의 bid_up은 **차단**한다"* ⇒ **액셀 0 / 브레이크 1** — §7이 경고한 구조적 편향이 **지혜층에서 실측된 첫 증거**.
(자동 적용은 D-NAO-54 설계 금지선이라 이 0은 고장이 아니라 **한 바퀴 미완주**다.)

### 2-4. ★발견 C — 다중 로직 실험 능력 0, 그리고 **북극성에 «빠진 축»**
Jino 질문 원문: *"그런 로직 갯수를 여러개 둬서 다양한 실험을 해볼 수 있어?"*
- 판정 함수는 저장소 전체에 **1개**(`gave_score.compute_gave_score`, `gave_score.py:21`). 캠페인별 차이는 `gamma`·`target_roas_override` **두 다이얼** = 같은 공식의 파라미터 변주.
- 실험 개념 전수 grep **각 0건**: `experiment`·`arm`·`ab_test`·`treatment`·`bandit`·`thompson`. (`holdout`=오프라인 재현성 검증 · `canary`=주석 · `variant`=카페24 SKU · `epsilon`=탄력성 변수명)
- ⚠️`naver_campaign_settings.mode`(growth/recovery/launch/defense) = **`routers/`에만 있고 `services/` 어디도 안 읽는 죽은 필드**(QA 확인).
- **없는 부품 4**: ①실험군 식별자(`naver_change_log`에 로직·버전 컬럼 0) ②병렬 실행 라우팅 층 ③실험군별 집계 채점기(채점 3모듈 `GROUP BY` 0건) ④**캠페인별 가드레일**(`guardrail_params`가 `naver_account_settings` 위 **계정 단위 KV** — 캠페인 분화 불가. 현재 해당 key 행 0건 = 코드 상수 폴백 중)
- **대조군 개념 0건** — 채점은 「같은 엔티티 조치 전/후」뿐(`proposal_scoreboard.py:52-73`). ★ref 91이 *"대조군 분할은 이미 공짜로 있다"*를 최대 신규 아이디어로 꼽았는데 **코드엔 없다.**
- **재사용 가능**: ⓐ캠페인별 `gamma`/`target_roas_override` 배선(4모듈 소비 중) ⓑ`outcome_profit` 총이익 델타 산식(D-NAO-225) ⓒ`approval_source`(레인 식별자 → 실험군 태그 확장의 최소저항 경로)

## 3. ★정정 4건 — 인용 전 반드시 알고 갈 것

### 3-1. ★★「30일 광고비 4,220만원」은 2배 중복이다 — 계정도, 캠페인별도
```
실단위   21,098,946원   ← 정본 (adgroup_id <> '__backfill__', keyword 필터 없음)
sentinel 21,098,913원   ← 같은 돈의 다른 집계
전체 합  42,197,859원   ← 이걸 광고비로 써왔다
```
- 이 오류는 **직전 세션 HANDOFF §0 · 프로젝트 `CLAUDE.md` D-NAO-226 문단 · 이 세션 착수 확인줄**에 실려 있었다 → **셋 다 정정 완료**(`f440227e`·`b0c70532`).
- ★★**그리고 캠페인별 표도 같은 2배였는데 나는 그걸 못 잡았다** — QA가 잡았다. `008425541`은 **1,801만원이 아니라 9,006,795원**. ⇒ **이 세션 문서에 나오는 캠페인별 광고비는 전건 ÷2로 읽을 것.** (전환매출 열도 같은 검증이 필요한데 **미확인**이다.)
- 정본 규칙은 메모리 `naver-ad-daily-aggregation-rule`에 이미 있었다 — **읽고 썼으면 안 틀렸다.**

### 3-2. 내가 손계산한 캠페인별 「총이익」 표는 취소한다
5개 캠페인 부호가 반대였다. 원인 ①보정계수 누락 ②BEP를 단순평균으로 씀(정본은 매출가중평균). **「최대 캠페인이 최대 적자」는 취소.** 제품 정본은 `profit_scorecard` = `보정 conv_amt ÷ bep_roas − cost`이고 그 산출물은 `ops_diary_entries`(action=`profit_scorecard`, 매일 23:40)에 저장돼 있다 — **손계산하지 말고 그걸 읽을 것.**

### 3-3. 「A안 후보 3개는 대행사 조작 30일 0건」은 틀렸다
상위 15개만 본 값이었다. 전수: `008757089` **0건** · `008883255` **0건** · `008902804` 1건 · **`008452546` 19건**. ⇒ 진짜 무접촉+볼륨은 **갤럭시탭 계열 둘**뿐.

### 3-4. BEP 「팬아웃」은 없었다
직전 실측의 「has_bep > total 역전」은 **non-distinct join count를 센 것**이었다. `naver_product_bep`는 전 758행이 `channel_id=6`이고 `(channel_id, channel_product_id)` UNIQUE가 지켜진다. 정본 조인은 `campaign_target_resolver._weighted_target_for_cpids`(`:65-115`).

## 4. 완료 QA (판정 원문 그대로 — 미달 포함)

### 작업 목적(정본 원문 — 트랙 계약 헤더 `목표:`)
*"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."* (Jino 2026-07-19 · D-NAO-59)

**판정(북극성 §6 M4 행·§8 ①②): 부분달성** — M4 게이트 자체(§8 결정 2건)는 확정 못 했으나, §6 M4 행이 요구하는 "왜 재가동이 위험한가"의 실제 기제(fail-closed 확정 · `naver_agency_op` 185건이 settings 9개 중 최고비용 캠페인을 침범 중)를 라이브로 검증·반증까지 해냈다. §7 「액셀·브레이크 대칭」 검사는 BEP 커버리지 조인 결함으로 «발동 표면 자체가 데이터에 없다»는 것까지 확인 — **진단은 달성, 결정은 미달.** (21:58 KST)

**판정(Jino 2026-08-22 20:42 선택 원문 "A — M4 결정 2건 논의"): 부분달성** — "논의"는 실제로 일어났고 그 논의가 Jino 자신의 "준비 안 됐다" 결론으로 이어졌으므로 지시의 문언은 이행됐다. 다만 논의가 「M4 결정 2건」에서 「북극성을 부분만 읽고 진행한 사고」로 크게 옆으로 번졌고, **이 전환은 Jino가 직접 승인했으나 원 지시의 스코프는 아니었다.** (21:58 KST)

**판정(트랙 궁극 목표 D-NAO-59): 미달** — ④자동화·⑤상시개선 착수 0 · `optimizer='ours'` 전환 0건 · 실집행 0건. **오히려 총이익 계산의 근거 자체(보정계수)가 부풀려져 있다는 걸 발견해 "지금까지의 흑자 판정도 못 믿는다"는 방향으로 목표에서 한 걸음 더 멀어졌다**(정직한 후퇴). ★**열 번째이고 낮추지 않는다.** (21:58 KST)

- 합격: **ⓒ 달성**(9 vs 46 숫자 전건 일치·fail-closed 3좌표 정확) · **ⓕ 달성** / **ⓐⓑⓓⓔ 미달**(`docs/PLAN_naver-m4-*` 부재를 QA가 확인)
- **「안 함」 7항목 침범 0건**(커밋 diff가 문서 4개뿐·코드 0) · **prod 쓰기 0건 독립 검증**(착수 후 `naver_change_log` 45건 전부 `flight_pacing` 크론 `dry_run=1`, 실쓰기 0)
- **목적 전환**: 정형 `🔁` 선언 **없음**. Jino가 대화 중 직접 방향을 바꿨고 원문이 `확인:` 줄에 남음 ⇒ **경계 사례로 기록**

### QA가 확인 못한 것 5건 (그대로 기록)
①**Fable 위임문에 §5가 실제로 빠졌는지** — 서브에이전트 프롬프트 로그 접근 불가 ⇒ **교훈 #346의 핵심 주장은 독립 검증 불가** ②Jino 발화 인용의 축어성 ③보정계수 분모(35,698,990)는 나눗셈 근사로만 확인, `metrics_aggregator.aggregate` 원 함수 미재실행 ④반성 크론 결번의 원인 ⑤**트랙 `확인:` 누적 개수가 문서 총계 38과 안 맞물림 — 병행 세션 삽입 가능성, 미해소**

## 5. 트랙 진행률
- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: §4와 동일(D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **체크박스 없음.** M4는 결정 자체가 보류됐다.
  증거: 커밋 `a15ed85f`·`f440227e`·`aae487b1`·`b0c70532`·`bd7b6099` (전부 문서) · **코드·배포·prod 쓰기 0건**
- **확인 줄**: 3건 추가(착수·종료·QA) · **트랙 종결 여부**: 미도달
- ⚠️계약 헤더를 파일 전체로 grep하면 오탐이 난다 — **`합격:` 블록만 세야 2/7**이다.

## 6. 확정된 결정사항
- ★**D-NAO-227 — 북극성 검토 범위를 「이번 M이 걸린 절 전부」로**(Jino 승인 *"그래, 그러자"*, 21:5x). 검토 3줄 → **4줄** + §목차 선행 훑기 + **서브에이전트 위임문에 해당 절 원문 동봉**. **새 게이트 아님**(기록 의무). 규율 본문 = 프로젝트 `CLAUDE.md`, 근거 = 교훈 #346.
- **교훈 #346** 기록(`.claude/memory/LESSONS_LEARNED.md` · `failures.jsonl` 1줄). `next_ids.sh` 발급값 사용 — **다음 가용: D-NAO-228 · 교훈 #347**.
- §8-①② 자체는 **결정 없음(보류)** — Jino *"너 아직 PAO를 시도할 준비가 안되어있네"*(21:44).

## 7. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)** — 트랙 계약 헤더 `목표:` 줄 그대로: §4 참조. 이번 칸 = **M4 이전에 무엇을 채울 것인가**.

- [ ] ⓪**착수 시 북극성 대비 검토 «네 줄»**(D-NAO-227 — §0). M4를 건드리면 **§5·§7·§8을 함께 읽는다.**
- [ ] ①★★**08-23 08:10 크론의 첫 채점 관측** — 직전 인계의 1순위가 그대로 이월됐다(오늘은 시각 미도래). `outcome IS NULL ∧ dry_run=0 ∧ verify_date<=오늘` = 101건이 대상. 관측:
      `ssh sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db \"SELECT COALESCE(outcome_profit,'(null)'), COUNT(*) FROM naver_change_log GROUP BY 1;\""`
      **동시에**: 기존 `outcome` 358건 분포가 여전히 불변인가(§8-Q1). ⚠️**수동 강제 실행 금지**(교훈 #274).
- [ ] ②★★**보정계수 설계 재심** — §2-2. 「광고가 스토어 매출 전체를 견인한다고 볼 것인가」는 데이터가 아니라 **판단**이라 Jino 결정 사안. **이게 안 정리되면 M4 1주 판정도 부풀어진다.**
- [ ] ③★**반성 크론 결번 20일 원인 규명** — §2-3. 학습 사슬 ②번 칸이 절반만 도는데 아무도 몰랐다.
- [ ] ④★**다중 로직 실험 축을 북극성에 넣을지** — §2-4. 넣으면 ref 82 개정(= Jino 승인 계약 사안).
- [ ] ⑤`naver_campaign_settings.mode` 죽은 필드 처분(배선 or 삭제)
- [ ] ⑥**M4 결정 2건**(§8-①②) — 보류 중. 재개하려면 ②③이 선행되는 게 자연스럽다.
- [ ] ⑦**광고그룹↔상품 매핑 누락**(쇼핑 478 중 240) · **광고 상품 원가 미연결 258/702(36.8%)** — 원가를 «모르는» 게 아니라 DB에 «안 이어진» 것 → **소관: `docs/tracks/active/track_product-connection-map.md`**
- [ ] ⑧이 세션 문서의 **캠페인별 전환매출 열**이 2배인지 검증(광고비는 2배 확정, 전환매출은 미확인)
- [ ] ⑨직전 인계 §6의 승계분: M3-c(M2-d 의존·08-28 이후) · M2-d/M2-z(08-28 이후) · 계약 §8-Q5 콘솔 캡처 1장(Jino) · 「실시간 성과 감지」 이월 7건 · `entity_type='ad'` grain 오귀속 · 워크트리·디스크 정리
- [ ] ⑩**다음 구조 감사 트리거 = 08-25 이후**(마지막 §4 감사는 ref 69, 08-18)

## 8. 세션 최종 상태
- 메인 체크아웃: **main** (`bd7b6099` 시점 커밋 완료 — push 상태는 다음 세션이 `git status -sb`로 확인할 것)
- 이 세션 커밋 **5건**: `a15ed85f`·`f440227e`·`aae487b1`·`b0c70532`·`bd7b6099` — **전부 문서**
- **prod 쓰기 0건 · 코드 변경 0건 · 배포 0건** (QA 독립 검증)
- 앵커: `.claude/anchors/a3343a99-8206-4723-b59f-c2934673cc4a.md` (판정 3줄·이월 6건 포함)
- ⚠️워킹트리 잔존(**병행 세션 것 — 미접촉**): `HANDOFF_rg-net-ledger-wired_20260822.md` · `.claude/settings.local.json.bak-20260821` · 체인 `sellc-원가-메뉴.jsonl`

## 9. 새 세션 시작 프롬프트
```
.claude/memory/HANDOFF_m4-not-ready-northstar-incident_20260822.md 읽고 이어서 작업해줘
```
(체인 이어받기: `/session-relay PAO 논의` — 이번이 **36**번이었다)
