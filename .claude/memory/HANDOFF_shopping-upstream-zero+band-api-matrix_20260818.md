# 세션 인수인계: 쇼핑 상류 0 원인 규명 + 성과등급×API 매트릭스 (야간 무인 진행)

> 저장일시: 2026-08-18 00:3x KST
> 트랙: **PAO(파오)** — `docs/tracks/active/track_naver-ad-optimization.md`
> 결정: **D-NAO-190** · 교훈 **#309 · #310**
> 커밋 6건(이 세션분) + 앞 세션 7건 = **origin 대비 총 13커밋, 전부 로컬 main에만**
> ★**origin 미푸시 — Jino 지시(22:26 「지금은 github에 올리지 말아」)로 push 보류. 다시 묻지 말 것.**
> 앞 세션: `HANDOFF_rank-axis+paper-application+exclusion-slots_20260817.md` §7의 새 세션 시작 프롬프트를 그대로 이어받아 착수.
> 이 세션은 **전량 읽기 전용 + 문서 산출** — 네이버 API **0콜**, prod DB `sqlite3 -readonly`만, `backend/`·`alembic/`·`frontend/` 변경 **0건**, 배포 0건, **PAO 완전 정지 유지**(`optimizer='none'` 7/7).
> ★**이 세션은 Jino가 취침한 상태(23:50~)에서 무인으로 완주했다** — 승인 지점 없이 진행 가능한 것만 했고, prod 쓰기·배포·API 쓰기는 전부 의도적으로 보류했다(§5).

---

## 0. ★Jino 지시 원문 대장 (이 세션 전건 — 목표를 새로 발명하지 않기 위한 정본)

| # | 시각 | Jino 원문 | 처리 |
|---|---|---|---|
| 1 | 세션 시작 | (앞 HANDOFF §7 인계) HANDOFF §6-b부터, 히스토그램·`bidWeight` 판독 먼저, **다시 설계하지 말고** §6-b 목록을 그대로 앵커로 | ✅ 슬라이스 1 = **달성**(완료 QA, 커밋 `658fd8c3`·`44120398`) |
| 2 | (인계) | push 다시 묻지 말 것 | ✅ 유지(현재 13커밋 미푸시) |
| 3 | 23:40 | *"쇼핑 상류 0의 원인 규명부터 해줘"* | ✅ 슬라이스 2 = **달성**(D-NAO-190, 커밋 `8173ea47`) |
| 4 | 23:46 | *"성과 등급별로 우리가 받을 수 있는 모든 api에 대한 정보를 보여줘봐"* | ✅ 슬라이스 3(↗️스코프 밖 자백 후 진행 — ref 59 ⑥ 영역) = **달성**(매트릭스, 커밋 `8173ea47`) |
| 5 | 23:50 | *"내가 지시한거 빼먹지 말고 모두 진행하고 QA까지 하자. 난 이제 자니까 너가 끝까지 진행해놔"* | ✅ 완료 QA 1회(별도 Sonnet·읽기 전용) → **1차 부분달성 → 미달 2건 수정(`4d98d081`·`4fc568b9`) → 같은 QA 기에 재판정 1회 → 해소 2/2 → 달성**(커밋 `b557e1ca`) + 이 HANDOFF 작성으로 완주 |

이 대장은 앵커 `.claude/anchors/da48087f-ae21-4a2a-bd0f-595bd9fe31a1.md`의 「야간 무인 진행 계획」 표를 원문 그대로 옮긴 것이다 — 인계 세션이 목표를 재발명하지 않기 위해 원문을 다시 적는다(전역 §1).

---

## 1. 프로젝트 위치 및 환경

- **로컬 경로**: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 메인 체크아웃, 브랜치 **main**)
- **prod 조회**(읽기 전용): `ssh -o BatchMode=yes sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db" < 로컬.sql`
  ★인라인 heredoc은 따옴표가 벗겨진다 — 반드시 로컬 `.sql` 파일 + stdin
- **깔때기 재현**: `docs/references/data/67_shopping_upstream_zero/funnel_q2_group14.sql`(14일 창)·`funnel_q3_group90.sql`(90일 창)을 prod에서 재실행 → `funnel_sim.py`·`funnel_sim2_interaction.py`·`funnel_sim3_margin_health.py` 3본으로 재현. **대용량 원자료 2건(14MB·70MB)은 커밋하지 않았다** — 위 SQL로 재생성된다(완료 QA가 재생성해 `diff` 0줄 확인 완료).
- **결정적 귀속 재현**: `docs/references/data/67_shopping_upstream_zero/whitelist_token_coverage.py`(`covers`/`sole_blame`, `sorted()` 순회 — set 순회 비결정성 없음)
- **번호 발급**: `./scripts/next_ids.sh` — 다음 번호는 **D-NAO-191 · 교훈 #311**(2026-08-18 00:3x 확인, origin/main=186·306 / 내 브랜치=190·310 중 큰 쪽 +1)
- 환경변수: `NAVER_SA_*`(backend/.env) — 이 세션 미사용(콜 0)

---

## 2. 이번 세션 완료 목록

### 슬라이스 1 — S1 판독 실측 3건(제외 슬롯 분포·`bidWeight`·인계 주장 5건) → 커밋 `658fd8c3`·`44120398`
- ✅ 제외 슬롯 히스토그램(쇼핑 116그룹/3,880건, p90=p95=max=70, 70/70 도달 15그룹) — `docs/references/data/66_exclusion_slots/S1_readout_20260817.md` §1
- ✅ `bidWeight` 실설정 판독(1,271행, ≠100은 8행/0.6%·4그룹/1.0%·비용 0.97%) — 동 §2, 원자료 편입 `docs/references/data/58_sa_surface_probe/criterion_real_20260817.json`
- ✅ 인계 주장 5건 재판정(유효 3 / 부분오류 2) — 동 §3, **교훈 #309**의 발단(쇼핑 제외 브리핑 26일간 0회 발동 발견)
- ✅ 완료 QA = **달성**(별도 Sonnet 읽기 전용, CSV·JSON 원자료 독립 재계산 + prod 라이브 재조회로 소수점까지 재현)

### 슬라이스 2 — 쇼핑 exclude 상류 0의 원인 규명(D-NAO-190) → 커밋 `8173ea47`
- ✅ `search_term_judge.judge_search_terms`의 게이트를 원문 그대로 이식(가설 미제공)해 깔때기 실측
- ✅ 판정: **「정상 0」**(결함 아님) — 병목은 G3(`clk≥10`)이고 G2(화이트리스트)는 절대 탈락 1위지만 병목이 아니다
- ✅ 산출물: `docs/references/data/67_shopping_upstream_zero/DIAGNOSIS_shopping_upstream_zero_20260817.md`

### 슬라이스 3 — 성과등급 × API 매트릭스(스코프 밖 자백 후 진행) → 커밋 `8173ea47`
- ✅ 새 분석 0건, ref 63·64·65·58·57·66 좌표 병기 조립만
- ✅ 등급 교차 완료 5축 / 구조 사실만 1축 / 원료 있음·미교차 10축 / 원료 없음 5축 / 미상 2축 (합 23)
- ✅ 산출물: `docs/references/data/68_band_x_api_matrix/performance_band_x_api_inventory_20260817.md`

### 완료 QA + 미달 수정 + 재판정 → 커밋 `4d98d081`·`4fc568b9`·`b557e1ca`
- ✅ **1차 부분달성**(미달 2건: ①DIAGNOSIS §4 토큰 귀속표 재현 불가 ②ref 68 §3-1 축 집계 24≠23) → 두 항목만 수정 후 같은 QA 기에 재판정 1회 → **해소 2/2 → 달성**
- ✅ 교훈 #310 신규(귀속 도구의 비결정성 — §3-1 참조)
- ✅ 트랙 D-NAO-190 절 기입 + `claude-progress.txt` 갱신

---

## 2-1. 완료 QA (판정 원문 그대로)

### 슬라이스 1 (제외 슬롯·`bidWeight`·인계 실측) — **달성**
- **판정 원문**(앵커 「슬라이스 1 판정」): "합격기준 5항 전건 달성·「안 함」 위반 0(별도 Sonnet 읽기 전용 QA가 CSV·JSON 원자료 독립 재계산 + prod 라이브 재조회로 문서 수치를 소수점까지 재현, `git show --stat 658fd8c3`에 `backend/`·`alembic/`·`frontend/` 0건, `optimizer` 7/7 `none` 라이브 재확인, origin 미푸시 유지). 커밋 `658fd8c3`·`44120398`. (2026-08-17 23:27 KST)"
- **항목별**: 합격기준 1(히스토그램) **달성** — CSV 116행 독립 재계산 + prod 라이브 재조회 완전 일치(avg 33.4483·p50 28·p75 61·p90=p95=max 70·70도달 15그룹 전건 `n_ours=0`) / 합격기준 2(`bidWeight`) **달성** — 원자료 385그룹 재파싱 전건 일치, 90일 cost 844,864원/전체 87,477,193원=0.966% 재확인, 미독 4좌표 실재 / 합격기준 3(인계 5건) **달성** — QA 독립 재실행이 유효 3/부분오류 2 어휘까지 재현 / 합격기준 4(미상 규율) **달성** / 합격기준 5(쓰기·배포 0) **달성**(23파일 전부 `docs/`·`.claude/memory/`·`claude-progress.txt`)

### 슬라이스 2·3(D-NAO-190 + 매트릭스) — **1차 부분달성 → 미달 2건 수정 → 재판정 해소 2/2 → 달성**
- **판정 원문**(트랙 D-NAO-190 절): "**완료 QA(별도 Sonnet·읽기 전용, 2026-08-18 00:0x~00:2x) = 달성** — 1차 **부분달성**(미달 2건: ①DIAGNOSIS §4 토큰 귀속표가 `set` 순회 승자독식 때문에 **재현 불가** — QA가 재실행으로 「필름 13,223」→「보호필름 25,690」 뒤바뀜을 실증 ②ref 68 매트릭스 §3-1 축 집계 5+12+5+2=24 ≠ 23행) → **그 두 항목만 수정 후 같은 QA 기에 재판정 1회 → 해소 2/2 → 달성**(라운드 증식 차단 규율)."
- **★부분달성이었다는 사실을 숨기지 않는다**: 1차 QA는 두 항목에서 미달을 냈다. 미달 사유는 (i) 초판 귀속표가 Python `set` 순회 순서(프로세스 해시 시드 의존)로 「어느 토큰 탓인가」를 승자독식 방식으로 매겼고, QA가 같은 스크립트를 재실행하니 1위가 「필름」에서 「보호필름」으로 뒤바뀌었다(재현 불가) (ii) 매트릭스 §3-1의 상태별 축 개수 합이 24였는데 표 자체는 23행이라 산술이 어긋났다(원인 = 축 #12 A4가 「측정됨」과 「미교차」 두 성격에 걸쳐 있었는데 한쪽에만 셈).
- **수정 내용**(커밋 `4d98d081`·`4fc568b9`): ①초판 귀속표 폐기 → 결정적 지표 둘로 재측정(`covers`/`sole_blame`, `sorted()` 순회) — `docs/references/data/67_shopping_upstream_zero/whitelist_token_coverage.py` 신규 커밋 ②매트릭스에 「구조 사실만(등급 교차 0)」 칸을 별도로 신설해 5+1+10+5+2=23으로 정정
- **재판정에서 QA가 확인한 것**: `funnel_q2_group14.sql`을 prod에 재실행해 커밋 안 된 14MB 원자료를 재생성 → **`diff` 0줄**(재생성 가능 주장이 참) · `funnel_sim*.py` 3본 재실행이 문서 표와 전 항목 일치 · 시뮬레이터가 실코드 게이트·`_build_whitelist`·margin 매출가중을 원문과 완전 일치하게 이식했음을 파일:줄 대조로 확인 · 두 프로세스 출력 `diff` 0줄(결정성) · §4-1·§4-2 전건 재현 · 23행 전수 재분류 일치 · 코드 변경 0·`optimizer` 7/7 `none` 재확인
- **★결론이 뒤집힌 대목(§4-2, 이 재측정의 실질 발견)**: 결정적 지표로 병목(G3)을 통과한 11건을 실제로 열어 보니 **전부 상품 핵심어**였다(「아이폰16프로필름」·「폴드8전면보호필름」·「갤럭시폴드와이드」…). 초판은 「일반 명사까지 과보호하는 버그」라는 함의를 냈으나, **원행을 열어 보니 화이트리스트는 오컷 방지 설계대로 정확히 작동 중**이었다. 방향까지 틀린 초판 결론이었다 — §3-2 참조.
- **QA 이월 2건**: ⓒ(n-gram) 설계는 §4-2의 반전을 전제로 시작해야 한다 · 「갤럭시와이드」·「갤럭시8와이드」(합 59,587원)의 구형 단말명 여부 **사람 확인 미완**.

---

## 3. 확정된 결정사항 (번복 금지)

### 3-1. ★인계 항목을 뒤집은 실측 3건 — 「옛 주장 → 실측 → 판정」

| # | 옛 주장(출처) | 실측 | 판정 |
|---|---|---|---|
| 1 | `bidWeight` 실설정 **1,343행**·「다수가 70/80」(ref 58→`ad_type_inventory.md`→`paper_adoption_audit.md`→ref 65→트랙) | 실제 **1,271행**(WEB_SITE AG 72행이 SHOPPING에 **이중 계상** — 1,271+72=1,343으로 정확히 들어맞음). ≠100은 **8행/1,271(0.6%)·4그룹/385(1.0%)·비용 844,864원(0.97%)** | **부분오류(과장)** — 원자료 출처: `docs/references/data/66_exclusion_slots/S1_readout_20260817.md` §2-3 |
| 2 | 제외 슬롯 평균 33.4건(48%) — 「이미 70에 붙은 그룹」 분포는 미상 | 116그룹 히스토그램 실측: p50 28·p75 61·**p90=p95=max=70**·**70/70 도달 15그룹**·60~69 16그룹(26.7%). 도달 15그룹은 **전건 대행사 귀속**(우리 몫 0) | **48% 자체는 유효**(분모 116그룹에서), 단 「분포 미상」이 이번에 **채워짐** — 출처: 동 §1-2·1-3 |
| 3 | HANDOFF §5(앞 세션): "현재 시스템은 매일 쇼핑 손실 검색어를 골라 diary에 「콘솔 수동 제외 대상」이라 쓰고 있다" | `ops_diary_entries`에 해당 문구 **0건**(2026-07-16~08-17 전 기간) · `naver_proposals` 쇼핑 `search_term_exclude` **all-time 0건** · 배포(2026-07-22) 이후 **26일간 0회 발동**. 원인은 이번 세션의 D-NAO-190이 규명 = **정상 0**(결함 아님, §3-2) | **틀렸다 — 교훈 #309** — 출처: 동 §3-1, `docs/references/data/67_shopping_upstream_zero/DIAGNOSIS_shopping_upstream_zero_20260817.md` |

### 3-2. ★★D-NAO-190 — 쇼핑 제외 후보 26일 0건은 「결함」이 아니라 「정상 0」이다
정본: `docs/references/data/67_shopping_upstream_zero/DIAGNOSIS_shopping_upstream_zero_20260817.md`. 방법 = `search_term_judge.judge_search_terms`의 게이트를 원문 그대로 이식(`_build_whitelist`·`_is_whitelisted`·게이트 순서·margin 매출가중까지)해 게이트별 통과 행수를 셈. 읽기 전용(prod `sqlite3 -readonly` + 로컬 python)·API 콜 0·쓰기 0·배포 0·**수리 0**. 실측기에 가설을 주지 않았다(확인 편향 차단).

- **깔때기(14일 창, `source=shopping`)**: G0 원료 107,643 → G1 전환보호 −544 → **G2 화이트리스트 −100,163** → **G3 `clk≥10` −6,936 → 0**. G4·G5는 도달 행이 없어 호출 자체가 안 됨.
- **★인과 순서를 가른 상호작용 분해**(G1 생존 107,099 기준): `clk≥10`인 행이 **11건(0.01%)**이고 **그 11건 전부가 화이트리스트에 걸린다**(`clk≥10 ∧ 미걸림 = 0`). 절대 탈락 1위는 G2지만 **병목은 G3**다 — 자를 만한 표본 자체가 11건뿐이다.
- **로직은 죽지 않았다**: 창을 90일로 넓히면 실제로 **2건 통과**(`01. 버디필름`의 「골프」clk=16·cost=36,098 / 「골프스티커」clk=10·cost=13,328). ⚠️버디=골프 용어라 관련어일 가능성 있음 → **오컷 위험 후보, 「무관」 단정 금지, 자르기 전 사람 확인 필수**.
- **민감도(계산만, 상수 미변경)**: 현행 0 · G2 off = **10** · G3 `clk≥5` = 0 · G4 fail-open = 0 → **문턱 문제가 아니라 개별 검색어 × 14일 grain의 표본 희소성**.
- **범인이 아닌 것 둘**: G1(0.5%만 자름, 전환 컬럼 생존) · G4(쇼핑 203그룹 중 201=99% margin 해석 가능). 원료 건강(최신 `ad_date` 08-16, 원료 시작 07-04).

### 3-3. ★★결론이 뒤집힌 대목(§4-2) — 화이트리스트는 「버그」가 아니라 「의도대로 작동」이었다
- 초판(1라운드) 귀속표는 「일반 명사가 광범위하게 후보를 막는다」는 함의를 냈다. **QA가 재현 못 해 미달을 냈고**, 결정적 지표(`covers`/`sole_blame`)로 다시 측정하면서 **병목(G3)을 통과한 11건 전체를 직접 열어 보았다**.
- 결과: 11건 전부가 **「갤럭시폴드와이드」「아이폰16프로필름」「폴드8전면보호필름」** 같은 **우리 상품 자체를 찾는 핵심 검색어**였다(합 283,791원·전환 0). **G2가 이를 막는 것은 오컷 방지 설계가 의도대로 작동한 것**이지 「과보호 버그」가 아니다.
- ★**처분의 방향이 바뀐다**: 진짜 질문은 「화이트리스트를 풀까」가 아니라 「상품 관련어인데 14일 28만원 쓰고 전환 0인 검색어를 어떻게 다룰까」이고, **제외는 그 답이 아니다**(자르면 상품 핵심어를 잃는다). **이것이 정확히 Jino 구상(ref 66 §6 구제 레인 — 자른 검색어를 저CPC 별도 그룹에서 받기)이 겨눈 문제다.** 관련어-적자 검색어의 처분은 **제외가 아니라 입찰 하향 / 구제 레인**의 영역이다.
- ⚠️단 「갤럭시와이드」·「갤럭시8와이드」(합 59,587원)는 **구형 단말명일 가능성**이 있어 관련성 판정이 다를 수 있음 → **사람 확인 대상**.
- **★★계획에 남기는 것**: ①ⓔ(`search_term_ss_lane.py:674` 옛 전제 제거)는 **후보를 만들지 못한다** — ref 65 S1 합격 ⑤의 경로가 ⓔ가 아니다 ②병목이 정확히 **논문 [8] n-gram이 푸는 문제**다 → **ⓒ가 실제 해제 레버, ⓔ는 그 뒤 순서** ③**ⓒ의 선행 입력 = 화이트리스트 정책 재심** — `_build_whitelist`가 상품명 파생 372/378 토큰을 substring 보호하는데(「필름」·「액정보호」·「강화유리」·「2매입」 등) n-gram 후보는 그 토큰과 같은 층이라 만들자마자 같은 게이트에 걸린다. 의도인지 미튜닝인지 **판정불능**.

### 3-4. 성과등급 × API 매트릭스 — 조립 결과
정본: `docs/references/data/68_band_x_api_matrix/performance_band_x_api_inventory_20260817.md`. 새 분석 0건, 좌표 병기 조립만.

| 상태 | 축 수 | 해당 축 |
|---|---|---|
| 등급 교차 완료 | **5** | 캠페인유형·S1 순위(제한적)·S5 상품BEP·S6 절대액·달력요인 |
| 구조 사실만(등급 교차 0) | **1** | A4 타겟팅 실설정(`bidWeight` — 연령 grain 없어 등급 분해 원리적 불가) |
| 원료 있음·미교차 | **10** | S4·A1·A2·A3·A5·A7·A10·B2·B3·B4 |
| 원료 없음 | **5** | S2·S3·A9·B1·ADVoost·GFA |
| 미상 | **2** | A6 PC/모바일 가중치·A8 확장검색 변이 |
| **합** | **23** | ref63 §13-b의 「실질 1.5단계」 자기판정과 정합 |

가장 큰 공백: **①등급을 가장 직접 흔드는 두 축(순위·연령성별)이 정작 등급과 안 엮여 있다**(순위는 정적 목표 미지지·홀드아웃 8/8 미재현이라 예측에 못 씀 / 연령·성별은 1년 소급 실증됐는데 저장 테이블 자체가 0개) **②band3 판정이 계정 블렌디드 BEP 위에 서 있어** 상품BEP 재판정 시 48그룹 이동·624그룹(73%) 미판정 **③신규그룹 43개**는 baseline 잔차법의 사각.

---

## 4. 핵심 파일 목록

| 파일 | 역할 |
|---|---|
| `docs/references/data/66_exclusion_slots/S1_readout_20260817.md` | 슬라이스 1 정본 — 제외 슬롯 히스토그램·`bidWeight` 판독·인계 5건 재판정 |
| `docs/references/data/58_sa_surface_probe/criterion_real_20260817.json` | `bidWeight` 판독의 원자료(385그룹 전수, 남의 스크래치패드에서 이 세션이 편입) |
| `docs/references/data/67_shopping_upstream_zero/DIAGNOSIS_shopping_upstream_zero_20260817.md` | D-NAO-190 정본 — 깔때기·상호작용 분해·§4-2 결론 반전 |
| `docs/references/data/67_shopping_upstream_zero/funnel_q2_group14.sql`·`funnel_q3_group90.sql` | 깔때기 원자료 재생성 SQL(14MB·70MB 원자료는 커밋 안 함) |
| `docs/references/data/67_shopping_upstream_zero/funnel_sim.py`·`funnel_sim2_interaction.py`·`funnel_sim3_margin_health.py` | 게이트 이식 시뮬레이터 3본(원코드와 파일:줄 대조 확인됨) |
| `docs/references/data/67_shopping_upstream_zero/whitelist_token_coverage.py` | 결정적 귀속 재측정 도구(`covers`/`sole_blame`, `sorted()` 순회) — 교훈 #310의 산출물 |
| `docs/references/data/68_band_x_api_matrix/performance_band_x_api_inventory_20260817.md` | 슬라이스 3 정본 — 등급×API 23축 매트릭스 |
| `docs/tracks/active/track_naver-ad-optimization.md` | D-NAO-190 절(완료 QA 판정 원문 포함, line ~505-518) |
| `.claude/memory/LESSONS_LEARNED.md` | 교훈 #309(인계=코드의도≠관측)·#310(귀속의 비결정성) |
| `.claude/anchors/da48087f-ae21-4a2a-bd0f-595bd9fe31a1.md` | 이 세션 정본 앵커 — 목표·안함·합격기준·슬라이스 1/2/3·판정 줄·야간 계획·이월 |

---

## 5. 알려진 이슈 / 주의사항

- ★**origin 미푸시 — 총 14커밋**(`git rev-list --count origin/main..main` = 14, 2026-08-18 06:5x 재실측. ★초판은 **13**이라 적었는데 그건 **이 HANDOFF 커밋 자신(`110ea4d6`)이 빠진 자기참조 오차**였다 — 「인계 숫자는 실측 전엔 못 믿는다」의 또 다른 사례로 남긴다). 이 세션분 7건: `658fd8c3`·`44120398`·`8173ea47`·`4d98d081`·`4fc568b9`·`b557e1ca`·`110ea4d6`(이 문서). 앞 세션분 7건: `5e14d191`·`4b64e68a`·`0b2b1259`·`e5713308`·`6fd19fa3`·`9a2e2238`·`c4e3b52a`. **Jino 22:26 지시로 push 보류 — 다시 묻지 말 것.**
  - 이 체크아웃을 지우거나 리셋하지 말 것 — origin에 없는 산출물이 여기만 있다.
- ★**야간에 하지 않은 것과 그 이유**(앵커 원문 그대로 — 「빼먹었다」와 「의도적으로 남겼다」는 다르다):
  - **prod 배포 0 · prod DB 쓰기 0** — 되돌릴 수 있더라도 **CI가 결제 정지로 죽어 있어**(빨강·초록 둘 다 코드 신호 아님) 야간 무인 배포는 검증 없는 배포다. 크론이 07:50에 돈다는 점도 감안.
  - **ⓓ BRAND_SEARCH settings 등록 보류** — 이건 **prod DB INSERT**다. Jino 부재 중 상태 변경은 하지 않는다.
  - **ⓑ 계층 풀링 배선 · ⓒ n-gram 집계층 보류** — 둘의 합격기준이 **라이브 관측**(크론 1회전 산출물 대조)인데 배포 없이는 원리적으로 못 댄다. 배포 없이 코드만 쓰면 이 프로젝트가 반복해 다친 「코드엔 있는데 안 돈다」 상태를 스스로 만드는 것이다.
  - **ⓔ 잠김 1 수리 보류** — 슬라이스 2가 「상류 0 = 정상」으로 확정됐으므로 수리 대상이 674줄이 아니라 ⓒ(n-gram)로 바뀌었다. **원인 확정 전 수리 금지**가 이 슬라이스의 「안 함」이었고, 확정된 지금도 674줄만 고쳐서는 목적을 못 달성하므로 그대로 둔다.
  - **네이버 API 쓰기·GET 1회전(매칭 타입·`bidWeight` 적재) 보류** — GET은 읽기지만 385그룹 순차 호출은 계정에 대한 외부 액션이고 적재 테이블(마이그레이션)이 선행이다. 다음 세션 슬라이스로 남긴다.
- **PAO 완전 정지 유지** — `optimizer='none'` 7/7 재확인(재판정 QA 포함, 이 세션 내 3회 확인).
- **쇼핑 SA는 검색어 grain에 입찰 조준(양) 레버가 없다** — 키워드 엔티티 0건. 제외(음)만 가능. WEB_SITE는 키워드 엔티티 91,172개로 양방향 가능(ref64 §6-1, 매트릭스 §1 재확인).
- **매칭 타입(exact/phrase) 컬럼이 원장에 없다** — 3,880건이 exact인지 phrase인지 DB로는 원리적으로 복원 불가, `/ncc/targets` GET 재조회로만 판독(§6-a③).
- 기존 미결 유지: 품질지수 죽은 신호(`qi_grade=4` 91,172건) · prod 프론트 미배포 · `.pm2/logs` 627MB 무로테이션 · **GitHub Actions 결제 정지로 CI 미가동**(빨강·초록 둘 다 코드 신호 아님) · `PLAN_naver-ad-execution-loop.md` §0-4 완료 정의 달성 불가능한 형태 · **구조 감사 7일 경과**(마지막 `docs/references/54_*`, 2026-08-10 — §4 트리거, Jino 결정 필요).

---

## 6. 다음에 할 작업 (미완료)

### 6-a. Jino 결정 대기

| # | 항목 | 배경 |
|---|---|---|
| 1 | **`optimizer` 해제 범위**(기존) | 전 캠페인 vs 카나리 2~3개 — 쇼핑 제외를 반자동/완전자동 중 무엇으로 갈지 |
| 2 | **대행사와 제외 칸 소유권 분리**(기존) | D-NAO-188의 조건별 채점이 여기 걸려 있다. 70/70 도달 15그룹의 칸이 전건 대행사 것이라 우리가 후보를 골라도 넣을 칸이 0(§3-1 #2) |
| 3 | ★**순서 재판단**(신규) — `bidWeight` 최우선 근거 축소 vs 칸 고갈 실측 | ref 65가 S1-ⓐ(`bidWeight`)를 최우선으로 둔 근거는 「계통적 오류」였으나 실측 규모가 0.97%로 축소됐다(§3-1 #1). 반면 제외 칸은 15그룹이 이미 70/70이다(§3-1 #2). 어느 쪽을 먼저 열지는 모델이 조용히 재배치하지 않는다(ref 65는 승인 문서) |
| 4 | ★**화이트리스트 정책 방향**(신규) | §3-3에서 「일반 명사까지 substring 보호」가 의도인지 미튜닝인지 판정불능으로 남았다. 방향은 「풀기」가 아니라 「관련어-적자 검색어의 별도 처분(구제 레인)」 쪽이 실측으로 지지된다 — 채택 여부는 Jino 결정 |
| 5 | ★**창 14일 → 90일 확대**(신규) | 90일 창이면 후보 2건이 통과한다(§3-2). 단 창 확대는 ref 64가 실측한 「스프레드는 창의 함수」 문제를 함께 들여온다 — 파라미터 결정 |
| 6 | ★**「갤럭시와이드」 관련성 사람 확인**(신규, QA 이월) | 「갤럭시와이드」·「갤럭시8와이드」(합 59,587원)가 구형 단말명인지 사람이 확인해야 §3-2의 「90일 2건 = 관련어 가능성」 판정이 완성된다 |

앞 세션 이월(6건 유지): ref 65 §10의 나머지 6건(계약 슬라이스 구성·ADVoost·GFA 취급·`bidWeight` 적용 재심·add-keyword 배선·S3 증액 표본 0 지속 시·순위 서보 진퇴).

### 6-b. 다음 세션 착수 슬라이스 — ref 65 S1의 남은 것 (ⓔ는 뒤로 밀림)

각 항목의 **합격기준은 라이브 관측이라 배포 가능한 세션이 필요하다**(이 세션이 무인·야간이라 보류한 것과 같은 이유 — §5).

| 순서 | 항목 | 내용 | 선행 조건 |
|---|---|---|---|
| ⓐ | 적재·배선 + **매칭 타입 판독** | `/ncc/targets` GET 385그룹 1회전(exact/phrase)·`bidWeight` 판독분 신규 테이블 적재. 합격 = 콘솔 실측 1그룹과 값 일치 | 마이그레이션 선행, GET은 계정 외부 액션이라 배포 세션 필요 |
| ⓑ | 계층 풀링 배선(**좌표 정정본**) | `hierarchical_pooling.pool_all`(호출부 0)을 **정정된 실제 소비처 3곳**에 배선 — `exploration.py:367`·`auto_operator.py:1509`·`growth_sweeper.py:114`. **앞 세션 인계의 `flight_loop:47`·`bid_ceiling_calculator:34`는 틀린 좌표였다**(S1_readout §3, 죽은 import·주석이었다는 것을 이 세션이 실측으로 정정) | 합격기준 = 크론 1회전 산출물 대조(라이브 관측) → 배포 필요 |
| ⓒ | **n-gram 집계층** — 실제 해제 레버 | `search_term_judge`의 `GROUP BY search_term`에 n-gram 층 추가, phrase(`type=2`) 후보 산출까지 | **선행 입력 = 화이트리스트 정책 재심**(6-a #4) — Jino 결정 없이 착수하면 §3-3의 반전(상품 핵심어를 다시 막는 결과)이 재연된다 |
| ⓓ | BRAND_SEARCH settings 등록 | `optimizer='none'`으로 등록 — 관측 사각 해소만 | **prod DB INSERT**라 승인 필요(§5에서 야간 보류한 이유와 동일) |
| ⓔ | 쇼핑 제외 잠김 1 수리(674줄) | **뒤로 밀림** — D-NAO-190이 「674줄만 고쳐선 후보를 못 만든다」를 확정했으므로 ⓒ(n-gram) 완료 뒤가 이 항목의 실제 선행 순서 | ⓒ 완료 후 |

### 6-c. 이월(앞 세션·이번 세션 누적, 앵커 「## 이월」 전건)

- ★QA가 재현 못 한 증거 1건 — prod 크론 로그 `[스케줄러] naver 검색어 제외: shopping=0`을 `ohisell-backend-8001-out.log`에서 못 찾음. 결론(26일간 0회)은 DB 증거로 지지되므로 유지하되 **로그 좌표는 미확인**으로 강등(문서 2곳 수정 완료). 다음 세션이 로그 대상(프로세스명·파일)을 확정하면 좌표 복구.
- ★쇼핑 exclude 상류 0의 원인 — **이번 세션이 규명 완료**(정상 0, §3-2). ⓔ 착수 전 조건은 충족됐으나 순서가 ⓒ 뒤로 밀림.
- ★매칭 타입(exact/phrase) 판독 — 원장에 컬럼 없음. `/ncc/targets` GET 1회전에 `bidWeight` 적재와 동승(6-b ⓐ).
- ★순서 재판단(bidWeight 최우선 근거 축소 vs 칸 고갈 실측) — Jino 결정 후보로 위 6-a #3에 등재.
- 6-a Jino 결정 2건(optimizer 해제 범위·대행사와 제외 칸 소유권 분리) — 유지, 위 6-a #1·#2.
- ref 66 QA 미실시 · ref 66 §2-2 M3 판정불능(복수 그룹 + 입찰 서열).
- ref 65 §1-2 「광고그룹」 행 표기(494 = 두 유형 합산) — QA 이월, 표기 개선.
- D-NAO-185 첫 슬라이스 미착수(필독만 완료).
- `PLAN_naver-ad-execution-loop.md` §0-4 완료 정의가 달성 불가능한 형태.
- 구조 감사 7일 경과(마지막 `docs/references/54_*`, 2026-08-10) — §4 트리거, Jino 결정.
- QA 이월 2건(이번 세션 신규): ⓒ(n-gram) 설계는 §4-2의 반전을 전제로 시작해야 한다 · 「갤럭시와이드」·「갤럭시8와이드」 구형 단말명 여부 사람 확인 미완(위 6-a #6과 동일 항목).

---

## 7. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_shopping-upstream-zero+band-api-matrix_20260818.md 읽고 이어서 작업해줘.
§6-a 「Jino 결정 대기」부터 확인 — 특히 #3(순서 재판단)·#4(화이트리스트 정책 방향)는
다음 슬라이스(ⓒ n-gram) 착수 전 필요하다.
§6-b는 전부 라이브 관측이 합격기준이라 배포 가능한 세션이 필요하다 — 이 전 세션은
Jino 취침 중 무인 진행이라 배포를 의도적으로 보류했다(§5).
★origin 미푸시 13커밋 상태다(Jino 지시) — push 다시 묻지 말 것.
```

---

## 8. 상태·환경 요약

- **main 기준**, 배포 없음, prod 무변경, **PAO 완전 정지 유지**(`optimizer='none'` 7/7, 이 세션 내 3회 재확인)
- 다음 번호: **D-NAO-191 / 교훈 #311**(2026-08-18 00:3x `./scripts/next_ids.sh --no-fetch` 직접 확인)
- origin 대비 미푸시 **13커밋**(`git rev-list --count origin/main..main`, 2026-08-18 00:3x 직접 확인) — Jino 지시로 보류, 다시 묻지 말 것
- 이 세션이 쓴 서브에이전트: 읽기 전용 Sonnet(D-NAO-190 진단)·Sonnet(매트릭스 조립)·Sonnet(완료 QA 1라운드)·Sonnet(완료 QA 재판정) — 전량 읽기 전용, 상태 변경 도구 미사용

---

## 9. ★추가 세션분 (2026-08-18 06:5x~07:2x) — Jino 「안한거 있어?」 → 「모두 해」

> 커밋 `e58f778f`. 배포 0 · `backend/`·`alembic/` 0 · API 콜 0 · push 0 · `optimizer` 7/7 `none` 유지.
> **origin 미푸시 = 15커밋**(`git rev-list --count origin/main..main`, 2026-08-18 07:2x 실측).

### 9-1. 자백한 「안 한 것」 3건과 처분

| # | 자백 내용 | 처분 |
|---|---|---|
| 1 | **ref 66 완료 QA 미실시** — 직전 세션이 판정불능으로 넘겼고, 밤새 QA를 세 번 돌리면서 이것만 손대지 않았다 | ✅ 실시 → **부분달성**(달성 3 / 부분달성 3 / **미달 0 · 판정불능 0**) |
| 2 | **구조 감사 트리거를 착수 시점에 재지 않았다** — 규칙은 §6 착수 필독 때 `ls -t docs/references/*audit*`로 재고 7일 초과면 그 자리에서 제안. 실측하니 마지막이 ref 54(2026-08-10) = **8일 경과** | ✅ 제안에 그치지 않고 **착수** → `docs/references/69_audit_pao_drift_20260818.md`(222줄, Fable) |
| 3 | **HANDOFF 미푸시 커밋 수 자기참조 오차**(13 → 실제 14) | ✅ 정정 + 오차의 성격을 문서에 명기 |

### 9-2. ref 66 완료 QA = **부분달성** (판정 원문 그대로)

- 달성 3항: **공식 출처 확인 정직**(공식 Wiki 인용 4건이 조사 원문과 **글자 단위 일치** · 「70은 공식 미기재·콘솔 캡처가 유일 근거」 자백이 3곳 생존) · **Jino 기획 판정 정확**(화이트리스트 부재=공식 확정 / 입찰 서열=판정불능으로 **분리**) · **파라미터 자백 정직**(§8 6건 전부 코드에 대응 값 없음, 반대로 근거 있는 상수를 자백으로 위장한 사례 0건).
- 부분달성 3항이 **한 결함**에서 나왔다: 「기존 3,880건이 **전부 exact 습관**으로 등록돼 있다」가 **116그룹 중 1그룹(01. 버디필름) 콘솔 화면 관측을 전체 모집단으로 확대**한 것이고 `[미상]` 라벨도 §10 목록도 없이 **「정본」으로 승격**돼 있었다(표본 출처 `docs/HOWTO_console-exclusion-export.md:130`). 2026-08-18 재측정(§3-1 참조)이 「원장에 매칭 타입 컬럼 없음 → DB로 원리적 복원 불가」를 확정해 전제의 원료 부재를 드러냈다.
- **정정 3곳**(§2-1 M1 · §3-3 · §10 #7 신설) + 개정 이력. ★**판정은 올리지 않았다** — 통과시키려 고친 게 아니라 **근거 없는 문장을 정본에 남기지 않으려** 고쳤다. 코디네이터가 세운 「ref 66 불변」 경계를 **↗️자백 후** 넘었고, 그 자백이 4곳(커밋 메시지·ref 66 개정이력·트랙·진행로그)에 정정과 같은 커밋으로 기록됐다.
- ★**실무 결론**: **phrase 통합으로 칸을 비우는 계획은 「현재 타입 구성을 안다」를 전제하는데 그 원료가 없다** → `/ncc/targets` 라이브 판독이 §3-3 실행 전 **필수 조건**으로 §10 #7에 등재(§6-b ⓐ의 GET 1회전에 동승).

### 9-3. 주기 구조 감사 (ref 69) — 실측과 발견

**라이브 실측**(전부 prod 읽기 전용, 원자료 `docs/references/data/69_audit_pao_drift/RAW_DATA_20260818.md`, 완료 QA가 **prod 재쿼리로 건수까지 재현**):

| 축 | 관측 |
|---|---|
| 집행 | `optimizer='none'`·`auto_operate=0` **7/7**(6행 `updated_at`이 D-NAO-132 정지 시각 07-30 KST 10:48과 일치) · 우리 자동집행 **19일째 0건** |
| 총이익 | **+2,069,289원**(06-18~07-17) → **−803,029원**(07-18~08-16) |
| BEP 미달 그룹 비용 | 8,428,248 → **15,135,972원(+79.6%)** |
| 대행사 조치 | 70건(10.0/일) → **388건(21.6/일) = 2.16배** |
| 최대 손실 | `01. 갤럭시_지문방지_TPU` **−1,648,196원**(30일) |
| 죽은 신호 | `qi_grade=4` **91,172/92,235(98.8%)** 여전히 · `pool_all` 프로덕션 호출부 **0** |
| 약속 불일치 | `flight_pacing` 1,183행 **100% `dry_run=1`**(07-12~08-17) |
| 건강한 것 | 제외 생존 감시 3,986건 **100% 24h 내 점검 · breached 0**(인계 우려와 **반대**) |

★★**교란 경고가 §3 최상단에 박혔다 — 「정지 때문에 적자」로 읽으면 안 된다**: 두 비교창의 계절이 다르다. 휴가창(**고정 7/20~8/15**, D-NAO-183) 겹침이 `prior_30d` **0일** vs `last_30d` **27/30일**이고(QA가 직접 날짜 계산으로 검산) Z폴드8 출시도 같은 창이다. → **「halt 후 창에서 총이익이 적자다」 = `[실측]` / 「halt가 적자의 원인이다」 = `[미판정]`**. 분리 경로(평시 행만 대조 / 작년 동기 / 매칭 DiD)는 **이번 감사 범위 밖**.

**§4 구조적 발견 5건**: ⓐ재개 선행조건은 **트랙 L167(D-NAO-132 ④ P0~P4)에 실재**하는데 **해소 상태를 재는 곳이 없다** ⓑ**우리 정지 ≠ 계정 정지**(대행사 2.16배 → 안전선이 계정 일부에만) ⓒ**`flight_loop` 갭은 정지와 독립**(100% dry_run, 「1주 후 실전환」이 정지 **이전부터** 5주+ 미이행 → 재개해도 안 풀린다) ⓓ죽은 신호가 감사 사이를 생존(배출구 끝에 **처분 주체 부재**) ⓔPLAN 정본 지위 사망 vs D-NAO-34 모순(체크리스트가 **양방향**으로 어긋난다 — 「X1a 카나리 대기[ ]」인데 `update_bid` 수백 건 집행, 「X2 T3 완료[x]」인데 실집행 0건).

★**저자(Fable)가 코디네이터 전제를 반증**: 「재개 선행조건이 문서에 없다」고 전달됐으나 트랙에 실재한다 → 「해소 상태 측정 부재」로 정정. 실측기 보고를 검증 없이 넘긴 것이 원인. 저자 추가 발견 2건: **원자료 내부 불일치**(실집행 건수 228/18 vs 425/21, QA 재현은 **231** — 3중 불일치, §7-4에 재계수 등재) · **2.16배의 교란 2건**(소재 외부변경 탐지가 정지 후 창에 배포·복구돼 **관측 표면 비대칭** · `ad_edit` 302건=66%가 D-NAO-136 **피드 재적용**일 수 있음) → **「대행사가 2배로 일했다」도 그대로 믿을 숫자가 아니다.**

### 9-4. ★Jino 결정 대기 — 감사 재심 제안 5건 (전건 「☐채택 ☐기각 ☐보류」 빈 칸)

| # | 제안 |
|---|---|
| 1 | **재개 선행조건(P0~P4) 잔여량 실측 발주** — 19일째 정지인데 잔여를 세는 곳이 없다 |
| 2 | **D-NAO-34 재심** — PLAN 지위 재정의 3안(정본 사망 vs 「스프린트 끝까지 PLAN 우선」 지시가 모순) |
| 3 | **X2 T3 실전환 약속 처분** — 5주+ 미이행을 이행할지 약속을 폐기할지 |
| 4 | **`qi_grade` 처분** — 1차 문서 대조 or 신호 폐기(배출구 끝에 처분 주체를 세울 것) |
| 5 | **대행사 손익 기여 측정 설계** — D-NAO-188 경로(조치×환경, 매칭 DiD 3변수 사전 고정) |

### 9-5. 슬라이스 4 완료 QA = **달성** (판정 원문 그대로)

합격기준 5항 전건 달성 · 「안 함」 위반은 **ref 66 수정 1건뿐이고 자백 4곳 기록** → 「위반이나 자백 기록 있음」(무단 아님) · 목적 전환 없음. (2026-08-18 07:23 KST)
**QA 신규 이월 2건**: ①ref 69 §3-2 BEP 미달 **비율의 분모·분자 population 불일치**(분모 475/454는 「창에 행이 있는 그룹」·zero-cost 70개 포함 / 분자 225/236은 「cost>0 그룹」 — **달러 수치는 완전 정확**, 퍼센트만 내적 비일관) → 다음 세션 정정 ②`update_bid dry_run=0` 건수 3중 불일치는 ref 69 §7-4에 이미 등재됨(신규 아님).
