# HANDOFF — PAO 스코프 기간 바 교체 (체인 `pao-uiux` n=6 · 세션 4b11d8b5)

**검증 명령:** `git show origin/main:frontend/src/lib/periodRange.ts | grep -c presetWindowExcludingToday` → **1**이어야 한다. 0이면 이 인계는 낡았다.

날짜: 2026-09-02 23:35 ~ 2026-09-03 01:0x (KST)
트랙: `docs/tracks/active/track_pao-ui-ux.md` (**10/22 불변**)
워크트리: `~/.claude-worktrees/ohisell/pao-uiux-n4` · 브랜치 `feat/pao-uiux-n4`

---

## §0 목적 원문 (발명하지 말 것)

> (2026-09-02 23:35) "첨부된 이미지 보면 캘린더가 너무 부실하다. 대시보드에 있는 캘린더처럼 만들자"

---

## §1 한 일

**PR #677 머지 `055f7c51`** · CI 3/3 · **prod 배포 0회**.
프론트 **1,441 passed**(인구조사 97/97) · 백엔드 **7,690 passed** · eslint 0 errors(96/96).

버튼 3개(`7일/21일/51일`) → **날짜 입력 2개 + 프리셋 5개**(`어제/7일/21일/30일/90일`) + 축 이름 **「성과 발생일」**.

- 공용 `PeriodRangeBar`는 **이미 있었고** 대시보드·NaverOps·CoupangOps가 쓴다 — 스코프만 자기 것을 들고 있었다.
- ★**API가 `days`만 받아서** 프론트만 고치면 고른 날짜와 실제 창이 갈라진다 ⇒ `date_from`/`date_to` 가산(종전 경로 불변).
- 오늘을 고르면 clamp + `window.clamped`·`note`로 자백 → 화면 앰버 배너.

---

## §2 ★★적대 리뷰 1R FAIL(P1 2) → 2R PASS — 둘 다 내 얼굴에 되돌아왔다

**P1-1**: 공용 프리셋이 전부 «오늘로 끝나는데» 이 화면 관례는 **D-0 제외**다. 프리셋을 누를 때마다 서버가 창을 잘라서 —
- 「예외일 때만 뜨는 경고」가 **상시 경고**가 됐다(내 테스트 주석이 *"늘 뜨는 경고는 아무도 안 읽는다"*고 못 박은 상태)
- 자백문이 *"…까지 고르셨지만"*이라 **사용자가 하지 않은 입력을 사용자 탓**으로 돌렸다
- 라벨 「7일」인데 실제 창은 **6일**, 그 상태로 버튼은 «눌린 것»으로 보였다

★**내가 이 PR의 존재 이유로 「고른 날짜 ≠ 실제 창」을 들어 놓고, 그 갈라짐을 `days` 축에서 없앤 대신 프리셋 축으로 그대로 옮겨 놨다.**

**P1-2**: 내가 커밋에 「불변」이라 적은 세 값(시작 창 21일·끝점 어제·축 이름)에 **테스트가 0건**이었다. 방어선이 「목 데이터의 `clamped`를 화면이 그리나」에만 있었고 **「이 화면이 실제로 어떤 창을 요청하나」에는 한 줄도 없었다** — 그 공백이 P1-1을 통과시켰다.

**수리**: `presetWindowExcludingToday`(끝점 어제, 길이는 라벨 그대로) + `PeriodRangeBar`에 `windowFor` **단일 주입점**(그 컴포넌트가 이미 가진 교훈 — «쓰는 창»과 «눌린 것처럼 보이는 창»은 한 함수에서) + `21d` 어휘 복원 + 프리셋마다 «나가는 인자»를 재는 테스트.

**2R 실측**(리뷰어가 프리셋 5개를 실제로 눌러 재현): 전건 오늘 미포함 · 창 길이 = 라벨 · `clamped=False` · 열자마자 21일 ON. 51일 제외 처분과 P2-2 이월도 **타당** 판정. 리뷰가 **이월로 권고한 F16**(하이라이트 축 공백)까지 닫았다.

---

## §3 완료 QA (원문 그대로)

판정(Jino 지시 원문): **부분달성** — 코드 수준에서 완결됐으나 prod 배포 0회로 **Jino가 실제로 보는 화면은 세션 종료 시점에도 안 바뀌었다** — 지시의 최종 표적(사람이 보는 화면)에 아직 안 닿았다 (00:52 KST)
판정(화면 요소는 한 곳에): **달성** — 자체 버튼을 제거하고 공용 컴포넌트로 교체. 규율의 「갈라진다」 경고를 **두 번 재확인·재수리한 이력**이 남았다 (00:52 KST)
판정(창 관례): **달성** — 조용히 자르지 않고 자백. 프리셋 경로는 애초에 오늘을 안 보낸다 (00:52 KST)
판정(종합): **부분달성** — ②③④는 강하게 재현되나 ①의 「화면에 뜬다」에서 prod 배포 0회가 걸린다 (00:52 KST)

**QA가 확인 못한 것(원문)**: CI 백엔드가 관측 시점 pending(**그 뒤 3/3 pass 확인** — 머지함) / **실제 브라우저 렌더** — 오프라인 뷰어가 `Layout.tsx`의 다른 헬스·스케줄러 API 모양을 못 맞춰 크래시, jsdom App 전체 렌더로 대체(CSS 레이아웃 미검증).

---

## §4 트랙 진행률

**10/22 → 10/22 (불변).** 계약 2의 10항목은 문서를 재므로 구현은 체크박스를 안 켠다. 확인줄 1건 누적(`63c78a33`, +1/−0).

---

## §5 ⚠️ 다음 세션이 먼저 볼 것

1. **prod 배포 대기 3건** — §7½ 1·2단계는 배포됐고, **이번 기간 바는 main에만 있다**. 완료 QA가 「부분달성」을 준 유일한 사유다.
2. **§7½ 3단계는 09-15 «전»**에 끝나야 한다(그 뒤엔 결과 칸이 안 비어 「빈 칸 규약」이 무의미).
3. ⚠️ **eslint 여유 0**(96/96) — 경고 하나면 CI 빨강. 이 세션에서 내가 **두 번** 넘겼다(둘 다 컴포넌트 파일에서 컴포넌트 아닌 것을 export). 순수 함수는 `lib/`에 둔다.
4. ⚠️ **prod 백엔드를 로컬 8000에 붙이지 마라.** 붙였다면 절대 테스트를 돌리지 마라(이 세션이 그렇게 prod에 POST를 보내고 API를 40분 죽였다).

## §6 ↗️ 이월

- **시작일 칸을 비우면 서버가 시작일을 «지어내고 자백하지 않는다»** — `lib/periodRange.ts`가 스스로 못 박은 불변식(*"백엔드가 막는 입력은 프론트가 먼저 막는다"*)을 이 화면만 안 지킨다(`customRangeError` 미배선) → 소관: 이 트랙 다음 슬라이스
- 원장 초과 **1,824칸(58그룹)** — 우리가 건 제외가 라이브에 안 보인다 → 소관: 「조치 생존」·PAO 최적화 트랙
- 스윕이 **10그룹을 9일째 못 본다**(프로브 실패) → 소관: PAO 최적화 트랙
- 백엔드 `entity_sync.py` rationale이 커맨드 센터 표 셀에 'MOP'를 찍는다 → 소관: PAO 최적화 트랙
- 드릴다운 성능(요약·`_today_count`를 안 쓰는데 계산) → 소관: 이 트랙 5단계
- ↗️ 훅 오탐 1건 — **완료 QA 위임을 적대 리뷰로 오인**해 「표면 변이 누락」이 떴다(세션당 1회, 재전송으로 통과)

## §7 ⛔ 무접촉
`pao-논의` n=81(`14873038`) · `ava-광고총괄` n=8(`cb0ed825`·AI_office) · `sellc-원가-메뉴` n=28(`16610af6` — 등록부를 못 닫고 죽었다. 회수는 Jino 명시 지시가 있을 때만)

## §8 착지
PR #677 머지 `055f7c51` 완주 · CI 3/3 · `--force` 미사용 · 멈춘 단계 없음.

---

## §9 ★prod 배포 완료 (n=7 · Jino 2026-09-03 07:37 "prod에 배포해줘")

§5-1의 「prod 배포 대기」가 **해소됐다.** 완료 QA가 세 대조 모두 「부분달성」을 준 **유일한 사유**였다.

- 백엔드 `pao_scope_roster.py`·`naver_ad.py` — CAS 통과 · 마이그레이션 없음 · **무중단 재시작 다운타임 0초** · 스케줄러 리더 승격 확인
- 프론트 dist — 백업 `dist_backup_20260903_0749`
- 배포 커밋 `fec05849` · prod 스탬프 동일 · **PR #678로 main에 반영**(안 넣으면 내용이 같아도 다음 세션 CAS가 막힌다)

**라이브 3계약 실측**(https://sellc.ohitech.co.kr):
1. **고른 날짜 = 실제 창** — `date_from=2026-08-10&date_to=2026-08-20` → 창 `08-10~08-20 (11일)`, `clamped=False` ✔
2. **오늘 선택 시 clamp + 자백** — 종료일에 오늘을 넣으면 창이 `~09-02`로 잘리고 note가 *"2026-09-03까지 고르셨지만 … 2026-09-02까지로 보여드립니다"* ✔
3. **거짓 문장 없음** — 두 칸 다 오늘이어도 「시작일이 종료일보다 뒤」가 **안 뜬다**(P2-1 수리 확인) ✔

**화면**(prod): 21일 버튼 **ON**(F16이 짚은 하이라이트 축) · 창이 어제로 끝남 · **앰버 경고 없음** — 「예외일 때만 뜨는 경고」가 실제로 예외로 남아 있다.

★**판정은 소급해서 안 고친다** — 위 §3의 「부분달성」은 그 시점의 사실이고, 재판정은 다음 세션 몫이다.

★배포 순서(다음 세션도 이대로): ①`git merge-base --is-ancestor <prod스탬프> HEAD`로 **조상 여부를 먼저 재고** ②거부되면 `origin/main` 병합 → **재빌드** → 합친 코드로 전체 테스트 → 배포 ③배포 뒤 **스탬프 커밋을 PR로 main에 넣는다.**
