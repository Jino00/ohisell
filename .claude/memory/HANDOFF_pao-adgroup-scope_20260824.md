# 세션 인수인계: **스코프 이원화(캠페인+광고그룹) + PAO 대시보드 + 자(尺) 정직화**

> 저장일시: 2026-08-24 22:5x KST · 체인 `pao-논의` **n=47** · 세션 `0d535b6d`
> 앞 문서: `HANDOFF_keyword-accel-plan+canary-withdrawn_20260824.md`(n=45/46)
> **종합 판정: 부분달성** (완료 QA 3대조 — §2-1에 원문)

---

## 0. ★★우리가 향하는 곳 — PAO 북극성 궁극 목표 (원문 그대로)

> **정본**: `docs/references/82_pao_north_star_20260819.md` §1 (D-NAO-208)

**PAO의 궁극 목표는, 네이버 광고·스마트스토어 API가 주는 모든 지표와 4등급 성과등급 사이의 «개연성(연관)»을 홀드아웃 검증까지 통과한 지식으로 확정하고(①), 논문에서 채택한 기법과 이 트랙이 이미 세운 실행 구조(레인·가드레일·고삐·확장압력) 위에서(②), 우리가 운영하는 모든 광고 형태를 총이익 절대액이 최대가 되는 방향으로(③, D-NAO-59) 자동화 운영하며(④), 운영의 매 행위와 결과를 일기→반성→지혜 승격의 학습 사슬에 태워 어느 지점을 배우면 성능이 더 오르는지를 스스로 찾아, 신호가 허락하는 최단 주기로 성능개선을 반복 시도하는 것(⑤)이다.**

### 트랙 최상위 목표 (D-NAO-59, Jino 원문 2026-07-19)
> *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야."*

**진행률 2/7 (M0·M1) — 이번 세션 불변.** ④자동화 **19세션째 착수 0**.

---

## 1. 프로젝트 위치 및 환경

- 로컬: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling`
- ⚠️ **공유 메인 폴더는 origin/main보다 277커밋+ 뒤처져 있다.** 코드·문서는 `git show origin/main:<경로>`로 읽거나 워크트리를 써라.
- 이 세션 워크트리: `~/.claude-worktrees/ohiselling/pao-n47` · 브랜치 `fix/pao-profit-band`
- prod: `sellc.ohitech.co.kr` · DB `/home/ubuntu/ohisell/backend/ohisell.db` · 배포는 **반드시** `scripts/safe_deploy.sh`
- prod 읽기 전용: `ssh sellc.ohitech.co.kr 'sqlite3 -readonly "file:/home/ubuntu/ohisell/backend/ohisell.db?mode=ro" < /tmp/q.sql'` (SQL은 scp로 올린다 — ssh heredoc은 따옴표를 먹는다)
- ⚠️ **활성 포트가 배포마다 바뀐다**(블루-그린 8001↔8011) — `ssh sellc.ohitech.co.kr 'pm2 list'`로 확인. 이번 세션 마지막 배포 후 **:8011**
- ★**CI가 살아 있다** — 이번 세션 PR 3건 전부 3/3 실통과, `--force` 미사용

---

## 2. 이번 세션 완료 목록

### 2-A. D-NAO-244 — 자동운영 스코프에 «광고그룹» 축 (PR #422 `a09b6662`)
- ✅ `naver_adgroup_scope` 테이블(마이그 `d5e6f7a8b9c0`) — campaign_id·adgroup_id·**role**(accel/boundary/brake)·enabled·memo
- ✅ `adgroup_scope.blocked_by_scope()` = **생성·실행 두 게이트가 읽는 단일 술어**
- ✅ 게이트 3지점: `harness.execute()`(실쓰기 초크포인트) · `auto_operator` 승인 지점 2곳 · `search_term_ss_lane._open_exclusion`(P1 상환)
- ✅ 캠페인 레벨 액션(예산) hold — 예산은 광고그룹 귀속이 원리적으로 불가한 유일 누수구
- ★**행이 0개면 배포해도 행위 변화 0**(B3 카나리 게이트 원칙을 테이블로 이전)

### 2-B. PAO 스코프 대시보드 (같은 PR)
- ✅ 사이드바 「🎛️ PAO 스코프」 + 화면 `/naver-ad/scope`(`NaverAdScope.tsx`)
- ✅ `GET /api/naver/ad/scope/roster` — 캠페인 × 광고그룹 **횡단** 로스터(`perf_campaign_harness`는 캠페인 1개씩이라 원리적으로 불가했다)
- ✅ `PUT/DELETE /api/naver/ad/scope/adgroup` — 스코프 upsert·삭제(역할 검증·`change_log` 기록). ⚠️**계약 위반이었다 — §2-1·§3 참조**
- ✅ 새 산식 0 — 집계·이름 조인·BEP 사다리·보정계수·총이익 전부 기존 부품 재조합

### 2-C. BEP 사다리 요청 단위 캐시 (PR #423 `f40e1930`)
- ✅ `exploration.resolve_exploration_bep_roas(..., cache=)` **옵트인** 파라미터 — 기본 `None`이라 레인 경로 소급 0
- ✅ prod 실측 **10.1초 → 4.14초**(응답 바이트 118,953B 동일 = 값 불변)
- ★사다리를 호출부에 복제하지 않았다(두 벌은 반드시 갈라진다 — D-NAO-125)

### 2-D. ★총이익을 «있는 그대로 + 구간»으로 (PR #424 `fc035293`) — Jino 지시
- ✅ `_profit_band()` — `gross_profit`(**무보정**) + `_low` + `_high`
- ✅ 응답에서 **단일 `correction_factor.value` 제거** — 하나만 있으면 화면이 그걸 집는다
- ✅ 화면: 큰 글씨 = 있는 그대로 / 작은 글씨 = `[하한 ~ 상한]` · 구간이 raw를 안 감싸면 **⚠️ + amber**

### 2-E. 테스트
- 백엔드 **6,504 passed / 0 failed** · 프론트 **60파일 전부 실행** · `tsc --noEmit` 에러 0
- 신규: 백엔드 40종+ · 프론트 11종. **표면 절단 변이** SUR-1~6 전부 사망 확인

---

## 2-1. 완료 QA (별도 Sonnet·읽기 전용, 2026-08-24 22:35 KST) — **판정 원문 그대로**

- **작업 목적(정본=승인된 계약)**: PAO 자동운영 스코프를 **캠페인 + 광고그룹 이원**으로 만든다. 캠페인 안 일부 광고그룹만 엔진에 맡기고 나머지는 손대지 않는 것이 구조적으로 가능하고 **코드가 그것을 강제하는** 상태. + Jino 19:35 *"ohisell에 PAO 메뉴를 만들어서 어떤 캠페인 - 광고그룹 을 돌릴지, 그 성과는 어떻게 나오는지 보여주는 대시보드를 같이 만들자"*

**판정(승인된 계약 §4 합격기준): 부분달성** — 달성 8/10(①②③④⑥⑧⑨⑩), 부분달성 1(⑦ 독립커넥션 동시성 미재현·자인됨), 판정불능 1(⑤ 라이브 시나리오 관측 불가·격리 테스트만) (2026-08-24 22:35 KST)

**판정(Jino 2026-08-24 지시 원문): 부분달성** — 19:30·21:31·21:34는 정확히 달성(캠페인+광고그룹 이원화 / 「있는 그대로」 raw 전환매출을 큰 글씨로, 보정구간을 작은 글씨 병기 / raw 값이 곧 광고 직접귀속 conv_amt로 계수=1). 19:35는 「보여주는 대시보드」 요청을 넘어 **읽기+쓰기(PUT/DELETE `/scope/adgroup`, 화면 토글·역할선택·삭제)**까지 구현했고, 이는 **같은 계약의 "이번에 안 하는 것" 목록에 명시된 "스코프 편집 UI의 «쓰기»(읽기 표면만)"을 직접 위반**한다 — 목적전환 선언 없음. 13:07의 "논의 완성"은 이 슬라이스가 부분적으로만 기여(스코프 인프라 마련, 논의 자체의 완결은 아님) (2026-08-24 22:35 KST)

**판정(PAO 북극성 §1·§6·§7): 부분달성** — 이 작업은 L3 실행 인프라의 세분화로, §6 M0~M6 어느 항목에도 명시적으로 대응하지 않지만 §8-②(대행사 소유권 분리)의 향후 메커니즘으로 합리적 정렬됨. 계약이 자인한 대로 "M4 착수 아님"이 라이브로 확인됨(9캠페인 전건 `auto_operate=0` 불변) — §7 "자동화 확대 시 액셀·브레이크 대칭 검사" 요구는 자동화 자체가 0이라 해당 없음(정직하게 회피 안 함) (2026-08-24 22:35 KST)

**종합 판정: 부분달성**

**QA가 확인 못한 것**: 화면 육안(prod basic auth) · 항목④의 엄밀한 배포 전-후 스냅샷 비교(코드 불변성 논증+패턴 관찰로 대체) · 항목⑤⑦의 실제 WAL 크로스프로세스 동시성(QA 쓰기 금지 + 인메모리 SQLite 한계, 테스트가 자인).

**★QA가 어긋났다고 본 것 2건**(QA는 처분을 지정하지 않았다):
1. **계약 「안 하는 것」 위반** — 「스코프 편집 UI의 «쓰기»(읽기 표면만)」과 달리 완전한 쓰기 UI를 만들었다(`naver_ad.py:845,892` · `NaverAdScope.tsx:233-262`). **목적전환 선언 없음.**
2. **`campaign_level_allowed_now`가 호출부 없이 테스트에서만 쓰인다** — 예산 hold는 harness의 `blocked_by_scope(adgroup_id=None)` 경로로 이미 이뤄져 결함은 아니나, **PR 본문 설명이 실제 배선보다 넓었다.**

**목적 전환 여부**: `🔁 목적 전환` **선언 없음.** 1번은 §2 「선언 없는 전환은 결과가 좋아도 미달(무단전환)」에 해당한다.

★**처분(D-NAO-245, Jino 22:47 *"유지하자"*)**: 쓰기 UI **유지**. 계약 「안 하는 것」이 개정됐다(§1 승인 지점 ①).
⚠️ **그러나 판정은 안 바뀐다** — 완료 QA의 「부분달성」은 «약속 대비»를 잰 기록이고, 사후 승인이 그 기록을 지우지 않는다(§2 「미달 판정 뒤 합격기준을 낮추는 것은 금지」).

---

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: "무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야." (Jino 2026-07-19 — D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음(M 체크박스 기준).** 계약이 「트랙 M 체크박스는 움직이지 않는다」고 **미리** 못 박았다. L3 인프라(스코프 축)와 자(尺)의 정직화가 늘어난 것이지 M이 닫힌 게 아니다.
- **헤더에 남긴 확인 줄**: 2개 — `확인: 2026-08-24 22:3x KST [0d535b6d]`(D-NAO-244 완주+QA) · `확인: 2026-08-24 22:47 KST [0d535b6d]`(D-NAO-245 쓰기 UI 유지)
- **다음 세션 후보 항목**: **M2 S2** — 카나리의 실질 선행조건(데이터 하한 게이트)이 거기 있다. 그리고 §6의 신규 항목 ⑪·⑭(아래).
- **트랙 종결 여부**: 미도달(2/7).

---

## 2-3. 착지

(Step 6에서 채움)

---

## 3. 확정된 결정사항 (번복 금지)

- **D-NAO-244 — 스코프 이원화** (Jino 2026-08-24 *"우리 엔진의 스코프는 캠페인, 광고그룹 모두 포함해야해"*). 결합 규칙 «캠페인 마스터 ∧ 그룹 제한». 「캠페인 OFF인데 그룹만 ON」은 **일부러 지원 안 함**(끄는 방향이 항상 이긴다). 스코프 행이 전부 disabled면 **전 그룹 OFF**(전체로 폴백하지 않는다).
- **D-NAO-245 — 스코프 편집 쓰기 UI 유지** (Jino 22:47 *"유지하자"*). 계약 「안 하는 것」 개정. ⚠️판정은 부분달성 그대로.
- **총이익 표시는 「있는 그대로 + 구간」** (Jino 21:31). 단일 보정값 금지. `correction_factor.value` 부활 금지(테스트가 막는다).
- **보정계수는 「채널 매출 100%가 광고 공」 가정과 동치다** — 분자에 광고 귀속 조인이 없다(`diagnosis.correction_factor` 주석 자백). 표시에 상한 단독 사용 금지.

---

## 4. 핵심 파일 목록

| 파일 | 역할 |
|---|---|
| `backend/app/services/naver_ad/adgroup_scope.py` | ★진리표 단일 소스 `blocked_by_scope` · 역할 상수 |
| `backend/app/services/naver_ad/pao_scope_roster.py` | 횡단 로스터 · `_profit_band`(있는 그대로+구간) |
| `backend/app/services/naver_ad/exploration.py:293` | BEP 사다리 + 옵트인 `cache` |
| `backend/app/services/naver_ad/naver_execution_harness.py:2593` | 실행 게이트(`ScopeGuardError`) |
| `backend/app/services/naver_ad/search_term_ss_lane.py:346` | ★P1 상환 — harness 우회 경로의 스코프 게이트 |
| `backend/app/routers/naver_ad.py:845,892` | `PUT/DELETE /scope/adgroup` |
| `frontend/src/pages/NaverAdScope.tsx` | PAO 스코프 화면 |
| `frontend/src/pages/paoScopeReachesTheUser.test.tsx` | ★표면 절단 변이 SUR-1~6 |
| `docs/references/95_inflowpath_yardstick_census_20260823.md` | ★「광고가 판 것」의 정본 |
| `docs/references/93_correction_factor_consumer_census_20260823.md` | ★§1 행 9 = 표시 소비처 처방 |

---

## 5. 알려진 이슈 / 주의사항

### 5-A. ★★자(尺)에 대해 이 세션이 알아낸 것 — 다음 세션 필독
- **「광고가 판 것」의 명확한 숫자는 존재한다**: `orders.raw_data.productOrder.inflowPath`, **커버리지 100%**, 37종 라벨(ref 95)
- 30일 실측: 「광고>」5종 **24,317,650** ÷ 리포트 direct **29,400,780** = **0.827** ← D-NAO-234 하한의 정체
- ⚠️**[미상]**: `네이버플러스스토어검색>광고` **7,067,000원(447건 규모)**이 우리 SA인가 — **이 한 줄이 자를 0.827 ↔ 1.068로 가른다.** 네이버 공식 문서로 확인해야 한다(추정 금지)
- ⚠️**구조적 한계**: inflowPath는 **계정 단위**다. 주문에 「어느 캠페인이 데려왔는지」가 없어 **광고그룹별 총이익의 정확도는 원리적으로 이 한계 안에 있다**
- ★규율(ref 95 §2): inflowPath는 **마지막 터치**라 SA 간접전환을 못 잡는다 ⇒ **정합한 비교는 `÷ direct`뿐**

### 5-B. 예산 레버는 지금 거의 무력하다 (이번 세션 실측)
- 30일·1,207(날짜×캠페인) 중 **소진율 90%+ 는 20건(1.7%)** · **평균 6.3%**
- 유일한 예외 **TPU 65.5%**. 나머지는 1~20%(아이폰 파워링크 **1.9%**)
- ⇒ **병목은 예산이 아니라 입찰**(7월 「입찰 70원인데 51일 노출 0인 활성 키워드 99%」와 같은 이야기)
- ★**광고그룹 일예산은 실재한다**(`entity_sync.py:69` `GROUP_LIMITED_BY_BUDGET`) — 그런데 우리는 ①값을 DB에 안 담고 ②쓰기 함수가 없다. **쓰기 API 지원 여부 [미상]**

### 5-C. 기타
- ★`safe_merge.sh`가 한 번 **정상 거부**했다 — 체크 0건 시점에 *"이건 초록이 아니라 «모름»이다(교훈 #123)"*. `--force` 쓰지 않고 CI를 기다렸다. **이 도구를 우회하지 마라.**
- 워크트리에 `node_modules`가 없다 → `npm ci` 필요(약 1분). **다른 세션 워크트리의 것을 쓰지 마라**(`cost-menu-n8`은 살아 있는 체인 것)
- 다음 가용 번호: **D-NAO-246 · 교훈 #356** (`scripts/next_ids.sh` 재실행 필수 — 병행 세션 활발)

---

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: Jino 2026-08-24 13:07 *"나는 키워드 관련한 광고 운영 관련해서 더 논의를 해서 기획을 완성하고 싶어. 다음 세션에서 그걸 최우선으로 진행하자"* + 17:1x *"아직 오늘 논의한 내용조차 완성이 안됬는데"*
  ⇒ **읽는 법**: 「오늘 켜라」가 아니라 **「논의한 것을 전부 완성하고 그 다음에 켜라」**

- **남은 슬라이스** (n=45 §6의 13개 중 미완 + 이번 세션 신규):
- [ ] **① 카나리 계약 승인** — Fable 초안(처치 5그룹 역할 배정·하드스톱 2종·개방 사다리·2단 판정)은 **미승인 상태로 §7에 원문 보존**. ★**물어야 한다**(범위를 바꾸는 결정)
- [ ] **② 카나리 대상 재확인** — 강화유리(03)는 **MOP가 매일 2~5만원 집행 중**이라 D-NAO-13 충돌. 04는 07-30 이후 25일째 0원. ★**물어야 한다**
- [ ] **③ 데이터 하한 게이트**(M2 S2-⑤) — 표본 0.6%(494그룹 중 3개)에서 나온 판단이 카나리에 실리는 걸 막는다. **묻지 말고 진행**
- [ ] **④ 주말·공휴일 라벨**(M2 S2-④) — ⚠️`diary.py:52-54`·`probe_cell_aggregate.py:51-57`에 **판별 코드는 이미 있다**. 신설분은 «적용 배선»뿐. **묻지 말고 진행**
- [ ] **⑤ 대조군 지정** — 없으면 판정 자체가 불가(7월의 정확한 재방송). **묻지 말고 진행**
- [ ] **⑥ 7월 잔여물 처분** — 그 캠페인 21개 모델의 현재 입찰을 「유지/원복」으로 판정. **묻지 말고 진행**
- [ ] **⑦ 화면**: 콘솔 **페이지네이션**(`loadProposals()` `limit:100` 고정·offset 없음). ⚠️타입 필터는 **이미 배포됨**(`NaverAdOptimizationConsole.tsx:231-260`). **묻지 말고 진행**
- [ ] **⑧→⑫ 판정 규칙 = 판단 «층» 나누기** — 매시간(지혈·서빙여부·배분) / 일(클릭반응) / D+7~14(전환·총이익) / 2~4주(전략). ★매시간엔 **되돌릴 수 있고 표본 불요한 것만**. **묻지 말고 진행**
- [ ] **⑨ 검색량 시드 확장** — `keyword_volume_baseline.head_keywords()`가 SHOPPING을 직접 시드로(코드 0건 확인). 커버리지 62%→90%+. **묻지 말고 진행**
- [ ] **⑪ ★과거 데이터 반응 곡선 (최우선 후보)** — `naver_change_log` update_bid 전수(우리 425건 + 대행사분)에 전후 노출·클릭·전환을 붙여 ⑴입찰-노출 반응 ⑵포화점 ⑶**효과 크기 → 필요 표본·판정 기간 산출** ⑷**그 곡선의 오차**. 광고비 0원·승인 불요. **묻지 말고 진행**
- [ ] **⑬ Fable 위임 규율** — 데이터 판단을 맡길 땐 요약이 아니라 **DB 접근**을 준다(이번 세션에 실제로 적용해 효과 확인 — Fable이 내 51일 짝짓기를 최근 구간 실측으로 뒤집었다)
- [ ] **⑭ ★[미상] 해소 2건 (신규)** — ⓐ`네이버플러스스토어검색>광고`가 우리 SA인가(네이버 공식 문서) ⓑ**광고그룹 일예산 쓰기 API** 지원 여부. ⓑ가 되면 Fable의 「예산은 그룹 귀속 불가」 전제가 무너지고 누수구가 통째로 사라진다. **묻지 말고 진행**(조사만)
- [ ] **⑮ h(문턱) 산출 (신규)** — 「총이익이 늘면」이 아니라 **「한계이익률 ≥ h」**. h = 자본의 기회비용 = 재고 회전율 × 마진 ÷ 연간 광고 사이클 수. 재료 있음(`product_master` 963개 **전건 원가 보유** · `inventory` 이동 원장 · 주문 이력). ⚠️`inventory`가 스냅샷이 아니라 **이동 원장**이라 평균 재고 재구성 필요. ↗️원가·재고 트랙 소관일 수 있음 — 계약 짤 때 정할 것
- [ ] **⑯ 로스터 N+1 잔여 (신규)** — 4.14초까지 줄였으나 여전히 그룹 tier는 407회 조회. 더 줄이려면 벌크 조회가 필요(현재는 「충분히 빠름」으로 판단)

★**묻지 말고 진행할 것**: ③④⑤⑥⑦⑧⑨⑪⑬⑭⑮⑯ (되돌릴 수 있고 범위를 안 바꾼다)
★**물어야 할 것**: ①② (범위·목표를 바꾸는 결정)

---

## 7. Fable 카나리 설계 — 미승인 원문 보존 (n=45·47 합본)

> **저장소 계약 파일로 만들지 않았다** — 승인 안 된 문서가 `docs/contracts/`에 있으면 가짜 금지선이 된다.

**처치 5그룹(역할 기반, 최근 21일 기준)** — 짝짓기는 **폐기**했다(51일과 최근 7일이 순위를 뒤집는다):

| 역할 | 그룹 | 21일 ROAS | 21일 총이익 | 최근7일 일지출 |
|---|---|---|---|---|
| 액셀 | Z플립3 | 2.98 | +25,759 | 2,341 |
| 액셀 | Z폴드6 | 2.42 | +17,211 | 2,715 |
| 경계(북극성) | S23울트라 | 1.67 | −14,862 | 14,172 |
| 브레이크(A2형) | S25울트라 | 1.14 | **−102,352** | 13,321 |
| 브레이크(C형) | S20 | 0.75 | −81,461 | 4,564 |

- 처치 합산 일지출 **37,115원**(캠페인의 22.5%) · 21일 전환 **126건**
- ★**상방 위험은 액셀+경계 19,228원(11.6%)뿐** — 브레이크의 실패 모드는 「못 줄임」이라 상방이 아니다
- **대조군 = 잔여 약 48그룹 합산**(손으로 고른 3그룹이 아니라 — 큰 합이 개별 특이추세를 희석)
- **제외**: Z폴드8와이드·Z폴드8울트라·Z플립8(출시 준공선, D-NAO-183)
- **하드스톱 2종**: 코호트 합산 > 앵커×2 (2일 연속) → 전체 disable / **브레이크 역할 개별 > 자기 앵커×1.5** (2일 연속) → 그 그룹만
- **기준선은 개시일 직전 7일로 앵커·고정**(롤링이면 엔진이 자기 상한을 끌어올린다)
- **판정**: 4주·마지막 7일 제외 · **코호트 합산 diff-in-diff**(★그룹 단위 부호는 17일 창에서 노이즈 — S25FE도 21일 창엔 적자) · 처치 전환 <60건이면 **판정불능** 예약
- **전제조건**: **대행사 hands-off 지시(Jino)가 스위치보다 먼저**

---

## 8. 새 세션 시작 프롬프트

```
/session-relay PAO 논의
```
