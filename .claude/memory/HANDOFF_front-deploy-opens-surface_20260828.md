# 세션 인수인계: 1P계산서 목표 — **프론트 배포 한 단계가 표면 4항목을 열었다**

> 저장일시: 2026-08-28 16:2x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것
> 체인 `1p-계산서` **n=4** · 세션 `cb9cc75e`

## 1. 프로젝트 위치 및 환경
- 저장소: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 메인 폴더 — **이 세션은 미접촉**)
- 작업 위치: 워크트리 `~/.claude-worktrees/Ohiselling/1p-invoice-n4` (브랜치 `feat/1p-invoice-n4`)
- prod 백엔드: **포트를 매번 재라.** 이 세션 착수 시 **8011**. `ssh sellc.ohitech.co.kr "ss -lntp | grep -E ':(8001|8011)'"`
- ★**prod 웹은 nginx basic auth(401) 뒤에 있다** — 외부 `curl`로 화면을 열 수 없고 **자격증명 입력은 하지 않는다.** 화면 검증은 **서버에서 번들을 직접 세는** 것으로 대체한다.
- prod 조회: `scp -q q.py sellc…:/tmp/q.py && ssh sellc… "cd /home/ubuntu/ohisell/backend && .venv/bin/python - < /tmp/q.py"`
- 프론트 `node_modules`는 이 워크트리에 없다 — 형제 워크트리로 심볼릭 링크했다(`ln -s ../../1p-invoice-n3/frontend/node_modules`). gitignore됨.

## 2. 이번 세션 완료 목록

### ★인계 1순위 「프론트 배포 재시도」 완주 — 차단 요인이 스스로 소멸해 있었다
n=3이 막힌 이유는 「prod 프론트가 **어느 원격에도 없는 커밋**에서 배포돼 있어, 밀면 그 수정이 지워지고 복구 출처도 없다」였다(2026-08-06 clobber 사고의 재발 조건).

| | n=3 (11:40) | n=4 착수 (16:0x) |
|---|---|---|
| prod 스탬프 | `f4a513f4`→`72d3efd7` — 원격에 없음 | `c816d0f3` (`feat/cost-menu-n17`, 15:01:45 KST) |
| `origin/main`의 조상인가 | **아니오** ⇒ CAS 거부 | **예** ⇒ CAS 통과 |

원가 메뉴 트랙이 그 사이 자기 작업을 push·머지했다. ⇒ **`--force-frontend` 없이** 정상 배포.

- ✅ 배포 스탬프 `commit=22bc2ff5eb85…` · `branch=feat/1p-invoice-n4` · `ts=2026-08-28T07:07:38Z`
- ✅ 번들 `assets/index-BGH3fym_.js`(1,500,611 B) — 옛 `index-IPEErPaA.js` 제거, `index.html`이 새 번들 하나만 참조
- ✅ nginx `sellc.ohitech.co.kr` vhost root = `/home/ubuntu/ohisell/frontend/dist`(배포 대상과 동일)
- ✅ `deploy-manifest.jsonl` 마지막 행 `{"kind":"frontend","commit":"22bc2ff5","forced":false}` — 완료 QA가 독립 확인

### ★계약 §4 「1/8 → 5/8」 — 화면 표면 4항목이 한꺼번에 열렸다
직전 QA가 「1/8인 **유일한** 이유」로 지목한 자리가 그대로 열렸다. 열린 항목: **S1-1**(두 줄 분리 표면) · **S1-3**(「처음 관측됨」) · **S1-4**(발주 지목 → 이력 시간순) · **S1-5**(배선 전 자백 문구).

번들 실측(완료 QA 재계수, python `str.count()`):

| 표면 문자열 | 출현 | | 금지 문구 | 출현 |
|---|---|---|---|---|
| 이번 수집에서 달라진 것 | **3** | | 신규 발주 발생 | **0** |
| 처음 본 발주 | 1 | | 로 들어옴 | **0** |
| 상태·수량이 바뀐 발주 | 1 | | 확정됨 | **0** |
| 처음 관측됨 | 1 | | | |
| 관측 이력 | 2 | | | |
| 그 전 변화는 기록이 없습니다 | 1 | | | |

**라이브 실사례**: 배선 전 발주 `139899796` 조회 → `rows=[]`인데 `empty_reason="이력은 2026-08-28부터입니다 — 그 전 변화는 기록이 없습니다"`. 빈 배열이 「변화 없음」으로 읽히지 않는다.

### ★★계수 방식이 틀릴 뻔했다 — `grep -c`는 «줄» 수다
내가 처음 쓴 `grep -c "<문자열>" index-*.js`는 **매칭된 줄 수**를 센다. minify된 번들은 사실상 한 줄이라 **몇 번 나오든 언제나 1**이다. 완료 QA가 python `str.count()`로 다시 세어 「이번 수집에서 달라진 것」이 실제 **3회** 출현임을 확인했다. 판정에 쓸 값은 QA의 계수다.
★그리고 QA 자신이 첫 시도에서 셸 `for` 루프 + 이스케이프로 한글 다중바이트를 grep했다가 **전건 0건이라는 거짓 관측을 만들 뻔했다**(`ssh-heredoc-strips-quotes-use-scp`와 같은 함정). 스스로 자백하고 python 경로로 전항목 재확인했다. — **번들 문자열 검증은 셸 grep 말고 python `str.count()`로 하라.**

### ★부수 정정 2건 (인계 문구가 부정확했다)
1. n=3 인계의 **「새로 들어온 발주」는 실제 문구가 아니다** — 화면 문구는 **「처음 본 발주」**다. 인계 표현을 그대로 grep했으면 0건이 나와 「배포가 안 됐다」로 오판했을 자리다.
2. 신선도 문구 **「지금 상태 모름」은 이미 prod에 있었다**(배포 전 실측 1건) — 원가 메뉴 트랙이 15:01 배포하며 `origin/main`의 문구 PR(#528)을 **같이 올려 줬다**. ⇒ 이 세션이 실제로 연 것은 **발주 변화 카드·이력 상세**뿐이다. 인계는 셋 다 대기 중이라 했다.

## 2-1. 완료 QA
> 별도 서브에이전트(Sonnet, 읽기 전용) · 2026-08-28 16:16 KST · **판정 원문 그대로**

- **작업 목적(정본 원문)**: Jino 2026-08-27 *"이걸 모아서 내가 누를 수 있게 해줘"* / *"내가 원하는건 SellC에서 눌러서 처리하는거야"* + Jino 2026-08-28 13:24 *"그래, 고치자"*(②가 왜 늘었는지 우리가 답을 못 하는 것)
- **합격기준(원문)**: 계약 `CONTRACT_1p_po_status_history.md` §4 체크박스 8항목 (현재 **5/8**)

```
판정(계약 §4 발주 관측 원장): 부분달성 — S1-1·S1-3·S1-4·S1-5·S1-8 5항목 모두 라이브
재검증 일치(번들 grep·API curl·DB SELECT·소스 grep 4중 교차), 과잉·부실 체크 0건.
S1-2·S1-6·S1-7은 라이브 미발현으로 정당하게 이월. "5/8은 「주장」이 아니라 실측
그대로였다." (2026-08-28 16:16 KST)

판정(Jino 지시 원문): 부분달성 — 발단 문제("②가 왜 늘었는지 답 못함")를 답할
메커니즘(first_seen/changed 구분·이력 상세)이 배포까지 완주했고 번들·API에서 정확히
작동함을 확인했다. 그러나 아직 라이브에서 실제 "②가 늘어난 사건"에 이 메커니즘이
적용되어 Jino가 답을 얻은 사례는 0건(회차 1개뿐, field_change 0건) — 고쳐졌다는
«증명»은 다음 실전 발주 유입/전이 회차를 기다려야 한다. (2026-08-28 16:16 KST)

판정(트랙 궁극 목표): 판정불능 — 이 작업은 트랙 헤더 스스로 「트랙 체크박스 항목이
아니다」로 못박은 체인 슬라이스이고, 트랙 10/12 진행률도 불변이라고 diff로 확인됨.
트랙의 남은 항목(S5 종합조망 뷰, S6 self-verify)은 이 작업 범위 밖이라 전진도 후퇴도
판정할 근거가 없다. (2026-08-28 16:16 KST)

종합 판정: 부분달성 — 계약·지시 원문 두 대조 모두 「메커니즘은 정확히 배포·작동하나
라이브 실전 발현이 아직 없다」는 같은 이유로 부분달성이고, 트랙은 원래 범위 밖이라
판정불능이다.
```

### ★QA가 잡은 **부실 체크 4건** — 수용하고 고쳤다
앵커 「합격」 1~4번이 **증거가 다 있는데 `[ ]`로 남아** 있었다(체인 note·PR 본문에는 완주로 적어 놓고 체크박스만 갱신 안 함). QA 원문: *"다음 세션이 이 앵커만 보고 「아직 안 됐다」로 오판할 위험이 있다."*
★**직전 세션이 «과잉» 체크로 걸린 것과 정확히 반대 방향의 같은 병이다** — 체크박스가 서술문과 갈라지면 방향과 무관하게 다음 세션을 속인다.

### ★★트랙 판정이 「미달」에서 「판정불능」으로 정정됐다
직전 세 세션이 트랙 대조를 전부 **미달**로 적어 왔는데, QA가 *"전진도 후퇴도 판정할 근거가 없다"*며 **판정불능**으로 바로잡았다. 계약 §6이 스스로 「트랙 체크박스를 움직이지 않는다」고 명시한 슬라이스에 「미달」을 적는 것은 **약속하지 않은 것에 대한 감점**이었다.

### QA가 확인 못 한 것(원문)
①**브라우저에서 사람이 렌더된 화면을 본 사실** — prod가 nginx basic auth(401) 뒤이고 QA는 자격증명을 입력하지 않는다. *"API 200·번들 문자열·소스 코드 3중 교차까지가 내가 닿은 한계이며, S1-1·S1-3·S1-4·S1-5의 「달성」 판정은 이 한계 안에서 내린 것이다."* ②PR #548 CI(조회 시점 pending — **이후 3/3 통과·머지 `56853af4`**) ③S1-2·S1-4의 실제 상태 전이(RP→PA) 사례 — `field_change` 0건이라 「구간이 양끝 다 찬」 렌더는 코드 존재만 확인.

### 목적 전환 여부
**없음**(`🔁 목적 전환` 0건 · `↗️ 스코프 밖` 0건).

## 2-2. 트랙 진행률
- **트랙**: `docs/tracks/active/track_coupang-rocket-1p.md`
- **트랙 목표 원문**: `"우리가 지금까지 Ofix에서 한 일을 OhiTech에서도 구현할 수 있어? 물론 OhiTech는 로켓배송이 추가되지" / "발주한 금액, 납품한 공급가, 정산 금액을 모두 봐야지. 매출은 쿠팡이 발주한 금액이 될꺼고" / "광고비용이 빠지겠지?"`
- **진행률**: 시작 **10/12** → 종료 **10/12** (불변)
  - 미달: **S5**(종합조망 로켓배송 뷰/축 + 갱신 버튼 + 원가 매핑 관리 UI + 커버리지% 배지) · **S6**(prod 라이브 self-verify + 적대 리뷰 + 배포)
- **이번 세션이 움직인 항목**: **없음.** 이 계약은 트랙 체크박스 항목이 아니다(계약 §6 명시) — 체인 슬라이스다.
- **헤더에 남긴 확인 줄**: 1개 추가(16:1x)
- **다음 세션 후보 항목**: **S5** (유일하게 남은 기능 슬라이스)
- **트랙 종결 여부**: **미도달**(10/12)

## 2-3. 착지
- **완료 단계**: 배포 → 커밋 → push → PR → 머지 → **완주**
- **좌표**: PR **#548** 머지 `56853af4` · CI **3/3 실통과**(backend py3.10 10m17s · py3.14 8m37s · frontend 1m33s) · `--force` 미사용
- **리뷰 판정**: ⚠️ **리뷰 생략: 기록물만** — `git diff --name-only origin/main...HEAD` = 계약 1 · 트랙 1 · 체인 등록부 1, **코드 0파일**. 프론트 배포는 이미 `origin/main`에 머지·리뷰된 코드를 빌드해 올린 것이다.
- **배포**: 프론트 **1회**(CAS 통과·`--force-frontend` 미사용). 백엔드 배포 **0회**, 마이그레이션 **0건**.
- **멈춘 단계**: **없음**
- **정정 경로**: `git revert -m 1 56853af4` · 프론트는 prod 백업 `dist_backup_20260828_1607`로 복구(`ssh sellc… "rm -rf …/frontend/dist && mv …/frontend/dist_backup_20260828_1607 …/frontend/dist"`). **force-push 미사용**.
- **L5로 「main에 세워둔다」 생략** — 로컬 `main`이 공유 메인 폴더에 체크아웃돼 있다.

## 3. 확정된 결정사항
- **번들 문자열 검증은 python `str.count()`로 한다** — `grep -c`는 줄 수라 minify 번들에서 언제나 1이고, 셸 이스케이프는 한글 다중바이트에서 거짓 0건을 만든다.
- **prod 화면 검증의 한계는 basic auth다** — 자격증명을 입력하지 않으므로 「사람이 봤다」는 영원히 QA 밖이다. 번들 적재·서빙 경로·API 200까지가 우리가 댈 수 있는 최대 증거다.
- **트랙 범위 밖 슬라이스에 「미달」을 적지 않는다** — 약속하지 않은 것에 감점하는 것이라 **판정불능**이 맞다(QA 정정).
- **체크박스는 서술문과 갈라지면 방향 불문 다음 세션을 속인다** — 과잉도 부실도 같은 병이다.

## 4. 핵심 파일 목록
| 파일 | 역할 |
|---|---|
| `docs/contracts/CONTRACT_1p_po_status_history.md` | **정본 계약** — §4 합격기준 **5/8** |
| `docs/contracts/CONTRACT_1p_invoice_confirm_write.md` | 쓰기 실행 계약 — §4 **5/9**(S2 대기) |
| `frontend/src/pages/rocketPipelineTabs.tsx` | `PoChangesCard`·`PoHistoryDetail`·확인 버튼·신선도 문구 |
| `backend/app/routers/overview.py` | `/rocket-po-changes`, `/rocket-po-changes/{seq}` |
| `backend/app/services/coupang/rocket_po_changes.py` | 조회 — `latest_round_changes`·`po_history`·`history_start` |
| `docs/references/107_ri_confirm_canary_20260828.md` | 카나리 실측 전문 |

## 5. 알려진 이슈 / 주의사항
- ★**prod 백엔드 활성 포트가 자주 뒤집힌다**(블루그린). 인계에 적힌 포트를 믿지 말고 `ss -lntp`로 **매번** 잴 것.
- ★**Mac 페처 런타임은 저장소가 아니다**(`~/.ohisell/tools/` 사본, 교훈 #371). 머지·prod 배포를 다 해도 설치본은 안 바뀐다 — `grep -c <새심볼> <실행경로>`로 세라.
- **여전히 1회전뿐**: `coupang_rocket_po_ingest_round` **1행**(15:12:44) · `coupang_rocket_po_change_log` **1행**(`first_seen`). S1-2·S1-6·S1-7은 이 상태가 풀려야 판정 가능하다.
- (이월, n=3에서 인수) 체인 `sellc-원가-메뉴` n=15가 `end_kst`를 채워 닫힌 뒤에도 커밋·prod 배포 — 소관: **하네스 규율**(전역 §6) + 원가 메뉴 트랙.
- (이월, n=3에서 인수) `~/.ohisell/tools/ad_settings_collect.py`는 어느 브랜치에도 없는 미커밋 배포본 — 소관: 광고 수집 트랙.
- (이월, n=3에서 인수) `backend/tests/test_rocket_promo_fetcher.py` 단독 실행 시 40 errors(순서 결합) — 소관: 로켓1P 트랙.
- (이월, n=3에서 인수) 훅 `review-surface-mutation.sh`가 **완료 QA 위임**에서 오발화 — 소관: 하네스 주기 감사.

## 6. 다음에 할 작업 (미완료)
> ★**3문 검사**: 아래 셋 다 ①목표·범위를 안 바꾸고 ②되돌릴 수 있으며 ③따라서 **묻지 말고 진행**한다. 단 **S2의 «누르는 행위» 자체는 Jino 손**이다(계약 §1: 매 건 사람 클릭이 승인 지점).

- **이어지는 작업의 목적(원문)**: Jino 2026-08-27 — *"그리고 거래명세서확인요청 내용을 SellC에서 모아서 볼 수 있나? **이걸 모아서 내가 누를 수 있게 해줘**"* / *"딥링크는 뭐야? **내가 원하는건 SellC에서 눌러서 처리하는거야**"*
- **남은 슬라이스**:
- [ ] ★**S2 전량 개방 — 이제 이게 1순위다.** 살아 있는 **6건 2,401,425원**을 Jino가 한 건씩 누른다(코드 0줄, 카나리와 같은 경로). 굳은 **8건 4,870,435원**은 「미종결 발주 재수집」을 한 번 돌리면 열린다. **쓰기 계약 §4의 S2 2항목이 이걸로 닫힌다.** ⚠️먼저 RI 큐를 다시 재라 — 굳음/살아있음 구성은 재수집마다 바뀐다.
- [ ] **관측 원장 계약 §4 나머지 3항목** — S1-2(상태 전이)·S1-6(수량·금액 diff)·S1-7(멱등 2회전). **코드는 다 있고 라이브 발현만 기다린다.** 다음 수집 회차가 2개 이상 쌓이면 그 자리에서 재판정(같은 QA 기에 그 항목만, 전역 §2 재판정 1회 규칙).
- [ ] `already_confirmed`·`unknown`이 처음 나면 **응답 원문을 ref 107 §7에 덧붙일 것** — 쓰기 계약 §7의 남은 `[미상]`(**실패 응답의 모양**)은 그때만 풀린다.
- [ ] (트랙 별건) **S5** — 종합조망 로켓배송 뷰/축 + 갱신 버튼 + 원가 매핑 관리 UI + 커버리지% 배지. 트랙 10/12를 움직이는 **유일한 남은 기능 슬라이스**. ★체인 슬라이스와 **다른 것**이니 섞지 말 것.

## 7. 새 세션 시작 프롬프트
```
/session-relay 1p-계산서
```
