# 세션 인수인계: **D-NAO-230/231 「총이익의 자」 구간 전환 — 구현·배포·병합 완주**

> 저장일시: 2026-08-23 **14:1x** KST · 체인 「PAO 논의 **39**」 (세션 `2a88cd92`)
> 새 대화 시작 시 이 파일을 먼저 읽을 것. 체인 이어받기: `/session-relay PAO 논의` — 이번이 **39**번이었다(다음은 40).
> 이번 세션은 **코드·배포·prod 쓰기가 있었다**. PR **#345** 병합(`b39c8bc1`) · prod 가동 중.

---

## 0. ★★우리가 향하는 곳 — PAO 북극성의 궁극 목표 (원문 그대로)

> **정본**: `docs/references/82_pao_north_star_20260819.md` §1 (D-NAO-208)
> ⚠️**참조 경로가 아니라 «원문»으로 싣는다.** 참조만 두면 안 읽힌다 — 이 트랙은 그걸로 두 번 샜다(교훈 #346).

**PAO의 궁극 목표는, 네이버 광고·스마트스토어 API가 주는 모든 지표와 4등급 성과등급 사이의 «개연성(연관)»을 홀드아웃 검증까지 통과한 지식으로 확정하고(①), 논문에서 채택한 기법(ref 26 TOP5 + ref 33 [0]~[10])과 이 트랙이 이미 세운 실행 구조(레인·가드레일·고삐·확장압력) 위에서(②), 우리가 운영하는 모든 광고 형태(SHOPPING·WEB_SITE·BRAND_SEARCH — ADVoost·GFA는 성과 관측이 원리적으로 막혀 있어 별도 취급, §8-④)를 총이익 절대액이 최대가 되는 방향으로(③, D-NAO-59) 자동화 운영하며(④), 운영의 매 행위와 결과를 일기→반성→지혜 승격의 학습 사슬에 태워 어느 지점을 배우면 성능이 더 오르는지를 스스로 찾아, 신호가 허락하는 최단 주기로 성능개선을 반복 시도하는 것(⑤)이다.**

### 트랙 최상위 목표 — Jino 원문 (D-NAO-59, 2026-07-19)

> *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."*

### 마일스톤 지도 — §6 M0~M6 (현재 위치 **2/7**, 이번 세션 불변)

| M | 내용 | 상태 |
|---|---|---|
| **M0** | 효율 검정 완주 (L1) | ✅ 달성 |
| **M1** | L0 마감 — C10·커머스 75건 | ✅ 달성 |
| **M2** | L2 배선 — ref 65 S1+S2 | ❌ 미달 (M2-d 의존) |
| **M3** | L4 부품 — 지혜 성적표 + 확정 지식 주입 | ❌ 미달 (b·a 닫힘, c는 M2-d 의존·z 미실시) |
| **M4** | **L3 재개 — 소유권 분리 → 카나리 `optimizer='ours'` 1주 무위반** | ❌ 미달 — **유일한 직렬 게이트.** §8-①② Jino 결정 **보류 중** |
| **M5** | 3속도 학습 루프 상시화 | ❌ 미달 — 선행 인프라만 |
| **M6** | S4 정교 수학 | ❌ 미달 |

★**진행률이 안 움직인 것은 설계상 맞다** — 이번 작업은 M 체크박스가 아니라 **M4·M5의 «판정 전제»**(총이익을 무슨 자로 재는가)를 고쳤다.

---

## 0-b. 착수 시 북극성 대비 검토 (D-NAO-226 + D-NAO-227 — **네 줄**)

1. **어느 M인가** — M 체크박스 아님. **M4·M5 두 M의 판정 전제**. §6 각주가 「M4 합격 관측에 총이익 항이 아예 없어 자가 부풀어도 카나리가 통과하고, 그 위에서 M5가 부푼 자로 「기여 양수」를 내면 두 M이 연달아 거짓 합격」이라고 적어 둔 그 자리다.
2. **5요소 중 움직인 것** — **③총이익 방향**뿐. ①②⑤ 0 · **④자동화는 연속 13세션째 착수 0**.
3. **§7 대칭 검사** — ★이번 세션의 핵심이었다. 아래 §2-2 참조.
4. **관련 절 정독** — ref 82 §목차 훑기 후 §1·§3(자 표)·§5·§6(표+M4·M5 각주)·§7·§8 원문 정독. 서브에이전트 위임문 4건 전부에 해당 절 원문·규칙 동봉.

---

## 1. 프로젝트 위치 및 환경

- 로컬: `~/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling`
- **작업 워크트리**: `~/.claude-worktrees/Ohiselling/profit-yardstick-s3` (브랜치 `claude/profit-yardstick-s3` — **병합 완료**, 정리해도 된다). `frontend/node_modules`는 `reflection-visibility` 워크트리로 **심볼릭 링크**를 걸어 뒀다(`npm ci` 재실행 불필요).
- prod: **`sellc.ohitech.co.kr`** · prod DB는 항상 `sqlite3 -readonly` · 서버 시각 UTC(KST = UTC+9)
- prod API 조회: `curl -s -u "$(cat ~/.ohisell_prod_auth)" https://sellc.ohitech.co.kr/api/naver/ad/diagnosis`
- 테스트 인터프리터: `.claude/worktrees/naver-ad-execution-loop-6cc75b/backend/.venv/bin/python3`
- ⚠️ **CI는 결제 정지 상태다** — 잡 `steps_count: 0`·1~3초. **판별 명령**: `gh api "repos/Jino00/ohisell/actions/jobs/<jobid>" --jq '.steps|length'` → 0이면 실행 자체가 안 된 것이다. **CI 빨강을 실제 실패로 오독하지 말 것**(교훈 #123).
- ⚠️ **공유 메인 체크아웃은 병행 세션 것이다** — 미접촉. 단 **내 체인 파일 5종은 origin/main 내용과 일치시켜 뒀다**(해시 대조 확인) — 다른 세션이 pull 할 때 충돌하지 않는다.

---

## 2. 이번 세션 완료 목록

### 2-1. ★★D-NAO-231 (Jino 결정) — 「액셀=하한」은 «후보 선정»이 아니라 «실쓰기의 크기»에 적용한다

**질문 원문**: *"안3(구간 자)을 배포하면 액셀만 11% 줄어 브레이크 편중이 3.02:1→3.41:1로 벌어집니다(실측). 계약 §6 금지선 2는 「자 교정을 액셀 재조정과 같은 배포 단위로만」이라 정했습니다. 액셀 쪽을 어떻게 처리할까요?"*
**Jino 채택 라벨 원문**: ***"후보 선정은 상한·실쓰기 크기만 하한 (권장)"*** (선택지 채택이라 원문은 라벨 그대로다 — 각색 금지, D-NAO-230 ②③과 같은 표기 규약)

**근거가 된 배포 전 실측** (12:5x KST, prod `sqlite3 -readonly` 원본 universe 재구성 — WEB_SITE 키워드 **749행**·SHOPPING adgroup **156행**·factor 1.3133):
census §3 초판대로 액셀 «선정»까지 하한을 쓰면 `starving_winners` 135→130 · `shopping_group_growth` 85→65[근사] ⇒ **액셀 220→195건(−11%)·브레이크 665건 불변 ⇒ 3.02:1 → 3.41:1**.
재구성 검증: 같은 universe에 라이브 factor를 다시 적용하면 600/600·135/135·65/65로 **정확히 일치**(로직 신뢰 확인).

★**복원 다이얼도 계산했으나 쓰지 않았다** — `account_target_roas` 1.936 → **1.552~1.680**이면 두 보드가 동시에 복원되는 공통해가 존재한다(실측). 안 쓴 이유 둘: ①그 값은 오늘 분포에서 역산한 스냅샷이라 롤링 창이 움직이면 근거가 흔들린다(계약 §3-7) ②목표ROAS는 상품 BEP × 공격성 다이얼의 매출가중평균이라 손대면 경영 판단이다(계약 §3-4).
⇒ **선정을 상한에 두는 것이 곧 §6 금지선 2가 허용한 「액셀 판정 불변」의 실측 이행**이고, 보수화는 실쓰기 층에서만 일어난다.

### 2-2. ★§5-6 비대칭 검사 — 배포 전후 라이브 대조 (이 세션의 핵심 증거)

| | 배포 전(13:5x) | 배포 후(14:0x) |
|---|---|---|
| 브레이크 후보 | 665 | **665 (불변)** |
| 액셀 후보 | 220 | **220 (불변)** |
| 브레이크:액셀 | 3.023:1 | **3.023:1 (불변)** |

보드별로도 전건 불변: `bleeding 600→600` · `starving 135→135` · `shop_bep 65→65` · `growth 85→85` · `resume 0→0` · `shop_resume 0→0`.
⇒ **D-NAO-231이 약속한 「액셀 판정 불변」의 라이브 실증.** 초판 배정대로 갔으면 여기서 액셀이 195건으로 줄어 3.41:1이 됐을 자리다.
★**단, 비대칭 3.02:1 «자체»는 그대로 남는다** — 자 교정은 그것을 고치지 않는다는 사실이 이번에 수치로 확정됐다(§6 이월).

### 2-3. 구현 — 무엇이 어떻게 바뀌었나

- **`diagnosis.py`** — `_as_interval`(하한=min(1,p)·상한=max(1,p)·점추정 보존) · `_factor_payload`(응답에 4종 전부) · 보드 배정을 한 곳에서 결정(원칙18 허브). **보드는 전부 상한**.
- **`bid_simulator.py`** — census가 `[미상 — 설계질문]`으로 남긴 「한 값이 두 역할」 해소: ①`direction`은 **상한**(판정 불변) ②`up`이면 크기만 **하한** ③하한이 현재 입찰을 못 넘으면 `hold`(`basis="interval_floor_blocks_up"`) ④`down`이면 **상한**. 반환에 `economic_ceiling_low/high`·`direction_low/high`를 실어 사후 재구성 가능.
- **실쓰기 하한 배선** — `naver_execution_harness`(증액 가드 3곳) · `expansion_allocator` · `expansion_pressure` · `auto_operator`(UP 거부권 `_settlement_roas_status` · 심층확장 진입 · **서보 경제성 상한**) · `proposal_pipeline`(두 끝 명시 전달).
- **`profit_scorecard.py`** — 총이익을 **상한기준·하한기준 두 벌**로 계산해 Slack·일기·응답에 병기. 응답 키 `total_profit_high/low`·`total_profit_avg7_high/low`·`correction_factor_low/high`·`profit_window_note`.
- **산출물 B** — `diagnosis.py:22` 화석 주석 정정: 「convAmt가 ~2.6배 과대 → 곱해 줄인다」 → **「공식은 방향 중립 — 창의 데이터가 방향을 정한다」**. 2.6배(D-NAO-7)의 비교 상대는 우리 원장이 아니라 **MOP 화면**이었고 라이브는 반대 방향(1.31 > 1)이다.
- **표면 (계약 §5-5)** — 진단 보드 「D-NAO-21 보정계수 (구간 자)」 카드에 **구간 양끝 + 점추정 + 「100% 견인 가정」 문구 + 어느 끝이 어느 층에 쓰이는지**. prod 번들 `index-BXsOzfxB.js`에 3종 문자열 전부 실재 확인.

### 2-4. ★★census 「9파일 전수」가 **두 번** 틀렸다

- **초판(세션 38)**: 파일 «목록»을 다시 세지 않고 정독만 해서 **4파일** 누락 — `expansion_pressure.py:161` · `auto_operator.py:433` · `auto_operator.py:2333` · `diary_outcome.py:329` · `probe_revert.py:429`(5호출부).
- **내 정정판**: 파일은 셌는데 **한 파일 안의 호출부**를 안 세서 `auto_operator.py:3085`(서보) 1곳 누락 — **적대 리뷰가 잡았다**.
- ⇒ **전수 = 13파일 25행**. census `§1-B` 신설(초판 배정은 「초판」 열에 보존, 지우지 않음).
- ★**교훈(#348 후보)**: **「전수」에서 세야 하는 것은 파일이 아니라 호출부다.** 확정 명령: `grep -rn "correction_factor" --include="*.py" backend/app/ | grep -v test`. 북극성 §7 「전수 조사 완료라고 쓰지 않는다」가 그 문서 자신에게 적용된 사례다.

### 2-5. 적대 리뷰 — 1R FAIL(P1 2) → 2R PASS

- **P1-1 표면 오정보** — 카드 문구가 **배포되는 동작과 정반대**였다. D-NAO-231로 보드를 전부 상한으로 바꿨는데 화면엔 폐기된 초판 배정(「액셀은 하한으로 판정」)이 남았고, **프론트 테스트가 그 거짓을 단언**하고 있었다. `api.ts` JSDoc 2곳도 같은 거짓.
- **P1-2 서보 하한 배선 누락** — `auto_operator.py:3085`. 매시 :20 KST **실입찰** 레인이고, 방향은 밴드 관제가 이미 정하므로 여기서 계수가 정하는 건 **크기**뿐이다 = D-NAO-231이 하한으로 못 박은 층. 상한 사용 시 **+31%**(1,030 → 1,350원).
- ★**리뷰어가 함정을 수치로 검증**했다:
  ```
  PRE (1R 이전):  direction=hold   ← 원래 판정
  TRAP(하한만):   direction=down   ← 액셀 판정이 브레이크로 뒤집힌다
  FIX (두 끝):    direction=hold   ← PRE와 동일 ✅
  ```
- ★**1R에서 변이 M7·M8 생존** — 실쓰기 하한 배선 7줄과 API 배선을 각각 끊어도 전건이 초록이었다. **P1-2가 정확히 그 구멍으로 빠져나갔다.** 2R에서 둘 다 사망.
- **표면 변이 4종은 1R·2R 모두 사망**(회귀 없음).
- **P2 5건 전부 채택** — 특히 P2-a: 내 새 가드 3건이 **소스 문자열 grep**이라 런타임을 안 봤다. 서보 픽스처 `_SERVO_CORR`는 두 끝이 모두 1이라 **원리적으로 구별 불가**였다(그게 M7 생존의 이유). → 두 끝을 벌린 `_SERVO_CORR_WIDE`로 `run_hourly_lane`을 실제로 돌려 **넘어온 인자를 spy로 단언**하는 테스트로 교체. 자가 변이 검증 통과(배선 되돌리면 `1.3133 == 1` 실패).

### 2-6. 테스트

- 신설 **28종**(백엔드 23·프론트 5): 구간 불변식(계수<1 포함) / **보드가 하한을 받으면 실패하는 회귀 가드** / `bid_simulator` 4규칙 / 응답 구간 키 유지(정상·조기반환 두 가지) / 실쓰기 배선 4종 / **서보 런타임 spy 2종** / 이중 표기 / **표면 5종**
- ★**테스트가 실제 결함 2건을 잡았다**: `창 None~None` 출력 · 계수 자릿수가 카드와 불일치
- 전건 **2 failed / 6,300 passed** — 실패 2건(`test_health_partial_sync`·`test_vendor_item_axis`)은 **변경 전 기준선과 동일**(신규 회귀 0). tsc 통과 · 프론트 574 passed / 48 files.

### 2-7. 배포·병합

- 백엔드 8파일 `safe_deploy.sh --restart` — CAS 전건 통과, 블루-그린 **다운타임 0초**, 마이그레이션 0건
- 프론트 — **CAS 1회 거부**(다른 세션이 `beb85e5a`에서 배포). 규칙대로 `git merge origin/main` → **재빌드** → 재배포. prod 스탬프 `a76b1e3d`. ⚠️병합만 하고 옛 dist를 올리면 상대 작업이 사라진다.
- PR **#345** 병합 `b39c8bc1` — CI 빨강이 `steps_count=0`(결제 정지)임을 **잡 API로 직접 확인**한 뒤 `--force` + 자백 로그.

---

## 3. 완료 QA (판정 원문 그대로 — 미달 포함)

### 대조 대상 3건 (앵커 `대조:` 줄)
1. 승인된 계약 `docs/PLAN_naver-profit-yardstick-review.md` §5
2. 트랙 계약 헤더 `목표:` 원문 (D-NAO-59)
3. PAO 북극성 §1 궁극 목표 원문 + §6 M4·M5

### 1차 판정 (별도 Sonnet·읽기 전용, 2026-08-23 14:09 KST)

> **판정(계약 §5): 부분달성** — 달성 1·2·3·5(+4 재확인) / **미달 6**(배포 후 사후 기록 트랙 미병기) / **판정불능 7**(다음 자연 크론 실행 대기, 이번 세션 잘못이 아니라 시각상 도래 전)

> **판정(트랙 목표 D-NAO-59): 미달** — `optimizer='ours'` 0건·실집행 0건(07-30 이후 불변), 이번 세션도 실제 총이익을 움직이지 않았다. 다만 이번 세션은 그 총이익을 재는 자 자체의 결함(100% 견인 가정)을 고쳐 «장래 M4·M5 판정이 부풀지 않게» 만든 준비작업이었다는 것은 §6 대조에서 별도로 평가한다

> **판정(북극성 §1·§6): 부분달성** — 5요소 중 ③만 실질적으로 이동(달성), ①②④⑤는 이동 없음(0). M4·M5 자체의 라이브 합격 관측은 이번 세션 스코프 밖(계약 §2 비목표)이라 원리적으로 판정 대상이 아니지만, M4·M5 각주가 요구한 처분 자체(자 교정)는 코드·표면 수준에서 완결됐고 그 산출물(profit_scorecard 이중 표기)의 라이브 관측만 다음 크론 도래를 기다리는 중

★**트랙 목표 미달은 열세 번째이고 낮추지 않는다.**

### §5-6 재판정 (같은 QA 기·읽기 전용)
1차 미달 근거는 정확했다 — 실측은 했으나 **트랙 확인줄에 병기하지 않았다**. 커밋 `d597ef8f`로 TRACK-CONTRACT 블록 안에 배포 전후 대조를 병기하고 **그 항목만** 재판정을 요청했다.
> **판정(계약 §5-6 재판정): 부분달성** — 정지 후보 건수·확장 후보 건수는 트랙 확인줄에 병기됐고 QA가 독립 재조회로 수치까지 일치 확인(달성) / 「입찰 상한 표본 3건」(bid_simulator의 economic_ceiling, 원화 단위 실쓰기층 값)은 확인줄 어디에도 없고 다른 값(bleeding_keywords의 roas_corrected)으로 대체되지도 않았다 — 요구된 세 항목 중 하나가 그대로 누락(미기록) (2026-08-23 14:13 KST)

★★**QA가 내 대체값을 정확히 갈랐다.** 나는 `bleeding_keywords` 상위 3건의 `roas_corrected`를 「입찰 상한 표본」으로 찍었는데, 그건 **선정층의 ROAS «비율» 지표**이고 §5-6이 요구한 것은 `bid_simulator`의 **원화 `economic_ceiling`**(실쓰기층)이다. 계약이 「정지 후보 건수·확장 후보 건수」와 **나란히 별도 항목으로** 적은 것 자체가 둘을 다른 것으로 취급한다는 뜻이다. **판단을 QA에 넘긴 것**(위임문에 「내가 대신 판단하지 않겠다」 명시)이 이 건에서 실제로 작동했다 — 내가 판단했으면 합격기준을 조용히 낮췄을 자리다.

**보충한 것**: 배포 «전» 표본 3건을 prod에서 확보했다(`sqlite3 -readonly`, `naver_proposals` 중 배포(14:02 KST) 이전 생성분, 최신 13:07):
| target_id | 경제성상한 | 추천입찰 | 유형 |
|---|---|---|---|
| `nkw-…120150` | **1,940원** | 1,310원 | `growth_bid_up` · 시뮬 근거 `rank_target` |
| `nkw-…120144` | **1,940원** | 1,390원 | 〃 |
| `nkw-…120134` | **1,940원** | 1,430원 | 〃 |
(전건 `target_roas=account_default 1.8897`)

**배포 «후» 표본은 미도래** — `bid_simulator`는 제안 생성 시에만 도는데 그 레인이 크론이라 새 코드(방향=상한·크기=하한)로 계산된 첫 `economic_ceiling`은 다음 제안 생성분에서 나온다. **수동 강제 실행 안 함**(교훈 #274). ⇒ 다음 세션이 같은 쿼리로 배포 후 표본을 뽑아 위 셋과 대조하면 §5-6이 닫힌다:
```bash
ssh sellc.ohitech.co.kr 'sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db "SELECT target_id, target_bid, substr(rationale,1,160) FROM naver_proposals WHERE rationale LIKE \"%경제성상한%\" AND created_at > \"2026-08-23 05:10\" ORDER BY created_at DESC LIMIT 3;"'
```

★**재판정 뒤에도 부분달성인 채로 닫는다** — 남은 미기록이 «미도래»라 이번 세션에서 원리적으로 못 닫는다(§2 라운드 증식 차단). **합격기준은 낮추지 않았다.**

### 그 밖의 QA 검사 결과
- **「안 함」 침범 0/10** — diff 27파일 전부 `naver_ad` 서비스/테스트/이 트랙 문서/`pao-논의.jsonl`. 병행 세션 브랜치에 쓰기·제안 0건.
- **목적 전환 없음**(🔁 선언 없음, 필요도 없었다).
- **진행률 2/7 불변** — 체크박스 자체가 안 움직이는 것이 맞는 설계다.

### ★QA가 확인하지 못한 것 (그대로 옮긴다)
1. §5-6의 "배포 후" 수치를 세션이 직접 다시 관측했는지 — 트랙 확인줄엔 없었다(재판정 대상).
2. §5-7 `profit_scorecard`의 실제 Slack/일기 **렌더 형태** — 다음 크론 전이라 육안 확인 불가, 코드 존재만 확인.
3. `naver_campaign_settings` 9행 vs 실제 캠페인 46개(활성 26)라는 **기존 캐벗** — 이번 세션 스코프 밖.
4. census §1-B의 최종 행수(24 vs 25)를 원본 최신판에서 재대조 안 함 — **코디네이터 확인 결과 25행으로 일관**(`grep -n "25행"` 88행 단독 히트).
5. PR #345 코멘트 본문 미열람(근거 오염 방지 지시대로).
6. 세션↔Jino 대화 로그에 독립 접근 불가 — 승인 원문의 진위는 **구조적으로 QA가 볼 수 없는 영역**(이전 세션 QA와 동일 한계).

---

## 4. 트랙 진행률

- 트랙: `docs/tracks/active/track_naver-ad-optimization.md`
- **시작 2/7 → 종료 2/7** (불변)
  - 달성: M0(효율 검정) · M1(L0 마감)
  - 미달: M2 · M3 · M4 · M5 · M6
- **이번 세션이 움직인 것**: M 체크박스 아님 — **M4·M5의 «판정 전제»**(총이익의 자). 증거 좌표: PR #345 병합 `b39c8bc1` · prod 응답 `factor_low/high/point` 실재 · 번들 `index-BXsOzfxB.js`
- **트랙 목표 원문**: §0 참조 (D-NAO-59)
- **확인줄**: 2건 누적(13:1x 착수·설계 / 14:0x 배포·§5-6 사후 기록) — **TRACK-CONTRACT 블록 «안»**
- **다음 세션 후보**: §6 참조

---

## 5. 알려진 이슈 / 주의사항

- ⚠️ **확인줄은 TRACK-CONTRACT 블록 «안»에 쓴다** — 밖(파일 끝)에 쓰면 훅이 「기록 누락」으로 잡고, 실제로 이번에 **Stop 훅이 세 번** 걸렸다. 세션 38의 5줄도 내가 이관하며 밖에 붙였던 걸 `8ac8a637`로 되돌렸다. **훅이 반복해 걸리면 그건 잔소리가 아니라 규격 위반 신호다.**
- ⚠️ **CI 빨강 오독 금지** — `gh api "repos/Jino00/ohisell/actions/jobs/<id>" --jq '.steps|length'`가 0이면 결제 정지다.
- ⚠️ **프론트 CAS 거부 시 재빌드 필수** — 병합만 하고 옛 dist를 올리면 상대 작업이 사라지는데 스탬프만 최신이 되어 더 나쁘다.
- ⚠️ 커밋 메시지에 **백틱을 쓰면 셸이 먹는다** — `275c5812` 본문에서 함수명 3개가 지워졌다. 내용은 diff·census·PR에 온전하고, **force-push는 정정 경로를 파괴하므로**(§6 금지선) 되감지 않았다.
- **`account_target_roas`는 상품 BEP × 공격성 다이얼의 매출가중평균**(`campaign_target_resolver:164`) — 손대면 경영 판단이다.

---

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)** — §0의 북극성 §1 원문 + D-NAO-59 원문 그대로.

### ★★6-0. 슬라이스 선택 규율 — «잔여 목록의 위»가 아니라 «북극성에 가장 가까운 것»을 고른다

> **Jino 지시 원문 (2026-08-23 08:2x KST)**: *"pao북극성의 목표를 달성하는 위주로 진행되게 목표잡아주는것도 넣어줘"*
> **Jino 재확인 원문 (2026-08-23 12:35 KST)**: *"이제 handoff를 하고 다음 세션에서부터는 PAO북극성목표를 달성하는 위주로 진행되게 하고 다른 트랙의 일을 집어와서 여기세 섞지 말라는 것을 인계파일에 넣어줘."*

**아래 목록은 «남은 일»이지 «우선순위»가 아니다.** 목록 순서대로 집으면 싸고 가까운 것부터 닫히면서 **④자동화·⑤상시개선은 계속 0으로 남는다** — 실제로 그렇게 **13세션**이 지났다. 착수 전에 이 표로 고르고, **고른 근거를 「5요소 중 무엇을 미는가」로 앵커에 적는다**(답이 「없음」이면 연속 몇 세션째인지 병기).

| 후보 | §6 M | 5요소 | 거리 |
|---|---|---|---|
| ①★★**M4 결정 2건**(북극성 §8-①② 소유권 분리·optimizer 해제 범위) | **M4** | **④자동화 · ⑤상시개선** | ★**가장 가깝다. 북극성이 지정한 유일한 직렬 게이트.** 여기가 안 열리면 ④⑤는 원리적으로 영원히 0이다. 세션 36에서 Jino가 *"너 아직 PAO를 시도할 준비가 안되어있네"*로 보류했는데, **그 「준비」로 지목됐던 자(尺) 교정이 이번 세션에 끝났다** — 보류 사유가 하나 줄었다 |
| ②**§5-7 라이브 확인**(08:40 크론 첫 발화) | M5 전제 | ③ | ★**5분.** 08-24 08:40 이후 `profit_scorecard` 일기/Slack에 상한기준·하한기준이 실제로 뜨는지. **이 세션의 유일한 미도래 항목**이라 다음 세션이 먼저 닫는 게 싸다 |
| ③**액셀 확대**(비대칭 3.02:1 자체를 좁힌다) | M4 동반 | **③ + ④** | ★자 교정은 비대칭을 «안 벌린» 것이지 «좁힌» 게 아니다. 실측 공통해는 `account_target_roas` 1.552~1.680인데 **경영 판단**이라 Jino 결정 사안 — ①과 한 묶음이 자연스럽다 |
| ④census `[미실측]` 감도 채우기 | M4 판정 전제 | ③ | 중간. 확정 명령이 §4에 행별로 병기돼 있다 |
| ⑤다중 로직 실험 축의 북극성 편입(세션 36 발견 C) | 북극성 개정 | ②논문·구조 | 중간. ref 82 개정 = 새 계약 |
| ⑥~⑩ (아래 잔여) | — | **없음** | **멀다.** 전부 위생·부채다 |

**⇒ 권고 경로: ②(5분) → ①M4 결정 → ③액셀 확대.**
★**이건 새 게이트가 아니다** — 다른 것을 고르는 것도 정당하다. 다만 **고른 이유를 5요소로 적어야 하고, 「없음」이면 연속 세션 수를 병기한다**(D-NAO-226). **위생 작업만 고르면서 「진행 중」으로 보이는 것이 이 트랙의 상습 실패 모드다.**

### ★★6-0b. **다른 트랙의 일을 끌어와 여기 섞지 마라** (Jino 지시 — 절대)

> **Jino 지시 원문 (2026-08-23 08:2x KST)**: *"그리고 다른 트랙의 일을 집어와서 여기에 섞지 말라는 것도 꼭 넣어줘"*
> **Jino 재확인 원문 (2026-08-23 12:35 KST)**: *"다른 트랙의 일을 집어와서 여기세 섞지 말라는 것을 인계파일에 넣어줘"*
> 트랙 목표 규율의 발단이 된 원문(2026-08-20): *"처음에 목표한 일이 끝나도 **프로젝트 내의 다른 트랙의 업무를 끌고와서 마치 자기일인것처럼** 일을 하고 있고"*

| 이번 트랙에서 **안 하는 것** | 소관 트랙 |
|---|---|
| 쿠팡 로켓1P 손익 정합 · 대시보드 순이익 배선 | `docs/tracks/active/track_coupang-rocket-1p.md` |
| 쿠팡 광고 변경 이력 · 오하이테크 광고 | `track_coupang-ad-change-log.md` · `track_coupang-ohitech-ad.md` |
| **상품↔채널 매핑 · 원가 연결** | `track_product-connection-map.md` |
| 매출 원장 정합(Wing) | `track_revenue-wing-truth.md` |
| 층1 잔여 91 endpoint 개봉 · 「모든 데이터 완료」 선언 | 없음 — **신규 계약 필요** |
| 예산 변경 개방 | 없음 — 스프린트 X부터 **불변의 스코프 밖** |

⚠️ **지금도 위험하다** — 병행 세션 **3개**가 살아 있다(`sellc-원가-메뉴` n=2 `bd4adba0` · `접속-안정화` n=3 `19c76836` · `쿠팡-손익정합` n=6 `d3752faa`). 그 작업이 같은 저장소에 **눈앞에 보이는 상태**다. **보인다고 내 일이 아니다.**
발견했을 때 할 일은 «고치는 것»이 아니라 **«적는 것»**이다 — 앵커 `## 이월`에 **소관 트랙 파일명을 지목해** 한 줄. 손대야만 할 것 같으면 먼저 `↗️ 스코프 밖: <이유> / 계약 목표: <원문>` 자백을 출력한다.
★이번 세션 실적: 접촉 파일 27개 전부 이 트랙 소관, **「안 함」 침범 0/10**(QA가 `git diff --name-only`로 확인).

### 잔여 목록

- [ ] ①★★**M4 결정 2건** — 북극성 §8-①②(소유권 분리 · optimizer 해제 범위). **Jino 결정 사안.** 세션 36에서 보류됐고, 그 보류 사유였던 「준비」의 한 축(자 교정)이 이번에 끝났다.
- [ ] ②★**§5-7 라이브 확인** — 08-24 **08:40 KST** 이후 `profit_scorecard` 크론 첫 발화에서 상한기준·하한기준 총이익이 Slack·일기에 실제로 뜨는지. **수동 실행 금지**(교훈 #274). 관측 명령: `ops_diary_entries`에서 `action='profit_scorecard'` 최신 행의 `rationale`.
- [ ] ③★**§5-6 마감** — 배포 «후» 입찰 상한 표본 3건을 뽑아 배포 전 3건(전부 1,940원)과 대조. 명령은 §3에 있다. 이것만 채우면 §5-6이 달성으로 닫힌다.
- [ ] ④**액셀 확대**(비대칭 3.02:1 자체) — 실측 공통해 `account_target_roas` 1.552~1.680. **경영 판단**이라 Jino 결정.
- [ ] ⑤**census `[미실측]` 감도 채우기** — 25행 중 다수. 확정 명령이 `docs/references/93_*.md` §4에 행별 병기.
- [ ] ⑥**[미상] 1건 Jino 확인** — 「D-NAO-21 승인 당시 «분자=채널 전체»의 함의가 인지된 승인이었는가」. **막는 질문이 아니다.**
- [x] ⑦**교훈 #348 기입 — 완료**(2026-08-23 16:4x). 「전수에서 세야 하는 것은 파일이 아니라 «호출부»다 — 한 세션에서 두 번 틀렸다」. 다음 가용 교훈 번호는 **#349**.
- [ ] ⑧**「계약 §0에 북극성 원문을 싣는다」 상설 규율 승격** — **D-NAO-229가 이 항목용으로 예약돼 있다.** 실적 3건(세션 37·38·이번). **묻지 말고 진행 가능.**
- [ ] ⑨**하네스 정정 3건** — `scripts/next_ids.sh` 정규식(D-NAO도 과소산출) · 프로젝트 `CLAUDE.md:56` `.deploy-stamp`→`.build-stamp` · 적대 리뷰 템플릿에 「전건 테스트와 변이 주입 동시 실행 금지」
- [ ] ⑩**승계 미처분** — 세션 37: 1R 생존 변이 5종·볼트 오염·반성 상태 창 무한 성장·`naver_campaign_settings.mode` 죽은 필드 / 세션 36: 다중 로직 실험 축 편입·M3-c(M2-d 의존)·M2-d/M2-z
- [ ] ⑪**다음 구조 감사 트리거 = 08-25 이후**(마지막 §4 감사는 ref 69, 08-18)
- [ ] ⑫**워크트리 정리** — `~/.claude-worktrees/Ohiselling/profit-yardstick-s3`(병합 완료) · `reflection-visibility`(PR #340으로 landed). 단 전자의 `frontend/node_modules`가 후자로 심볼릭 링크라 **후자를 먼저 지우면 전자가 깨진다**.

### ★이월 (앵커에서 승계 — 소관 지목)

- **`account_target_roas` 하향이 액셀 볼륨을 정확히 복원한다는 실측**은 이번에 쓰지 않았다. **비대칭 3.02:1 자체는 그대로 남는다** → 소관: 이 트랙(M4 결정과 함께)
- **`diary_outcome`·`proposal_scoreboard`(사후채점)에 어떤 자를 쓸지 미해결** — 안3은 살아있는 액션 게이트용 프레임이라 학습 신호엔 안 맞는다. 지금은 상한 유지 → 소관: 이 트랙(M5 설계와 함께)
- **`exploration.py`의 raw-rpc 전제**는 점추정<1로 재확정되면 무너진다(census §4 [미상]) → 소관: 이 트랙
- **하위호환 키 `factor`의 의미가 «점추정»에서 «상한»으로 바뀌었다** — 점추정>1인 지금은 동일하지만, <1이 되는 순간 `proposal_scoreboard`·`diary_outcome`·`probe_revert`·`retro_snapshotter`·`proposal_writer`·`flight_loop`의 자가 조용히 바뀐다(적대 리뷰 P2-9) → 소관: 이 트랙

---

## 7. 세션 최종 상태

- 브랜치 `claude/profit-yardstick-s3` — **PR #345 병합 완료**(`b39c8bc1`). 커밋 6개: `a5b95b5e`(문서 승계) · `5de99f96`(구현) · `275c5812`(1R P1 상환) · `8ac8a637`(확인줄 배치 정정) · `3d30477a`(2R P2 채택) · `d597ef8f`(§5-6 사후 기록) + origin/main 병합 `a76b1e3d`
- **배포 완료** — 백엔드 8파일 + 프론트. prod 가동 중. 마이그레이션 0건.
- 공유 메인 체크아웃 — **미접촉**(병행 세션 것). 단 내 체인 파일 5종은 origin/main 내용과 **해시 일치** 확인.
- 로컬 `main`은 다른 워크트리에 잡혀 있어 `git switch main` **생략**(착지 전제 검사 L5). 다음 세션은 `git switch -c <새> origin/main`으로 시작할 것.

## 8. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_profit-yardstick-interval-shipped_20260823.md 읽고 이어서 작업해줘
```
