# HANDOFF — 「상한리셋 목표」(D-NAO-287) · 체인 `pao-상한리셋` n=1

- 세션 `ced45cb7` · 2026-09-05 11:36~14:0x KST · 트랙 `docs/tracks/active/track_naver-ad-optimization.md`
- 계약(정본): `docs/contracts/CONTRACT_auto_up_base_reset.md` — Jino 승인 2026-09-05
- **검증 명령:** `ssh ubuntu@sellc.ohitech.co.kr "sqlite3 /home/ubuntu/ohisell/backend/ohisell.db \"select id, entity_id, before_value, after_value, changed_at, rationale from naver_change_log where action='reset_auto_up_base' order by changed_at desc limit 5;\""` → **1행(id 8271)** 이 나오면 인계가 참이다.

## §0 목적 (원문 — 발명하지 말 것)

Jino 지시 원문(2026-09-05): *"상한 기준점(`auto_up_base`)을 사람이 명시적으로 리셋하는 입구 — API + 화면 버튼 + 감사 기록. 누가·언제·왜 리셋했는지가 `naver_change_log`에 남아야 한다."*

## §1 한 일

| | |
|---|---|
| PR | **#725 병합**(`bea1797`) · **#730 병합**(`818f161`) — 둘 다 CI 3/3, `--force` 미사용 |
| 커밋 | `6c69b36` 구현 · `3ee3ce4` #725 리뷰 처분 · `eec8f71` 트랙 · `50ac3c5` lint · `4b4e7c8` 라이브 결함 수정 · `8ec44b3` #730 리뷰 처분 |
| prod 백엔드 | **배포 2회** — CAS 통과·무중단·다운타임 0초·**마이그레이션 0**. 최종 `818f161`, **활성 포트 :8001** |
| prod 프론트 | ⛔ **미배포** (아래 §4) |
| 적대 리뷰 | 2회 모두 **1R FAIL(P1=1) → 2R PASS** |
| 완료 QA | 1차(배포 전) 부분달성 → **재판정(배포 후) 부분달성** |
| 네이버 광고계정 쓰기 | **0건** (읽기 `get_ad_bid` 1회뿐) |

만든 것: `GET /api/naver/ad/auto-up-ceiling` · `POST /api/naver/ad/auto-up-base/reset`(사유 필수 400 · 라이브 조회 실패 502) · `KNOWN_CHANGE_LOG_ACTIONS`에 `reset_auto_up_base` · `auto_up_base_bid()` 인식 필터 1곳 확장 · 콘솔 「자동 상향 여력」 판.

## §2 ★실측이 세 번 주장을 뒤집었다 (전부 원문 보존)

1. **인계문의 원인 지목이 반증됐다**(계약 §0) — 2.0배 상한은 prod에서 **전 기간 발화 0건**(`naver_change_log`에 「누적 상한」 0행). 09-01 두 소재의 차단 원문은 **쿨다운**(`가드레일 차단 — 쿨다운 중 … 최소 2시간 필요, D-NAO-19`, 4건). 「이틀째 실집행 0건」의 원인은 그 캠페인 `cmp-…10164717`의 **`auto_operate=0`**(Jino가 09-01에 껐다). ⇒ Jino 결정(선택지 A): 만드는 것은 원안 유지, **합격기준 #1·#3만 교정**(원안 원문 §4-부록 보존).
2. **내 계약 §2-1 근거가 테스트로 반증됐다**(계약 §2-부록) — 「별도 action이면 쿨다운에 안 잡힌다」는 틀렸다. `compute_change_cadence`는 `dry_run=False ∧ after_value IS NOT NULL`만 보고 **action을 안 본다**(docstring 원문 *"액션 유형 무관"*). 기준점은 `after_value`에 담아야 읽히므로 **회피가 원리적으로 불가** ⇒ 회피 대신 응답 `side_effect`와 화면 문구로 **표면화**.
3. **★라이브 1회 집행이 내 구현의 결함을 잡았다**(계약 §0-부록) — 판이 **37~43일 낡은 관측**으로 「상한 도달」을 **단언**하고 있었다. 대상 소재의 판 값 `current_bid 1870`(09-01 관측)에 대해 **라이브 실측은 590원**. `current_bid_source: "last_known"`으로 **출처는 정직하게 달아 놓고 그 값으로 판정을 내린 것** — 「0건과 안 재고 있음을 구분한다」던 판이 정작 **「닿았다」와 「닿았는지 모른다」를 구분 못 했다**(D-NAO-264의 시간축 판). **적대 리뷰 2R PASS·완료 QA·회귀 7,828·프론트 1,479가 전부 초록인 상태에서 나왔다.**
   처분: `capped`→`capped_by_last_known` · `capped_count`→`capped_by_last_known_count` · **`current_bid_age_days` 신설**. ★**새 문턱을 만들지 않았다** — 나이를 그대로 싣고 문턱은 보는 사람이 정한다.

## §3 완료 QA — 재판정 (2026-09-05 13:44~13:52 KST · 별도 Sonnet · 읽기 전용) — 원문 그대로

```
판정(계약 §4): 부분달성 — B·C·D·E 달성(4/5) · A 미달(1/5). A의 미달은 「증거에 못 닿음」이 아니라
  부재를 직접 관측한 것이다: prod dist에 「자동 상향 여력」 grep 0건 · .deploy-stamp 파일 부재 ·
  dist/assets mtime 09-04 14:34 KST(이 계약 첫 커밋보다 이르다) (2026-09-05 13:46 KST)
판정(Jino 지시 원문): 부분달성 — 「API + 화면 버튼 + 감사 기록」 3요소 중 API와 감사 기록은 세상에
  실재하나 화면 버튼은 prod에 없다. 코드는 main에 있고 배포만 CAS로 막혀 있다 (2026-09-05 13:46 KST)
판정(PAO 트랙): 부분달성 — 진행률 2/7 불변. D-NAO-59 총이익 축 직접 접촉 0 — ④자동화 운영의
  「복귀 손 마련」만 진행. 라이브 리셋이 기준점을 옮긴 것은 관측했으나 그것이 총이익 증가로
  이어졌다는 증거는 없다(관측 기간 부족) (2026-09-05 13:46 KST)
종합: 부분달성
```

**항목별**: A **미달**(프론트 미배포) · B **달성**(`id 8271` 주체 Jino·13:04:51 KST·사유 원문) · C **달성**(기준점 1,240→590·천장 2,480→1,180, 부작용 표면화 재현) · D **달성** · E **달성**(QA 직접 재실행 7,853 / 1,481 / build 통과).

**QA가 확인 못 한 것(원문 보존)**: D 세 변이 QA가 직접 재주입 안 함(📄) / 「하향 일캡 불변」 prod 라이브 미측정, 단위 테스트만(📄) / 리셋 POST 응답 원문 QA가 직접 재현 안 함 / 다른 소재 `…558107660` 미대조 / §0-부록의 쿨다운 차단 행 재대조 안 함(📄).

★**QA가 합격기준 교정을 「정당한 교정」으로 판정**(1차): ①구현 착수 «전» 실측 단계에서 Jino 승인 ②원안 원문 보존 ③교정판이 더 헐겁지 않다. 다만 **「계약 §8 종료조건에 배포가 없다」**는 별개 구조를 지적했다.

## §4 ⛔ 막힌 것 — 다음 세션이 이어받을 것

**프론트 배포가 CAS로 거부된다.** prod 프론트가 **다른 세션의 미푸시 커밋 `c3e4c08`**에서 배포돼 있어 `--force-frontend` 없이는 못 올린다. **쓰지 않았다**(Jino 「보류」) — 밀면 그 세션 작업이 복구 출처 없이 사라진다(08-06·08-28 두 번 겪은 사고).

**재개 명령**(그 세션이 push한 뒤):
```
cd /private/tmp/claude-501/pao-b1/repo && git fetch origin && git switch main && git pull --ff-only origin main
(cd frontend && npm run build) && bash scripts/safe_deploy.sh --frontend
# 그 뒤 합격기준 A 재판정:
ssh ubuntu@sellc.ohitech.co.kr "grep -rl '자동 상향 여력' /home/ubuntu/ohisell/frontend/dist/assets/ | head -3"
```
★**「머지·배포했다」가 아니라 「번들에 문구가 있나」로 판정할 것** — 이번 QA가 A 미달을 확정한 방법이 그것이다.

## §5 ↗️ 이월 (다른 소관 — 고치지 말고 기록만)

- **D-NAO-287 번호 충돌** — B2 세션(PR #728 진동차단 `7662b5a`)이 같은 번호를 썼다. 둘 다 비슷한 시각 `next_ids.sh`에서 287 수령, 머지는 내 #725(`bea1797`)가 먼저. **재번호는 두 세션 문서·커밋에 걸쳐 일방 처리하지 않는다** — Jino 결정 사안. 이 저장소가 문서로 남긴 **다섯 번째 재발**이고 원인은 도구가 아니라 **동시성**이다(`next_ids.sh`는 fetch 시점 스냅샷만 본다). → 소관: PAO 트랙
- **두 소재를 다시 돌리는 일** — 원인은 코드가 아니라 **Jino가 끈 `auto_operate=0`**이다. 켜는 것은 §2 승인 지점 → 소관: PAO 트랙
- **콘솔 화면 소관** → `docs/tracks/active/track_pao-ui-ux.md` (이월 1줄 기록됨. 이번 판은 **최소 표면**이다 — 디자인·배치·문구 정본 판단은 그 트랙)
- **배포 인프라** — 「남의 미배포 커밋이 전 트랙 배포를 막는다」(ref 109)의 **프론트판**이 재현됐다 → 소관: 배포 인프라

## §6 하네스 관측 (계수 대상)

- **훅 「트랙 기록 누락」은 오탐이 아니었다.** 내가 두 번 「오탐」이라 적었는데, 오탐이었던 것은 **내가 돌린 공유 폴더 grep**이지 훅의 지적이 아니었다. 훅은 origin/main을 읽고 **「자리가 틀렸다 — 파일 끝에 append했다」**고 처음부터 정확히 말하고 있었다. 확인줄은 `<!-- TRACK-CONTRACT -->` 블록 **안**에 넣어야 한다(실측상 09-03 이후 다섯 세션 연속 같은 실수).
- 완료 QA 위임문을 `review-surface-mutation.sh`가 **적대 리뷰로 오인해 1회 deny** — 그대로 재전송하니 통과(훅 안내대로).
- `[체인] ⛔ pao-논의 n=49` 훅은 **오탐**(공유 메인 폴더 HEAD `211decbc`가 8월 중순에 얼어 있다). origin/main 실측은 n=87 닫힘.

## §7 트랙 진행률

**2/7 불변** (M0·M1 달성 / M2~M6 미달). 이 계약은 트랙을 닫지 않는다 — `ref 131` L3-신1 한 칸만 메운다(계약 §8에 그렇게 명시).
북극성 대비(D-NAO-226): M4 귀속 · 5요소 중 **④자동화 운영**의 「복귀 손 마련」만 움직임(①②③⑤ 없음) · §7 대칭 = 이 슬라이스는 드물게 **액셀 쪽**(브레이크는 있는데 해제 손이 없어 비대칭이었다). 다만 그 브레이크는 **한 번도 안 밟혔으므로** 과대 계상하지 않는다.
