# HANDOFF — 광고 전면 정지 + 관측 붕괴·복구 (2026-07-29 저녁 ~ 2026-08-03 오후)

트랙: `docs/tracks/active/track_naver-ad-optimization.md` (네이버 SA 광고 최적화, 우리판 MOP)
관련 확정 결정: D-NAO-132(정지) · D-NAO-133(관측/실행 스코프 분리) · D-NAO-134(소급 백필)

## §0 가장 먼저 알아야 할 것

1. **우리 자동운영 광고는 07-30 10:50 KST부터 정지 상태다**(D-NAO-132). 캠페인 5개 `userLock=true`, 6개 `optimizer='none'·auto_operate=0`, pending 227건 reject.
2. **그중 01·03은 07-30 13:30:51~54에 외부(대행사 추정)가 다시 켰다.** 04·15·P.아이패드 파워링크는 **08-03 현재까지 노출 0**(닷새째). 재개 여부는 Jino 미결.
3. **관측 복구는 완료·배포됐다**(오늘, PR #174 = main `2bda659`, prod `d645281`). 자동운영은 여전히 닫혀 있다.
4. **내일(08-04) 07:45 크론이 소재 외부변경 탐지의 진짜 첫 판정이다.** 오늘은 기준선 수립이라 `외부변경 0건`이 정상.

## §1 07-29 저녁~30 오전 진단 — 정지에 이른 경위

- 자동 상향(D-NAO-130) 첫 실집행은 침묵 실패가 아니었다. 22:20·23:20 레인 정상 발동, 생성 제안이 전부 `[순위고삐]` 하향. **오늘 하루 ad-grain UP 판정 46건이 오전에 몰리고 오후로 갈수록 0으로 수렴** — `_intraday_loss_leash`가 "당일소진 ≥ 하루평균"에 걸리면 DOWN이 UP을 선점하기 때문. **저녁은 UP 관측에 구조적으로 최악의 시각**이었다(합격 판정 시각 설계 오류).
- **자동운영 범위 실측**: 최근 7일 광고비 발생 27캠페인 중 우리 자동운영은 5개(546만원, 55.4%). **22개 캠페인 440만원(44.6%)이 자동화 밖**이고 전부 `naver_campaign_settings` 행 자체가 없어서다(D-NAO-13 "설정 행 없음=optimizer='none'=실행 하드 차단"). 2위 스펜더 ○갤럭시 파워링크(156만원)가 관측 밖.
- **★순위 지표가 현실과 다른 축이었다**: 우리가 "4위"라 부르던 `avgRnk`는 **광고영역 내 순위**다. 네이버는 통합검색 순위를 `pcNxAvgRnk`·`mblNxAvgRnk`로 **별도 제공**(공식 `naver/searchad-apidoc` wiki `FAQ-stat.asciidoc:39-42`)하는데 우리는 수집하지 않는다. Jino가 "폴드8필름·폴드8와이드필름 12위 밖"을 본 것과 우리 지표(5~6위)가 어긋난 이유.
- **★소재별 성과는 API가 준다** — 라이브 실증: 그룹 avgRnk 4.2/266imp인 그룹의 소재별은 6.4/102·2.8/164. 07-29 HANDOFF의 "쇼핑은 소재별 성과 컬럼이 아예 없다"는 **틀렸다**(07-29 HANDOFF의 `naver_ad_daily` grain의 한계였지 API 한계가 아니다). 이 오판이 상향 브레이크를 대리 지표로 만든 근거였다.
- **폴드8 6매입 적자 확정**(3원료 수렴): 판매가 16,800 / 원가 6,090 / 수수료 6.11%(N배송 프리미엄 반영) / 물류비 274 → 공헌이익 8,554 → **BEP 1.9639**. 07-29 클릭 185·비용 241,742원·전환 7·매출 131,500 → **ROAS 0.544**(라이브 /stats 1.648 과다귀속 / 정착 0.544 / 실주문 상한 0.834). **손익분기 CPC 324원 vs 실 CPC 1,307원 = 4.0배.** 입찰 조절 문제가 아니라 상품 채산성 문제.
- 03(아이폰 강화유리) 3일: 07-27 ROAS 294%·+32,504원 / 07-28 186%·+2,929 / 07-29 **100%·−23,062**. 3일 합계 +12,372원. 손실 최대 요인 = 17프로 그룹(캠페인 광고비의 48%, 3일 연속 적자).
- **브레이크와 액셀이 다른 데이터를 본다(실증)**: 손실 고삐는 실시간, ROAS-UP(D-NAO-66) `_settlement_window()`는 **[오늘−8일, 오늘−2일]** → **최근 2일 악화를 원리적으로 못 본다**. 17프로가 07-29 19:20 자동 하향(2,790→2,380)된 뒤 **07-30 07:20에 자동이 2,730으로 되올렸고**, 09:20에 3,130 상향 제안이 pending이었다(정지 직전 반려).
- 예산 페이싱도 액셀: 03이 ROAS 100%였던 07-29 21시, 누적이 일예산 50,000을 넘던 그 시점에 **일예산이 50,000→66,700으로 자동 상향**.
- 출시창 순위 하한(D-NAO-121)이 폴드8 하향을 07-29 19:20~23:20 **5회 전부 차단**. Jino 07-29 11:02 확정 설계였으나 **감수 금액이 하루 18만원이라는 사실은 그 결정 시점에 알려지지 않았다.**
- 정지 직전 집행: 17프로 상향 제안 반려 / 17프로 입찰 **2,730→2,330**(±15% 클램프 한계, `change_log id=1123`) / 폴드8 출시창 하한 해제(`naver_learning_state` id=242 → NULL).

## §2 D-NAO-132 전면 정지 (07-30 10:50 KST)

Jino 원문 그대로: **"우리가 진행중인 광고 모두 정지 시켜줘. 너한테 맏기면 망하겠다"**

- 집행: 캠페인 5개 `userLock=true`→PAUSED(`change_log` 1142~1146, `manual_emergency_stop`) / 6개 `optimizer='none'`·`auto_operate=0` / pending 227건 reject / 대행사 22개 캠페인 미변경.
- 되돌리기: `naver_sa_writer.set_campaign_lock(cid, False)` + `PUT /api/naver/ad/campaign-settings/optimizer {"optimizer":"ours"}` + `UPDATE naver_campaign_settings SET auto_operate=1`.
- ★**Claude의 판단 착오 기록**: 03은 정지 직전 6일간 ROAS 2.00·이익 +51,395원으로 **건전했는데도** 01의 적자를 근거로 5개를 일괄 정지했다. 01 정지는 타당(6일 ROAS 1.26·하루 −84,307원). **03 정지는 과잉이었다.**

## §3 인계 전후 비교 (08-02 조사)

- **"대행사에 넘겼다"는 절반만 사실** — 5개 중 01·03만 재가동, 04·15·P는 정지 유지.
- 01: ROAS 1.26 → 1.50(+19%), 일평균 −84,307 → −29,733. **대조군(대행사 상시운영 22개)도 1.62 → 1.85(+15%)** — 개선폭이 시장 상승분과 겹쳐 **대행사 공로로 볼 수 없다.**
- 03: ROAS 2.00 → 1.43(−29%), 흑자→적자, avg_rank 3.86→4.55. **시장과 반대 방향.**
- 대행사 운영 방식: 우리 5개에 대해 **3.5일간 캠페인 on/off 1회 + 소재 편집 3건**이 전부. 그룹·키워드·예산 0건. 자기 원래 캠페인에선 일괄 균일 조정(그룹 입찰 전부 500원, 키워드 138개 일괄 −36%).

## §4 03 대행사 조작 규명 (08-03)

| 시각(KST) | 대상 | 변경 |
|---|---|---|
| 07-30 **11:28:36** | 17프로 `nad-…455468669` | **2,330 → 1,890 (−18.9%)** ★재가동보다 2시간 빠름(캠페인이 아직 PAUSED일 때) |
| 07-30 13:30:54 | 캠페인 | userLock 해제 |
| 07-30 15:47:13 | 14프로 `nad-…373443175` | 3,480 → 3,500 |
| 07-30 15:47:58 | "17" `nad-…455470648` | 1,970 → 1,670 (−15.2%) |

- 우리가 내린 2,330은 **38분 유지되다 외부가 더 깎았다.** 방향은 우리와 같았다(둘 다 내려야 한다고 판단).
- 예산·그룹 구조·제외키워드 전부 무변경.
- ★**정정**: 08-02에 "03이 다시 PAUSED"라고 기록한 것은 **오판**. 캠페인은 07-30 13:30 이후 계속 가동 중이고, 개별 소재 다수가 예전부터 PAUSED인 것을 오인한 것.

## §5 ★D-NAO-133 (신규) — 관측/실행 스코프 분리 (구현·배포·검증 완료)

**문제**: 07-30 긴급정지로 `optimizer='ours'` 캠페인이 0개가 되자, 그 값으로 스코프를 잡던 **관측·수집 5개가 함께 죽었다** — 소재 외부변경 탐지(D-NAO-127), 상품↔소재 매핑 수집, flight_loop 예측관측, P7 이익 스코어카드, 탐침 학습 게이트밴드. 특히 `shopping_ad_product_sync`는 대상이 0이 되자 **`naver_adgroup_product` 276행을 능동 삭제**했다(07-31 07:45). 그 결과 위 §4의 대행사 조작 3건을 **하나도 못 잡았다**.

**원칙 위반이었다**: D-NAO-13 원문에 *"진단·리포트·이상 알림은 **상태 무관 전 캠페인**(읽기는 무해)"* 이라고 이미 명문화돼 있었다. 그리고 `bm_diff`가 D-NAO-92 때 **같은 클래스의 사고**를 겪고 고쳤는데(주석 `bm_diff.py:10-13`: optimizer=none이 된 03의 우리 변경 7건이 전부 "대행사 조작"으로 오기록) **그 교훈이 나중에 만든 D-NAO-127에 안 퍼졌다.**

**수정**: `campaign_roster.observation_campaign_ids()` 신설 = 관측 스코프 단일 진실 원천(최근 7일 광고비>0 ∪ `naver_campaign_settings` 행 존재, **optimizer 무관**) / 삭제 로직 **전면 제거** + AST 테스트로 재도입 차단 / "관측 대상 0"과 "관측했는데 0건"을 로그에서 구분(WARNING 승격) / 스로틀 0.2s·실행예산 600s·`elapsed_s` 로깅 / 커서 링 순회로 starvation 해소(원자적 CAS, `naver_account_settings` KV) / 과거 날짜 거부·`as_of` 명시 요구 / `ad_id` 중복 시 최신 선택.

**실행 게이트 불변**: `naver_execution_harness`·`proposal_writer`·`cold_start_bid_lane` diff 0줄. 이 PR은 정지를 풀지 않는다.

**codex 교차 리뷰 4라운드**(경위를 반드시 기록): R1 P1 2건 → R2 **P1 4건**(가드를 쌓을수록 새 틈이 생겨 **삭제 기능 자체를 제거**하는 방향 전환) → R3 P1 1건(대응으로 신선도 필터 11곳 + fail-closed 추가) → R4 **P1 4건**(그 필터가 **커서 한 바퀴가 10일 넘으면 살아 있는 매핑을 age-out**시킨다) → **필터 전면 철회, 최소안 회귀.** ★교훈: 가드를 겹겹이 쌓는 대신 위험 기능 자체를 빼는 편이 나았고, 스코프를 부풀린 탓에 배포가 3시간 늦어졌다(Jino: "아침에 헛짓거리 한거 같네").

**★라이브 검증 (원칙22, 전부 실측)**:

| 기준 | 실행 전 | 실행 후 |
|---|---|---|
| `naver_adgroup_product` | **0행** | **1,759행**(236그룹, `ad_edit_tm` 전량 보유) |
| 소재 외부변경 탐지 | `관측 0소재`(사흘째) | **`관측 1759소재`** |
| `naver_change_log` 우리 쓰기 | 739 | **739(증감 0)** |
| `elapsed_s` | 미측정 | **90.3초** |

★**커서 한 바퀴 = 1일 확정**(242그룹/90.3s = 0.373s/그룹, 예산 507s의 17.8%만 사용, 완주). codex R4의 "10일~40일" 우려는 실측으로 기각. **이로써 신선도 창을 정할 실측 근거가 확보됐다.**

**PR #174 → main `2bda659` → prod `d645281`.** 배포 1차는 **CAS 거부로 중단**(다른 세션 `worktree-collection-watchdog`이 `scheduler_service.py`를 먼저 배포 — 덮었으면 쿠팡 수집 워치독이 죽었을 것). main 병합 후 재시도해 14/14 CAS 통과. 테스트 4,235 passed.

## §6 소급 백필 (08-03 12:53)

07-30 10:50 이후 소재 외부변경 **36건**을 `naver_agency_op`에 소급 기록(**id 104~139**, `[BACKFILL 2026-08-03: 소급 백필, 정규 탐지 아님]` 마커). 이전값 복원 5건 / **불명 31건**(`before=null`, "관측 공백" — `editTm`은 마지막 수정만 담아 원리적으로 복구 불가). **추측으로 채운 값 0건.** 되돌리기: `DELETE FROM naver_agency_op WHERE id BETWEEN 104 AND 139`. 검증: `change_log` 5,255행 불변, 스위치 불변.

## §7 다음 세션 할 일 (우선순위)

1. **★내일 08-04 07:45 크론 확인** — 소재 외부변경 탐지의 진짜 첫 판정. `naver_agency_op`에 새 행이 붙는지. (오늘 1,759행은 수동 실행으로 채운 것이라, 정규 크론이 이 기준선을 읽어 비교하는지 확인이 필요하다 — 백필 에이전트가 이 점에 의문을 제기했고 미확인 상태.)
2. **「수정 사항」 화면** — 계약 승인됨. 날짜 선택 → 그날 변경 목록 + 주체(**우리 자동화 / 대행사 / Jino**) 표시 + 정정 기능. 마이그레이션(정정 전용 테이블) + API(두 원천 union + 정정 쓰기) + 프론트 1페이지. ★용어 함정: 코드베이스의 `optimizer='mop'`은 "제3자 소유·우리가 안 건드림"이라 Jino가 말한 "MOP=우리 시스템"과 **정반대** — 화면에서 MOP라는 말을 쓰지 않는다.
3. **04·15·P 재개 여부** — 닷새째 매출 0. Jino 미결.
4. **재개 전 선행조건** (D-NAO-132 ④에 추가):
   - ROAS-UP 정착창이 최근 2일을 보게 수정(안 고치면 내린 입찰이 되올라감 — 실증됨)
   - 통합검색 순위(`pcNxAvgRnk`·`mblNxAvgRnk`) + 소재별 성과 수집
   - **RL3 장중 손실 고삐가 BEP 미확인 시 미발동** — 상향을 막아도 기존 출혈은 계속된다
   - 신선도 정책(실측 확보됐으므로 설계 가능) — ★**필터를 걸면 안 되는 4곳** 목록을 반드시 참조: `product_campaign_share`(분모 — 걸면 07-28 사고 코드 변경 없이 재도입), `effective_bid`(hold 게이트 도달 불가), `account_diagnosis`(스톱로스 창 7→3일 축소), `search_term_judge`(보호 토큰 감소로 정상 검색어 자동 제외). 일반화: **"현재 매핑"을 묻는 소비자만 후보이고, 분모·보호목록·역사적 기준선·hold 게이트 입력은 후보가 아니다.**
   - 폴드8 상품을 광고로 계속 팔지(손익분기 CPC 324원)
5. 이월: settings 히스토리 테이블 / stale 매핑 정리 / 전역 레이트리미터 / `metrics_aggregator` 배치화 / 자동운영 범위(440만원이 밖) / 15번 캠페인 보호

## §8 이 세션에서 Claude가 틀린 것 (반복 방지)

- "쇼핑은 소재별 성과가 없다"(07-29) → API가 준다. 이 오판이 상향 브레이크를 대리 지표로 만들었다.
- 폴드8 1,000원을 "4위에 맞는 값"이라며 대행사 되돌림을 수용 → 근거 무효로 확인.
- 일예산 초과 128% 보고 → **캠페인 ID 혼동**(64,069원은 01의 값, 03은 7,060원). 검증 없이 전달.
- 17프로 입찰 3,060원 → 실제 2,730원(stale 값 전달).
- 03 일괄 정지 → 03은 흑자였다(과잉 판단).
- "03이 다시 PAUSED" → 오판(계속 가동 중이었다).
- 서브에이전트의 "이월 구현했다" 보고를 검증 없이 수용 → codex가 "정렬도 커서도 없이 break한다"고 반증.
- 관측 복구 스코프를 부풀려 배포가 3시간 늦어짐.

**공통 패턴: 라이브 증거 없이 보고·기억을 신뢰했다.**

---

## §9 완료 — 「수정 사항」 화면 (D-NAO-135, 2026-08-03 오후)

Jino 요구 원문 그대로: *"SellC 네이버 광고 메뉴에 수정 사항 서브메뉴를 만들어서 내가 원하는 날짜를 고르게 하고, 수정 내용을 리스트하고 수정 주체가 MOP인지 대행사인지 구분하는 거지. 너가 수정한 내용이 아니면 대행사일 거야. 내가 수정할 일은 많이 없으니까. 그래도 혹시 모르니 default로는 대행사로 주체를 만들어놓고 내가 간혹 한 수정에는 내가 한 거라고 수정할 수 있게 하자."*

- **경로**: `/naver-ad/modifications`, LayerNav에 "수정 사항" 탭.
- **두 원천 union**: `naver_change_log`(우리 것 + `external_*` 4종)와 `naver_agency_op`(외부 전용). 신규 엔드포인트 `GET /api/naver/ad/modifications`(기존 `/change-log` 계약을 오염시키지 않기 위해 확장이 아니라 신설).
- **주체 3분류**: 우리 자동화 / 대행사 / Jino. **기본값 대행사**. 판정 규칙은 `backend/app/services/naver_ad/change_actor.py` 한 곳에 모음.
- **정정은 원천을 안 건드린다**: 신규 테이블 `naver_change_actor_override`(마이그레이션 `d4f6b8c0e2a5`). `PUT /api/naver/ad/modifications/{source}/{source_id}/actor`, `actor:null`이면 정정 삭제(자동 판정 복귀).
- **날짜는 실제 발생 시각 우선**(`occurred_at` → 없으면 감지일). 백필 36건은 감지일이 08-03이지만 실제로는 07-30 일이라, 감지일 기준이면 07-30에 안 보인다.
- **★용어 함정**: 코드베이스의 `optimizer='mop'`은 "제3자 소유·우리가 안 건드림"이라 Jino가 말한 "MOP=우리 시스템"과 **정반대**. 화면에서 MOP라는 말을 쓰지 않는다.
- **구현 중 잡은 버그**: 긴급 정지 5건이 "50,000원 → 50,000원"으로 표시됐다(광고를 멈춘 사건인데 "안 바뀜"이라고 말함). 서술 생성기가 액션 종류를 안 보고 payload에서 예산 필드를 집던 것 → 액션별 매핑 + 회귀 2건.
- **의도적으로 안 한 판정**: `manual_emergency_stop`은 rationale에 "Jino 직접 지시"가 있지만 우리 시스템이 지시를 받아 집행한 것이지 콘솔 직접 조작이 아니므로 "우리 자동화"로 둔다(문자열 추측은 판정이 아니다).
- **codex 스킵**: Jino 지시(15:56 "codex는 스킵하자") — 부채 아님, 재실행 불요.
- **라이브 합격 6/6**(원칙22): 07-30 선택 시 88건(우리 40·대행사 48) / 03 소재 3건 "대행사"(`11:28:36 2,330→1,890`) / 10:12 17프로 인하 "우리 자동화"(`2,730→2,330`) / 이전값 불명 18건 표시 / 정정 실동작 + **원천 문자단위 불변 확인** + 원상복구(prod 흔적 0) / 서브메뉴 실재. 테스트 4,406 passed.
- PR #179(`4f2b0b2`) + PR #180(alembic 헤드 합류, `cc73bf5`) → 배포 `593e171`, prod alembic `f3c1d7e9a482`.

## §10 ★사고 기록 — 병행 세션 alembic 헤드 2회 분기

- 07-30 이후 **같은 `models.py`에 세션 넷이 3~20분 간격으로 prod 배포**(13:51 / 14:13 / 14:16 / 15:07).
- 1차: `worktree-nbaesong-shipping-cost`가 마이그레이션을 prod에 배포하고 main 미병합 → 내 PR #179가 먼저 병합되며 **헤드 2개**. 내가 재부모 PR #180으로 정리.
- 2차: 그 사이 그쪽이 **스스로 머지 리비전**(`f3c1d7e9a482`)으로 합류시켜 배포 → 내 재부모와 그래프가 어긋남. 게다가 `models.py` **CAS 실패**(prod엔 그쪽 `NaverClaimSettlementProbe` 60줄, main엔 없음) — 덮었으면 이미 배포된 서비스가 import 실패해 **앱 부팅 전체가 죽었을 것**. CAS가 막았다.
- 해소: 그쪽이 PR #181로 병합된 뒤 main 병합 → 재부모 유지 판단(**prod가 이미 재부모본을 쓰고 있었다** — 그쪽이 내 PR #180 병합본을 가져다 배포함) → `--migrate` 없이 코드만 배포.
- ★**교훈**: CAS가 두 번 사고를 막았지만 막은 것은 clobber이지 **작업 직렬화 부재 자체는 아니다**. 배포 창을 세션별로 나누는 규칙이 없으면 다음엔 CAS가 통과하는 조합(같은 파일의 서로 다른 조상)에서 뚫린다. **prod 상태는 3분 만에 stale해진다** — 배포 직전 재확인이 필수(오늘 14:10에 읽은 상태가 14:13에 낡음).

## §11 ★신규 과제 (Jino 지시: "새로운 세션에서 해줘") — 탐지 주기 상향

**문제**: 현재 소재 외부변경 탐지는 **하루 1회**(07:45 크론)라 두 가지를 구조적으로 못 본다.
1. **하루 안의 왕복** — 아침에 올렸다 저녁에 되돌리면 스냅샷 비교상 무변동. 2026-07-29 15:39 대행사가 폴드8와이드를 1,600→1,000으로 되돌린 것이 정확히 이 부류(그때는 `editTm` 앵커로 잡았다).
2. **소재의 중간 이력** — 네이버가 `editTm`(최종 수정 시각) 하나만 주므로 하루에 두 번 만지면 한 번으로 보인다. 백필 36건 중 31건이 이전값 불명인 근본 원인.

**실측 근거(2026-08-03 확보)**: 242그룹 수집에 **90.3초**, 0.373s/그룹(스로틀 0.2s + API 지연 ~0.17s). 실행 예산 600s 중 507s가 실수집분이라 **17.8%만 사용**, 한 바퀴 완주. 즉 하루 1회 기준으로는 여유가 5.6배.

**핵심 제약**: 시간당으로 올리면 **하루 242콜 → 5,808콜**(24배). 네이버 SA API의 공식 rate limit은 **문서에서 확인 안 됨**(2026-08-03 조사). 그리고 다른 수집 잡(entity_sync·bm_snapshot·성과 리포트 등)이 같은 계정에 붙고, **스로틀이 프로세스 로컬이라 크로스잡 동시 호출을 못 막는다**(전역 레이트리미터 부재는 이미 이월 항목).

**검토해야 할 선택지(설계는 다음 세션 몫)**: 전량 시간당 / 변경 가능성 높은 소재만 시간당(예: 최근 변경 이력 있는 것, 활성 지출 상위) + 나머지 하루 1회 / 주기 중간값(예: 3~6시간) / 네이버가 변경 알림을 주는 다른 표면이 있는지 조사.

**선행 확인**: 내일(08-04) 07:45 크론이 정규 탐지 첫 판정이다. **그 결과를 보고 나서 주기 상향을 논해야 한다** — 하루 1회로 실제 무엇이 잡히는지 모른 채 24배 비용을 올리면 안 된다.

## §12 다음 세션 시작 프롬프트

아래를 복사해 다음 세션 첫 메시지로 쓸 것:

```
트랙: 네이버 SA 광고 최적화(우리판 MOP). 이어받는 상태:
- 자동운영은 07-30 10:50부터 여전히 정지(D-NAO-132). 01·03은 대행사 추정 주체가 07-30 재가동, 04·15·P.아이패드는 08-03 현재 닷새째 노출 0.
- 관측(소재 매핑·외부변경 탐지)은 08-03 오전 복구·배포 완료(D-NAO-133·134).
- 「수정 사항」 화면(D-NAO-135)은 08-03 오후 배포 완료 — 날짜별 변경 이력 + 주체(우리 자동화/대행사/Jino) 조회·정정.

첫 할 일: 08-04 07:45 크론이 소재 외부변경 탐지의 진짜 첫 정규 판정이다. `naver_agency_op`에 새 행이 붙는지, 정규 크론이 08-03 백필 기준선(id 104~139)을 "직전 관측"으로 정확히 읽어 비교하는지 확인할 것.

그다음: 그 결과를 보고 나서 탐지 주기 상향(하루 1회 → ?)을 검토할 것. 실측 여유는 있으나(90.3초/600초 예산) API 콜이 24배로 뛰고 네이버 rate limit은 문서에서 확인 안 됨 — 상세는 이 HANDOFF §11.

미결: 04·15·P.아이패드 재개 여부(닷새째 노출 0, Jino 결정 대기). 재개 전 선행조건 목록은 트랙 파일 D-NAO-132 ④ + "다음 액션" 섹션 참조.

문서: 트랙 `docs/tracks/active/track_naver-ad-optimization.md`, 이 HANDOFF `.claude/memory/HANDOFF_ad-stop-and-observation-restore_20260803.md`.
```
