# 세션 인수인계: 수집 전면 성공 — 계약 승인 + W1(Wing 자동 재로그인) 라이브 개통
> 저장일시: 2026-08-23 12:55 KST (13:10 갱신 — PR #342 병합 + Jino 스코프 지시 추가)
> 새 대화 시작 시 이 파일을 먼저 읽을 것
> ★비트랙 세션이다(앵커에 `트랙:` 줄 없음) — 운영/인프라 수리. 활성 트랙 11개 전수 확인 결과 「수집 인프라 안정성」 소관 트랙이 **없다**. 「트랙 진행률」 절 없음.

---

# ⛔ 다음 세션에 대한 Jino 지시 (2026-08-23 13:08 KST) — 이 파일에서 가장 먼저 읽을 것

> **"다음 세션에서부터는 접속 안정화 재발 문제목표를 달성하는 위주로 진행되게 하고 다른 트랙의 일을 집어와서 여기에 섞지 말라"** (Jino 원문)

**다음 세션이 지켜야 할 것 셋:**

1. **이 목표만 판다.** 목표는 *"수집이 폰이나 노트북이든 어디서든 문제없이 모두 잘 되도록"*(Jino 12:03)이고 남은 슬라이스는 **W2 → W3 → W4 → W5**다(§6). 진행률 **2/6** — 이 작업은 **아직 안 끝났다.** 새 일을 찾지 말고 남은 네 칸을 닫아라.
2. **다른 트랙 업무를 끌어오지 마라.** 이 저장소엔 활성 트랙이 11개 있고(PAO 네이버 광고 2/7 · 쿠팡 2P 손익정합 8/16 · 원가 진실 원장 2/6 · 로켓1P 4/6 …) **그중 어느 것도 이 작업의 소관이 아니다.** 눈에 띄어도 손대지 않는다. Jino가 경계하는 병이 정확히 이것이다 — 전역 §6 원문: *"처음에 목표한 일이 끝나도 프로젝트 내의 다른 트랙의 업무를 끌고와서 마치 자기일인것처럼 일을 하고 있고"*.
3. **스코프 밖 발견은 «고치지 말고 적는다».** 앵커 `## 이월`에 **소관 트랙 파일명을 지목해** 한 줄. 나갈 땐 `↗️ 스코프 밖: <이유> / 계약 목표: <원문>` 자백을 **먼저 출력**한다. 이탈은 대개 연쇄 정당화(A를 고치려면 B가 필요해 보이고…)로 오는데 각 단계가 국소적으로 정당해서 금지문이 안 걸린다.

★**이 지시가 필요했던 이유 — 이 세션의 실제 관측**: Jino가 *"이 트랙의 목표가 뭐야?"*라고 물었을 때 내가 **활성 트랙 11개를 나열하는 헛발질**을 했다. 이 작업엔 트랙이 없는데도 트랙 목록으로 새어 나간 것이다. **비트랙 작업일수록 목표가 흐려진다** — 재주입되는 앵커 `목표:` 줄이 유일한 방어선이니 매 턴 그걸 대조하라.

★**앵커 작성 시**: §6의 「이어지는 작업의 목적(원문)」을 **원문 그대로** `목표:`로 옮긴다(발명 금지). `트랙:` 줄은 **쓰지 않는다** — 비트랙이고, 쓰면 훅이 경로로 읽으려다 실패한다.

---

## 0. 이 세션이 답한 질문 — 「왜 또 같은 문제가?」

Jino가 폰 스크린샷을 들고 물었다: *"어제 접속 안정화를 위해서 fable까지 기획해서 작업을 했는데, 왜 또 같은 문제가 발행을 했지?"*(11:30).

**답: 어제 계약에 Jino가 정한 적 없는 금지선이 들어가 있었고, 그게 진짜 해결책을 시야에서 가렸다.**

- 어제 S1 계약 §4:106 *"자격증명 자동 입력·자동 로그인 구현 금지 (영구). 쿠팡 로그인은 사람이 한다."*
  → **전 저장소에서 그 문구는 S1 계약과 그걸 베낀 오늘 오전 초안, 두 곳뿐**이고 다른 근거가 없다.
- **Jino의 실제 결정은 정반대**: `docs/tracks/active/track_coupang-ohitech-ad.md:15` **D-7(Jino 선택 A)** — *"Keychain 자동로그인 상주화(오픽스 방식). 비번 등록 1회는 Jino 직접(제약: **AI 비번 입력 금지**)"*. 진짜 제약은 「AI가 비번을 취급하지 않는다」 하나뿐이다.
- 그 가짜 금지선 탓에 어제 세션은 **Wing 자동로그인 배선을 검토조차 하지 않았고**, 「사람이 로그인하러 갈 때의 불편을 9분→40초로 줄이는」 쪽으로만 갔다.
- Jino 확인(12:17): *"당연히 자동 로그인이 되어야 하는 거 아니야?"* → **"그래"**

★**교훈의 일반형**: **근거 없는 금지선 한 줄이 다음 세션의 탐색 공간을 통째로 잘라낸다.** 계약은 실행을 돕지만 틀린 계약은 실행을 **정확하게** 방해한다. 상속한 금지선은 베끼기 전에 출처를 확인할 것.

### 오늘 아침 폰 화면 6줄의 실제 정체 (실측)
| 화면 | 실제 |
|---|---|
| 로켓 발주/정산 ⚠️「응답 없음 — Mac이 켜져 있는지」 | **수집 성공**(10:32:54, prod `fresh`). 화면이 12초 먼저 포기한 **오보** |
| 오픽스 RG 🔑「로그인 필요」 | **진짜 실패**. Wing 세션 만료 — 자동복구가 배선돼 있지 않았다 |

★그리고 **같은 「전체 갱신」 회차 안에서** 옆 레인 셋이 같은 자격증명으로 자동 복구했다:
```
10:29:37  오픽스 Wing      → login_required ("Mac Chrome에서 로그인하세요")
10:30:04  오픽스 광고       → 자동 로그인 성공   ← 같은 계정 ofixohi·같은 Keychain 항목
10:30:12  오하이테크 광고    → 자동 로그인 성공
10:30:18  로켓 공급자허브    → 자동 로그인 성공
```

## 1. 프로젝트 위치 및 환경
- **워크트리**: `/Users/jino/.claude-worktrees/ohiselling/collection-works-everywhere` (브랜치 `feat/collection-works-everywhere`, base `origin/main` `aab20a2a`)
- 공유 메인: `~/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` — **병행 세션의 미커밋 파일 20건+ 있음. 여기서 작업 금지**(pre-commit 훅이 지킨다)
- prod: `sellc.ohitech.co.kr` · 앱 포트 8011 · 관측은 **원격 루프백**: `ssh sellc.ohitech.co.kr "curl -s http://127.0.0.1:8011/api/coupang/ops/collection-status"` (nginx 경유는 Basic Auth에 막힌다)
- prod DB = SQLite `/home/ubuntu/ohisell/backend/ohisell.db`. **조회는 scp+파일 방식**(ssh heredoc은 따옴표를 먹는다)
- **Mac 페처**: 데몬은 repo가 아니라 `~/.ohisell/tools/`의 설치 사본을 돈다. 배포 `bash tools/install_local_runtime.sh`(CAS 가드)
  - 데몬: `com.ohisell.{wing,wing2,rocket,adcost,ohitech-ad}` + `scheduler-watchdog`
  - CDP: 9222=오픽스 Wing · 9223=오하이테크 Wing · 9224=오하이테크 광고센터 · 9225=공급자허브
  - ⚠️ **`backend/.venv`가 없다.** 테스트는 시스템 `python3`(pytest 9.0.3)로 돌린다. 데몬 런타임은 `~/.ohisell/venv/bin/python3`인데 **pytest가 없다**
- 배포: `scripts/safe_deploy.sh [파일...] [--migrate] [--restart]` / `--frontend` · 병합 `scripts/safe_merge.sh <PR> [--force]`

## 2. 이번 세션 완료 목록

- ✅ **진단(라이브 실측)** — 결함 3종 분리
  1. 어제 W4의 문구 3분할이 `streamRefresh.ts:335-368 describeOutcome()`에만 착지. **대시보드 「전체 갱신 결과」 패널은 그 함수를 안 쓴다** — `Dashboard.tsx:454`가 outcome을 `{kind:"timeout"}`으로 축약해 버리고 `:71`이 옛 문구를 출력. 어제 커밋 `6661d926`은 `Dashboard.tsx`를 **건드리지 않았다**(최종 수정 `236d1ee8`, 08-20). ★`streamRefresh.test.ts`가 「Mac이 켜져 있는지가 뜨면 안 된다」를 **3곳에서 단언하며 전부 통과**한다 — 테스트가 보는 함수와 사람이 보는 화면이 다른 파일이었다
  2. `supplier_hub` 실소요가 화면 상한 215초를 넘는다 — **08-10 이후 버튼 트리거 34건 중 6건(17%) 초과**. 자가복구 없음 n=25 중앙값 152초·**최대 367초** / 자가복구 있음 n=9 중앙값 175초·최대 306초. 오늘 227초(임대 10:29:07→완료 10:32:54). ★**화면 T0와 데몬 임대 사이 ~25초 지연**이 있어 데몬 실질 예산은 190초다
  3. 폴링에 `visibilitychange`/`document.hidden`/`pagehide` **0건**(grep). `streamRefresh.ts:219-225`는 **마감을 벽시계(`Date.now`)로 재고 진행을 `setTimeout`으로** 한다 → iOS가 타이머를 정지시키면 복귀 즉시 거짓 타임아웃. **미발동 잠재 결함**(오늘 사고는 이것이 아니다)
- ✅ **가짜 금지선 발견·정정** (§0)
- ✅ **계약 승인** `docs/contracts/CONTRACT_collection_works_everywhere.md` (Jino "그래" 12:29) — 옛 초안 `CONTRACT_collection_observability.md` 삭제
- ✅ **W0 = 작업 0** — Keychain에 `ofixohi`·`ohitech` **이미 등록돼 있었다**(Jino 12:26: *"WING1하고 오픽스 광고의 ID/PW 모두 같아…WING2하고 오하이테크 광고의 ID/PW 모두 같아"*). `coupang_auth`가 4종이 같은 서비스명(`ohisell-coupang-ad`)을 공유하도록 설계돼 있어 Wing이 그대로 읽는다
- ✅ **W1 구현·배포·라이브 검증** — 커밋 `5f6bb338`
  - `tools/wing_browser_fetcher.py`: `import coupang_auth` · `_recover_rg_session()` · `_landed_on()` · `_acct_label()` · `_do_rg_run`의 AUTH 분기에서 **사람을 부르기 전에** 복구 시도 + 앱 프로브 재확증 · login_id 부재 시 경고 · ①SSO 타임아웃 45→20초
  - config: `~/.ohisell_wing_fetcher.json`에 `wing_login_id="ofixohi"`, `~/.ohisell_wing2_fetcher.json`에 `="ohitech"` (백업 `*.bak-20260823-123610`)
  - `tools/tests/test_wing_session_recovery.py` 신설 **12건**
- ✅ **변이 주입 4/4 사망** — ①배선 절단(`if False`) ②착지 판정에서 경로 조건 제거 ③호스트 판정을 부분문자열로 ④login_id 미전달. 전체 스위트 71건 통과
- ✅ **Mac 런타임 배포** — `install_local_runtime.sh` CAS 통과, 데몬 5종 reload. `diff` 워크트리↔가동본 **0**

## 2-1. 완료 QA (별도 Sonnet 기·읽기 전용 · 2026-08-23 13:02 KST) — 판정 원문 그대로

- **작업 목적(정본 원문)**: *"나는 다른건 모르겠고 수집이 폰이나 노트북이든 어디서든 문제없이 모두 잘 되도록 만들고 싶어. 이게 내 목표야."* (Jino 2026-08-23 12:03 KST)
- **대조 대상 3개** — 계약 §5 / Jino 지시 원문 / 어제 S1이 미달로 남긴 ⑤

**판정(계약 §5 합격기준): 부분달성** — ③만 라이브 증거로 달성(단 트리거는 재클릭이 아니라 데몬의 재claim). ①②는 W1 배포 후의 실제 폰/노트북 재현이 없어 판정불능, ④는 W2 미착수로 미달, ⑤는 범위 밖.

**판정(Jino 지시 원문): 부분달성** — Wing(오픽스 RG) 1레인의 실제 결함(3주간 자동복구 미배선)을 찾아 고쳤고 자동 재로그인 메커니즘 자체는 라이브로 작동을 증명했다. 그러나 "어디서든 잘 된다"를 사람이 폰에서 직접 재확인한 사례가 이번 세션엔 없고, 목표 판정의 필요조건으로 못 박은 화면 정합(④)이 그대로 깨진 채이며, 6작업(W0~W5) 중 W1 하나만 완료됐다.

**판정(어제 S1 미달 항목 ⑤ supplier_hub SLA): 미달(변화 없음)** — 이번 세션은 `streamRefresh.ts`·`supplier_hub` 관련 코드를 전혀 건드리지 않았다. 오늘 유일한 supplier_hub 회차(227초)는 이 세션의 배포 이전 발생분이라 이번 작업의 결과가 아니다. 어제 미달 그대로 이월.

### ★QA 판정문의 사실 오류 1건 — 판정은 안 고치고 근거만 바로잡는다
QA가 12:41 회차를 *"10:28:53의 낡은 요청을 데몬이 재claim한 것"*이라 적었으나 **틀렸다**: 코디네이터 트리거 응답이 `{"requested":true,"requested_at":"2026-08-23T12:41:50.513920","already_pending":false,"attempt_count":0}`이고(이미 pending이면 lease 계약상 no-op으로 **기존** requested_at을 돌려준다 — `refresh_contract.py:109`), 페처 로그 `12:41:50,963 RG 정산 다운로드 트리거(버튼)`은 **0.45초 뒤**다. 새로 만든 요청이다.
★**그래도 판정은 바뀌지 않는다**: 그 트리거는 코디네이터의 API POST이지 **Jino의 폰 클릭이 아니다.** 합격 ①②는 «사람이 그 화면에서 눌러 그 화면에서 본다»를 요구하므로 판정불능이 맞다.

### 라이브 증거 — W1 자동 복구 (12:41~12:43, 자연 발생 케이스)
오픽스 RG가 10:29부터 `needs_login`으로 남아 있었다 = **Jino가 폰에서 맞닥뜨린 그 상태 그대로**.
```
12:41:50  트리거(버튼 경로 POST request-refresh)
12:41:58  RG 진입 프로브 auth — 로그아웃 확증(xauth 오리진 이탈)
12:42:20  SSO로 복구 안 됨 → Keychain 자동 로그인 시도      ← ①은 예상대로 무효
12:42:58  ⚠️ 자동 로그인 실패(목적지 미착지) — 수동 폴백     ← URL 착지 판정은 실패했다
12:42:58  ✅ 앱 세션 검사 통과(자동 로그인 후) — 복구 확정    ← verify가 뒤집었다
12:42:59  RG: 자동 재로그인으로 세션 복구 — 사람 개입 없이 수집 계속
12:43:12  RG 다운로드 완료 — push 성공 1 / 실패 0 (upserted 158)
```
**69초, 사람 개입 0.** prod: `needs_login → fresh`, `last_error_kind` 클리어, `refresh_requested_at=None`.

★**설계 판단이 첫 실행에서 값을 했다**: `verify=_rg_session_ok`(앱 자신의 프로브)를 안 넘겼으면 URL 판정 실패에서 ③알림으로 escalate 해 **Jino를 또 불렀을 것**이다. 코드 주석에 「redirect_uri가 오리진 루트라 is_landed가 False로 남을 수 있다」고 적어 둔 그 경우가 그대로 나왔다.

### ★합격 ①의 표현 하나가 이 레인의 결과 안 맞는다 (기준을 임의로 낮추지 않았다)
합격 ①에 「`last_success_at` 당회 갱신」을 넣었는데 복구 후에도 그 값은 08-22 15:25:14 그대로다. **결함이 아니다**: 로그 `RG: 열거 6개 / prod 결손 0 → 대상 1개 [PRODUCT_SIZE_COMPARISON]` — 정산 결손이 0이라 정산 엑셀은 받을 게 없었고, 받은 상품크기 리포트는 `/rg/product-size/upload-xlsx`로 가는데 그 엔드포인트는 신선도를 안 올린다(신선도는 `/rg/settlement/upload-xlsx`의 `rg_mark_heartbeat` 몫 — `coupang_ops.py:2730`). 현재 `stale:false`·age 21.4h(임계 9일).
- ⇒ **「수집이 됐다」와 「새 데이터가 들어왔다」가 다른 축인데 판정식이 둘을 묶었다.** 조용히 고치지 않고 올린다.
- ⇒ 파생 질문(내 변경과 무관·미결): **성공적으로 «확인만» 한 회차도 신선도를 올려야 하는가.** 안 올리면 데이터 없는 기간에 건강한 레인이 stale로 늙어 거짓 경보가 된다 — 이 계약이 겨냥하는 병과 같은 형태다.

### QA가 확인 못 한 것 (다음 세션 입력)
- W1이 **WING2**(오하이테크)에서도 작동하는지 라이브 미검증(WING2는 배포 전 이미 정상이라 재현 기회가 없었다)
- **적대 리뷰 미실시** — 계약이 PR 경계 1회를 의무로 규정. ★코디네이터가 변이 4종 주입해 4/4 사망을 관측했으나 **그 기록이 파일에 없어** QA가 독립 검증 못 함 → 다음 리뷰에서 재주입 대상
- ①②는 **Jino 본인이 폰·노트북에서 재현해야만** 판정 가능
- `refresh_contract.py:418` 폐기 문구가 브라우저에 실제로 어떻게 보이는지(W2 대상)

### 목적 전환 여부
없음. `🔁 목적 전환` 선언 0건. ★단 **목표 문장 자체는 Jino가 직접 세 턴에 걸쳐 좁혀 확정**했다(11:50 "이 트랙의 목표가 뭐야?" → 11:54 "이 세션의 목표를 말해보라고" → 11:56 "목표를 작성해봐" → 12:03 확정). 모델이 바꾼 게 아니라 목표의 주인이 명확화한 것이므로 전환 선언 대상이 아니다.

## 2-3. 착지 — ✅ 완주 (머지까지)
- **완료 단계**: 커밋 → push → **PR #342** → **적대 리뷰 PASS(P1 0)** → **머지 `beb85e5a`**
- **좌표**: 커밋 `5f6bb338`(코드) · `02836b90`(인계) · PR https://github.com/Jino00/ohisell/pull/342 · 머지 `beb85e5a`
- **멈춘 단계**: 없음
- **리뷰 판정**: **PASS(P1 0)** — 별도 Sonnet 기. ★리뷰어가 **내 변이 4종을 독립 재실행해 4/4 사망을 재확인**했다(완료 QA가 「기록이 파일에 없어 독립 검증 못 함」으로 남긴 구멍이 이걸로 닫혔다).
- ⚠️ **`--force` 병합 자백**: CI 빨강 상태에서 병합했다. 근거는 실측이다 — `gh run view 32616723549` 결과 3개 job 전부 **`steps: 0`·2~3초**로, **단 한 스텝도 실행되지 않았다**(결제 정지). `mergeable: MERGEABLE`(충돌 없음)·`mergeStateStatus: UNSTABLE`. 기록: 화면 + `$TMPDIR/safe_merge.log` (`2026-08-23 13:06:48 KST PR#342 --force 병합`).
- **착지 전제 검사(L1~L5)**: L2 워킹트리 깨끗 · L3 브랜치는 내 워크트리 전용 · L4 병합 전 `origin/main`이 8커밋 앞서 있었으나 충돌 없이 병합 · **L5 로컬 main을 공유 폴더가 잡고 있어 「main에 세워두기」 생략**

### ★적대 리뷰가 남긴 P2 — 다음 세션 W4에 편승할 것
- **P2-1 (채택)**: **`verify=` 인자를 제거해도 71건이 전부 통과한다**(리뷰어가 변이로 실증 — **유일한 생존 변이**). 그런데 그 인자가 **라이브에서 실제로 복구를 살린 것**이다(12:42:58 URL 착지 판정 실패 → `verify`가 뒤집음). 계약 §2가 설계로 명시했는데 가드가 없다. → `spy_ensure["verify"]`가 callable인지 단언 1개 추가(비용 낮음).
- **P2-2 (이월)**: `_recover_rg_session`이 예외를 던지면 `_do_rg_run`의 파일 레벨 catch가 **rc=1(재시도)**로 처리한다 — 2026-08-22 W2가 고친 「같은 병에 두 처분」과 같은 형태로 재시도 3회×창 대기가 되살아날 수 있다(창 유지 자체는 보존됨을 리뷰어가 확인). → 호출부를 자체 try/except로 감싸 `LOGIN_REQUIRED`와 동일 취급.
- **P2-3 (이월)**: 복구 후 재확증이 `_PROBE_UNKNOWN`이면 `keep_open=True`가 안 되돌려져 창이 불필요하게 남는다(정확성 문제 아님).
- **P2-5 (이월)**: 이중 `if entry_verdict == _PROBE_AUTH:` 구조는 정확하지만, 누군가 `elif`로 「정리」하면 **재확증 배선이 조용히 깨진다**. 함정 주의.
- **P2-4 (기각)**: `vendor_id`가 macOS 알림 제목에 실리는 것 — 기존 관행과 동일 노출 수준이고 로컬 알림이다.

## 3. 확정된 결정사항 (번복 금지)

- **가짜 금지선 폐기**(Jino 12:17 "그래") — 구 「자격증명 자동 입력·자동 로그인 구현 금지(영구)」는 **근거 없음**. 유효한 제약은 **「AI 비번 입력·취급 금지」 하나**(등록은 Jino가 터미널에서 1회, 비번은 메모리에서만·로그/파일/git 무기록). **다음 세션은 S1 §4를 베끼지 말고 새 계약 §4를 정본으로 삼는다.**
- **목표 정본**(Jino 12:03): *"수집이 폰이나 노트북이든 어디서든 문제없이 모두 잘 되도록"*. ★**「수집이 잘 된다」이지 「화면이 정직하다」가 아니다** — 화면 정합은 합격 ④(판정 필요조건)로 격하. 어제·오늘 오전 계약이 미끄러진 지점이 정확히 이것이다(둘 다 «수리 묶음»을 목표로 세웠고, 수리는 합격했는데 체감은 「또 안 됐다」였다)
- **W3 제안값 채택**(계약 승인에 포함): `T_pickup=90초` · `T_max=600초`
- **S2 사전 프로브는 여전히 닫혀 있다** — 자동 재로그인이 그 필요를 상당 부분 대체한다(만료를 «예방»하는 게 아니라 «무해화»한다)
- **S3 레인 격리 트리거 반증** — 오늘 227초는 굶김이 아니라 **7개 하위 수집의 직렬 합**(자가복구 69초 + 발주 list + 발주상세 79초 + 판매분석 + 쉽먼트 33초). 어제 트리거 조건(「로그인 유효한데 굶김 no_response 주 1회+」)은 살아 있다
- **비트랙 세션이다** — 활성 트랙 11개 중 소관 트랙 없음. 트랙 파일에 확인줄을 남기지 않는다

## 4. 핵심 파일 목록

| 파일 | 역할 |
|------|------|
| `docs/contracts/CONTRACT_collection_works_everywhere.md` | **계약 정본(승인됨)** — §0 진단·§3 W0~W5·§4 금지선 정정·§5 합격기준·§7 미확인 |
| `.claude/anchors/064224f5-58e1-46e0-9586-76e4d9f35243.md` | 이 세션 앵커(목표 원문·QA 판정 3줄·이월) |
| `tools/wing_browser_fetcher.py` | **W1 배선처**. `_recover_rg_session`·`_landed_on`·`_do_rg_run` AUTH 분기 |
| `tools/coupang_auth.py` | 공용 3층 복구(①SSO ②Keychain ③알림). **2026-08-03에 Wing 포함 4종을 위해 만들어졌다** |
| `tools/tests/test_wing_session_recovery.py` | W1 가드 12건 (⚠️ CI 밖) |
| `frontend/src/pages/Dashboard.tsx` | **W2 대상** — `:71` 옛 문구 · `:454` outcome 축약 · `:453` `isLoginRequired` 1인자 호출 |
| `frontend/src/lib/streamRefresh.ts` | **W3 대상** — `:201` 215초 고정 · `:219-225` 벽시계 마감+setTimeout(W5) |
| `backend/app/services/coupang/refresh_contract.py` | `:418` 폐기된 처방 문구(W2 정정 대상) |

## 5. 알려진 이슈 / 주의사항

- ⚠️ **W1 코드가 적대 리뷰 없이 Mac 런타임에 배포돼 라이브로 돌고 있다.** 되돌리기: `~/.ohisell/tools/wing_browser_fetcher.py`를 이전 버전으로 되돌리거나 config의 `wing_login_id`를 지우면 ②층이 폴백한다(백업 `*.bak-20260823-123610`)
- ⚠️ **CI는 결제 정지로 죽어 있다** — 전 job이 `steps=0`·2초 실패. **빨강은 코드 신호가 아니다.** 로컬 스위트로 판단
- ⚠️ **`tools/tests`는 CI가 안 돈다** — `.github/workflows/ci.yml:43`이 `working-directory: backend`
- ⚠️ **`backend/.venv`가 없다** — 테스트는 시스템 `python3`로. `~/.ohisell/venv`엔 pytest가 없다
- ★**자동 로그인 성공률은 100%가 아니다** — `ohitech_ad` 08-22 23:04:25 `자동 로그인 실패(수동 폴백): TimeoutError: Locator.fill 30000ms` 1건(직전·직후는 성공). 그래서 합격 ③은 「항상 된다」가 아니라 「기본 경로이고 실패 시 사유가 정직하게 남는다」로 판정한다
- ★**Wing은 ①SSO 무언 재발급이 원리적으로 불가** — 로그인이 `JSESSIONID` 세션 쿠키에 얹혀 있고 이 프로필의 xauth엔 `KEYCLOAK_IDENTITY`가 없다(`wing_browser_fetcher.py:464`). **②Keychain층이 본체**다. 라이브에서도 그대로 확인됐다(12:42:20 "SSO로 복구 안 됨")
- ★**`is_landed`가 출발 URL을 만족하면 `coupang_auth`가 `LandingPredicateError`로 즉시 죽는다** — 설정 실수는 시끄럽게 죽는 게 낫다는 모듈 계약. `_landed_on`은 `/tenants/` 경로를 요구해 이를 피한다
- ★**호스트는 반드시 파싱해서 비교한다** — keycloak URL이 redirect_uri에 목적지 호스트를 싣고, `m-wing.coupang.com`이 `wing.coupang.com`을 **포함한다**(2026-08-03 실사고 2종)
- `install_local_runtime.sh`가 `ad_settings_collect.py`를 **CAS로 건너뛴다** — 다른 세션의 미커밋 배포본. 내 것이 아니므로 건드리지 않았다
- 공유 메인 폴더에 병행 세션 미커밋 파일 20건+ — `git add -A` 금지, 항상 경로 지정

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: *"나는 다른건 모르겠고 수집이 폰이나 노트북이든 어디서든 문제없이 모두 잘 되도록 만들고 싶어. 이게 내 목표야."* (Jino 2026-08-23 12:03 KST)
  → 받는 세션은 이 문장을 **원문 그대로** 앵커 `목표:`로 옮긴다. 발명 금지.
- **남은 슬라이스**: W2 → W3 → W4 → (W5는 이월 1순위) → PR·적대 리뷰·머지

- [ ] **W2 표면 정합 A** — `Dashboard.tsx:447-455`가 outcome **전체**를 운반하게 하고(`bulkRefreshPersistence.ts` 타입 확장 포함), `:53-84 bulkStateText`의 failed/timeout 자체 문구를 `describeOutcome` 위임으로 교체, `:453` 2인자 호출로 정정, `refresh_contract.py:418` 폐기 문구 정정(「로그인 필요」 접두는 유지 — 프론트 폴백 매칭이 걸려야 한다). **타임아웃/실패 문구를 직접 만드는 다른 렌더 경로 grep 전수**
- [ ] **W3 표면 정합 B** — 215초 고정 → 2단 판정. `attempt_count==0`이 **T_pickup 90초** 지속 → 「Mac이 요청을 집지 않았습니다」 조기 정착 / 픽업 후엔 **T_max 600초**까지 추적 연장, 도달 시 실패가 아니라 「추적 종료 — 수집은 계속」. **레인별 고정 상한 표는 만들지 않는다**
- [ ] **W4 회귀 방지** — 패널↔`describeOutcome` 동일 문구 단언. ★적대 리뷰 위임문에 **변이 2종 의무** 명기: ①패널→`describeOutcome` 배선 절단 ②wing의 `coupang_auth` 호출 제거
- [ ] **W5 가시성 인지**(이월 1순위) — visibility 복귀 시 판정 전 `getStatus()` 1회 강제
- [x] ~~**PR → 적대 리뷰 → 머지**~~ **완료** — PR #342 · 리뷰 **PASS(P1 0)** · 머지 `beb85e5a` (§2-3). W2~W5는 **새 PR**로 간다
- [ ] **P2-1 편승** — `verify=` 인자 가드 단언 1개(§2-3의 유일한 생존 변이). W4에 같이 넣어라
- [ ] **Jino가 폰에서 「전체 갱신」 1회** — 합격 ①②는 사람이 눌러야만 닫힌다. 읽기 전용 QA로는 원리적으로 불가
- [ ] **WING2 자동복구 라이브 확인** — 자연 발생 대기(만료를 억지로 만들지 않는다)

### Jino 미결 2건 (착수를 막지 않는다 — 묻지 말고 W2부터 진행할 것)
- **N일**: 합격 ⑤(기간 유지) 검수 일수. 계약 제안 14일
- **범위**: 목표의 「모두」가 쿠팡 6레인까지인가, 네이버·스마트스토어 등 타 수집 경로까지인가(**동병 여부 미확인** — 넓히면 조사 슬라이스 선행)

### 파생 질문 1건 (내 변경과 무관 — 기록만)
- **성공적으로 «확인만» 한 회차도 신선도(`last_success_at`)를 올려야 하는가.** 안 올리면 데이터 없는 기간에 건강한 레인이 stale로 늙는다. 지금은 RG 임계가 9일이라 즉시 문제는 아니다

## 7. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_collection-works-everywhere-w1_20260823.md 읽고 이어서 작업해줘
```
