# 세션 인수인계: 오컷의심 13건 개방 결정 — «열지 않는다» + 제외 원장의 정체

> 저장일시: 2026-09-03 21:5x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것 (**직전 인계 `HANDOFF_the-label-was-not-the-enforcement_20260903.md`(n=83)를 먼저 읽고 오면 맥락이 붙는다**)
> 체인: `pao-논의` n=84 (세션 e91888d5 — n=83과 **같은 프로세스**, 종결 후 Jino 지시로 이어감) · 목표이름: **목적관철 목표**
> 검증 명령: `ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python -c \"from dotenv import load_dotenv; load_dotenv('/home/ubuntu/ohisell/backend/.env'); from app.database import SessionLocal; from sqlalchemy import text; print(SessionLocal().execute(text(\\\"SELECT COUNT(*) FROM naver_search_term_exclusion WHERE source='console_import' AND (restrict_kwd_id IS NULL OR restrict_kwd_id='')\\\")).scalar())\""` (기대 **3882**)
> → 이 수가 3,990에 가까울수록 「제외 원장은 우리 엔진의 산물이 아니라 Jino 수작업의 기록」이다.

> 제목의 뜻: **자른 것이 닫히지 않았다.** 원장은 「제외됨」이라 했고, 라이브는 계속 돈을 쓰고 있었다.

---

## 1. 프로젝트 위치 및 환경
n=83 인계 §1과 동일. 워크트리 `~/.claude-worktrees/ohisell/pao-n83`(브랜치 `feat/pao-n83`), venv `backend/.venv`(`PYTHONPATH=.` 필수).

## 2. 이번 세션 완료 목록

**읽기 전용 · 코드 0줄 · prod 쓰기 0 · 네이버 광고계정 쓰기 0.** Jino 지시 「너가 봤을때 가장 좋은 방법으로 진행해」(21:49).

- ✅ **결정 ① 확정 — 오컷의심 13건을 «열지 않는다».** 근거 셋(§3).
- ✅ **제외 원장의 정체 실측** — 3,990건 중 **3,882건(97.3%)이 `console_import` ∧ `restrict_kwd_id` NULL**.
- ✅ 등록부 n=84 append(n=83 종결 후 같은 프로세스로 이어감 — 닫힌 채 쓰지 않기 위해).
- ✅ n=83 착지 잔여 완주: PR **#699** 머지(기록물) · PR **#700** 머지(n=83 종결 행) — CI 각 3/3.

## 3. ★결정 ① — 열지 않는다 (근거 셋, 전부 실측)

**직전 세션이 「BEP 4~7배로 돌던 검색어를 잘라 뒀다」며 Jino 결정으로 올린 13건이다. 재보니 열 이유가 없다.**

**근거 1 — 「BEP 4~7배」는 전환 1건짜리 표본이다.**
13건 **합산**: 노출 1,283 · 클릭 **41** · **전환 14건**. 13건 중 **11건이 전환 1건**(최대 2건).
`폴드8와이드전면필름` RoAS **8.93** = **클릭 1 · 전환 1** · `갤럭시울트라S26지문방지` RoAS **7.83** = **클릭 1 · 전환 1**.
⇒ 오늘 계속 지적한 「하한 게이트 부재」의 **반대 방향 사례**다 — 표본이 없으면 나쁜 하향만 생기는 게 아니라 **「잘못 잘랐다」는 서사도 만들어진다.**

**근거 2 — 크기가 10배 작다(내 산술 오류 정정).**
초판은 검색어별 「일평균」을 그냥 더해 **26,531원/일**을 냈다. 창이 서로 달라(하루짜리 창이 +4,543/일로 잡힘) 틀렸다.
**공통 창(2026-07-04~08-04, 32일)**: 비용 **64,238** · 전환매출 **248,500** · RoAS 3.87 · 총이익 근사(매출/BEP 1.676 − 비용) **84,032원** ⇒ **2,626원/일**.

**근거 3 — ★가설이 이미 반증돼 있다.**
**08-17 제외 이후에도 같은 (검색어, 광고그룹)이 계속 돌았다**: **39행 · 비용 5,531원 · 전환매출 0원**(최근 09-02).
`S26울트라보호필름` 10일 2,898원 · `갤럭시Z폴드8필름` 3일 1,352원 · `폴드8와이드전면필름` 2일 1,281원 — **전부 전환 0**.
⇒ **「열면 다시 벌 것」은 가설이 아니라 이미 실험된 것이고, 결과는 «안 벌었다»**이다.

★이 결정은 **소유권 분리(북극성 §8-②)를 기다리지 않는다** — 근거가 「기다려야 해서」가 아니라 **「열 이유가 없어서」**로 바뀌었다.

## 4. ★★제외 원장의 정체 — 97.3%가 우리 것이 아니다

```
naver_search_term_exclusion                     3,990건
  source=console_import ∧ restrict_kwd_id NULL    3,882  (97.3%)
  restrict_kwd_id 있음 (= 우리가 API로 건 것)         105
  기타                                                3
naver_change_log 의 exclude_search_term              1건
```

⇒ **오늘 하루 「제외 자동화」를 논한 그 원장은 대부분 Jino가 콘솔에서 손으로 자른 것을 받아적은 기록**이다. 오컷의심 13건도 **전부 `console_import`**이고 `restrict_kwd_id`가 NULL이다(우리가 만든 게 아니니 당연).

## 5. 알려진 이슈·주의사항

- ★★**[미상] — 원장이 「제외됨」이라 하는데 라이브가 계속 돈을 썼다.** 13건이 08-17 이후 5,531원. **원인 미조사**: ⓐ콘솔 제외가 실제로 안 걸렸나 ⓑ리포트 지연 ⓒSHOPPING 제외가 「검색어」 축으로 안 도나(n=83이 잰 「SHOPPING은 restricted-keywords API가 거부」와 같은 뿌리일 수 있다) ⓓ다른 매칭 경로. **셋 다 안 봤다 — 추측하지 말 것.**
  ⇒ 3,882건 규모에서 이게 일반적이면 **제외 원장 전체가 못 믿을 자가 된다.**
- **n=83의 이슈·이월은 그대로 유효** — 그 인계 §5를 함께 읽을 것(계기판 지체 4회 · 「됐다 24」가 부풀 수 있음 · 이월 6건 · 리뷰어 임시본 잔존 · 무접촉 체인 3개).
- ⚠️ 훅의 `[체인] ⛔ n=49`는 공유 폴더 stale 오탐. 생존 판정은 **origin/main** 또는 `ps -p <proc pid>`.

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: 계약 D-NAO-280 발단 Jino 원문(n=83 인계 §2-1) + 「PAO의 기능별로 하나씩 구현을 해나가자」(2026-09-03 17:21).
- **남은 슬라이스**:

- [ ] ★★**제외가 «실제로 닫혔는가» 감사** — 이번 [미상]. 원장 `excluded` 3,986건 중 제외일 이후에도 지출이 이어진 건수·금액을 세고, 원인을 ⓐ~ⓓ로 가른다. **읽기 전용으로 시작**. **묻지 말고 진행**. ★이게 「원장이 거짓말한다」의 가장 큰 인스턴스일 수 있다.
- [ ] **결정 ② — SHOPPING `negative_keyword`를 정보성으로 편입.** `INFORMATIONAL_PROPOSAL_TYPES`(`proposal_writer.py:57`) 선례를 그대로 쓴다 — 새 기제 발명 0, 신호 보존, 화면 대기 없음. ⚠️`negative_keyword`는 SHOPPING·WEB_SITE 공용이라 **유형 전체를 돌리면 WEB_SITE가 죽는다** — 생성 시점 캠페인유형 분기 또는 타입 분리가 필요. **묻지 말고 진행**(되돌릴 수 있고 범위 안).
- [ ] **하한 게이트 — 두 레인 모두.** 세 곳이 1순위로 지목했고 **이번 세션이 그 필요를 «반대 방향»에서 한 번 더 확인했다**(n=1 표본이 「잘못 잘랐다」 서사를 만들었다). ★계약은 일 레인을 지목하나 **오늘 −18.5%를 만든 건 시간당 레인**이다.
- [ ] n=83 인계 §6의 나머지(값 검사 부분 회복 · 커버리지 3종 · P2-6 배선 · 미검증 3,970 재분류).
- [x] ~~결정 ① 오컷의심 13건 개방 여부~~ **확정 — 열지 않는다**(§3). 재론은 새 증거가 있을 때만.

## 7. 새 세션 시작 프롬프트

/session-relay pao-논의
