# 세션 인수인계: 「점화준비 목표」 S2 완주 + PAO 점화 체크포인트 — 「PAO 논의 58」

> 저장일시: 2026-08-27 11:2x KST · 체인 `pao-논의` **n=58** · 세션 `f9089bb3`
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## ★관측 명령 (계약 §4-C S2-a가 요구한 «readonly 1줄» — 최상단 고정)

```bash
ssh sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python3 scripts/exclusion_grade_backfill.py"
```

**인자 없이 실행하면 아무것도 안 바뀌고 표만 나온다**(기본이 읽기 전용). 2026-08-27 11:0x 실행 시:

```
미검증  3,971 · 오컷의심 13 · 성과미달 6 · 합계 3,990 · 계정 기본 BEP(현재) = 1.6760
── 계약 §4-C S2-a 기대치 대조 ──
  오컷의심 기대 13 실제 13  일치 / 성과미달 기대 6 실제 6  일치 / 미검증 기대 3,970 실제 3,971  차이 +1
── 이탈의 이유 ── (그 +1의 정체와 사유가 함께 출력됨)
```

★인터프리터는 `.venv/bin/python3`다(`venv/`가 아니다 — 처음 틀렸다).
★`--backfill`은 **이미 실행됐다.** 다시 돌릴 이유가 없다(재실행해도 `only_missing=True`라 안전하지만 관측이 상태를 바꾸면 안 된다).

---

## 1. 프로젝트 위치 및 환경

- 워크트리: `~/.claude-worktrees/ohiselling/pao-n58` (브랜치 `docs/pao-n58-close`, base `origin/main`)
- prod: `ssh sellc.ohitech.co.kr` · 앱 `/home/ubuntu/ohisell/backend` · DB `ohisell.db` · 인터프리터 `.venv/bin/python3`
- 활성 프로세스: `ohisell-backend-8001`(:8001) — 이번 배포로 8011→8001 전환됨. **블루-그린이라 다음 배포에서 또 바뀐다**(로그 grep에 프로세스 이름을 박지 말 것)
- 배포: `scripts/safe_deploy.sh <파일…> [--migrate] [--restart]` · 병합: `scripts/safe_merge.sh <PR번호>`
- 환경변수: 키 이름만 — `DATABASE_URL`

## 2. 이번 세션 완료 목록

- ✅ **S2 구현** — `backend/app/services/naver_ad/exclusion_grade.py`(신규, 등급 5종·팩토리·백필·분포 보고) · `models.py`(`grade`·`grade_reason` 2칸 additive nullable) · 마이그 `exgrade1s2_add_exclusion_grade.py` · 4경로 배선(`search_term_execution.py` 2곳 · `search_term_ss_lane.py` 2곳) · `backend/scripts/exclusion_grade_backfill.py`(신규, 관측/백필 겸용) · `backend/tests/test_exclusion_grade.py`(신규 32건)
- ✅ **적대 리뷰 1R FAIL → 수정 → 2R FAIL → 수정** (둘 다 «Jino가 읽는 화면»의 결함)
- ✅ **PR #494 머지 `d9132d10`** · CI 3/3 실통과 · `--force` 미사용
- ✅ **prod 무중단 배포** — 마이그 `cst60auto → exgrade1s2` · CAS 3/3 · 다운타임 0 · 활성 :8001
- ✅ **prod 백필 3,990행 라이브 완료** + readonly 관측으로 분포 확인
- ✅ **ref 104 「PAO 점화 체크포인트」** 저술(Fable) — `docs/references/104_pao_ignition_checkpoint_20260827.md`, 조건 **50개**·현재 **4/50**
- ✅ **08-25 vs 08-26 순이익 하락 실측**(Jino 질문 답)
- ✅ 트랙 확인줄 2건 누적(착수 09:1x · 결과 11:1x) · 체인 n=58 등록

## 2-1. 완료 QA (판정 원문 그대로 — 별도 Sonnet·읽기 전용)

- **작업 목적(정본 원문 — 계약 `docs/contracts/CONTRACT_ignition_readiness.md` §1)**: *"2026-08-26 실측으로 「임대 제외·재개방 실험·파라미터 학습·초기 탐색 허용」 운영 방식이 합의됐으나, 그 방식이 서기 위한 배선 11개가 없다. … 이 계약의 산출물은 성과가 아니라 «준비 완료 상태»다."*
- **합격기준(원문 — §4-C 중 S2 몫)**: S2-a 등급 분포표 출력 + [E] 차이 시 이유 병기 / S2-b 신규 제외 경로가 등급·만료일 없이 행을 만들 수 없음이 테스트로 고정 / 공통 광고계정 쓰기 0 · 적대 리뷰 P1=0(표면 변이 2종 포함) · 마이그 `--migrate` 순서

> 대조 대상 4개를 **각각** 판정(전역 §2 강화 조항).

판정(계약 §4-C S2): **달성** — S2-a·S2-b·공통 세 항목 모두 라이브 증거로 확인됨 (2026-08-27 KST 11:10경)

판정(Jino 2026-08-26 23:08 지시 원문): **달성** — 자율 판단 하에 무정지 이행, §4-B④-ⓓ(유일한 질문 지점) 미발동 확인 (2026-08-27 KST)

판정(트랙 헤더 합격 M항목): **달성** — 진행률 2/7 불변이 정확히 자기보고됨(과대·과소 주장 없음) (2026-08-27 KST)

판정(북극성 §1·§6): **달성** — 5요소 이동 없음이 정직하게 보고됨, §7 액셀·브레이크 대칭 검사 실시 (2026-08-27 KST)

**종합 판정: 달성**

- **항목별 근거(QA 원문 요지)**: S2-a = readonly 실행에서 13/6/3,971·합계 3,990, 차이 +1의 원인 행(id=579)과 근거 문장을 즉시 지목 ⇒ *"계약 원문 「수치가 [E]와 다르면 다른 이유가 함께 출력·기록돼 있다」를 문자 그대로 충족"* / S2-b = `NaverSearchTermExclusion(` 팩토리 밖 호출 **0건**, 4경로 개별 테스트 + 재제외 경로 테스트 존재 / 공통 = `auto_operate` `(0,'none',9)` 전건·`max(updated_at)=2026-08-24 08:15:01`(배포 01:57 UTC보다 이전) · `next_review_at` NULL 3,987/date 3 백필 전과 동일 · md5 3파일 로컬↔prod 일치 · 매니페스트 순서 마이그(01:57:53)→upgrade(01:57:59)→코드(01:58:09)→재시작(01:58:25)
- **미달·미판정 항목**: **없음**(4대조 전부 달성)
- **목적 전환 여부**: 없음. 단 **Jino 발의 2건**(점화 체크포인트 저술 · 순이익 하락 실측)이 세션 중 추가됐고, 앵커 `이번 세션` 줄에 선언 후 착수했다(§7 「Jino가 먼저 꺼낸 것」 경로).
- **QA가 확인하지 못한 것(그대로 보존)**:
  1. 적대 리뷰 1R/2R 원 트랜스크립트 — 서브에이전트 수행이라 **PR 댓글·리뷰 0건**(`gh pr view 494 --json comments,reviews` 둘 다 빈 배열). 커밋 메시지 서술 ↔ 대응 테스트 변경의 정합으로 **간접 확인만**. 리뷰어가 실제로 다른 기였는지, 3R 미실행에 리뷰어가 동의했는지는 직접 관측 불가.
  2. `grade` 라벨이 M4(소유권 분리 후 재개방) 의사결정에 실제로 어떻게 쓰일지 — 계약이 스스로 「라벨링이지 실행이 아니다」라 명시한 범위 밖.
  3. **id=579 외 유사 자기모순 253행/236쌍의 등급 재분류 완결성** — 표본 1건(id=579)만 직접 대조. classify() 원리는 확인됐으나 253행 전부가 의도대로 떨어졌는지는 개별 재계산 안 함.
  - ※로컬 전체 스위트 재실행은 QA 제출 «후» 완료돼 미확인에서 제외됨(exit 0·실패 마커 0 — CI 3/3과 일치).

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."* (Jino 2026-07-19, D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** (불변) — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음.** 계약 §7-1이 *"어느 M 체크박스도 직접 못 움직인다 — 이 계약은 M4의 «승인 필요» 칸을 제외한 모든 전제를 초록으로 만드는 준비 계약"*이라 자기규정했다. S2는 **L3 집행 원장 정비**.
- **헤더에 남긴 확인 줄**: 2건 누적 — `확인: 2026-08-27 09:1x KST [f9089bb3] — … 착수 기록` · `확인: 2026-08-27 11:1x KST [f9089bb3] — … 결과 기록`
- **다음 세션 후보 항목**: 계약 §4-A 순서로 **S3 / S4 / S5 / S6** — **넷 다 선행 없음.** ★**S4 권고**(사유: 오늘 실측이 *"학습 가능한 파라미터 3개가 전부 브레이크"*를 확정했고, S4가 그 봉투를 여는 유일한 슬라이스 = 액셀 쪽을 학습이 만질 수 있게 되는 경로. 북극성 §7 대칭 요구에 직결). 마이그 0.
- **트랙 종결 여부**: **미도달**(2/7).

## 2-3. 착지

- **완료 단계**: **전 단계 완주.** 코드 PR #494 = 커밋 → push → PR → 적대 리뷰 2R → 머지 → **prod 무중단 배포 → 백필 → 라이브 관측**. 기록물 PR #496 = 커밋 → push → PR → (리뷰 생략·기록물만) → 머지.
- **멈춘 단계**: **없음** — 코드 PR #494·기록물 PR #496 **둘 다 머지 완료**.
- **재개 명령**: 해당 없음(전 단계 완주). 검증은 `gh pr view 494` · `gh pr view 496` · `git log origin/main --oneline -3`
- **좌표**: 커밋 `2c91c8e0`(S2 구현) → `291ff0c6`(1R P1 수정) → `45abc5a1`(2R P1 수정) → `67836b26`(ref 104·트랙 착수줄·체인) / PR **#494** → 머지 **`d9132d10`** / PR **#496** → 머지 **`87754f78`**(트랙 결과줄 + 이 HANDOFF + 체인) · CI **3/3 실통과**(py3.10 8m52s·py3.14 9m23s·frontend 1m28s)·`--force` 미사용
- **리뷰 판정**: PR #494 = 적대 리뷰 **1R FAIL(P1-1) → 2R FAIL(P1 잔여) → 수정 후 P1=0** / PR #496 = `⚠️ 리뷰 생략: 기록물만 — docs/tracks/active/track_naver-ad-optimization.md · .claude/memory/HANDOFF_ignition-readiness-s2_20260827.md · .claude/memory/chains/pao-논의.jsonl`
- **배포**: `safe_deploy.sh --migrate --restart` · 마이그 `cst60auto → exgrade1s2` · CAS 3/3 · **무중단(다운타임 0초)** · 활성 `:8001`
- **L5**: 「저장소를 main에 세워둔다」 **생략** — 로컬 `main`이 공유 메인 폴더에 점유돼 있다. 다음 세션은 `git switch -c <새> origin/main`으로 시작할 것.

## 3. 확정된 결정사항

- **D-NAO-259** — 제외 «임대» 등급. 원장 행이 태어나는 자리는 `exclusion_grade.new_exclusion()` **하나뿐**이고, 인구조사 테스트가 그 밖의 직접 호출을 **0으로 못 박는다**(별칭 import·`scripts/` 스캔 포함).
- **재심사 백오프의 «정의 자리»가 `exclusion_grade`로 통합됐다** — 값(30일·상한 90일)은 불변. `search_term_execution._REVIEW_BACKOFF_*`는 그 이름을 되읽는다(호환).
- **`cost=0 ∧ conv≥1` → `미검증`(보류)** — ★초판은 `오컷의심`이었고 **적대 리뷰가 뒤집었다**. 근거: 제외(08-17) «후»인 08-24·25에도 노출이 나서 **컷이 그 매출을 막지 못했다** ⇒ 「컷이 틀렸다」의 실측이 아니다. 같은 데이터가 「컷은 돈을 안 쓰게 했고 매출은 어차피 났다」로도 읽힌다(그러면 총이익은 **더 좋다**). 판정 근거가 없으면 보류가 정직하다.
- **백필은 만료일을 안 건드린다**(`keep_review_at=True`) — 라벨링이지 실행이 아니다. 라이브로 증명됨(NULL 3,987/date 3 불변).
- **3R 미실행** — 리뷰어가 정확한 패치를 지정했고 설계 이견이 아니라 문자열이며, 그가 지정한 단언을 테스트에 박았다(§4 라운드 증식 차단). **QA가 이 판단을 정당한 처분으로 판정**했다(단 리뷰어의 독립 재확인은 미관측).

## 4. 핵심 파일 목록

| 파일 | 역할 |
|---|---|
| `docs/contracts/CONTRACT_ignition_readiness.md` | **정본 계약** — §4-A 슬라이스 S1~S6 · §4-B⑥⑦ 등급·백필 규칙 · §4-C 합격기준 · §6 착수 규칙 |
| `docs/references/104_pao_ignition_checkpoint_20260827.md` | **★신규** 점화 체크포인트 50항목(4/50) · §9 파편 지도 · §9-1 갈라진 기록 12건 · §10 자백 8건 |
| `docs/references/82_pao_north_star_20260819.md` | 북극성 — §1 5요소 · §5 학습 루프 · §6 M0~M6 · §7 금지선 · §8 Jino 결정 |
| `backend/app/services/naver_ad/exclusion_grade.py` | **★신규** 등급 단일 결정 지점(팩토리·classify·backfill·distribution_report) |
| `backend/scripts/exclusion_grade_backfill.py` | **★신규** 관측(기본)·백필(`--backfill`) — S2-a 표면 |
| `backend/tests/test_exclusion_grade.py` | 32건 — 인구조사·4경로·재제외·화면 출력 3종 |
| `backend/app/services/naver_ad/search_term_ss_lane.py` | autofire·고아치유·재개방(`_open_exclusion` 297~) |
| `backend/app/services/naver_ad/search_term_execution.py` | `record_execution`·`import_console_exclusions` |
| `.claude/memory/HANDOFF_exclusion-learning-contract_20260826.md` | 실측 [A]~[I] 원문(계약 부록의 정본) |

## 5. 알려진 이슈 / 주의사항

1. ⚠️★**시크릿 노출 — 재발급 필요(Jino 손).** prod 인터프리터를 찾다 `pm2` 덤프 앞부분을 출력해 **`CLAUDE_CODE_OAUTH_TOKEN`이 채팅에 노출**됐다(2026-08-27 11:0x). n=55부터 이월된 「API 시크릿 3세트 재발급」에 **이 건이 추가**된다. **`/home/ubuntu/.pm2/dump.pm2`를 열지 말 것.**
2. ⚠️ **학습 재료 만료 2026-10-28** — `blocked` 일기 3,863건이 90일 수확 창을 이탈한다. 오늘 기준 **D-62**.
3. ⚠️ **훅 오탐 11회**(이 세션에서만) — 공유 메인 폴더가 `origin/main`보다 **535커밋** stale이라 `[체인] ⛔` 2건·`[트랙 기록 누락]` 1건이 매 턴 뜬다. **확인줄은 워크트리·`origin/main`(커밋 `67836b26`) 양쪽에 실재한다.** 하네스 소관이라 손대지 않았다.
4. **인터프리터는 `.venv/bin/python3`** — `venv/bin/python`이 아니다(처음 틀려서 실패했다).
5. **순이익 「정본」이 둘이고 홈 화면에 둘 다 쓰인다** — KPI 카드 `/api/dashboard/kpi`(4채널) vs 바로 아래 일별 추이 차트 `/api/dashboard/trend`(**네이버+자사몰만, 쿠팡 누락**). 같은 이름의 두 숫자가 다르다(08-25 네이버 매출 1,630,020 vs 1,664,720). → 소관: `track_coupang-rocket-1p.md`. **가져오지 않았다.**
6. **`naver_hourly_snapshot` 일마감 총계와 `naver_ad_daily` 정본이 불일치**(08-26: 407,920 vs 465,063), 원인 [미상].
7. QA 미확인 3건은 §2-1 그대로. 특히 **253행/236쌍 자기모순 레코드의 등급 재분류 완결성 미검증**.

## 5-1. ★어제 순이익 하락 실측 (Jino 질문 2026-08-27 10:3x — 답의 근거)

> Jino 원문: *"그리고 어제 같은 경우 그저께 대비해서 순이익이 굉장히 떨어졌어. 이렇게 떨어지지 않도록 우리가 즉각 대응해서 어제의 순이익을 방어할 수 있는 방법이 있었을까?"*

- **하락은 네이버 단독**: 순이익 389,159 → 186,861 (**−52.0%**). 자사몰 +23,146 · RG +15,080 · 1P +61,091(덜 나쁨) — **나머지 셋은 전부 개선**.
- **방향이 반대로 움직였다**: 매출 **−16.0%**(1,630,020→1,368,820) · 주문 **−11.0%**(91→81) · **광고비 +6.8%**(530,695→566,916). 원가·수수료는 매출 비례로 정상 감소.
- **광고비 증가분 36,221원의 55%(20,017원·+24.5%)가 GFA**(디스플레이). SA는 +3.6%.
- ★**GFA는 하루 중 관측이 원리적으로 불가** — `ad_costs`의 `gfa:advoost`/`gfa:da`가 **22:10 일 1회 배치**. 시간별 신호 0. **다음 날 아침에야 알 수 있다.**
- **SA는 시간별로 보이나 특정 시간 급발진 없음** — 오후~저녁에 고르게 조금씩 높았고, 최대 증분 시간(17시)은 08-25도 동일.
- ★**같은 요일 4주 비교**: 수요일 순이익 590,198 → 583,642 → 774,742 → **230,030**(직전 수 **−70%**, 명확한 이례) / 화요일 417,432 → 402,169 → 569,635 → **409,182**(08-25는 중간값 근처). **매출은 4주째 하락 추세**(수 기준 258만→257만→234만→**146만**).
- **그날 조치성 일기 0건**(`observe` 4건만) · `naver_adgroup_scope` **0행** · `auto_operate` 9행 전건 0 · 크론은 정상 발화.
- ⇒ **결론: 어제 즉각 대응은 원리적으로 불가능했다.** 손이 꺼져 있었고(7-30 이후 우리 실쓰기 0), 증가분의 절반은 관측조차 안 됐고, 나머지 절반도 하루 중엔 이상 신호로 안 보였다. **그리고 하락의 6분의 5는 매출이 빠진 것**이라 광고를 조여서 되찾을 수 있는 몫이 아니다(D-NAO-85가 경계하는 방향).
- 실측 좌표: `dashboard.py`(`_channel_rows`/`_kpi_totals`/`calculate_daily_trend`) · `profit_calculator.py:807` · prod `ad_costs`·`naver_ad_daily`·`naver_hourly_snapshot`·`scheduler_state`

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문 — 계약 §1, 받는 세션은 이걸 앵커 `목표:`로 그대로 복사한다)**:
  > *"2026-08-26 실측으로 「임대 제외·재개방 실험·파라미터 학습·초기 탐색 허용」 운영 방식이 합의됐으나, 그 방식이 서기 위한 배선 11개가 없다. 되면 — 점화 결정이 나는 순간 — ⓐ탐색이 stale 벽 없이 돌고 ⓑ새 제외마다 등급·만료일이 붙고 ⓒ재개방이 일기→지혜 사슬에 타며 ⓓ학습이 게이트 숫자 4개를 승인 카드로 조정할 수 있고 ⓔJino가 명령 한 줄로 각 부품의 생존을 확인한다. 이 계약의 산출물은 성과가 아니라 «준비 완료 상태»다."*
- **남은 슬라이스** (계약 §4-A · §6 *"완료된 슬라이스는 건너뛰고 첫 미완 슬라이스부터"* · **묻지 말고 진행**):
- [ ] **S3** 재개방=실험 배선 — 재개방·재제외 경로에 execute 일기 기록(harness 우회 경로 포함) + 복귀 후 성적 채점기 실측(`[미상]` 해소). 마이그 0. ★라이브 발생은 점화 전 불가 ⇒ 완료 QA는 «배선 존재 확인 + 라이브 판정불능»으로 남긴다(계약 §4-C S3-a 원문)
- [ ] **S4** 파라미터 SPECS 승격 4개(§4-B⑤ 봉투 표 그대로) + 검색어 원본 보존 16일→창 상한 이상. 마이그 0. ★**권고 1순위** — 오늘 실측이 「학습 가능 파라미터 3개 전부 브레이크」를 확정했고 S4가 그 봉투를 여는 유일한 경로다. ⚠️승격 전 **분산 import 검사 필수**(`max_change_pct`가 생성기 7곳 분산 탓 SPECS에서 빠진 전례). ★부수 발견: 백오프도 3곳 분산이었다(이번에 통합) — 같은 검사를 다른 파라미터에도 돌릴 것
- [ ] **S5** 파워링크 광고 제목·설명 수집(`get_ads` 응답의 버려진 절반) + ref 103 §10-3 「스냅샷 동결 미착수」 실상 정정. 마이그 1. ⚠️WEB_SITE 그룹 수 착수 실측 필요(A5/A6 스윕 1,013콜/일보다 크면 착수 전 표면화하고 멈춤)
- [ ] **S6** 그룹별 제외 사용률·소진 예상일 표면 + 스코프 0행 선행 검사 + 관할 등록(`auto_operate=0`으로만). 마이그 0
- [ ] **PR #496 머지** — 이 HANDOFF·트랙 결과줄·체인. 기록물만이라 리뷰 생략

**Jino 손이 필요한 것** (묻지 말고 진행 대상 «아님»):
- [ ] ★**시크릿 재발급** — n=55 승계 3세트 + **이 세션이 노출시킨 `CLAUDE_CODE_OAUTH_TOKEN`**
- [ ] 의미단위 등록 표현 실증 — 콘솔 수동 1~2건(2026-08-26 승낙분)
- [ ] 확장 「메이커 셀링 도우미」 검색광고 API 키 3종 교체 · 크롬 「Apple 이벤트의 JavaScript 허용」 되돌리기 (n=55 승계)

**Jino 결정 대기** (전부 ref 104에 조건으로 등재됨 — **내가 안건화하지 않는다**):
- [ ] **점화**(북극성 §8-①) — 2026-10-28 전이 유리(옛 학습 재료 대조 가능 시한)
- [ ] **소유권 분리**(§8-②) · `conv≥1` 절대보호 개정 · 정식 키워드 등록 쓰기 손 · D-NAO-114 인수 범위
- [ ] ★**F-3 승격(액셀) 실행 경로 — 소관 미배정** (ref 104 §8이 새로 등재). `growth_bid_up` 자동레인 배제 + 콘솔 pending이 `trigger_pacing` 4,386건에 덮여 **전 역사 실집행 0건**. 액셀의 유일한 집행 경로인데 **S1~S6·점화 계약 어디에도 수리 항목이 없다.** 배정은 Jino 발의 몫
- [ ] ★**GFA 취급**(§8-④) — 어제 광고비 증가의 55%인데 성과 관측이 원리적으로 막힘. 계열 90일 845만원. 현상 유지/대조축 개통/지출 재검토 중 택일

**이월 (이 세션 스코프 밖 — 기록만)**:
- 순이익 정본 이원화(§5-5) → 소관 `track_coupang-rocket-1p.md`
- `naver_hourly_snapshot` ↔ `naver_ad_daily` 불일치(§5-6) → PAO 트랙이되 현 계약 S1~S6에 없음
- 훅 오탐 상습(§5-3) → 하네스 소관
- **다음 가용 번호: D-NAO-260 · 교훈 #371** (이 세션 미사용)

## 7. 새 세션 시작 프롬프트

```
/session-relay PAO 논의
```
