# HANDOFF — 소재 하향 자동 개방(D-NAO-125, 배포 보류) + 대행사 외부변경 재확인(D-NAO-126)
날짜: 2026-07-29 오후~저녁 (KST)
프로젝트: ohisell — 네이버 SA 광고 최적화 트랙
브랜치: main(문서 세션) / 코드는 `claude/d-nao-125-ad-bid-down-open`(커밋 `cc778de`, push 완료, **미배포**)

---

## §0 가장 먼저 알아야 할 것

1. **대행사가 아직 계정을 만지고 있다.** 오늘 15:39:05 KST에 폴드8와이드 소재(`nad-a001-02-000000554755092`) 입찰이 1,600→1,000원으로 **기록 없이** 되돌려졌다. 네이버 소재 객체의 `editTm` 필드로 시각 확정. Jino 본인 아님(확인함) → 대행사로 추정. Jino가 "손대지 말라고 했으니 이제 우리가 컨트롤한다"고 확정.
2. **소재(ad) grain 외부 변경 탐지가 전무하다.** `entity_sync`·`naver_agency_op`·`naver_entity`가 campaign/adgroup/keyword grain만 추적한다. 07:37 그룹 예산 200,000→300,000 외부 변경은 `naver_agency_op` id 83으로 잡혔지만, 15:39 소재 입찰 변경은 **내일 배치가 돌아도 영원히 안 잡힌다.** 이번 건은 쓰기 직전 현재값 확인 중 우연히 걸린 것.
3. **D-NAO-125는 브랜치 `claude/d-nao-125-ad-bid-down-open`(커밋 `cc778de`, push 완료)에 있고 배포하면 안 된다** — codex 게이트 FAIL(P1 4건, 아래 §2).
4. 폴드8와이드 입찰은 **현재 1,000원 그대로 두기로 확정**(아래 §3 근거).

---

## §1 오늘 완료된 것

- **D-NAO-122 당일 그룹 시간별 수집** — PR #165 병합(`eb8dab9`), prod 배포. 크론 `sweep_naver_today_hourly` = **`57 * * * *`**(★코드 기본값 :35가 아니라 :57이 prod 실제값. 세션 중 이 불일치로 혼선 있었음). 첫 발동 14:58:08 KST **합격 4/4**: last_run_at 채워짐 / 2,112행·226그룹 적재 / 진행 중 시간 버킷 미저장(max hour 13 < 14) / `naver_keyword_hourly` 오늘 0행(전용 테이블 분리 유지). 로그 `{'targets':236,'swept':236,'calls':236,'rows':2112,'failed':0,'skipped':10}`.
  - ★설계 핵심(codex P1 대응): 당일 진행분을 `naver_keyword_hourly`에 넣으면 `auto_operator._exploration_yesterday_flow`의 완결 불변식("행이 있다 = 그 날이 통째로 스윕됐다")이 깨져 롤링 24h 흐름이 과소계상된다 → 전용 테이블 `naver_adgroup_hourly_today` 신설(alembic `a1c4e7f20b91`).
- **PR #121(D-NAO-93, 2일 묵음) 해소·배포·병합**(`5249447`) — 원인은 코드 충돌이 아니라 alembic 형제 head 분기. merge 리비전 `b7d2f4a09c31` 신설(DDL 없음), `safe_deploy --migrate --restart`, pm2 restart 283, 라이브 검증(alembic current=b7d2f4a09c31, 컬럼 2개 실재, 에러 0). 테스트 4,091.
- **3세션째 인계되던 부채 정리**: 로컬 main ↔ origin/main 분기 해소(`reset --hard` 안 쓰고 커밋 후 `rebase --onto` — 커밋 안 된 LESSONS #67 48줄이 날아갈 뻔했음). iCloud `" 2"` 복제본 6건 대조 후 삭제(트랙 파일 복제본 고유 내용 0 확인 = "두 세션 동시 편집" 경고 해소). **열린 PR 0건 · main 동기화 완료.**
- **MOP59·MOP60 이행 감사** — 두 세션의 코드 산출물(D-NAO-117·118·119·121)은 전부 배포됨. 다만 HANDOFF로 넘긴 "다음 할 일"은 상당수 미이행. ★발견된 패턴: "코드는 배포까지 가는데 운영·위생 부채는 세션마다 목록만 옮겨 적히고 실행되지 않는다."

---

## §2 D-NAO-125 (배포 보류 — 브랜치에 있음)

- **원인**: D-NAO-70②(Jino 2026-07-21 16:11 원문 *"쓰기 카나리를 모든 캠페인으로 확대해"*)가 쓰기 경계(harness)에만 반영되고 **제안 생성 게이트에 반영 안 된 채 8일** 방치. 같은 주 적대 리뷰가 P2로 지적했으나 미이행.
- **피해 실측**: 소재 278개 중 256개(92.1%)가 `use_group_bid_amt=false`. 4캠페인·92그룹·256소재가 "운영 표시는 됐는데 손이 없음". 7일 홀드 78건(**UP 59 · DOWN 19** — 상향이 3배 더 막힘). 유일 카나리(맥세이프)는 ad-레버 유닛이 없어 **B3 메커니즘을 역대 한 번도 발동시킨 적 없음**.
- **변경**: `AD_BID_ROUTING_ENABLED` 킬스위치 신설, `_ad_bid_canary()`가 캠페인 목록 대신 킬스위치만 봄(스코프는 auto_operate가 이미 2중으로 검사). `AD_BID_CANARY_CAMPAIGNS`는 **불변**(delegation_gate·expert_briefing_builder가 정반대 의미로 씀 — 채우면 계정 전체 위임이 죽는다). `_AD_AUTO_EXEC_PROPOSAL_TYPES={"bid_down"}` + `_ad_auto_exec()`로 하향만 인라인 자동 실행. 테스트 4,098 passed.
- **★codex P1 4건(배포 차단 사유 — 다음 세션이 고쳐야 함)**:
  1. 하향에 **일일 상한·스톱로스가 적용되지 않는다** — `guardrail_gate`가 `bid_down`을 `_MAX_DAILY_CHANGES`에서 명시적 면제(DL3), 스톱로스/BEP는 UP 분기에만. 오판정 지속 시 2h 쿨다운으로 하루 8스텝 = 800원→약 220원 절단 가능. ★세션 중 Claude가 Jino께 "일일 3건으로 묶여 있어 안전하다"고 말한 것은 **틀린 설명이었고 정정됨**.
  2. dedup 제거 + 쿨다운만으로는 **동시 실행 중복 쓰기**를 못 막는다(레인 중복 기동 시 둘 다 pre-write 쿨다운 통과 가능).
  3. **킬스위치가 완전 롤백이 아니다** — `approved` 커밋 후 크래시하면 잔존 행이 harness를 통과. harness 초크포인트에서 `AD_BID_ROUTING_ENABLED` 재검사 필요.
  4. **첫 실행 blast radius 무제한** — 92그룹이 한 번에 하향될 수 있다(레인당/계정당 캡 없음).
  - P2 2건: `cold_op+bid_down` 역방향 매핑 비대칭 / `proposal_writer`의 `_ad_bid_canary` 소비처가 `auto_operate`가 아닌 `optimizer=="ours"`만 검사.

---

## §3 폴드8와이드 사건 (라이브 실측)

- 오늘 10:58 Jino 지시로 1,000→1,600원 인상(change_log 974). 울트라도 10:59 동일(975).
- 시간별 실측(신설 `naver_adgroup_hourly_today`): 인상 전(0~10시) 가중 avg_rank **3.41**, CPC 900 / 인상 후(11~13시) **1.56**, CPC 1,602. 대조군 플립8(1,900원 불변)은 3.26→3.53으로 거의 무변화.
- 오늘 이 그룹 광고비 15시까지 **약 194,000원**(14시 한 시간에 59,117원). 캠페인 일예산 300,000.
- 오늘 추정 ROAS **0.945** vs BEP ROAS **1.9611** = 손실 구간. RL3 손실 고삐가 13:20·14:20·15:20 세 번 판정했으나 세 번 다 `[레버 미연결] … B3 대기`로 미집행.
- **★1,600원은 과지출이었다**: 목표가 4위인데 실측 1.4~1.7위. 사다리는 "4위=1,360원"이라 했으나 **1,000원에서 이미 3.4~4.5위** → 사다리/실측 배율 약 **0.74**. 이것이 미착수 항목 "머리 키워드 보정계수"의 **첫 데이터점**이다.
- 과거 자연 실험과 정합: 1.41위 → ROAS 0.85 / 3.04위 → ROAS 3.15 (1위권이 오히려 손해).
- **결정: 입찰을 건드리지 않고 1,000원 유지**(실측상 목표 4위에 맞는 값).

---

## §4 제외키워드 현황 (Jino 질문에 대한 답)

- **작동 중이다** — `search_term_ss_lane._autofire_exclude`가 Confirm 없이 자동 실행. 실집행 1건(07-22 13:59 "아이패드종이필름", dry_run=0, 낭비비용 22,854원). 네이버 계정 `negative_kw_count`와 change_log 일치(라이브 교차검증). 일 레인(08:50) 안에서 07-23~29 매일 실행.
- **다만 스코프가 1개 캠페인** — 네이버 API가 쇼핑 검색어 제외를 구조적으로 거부(400). auto_operate 6캠페인 중 WEB_SITE는 아이패드 파워링크 1개뿐이고, 그마저 30일 clk 281의 초저볼륨이라 후보 0건. **우리 주력(쇼핑)에는 이 레버를 못 쓴다.**
- in-out 재심사는 `next_review_at=2026-08-21` 정상 대기.
- 부수 발견: 구 X1a 경로가 매일 `negative_keyword` pending 제안을 만드는데 **14건 전부 한 번도 실행된 적 없음**(쇼핑 대상이라 실행돼도 API 거부) — 죽은 경로가 Confirm 큐를 오염.
- 부수 발견: `naver_execution_harness` docstring이 *"자동 발사 없음… ss_exclude는 정의만·미배선"*이라고 **스테일**(실제로는 07-22 배선됨).

---

## §5 다음 세션 할 일 (우선순위 순)

1. **소재(ad) grain 외부 변경 탐지 추가** — `editTm` 필드가 신뢰 가능함을 오늘 확인(울트라의 editTm이 우리 change_log와 소수점까지 일치). 기존 `entity_sync`/`naver_agency_op`에 ad grain을 얹는다. 관측 전용이라 위험 낮음. **우리가 설정한 값이 유지되는지 확인할 수단이 없으면 자동 제어를 켜도 조용히 덮인다.**
2. **D-NAO-125 codex P1 4건 수정 후 배포** — 하향 전용 일일 상한, TOCTOU 재확인, harness 킬스위치 재검사, 레인당 하향 캡. 그 뒤 codex 재리뷰 → 배포 → 다음 :20 레인에서 자동 하향 첫 라이브 증거 확인.
3. **머리 키워드 보정계수** — 오늘 첫 데이터점 확보(배율 ~0.74). 07-30 아침 07:40 검색어별 D+1 실측이 들어오면 와이드·울트라 두 점 더 확보 가능.
4. **성과 상향 자동화(3단계)** — 실쓰기 경계에 새 (승인원⟺타입) 쌍 신설 필요. 클램프 면제 구멍(재현: 300원→90,000원)을 막은 자리라 codex 적대 리뷰 필수.
5. 구 X1a `negative_keyword` 죽은 경로 정리 + harness docstring 스테일 정정.
6. 이전 세션에서 넘어와 여전히 미착수: 이익 서보(D-NAO-124) / 시간별 이탈 경보 / `not_viable` 소비 경로 / 일예산 초과 자동 지출중단 / 출시배수 3.0.

---

## 트랙 기록

- `docs/tracks/active/track_naver-ad-optimization.md` — D-NAO-125·126 확정 결정 추가, "현재 진행 단계"·"다음 액션" 최신 블록 갱신 완료.
- `claude-progress.txt` — 이 세션 요약 갱신 완료.
