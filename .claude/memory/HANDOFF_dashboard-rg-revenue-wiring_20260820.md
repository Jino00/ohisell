# 세션 인수인계: 대시보드에 RG 매출 배선 + 채널 이름 분리 (착수 전)
> 저장일시: 2026-08-20 13:3x KST
> **이 파일은 「다음에 할 일」의 인계다.** 오늘 끝낸 두 작업은 별도 HANDOFF에 있다:
> `HANDOFF_ad-spend-reaches-net-profit_20260820.md`(D-22) · `HANDOFF_rg-button-cooldown_20260820.md`

---

## 0-A. 우리 목표 — 이 작업이 어디에 속하는가 (표류 방지용, 먼저 읽어라)

**이건 「새로 발견한 버그」가 아니다. 이 트랙이 2026-08-04에 착수하며 적어 둔 관측이고,
16일째 남아 있는 그 트랙의 합격기준①이다.** 새 기획으로 재해석하지 마라.

### 목표 계층 (전부 원문 인용)

**① 트랙 「쿠팡 손익 정합 (2계정 4채널)」** — `docs/TRACKS.md`, 2026-08-04 착수·계약 승인됨(08-05 00:03)
> 목적=스마트스토어처럼 **쿠팡 숫자를 정산 원장과 맞추기.**
> ★**대시보드가 회사 매출의 34.5%만 보여준다** — **RG1 2,170만원이 매출 0인데 비용
> 1,736만원만 들어가 오픽스가 −1,734만원**, 로켓1P는 `channel_type='consignment'`라
> **행 자체가 없다.**

★이 문장이 이 작업의 **정본 목적**이다. 오늘(08-20) 30일 창에서 다시 재도 같은 모양이다
(매출 178,200 / 순이익 −4,782,379). **로켓1P 쪽은 해결됐고(leaf 배선, PR #199) RG 쪽만 남았다.**

**② 하위 트랙 `track_coupang-rocket-1p.md`** (상태 `🟢 Active 4/6`)
> 오하이테크의 **로켓배송(1P, 쿠팡 사입판매)** 발주·납품·정산 데이터를 supplier.coupang.com에서
> 수집해 종합조망(Command Center)에 편입 — **3P/RG와 나란히 매출·순이익을 본다.**

**③ 이 작업의 좌표 = 그 트랙의 「합격기준①」**
> `- **미결(이월)**: ①오픽스 RG −17,342,298원(**합격기준①**) 미착수`
> `- ④**합격기준 ①**(오픽스 RG 매출 −17,342,298원 배선)은 이 세션 범위 밖 — 미착수.`

즉 **여러 세션이 「범위 밖」으로 미뤄 온 항목**이다. 이번엔 이것이 본 범위다.

### 왜 회사에 중요한가 (한 줄)

손익이 틀리면 **어느 채널에 돈을 더 쓸지 판단이 틀린다.** 지금 오픽스는 흑자일 가능성이
큰데 화면은 대형 적자로 보여 준다 — 그 상태로 광고·재고 결정을 하면 그 결정이 틀린다.

### 착수 게이트 (Jino가 2026-08-20에 세운 규율 — 반드시 밟아라)

Jino 원문(2026-08-20 오전): *"우리의 목적을 명확히 하고 너가 무슨 새로운 일을 시작하기 전에
이 목표에 맞는지 판단하고 진행해. 그리고 마지막에 이 목표가 완성되었는지 QA를 통해서
판단하고 마무리 하자"*

그래서 순서는 이렇다:
1. 위 ①②③ 목적을 **원문으로** 제시하고, 이 작업이 거기 맞는지 **「적합/부적합」을 판정해 밝힌다**
2. 계약 1장(§1)을 보여 **Jino 승인 1회**
3. 앵커에 목표·안 함·합격을 원문 복사 → 착수
4. 끝에 **별도 QA 기**(읽기 전용)가 합격기준 대조로 판정 → 앵커·HANDOFF·트랙 3곳에 원문 기록

### 세션 시작 필독 (전역 §6)

최신 HANDOFF(이 파일) → `claude-progress.txt` → `docs/TRACKS.md` → `docs/tracks/active/track_coupang-rocket-1p.md`
→ `git log --oneline -10` → `docs/wiki/WISDOM.md`.
활성 트랙이 둘 이상이면(PAO 네이버 / 쿠팡 손익 정합) **어느 트랙인지 먼저 묻는다** —
이 작업은 **쿠팡 손익 정합**이다. **PAO(네이버) 진도는 이 작업으로 안 움직인다.**

### 표류 신호 (이게 보이면 멈추고 Jino에게 물어라)

- RG 매출 계산식을 **새로 만들고 있다** → 재사용이 계약이다(세 번째 엔진 금지)
- 콘솔 GMV와 우리 원장을 **합치거나 고르려 하고 있다** → 종합 조망과 같은 것(우리 원장)으로 맞추고
  차이는 기록만. 새 원장을 고르면 두 화면이 또 갈라진다
- 종합 조망 화면을 고치고 있다 → 「안 하는 것」이다
- 로켓1P 매출 축 구조를 건드리고 있다 → 「안 하는 것」이다
- 「이왕 보인 김에」 네이버·자사몰까지 손대고 있다 → 앵커 `## 이월`에 적고 계속

---

## 0. 받는 세션이 할 첫 일

아래 §1 계약을 **원문 그대로** 앵커(`.claude/anchors/<session_id>.md`)의 `목표:` / `안 함:` / `합격:`에
옮긴다. **목표를 새로 발명하지 마라**(전역 §1). Jino 승인은 이미 받을 준비가 된 상태이나
**아직 승인 전이다** — 착수 전에 이 계약 1장을 보여 승인 1회를 받아라.

---

## 1. 계약 초안 (Jino 승인 대기)

**목표**: 대시보드가 쿠팡 3채널(3P·로켓그로스·로켓배송)을 **제 이름으로, 매출 빠짐 없이**
보여준다 — 트랙 「쿠팡 손익 정합」의 착수 관측(*"대시보드가 회사 매출의 34.5%만 보여준다"*)
중 **RG 몫을 닫는 것**이고, `track_coupang-rocket-1p.md`의 **합격기준①**이다.

**하는 것**
1. 대시보드 손익 엔진이 **RG 매출**을 읽는다. 원료 `coupang_rg_order_item`, 계산식은
   `intelligence.py`의 것을 **재사용**한다(새로 만들지 않는다 — 만들면 세 번째 엔진이 생긴다).
2. 채널 라벨 분리: `쿠팡 로켓그로스·윙` → **`쿠팡 3P(판매자배송)`** / **`쿠팡 로켓그로스`** /
   `쿠팡 로켓배송(1P)`.
3. RG 행에 **원가 커버리지**를 표시한다(2026-08-20 D-22가 만든 「⚠️ 원가 미상 N%」 재사용).

**안 하는 것**: 종합 조망(command-center) 화면 · RG 수수료 계산 규칙 변경 · 로켓1P 매출 축 구조 ·
네이버/자사몰 · 새 데이터 수집(원료는 이미 있다) · 두 RG 원장의 12% 차이 해소(조사만, 별건)

**판단기준**
- 항상 **기존 계산식을 재사용**한다. 왜냐하면 이 결함 자체가 「같은 숫자를 두 엔진이 따로 낸다」이고,
  세 번째 엔진을 만들면 문제를 키운다.
- 항상 **종합 조망과 같은 값**을 낸다. 갈라지면 어느 화면을 믿을지 모르게 된다.
- 모르는 것은 0으로 채우지 않는다(원가 못 붙은 RG 매출은 커버리지로 자백).

**금지선**: RG 매출을 `orders`에 밀어 넣지 않는다(RG는 주문 원장이 없다 — 평행 엔진 구조 유지) ·
콘솔 GMV와 우리 원장을 **더하지 않는다**(이중계상) · 추정 원가 금지.

**합격기준 (라이브, 30일 창 2026-07-21~08-19 기준)**
- ⓐ 오픽스 로켓그로스 행이 **매출과 함께** 뜬다(현재 매출 178,200원 = 3P만)
- ⓑ 그 행 순이익이 더 이상 「매출 178,200 / 순이익 −4,782,379」가 아니다
- ⓒ 채널이 3줄로 분리돼 각자 이름으로 뜬다(3P / 로켓그로스 / 로켓배송)
- ⓓ 대시보드 RG 매출 == 종합 조망(`/api/overview/command-center`)의 `revenue_rg` (같은 창)
- ⓔ **오하이테크에는 로켓그로스 행이 안 뜬다** (Jino 확인: 판매 없음)
- ⓕ 네이버·자사몰·로켓1P 숫자는 **불변**(회귀 없음)

**예산**: 반나절. 적대 리뷰 1회 + 완료 QA 1회 포함.

**끝나고 반드시 남길 것**(전역 §2 — 판정은 세 곳)
- 앵커에 `판정: <5값> — <한 줄 근거> (KST 시각)`
- HANDOFF 「완료 QA」 절에 판정 **원문 그대로**(미달·판정불능도 그대로)
- **트랙 파일 두 곳**: `track_coupang-rocket-1p.md`의 D-N 추가 + **미결 이월 ①을 닫거나**
  **못 닫았으면 왜인지** 갱신, 그리고 `docs/TRACKS.md`의 「쿠팡 손익 정합」 항목에 한 덩어리.
  ★D-N 번호는 그 트랙 자체 번호계(현재 최대 **D-22**)다 — `scripts/next_ids.sh`는 D-NAO·교훈만
  보고 **이 트랙 D-N은 안 본다**. `origin/main`을 fetch해 직접 최댓값을 확인할 것.

---

## 2. 왜 이 일이 필요한가 (진단 — 이미 실측 끝냈다)

**대시보드와 종합 조망이 서로 다른 엔진을 쓴다.**

```
종합 조망  → services/coupang/intelligence.py     → RG 매출 O · 채널명 분리 O
대시보드   → services/profit_calculator.py        → orders 테이블만 읽음
```

`orders`에 RG·로켓1P는 **행이 없다**(실측). 그래서 대시보드는 그 매출을 못 본다.
**그런데 RG 정산 수수료는 차감한다** → 오픽스가 매출 17만원에 순이익 −478만원으로 보인다.

★**2026-08-20 오전 D-22에서 고친 것과 같은 형태다** — KPI 카드와 요약표가 다른 경로를 써서
갈라지던 것. 그때 두 화면을 한 경로(`_channel_rows`)로 모았고, 남은 것이 그 경로가 RG를
안 보는 것이다. 교훈 #339와 같은 계열.

---

## 3. 이미 재놓은 실측 (다시 재지 마라 — 단 숫자는 창이 바뀌면 달라진다)

### 3-1. `orders` 원장 30일(2026-07-21~08-19) — prod 직접 조회
```
channel_id 1  COUPANG_WING1  오픽스        9건    178,200
channel_id 2  COUPANG_WING2  오하이테크     60건  1,069,500
channel_id 6  NAVER                     3,097건 48,909,110
channel_id 7  CAFE24                      179건  3,631,900
채널 3(RG1)·4(RG2)·5(ROCKET) → 행 0        ← 여기가 원인
```

### 3-2. RG 매출 원료는 **이미 수집돼 있다** — `coupang_rg_order_item`
```
account_key    건수   기간                 GMV
COUPANG_WING1  2,709  2026-05-05~08-19    45,524,870
COUPANG_WING2     12  2026-05-14~08-14       174,320
30일 창: WING1 373건/377개 6,419,160원 · WING2 4건/4개 54,800원
```
계산식: `SUM(unit_sales_price * sales_quantity)` — `intelligence.py:184`가 이미 이렇게 쓴다.

### 3-3. ⚠️ RG 매출 원장이 둘이고 **12% 다르다**
| 원장 | 30일 오픽스 RG |
|---|---|
| `coupang_rg_order_item` (우리 수집, 종합 조망이 쓰는 것) | **6,419,160원** (377개) |
| `coupang_vendor_summary_daily` RFM (쿠팡 콘솔 GMV) | 5,729,000원 (343개) |

**전자로 맞춘다**(종합 조망과 일치시키는 게 ⓓ). 차이 원인 미상(취소·반품 반영 시점 / `paid_at`
vs `summary_date` 귀속 / 콘솔 집계 지연 후보) — **추정하지 말고 조사 대상으로만 남겨라.**

### 3-4. 올바른 채널 이름표가 **이미 코드에 있다**
```python
# backend/app/routers/coupang_ops.py:474
"COUPANG_RG1": ("오픽스",    "로켓그로스")
"COUPANG_RG2": ("오하이테크", "로켓그로스")
```
대시보드가 쓰는 쪽은 뭉뚱그린다:
```python
# backend/app/services/profit_calculator.py:1587
else:  # 윙 / 로켓그로스
    seg, has_profit = "쿠팡 로켓그로스·윙", True    # ← WING(3P) + RG를 한 라벨로
```

### 3-5. 종합 조망은 맞게 보인다 (라이브 확인)
`GET /api/overview/command-center` → `revenue_rg: 712,440` / `revenue_3p: 148,900` (7일 창)

---

## 4. 오늘 내가 빠진 함정 두 개 — 같은 데 빠지지 마라

1. **두 원장을 나란히 놓고 모순이라 불렀다.** 대시보드 매출은 `orders` 기반이고
   `vendor_summary`는 쿠팡 콘솔 GMV다. 「오하이테크 1,022,000 vs 876,100 불일치」라고 했는데
   실제로는 `orders` 1,069,500 − 취소 57,500 + 배송비 = 1,022,000으로 설명된다. **정상이었다.**
2. **leaf 라벨에 속았다.** 「쿠팡 로켓그로스·윙」 한 줄이 WING+RG를 묶어 놓아서, RG가 없는
   오하이테크 행을 「오하이테크 RG 행」이라 불렀다. **Jino가 잡아줬다**(원문: *"오하이테크는
   RG 판매가 없어. 판매자배송 상품인 3P 매출만 있어."*). 이게 계약 §2(라벨 분리)의 직접 동기다.

---

## 5. 트랙 목표 대비 미달 현황 (정정본)

트랙 `docs/tracks/active/track_coupang-rocket-1p.md` — 상태 `🟢 Active (4/6)`.
목표 원문: *"…종합조망에 편입 — 3P/RG와 나란히 매출·순이익을 본다."*

| # | 미달 | 상태 |
|---|---|---|
| ① | 오픽스 RG 매출이 대시보드 손익에 없다 | **이 계약이 다룬다** |
| ② | 로켓1P만 매출 축이 둘이라 회사 전체 부호가 뒤집힌다(30일 −1,926만 ↔ +1,175만) | 축 선택 UI는 **이미 있다**(D-15). 「경고를 띄울까」는 미결 — 급하지 않음 |
| ③ | 회계 정본(계산서) 축에서 1P 순이익이 하한만 나온다 | 설계상 그렇다(원가 축 미도달). S5/S6 몫 |
| ④ | 원가 커버리지 % | **이미 화면에 있다**(판매 축 배지 `원가 커버리지 100.0%`) — 내가 틀렸다 |
| ⑤ | 채널 이름이 3P와 RG를 묶는다 | **이 계약이 다룬다** |

★②④는 2026-08-20에 내가 「미달」이라 보고했다가 **Jino가 스크린샷으로 반증**했다. 트랙
체크리스트만 보고 화면을 안 본 탓이다 — **다음 세션은 화면부터 볼 것.**

---

## 6. 손대면 안 되는 것 / 주의

- **공유 main 폴더에서 작업하지 마라.** 병행 세션이 상시 커밋 중이다(오늘 `CLAUDE.md` 수정 중).
  `git worktree add ~/.claude-worktrees/Ohiselling/<이름> -b <브랜치> origin/main`.
- **적대 리뷰를 위임하면 그 워크트리는 리뷰어 것이다.** 같은 트리에서 내 테스트를 돌렸다가
  리뷰어의 변이를 읽어 실패 25건+ 오염이 났다(오늘 2회). 검증은 `git worktree add --detach`로 격리.
- **GitHub Actions가 죽어 있다** — 잡이 `steps: 0`으로 즉사(계정 수준). PR 병합은
  `scripts/safe_merge.sh <PR> --force`가 필요하고, **그 실행은 Claude 자동 승인 정책에 막힌다**
  → Jino가 직접 돌려야 한다. CI 대신 로컬 전체 스위트로 검증할 것.
- **기존 실패 2건은 내 것이 아니다**: `test_health_partial_sync::test_health_route_actually_returns_partial_sync`,
  `test_vendor_item_axis::test_health_route_actually_returns_conservation` — `origin/main`에도 재현된다.
- prod 조회는 읽기 전용, `ssh sellc.ohitech.co.kr 'bash -s' < 파일`(인라인 heredoc은 따옴표가 벗겨진다).

## 7. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_dashboard-rg-revenue-wiring_20260820.md 읽고 이어서 작업해줘
```
