# 세션 인수인계: 외부 요인 → API 표면 전부 영향 지도 (ref 62) + 단말 출시일 확보

> 저장일시: 2026-08-17 17:3x KST
> 트랙: **PAO(파오)** — `docs/tracks/active/track_naver-ad-optimization.md`
> 결정: **D-NAO-183** · 커밋 **`6d161ed9`**(origin/main 반영 완료) · 배포 없음(문서·데이터만)
> 같은 날 앞선 HANDOFF 3건이 있다 — 이 파일은 **그 뒤 네 번째 작업**만 다룬다:
> ① `HANDOFF_shopping-exclusion-read-and-write_20260817.md` ② `HANDOFF_shopping-exclusion-rollback_20260817.md`
> ③ `HANDOFF_band-analysis-design-and-limits_20260817.md` ← **이 세션이 이어받은 문서**

## 1. 한 줄

Jino 지시(16:23·16:24)로 다음 세션 순서를 재배열해 **「외부 요인 → 우리가 받아올 수 있는 모든 API 정보」 영향 지도**를 먼저 만들었다(ref 62, Fable 작성). 부수로 **갤럭시·아이패드 한국 출시일을 웹 조사로 확보**했고(56 레코드), **인계 문서의 창 숫자 2건이 틀렸음을 라이브 실측으로 잡았다.** 완료 QA **달성**.

## 2. ★★새 세션이 할 일

### 확정된 것 (다시 묻지 말 것 — D-NAO-183)
- **「휴가 시즌」 = 고정 날짜 안**: **7/20 ~ 8/15**(절정 7/25~8/10). 겨울 휴가는 설 연휴 행과 겹쳐 **별도 행 없음**. 창은 튜닝 가능한 값이다.
  - ★**「매출 역산」안은 기각됐다** — 스마트스토어 주문이 2026-02-12부터(실질 3월~)라 **2025년 여름을 원리적으로 못 잡는다**(n=1). **재제안 금지.**
- **단말 출시일 = 웹 조사로 확보 완료** → `backend/app/data/device_launch_dates_kr.json`. 구체 날짜는 **이 파일이 정본**이다 — 기억으로 적지 말 것.
- 이전 세션 확정분(그대로 유효): 4구간 = ①BEP+20%↑ ②BEP~+20% ③BEP 이하 ④판정불가 · BEP RoAS **1.711** · ⑧ 범위 ⓑ안 · 수집 승인(재산정 **약 2,200콜**).

### ★순서 (앞 세션 HANDOFF §2의 순서에서 1번이 끝난 상태)

1. ~~[Fable] 외부 요인 → API 표면 전부 영향 지도~~ → ✅**완료(ref 62). 완료 QA 달성.**
2. **★흑자/적자 패턴 분해 — 다음 세션 1순위.** 그룹×주 단위 밴딩에 **요일·공휴일·연휴·휴가시즌(7/20~8/15)·출시일 4구간** 라벨을 붙여 「외부 요인을 제거하고도 남는 흑자/적자」를 뽑는다. 그 위에 대행사 조치(스냅샷 27일 + 제외 축 2년치)를 얹는다.
   - ★**ref 62 §4를 먼저 읽어라** — 오귀속을 막는 교란 목록(X0~X8)과 판별 방법이 거기 있다. 특히 **X0(Z폴드8 3중첩)**·**X5(출시×계절 준-공선)**.
   - **API 수집 없이 지금 데이터로 시작 가능**(ref 62 §5-1 — 콜 0 목록).
3. **ref 59 본문 갱신** — 실측이 강제하는 변경 4건: ①창을 축별로 분리 ②B3(입찰 추정) 축 제외 ③계절성을 외부 검색량으로 재는 경로 폐기 ④**창 숫자 2건 정정**(아래 §4).
4. **D-NAO-182 `col7/8/9` 적재** — 별개 트랙(매일 쌓이므로 병행 가능). **중형 작업이라 계약 1장 선행.** 소실 시한은 2027-02-13경(180일 소급)이라 급하지 않다.
5. B-2 밴드 판정기 → B-3 수집(~2,200콜) → B-4 경향 → B-5 산출물 ref 60.

### ⚠️ Jino 결정이 남은 것 (ref 62 §8-2)
1. **`/keywordstool`·`/estimate` 주기 적재 신설 여부** — ★소급이 원리적으로 불가하므로 **적재 시작 시점이 곧 관측 창의 시작**이다. 다음 단말 발표·출시를 이 축으로 보려면 그 전에 켜야 한다. 콜 비용·저장 설계는 **별도 계약 사안**.
2. **스마트스토어 주문을 「광고 밖 수요」 대조축으로 넣을지** — 넣으면 「광고 성과 변화 vs 수요 자체 변화」 분리가 가능하나 창이 **5.5개월**뿐.
3. (앞 세션 잔여) GitHub Actions 결제 정지 — CI가 안 돌아 빨강도 초록도 코드 신호가 아니다.

앵커: `.claude/anchors/c3227e00-8261-4b51-9b36-5448620d1ce0.md` (합격기준 M-1 원문 + 판정 + 이월 8건).

## 3. 완료 QA

> **별도 Sonnet(읽기 전용), 2026-08-17 17:03 KST. 종합 판정 = 「달성」 · 미달 0건.**
> 판정 원문(축별):
> - **M-1a 산출물 실재·규격 = 달성** — 행 10개가 요구된 7개 요인 범주 전부 포함(출시 축은 3→4 세분, 근거 명시). 열 14개가 ref 57 §5 「안 부르는 API」 목록과 ref 58 §1 라이브 호출 결과에 전건 매핑. 유일한 미해결 `/ncc/label-refs`는 §8-3에 「새 표면 발견 시 열을 늘린다」로 **명시 자백**(누락 은폐 아님).
> - **M-1b 칸의 4요소 = 달성** — 표본 뜬 모든 행에서 4열이 채워짐. ③열은 예외 없이 「지금 즉시(콜 0)」/「수집 후 180일」/「과거 원리적 불가」 중 하나로 창 제약 명시. ④열은 전부 「후보」·「검증 후」 조건부.
> - **M-1c 「미상」 규율 = 달성** ★이 판정의 핵심축 — awk로 전 셀 스캔, **신뢰도 라벨 누락 셀 0건**. `[미상]` 태그 셀은 예외 없이 「왜 미상인지」만 적고 방향·크기 **공란**. 지어낸 문장으로 메운 사례 **발견 못 함**.
> - **M-1d 라벨링 = 달성** — 요인→값 영향 칸에 `[실측]` **0건**(문서가 스스로 0이라 명시한 것과 실제가 일치). `[실측(구조)]`는 스펙·코드·DB 근거 칸에만. 격자 집계 140 = 85+9+20+26 **산술 일치**.
> - **M-1e 숫자 정확성 = 달성** — prod DB **6개 테이블 직접 재실측**(2026-08-17 16:59 KST): `naver_ad_daily` SHOPPING **391/391일**·68,143행 전부 광고그룹 레벨 · `naver_entity_snapshot` **27일**·28,602행 · `naver_search_term_daily(shopping)` **44일** · `console_excluded_at` **3,986/3,990**(2024-08-14 09:35:54~2026-08-11 22:26:00) · `qi_grade` **91,172 전건 4** · `optimizer` **'none' 7/7**. 지도 인용값과 **예외 없이 일치**, 구판 "381"은 문서 내 **0건**.
> - **M-1f 날짜 출처 = 달성** — JSON 56 레코드 **전부** `source_url`·`source_tier` 보유. 미확인은 `launch_kr: null`로 **명시 보존**(삭제 아님). **WebSearch 독립 대조**로 Z폴드8(7/28~8/3, 8/7)·S26(2/27~3/5, 3/11) 정확 일치. `iphone_launch_dates.json`은 과거 커밋 기존 파일 — 이번 세션이 지어낸 값 아님.
> - **M-1g 금지선 = 달성** — 변경 3건 전부 md·json, **backend 코드·alembic diff 0**(col7/8/9 미착수 확인) · `optimizer='none'` 7/7 라이브 · 광고 운영 지시 문구 **0건**(§6 레버 표는 전부 「후보」·「검증 후」).
> - **B-2~B-5 = 판정 대상 외**(미달 아님) — 앵커 `이번 세션:` 줄이 명시적으로 제외한 범위라 **이월**.

## 4. ★실측 결과 — 인계 숫자 검증 (유효 14 / 유령 0 / 부분오류 2 / 확인불가 0)

별도 Sonnet이 인계 문서·ref 61의 주장 16건을 prod 라이브로 재측정했다(2026-08-17 16:31 KST, 읽기 전용).

### 부분오류 2건 — 올바른 값

| 항목 | 적혀 있던 값 | **실측값** |
|---|---|---|
| `naver_ad_daily` SHOPPING 광고그룹 grain | 381/381일 | **391/391일 온전**(2025-07-22~2026-08-16, 결측 0) |
| 타겟팅 설정 스냅샷(`naver_entity_snapshot`) | 25일 | **27일**(2026-07-22~2026-08-17) |

★**「25일」은 단순 오기가 아니다** — QA가 출처를 추적하니 `naver_agency_op`가 **op_date distinct 19건·달력폭 25일**이다. 즉 ref 59 line 478은 **스냅샷 시작일(07-22)에 다른 테이블의 일수(25)를 붙인 축 혼선**이다. 같은 문서 line 383은 **27로 맞다.** → ref 59 갱신 시 **두 테이블을 갈라서** 정정할 것.

### 재확인되어 그대로 쓸 수 있는 것
- `holidays==0.98`이 `requirements.txt:12`에 실재하고 `diary.py:54`가 `holidays.SouthKorea()`로 **실사용 중** · `sales_velocity_estimator.py`도 동일 → **공휴일·연휴 라벨은 즉시·비용 0** 확정
- 갤럭시·아이패드 출시일이 코드·DB 어디에도 **0건**(반증 시도 실패) → 웹 조사가 유일 경로였음 확정
- `CRITERION` 문자열이 백엔드 `.py` 전체에 **0건** → 수집 로직 **신규 배선 필요** 확정
- `naver_bid_estimate_daily` **4일**(2026-07-27~30, 6,590행) → B3 축 제외 유지
- 원장 excluded 3,986 = SHOPPING/alive **3,880** + WEB_SITE/alive **106**(미매칭 0)
- 스마트스토어 `orders(channel_id=6)` 월별: 02월 **4행**·03월 1,017·04월 1,779·05월 1,900·06월 2,962·**07월 2,839**·08월(부분) 1,787
- ★**`naver_ad_daily` SHOPPING 68,143행이 전부 광고그룹 레벨, 키워드 레벨 0건** — 쇼핑의 검색어 grain은 `naver_search_term_daily(source='shopping')` **44일**이 유일하다

## 5. ★ref 62 지도의 내용 — 다음 세션이 알아야 할 것

`docs/references/62_external_factor_impact_map_20260817.md` (403줄, Fable 작성)

### 격자 규모와 채움 분포 (★이 숫자가 문서의 정직성 지표다)
행 10(출시 축 4구간 세분 포함) × 열 14 = **140칸**
**요인→값 영향으로 실측된 칸 0** / 강한 가설 9 / 약한 가설 20 / **미상 26** / 구조 판정(무영향·파생·관측불가 — 실측 수준) 85

### 발견 3건
1. **★X0 — Z폴드8 적자 −578,822원은 3중첩 구간의 숫자다**: 사전예약 2026-07-28~08-03 · 출시 **2026-08-07**(오늘 D+10) × **휴가 시즌 창 7/20~8/15가 통째로 덮음** × 대행사 조치 성패 판정. 지도는 **가르는 방법 4개만 적고 결론을 내지 않았다.** D-NAO-182 「외부 맥락 없이는 오귀속된다」의 실물 1호.
2. **★★X5 — 우리 주력 단말의 출시가 계절과 준-공선이다**: 아이폰=**매년 9월**(9/22·9/20·9/19 3년 실측) · Z 시리즈=**3년 연속 7월 중순~8월 초**(폴드6 사전예약 7/12·폴드7 출시 7/25·폴드8 출시 8/7). 분석 창 안 출시 4회 중 **「계절 없는 출시」는 S26(2026-03-11) 단 1건** → **출시 효과의 순수 관측이 사실상 1표본에 걸려 있다. 데이터를 더 모아도 해소되지 않는 구조적 제약이다.**
3. **설정 표면 7열은 외부 요인이 직접 못 움직인다**(구조 판정): 값이 변하는 유일한 경로가 쓰기 액션이고 `autobidStrategy`는 표본 12/12 꺼져 있다. → 거기서 요인은 「값의 변화」가 아니라 **「운영자 행동의 시점 분포」**로만 나타난다. 예: 제외 등록 epoch 3,880건의 월내 분포는 **지금 즉시 측정 가능**하고 그게 ⑧(행동 역공학)의 재료다.

### 분석 창(2025-07-22~2026-08-16) 안 출시 이벤트 4회
| 이벤트 | 사전예약 | 출시(KR) | 등급 | 제약 |
|---|---|---|---|---|
| Z폴드7·플립7 | 2025-07-15 | 2025-07-25 | press | ⚠️출시가 창 시작 3일 뒤 — **출시 전 구간이 창 밖** |
| 아이폰 17 | (없음) | 2025-09-19 | 기존 JSON | 출시 전은 D0−28 편의 정의로만 |
| **갤럭시 S26** | 2026-02-27 | **2026-03-11** | official | ★4구간 전부 창 내 · **유일한 비여름 출시**(X5 판별 열쇠) |
| Z폴드8·플립8 | 2026-07-28 | **2026-08-07** | official | 4구간 창 내, 단 오늘 D+10이라 출시직후 창 진행 중 |

### ★출시 축은 3구간이 아니라 4구간이다
사전예약일이 데이터에 실재하고(Z폴드8 7/28·S26 2/27·Z폴드7 7/15·Z폴드6 7/12·S25 1/24·S24 1/19) 사전예약~출시 간격이 **7~12일**로 짧다 → **「발표 → 사전예약 → 출시 → 성숙기」**. 「액세서리 수요는 출시일이 아니라 **사전예약일**에 먼저 뛴다」(F1b)를 독립 가설로 등록했고 검증 표본 2회(S26·Z폴드8)가 창 안에 있다.

### 검증 가능성 4분류 (§5)
| 분류 | 내용 |
|---|---|
| **지금 즉시, 콜 0** | 주말·공휴일·연휴·휴가철·월초말·요일 × 성과(391일 온전) · 출시 4회 × 성과 · **제외 epoch 월내 분포**(3,986건) · Z폴드8 예산소진 n=1 |
| **수집 후**(승인 2,200콜 내) | 시간대·지역·매체(**정확히 180일**, 2026-02-18~ · 2026 여름 온전 포함) · 연령·성별·관심사(**365일+**, 코드 배선 0건이라 신규 필요) |
| **지금 시작해야 미래분만** | `/keywordstool` 검색량·`compIdx` · `/estimate` — **소급 원리적 불가** |
| **원리적 불가** | 아이폰 효과 vs 9월 계절성 · Z 시리즈 vs 휴가철 단독 분리(준-공선) · 2026-02-18 이전 쇼핑 시간대·지역 · 2026-07-22 이전 입찰·예산 변경 이력 · 파워링크 시간대·지역·전환 |

### WISDOM 승격 후보 8건 — ★3건은 즉시 등재 가능
- **W5** 「keywordstool 검색량은 계절성 소급 측정에 쓸 수 없다」 — 스펙 확정, 관측 대기 아니라 **등재 대기**
- **W6** 「설정 표면은 요인이 직접 못 움직인다 — 요인은 측정 표면에서, 설정 표면은 운영자 행동의 시점 분포로 읽는다」 — 구조 명제
- **W7** 「아이폰=9월 · Z 시리즈=휴가철 창 → 우리 주력 단말의 출시는 계절과 준-공선이라 **단독 귀속 금지**」 — 달력 사실
- 나머지 W1·W2·W3·W4·W8은 승격 조건(관측 기준)이 §7에 명시돼 있다.

### ★함정 — 캠페인명↔단말 조인은 완전하지 않다
갤럭시 **A54/A55/A56 = SKT 전용 「퀀텀4/5/6」**, **A16/A17 5G = LG U+ 전용 「버디4/5」**로 리브랜딩 출시된다. 우리 캠페인에 「퀀텀」 그룹과 **「버디필름」 그룹이 실재**하는데 버디필름이 버디4/5(=A16/A17)를 가리키는지는 **확인 안 됨**. 조인 커버리지를 「완전」으로 선언하지 말 것. (추가 함정: `TEST_A16-1744937657247` 류 타임스탬프 접미사 그룹의 모델 정규화 전처리 필요.)

## 5-b. 출시일 JSON

`backend/app/data/device_launch_dates_kr.json` — **56 레코드**. 필드: `model`·`announce_kr`·`preorder_kr`·`launch_kr`·`source_url`·`source_tier`·`status`·`note`.
등급: **official 21 / press 22 / unverified 13 / 미확인 8**. 아이폰은 **중복 수록하지 않았다**(기존 `iphone_launch_dates.json`이 정본).
못 찾은 것은 **삭제하지 않고 `launch_kr: null` + note로 명시 보존**(목록에서 사라지면 「조사 안 함」과 「없음」이 구분되지 않는다). `unresolved_models_in_campaigns` 배열에 캠페인 실재 미해결 모델을 따로 남겼다 — 갤럭시 S9~S21 · Z폴드/플립1·2 · 탭 구형 · 아이패드 7~9세대·에어4·5·프로3~5 · 노트9/10/20. **전부 분석 창 밖 구형이라 출시일 축에는 무해.**
⚠️**갤럭시 A73만 다르다** — 국내 정식 출시 여부가 **출처 간 상충**이라 `null` 처리했는데 **캠페인명에는 실재**한다(§8-1).

## 6. 이월 (앵커 `## 이월` 8건 원문)

- **B-2~B-5 미착수** — 이번 세션 범위 밖(판정 대상 외).
- **§2-2 흑자/적자 패턴 분해 미착수** — 다음 세션 1순위, 콜 0으로 시작 가능.
- **ref 59 창 숫자 2건 정정 필요**(§4 — `naver_agency_op`와 `naver_entity_snapshot`을 갈라서).
- ref 62 §8-2 Jino 결정 2건(검색량 주기 적재 · 스마트스토어 대조축).
- ref 62 §8-1 확인 방법이 있는 미상 10건: 버디필름=버디4/5 여부 · col8 지역·col9 매체 코드 사전(`criterion-dictionary` RL 5,354건·MEDIA_TARGET id 대조) · col7 기준 시간대(심야 골짜기 실측) · **CRITERION 열 미상 10칸(미상 이유가 「데이터 없음」이 아니라 「미수집」이다)** · 월초말·주말 방향(콜 0) · A73 국내 출시 · JSON unverified 21건 등급 승격(창 내 press는 Z폴드7 1건이 우선) · **달력 라벨(`is_kr_holiday`)을 판정 로직이 실제로 소비하는지**(코드 추적 소과제) · 품질지수 grade=4 원인(~50콜) · 네이버 쇼핑 프로모션 달력(마케팅 사이트 접근 차단).
- WISDOM 즉시 등재 가능 3건(W5·W6·W7).
- 앞 세션들의 이월은 HANDOFF ①②③ 참조(`search_term_judge`가 원장을 안 봄 · 콘솔 43건 `console_excluded_at` 1시간 오차 · 중복 키워드 시 집합 검증 사각 · R-d 표본 재현 규약).
- 기존 미결 유지: 품질지수 죽은 신호 원인 미상 · prod 프론트 미배포 · `.pm2/logs` 627MB 무로테이션 · GitHub Actions 결제 정지.

## 7. 상태·환경

- **main = `6d161ed9`**(origin/main 반영 완료 — push 후 `next_ids.sh`가 `origin/main=183` 인식 확인). 저장소는 **main에 세워 둔 상태**.
- **배포 없음** — 문서·데이터 파일만. prod 백엔드 `4d09d046` 그대로. **PAO 완전 정지**(`optimizer='none'` 7/7 라이브 확인).
- 다음 번호: **D-NAO-184 / 교훈 #302**(`scripts/next_ids.sh`).
- 이 세션은 **전량 읽기 전용**이었다 — 네이버 API 0콜(쓰기·읽기 모두), prod DB 조회만(`sqlite3 -readonly`), 코드·마이그레이션 변경 0.
- 사용한 서브에이전트 4기: Fable(지도 작성) · Sonnet(인계 숫자 실측) · Sonnet(출시일 웹 조사) · Sonnet(완료 QA).

## 8. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_external-factor-impact-map_20260817.md 읽고 §2 순서대로 진행해줘.
2번(흑자/적자 패턴 분해)부터. ref 62 §4(교란)와 §5-1(콜 0으로 지금 가능한 것)을 먼저 볼 것.
```
