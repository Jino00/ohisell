# HANDOFF — 스위치는 보이는 것보다 무겁다 (pao-논의 n=75)

- **세션**: `67c50dc0` · 2026-08-31 11:31 ~ 16:4x KST · 계정 jino.kim@ohitech.co.kr
- **체인**: `pao-논의` **n=75** (직전 n=74 `75bed13a` 정상 종결 11:2x)
- **목표이름**: 목적관철 목표
- **계약**: `docs/contracts/CONTRACT_pao_purpose_and_hands.md` (D-NAO-280 · Jino 승인 2026-08-31 09:24)
- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **워크트리**: `~/.claude-worktrees/ohisell/pao-n75` · 브랜치 `feat/pao-n75` · base `origin/main` `5b6157fd`
- **커밋**: `270736cd`(H1 본체) + `929af233`(리뷰 P2 처분·트랙) · **PR #603**

---

## §0. 작업 목적 — 원문 (다음 세션은 이걸 앵커에 그대로 옮긴다)

계약 §1 원문:

> "누가 뭘 못하나 — 네 겹: **엔진이 「나쁨」과 「알 수 없음」을 못 가른다** … **사람이 엔진을 만질 손이 없다** … **잘린 것을 되살리는 손이 통째로 없다** … **올릴 때 «얼마로»를 정하는 로직이 없었다**"

이번 세션 몫은 **P2(되돌림·킬스위치·발의 «손 묶음»)**였고, 그중 **H1 하나**를 했다. Jino 2026-08-31 15:31 「권고대로 가자」.

---

## §1. 한 것 — 계약 **9/38 → 9/38 (불변)** · 트랙 **2/7 불변**

### H1 — `auto_operate` 토글 (계약 §6 P2 첫째)

| 층 | 산출물 |
|---|---|
| 백엔드 | `PUT /campaign-settings/auto-operate` — **이 저장소 최초의 `auto_operate` 쓰기 API** |
| 백엔드 | `_serialize_settings`에 `auto_operate` 노출 (종전 응답에 **없었다**) |
| 백엔드 | `ignition_preflight` **W3(`reopen_due`) 신설** |
| 프론트 | `NaverAdScope.tsx` 엔진 켜기/끄기 + 켜기 확인창 |
| 프론트 | `api.ts` PUT 헬퍼 + GET preflight 헬퍼(백엔드엔 있었는데 **호출부 0건**이었다) |
| 테스트 | 백엔드 15 · 프론트 7 신규 |

### ★★ 설계 핵심 — 이 스위치는 optimizer 스위치보다 **무겁다**

optimizer 스위치는 *"우리 시스템 내부 설정이지 광고 API 쓰기가 아니다"*라 적을 수 있다 — harness가 `optimizer=='ours'`를 하드체크하므로(D-NAO-13, `naver_execution_harness.py:2564`).

**그런데 제외 재개방 레인은 그 harness를 안 탄다.** `_open_exclusion`(`search_term_ss_lane.py`) 독스트링이 스스로 *"harness.execute()를 안 거치고 naver_sa_writer를 직접 부르는 예외 경로"*라 밝힌다. 게이트는 셋뿐:
1. 일일 복귀 캡 `_SS_DAILY_RETURN_CAP`
2. `_auto_operate_now` ← **이 플래그**
3. `blocked_by_scope` — **스코프 0행이면 안 막는다**

⇒ **`optimizer='none'`이어도 이 플래그를 켜면 네이버에 실제 삭제가 나간다.**
그래서 **끄기는 즉시·켜기는 고지 후**로 비대칭 설계했다(킬스위치에 마찰을 두면 급할 때 못 끈다). 차단은 안 한다 — 전역 §1.

---

## §2. ★라이브가 근거문서를 뒤집었다 — 다음 세션이 반드시 알 것

### ① ref 117 §2의 「재개방 대상 사실상 0건」은 **틀렸다**

재심사 루프 `_run_reexamination`(`search_term_ss_lane.py:678`)의 후보 쿼리를 그대로 재현한 결과 (2026-08-31 15:4x KST prod 읽기 전용):

| id | 검색어 | `next_review_at` | `live_state` | `source` | 그 캠페인 `auto_operate` |
|---|---|---|---|---|---|
| **1** | 아이패드종이필름 | **2026-08-21 (10일 경과)** | `alive` | NULL(우리 실행분) | **0** (`optimizer='none'`) |
| 2 | 골프 | 2026-09-10 (미래) | `alive` | NULL | **settings 행 자체가 없음** |

**due = 1건.** ref 117은 *"2행은 이미 `alive`(걸려 있음)"*이라 대상 아님으로 판정했는데, 모델 주석 원문은 `live_state: alive(**라이브에 걸려 있음**)` — **지울 것이 있다 = 재개방의 전제조건**이다. 뜻이 반대로 읽혔다.

### ② 「probation 0행」의 기제 확정

`probation` 행수 **0** · `probation_until` NOT NULL **0**. 원인은 코드 결함이 아니다 — 후보로 **선택은 되지만** `_auto_operate_now`가 **행 부재·OFF 모두 False**(fail-closed)라 `continue`(복귀 정지·미실행 정직). id=2는 캠페인이 `naver_campaign_settings`에 **행이 아예 없어** 09-10 도래해도 같은 이유로 안 열린다.

⇒ 계약 P2 처분 ⓐ(**「재개방하려면 그 캠페인의 auto_operate를 켠다」= H1 버튼**)가 이 자물쇠의 열쇠임이 라이브로 확증. **P2 첫째가 넷째의 선행조건**.

### ③ ★`AD_BID_CANARY_CAMPAIGNS`가 **꺼진 캠페인**을 가리킨다

| | 캠페인 | 이름 | optimizer |
|---|---|---|---|
| 코드 상수 | `cmp-…010769985` | [P_삭제금지]맥세이프카드케이스_쇼검 | **`none`** (07-30 10:48 ours→none) |
| 라이브 점화분 | `cmp-…008425541` | 01. 갤럭시_지문방지_TPU | **`ours`** (08-29 12:53) |

`_ad_bid_canary`는 `AD_BID_ROUTING_ENABLED=True`라 allowlist 분기가 **도달 불가**여서 «라우팅»엔 영향 없다(계약 서술이 맞았다). 그러나 상수를 **직접 읽는** `delegation_gate.py:114`(Confirm-only 봉쇄)·`expert_briefing_builder.py:82`(브리핑 제외)는 그 우회를 안 탄다 ⇒ **둘 다 한 달 전 꺼진 캠페인에 걸려 있고 실제 도는 캠페인엔 안 걸려 있다.**
⇒ **계약 §6 P2 일곱째는 「이름·주석 위생」이 아니라 살아 있는 오조준**이다.

### ④ 그 밖의 실측 정정 (인계 📄 → 실측)

| 항목 | 인계 📄 | 실측 |
|---|---|---|
| `optimizer='ours'` | "복귀 완료" | **9개 중 1개뿐** (나머지 8 none/0) |
| 점화 후 실집행 | 0건 | **0건 확인** (`flight_pacing` 141건 **전건 dry_run=1**) |
| 우리 엔진 마지막 실쓰기 | — | **`update_bid` 2026-07-30 10:20** (32일 정지) |
| 죽은 카드 | 154 | **133** (bid_up 88·bid_up_explore 30·bid_down 15) |
| pending | 427 | **472** |
| `naver_change_log` 컬럼 | `change_type`·`actor` | **둘 다 없다 — `action`이다** (n=74 §5 레시피가 그대로면 죽는다) |

### ⑤ 엔진 정지 32일의 값 (Jino 질문 「목표에 맞게 가고 있나」의 답)

| 창 | 계정 전체 | 그중 `optimizer='ours'` |
|---|---:|---:|
| 07-30 ~ 08-31 (32일) | **20,437,520원** · 클릭 16,469 · 직접전환 1,950 · 전환액 30,664,160원 | **7,834,872원** |
| 점화(08-29) 이후 2일 | 957,688원 | 227,254원(점화 캠페인) |

**32일간 2,043만원 전부를 대행사가 집행**했고, 그중 **784만원은 이름상 「우리 관할」**인데 우리 엔진 실쓰기는 0이다. 직접전환 ROAS **1.50**(간접 미포함이라 하한).

---

## §3. 완료 QA — 판정 원문 그대로 (별도 Sonnet · 읽기 전용 · 16:27 KST)

> **판정(계약 §6 P2 7항목): 미달** — 7항목 중 6항목은 착수 0(코드 diff 없음), 1항목(auto_operate 토글)은 코드·격리 테스트는 실재·재현됐으나 라이브 증거가 없어 계약 자신의 §4-1 기준을 충족 못 하고 체크박스도 `[ ]`로 남아 있다. 계약 진행률 9/38로 이 세션 전후 **불변**.
>
> **판정(Jino 2026-08-31 15:31 지시 원문 「권고대로 가자」): 부분달성** — 방향(P2 착수)은 정확히 따랐고 계약 §6 P2 첫째 항목의 코드·테스트·PR·적대 리뷰까지 완주했다. 그러나 계약이 명시한 P2 예산(세션 3.5·토큰 210만·5일)에 비추면 이번 1세션은 그 초입이다. 권고문 자체가 1세션 완결을 요구하지 않았으므로 미달로 보지 않는다.
>
> **판정(북극성 5요소): 달성** — 이 대조가 요구하는 것은 "5요소가 모두 크게 전진했는가"가 아니라 "이번 슬라이스가 북극성 대비 정직하게 평가·기록됐는가"이며, 실측 결과 그 기록(M 귀속·5요소 이동·대칭 검사·관련 절 전독)이 라이브 사실과 어긋나지 않는다.
>
> **종합 판정: 부분달성** — 세 대조가 다른 값을 낸 이유: ①은 "P2가 끝났는가"를 묻고 ③은 "이번 슬라이스가 북극성에 비추어 정직하게 기록됐는가"를 묻는다. 세션은 "1항목만 했다"는 자기 주장에 정직했고(과장·은폐 없음), 코드·테스트는 독립 재현됐다. 다만 그 1항목조차 계약이 요구하는 "라이브 증거"는 아직 없다.

**QA가 확인 못 한 것**(원문): 적대 리뷰가 별도 기였는지의 독립 검증 · 변이 19종 각각의 사망 재현(읽기 전용 원칙상 미시도) · CI step별 로그 · pao-uiux 워크트리 내부 전체 대조 · `AD_BID_CANARY_CAMPAIGNS` 참조처 grep.
⇒ 마지막 항목은 **이 세션이 직접 실측했다**(QA가 못 닿았을 뿐 미검증 아님).

---

## §4. 적대 리뷰 — **PASS (P1 = 0)**

리뷰어 자체 변이 **5종 전건 사망**(저자 14종과 다른 것들), **표면 절단 2종** 포함: 경고 목록 `<ul>` 렌더 제거 · 「그래도 켠다」 onClick no-op화.

**P2 3건 트리아지**(라운드 안 늘림):
- **채택** W3 문구 「다음 08:50 레인**이**」 → 「레인**부터**」.
- **기각(기록만)** 응답에 `auto_operate` 두 값(최상위=켠 뒤 / preflight 안=켜기 전) — 검사는 「켜기 전 상태에 대한 판정」이 맞다.
- **기각** 조회-후-생성 레이스 — 기존 optimizer 스위치와 동형, 실패 모드가 500(조용한 오작동 아님).

★**캡을 W3에 반영하지 «않는» 것이 판단이다** — 반영하면 「오늘 안 열리니 없는 셈」으로 읽혀 **누락 방향으로** 틀린다. 과다는 안전하고 누락은 안 안전하다.

---

## §5. ★이 세션의 교훈 — 「테스트 통과」 ≠ 「구조가 옳다」

초판이 스위치를 캠페인 행 **`<button>` 안에** 넣어 중첩 버튼이 됐는데 **jsdom이 HTML 중첩 규칙을 강제하지 않아 27건이 전부 초록**이었고 vitest도 조용했다. 변이 주입도 이걸 못 잡았다 — **변이는 「내가 생각한 실패 모드」만 훑기 때문**이다. 잡은 것은 코드를 다시 읽은 구조 점검이었다.

수정: 형제 구조로 재배치 + `container.querySelectorAll("button button")` 개수를 세는 회귀 추가(그 변이를 되돌리면 실제로 죽는다).

**부수 적발(이월)**: optimizer 스위치의 `changed_at=kst_now()`를 **자기 테스트가 안 지킨다** — 그 줄을 지워도 `test_naver_optimizer_switch_router.py` 9건 전건 초록. 코드 주석이 *"같은 함정 세 번째"*라 적어 둔 자리인데 회귀 잠금이 없다.

---

## §6. 트랙 진행률

- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **시작 2/7 → 종료 2/7 (불변)** — 달성 M0·M1 / 미달 M2~M6
- **이번 세션이 움직인 항목**: **없음.** H1은 §6 표의 어느 M 합격 관측도 아니다(L3층 «사람 손»이자 **M4의 전제**).
- **트랙 목표 원문**: *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야."* (Jino 2026-07-19 · D-NAO-59)
- **북극성 대비(D-NAO-226·227)**: ①M 귀속 없음(M4의 전제) ②5요소 이동 **없음** — **④자동화 34세션째 0** ③§7 대칭: P2는 **되살리는 손**을 만드는 슬라이스라 비대칭을 줄이는 방향이고, H1은 켜기·끄기 양방향을 같이 냈다 ④§목차·§1·§4 간극3~5·§6+6-b·§7·§8 **전독**
- **다음 세션 후보**: 계약 **P2 잔여 6항목**(아래 §7 순서 권고).

---

## §7. 이월 — 다음 세션이 그대로 받는다

1. **P2 잔여 6항목**(1순위). 권고 순서와 이유:
   - ⓐ **재개방 dispatch**(P2 넷째) — 이제 due 1건이 실재하니 **「대상 0건」 분기가 아니다.** 단 계약이 **쇼핑 개방은 별도 PR + 적대 리뷰 2R + 「나머지 생존」 변이 의무**로 못 박아 뒀다(`PUT /ncc/targets`가 replace 의미론이라 그 그룹 제외 **전체 유실** 위험, 계정에 대행사분 3,880건이 백업 없이 있다).
   - ⓑ **`AD_BID_CANARY_CAMPAIGNS` 개명**(P2 일곱째) — §2-③의 오조준 때문에 «위생»이 아니라 실결함 수리다.
   - ⓒ 런타임 킬스위치 2종 · 발의 폼 · 복원 발의 · void 배선.
2. **H1의 라이브 검증** — 스위치를 실제로 켜는 것은 **§1 승인 지점**(네이버 실쓰기가 나간다). 켜면 due 1건 `아이패드종이필름`이 다음 08:50 레인에 열린다. **Jino 발의 없이 켜지 않는다.**
3. **optimizer 스위치의 `changed_at` 회귀 부재**(§5) — 소관: 이 계약(P2 밖).
4. **`naver_change_log`엔 `change_type`·`actor`가 없다**(`action`이다) — n=74 인계 §5 레시피가 그대로면 죽는다. 소관: 정본 §14 레시피.
5. **ref 117 §2 「재개방 대상 0건」 정정**(§2-①) — P1-e 정정거리에 합류. 소관: 이 계약.
6. **n=74 이월 6건 그대로 승계** — P1-e 미착수 · 계약 §0 논증 교체 · 유령 3좌표 · pending 136건 미지목 · `safe_merge.sh` 600초 부족 · 훅이 `$CWD`를 읽는 근본원인.

---

## §8. 실측 좌표 — 다음 세션이 재현할 명령

```
# prod 읽기 전용 (스크립트 → scp → stdin. 인라인 heredoc은 따옴표가 벗겨진다)
scp -q /tmp/q.py sellc.ohitech.co.kr:/tmp/q.py && \
  ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python - < /tmp/q.py"
# DB: sqlite3.connect("file:/home/ubuntu/ohisell/backend/ohisell.db?mode=ro", uri=True)
# ★change_log는 `action` 컬럼이다(`change_type`·`actor` 없음)

# 재개방 due 후보 — 레인 쿼리 그대로
SELECT id, campaign_id, search_term, next_review_at, source, live_state
FROM naver_search_term_exclusion
WHERE status='excluded' AND next_review_at IS NOT NULL AND next_review_at <= <today>;

# 체인 생존 판정 — ★반드시 origin/main으로 (공유 폴더 909커밋 stale)
git show "origin/main:.claude/memory/chains/pao-논의.jsonl" | tail -1

# 테스트 (이 워크트리엔 venv 없음 — 시스템 python3에 deps 있음)
cd backend && PYTHONPATH=. python3 -m pytest -q -p no:warnings          # 전건 7,406 (약 5m40s)
cd frontend && npx vitest run src/pages/paoScopeReachesTheUser.test.tsx  # 28건
# ★frontend/node_modules 없음 — pao-n68 것을 심링크해 썼다(의존성 명세 동일 확인 후)
```

**살아 있는 다른 세션**(2026-08-31 16:3x, origin/main 실측): `sellc-원가-메뉴` n=2 — 다른 소관, **무접촉 유지**.

---

## §9. 착지 — 완주

커밋 4개(`270736cd` H1 본체 · `929af233` 리뷰 P2 처분·트랙 · QA 판정·HANDOFF · `0f99ba5a` origin/main 병합)
→ push → PR **#603** → 적대 리뷰 **PASS(P1=0)** → **CI 3/3 실통과**(py3.10 9m27s · py3.14 9m40s · frontend 1m42s)
→ 머지 **`3ba3e64c`** · `--force` **미사용**.

- **멈춘 단계: 없음.**
- ★**L4 갱신을 머지 «전»에 했다** — `origin/main`이 12커밋 앞서 있어 병합 후 전건을 다시 돌렸다
  (**7,406 → 7,409 passed**, 실패 0). 직전 세션이 이 순서를 틀려 CI 3건이 깨진 전례가 있다.
- ★`safe_merge.sh` 기본 600초가 **세 번째로** 모자랐다 — 처음부터 `MERGE_WAIT=900`으로 갔다.
  n=73·n=74에 이어 3연속 실측이므로 **기본값 자체가 이 저장소 backend CI에 안 맞는다**(소관: 하네스).
- **L5로 「main에 세워둔다」 생략** — 로컬 `main`이 공유 메인 폴더에 체크아웃돼 있다.
  다음 세션은 `git switch -c <새> origin/main`으로 갈라야 한다.
- 정정 경로: `git revert -m 1 3ba3e64c` · PR close. **force-push 금지**.
- **prod 배포 0 · 광고계정 쓰기 0 · 스위치 미점화** — 배포는 다음 세션 몫이 아니라 «켜기 결정» 뒤의 일이다.

