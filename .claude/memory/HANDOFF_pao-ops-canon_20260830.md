# 세션 인수인계: PAO 운영 정본 + 좌표 자동 검사 (체인 `pao-논의` n=72, 세션 d61698ea)
> 저장일시: 2026-08-30 18:4x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## 0. 이 인계의 중심 — 먼저 읽을 것

n=71이 넘긴 문장은 **기술이 아니라 신뢰**였다: Jino 원문 *"너가 이렇게 오탐을 많이 하니까 너가 하는 말을 이제는 믿을수가 없네"*. 그 처방으로 0순위에 올려 둔 것이 **`docs/PAO_OPS.md` 전수 재검증**이었고, Jino가 *"먼저 이걸 완료하는게 맞겠지"*(17:03)로 열어 줘 이번 세션이 그걸 했다.

★**이번 세션의 값은 「문서를 고쳤다」가 아니라 「문서가 낡으면 기계가 말하게 만들었다」**이다. 그리고 그 기계에도 구멍이 있었고(적대 리뷰 P1), 그 구멍이 **문서가 낡는 걸 막으려 만든 도구가 스스로 침묵하는** 모양이었다.

★★**Jino가 사각지대를 찔렀다** (17:1x): *"광고 성과 지표는 안봐? 광고가 잘 돌아가는지 아닌지를 판단해야하잖아?"* — **맞았다.** 옛 `PAO_OPS.md` 13개 절도, 내가 새로 만든 주기 체크리스트도 **「기계가 도나」에 치우쳐 「돈을 버나」를 정면으로 안 물었다.** 둘 다 같은 원료(옛 문서)를 썼기에 같이 놓쳤다. 원인은 내 위임문이 틀을 *「목적에서 안 새는가」*로 잡은 것이다.

## 1. 프로젝트 위치 및 환경
- 로컬 경로: `/Users/jino/.claude-worktrees/ohiselling/pao-n72` (브랜치 `feat/pao-n72`, base `origin/main` `462b4180`)
- 공유 메인 폴더: `~/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` — ⚠️**origin/main보다 860커밋 stale**(훅이 여기를 읽어 오탐 2종 발생, §5 참조)
- 검사기 실행: `python3 scripts/check_pao_canon.py` (repo 루트, 읽기 전용, 표준 라이브러리만)
- 테스트: `cd backend && python3 -m pytest tests/test_pao_canon_check.py -q`
- prod 조회(읽기 전용): `scp /tmp/q.py sellc.ohitech.co.kr:/tmp/q.py` → `ssh -o BatchMode=yes sellc.ohitech.co.kr "cd /home/ubuntu/ohisell/backend && .venv/bin/python - < /tmp/q.py"` · 스크립트 첫 줄 `load_dotenv("/home/ubuntu/ohisell/backend/.env")` · **서버는 UTC**
- 주요 환경변수: 값 없음(이 세션은 앱 코드·prod 무접촉)

## 2. 이번 세션 완료 목록

- ✅ **계약 승인** — `docs/contracts/CONTRACT_pao_ops_canon.md` (「운영정본 목표」, D-NAO-279, Jino 17:06 *"①만 고쳐서 승인"*). 초안 대비 개정 1건: §5 S1 「반증 ≥1건」 → **「반증 목록 절 존재 — 0건이면 왜 0건인지(밟은 절 수·실행 명령 수) 병기」**. 이유: 발견을 «합격 조건»으로 만들면 **반증을 지어낼 유인**이 생기고, 그게 이 저장소가 싸워 온 「억지 충족」과 같은 모양.
- ✅ **S1 전수 재검증** — `docs/references/111_paoops_reverify_20260830.md` (206줄). 별도 Sonnet·읽기 전용·**prod 13회 조회**. 사실 주장 **153개** 항목화(직접 재현 60개=39%) → **반증 4 / 재현 52 / 미상 4 · 죽은 좌표 1**.
- ✅ **S2 정본 전면 개정** — `docs/PAO_OPS.md` 324→317줄. §1 광고 성과 절 **신설** · MEASURED 블록 6개로 실측값 격리 · 좌표를 `파일:줄` → **`파일::심볼`** 전환(줄번호 잔존 0) · [미배선] 병합 13건.
- ✅ **S3 좌표 자동 검사기** — `scripts/check_pao_canon.py`(370줄) + `backend/tests/test_pao_canon_check.py`(**32 passed**) + `docs/wiki/AUDIT_PROTOCOL.md` §3-4에 1줄.
- ✅ **적대 리뷰 1R FAIL → 수리 → 2R PASS** (§2-1·§5 참조)
- ✅ 트랙 확인줄 2건(착수·종결) · 체인 등록 n=72

**라이브 관측 (2026-08-30 18:2x KST, 내가 직접 실행)**
```
$ python3 scripts/check_pao_canon.py
전체 117 / 통과 97 / 실패 0 / 미분류 20   (MEASURED 구간 6 · 제외 백틱 1 · 블록내 좌표오염 0)
유형별 — 파일 32 / 심볼 24 / API 12 / 테이블.컬럼 7 / 크론 22
EXIT=0
```

### ★S1이 잡은 반증 4건 (정본에 반영 완료)
1. 「어제(08-29) 537,105원」 → 537,105원은 실제 **08-28** 지출. 08-29는 **443,894원**
2. 「계정 7일 4,051,618원」 → 실은 **8일**(08-22~29) 합. 진짜 7일(08-23~29)은 **3,561,309원**
   ★**①②는 독립 오류가 아니라 「하루 밀림」 한 원인의 두 증상.** 정본은 값만 고치지 않고 **창의 시작·끝 날짜를 명시하는 서식**으로 바꿔 재발을 막았다.
3. §11 「부품 생존」 레시피가 `scripts/ignition_parts_alive.py`를 **prod에서** 돌리라 하는데 **prod에 `scripts/` 디렉터리 자체가 없다**(repo엔 있음 — 죽은 건 좌표가 아니라 «prod에서 돌린다»는 서술)
4. (약함) 「크론 35종」 → `job_name LIKE '%naver%'` 실측 **36건**, 카운트 방법론 불명 ⇒ 정본은 「총계를 자로 쓰지 않는다」로 대체

### ★성과 표면 재고 (Jino 지적 대응 — §1 신설의 내용)
| | 실측 |
|---|---|
| 성과 원장 | **있음** — `naver_ad_daily` · `naver_search_term_daily` |
| 총이익 계산 코드 | **있음** — `profit_scorecard.py` (D-NAO-59 목적함수 그대로 구현) |
| **화면** | **있음** — `frontend/src/pages/NaverAdPerformance.tsx` **1,253줄**, ROAS·BEP·공헌이익·관할밴드 |

★**옛 운영 문서 13개 절이 이 화면을 한 번도 언급하지 않았다**(S1 기의 「가장 놀란 것」 1번).

**지금 값** (2026-08-30 관측, 계정 전체): 7일 광고비 **3,561,309원** / 30일 **19,923,726원** · 전환 **2,225건** · 전환액 **35,377,700원** · ROAS **177.5%**
⚠️ **이 ROAS(30일 창)를 BEP 171.1%(계정 블렌디드·391일 창) 📄와 그대로 비교하지 말 것 — 창이 다르다.** 「본전 위/아래」는 현재 **판정 불가**.

★★**최대 구멍**: `profit_scorecard.py`가 계산한 총이익이 **API·화면 어디에도 안 닿는다**(diary+Slack만). `/performance/*` 화면들은 **전부 별도 코드 경로로 재계산**한 것. ⇒ **목적함수를 계산하는 유일한 모듈의 출력을 사람이 볼 수 없다.** (정본 §12에 [미배선]으로 등재. **수리는 이번 범위 밖** — 계약 §1)

## 2-1. 완료 QA (별도 Sonnet · 읽기 전용 · 2026-08-30 18:27 KST)

- **작업 목적(정본 원문)**: Jino (16:53) *"C로 하면 일단 이 내용에 대한 정리는 되는거지? 나는 이걸 PAO내에 정리해두고 싶어. 그래야 최종버전의 자동 업데이트 작업이 가능할꺼잖아?"* + 자동 업데이트 두 갈래 중 **①「좌표 자동 검사부터」** 선택 + *"먼저 이걸 완료하는게 맞겠지"*(17:03)
- **합격기준(원문)**: 계약 `docs/contracts/CONTRACT_pao_ops_canon.md` §5 체크박스 6개

**판정 원문 그대로 — 대조 4개 각각**:
```
판정(계약 §5 합격기준): 달성 — 6항목 전부 명령 재현으로 확인, §4 금지선(앱 코드 변경 0건) 준수 (2026-08-30 18:27 KST)
판정(Jino 지시 원문): 달성 — ①「PAO내에 정리」=같은 경로 전면 개정·신규 정본 없음 ②「좌표 자동 검사」=명령 한 줄로 가동 ③「광고 성과 지표」=§1 신설, 화면 경로·지금 값·구멍 목록 포함 (2026-08-30 18:27 KST)
판정(트랙 헤더 합격 M항목): 달성(약속대로 불변) — N/M=2/7, 계약이 처음부터 M 무귀속을 선언했고 실제로 체크박스 무변경 (2026-08-30 18:27 KST)
판정(북극성 5요소): 부분달성 — 「이동 없음(준비 작업)」 자기신고가 실측과 정합하고 §7 대칭 무접촉·§7 원문 옮김 왜곡 0, 다만 §1 신설의 준비적 진전 폭을 계약·앵커가 과소평가했을 가능성 배제 못함 (2026-08-30 18:27 KST)

종합 판정: 부분달성
```

- **항목별**: S1(달성 — 반증 목록 절 + 항목 수 153·재현 60 병기) / S2(달성 — MEASURED 12회=6쌍, 머리 규약 4줄, 줄번호 잔존 0) / S3-a(달성 — `전체 117 / 통과 97 / 실패 0 / 미분류 20`, EXIT=0) / S3-b(달성 — 좌표 오타 주입 시 이름으로 실패 목록에 출력, EXIT=1) / S3-c(달성 — AUDIT_PROTOCOL §3-4) / 트랙 흔적(달성 — 확인줄 1건, 체크박스 미변경) / §4 금지선(준수 — `backend/app/`·`frontend/` 변경 **0건**)
- **미달·미판정 항목**: 없음(미달 0). QA가 확인 못 한 것 4건 → §5-B
- **목적 전환 여부**: 없음(`🔁 목적 전환` 선언 0건)

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."* (Jino 2026-07-19, D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음** — 이 계약은 **M0~M6 어디에도 안 속하는 문서·하네스 인프라**다. 계약이 착수 전에 그렇게 선언했고 QA가 「약속대로 불변」으로 확인했다. 표류가 아닌 근거: **M4(광고 재개) 판단이 바로 이 운영 문서 위에서 내려진다.**
- **헤더에 남긴 확인 줄**: `확인: 2026-08-30 17:2x KST [d61698ea]`(착수) · `확인: 2026-08-30 18:3x KST [d61698ea]`(종결)
- **다음 세션 후보 항목**: §6 참조 — 트랙 M을 움직이려면 **M4 게이트(소유권 분리·Jino 결정 2건)** 또는 **M2 잔여(S2 5항목)**
- **트랙 종결 여부**: **미도달**(2/7)
- **북극성 대비(D-NAO-226·227)**: 5요소 이동 **없음(준비 작업)** · **④자동화 30세션째 0** · §7 대칭 **무접촉**(정본에 대칭 절을 싣되 이미 배선된 집행 지점 3개 지목만, 새 게이트 0)

## 2-3. 착지

(Step 6에서 채움)

## 3. 확정된 결정사항

- **D-NAO-279** — 「운영정본 목표」 계약 승인. `docs/PAO_OPS.md`가 PAO 운영 사실의 **정본**이고, 그 좌표는 `scripts/check_pao_canon.py`가 검사한다.
- **정본의 서식 규약**(계약 §8, 바꾸려면 계약 개정): 실측값은 `<!-- MEASURED -->` 블록 안 · 좌표는 **`파일::심볼`(줄 번호 금지)** · 좌표 5유형(파일/심볼/API/테이블.컬럼/크론) · 5유형 아니면 미분류(실패 아님).
- **검사기에 강제층을 세우지 않는다**(계약 §3-6) — 훅·CI 필수화·머지 차단 없음. 실행 지점은 **주간 감사**(AUDIT_PROTOCOL §3-4).
- **검사기는 repo 정적 사실만 본다** — prod DB·네이버 API 호출 0(계약 §3-3).
- ★**「MEASURED 블록은 «값»을 담지 «좌표»를 담지 않는다」** — 적대 리뷰 P1-1의 수리 원리. 블록 안에 분류가능 좌표가 보이면 마커가 잘못 놓였다는 뜻이므로 실패로 올린다. **임계값이 아니라 원리**라 문턱이 필요 없다.
- **값 자동 채움(운영 대시보드)은 별도 계약** — Jino가 「좌표 검사부터」를 먼저 골랐다.

## 4. 핵심 파일 목록

| 파일 | 역할 |
|------|------|
| `docs/PAO_OPS.md` | ★**PAO 운영 정본** — 이거 하나만 열면 답이 나온다 |
| `scripts/check_pao_canon.py` | 좌표 생사 검사기(읽기 전용·표준 라이브러리만) |
| `backend/tests/test_pao_canon_check.py` | 검사기 회귀 32건(표면 절단 변이 방어 포함) |
| `docs/contracts/CONTRACT_pao_ops_canon.md` | 이 작업의 계약(D-NAO-279) |
| `docs/references/111_paoops_reverify_20260830.md` | S1 전수 재검증 판정문(반증 4건) |
| `docs/wiki/AUDIT_PROTOCOL.md` §3-4 | 검사기의 실행 지점(주간 감사) |
| `docs/references/82_pao_north_star_20260819.md` | 북극성 — 궁극 목표 5요소·M0~M6·금지선 |

## 5. 알려진 이슈 / 주의사항

### 5-A. 적대 리뷰가 잡은 것 — 이번 세션 최대 산출물
- **1R FAIL (P1 1건)**: 검사기가 독스트링에 스스로 *「가장 위험한 자리」*라 써 둔 방어에 구멍. 마커 **개수 불일치**만 막고 **개수가 맞은 채 구간이 잘못 벌어지는 것**은 안 봐서, 산문에 마커 쌍이 들어가면 좌표 **94/113(83%)이 침묵 제외되고 exit 0**. 계약 §3-5 위반(「발견 0건」과 「검사 안 됨」이 같은 숫자).
- **수리 `feead3cf`**: ①백틱·펜스 안 리터럴 마커는 마커로 안 침(구간 7→6) ②**MEASURED 블록 안 분류가능 좌표 = 실패** ③제외량 상시 출력. + P2 채택 5건.
- **2R PASS**: P1-1 해소·회귀 0. 리뷰어가 **변이 6,207종 전수 스윕**으로 「분류가능 좌표의 침묵 손실 **0회**」 확인 📄. 원리적 근거: *좌표가 침묵 제외되려면 구간 «안»으로 들어가야 하는데 「구간 안에 있음」이 곧 오염 검사의 조건 — **탐지 조건과 피해 조건이 동일 집합**이라 임계값 없이 닫힌다.*
- 1R 생존 변이 **M12·M13·M14**(크론·테이블·API 검사기를 `return True`로) → P2-10으로 **전부 사망 전환**. 표면 절단 변이는 1R·2R 모두 **전건 사망**(테스트가 `capsys`로 stdout 문자열을 본 덕).

### 5-B. QA가 확인 못 한 것 (판정문 그대로)
1. HANDOFF — 판정 시점 미존재(정상 순서)
2. 적대 리뷰의 변이 6,207 스윕 로그 — 리뷰 층 산출물이라 QA가 재현 안 함
3. ★**계약 §5 원문 「[미배선] 11건」 vs 정본 §12 실물 「13건」의 격차 경위** — 보충: S2 위임문이 「체크리스트 11건 + ref 111 성과 구멍을 병합·중복 제거」를 지시한 결과가 13이다. **계약 문언이 병합 전 숫자로 남은 것** — 다음 세션이 계약 §5를 읽을 때 이 줄을 같이 볼 것
4. 북극성 §8 [미상] 해소 여부 — 대조 대상 밖

### 5-C. ⚠️ 하네스 훅 오탐 **3건** (계약 §6 계수 대상)
1. `review-surface-mutation.sh`가 **읽기 전용 재검증 위임**을 적대 리뷰로 오인해 1회 deny → 재전송 통과
2. `[체인] ⛔ pao-논의 n=49` — 공유 메인 폴더가 origin/main보다 **860커밋 stale**. 생존 판정은 **origin/main으로만**(origin/main에선 n=71까지 `end_kst` 채워짐)
3. **트랙 확인줄 훅** — 내 확인줄은 워크트리에 실재(`grep -c d61698ea` = 1)하나 훅이 stale 공유 폴더를 읽어 「기록 누락」. **공유 폴더에 대신 쓰지 않았다**(남의 커밋에 쓸려 들어간다 — [[track-confirm-line-hook-reads-shared-folder]])

### 5-D. 이월 (스코프 밖 — 고치지 않고 기록만)
- `pao_scope_roster.py`가 D-NAO-244 진리표를 안 따름 📄(ref 109 §5 — 「스코프 행 없음=전 그룹 ON」을 전 그룹 False로 계산, `/naver-ad/scope` 화면이 이 값을 씀) → 소관: 이 트랙, 별도 수리 안건
- `routers/naver_ad.py` 스코프 PUT이 갱신에도 `before_value=None` 📄 → 소관: 이 트랙
- prod↔origin/main 배포 드리프트 상시 감시가 없다 — 미배포 수리가 하루 +39건 부패를 쌓는 동안 아무 표면도 안 울렸다 📄 → 소관: 배포 인프라
- 리뷰 2R 이월 후보: `.gitignore`처럼 「/」 없는 단일 세그먼트 점파일은 여전히 미분류 · `guardrail_params.py::max_auto_up_multiple`은 파이썬 심볼이 아니라 SPECS dict 키(실제 상수는 `guardrail_gate.py::_MAX_AUTO_UP_MULTIPLE`) · `check_table`이 `mapped_column` 애노테이션 패턴에 의존 · 크론 «표현식» 일치 검사 미구현(계약 §8이 명시했으나 미구현 — 오늘 22/22 일치는 리뷰어가 확인) · `::심볼` substring-anywhere · 디렉터리가 파일로 통과 · **프런트 라우트 6번째 유형**(계약 §8 개정 필요)
- ⚠️ 학습 재료 만료 **2026-10-28**(D-59)

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문)**: 계약 `CONTRACT_pao_ops_canon.md`는 **3슬라이스 전부 완주**했다. 이 계약은 여기서 닫힌다.
- **남은 슬라이스**: 없음(이 계약 기준). 아래는 **다음 계약 후보**다.

★**3문 검사 결과 — Jino에게 물어야 하는 것은 «없다»**(전역 §2). 아래 ①②는 목표·범위를 바꾸므로 새 계약이 필요하고, 그건 Jino 발의 사항이다. ③은 되돌릴 수 있고 범위 안이라 **묻지 말고 진행**해도 된다.

- [ ] ★**Jino가 「자동 업데이트 ②」를 열면** — 값 자동 채움(운영 대시보드). 정본 §12의 [미배선] 13건 중 [표면 없음] 항목들이 대부분 여기로 들어간다. **최우선 후보 = `profit_scorecard.py` 출력을 API·화면에 배선**(목적함수를 계산하는 유일한 모듈인데 사람이 못 본다). ⇒ **새 계약**(Jino 발의)
- [ ] **트랙 M을 움직이려면**: M4 게이트(소유권 분리 — **08-19부터 12일째 Jino 결정 대기**) 또는 M2 잔여(ref 65 S2 5항목). ⇒ **새 계약**(북극성 §8-③이 「계약 1장 + Jino 승인」을 명시)
- [ ] **묻지 말고 진행 가능**: 정본 §14 「재는 법」의 부품 생존 레시피가 아직 🧠 미검증이다(반증 3의 수정판). prod에서 실제로 돌려 보고 정본을 확정하면 된다 — 되돌릴 수 있고 계약 범위 안이다.
- [ ] **주간 감사 때**: `python scripts/check_pao_canon.py`가 AUDIT_PROTOCOL §3-4에 실렸다. **첫 실행은 다음 감사 몫**이고, 그 결과가 이 배선의 라이브 증거가 된다.

## 7. 새 세션 시작 프롬프트

```
/session-relay pao-논의
```


---
---

# ★2부 (18:4x → 08-31 03:xx KST) — 「목적관철 목표」 계약 저술

> ⚠️ 위 1부는 **18:32 시점**의 인계다. 세션은 그 뒤 6시간 넘게 계속됐고 **산출물의 무게중심은 2부에 있다.**
> **다음 세션이 실제로 이어받을 것은 2부다.**

## 2-0. 한 문장

**다음 세션은 `docs/contracts/CONTRACT_pao_purpose_and_hands.md`(「목적관철 목표」·D-NAO-280)를 Jino 승인 뒤 P0부터 끝까지 돌린다.**
계약은 **「Jino에게 물어볼 것이 하나도 없도록」** 설계됐다(§7-A: *"Jino가 승인 전에 결정할 것: 없음"*).

★★**계약이 막히면 묻지 말고 계약을 고치고 진행한다.** 계약 문언과 라이브가 어긋나면 **라이브가 이긴다** — 계약을 그 자리에서 고치고(§5 말미 검산 스크립트를 같이 돌리고) 진행한 뒤 그 사실을 트랙·HANDOFF에 남긴다. 하네스 §1 3문 검사상 ①목표·범위를 바꾸지 않고 ②되돌릴 수 있고 ③사후 가시성·정정 경로·근거 보존이 있으면 **승인 지점이 아니다.**

## 2-1. Jino가 연 범위 (원문)

- *"1번 계약, 1번 이슈를 포함해서 운영을 위해 손을 달고 쿠팡에서 잘 팔리는데도 불구하고 노출을 일으킬 수 없는 경우 어떻게 극복방안을 종합해서 계약서를 작성하자"*
- *"이 세션이 최우선 세션이야. 여기서는 모든 다른 세션의 소관을 가져와서 작업해도 돼"*
- *"인수해도 돼"* (→ `pao-uiux` 체인 n=2 인수 행 append, 과거 행 무변경)
- *"너는 항상 기능은 만들어놓고 손을 안만들어서 실제 사용이 안돼는 경우가 대부분이야"* ← **이 계약의 제1원칙이 여기서 나왔다**
- *"다음 세션에서 내가 별도의 승인 없이도 끝까지 돌 정도로 계약이 잘 작성되어 있어?"* ← **품질 기준**

## 2-2. 계약 구조 (읽는 순서)

| Phase | 무엇 | 예산 |
|---|---|---|
| **P0** | 즉각 처치 — 제외 원장 재실측·pending 처분·§0 그룹 처분(ⓒ)·`optimizer` 복귀(ⓔ) | 2세션 |
| **P1** | 정본·정정 — 시간대 판정·원가 동기화(**E가 움직이면 ⓒ 판정이 뒤집힌다**) | 2세션 |
| **P2** | 되돌림·킬스위치·사람 발의 | 2세션 |
| **P3** | 눈(카운터·화면) + pao-uiux 인수 + AVA 브리핑 + **탐침 카드** | 3.5세션 |
| **P4** | 자·대칭 + §2-D 산식 코드화 + 총이익 집행 게이트 | 3세션 |
| **P5** | 학습(한계ROAS 곡선 — 실집행 없이 견적 API로) | 2세션 |
| **P6** | 승격 executor | 1세션 |
| **정규 소계** | **15.5세션 · 825만 토큰 · 3주** | |
| **빠른 길** | P0+P1+P2+탐침 카드만 | **7~7.5세션 · 450만 · 1.5~2주** |
| P8 | 조건부 — **체크박스 아님**(발동 시 승격) | — |

**M = 38** (`grep -c '^- \[ \]'`가 그대로 38). 손 좌표 11 · 라이브 체크 20.

★**계약을 고칠 때는 §5 말미의 내장 검산 스크립트를 반드시 돌린다** — M·손 좌표·P8 체크박스 0·예산 합계·라이브 체크를 한 번에 센다. 이 계약의 회귀 **7건이 전부** 「한 곳 고치고 연결된 곳 안 고침」이었고, v9 작성 중 2건은 이 스크립트가 잡았다.

## 2-3. 다음 세션이 «먼저» 알아야 할 실측 3개

1. **§0 그룹(Z폴드8와이드)은 판정불능이 «아니다»** — 90일 클릭 **2,508** · 전환 **168** · 매출 289만원 · `confident=True` · `evidence_window() active=False`. 코드 자신이 「이미 충분히 측정된 소재」라 부른다. ⇒ P0-ⓒ 기본값 **「축소」**가 §2-A 하향 동결·§5 금지선에 **안 걸린다**(v9 F1이 결합자를 `ⓐ ∧ ⓑ`로 명문화해 이걸 닫았다).
2. **E(610) < F(1,050) < R(1,300)** — §2-D ③분기 = **손해 구매 카드**(자동 탐침 금지, 사람만). 「안 보여서 안 팔린다」가 아니라 **「보여도 남는 게 없다」**. ⚠️ **F·R은 08-30 22:38 값이고 지금 재실측 불가** — ⓔ로 `optimizer='none'`인 동안 사다리 원료가 끊긴다. 순서: **ⓒ 처분 → ⓔ 복귀 → 사다리 수집 → F·R 재실측**. **E는 오늘 다시 잴 수 있다**(P1 원가 동기화가 E를 움직이면 ③분기가 뒤집힐 수 있어 ⓒ가 P1 뒤다).
3. **prod 배포는 오늘 «안» 막혀 있다** — `alembic current` = `heads` = `pgprice1s1a`, 로컬 heads와 diff 0(08-30 실측). v7까지 계약이 「막혀 있다」를 📄에서 단정문으로 썼던 것을 v9가 정정했다. 다만 **남의 트랙 마이그가 언제든 막을 수 있고, 남의 마이그는 대신 적용하지 않는다**(금지선) — 계약 §3에 측정 명령이 있다.

## 2-4. 근거 산출물

| 파일 | 무엇 |
|---|---|
| `docs/references/114_issue1_pao_purpose_vs_implementation_20260830.md` | **이슈1** — PAO 목적 대비 구현 간극 |
| `docs/references/115_issue2_pao_missing_hands_20260830.md` | **이슈2** — 손 24건 (A 사람 입구 / B 시스템 배선 / C 보는 표면 / D 되돌리는 손) |
| `docs/references/116_issue3_fold8wide_death_spiral_20260830.md` | **이슈3** — Z폴드8와이드 사례 + 원가 확인·E 재산출 |
| `docs/references/112_pao_gates_dont_read_profit_20260830.md` | 게이트가 총이익을 **보수 프록시로만** 본다 |
| `docs/references/113_pao_purpose_checklist_20260830.md` | 목적 축 체크리스트 7고리 28항목 |

## 2-5. 내가 2부에서 틀린 것 — 전부 «남»이 잡았다

1. **개별 판단으로 처방** (「1,300원 올리자」) → Jino: *"이건 이 광고 그룹에만 적용되는 이야기잖아"* ⇒ §2-D 전역 산식 `target = min(E, R)`, `target ≥ F`.
2. **손해 구매 추천 → 실측이 뒤집음** (위 2-3-1·2).
3. **유령 부활** — 「Z폴드8 적자 −578,822원」은 `LESSONS_LEARNED.md` **#303이 이미 정정한** 쿠팡 1P 채널 손익. 08-16 세션이 네이버로 오귀속한 것을 내가 다시 인용했다.
4. **남의 트랙 승인 합격기준을 낮췄다** — pao-uiux 잔여 2건. **인수는 «소관» 인수이지 «기준 개정» 승인이 아니다**(하네스 §2). v8에서 철회.
5. **지어낸 명령** — `safe_deploy.sh --dry-run`(grep 0건). v9에서 `ssh … alembic current` ↔ 로컬 `alembic heads`로 교체.
6. **QA 금지선 위반 좌표** — 실사 수단으로 `check_survival`을 지목했는데 그건 `db.commit()`으로 원장을 쓴다. v9에서 **읽기 전용 `exclusion_survival.py::survival_summary`**로 교정.
7. **인라인 편집 회귀 5건** — 「옮겨 적기 수준이라 위임 안 한다」는 판단이 틀렸다.

★**공통 모양**: 7건 중 5건이 **「📄를 실측처럼 단정」** 또는 **「한 곳 고치고 연결된 곳 안 고침」**이다. 하네스 §2의 📄/🧠 꼬리표 규약이 정확히 이걸 겨눈다.

## 2-6. 완료 QA

앵커 `대조:` 4대상에 대한 판정은 **1부(18:27, 별도 Sonnet·읽기 전용)**에서 끝났고 원문은 위 1부 §에 있다 — **종합 부분달성**.
2부(계약 저술)는 **독립 검사 3회 + 기계 검산**으로 갈음했다. 2부 판정: **달성(저술) — 단 계약은 「승인 대기」이므로 계약 자신의 38항목은 0/38.**

## 2-7. 이월 (2부)

- ↗️ 계약 §5 「[미배선] 11건」 vs 정본 §12 「13건」 — 병합 전 숫자 잔존 → 소관: 이 트랙.
- ↗️ `pao_scope_roster.py`가 D-NAO-244 진리표 미준수 📄(ref 109 §5) → 소관: 이 트랙.
- ↗️ `routers/naver_ad.py` 스코프 PUT이 갱신에도 `before_value=None` 📄 → 소관: 이 트랙.
- ↗️ prod↔origin/main 배포 드리프트 상시 감시 부재 📄 → 소관: 배포 인프라(트랙 미상).
- ↗️ **하네스 오탐 누적 4회** — `review-surface-mutation.sh`가 읽기 전용 조사 위임을 「적대 리뷰」로 오인 → 소관: 하네스(계약 §6 계수 대상).
