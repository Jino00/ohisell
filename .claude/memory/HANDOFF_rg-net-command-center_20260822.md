# 세션 인수인계 — 종합조망 RG 매출을 콘솔 net 축으로 (D-CPP-49) · **트랙 종결**

> 저장: 2026-08-22 18:0x KST · 세션 `d8e6ddbb` · 사슬 「쿠팡-손익정합」 **n=3**
> 트랙: **`docs/tracks/completed/track_coupang-rg-net-ledger.md`** — **7/8 → 8/8 · 종결(달성)**
> PR **#330 병합** · prod 배포 **`05855acf`** · 문서 PR **#331 OPEN**
> 완료 QA 3대조 **달성/달성/달성** · 트랙 종결 QA **달성** — 원문은 §5·§6.

---

## 1. 발단

n=2(`a1ae61e2`)가 남긴 미달 **ⓑ 하나**. 그 인계 §7이 *"ⓑ 하나뿐이고, 막던 것이 사라졌다"*고 했고 실제로 그랬다.

## 2. ★착수 실측이 인계를 4건 정정했다

**이게 이 세션에서 가장 값진 부분이다** — 인계 목록은 실측 전엔 못 믿는다(§6).

1. ★★**로컬 공유 main이 origin/main과 갈라져 12커밋 뒤처져 있었다.** RG 넷 원장 코드도 트랙 파일도
   그 체크아웃엔 **아예 없었다.** 거기서 착수했으면 stale 브랜치 위에서 일할 뻔했다.
   HANDOFF §7 「착수 시 참고」에 이 divergence가 **한 줄도 없었다.**
   ⇒ `origin/main`(02b35f15) 기준 워크트리 `claude/rg-net-command-center`를 새로 팠다.
   ⚠️로컬에만 있던 커밋 `34976464`는 **다른 세션(「PAO 논의 34」)의 미푸시 커밋**이라 안 건드렸다.
2. **진행률은 4/8이 아니라 7/8**이었다(HANDOFF 머리말이 stale, 본문 5-A 재판정이 정본).
3. **소비처가 셋이 아니라 넷**이었다 — 인계가 빠뜨린 `revenue_reconcile`이 `revenue_rg`를 소비한다.
4. `scripts/next_ids.sh`는 **D-CPP를 안 다룬다**(네이버 트랙 경로 하드코딩) — 번호는 실측 grep으로.

## 3. ★설계를 «실측이» 정했다

대시보드는 **요약축**, 종합조망은 **옵션축**(옵션 grain이 필요하다)을 읽는다. 두 축이 갈라지면 ⓑ가
깨지므로, 등가를 16일 → **147 계정-일**로 확장 재측정했다(prod 읽기 전용, 15:47 KST):

| 계정 | 기간 | 일수 | gmv 불일치 | 수량 불일치 | 한쪽만 있는 날 |
|---|---|---|---|---|---|
| WING1 | 06-07~08-21 | 76 | 0 | 0 | 0 |
| WING2 | 06-12~08-21 | 71 | 0 | 0 | 0 |

⇒ **잔여 버킷·안분 설계 불필요.** 갈아끼우기만으로 ⓑ가 성립한다.

## 4. 소비처 넷의 처분 — 「일관성」이 항상 정답은 아니다

| 소비처 | 처분 | 이유 |
|---|---|---|
| `intelligence.compute_command_center` | **net 교체** | ⓑ 본체 |
| `product_pnl.py:294` | **같이 교체** | 권위값이 cc의 `revenue_rg`다. 할당분만 gross면 보존식이 깨져 `trustworthy=false` → **SKU 손익 표가 렌더 자체가 안 된다**(그 라이브 사고가 코드 주석에 있다) |
| `revenue_canonical._agg_by_type` | ★**gross 유지** | 누락이 아니라 **판단**이다. 여기 gross는 ⓐ Wing net을 옵션에 안분하는 «가중치»이고 ⓑ 당일분 프록시다(콘솔 D+1이라 오늘의 net이 원리적으로 없다). net으로 「일관성 있게」 맞추면 `factor_rg≈1`로 환산이 죽고 **오늘 RG 매출이 0으로 사라진다** |
| `revenue_reconcile` | **신호 이관** | RG 드리프트가 «같은 축끼리의 비교»가 됐다 — 0은 「정합」이 아니라 「같은 숫자를 두 번 읽었다」다. ref 18이 재던 신호(우리 gross vs 콘솔 net)를 이름이 다른 칸 `*_rg_gross`로 옮겼다. 안 옮기면 **「드리프트 0」이 「감시 안 됨」을 덮는다**(교훈 #123) |

## 5. 완료 QA (별도 기 · 읽기 전용 · **판정 원문 그대로**)

> **판정(트랙 계약 합격 ⓑ): 달성** — prod 라이브 재현: WING1 3,152,860==3,152,860, WING2 30,000==30,000, 둘 다 옵션축 16/16 complete (2026-08-22 17:50 KST)
>
> **판정(Jino 2026-08-21 승인 계약 ① 원문): 달성** — 대시보드·종합조망 둘 다 라이브에서 net 값만 노출 확인, `product_pnl.py` 소비처도 net 축(authoritative_total=3,152,860)으로 라이브 동작 확인 (2026-08-22 17:53 KST)
>
> **판정(트랙 궁극 목표): 달성** — 종합조망(command-center)에 revenue_3p·revenue_rg가 나란히 뜨고, net_profit·net_profit_pre_rg 등 순이익 계산에 편입됨을 라이브 확인 (2026-08-22 17:53 KST)

★**QA가 내가 주지 않은 재현 경로를 스스로 발굴했다**: `/api/products/pnl-reconciliation`에서
`coupang_rg/revenue`의 `authoritative_total="3152860"`(net) · `conservation_ok:true` ·
`conservation_diff:"0"` · `summary.trustworthy:true`. 위 표 2행의 설계 근거가 **실제로 지켜지고 있음**을
독립 확인한 것이다. ⓗ도 자기가 `origin/main` 별도 워크트리(`7e35fa63`)를 파서 같은 2건을 재현했다.

**QA가 확인 못 한 것**(그대로 옮긴다):
- 브라우저 실제 픽셀 렌더링(스크린샷) 미확인 — HTTP 응답까지만. 화면 렌더는 프론트 테스트 11종으로 간접 확인
- 「147 계정-일 등가」는 SQL 재현 없이 evidence·주석 수치를 신뢰
- ⓕ 원가 절대값 720,069원 미재계산
- 오하이테크 3P 7,500원 차이는 「범위 밖」 처분을 수용만 함
- PR #330 CI FAILURE가 「결제정지 서명」이라는 판단은 재검증 안 함
- 트랙 종결 QA(다른 기)의 실행 과정은 독립 검증 안 함

**stale 제외 증거: 없음** — 관측 전건이 배포 `05855acf`(17:42 KST) 이후, 그 뒤 배포 기록 없음.

## 6. 트랙 진행률 — **종결**

- 트랙: `docs/tracks/completed/track_coupang-rg-net-ledger.md` (active/ → completed/ 이동)
- **시작 7/8 → 종료 8/8.** 이번 세션이 움직인 항목 = **ⓑ 하나**.
- **트랙 종결 QA(별도 기·읽기 전용) 판정: 달성** — 원문:
  > *"8/8 항목 전부 지금 시점 라이브 재현 성공, 회귀 없음(같은 2건 사전 실패만 확인), 「안함」 범위 이탈 없음"* (2026-08-22 17:54 KST)
  ★그 기는 **8항목 전부를 직접 재현**했다(내 증거 묶음을 베끼지 않고). 실패 2건의 무관함도 나와
  **다른 경로**로 세웠다 — 나는 pristine origin/main 워크트리 실행으로, QA는 「그 파일들이 이 PR diff에
  없음」으로. ★그 QA가 확인 못 한 것: ⓕ 절대 원가 재계산 불가(옵션축 테이블에 `unit_cost`가 없어
  원가 매핑 조인 필요 — `cost_coverage=0.9943` 근사 일치로 갈음) · 픽셀 렌더 · `net_scope="partial"` 사유.
- **트랙 목표 원문**: *"대시보드가 회사 매출의 34.5%만 보여준다 — RG1 2,170만원이 매출 0인데 비용
  1,736만원만 들어가 오픽스가 −1,734만원"* 중 **RG 몫을 닫는다.** 하위 목표: *"종합조망(Command Center)에
  편입 — 3P/RG와 나란히 매출·순이익을 본다."*
- **다음 계약 후보**(트랙 「안함」에 등재돼 있다): 상품별 RG 손익 화면 · 일자 추이 차트(`/trend-by-channel`)
  RG 편입 · 갭 34건 건별 신원 · `order_count` Optional 전환.

## 7. 적대 리뷰 — 1R FAIL(P1 1) → 2R **RESOLVED**(P1 0) · 변이 누적 20종 / 생존 1

- ★**1R P1 = 내가 넣은 자백 장치가 정작 기본 화면에서만 꺼졌다.** `_rg_axis_coverage`가 `None`을
  반환하는 창에서 프론트가 `=== false`로만 경고하므로 경고도 힌트도 사라져 화면이 「RG 0원」을
  **단정**했다. 계정 지정 뷰는 실토하는데 전체합산(기본) 뷰만 침묵 — **토글 하나로 화면이 다른 말**.
  ★**「어제」 버튼 한 번으로 재현된다** — `ago(1)`이 `today - 0`이라 창이 오늘~오늘이 되고, 콘솔은
  D+1이라 두 축 다 비어 있다. (그 버튼이 오늘을 주는 것 자체는 선행 사항 → §9 이월)
  ⇒ **「모른다」를 표현하는 방법은 필드를 지우는 것이 아니라 `complete=False`다.**
- **P2-5 동시 해소**: 콘솔 D+1인 오늘을 분모에 넣으면 **오늘이 든 모든 창**(기본 7일 창 포함)이 영원히
  미완이라 경고가 상시 켜져 **무뎌진다**. 닫힌 날만 분모로 세고, 안 닫힌 날은 사실(`rg_open_days`)로 낸다.
  ⚠️`option_axis_coverage` 자체의 뜻은 **안 바꿨다** — 대시보드가 그걸로 원가·순이익을 게이트하는 배포된 경로다.
- ★★**생존 변이 3종이 전부 프론트였다.** 백엔드는 변이 14종이 다 죽었는데 «값이 화면 픽셀이 되는
  마지막 한 칸»은 무보호였다(⚠ 부분치 경고 삭제 · 드리프트 표 gross 줄 삭제 · 힌트 삭제 — 셋 다
  471/471 초록). → `rgNetAxisSurface.test.tsx` **11종** 신설.
- 2R 생존 1종 = **페이지가 카드를 마운트하는 자리**(`CommandCenter.tsx:915`) — 이 커밋 이전부터
  무보호. 신설 테스트는 컴포넌트를 직접 render한다 → §9 이월.
- 2R P2 채택 2건: 미래 창에서 `open_days`가 창 길이보다 컸다 · 프론트 `?? "0"` 폴백이
  「−178만원 드리프트」 거짓 경보를 냈다.

## 8. 배포·라이브

- PR **#330 병합** — `safe_merge.sh 330 --force`. CI 3잡 FAILURE지만 **결제정지 서명을 직접 확인**했다:
  `steps: 0` · **2초**(07:51:24→07:51:26 UTC) · 로그 blob `BlobNotFound`. 코드 신호가 아니다.
  자백은 `$TMPDIR/safe_merge.log`.
- prod 배포 **`05855acf`** — 백엔드 5파일 CAS 전건 통과 · **무중단 블루그린**(:8001→:8011, 다운타임 0초).
- ★**프론트 첫 배포 시도가 dist 스탬프 가드에 거부됐다**(「병합만 하고 옛 dist 올리기」 차단) → 재빌드 후 배포.
  가드가 없었으면 병행 세션 32커밋 분량의 프론트 작업이 조용히 사라졌을 자리다.
- **ⓑ 라이브 확정**(17:43~17:45, QA가 17:50~17:53에 독립 재현):

| 계정 | 종합조망 | 대시보드 | gross였다면 | 옵션축 |
|---|---|---|---|---|
| WING1 오픽스 | **3,152,860** | **3,152,860** | 3,651,030 | 16/16 |
| WING2 오하이테크 | **30,000** | **30,000** | 53,900 | 16/16 |

  교차검산: 오픽스 회사 소계 **3,271,660** = RG 3,152,860 + 3P 118,800 = 종합조망 `revenue`.
  이관 신호 생존: `rg_same_axis=true` · `pct_rg=0`(같은 축) · **`pct_rg_gross=+15.80%`**.
- 증거: `docs/contracts/evidence_rg_net_command_center/`(prod 원 응답 4건 + 재현 명령 +
  **「이 증거가 답하지 못하는 것」**). D-NAO-204 전례(「라이브 화면」이 기준인데 증거를 안 남겨
  독립 검증 불능)를 되풀이하지 않기 위한 것.

## 9. 이월 (스코프 밖 발견 — 고치지 않고 적는다)

- ★★**main이 이미 빨간불이다 — 2건**: `test_health_partial_sync::test_health_route_actually_returns_partial_sync` ·
  `test_vendor_item_axis::test_health_route_actually_returns_conservation`. 둘 다 `/api/scheduler/health`
  HTTP 관통 테스트고 pristine `origin/main`에서도 실패한다(`bd5a660d`·`239e1b55`·`e76ee101`·`7e35fa63`
  네 지점에서 재현). **가설(미검증)**: 테스트가 `2026-08-05` 같은 날짜를 하드코딩하는데 헬스 라우트
  관측 창이 오늘 기준 롤링이라 시간이 지나며 창 밖으로 밀려났다 — 「시간이 지나면 썩는 테스트」.
  **CI가 결제정지라 아무도 안 보고 있다.** → 소관: 별건(테스트 하네스)
- **「어제」 퀵버튼이 오늘 창을 만든다** — `ago(1)` = `today - 0`(`CommandCenter.tsx:83-88`).
  라벨과 동작이 어긋난다. 선행 사항 → 소관: 별건(프론트)
- **페이지가 카드를 마운트하는 자리가 무보호** — `CommandCenter.tsx:915`의 `<ReconciliationCard>`·
  `<RevenueDriftCard>` 마운트를 지워도 전체 초록. → 소관: 별건(프론트 테스트)
- **오하이테크 3P가 두 화면에서 7,500원 다르다**(종합조망 665,800 vs 대시보드 673,300). RG 무관·선행
  사항 → 소관: `track_coupang-rocket-1p.md` 또는 별건
- **상품 탭 `order_count`에 자백·라벨 없다**(적대 리뷰 1R P2-3·P2-4) — 대시보드 쪽엔 게이트가 있는데
  종합조망 상품 탭엔 없다 → 소관: 이 트랙의 **다음 계약**(「안함」의 `order_count` Optional 항목과 같은 자리)
- ★**Mac 윙 페처 수동 실행 시 env를 «한 벌»로 걸어야 한다** — n=2 백필이 `OHISELL_WING_CONFIG`만
  wing2로 걸고 `OHISELL_WING_LOG`·`OHISELL_WING_LOCK`은 기본값을 써서 **WING1의 로그에 쓰고 WING1의
  락을 잡았다.** 병행 세션(collection-stability-s1)의 오픽스 회복 감지가 30초→3.5분 지연됐다.
  n=2 HANDOFF엔 없는 사실이고 **그 세션이 알려 줬다.** → 소관: 운영 절차
- **하네스 — 훅 오탐 1건**(계약 §6 계수 대상): `review-surface-mutation.sh`가 **완료 QA 위임문**에서
  발동했다. 위임문에 「적대 리뷰」가 **맥락으로만** 적혀 있었는데(산출물 좌표 줄) 리뷰 위임으로 읽혔다.
  QA에 변이 주입을 시키면 §2의 「QA는 읽기 전용」을 정면으로 어기므로 요구를 따를 수 없는 종류의
  위임이다. 안내대로 그대로 재전송해 통과. **오탐 1 / 정탐 0**(실제 리뷰 위임 2건은 표면 문단을
  담고 있어 발동 안 함 = 훅이 막을 일이 없었다).

## 10. 좌표

- 브랜치 `claude/rg-net-command-center` (origin push 완료)
- 커밋: `84521a4b`(본체) · `f768ce76`(1R P1) · `e3237bce`(2R P2) · `9a3a8c11`(main 병합) ·
  `947187c5`(트랙 확인줄) · `2c9684ee`(증거+8/8) · `66a3ff07`(트랙 종결)
- PR: **#330 병합**(https://github.com/Jino00/ohisell/pull/330) · **#331 OPEN**(문서, https://github.com/Jino00/ohisell/pull/331)
- prod 배포: `05855acf` (백엔드 5파일 + 프론트)
- 앵커: `.claude/anchors/d8e6ddbb-03fc-49ed-8a29-76f246405dd0.md`(판정 4줄 + 리뷰 처분 + 이월)
- 새 D-N: **D-CPP-49** · 새 교훈 번호 미부여(다음 세션이 `scripts/next_ids.sh`로 — ★단 그 스크립트는
  교훈·D-NAO만 다루고 **D-CPP는 안 다룬다**)
- 사슬 등록부: `.claude/memory/chains/쿠팡-손익정합.jsonl` — 이 세션이 **n=3**
