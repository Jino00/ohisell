# 세션 인수인계: 쇼핑의 키워드·제목 축 — 「PAO 논의 55」
> 저장일시: 2026-08-26 19:3x KST
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## §0. 궁극 목표 — 원문 (발명 금지)

**PAO 북극성 §1**: PAO의 궁극 목표는, 네이버 광고·스마트스토어 API가 주는 모든 지표와 4등급 성과등급 사이의 «개연성(연관)»을 홀드아웃 검증까지 통과한 지식으로 확정하고(①), 논문에서 채택한 기법과 이 트랙이 이미 세운 실행 구조(레인·가드레일·고삐·확장압력) 위에서(②), 우리가 운영하는 모든 광고 형태를 총이익 절대액이 최대가 되는 방향으로(③, D-NAO-59) 자동화 운영하며(④), 운영의 매 행위와 결과를 일기→반성→지혜 승격의 학습 사슬에 태워 어느 지점을 배우면 성능이 더 오르는지를 스스로 찾아, 신호가 허락하는 최단 주기로 성능개선을 반복 시도하는 것(⑤)이다.

**트랙 목표 원문(D-NAO-59, Jino 2026-07-19)**: *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."*

## 1. 프로젝트 위치 및 환경
- 워크트리: `/Users/jino/.claude-worktrees/ohiselling/pao-n55` (브랜치 `feat/pao-n55`)
- prod: `sellc.ohitech.co.kr` · DB `/home/ubuntu/ohisell/backend/ohisell.db` · **API 활성 포트 8011**
- 배포 `scripts/safe_deploy.sh` / 병합 `scripts/safe_merge.sh` / 채번 `scripts/next_ids.sh`
- 테스트: 워크트리 `backend/`에서 `python3 -m pytest tests/ -q`(시스템 python3에 pytest 있음 — 이 워크트리엔 venv 없음)
- ⚠️ prod SQL은 heredoc이 따옴표를 먹으므로 파일로 `scp` 후 `sqlite3 -readonly … < 파일`

## 2. 이번 세션 완료 목록
- ✅ **D-NAO-255 주석 표류 전수 수리** — 옛 단가 「3,020」 7곳 + 옛 산식 「1,900+1,120p」 4곳 + 테스트 주석 7곳 + **실행 코드 1곳**(`backend/scripts/verify_bep_delivery_parity.py` — 주석이 아니라 하드코딩이었고, 수취 창도 `wide`→`recent` 미갱신이라 «검증 스크립트»가 거짓 불일치를 뱉던 상태). 재수출·설명 줄에서 **숫자를 상수 이름으로 교체**. 동작 변경 0.
- ✅ **`docs/references/102_privacy_campaign_product_axis_20260826.md`** — 사생활 캠페인 상품 축(A/B/C 3종 혼합·50개 중 47개 타 캠페인 중복) + 그룹상품 52308509 손익(6소재 25일 −238,549원) + N배송·수수료·부가세 반영 재확인
- ✅ **`docs/references/103_shopping_keyword_title_axis_20260826.md`** — 쇼핑의 키워드·제목 축 전수(10개 절). **이 세션의 주 산출물.**
- ✅ **교훈 #365** — 값을 고쳤으면 그 값을 설명하는 주석도 전수로 고쳐라
- ✅ 트랙 확인줄 3건 누적 · 체인 등록부 n=55

## 2-1. 완료 QA (판정 원문 그대로 — 미달·판정불능 포함)

- **작업 목적(정본 원문)**: 앵커 `목표:` = n=54 HANDOFF §0의 Jino 2026-08-25 09:20 원문(*"나는 우리의 자료가 옵시디언, LLM wiki를 통해 학습이 되고 지혜로 올라간 뒤에 다시 그 학습된 지혜가 우리의 광고에 적용이 되었으면 해…"*). 계약 없음 ⇒ **앵커가 정본.**
- **판정(n=54 HANDOFF §6 남은 슬라이스)**: **판정불능 → 이후 목적 전환** — 착수 실측에서 「묻지 말고 진행」 4건이 **전부 게이트에 막혀 있음**을 확정(①-b·①-c 08-27 크론 대기 / M3-c 08-28 게이트 / 점화 Jino 결정). 그 뒤 Jino가 방향을 세 번 추가했다. (11:10 KST)
- **판정(북극성 §1·§6, D-NAO-226)**: **달성** — 진행률 2/7 불변, 새 M 안 엶, §7 대칭 검사 수행. ★이번 세션이 **비대칭의 «구조적 원인»을 처음 규명**(쇼핑에 액셀 레버가 광고 밖에 있었다). (11:10 KST, 19:1x 보강)
- **판정(Jino 지시 원문 — 세션 중 추가 3건)**: **달성** — ①*"fable이 한번 기획해봐"* → Fable 산출물 2건 최소 편집 릴레이 ②*"광고비까지 모두 고려된거지?"*·*"수수료 5.6% vs 실효 10.5% 이거부터 확인해줘"* → N배송·수수료·부가세 전수 검산, 「누락 없음」 확정 ③*"pao에 모두 반영해줘"* → D-NAO-255 + ref 102 + 교훈 #365, PR #474 머지 완료
- **종합 판정: 부분달성**

### ⚠️ 판정의 커버리지 한계 (정직 기록)
11:10 판정은 **그 시점까지의 슬라이스만** 대조했다. 그 뒤 늘어난 몫(D-NAO-255 · ref 102 · ref 103 · Fable 기획 2건)은 **별도 완료 QA를 받지 않았다.** 대신 걸린 검증은 다음뿐이다:
- 코드 부분(D-NAO-255): **적대 리뷰 1R PASS(P1 0) → 2R PASS(P1 0·P2-1 해소)**, 379 passed/0, CI 3/3 실통과
- 기록물 부분(ref 102·103): **리뷰 생략(기록물만)** 자백 하에 PR
⇒ **다음 세션이 ref 103 §10 미결을 착수할 때, 이 문서의 숫자를 실측 전엔 믿지 말 것.**

- **목적 전환 여부**: `🔁 목적 전환` **선언 없음**. 슬라이스가 세 번 바뀌었으나 전부 **Jino 발의**였고 §7 「Jino가 먼저 꺼낸 것은 착수 가능」 경로로, 스코프 밖 착수 시 `↗️ 스코프 밖` 1회 표면화했다(확장프로그램 분석 착수 시). 앵커 `합격:` 체크박스도 그때마다 갱신했다.

## 2-2. 트랙 진행률
- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: §0 참조 (D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음** — M 단위 미이동. 이 세션의 산출은 **L1 지식층의 «외생 어휘» 축 정찰**과 **주석 표류 수리**라 어느 M에도 체크가 안 붙는다.
- ⚠️**진행률 계수 함정**(승계): 순진하게 `- [` 를 세면 왜곡된다. **①TRACK-CONTRACT 헤더 블록으로 한정 ②줄머리(`line.startswith('- [')`) 기준** 둘 다 적용해야 **7**이다(파일 전체 줄머리는 14 — 뒷부분 옛 P0~P5 체크리스트).
- **헤더에 남긴 확인 줄**: 3건 — `[1e29cb1a]` 14:0x(D-NAO-255·교훈 #365·ref 102) · 19:1x(ref 103)
- **다음 세션 후보 항목**: M2-S2(5/5 미착수·통A) 또는 M3-② A#8 판정면 주입. **단 §6의 미결 5건이 그보다 앞선다.**
- **트랙 종결 여부**: **미도달**(2/7)

### 북극성 대조 (D-NAO-226·227)
① **M**: 어느 M에도 안 속함 — L1 정찰. 새 M 안 엶 ② **5요소**: 이동 **없음** — **④자동화 27세션째 0** ③ **§7 대칭**: ★**비대칭의 구조적 원인을 처음 규명** — 쇼핑(광고비 78%)엔 «어떤 검색어에 들어갈까»를 정하는 액셀 레버가 **광고 밖(상품명·태그)**에 있다. 지혜 2건이 둘 다 브레이크인 이유가 여기 있다 ④ **관련 절 정독**: §목차 → §1·§2·§3(총이익의 자 행)·§4 간극1~5·§5-3·§6 전표·§6-b·§7·§8

## 2-3. 착지
- **완료 단계**: 커밋 → push → PR → 리뷰 → 머지 → **완주**(3회전)
- **멈춘 단계**: **없음**
- **재개 명령**: 해당 없음
- **좌표**:
  | 회전 | 내용 | 커밋 | PR | 머지 | CI |
  |---|---|---|---|---|---|
  | 1 | D-NAO-255 주석 표류(단가 7·산식 4) + ref 102 + 교훈 #365 + 트랙 | `13799146` | **#474** | `8569fbdb` | 3/3 실통과 |
  | 2 | 적대 리뷰 P2-1 상환(실행 코드 하드코딩 + 테스트 주석 7) | `91fbf44a` | 〃 | 〃 | 〃 |
  | 3 | ref 103 + 트랙 확인줄 | `425c758a` | **#481** | **`9c88eca6`** | **3/3 실통과**(backend py3.10 11m4s · py3.14 8m27s · frontend 57s) |
- **리뷰 판정**: #474 **1R PASS(P1 0) → 2R PASS(P1 0 · P2-1 해소)** · #481 `⚠️ 리뷰 생략: 기록물만 — docs/references/103_shopping_keyword_title_axis_20260826.md · docs/tracks/active/track_naver-ad-optimization.md`(코드 0파일)
- **`--force` 미사용** · force-push 0 · **prod 배포 0 · 마이그 0 · 광고계정 쓰기 0 · DB 쓰기 0**
- ⚠️ **변이 M1·M2 생존(구조적)**: `backend/scripts/verify_bep_delivery_parity.py`는 **어떤 pytest에도 수집되지 않는다**(`--collect-only` 확인). 이번에 고친 그 스크립트의 회귀를 잡아 줄 자동 검사가 **없다** — 수동 실행만이 드러낸다.
- ⚠️ 1R 생존 변이: `net_ship = max(0, ·)` 보수 클램프를 제거해도 359건 전부 초록 — **기존 테스트 공백**(이번 diff 밖)
- ⚠️ **L5로 「main에 세워둔다」 생략** — 로컬 `main`이 공유 메인 폴더에 체크아웃돼 있다. 다음 세션은 `git switch -c <새> origin/main`으로 원격을 명시해 분기할 것

## 3. 확정된 결정사항
- **D-NAO-255** 주석 표류 전수 수리 — 재수출·설명 줄엔 **숫자 대신 상수 이름**을 쓴다. 이력 서술(`3,020 → 3,245 → 3,377`)은 «현재값 주장»이 아니라 «변경 기록»이라 존치.
- **교훈 #365** — 값을 고쳤으면 그 값을 설명하는 주석도 **전수로** 고쳐라. 주석은 실행되지 않아 어떤 자동 검사에도 안 걸린다.
- **N배송·수수료·부가세는 이미 정확하다**(D-NAO-168 · `bep_calculator`) — 지불 3,377 − 수취 3,000 = **순배송원가 377원**, 공헌이익 = `(판매가−수수료−원가−물류)÷1.1`. 「실효 수수료 10.5%」는 **부가세를 수수료로 오독**한 것.
- **Fable 판정(미승인 기획)**: 제목·태그 축은 **반쪽 소관** — 지식(L1)·판정(L2)은 PAO, **쓰기는 상품 축**. 제목·태그 변경은 **「되돌릴 수 없는 액션에 준하여」** 취급. 위험 서열 **태그 < 제목**.
- **경쟁 SERP 인구조사 계약(v1 초안)은 «미승인·중단»** — 데이터 획득 4경로 전부 차단(§5 참조). 계약 초안 전문은 이 세션 트랜스크립트에만 있다(파일 미저장).

## 4. 핵심 파일 목록
| 파일 | 역할 |
|---|---|
| `docs/references/103_shopping_keyword_title_axis_20260826.md` | **이 세션 주 산출물** — 쇼핑 키워드·제목 축 전수 |
| `docs/references/102_privacy_campaign_product_axis_20260826.md` | 사생활 캠페인 상품 축 + 그룹 52308509 손익 |
| `backend/app/services/naver_ad/search_term_judge.py` | 제외 후보 게이트(쇼핑 14일/clk≥10 · 파워링크 30일/clk≥5) |
| `backend/app/services/naver_ad/search_term_ss_lane.py` | `_autofire_exclude` — 파워링크 자동 발사 경로(실재) |
| `backend/app/services/naver_sa_ad_fetcher.py` | `get_ads` — **파워링크 소재를 버리는 지점**(`mall_product_id` 있는 것만) |
| `backend/app/services/naver_ad/bep_calculator.py` | BEP·물류비(N배송 순배송원가) |
| `backend/app/services/order_delivery.py` | 배송 단가 **정본**(NORMAL 1,900 / NBAESONG 3,377) |
| `docs/references/82_pao_north_star_20260819.md` | 북극성(§1·§3 총이익의 자·§4 간극·§6·§7·§8) |

## 5. 알려진 이슈 / 주의사항
- ⚠️★**prod가 이 브랜치와 다른 소스를 돌고 있을 수 있다** — 감사가 prod 로그 형식과 브랜치 `scheduler_service.py:925-931`의 로그 문자열 불일치를 관측했다. **사실이면 이 세션이 코드로 내린 판정 일부가 흔들린다. 다음 세션 최우선 확인.**
- ⚠️ **인계 숫자 2건이 재현 불가**(ref 103 §3): 「제외 제안 686건 중 실행 1건」 → 역대 총 **6건** · 「잘라야 할 165개·90일 450만원」 → **2건·306,940원**(이미 제외 완료). **출처 특정 실패. 근거로 쓰지 말 것.**
- ⚠️ `naver_execution_harness.py:1281-1283` 주석 *"자동 발사 없음… 어디에서도 자동 승인을 배선하지 않는다"*가 **지금 코드와 어긋난다**(교훈 #365 계열, 미수리).
- ⚠️ **경쟁 SERP 자동 획득 4경로 전부 차단**: 헤드리스 HTTP 418 · 네이버 검색 API `shop` 엔드포인트 **404**(이 앱에 권한 없음, 인증은 정상) · 실제 크롬 조작도 차단 페이지 + **CAPTCHA** · Claude 크롬 확장은 도구 정책상 접근 불가. **차단 우회는 금지선.** ★단 **Jino 본인 브라우저에선 정상**이다 — 사람이 보고 복사하는 경로만 살아 있다.
- ⚠️ **이 Mac의 IP는 매일 07:30~09:55 네이버 광고 API 수집에 쓰인다.** 쇼핑 검색을 반복 두드리면 그 수집이 위험하다.
- ⚠️ **Jino가 채팅에 API 시크릿 3세트를 평문으로 붙여넣었고 스크린샷에도 남았다** — 재발급 필요(미완).
- ⚠️ **크롬 「Apple 이벤트의 JavaScript 허용」이 켜진 상태일 수 있다** — 이 세션이 켜 달라고 요청했다. 되돌리기 안내했으나 확인 안 됨.
- 확장 「메이커 셀링 도우미」의 검색량이 **HTTP 403** — 커머스 API 앱 ID가 들어가 있다. **검색광고 API 3종**(prod `.env`의 `NAVER_SA_*`와 같은 계열) 필요.
- 「연관/함께 많이 찾는 검색어」 블록의 **꼬리가 오염**된다(폴드8에서 6/30 무관). 전역 블록은 **아님**(강아지사료 대조로 반증). 정체 [미상] — **눈으로 걸러야 하고 자동으로 못 거른다.**

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)**: §0의 Jino 2026-08-25 09:20 원문. **★단 이 세션이 그 목적의 «구조적 전제»를 바꿨다** — 쇼핑에서 지혜가 광고에 적용되려면 그 적용면이 입찰이 아니라 **제목·태그**일 수 있다(ref 103 §5·§9).
- **남은 슬라이스** (전부 **묻지 말고 진행** — §2 3문 검사상 목표 변경도 비가역도 아니다):
- [ ] **★prod ↔ 브랜치 소스 대조** — 로그 형식 불일치 확인. `ssh … md5sum`으로 주요 파일 비교. **최우선**(이게 틀리면 아래가 다 흔들린다)
- [ ] **파워링크 광고 제목·설명 수집** — `get_ads`가 `mall_product_id` 있는 소재만 남긴다. 같은 응답에 이미 옴(**추가 호출 0**). Jino 지시 2026-08-26 19:23: *"파워링크 광고 제목 수집은 다음 세션에 하고"*
- [ ] **제목·태그 스냅샷 동결** — 1,213 상품의 현재 제목·태그를 버전 붙여 보존. **시간 민감**(콘솔에서 누가 만지는 순간 원복 좌표 소멸). Fable 「하나만 한다면」 1순위
- [ ] **`naver_execution_harness.py:1281-1283` 주석 수리** — 교훈 #365 계열
- [ ] ①-b·①-c 익일 관측(**2026-08-27 08:45** 크론 이후) — n=54 승계분. 볼 것: pm2 로그의 `harvest scanned=… rejected_tally_resumed=… reopened=…` / `prior_judgments_json` 누적
- **Jino 손이 필요한 것** (묻지 말고 진행 대상 아님 — 사람이 해야 함):
- [ ] 확장 설정의 **검색광고 API 키 3종** 교체 → 112개 단어에 검색량이 붙고 우선순위가 정해진다
- [ ] **API 시크릿 3세트 재발급**(채팅·스크린샷 노출)
- [ ] 크롬 「Apple 이벤트의 JavaScript 허용」 **되돌리기**
- **Jino 결정 대기 (오래된 것)**:
- [ ] **카나리 대상** — A류(폴드/플립: 상품 축 깨끗·출시창 교란) vs B·C류(S계열: 기준선 김·상품 축 오염) vs **★파워링크(오늘 새로 부상 — 키워드·광고제목 둘 다 만질 수 있는 유일한 자리)**
- [ ] **D-NAO-114 인수 범위** — *"그래, 우리가 인수할꺼야"*(2026-07-28)가 3그룹이었나 68그룹이었나. **한 달째 미확인**
- [ ] **D-NAO-21 보정계수 전제** — 승인 당시 「채널 매출 100%를 광고가 견인」과 수학적 동치임을 인지했는가 [미상]

## 7. 새 세션 시작 프롬프트
```
/session-relay PAO 논의
```
