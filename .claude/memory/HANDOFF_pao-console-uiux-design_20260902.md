# HANDOFF — PAO 콘솔 UX/UI 설계서 (체인 `pao-uiux` n=3 · 세션 c72be2df)

**검증 명령:** `git show origin/main:docs/references/122_pao_console_uiux_design_20260902.md | wc -l` → **503**이어야 한다. 아니면 이 인계는 낡았다.

날짜: 2026-09-02 (KST) · 착수 09:10 · 종료 11:3x
트랙: `docs/tracks/active/track_pao-ui-ux.md` (**10/12 → 10/22**)
계약: `docs/contracts/CONTRACT_pao_console_uiux.md` (「PAO 화면 목표」)
산출물: `docs/references/122_pao_console_uiux_design_20260902.md` — 503줄·12절·체크박스 24·자리표시자 0

---

## §0 목적 원문 (발명하지 말 것 — Jino 발언 그대로)

> (09:10) "PAO의 UX/UI를 명확히 작성하고 지나가자"
> (09:32) "내가 원하는걸 논의해보고 결정을 하자"
> (09:37) "PAO를 통해서 내가 보고 싶은건 실적, 기능들에 대한 셋팅이야. 이건 AVA와 종합적으로 이야기가 될 수 있도록 대화창도 있었으면 좋겠어."
> (09:44) "손도 대야지. 지금 AVA광고총괄목표 세션에서 그 작업을 진행중이거든."
> (09:51) "«어제 누가 무엇을 바꿨고 그 결과가 어땠나»까지 한 화면에서 보고 싶어"
> (09:57) "1. 광고그룹 레벨에서 PAO에 맞기는걸 On/Off하도록 스위치를 달자. 2. 광고캠페인, 광고그룹 레벨에서 우리가 가지고 있는 모든 기능의 운영상황과 성과를 볼 수 있게 해줘. 3. 이건 2-1인데, 제외키워드 셋팅의 In/out 현황도 보여야해"
> (10:01) "일단 그렇게 진행을 하자. 그리고, 불편하면 수정하자"
> (10:23) "보는건 모두 보고 싶어. 그걸 한번에 다 튀어나오지않게 sorting해서 보여지게 해줘"
> (10:31) "가장 범위가 넓은곳부터 디테일한 곳으로 들어가게 구성해줘"
> (10:38) "1. 크게 움직이는 곳 / 2. 후자 / 3. 아니. 제안카드 기본 입구는 실적대비 제안이 되어야 할 거 같은데?"

---

## §1 한 일

**PR #661 머지 `008f0b2c`** · CI **3/3 실통과**(py3.10 8m46s · py3.14 11m23s · frontend 1m38s) · `--force` 미사용 · 멈춘 단계 없음.
**코드 0줄 · prod 배포 0회** — 계약 §3 금지선대로.

기획은 **Fable 상주 파트너 + Opus 릴레이**로 돌렸다. ★그 슬롯을 내가 비워 뒀던 것을 Jino가 잡았다(09:32) — 그전까지 전부 Opus 인라인이었다.

---

## §2 ★실측이 지시 3건 중 2건을 뒤집었다 (이 세션의 본체)

- **①광고그룹 On/Off 스위치 — 이미 달려 있다.** `NaverAdScope.tsx:719-722`가 `putPaoScopeAdgroup({campaign_id, adgroup_id, role, enabled})`를 부르고, 백엔드 `PUT /scope/adgroup`(upsert)·`DELETE`(되돌리기)가 다 있다. **못 쓴 이유는 그 화면이 상단 탭바 8개에 링크가 없어서**다. ⇒ 설계서는 이걸 「신설」이 아니라 **「도달과 진실」**로 다룬다.
- **③제외 In/Out — 백엔드 완성, 프론트 호출 0.** `GET /search-term/exclusion-slots`(`naver_ad.py:2867`) + `exclusion_slot_usage.py`가 있는데 `api.ts`·페이지 어디서도 안 부른다.
- **②만 진짜 새것**(캠페인·광고그룹 레벨의 기능 격자).

★교훈: **「달자」는 「없다」가 아니다.** 지시를 신설로 읽기 전에 grep 한 번이 두 벌 구현을 막는다.

---

## §3 ★기능 그레인 21개 전수 실측 (미확인 0)

엔티티 나무가 **3층이 아니라 4층**이다(계정·캠페인·광고그룹·키워드/검색어/소재).

- **계정 전역 5**: 가드레일(KV 1행·`key` unique) · 확장버킷(집계 1건) · 키워드 위생(집계·목록 없음) · 액셀 게이트(**DB를 안 읽는 메타집계**) · Ava 위임(제안 유형 축)
- **캠페인 6**: 예산 변경(**캠페인만** — 타 단위 fail-closed) · 관할①② · 모드/공격성 · 손실 정책 · 악순환 감지
- **광고그룹 5~**: 관할③ · 검색어 제외(슬롯 70칸) · 제외키워드 등록 · 쇼핑그룹 BEP 미달 · 입찰/잠금의 그룹 변형
- **그룹 아래 5~**: 출혈 키워드 · 굶는 승자 · 제외 후보 검색어 · 입찰/잠금의 키워드 변형 · 소재 입찰 · 바닥대기 파워링크분

★★**총이익 축이 광고그룹 아래에서 끊긴다** — BEP가 **상품별** 산출(`bep_calculator.py:1-2`)이라 키워드·검색어 층엔 총이익 절대액이 없다. 있는 건 비용과 보정ROAS이고, 코드의 출혈 키워드 보드가 이미 **비용순**이다(`account_diagnosis.py:113`). ⇒ 넷째 층 정렬은 광고비순이고, **「정렬: 광고비순 — 이 층엔 총이익이 없다」를 목록 머리에 상시 표시**한다.

★**정렬 키 하나가 원리적으로 불가능**: 제안의 `expected_effect`가 **수치가 아니라 서술 텍스트**(`models.py:2915`)이고, 그건 실수가 아니라 설계 결정이다(*"「예측값 대 실측값」 수치 비교는 하지 않는다"*, P2-S3). ⇒ 제안 목록은 **대상 엔티티의 정렬값 상속**으로.

★★**콘솔 어디에도 「PAO」라는 말이 없다** — `OptimizerSwitch.tsx:19-21`이 `ours`를 「**우리 MOP**」, `mop`을 「**원본 MOP**」로 부른다. 경쟁 도구 이름 하나가 **두 뜻**으로 쓰인다(`none`은 「수동」으로 이미 멀쩡). 렌더 지점은 **커맨드 센터** `/naver-ad` 하나(`NaverAdCommandCenter.tsx:16, :79`).

---

## §4 Jino가 확정한 것 7

세 방(실적·셋팅·Ava 대화창) / Ava는 **손도 댐**(구현은 남의 소관) / 실적 = **돈 축 + 행위 축 한 화면** / 넷째 층까지 **다 보되 정렬로 절제** / **넓은 범위 → 디테일** 진입 / 정렬 맨 위는 **크게 움직이는 곳**(액셀·브레이크 대칭) / 제안은 **실적 행에 딸린 넷째 칸**.

---

## §5 완료 QA (원문 그대로 — 미달·부분 포함)

판정(계약 §4): **부분달성** — 10항목 중 9개는 «코드 대조»까지 통과. ⑧은 문서가 반복 주장한 「11개」가 실측(10개)과 불일치 (11:0x KST)
판정(Jino 지시 원문): **달성** — 발언 7개 전부에 대응하는 설계 답이 있고, 계약이 원문을 옮기며 뜻을 바꾼 왜곡 없음 (11:0x KST)
판정(트랙 궁극 목표): **부분달성** (11:0x KST)
판정(종합): **부분달성** (11:0x KST)

### 재판정 1회 (⑧·트랙 축만)

판정(계약 §4) [재판정]: **부분달성** — 지정 리터럴은 0건이나 **「11화면」 2곳 잔존** + 수정 커밋 **미푸시**라 PR 표면엔 오류 그대로 (11:0x KST)
판정(트랙 궁극 목표) [재판정]: **부분달성** — 앞 QA의 「커밋·PR 부재」는 재실측으로 뒤집힘(PR #661 OPEN·10/22·확인줄 실재). 다만 산출은 설계서 1장·코드 0줄·배포 0회 (11:0x KST)
판정(종합) [재판정]: **부분달성** (11:0x KST)

★재판정 지적 2건은 그 뒤 수리(`85f29a16`·`ef52ca1e`)했으나 **판정은 안 낮췄다.**

---

## §6 ★내 오류 2건 — 둘 다 «별도 기»가 잡았다

1. **서브컴포넌트를 화면으로 셌다.** `NaverAdProposalForm.tsx`(246줄)는 `App.tsx`에 라우트 **0건**인데 화면으로 세어 **10→11개 · 7,369→7,615줄**이 됐고 세션 내내 Jino에게 그 숫자를 말했다. ★**설계서 §2-3 표 자신은 처음부터 10행이었다** — 저자는 실측했는데 서술은 내가 준 숫자를 물려받았다. **내가 준 숫자를 아무도 다시 안 셌다.** (완료 QA가 잡음)
2. **수정을 리터럴로 찾고 개념으로 안 찾았다.** `11개`·`7,615`만 grep해 **`11화면` 2곳**을 놓쳤고, 하필 하나가 «10행 표를 이고 있는 제목»이었다. **push도 안 해** PR엔 수정 전이 걸려 있었다. (재판정이 잡음)

⇒ **일반형: 내가 상류에 흘린 숫자는 하류가 검산하지 않는다.** 그리고 **수정 범위는 리터럴이 아니라 개념으로 잡아야 한다.**

---

## §7 다음 세션

**설계서 §7½ 1단계 — 「도달과 이름」**(한 세션):
- 상단 탭바에 `/scope`·`/exclusion-list` 링크 추가(탭 8→10 잠시 증가 — 최종형의 역방향이지만 도달이 먼저다)
- `ours`→「**PAO**」 · `mop`→「**제3자(대행사)**」 개명(`OptimizerSwitch.tsx` + 확인창 + `CoverageBar`). `none`=「수동」은 **그대로 둔다**
- **링크·문자열뿐이라 데이터 경로 무접촉.** 합격 표면: 탭바에서 두 화면에 클릭으로 닿는다 · 커맨드 센터 라벨·확인창·title에 'MOP' 0건

★**3단계는 09-15 «전»에 끝나야 한다** — 그 뒤엔 결과 칸이 더는 안 비어서 「빈 칸이 무엇을 말할지」 규약이 무의미해진다.
★4단계는 선행 소관 있음(`pao_scope_roster.py:351` 진리표 수리 — 네이버 광고 최적화 트랙). 수리 전이면 값 옆에 「(진리표 미적용 — 행 값)」.
★6단계는 선행 소관 둘(Ava의 손 = `ava-광고총괄`·AI_office / 주체 판정에 Ava 추가) — 그래서 마지막.

---

## §8 ⛔ 무접촉 (다음 세션도 유지)

- **`ava-광고총괄` n=8**(cb0ed825 · 저장소 **AI_office** · `end_kst` 비어 있음) — Ava의 손·권한 경계는 전부 그쪽 소관.
- **`pao-논의` n=81**(14873038 · 주기 구조 감사).

## §9 이월 (스코프 밖 — 기록만)

- ↗️ `pao_scope_roster.py:351`이 `in_scope`를 진리표가 아닌 행 값으로 계산 → 소관: `track_naver-ad-optimization.md`
- ↗️ 프론트 호출 0인 엔드포인트 5개 — `bm/agency-ops`·`bm/snapshot`·`bm/benchmark`·`search-term/executions/import`·`search-term/exclusion-slots`(마지막은 이번 설계서가 화면 자리를 지정) → 소관: 같은 트랙
- ⚠️ `MERGE_WAIT` 기본 600초가 또 부족했다(PR #662) — **6번째**. `--force` 대신 `MERGE_WAIT=1500`으로 재시도가 정답.

## §10 착지

PR #661 머지 `008f0b2c` 완주. 등록부 마감 PR #662는 CI 대기(`MERGE_WAIT=1500` 재시도 중) — 멈추면 재개 명령은 `MERGE_WAIT=1500 bash scripts/safe_merge.sh 662`.
