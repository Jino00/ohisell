# 세션 인수인계: D-NAO-197 ③ 매체 블랙리스트(A5)·PC/모바일(A6) 적재 완료 (D-NAO-201)

> 저장일시: 2026-08-19 09:5x KST · 저장소 **main** · **origin 미푸시 커밋 = 8개**(내 7개 + 병행 세션 1개, §7 참조 — **push 하지 말 것**, 이 문서를 쓴 세션도 push 안 함)
> 트랙: **PAO(파오)** — `docs/tracks/active/track_naver-ad-optimization.md`
> 결정 **D-NAO-201** · 교훈 **#317 · #318** · 다음 번호 = **D-NAO-202 / 교훈 #319**(next_ids.sh로 재확인할 것)
> prod alembic head = **`agt1media2black3`** · PAO 정지 유지(`optimizer` 7/7 `none`, 이번 세션 배포 매니페스트로 재관측)

---

# 0. ★★두 게이트 — 매번 지킨다 (D-NAO-200, Jino 2026-08-19 07:50)

> *"너가 무슨 새로운 일을 시작하기 전에 이 목표에 맞는지 판단하고 진행해. 그리고 마지막에 이 목표가 완성되었는지 QA를 통해서 판단하고 마누리 하자"*

**「이 목표」 = §1-0의 Jino 11:38 원문.**

## ① 착수 게이트 — 새 일을 시작하기 **전에**
무엇을 하든 **먼저** 목표 대조 판정을 **출력하고** 진행한다. 3값:

| 값 | 뜻 | 같이 적을 것 |
|---|---|---|
| **적합** | D-NAO-197 범위 5개 중 하나이거나 그 선행 조건 | **어느 항목인지 번호** |
| **선행조건** | 범위 항목은 아니나 없으면 못 한다(예: prod 디스크) | **왜 없으면 막히는지 한 줄** |
| **밖** | 목표 밖 | `↗️ 스코프 밖` 자백 후 진행하거나, 목표를 바꿀 일이면 **새 계약**(Jino 승인) |

★**「이왕 보인 김에」로 시작하지 않는다.** 이 트랙의 표류는 전부 국소적으로 정당한 연쇄에서 왔다.

**이번 세션 이행**: 착수 시 **「적합 — D-NAO-197 범위 ③(매체 블랙리스트 A5/A6)」** 판정을 출력하고 진행(트랙 D-NAO-201 첫 줄). 계약은 Jino 08:2x 승인. 형식은 지켰다.

## ② 종료 게이트 — 닫기 **전에**
**별도 서브에이전트**(Sonnet high·읽기 전용)에 완료 QA를 맡기고 판정을 **①앵커 ②HANDOFF ③트랙** 세 곳에 남긴다. 어휘 5값(달성/부분달성/미달/목적전환/판정불능).

★**QA에 선판정 2개를 먼저 시킨다**(2026-08-18에 이 형식으로 돌려 잘 작동했다):
- ⓐ **합격기준이 계약을 덮는가**
- ⓑ **계약이 지시(11:38 원문)를 덮는가** — **진도 N/5**와 **층2 분모 M/36**을 병기시킨다

★**「완료」 ≠ 「모든 데이터」를 매번 명시**하게 한다. 이 트랙은 「완료됐나」에 **세 번 잘못 답했고**, 세 번 다 잡은 것은 **Jino의 재점검**이지 모델의 절차가 아니었다.
★**미달도 유효한 종결**이다. 미달인 채 닫고 판정 원문 그대로 적는다 — 성공만 적힌 인계는 홍보다.

**이번 세션 이행**: 종료 전 별도 Sonnet 읽기 전용 완료 QA를 09:42 KST에 1회 실행 — **「달성」·미달 0건**(§5). 형식은 지켰다. ★단 이번 세션도 QA가 완료 **범위**를 재심하지는 않았다(전역 규율대로 계약 승인 시점에 이미 확정된 것을 QA가 다시 여는 구조는 아님).

## 왜 결정으로 박았나 (승계)
전역 하네스(§1 앵커·§2 완료 QA)에 이미 있는 규율인데 **이 작업에서 두 번 새어 나갔다**:
①「13:27 상품메타+광고3축만」을 범위 상한으로 오독(D-NAO-196) ②매트릭스 36축을 「받아올 수 있는 것」의 목록으로 오독해 **분모가 틀린 채 「완료」를 셌다**(D-NAO-197). 둘 다 **착수 시 목표 대조가 없었던 것**이 원인이다.

---

# ★ 이 파일을 읽는 순서
1. **§1 — 다음 세션이 할 일**(진도 2/5, 남은 3개). 여기만 읽어도 착수할 수 있다.
2. **§6 — ★정정된 것**(이번 세션의 핵심 — 내 주장이 세 번 반증됐다).
3. 나머지는 참조: §2 착수 전 5분 · §3 확정 결정 · §4 이번 세션이 한 것 · §5 완료 QA · §7 이월·결정대기

---

# 1. 다음 세션이 할 일

## 1-0. 목적 (원문 — 새로 발명하지 말 것)

> *"성과로 나눈 등급에 대해서 광고, 스마트스토어에서 API로 받아올 수 있는 모든 데이터를 나누자"*
> — Jino 2026-08-18 11:38

**이 문장을 그대로 앵커 `목표:`에 복사한다.**

## 1-0b. 도착지 (D-NAO-199, Jino 2026-08-18 23:1x) — 변경 없음

> *"자료를 모두 받아서 fable이 성과 등급의 연관성을 찾아내고 스마트스토어 광고운영에 반영하자"*

**3단: ①수집(D-NAO-197 범위 5개) → ②Fable 연관성 분석 → ③광고운영 반영.**
②의 저자는 Fable로 규약돼 있다(Jino 08-18 12:30). ★**③은 별도 계약이다**(이유는 전 HANDOFF §7-2, 변경 없음 — PAO 완전정지·대행사 소유권 분리·전략은 Jino 몫 3조건 그대로).

## 1-1. 완료 정의 = D-NAO-197 (Jino 승인) — **진도 2/5**

| # | 항목 | 상태(2026-08-19 09:5x) | 다음 세션 비용 감각 |
|---|---|---|---|
| ① | 시간대·지역·매체(col7/8/9) | ✅완료(D-NAO-198, QA 달성) | — |
| ② | **연령·성별·관심사**(CRITERION) | 테이블 0개, `/ncc/criterion` 코드 호출 **0건**(이번 세션 착수 필독 실측으로 재확인) | 코드+마이그+배포+365일 백필. ★엔티티 스윕 482콜 전에 StatReport 벌크 경로 재확인 |
| ③ | **매체 블랙리스트**(A5/A6) | ✅**완료**(D-NAO-201, QA 달성) | — |
| ④ | 상품 메타(C10) | 미착수 | ⑤ 뒤에 — 순서 지킬 것 |
| ⑤ | 커머스 미개봉 75건 개봉 | 미착수(41/116=35.3% 그대로) | 문서 읽기 |

**진도 2/5.** 층2 분모(ref 73 §1-C, 매트릭스 개정 6 기준)로는 교차완료 **17/36**(개정 5의 15에서 +2).
★**「몇 % 됐나」엔 반드시 어느 층의 분모인지 병기한다**(§0-A 3층 깔때기: 표면 ⊃ 축 ⊃ 교차완료).
★**「완료」 ≠ 「모든 데이터」** — 5개를 다 채워도 층1 대비 N1~N8·C2/C3/C7/C8/C9/C12가 범위 밖이다. 이건 Jino가 승인한 명시적 축소이지 은폐가 아니다.

## 1-2. 착수 순서 — 비용이 정한다

### 1️⃣ ② CRITERION 365일 백필
- 엔드포인트 `/ncc/criterion/{ownerId}`(adgroup grain). 코드 호출 **0건**(이번 세션 착수 필독이 재확인 — 단 위임문에 「부분오류」로 적었던 배경 사실 1건이 있었다: 처음 CRITERION 상수를 코드에 있는 것으로 잘못 전제한 문구가 있었고, 실측이 0건임을 확인해 바로잡았다. §6-10 참조).
- **1년 소급 실증됨** — 연령·성별과 관심사가 여기서 열린다.
- ⚠️꺼 놓은 행은 행 0이라 365일 소급이 완전하지 않다.
- ★**벌크 경로 재확인 먼저**: StatReport `CRITERION`/`CRITERION_CONVERSION` 리포트 2종 — 엔티티별 GET 스윕(482콜/일 감각) 없이 리포트 1건으로 전 그룹 취득 가능성(ref 75 ADS §4-4, 응답 스키마·성공 여부 여전히 **미확인**). **엔티티 스윕을 짜기 전에 이 경로부터 재 볼 것.**
- 코드 + **마이그레이션** + 배포 + 백필 → `--migrate` 순서 강제.
- ★**시한부 자료 백필 안전 패턴**(D-NAO-198이 세운 것, 그대로 승계): **delete 대상은 「요청 범위」가 아니라 「원천이 실제로 준 날짜」다.** 범위 전체를 지우면 원천이 일시적으로 안 나온 날의 기존 적재분이 원본이 사라진 뒤에 지워지고, 소급 한도가 있는 자료에선 복구 불가다. CRITERION도 365일 한도가 있으니 같은 규율 필요.

### 2️⃣ ⑤ 커머스 75건 개봉 → 그 결과로 ④ C10
- ★**순서 지킬 것 — ⑤ 없이 ④ 하지 말 것.** C10 라벨 자체가 `[부분확인]`이다(커머스 116건 중 개별 문서를 연 것이 41건뿐, 공백이 상품 도메인에 몰림).
- 이전 개봉에서 라벨 어긋남이 4건 나왔다 — 안 열면 「상품메타가 층화에 쓸 만한가」조차 못 믿는다.
- 1차 출처: `https://apicenter.commerce.naver.com/llms/llms.txt` + endpoint별 개별 `.md`.

## 1-3. 새 테이블 grain을 정할 때 — 이번에도 다시 적는다(교훈 #315)
★**새 테이블 grain을 정할 때 ref 73(매트릭스)에서 그 축을 참조하는 셀을 먼저 grep**할 것. D-NAO-198에서 이 규율을 어겨 검색어 텍스트 소실이 늦게 발각됐다. ②(CRITERION)는 adgroup grain이 유력한데, 이 grain에 기대는 문서가 있는지 착수 첫 단계로 확인할 것.

## 1-4. 합격기준을 쓸 때
- **각 항목** ⓐprod 라이브 행(**행수·창 병기, 0행이면 미달**) ⓑ등급 교차 산출물 + 재현 SQL ⓒ매트릭스(ref 73) 반영.
- ⑤는 「몇 건 열었나 / 라벨 바뀐 것 몇 건」으로 잰다.
- ★**「전수 조사 완료」라고 쓰지 않는다** — 커버리지 자백 절 의무.
- ★완료 QA에 **선판정 2개**를 먼저 시킨다: 「합격기준이 계약을 덮는가」 + 「계약이 지시를 덮는가(진도 N/5·층2 M/36)」.
- ★**「편승하면 공짜」류 설계를 계약에 쓸 때는 편승 지점의 대상 그룹 수 ÷ 목표 모집단을 먼저 세어 같이 적는다**(교훈 #317, 이번 세션이 첫 사례).
- ★**한 사실을 표·파생 카운터·이벤트 원장 등 여러 곳에 쓸 때는 실패 경로가 전부에서 같은 방향(fail-closed)인지 확인**(교훈 #318).

---

# 2. 착수 전 5분

## 2-1. 환경·명령 (승계, 변경 없음)
- **로컬**: `/Users/jino/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (공유 메인, **main**)
- **prod 조회**(읽기 전용): `ssh -o BatchMode=yes sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db" < 로컬.sql`
  ★**인라인 heredoc은 따옴표가 벗겨져 SQL이 깨진다 — 반드시 로컬 `.sql` 파일 + stdin.**
- **prod에서 배포 코드 실행**: venv `/home/ubuntu/ohisell/backend/.venv/bin/python`
- **로컬에서 네이버 SA API 호출**: `.env` 명시 로드 필요(안 하면 403).
- **배포**: `scripts/safe_deploy.sh <파일…> [--migrate] [--restart]`
- **번호**: `./scripts/next_ids.sh` — 결정하면 즉시 트랙에 쓴다.
- **테스트**: `cd backend && python3 -m pytest tests/ -q -p no:randomly` (약 3분)
- ⚠️ GitHub Actions 결제 정지로 **CI 미가동**.
- ⚠️★**현재 작업트리에 이번 세션과 무관한 미커밋 변경이 있다**(§7 참조) — `git status`를 먼저 확인하고 손대지 말 것(공유 메인 폴더, 다른 세션 것일 가능성).

## 2-2. ★마이그레이션 순서 — 어기면 ingest가 통째로 침묵한다 (승계)
②는 스키마 변경이다. 이 앱은 **부팅 시 인프로세스 마이그레이션을 하지 않는다.**
→ `safe_deploy.sh <마이그파일> <코드파일…> --migrate --restart`.
★단 **신규 테이블만 추가하는 마이그레이션은 순서 위험이 낮다**(어겨도 「no such table」만 나고 기존 테이블 SELECT는 안 죽는다). ②는 신규 테이블만 추가할 가능성이 높지만, 그래도 순서는 지킨다.

## 2-3. ★함정 (승계 + 이번 세션 추가분)
1. **`__backfill__` 센티널** — 공용 필터가 없다. 배제 관용구를 쓰는 파일은 **43개**. 새 집계 SQL마다 다시 적을 것.
2. **인계 숫자는 실측 전엔 믿지 마라** — 이번 세션도 착수 필독 실측에서 배경 사실 1건이 정정됐다(§6-10).
3. **위임문의 「배경 사실」도 검증 대상**(교훈 #314).
4. **창 없는 distinct는 축의 정체가 아니다**(교훈 #316) — 이번 세션은 이게 **모집단(그룹 표본 vs 전수) 차원에서도** 재현됐다(§6-2, A6 「퇴화」 반전).
5. **grain 절삭은 로컬 결정이 아니다**(교훈 #315).
6. **「크론이 있다」 ≠ 「그 결정이 실행됐다」**.
7. ★신설(교훈 #317): **「경로가 존재한다」와 「커버리지가 충분하다」는 다른 명제다.** 편승 설계를 쓸 때 모집단 대비 비율을 반드시 센다.
8. ★신설(교훈 #318): **같은 사실을 여러 표에 쓸 때 실패 경로 방향이 갈리면, 정상 복구 뒤에도 거짓 기록이 남는다.** 적재 코드 리뷰 시 "이 사실이 몇 개의 표/컬럼에 쓰이나"를 먼저 센다.

## 2-4. 착수 시 확인 명령 (복붙용)
```bash
git branch --show-current && git status --short && git rev-list --count origin/main..main
./scripts/next_ids.sh
ls -t docs/references/*audit* | head -1   # 주기 구조 감사 7일 경과 여부(전역 §4)
```

---

# 3. 확정된 결정사항 (번복 금지)

1. **D-NAO-201** = D-NAO-197 ③ 매체 블랙리스트(A5)·PC/모바일(A6) 적재 계약(Jino 승인 08:2x). 완료·QA 달성.
2. ★**「추가 API 콜 0」을 포기하고 커버리지를 산다** — 편승(생존감시 08:25) 커버리지가 실측 24.6%였다. non-deleted 광고그룹 전건(1,013개) 매일 1콜(09:35 크론)로 전환. 번복하려면 그 24.6%를 다시 재는 근거가 필요하다.
3. **A6(PC/모바일)는 저장하되 「퇴화」로 기록** — 단 이 「퇴화」는 **SHOPPING에 한해서만 참**이다(WEB_SITE는 37.7%가 한쪽만 설정, §6-2). 다음 세션이 A6를 재론할 때 유형을 반드시 구분할 것.
4. **설계 = 일일 스냅샷이 아니라 현재상태(`naver_adgroup_target_current`) + 변경이력(`naver_adgroup_target_change`)** — prod 디스크 92% 제약, 타겟팅은 거의 안 바뀐다(533그룹 중 MEDIA_TARGET editTm이 2026년 이후인 것 83건).
5. **매체 코드의 동일성 확정**(ref 77): `MEDIA_TARGET`의 media id ≡ `naver_search_term_dim_daily`(dim_type='m', col9) 매체 코드. 인과 검정 기반(§4 참조), 반증 0. **이제 172일 성과와 소급 교차 가능.**
6. **소급이 원리적으로 불가능하다** — `/ncc/targets`는 「지금」만 준다. 개별 media 등재 시점은 [미상], 변경 원장 시작 = 최초 적재일(2026-08-19). 그 전 변경은 「없었다」가 아니라 「관측되지 않았다」.
7. **매트릭스 정본 = ref 73**(개정 **6**, `data/73_band_x_all_api/BAND_X_ALL_API_MATRIX_20260818.md`). 교차완료 15→17, 「호출가능·미적재」10→9(초판 이래 첫 감소), 미상 3→2.
8. **금지선**: `/ncc/targets` **PUT 금지**(읽기 GET만) · 프로브 실패 그룹의 블랙 행을 지우지 않는다(조회 실패≠블랙 소멸) · 매체 코드의 «뜻» 사전 만들지 않기(D-NAO-198 승계) · 「전수 조사 완료」 문구 금지.
9. **승계**: C3 반품 제외 · A2 요일·시간 제외 · fan-out 배분 안 함 · 코드 사전 안 만듦 · `naver_search_term_daily` 스키마 불변.

---

# 4. 이번 세션이 한 것

## 4-1. ref 77 — `/ncc/targets` 전수 실측·동일성 판정 (읽기 전용, 커밋 `33b82e81`)
- SHOPPING 172일 창 등장 307그룹 + 최근 30일 활동 그 밖 230그룹, 총 537건 프로브(200 OK 533·404 4).
- **동일성 판정 = 같은 코드 공간**: 집합 대조(B=48종·M=37종·∩17종)는 정황일 뿐이고, **인과 검정이 결정적**이다 — 블랙 (그룹,매체) 쌍 1,864건 중 송출 실적 있는 쌍 1건뿐, 그마저 등재 당일(2026-07-21) 노출로 해소. 같은 그룹집합의 비-블랙 쌍은 **197,835행·41,101,626원**. 반증 0.
- 부수: `targetTp`는 실재 4종뿐(`MEDIA_TARGET`·`PC_MOBILE_TARGET`·`RESTRICT_KEYWORD_TARGET`·`NON_SEARCH_KEYWORD_TARGET`) — ref 75 §4-3의 「GENDER/AGE/TIME_WEEKLY/REGIONAL도 포함」 주장은 533그룹 전수 0건으로 반증. `bidWeight`도 0건(targets 표면에서 배제 확정). 404 4건 = 전부 삭제된 광고그룹.
- 원자료 652KB 보존(`docs/references/data/77_targets_surface/`) — ref 73 #12가 「원 프로브 휘발로 재현 불가」로 [미상] 남긴 전례 반복 방지.

## 4-2. 적재·배포 (커밋 `6a9c77ba` → `c0f887d1` → `2ee5ef0f`)
**신설**: `NaverAdgroupTargetCurrent` · `NaverAdgroupMediaBlack` · `NaverAdgroupTargetChange`(models.py), 마이그 `agt1media2black3`(신규 테이블만), `naver_sa_ad_fetcher.get_adgroup_targets`(404를 예외 아닌 status로), `app/services/naver_ad/adgroup_target_ingest.py`, 09:35 크론(`_CATCHUP_ORDER` 제외 — 매일 전량 재관측), 테스트 **22종**(`backend/tests/test_naver_adgroup_targets.py`).

**적대 리뷰**: 1R **FAIL(P1 3건)**:
- P1-1(계약 금지선 직접 위반) — 블랙 «표»엔 fail-closed, «변경 원장»·`black_media_count`엔 fail-open. HTTP 500 1회로 `black_media_json` 9행이 None으로 새겨졌다 복구에 9행 더(18행 변화, 실제 블랙은 불변). **그 fail-open을 정답으로 박아 둔 테스트가 있었다**(교훈 #318).
- P1-2 — delete+insert 갱신 방식이 `first_seen_at`을 매 스윕마다 오늘로 리셋(재현: 42일 뒤 재적재 시 42일 이동). → 차집합 갱신(사라진 것만 삭제·새 것만 INSERT·잔존은 observed_at만 갱신).
- P1-3 — try가 fetch만 감싸 DB 오류 1건에 스윕이 통째로 죽고 요약 로그도 안 남음. → 루프 본문 전체 보호 + rollback + dedupe.
전건 수정 → 2R **PASS(P1 0)**. 2R가 새 결함(카운터 이중계상: try 확대로 ok/failed에 같은 그룹이 동시 계상) 발견 → `2ee5ef0f`로 배타 카운팅·`ok+failed==swept` 단언 추가.

**변이 주입**: 만든 쪽 8 + 리뷰어 8 + 재봉쇄 6 = **22종**. 미검출 1종은 SQLite TEXT affinity로 **원리적 미검출**(사유를 코드 주석에 남김).

**배포**: `safe_deploy.sh --migrate --restart`, `std1dim2axes3 → agt1media2black3`, 다운타임 0초.

## 4-3. 라이브 (2026-08-19 09:13:53 수동 스윕 + 09:35:00~09:38:38 정기 크론 첫 발화, 둘 다 관측)
- `naver_adgroup_target_current` **1,013행/1,013그룹**(창: 스냅샷, 2026-08-19)
- `naver_adgroup_media_black` **5,639행/965그룹/63코드**
- `naver_adgroup_target_change` **0행**(최초 적재라 정상 — 변경 이력은 다음 스윕부터)
- 스윕 결과 둘 다: **swept 1,013 · ok 1,013 · failed 0 · new 1,013**
- 인계·매트릭스의 「추가 API 콜 0·별도 스윕 불요」는 **부분오류**로 확정(§6-1) → 전수 일일 스윕 09:35 크론으로 전환.

## 4-4. ref 78 — 밴드 × 타겟팅 축 연관성 (Fable) + 매트릭스 개정 6
`docs/references/78_band_x_targeting_20260819.md`(207줄) + `data/78_band_x_targeting/`(160KB).
**핵심 = 「가른다」가 아니라 「못 가른다」 — 이번에도**:
1. A5(블랙 개수) raw 격차(band3 5 vs 나머지 6)는 **캠페인 유형 교락**이었다 — 유형 통제 시 WEB_SITE 평평, SHOPPING은 raw와 **반대 방향**(band1이 가장 낮음).
2. A6 raw 격차도 유형이 결정한다 — SHOPPING 99.4% 단일값(교차 불능) vs WEB_SITE 37.7% 한쪽만(진짜 변이). WEB_SITE 안에서 「band1 둘다 비율 최저」는 홀드아웃 양쪽에서 **방향만** 재현(크기 45%→59%로 불안정) → **[미검증 가설]**.
3. 단단하게 남은 것: 송출 비용 95.1%가 **아무도 차단하지 않은 17종 코드**에 흐른다(동일성의 배포된-표 재확인).
**F20 신설**(매트릭스): 유형 혼합 분모로 밴드를 비교한 **기존 발견(F13·F14 등)도 [미검사]**로 표시 — 재검은 별도 슬라이스.
매트릭스 개정 6: 층1·층2 불변(층2 36 그대로) / target 파싱 실사용 1/12→3/12 / 층3 광고 13/24→15/24(합 17/36) / 교차완료 15→17 / 호출가능·미적재 10→9 / 미상 3→2.

## 4-5. 교훈 신설
**#317**(「경로가 존재한다」≠「커버리지가 충분하다」) · **#318**(한 사실을 두 곳에 쓰며 한쪽만 fail-closed면 다른 쪽이 거짓을 영구 기록).

---

# 5. 완료 QA — 별도 Sonnet·읽기 전용, 2026-08-19 09:42 KST

## 5-0. 착수 게이트 판정 (§0-① 이행 기록)
착수 시 **「적합 — D-NAO-197 범위 ③」** 판정을 출력하고 진행. 계약은 Jino 08:2x 승인(트랙 D-NAO-201 첫 줄에 명시). 형식 준수 확인.

## 5-1. 선판정 2개
- **ⓐ 합격기준이 계약을 덮는가** = **덮음**
- **ⓑ 계약이 지시(11:38 원문)를 덮는가** = **덮지 못함** — 진도 **2/5**, 층2 **17/36**. 「완료」≠「모든 데이터」이고 문서가 그 구분을 스스로 자백하고 있어 정합.

## 5-2. 항목별 판정
- ⓐ **달성** — 라이브 `naver_adgroup_target_current` 1,013행/1,013그룹 · `naver_adgroup_media_black` 5,639행/965그룹/63코드 · `naver_adgroup_target_change` 0행(최초 적재라 정상) 판정기가 직접 재관측. alembic head `agt1media2black3` 확인. 배포 3파일 sha256 = 로컬 HEAD 일치.
- ⓑ **달성** — ref 78 재현 SQL을 판정기가 prod에 **직접 재실행**해 자릿수까지 일치.
- ⓒ **달성** — 매트릭스 개정 6 반영 확인.
- **금지선 8종 전건 준수**(PAO 정지 `optimizer='none'` 7/7·`auto_operate=0` 7/7 포함) — `/ncc/targets` PUT 호출 0건도 코드 정적 확인.
- ★**판정기가 09:35 정기 크론의 첫 발화를 실시간 목격**(09:35:00→09:38:38, swept 1,013·ok 1,013·failed 0) — 09:13 수동 스윕과 **독립 재현**.
- **종합: 달성 · 미달 0건.**

## 5-3. ★QA가 확인하지 못한 것 — 2건 (「달성」에 가려지지 않게 그대로 남긴다)
1. **fail-closed 실패 경로가 라이브에서 한 번도 발동하지 않았다.** 09:13 수동 스윕·09:35 정기 크론 둘 다 `failed=0`이라, P1-1·P1-2 수정이 실제 실패 상황에서 올바르게 동작하는지는 **코드 근거(테스트 통과)만 있고 라이브 관측은 없다.** 다음 실패가 처음 발생할 때가 첫 라이브 검증이다.
2. **N6(PostgreSQL 이행) 트리거 리스크가 이월됐다** — 적대 리뷰 2R가 `_diff_fields`의 `str()` 비교를 지적했고, SQLite에서는 문제없지만 PostgreSQL 이행 시 타입 비교가 달라질 수 있다는 이월이 있다(이전 세션부터 승계된 항목, 이번 세션에서 재확인만 하고 손대지 않음).

---

# 6. ★정정된 것 (이번 세션의 핵심 — 내 주장이 세 번 반증됐다)

| # | 무엇을 주장했나 | 무엇이 정정됐나 | 근거 |
|---|---|---|---|
| 1 | 인계·매트릭스 개정 5: 「추가 API 콜 0·별도 스윕 불요로 개통 가능」 | **부분오류.** 편승 지점(08:25 생존감시)은 제외 원장에 행이 있는 131그룹만 돈다 — 성과축 307 중 116(37.8%)·계정 전체 대비 **24.6%**. 「읽기 경로가 존재한다」(참)에서 「커버리지가 충분하다」(무측정 도약)로 건너뛰었다 | 교훈 #317, 실측(2026-08-19) |
| 2 | ref 77(533그룹 표본): A6 (T,T)가 98.5%라 「퇴화·교차 무가치」 | **창을 넓히자 뒤집혔다** — 1,013 전수에서 78.0%. 원인 = 캠페인 유형 혼합(SHOPPING 337 중 335=99.4% 단일값 / **WEB_SITE 486 중 183=37.7%가 한쪽만**). Jino 결정 ②(「퇴화로 기록」)의 근거 숫자가 표본값이었다 — 결정 자체는 유효하되 **「퇴화」는 쇼핑에 한해서만 참** | 교훈 #316의 재현, ref 78 §3-1 |
| 3 | 밴드 raw 표(A5 격차 band3 5 vs 나머지 6)가 밴드 성과와 관련된 서사로 읽혔다 | **캠페인 유형 교락이었다**(Fable, ref 78 §2-2) — 유형 통제 시 WEB_SITE 전 밴드 평평, SHOPPING은 **raw와 반대 방향**(band1이 가장 낮음). **F20 신설**: 유형 혼합 분모로 밴드를 비교한 **기존 발견(F13·F14 등)도 [미검사]**로 강등 | ref 78 §2 |
| 4 | 적대 리뷰 1R 결과 | **FAIL(P1 3건)** — 특히 **P1-1이 계약 금지선을 직접 위반**했다(블랙 표엔 fail-closed, 변경 원장엔 fail-open → HTTP 500 한 번에 「블랙이 사라졌다」가 9행+복구 9행 문자로 새겨짐). **그 결함을 정답으로 박아 둔 테스트가 내 파일에 있었다**(교훈 #318) | 커밋 `c0f887d1` |
| 5 | 내 교차 SQL 초판(q3, 폐기) | **인과의 결과를 분모로 착각**했다 — 성과축엔 송출된 칸만 있어 차단 매체는 정의상 행이 0. 차단 쪽 분모는 블랙 표에서 세어야 함 → q3b로 정정, 폐기본도 사유와 함께 보존 | ref 78 §4 |
| 6 | `models.py` `NaverSearchTermDimDaily` docstring: 「매체 id는 6자리, MEDIA_TARGET과 동일성은 미확인」 | **4~6자리 혼재**(`8753` 4자리·5자리 6종·6자리 30종, 172일 창 37종)로 확정, **동일성도 확정**(둘 다 정정) | ref 77 §5-ⓐ |
| 7 | ref 75 §4-3: 「`/ncc/targets` 응답에 GENDER/AGE/TIME_WEEKLY/REGIONAL도 포함돼 온다」 | **533그룹 전수 0건** — Swagger 구조 추론이었다, 실측과 어긋남 | ref 77 §5-ⓑ |
| 8 | `/ncc/targets` 404 `{"code":1018}` 4건의 정체 | **삭제된 광고그룹**(4/4가 `naver_entity` `status='deleted'`) — 권한 문제로 보였지만 아니었다 | ref 77 §6 |
| 9 | 「전수 = 533그룹」 | 실제 전수는 **1,013**(non-deleted adgroup `on` 795 + `off` 218). 533은 최근 30일 활동 분모였다 | 트랙 D-NAO-201 §520 |
| 10 | 착수 필독 실측: 인계 항목 배경 사실 검증 | 인계 19건 유효 / 유령 0 / 부분오류 2(둘 다 08-19 07:40 크론 정상 증분 — 사고 아님). ★**내 위임문의 배경 사실 1건이 틀렸다** — CRITERION 상수를 코드에 존재하는 것으로 전제한 문구가 있었으나 실측은 0건이었다(교훈 #314가 자기 자신에게 걸린 사례) | 착수 필독(Sonnet 위임), 2026-08-19 08:0x |

---

# 7. 이월 · 결정 대기

## 7-1. 실측값 (이 문서를 쓰며 직접 재측정)
- **prod 디스크 = 92%(사용 89G/97G, 여유 8.0G)** — `ssh sellc.ohitech.co.kr "df -h /"` 2026-08-19 09:5x 재측정. **직전 HANDOFF와 동일한 92%** — 이번 세션이 신규 테이블 3개를 추가했지만(스냅샷+블랙+변경이력, 최초 적재라 아직 작음) 눈에 띄는 증가는 없었다. ②(CRITERION 365일 백필)가 또 테이블을 늘리므로 **착수 전에 재측정할 것**.
- **origin 미푸시 커밋 = 8개**(`git rev-list --count origin/main..main`, 2026-08-19 09:5x 재측정) — 이번 세션 7개(`33b82e81`·`6a9c77ba`·`c0f887d1`·`2ee5ef0f`·`a92d8c15`·`3123249f`·`3732088d`) **+ 병행 세션 `ohiselling-ee`의 `be8aeeba`**(04-audit, D+20 정지 상태 감사 — 내 파일과 겹침 0). ★**push하면 그 세션 커밋도 같이 올라간다** — 이 문서를 쓴 세션은 push하지 않는다. 다음 세션이 push할 땐 `be8aeeba`가 자기 것이 아님을 인지할 것.
- **작업트리에 이번 세션·D-NAO-201과 무관한 미커밋 변경이 있다**(`git status --short`, 2026-08-19 09:5x): `backend/app/clients/naver.py`(M) · `backend/app/services/sync_service.py`(M) · `backend/tests/test_naver_last_changed_pagination.py`(신규, untracked). 커머스 `last-changed-statuses` 폴링 관련으로 보이나 이 세션이 만든 것이 아니다 — **손대지 않았고 커밋도 하지 않았다.** 공유 메인 폴더이므로 다른 세션(또는 같은 세션의 이전 슬라이스)의 것일 가능성이 높다. 다음 세션은 `git status`로 재확인하고, 자기 것이 아니면 그대로 둘 것.
- **`test_vendor_item_axis` 기존 실패 1건** — 다른 트랙 부채, 최소 08-11부터 승계(직전 HANDOFF 확인 기준). 이번 세션은 전체 스위트를 재실행해 다시 세지 않았다 — **이 문서의 숫자는 직전 세션 기록의 승계이지 이번 세션 재측정이 아니다.**

## 7-2. 이월 (다음 세션이 밟을 수 있는 것) — 직전 HANDOFF §7-1에서 유효/해소 구분
| 항목 | 상태 |
|---|---|
| prod 디스크 92% | **유효**(위 재측정으로 확인, 수치 불변) |
| `_cell_daily` distinct 일수 170 vs 마진 171 | **유효**(이번 세션 손대지 않음, 원인 미확인 그대로) |
| 「브랜드어 × 시간대」 원료 부재(D-NAO-198이 남긴 것) | **유효**(이번 세션과 무관, 별도 계약·180일 시한 그대로) |
| `_safe_int` 콤마 표기 0-낙하 | **유효**(관측만 하기로 한 결정 불변) |
| 07:40 잡의 GET 2배 | **유효**(이번 세션 손대지 않음) |
| 적대 리뷰 2R 이월(`_diff_fields` str() 비교, N6) | **유효 — 이번 세션 QA §5-3②에서 재확인** |
| `last_ok_at` 컬럼 부재 | **유효**(이번 세션과 무관) |
| `/ncc/targets` 중복 media 코드 여부 [미확인] | **유효** |
| 09:35 슬롯·스윕 실소요·429율 | **해소** — 이번 세션이 09:35 정기 크론 첫 발화를 라이브로 목격(§5-2) |
| 「브랜드어×시간대」 관련 grain 이슈 | 변경 없음 |

## 7-3. ref 78이 [미상]으로 남긴 것 (신규)
- 개별 media의 등재 시점 — [미상], 소급 불가(§3 결정 6).
- 블랙리스트 등재 주체(우리 vs 대행사) — 이 표면엔 없음, 제외 칸 소유권 분리 논의(ref 65 §10)와 같은 축이나 별개 표면.
- 매체 코드의 «뜻» — 여전히 미상, 사전을 만들지 않는다.
- 차단만 43종(송출 0)의 정체 — 원래 트래픽 없음인지 계정 세팅상 안 나옴인지 판정 불가.
- SHOPPING band1의 낮은 블랙 중앙값(4)의 기제 / WEB_SITE band1의 한쪽-설정 편중 방향 — 둘 다 재현되는 관측이나 기제 원료 없음.
- WEB_SITE 한쪽-설정 183그룹의 설정 «의도»(성과 판단 vs 생성 시 기본값) — 변경 이력이 쌓이기 전엔 못 가른다.

## 7-4. Jino 결정 대기 (승계, 변경 없음)
| # | 항목 | 배경 |
|---|---|---|
| 1 | A3 회색 토큰 4건 | 「버디」398,874원 외 3건, 총 영향 0.54%, 데이터로 못 가른다 |
| 2 | `optimizer` 해제 범위 · 대행사 제외 칸 소유권 · ref 65 §10 나머지 | 집행에 걸려 별도 계약감 |
| 3 | prod 디스크 92% | 회수 후보가 전부 다른 트랙 자산 |
| 4 | N3 AdExtension | 매트릭스 「API 생성 불가」와 공식 문서 CRUD 6+리포트 2종 상충, 진위 [미상] |

## 7-5. ★③「광고운영 반영」이 왜 별도 계약인가 (D-NAO-199, 승계)
1. PAO 완전 정지(`optimizer='none'` 7/7) — 해제 범위는 Jino 결정 대기.
2. 대행사와 제외 칸 소유권 분리가 선행 조건.
3. 「광고 전략 추천 금지 — 전략은 Jino 몫」이 트랙 규약.
4. ②가 ③을 자동으로 정당화하지 않는다 — 연관성 발견 → 집행 사이 홀드아웃 재현 필요. **이번 세션 ref 78도 그 규율로 A5 raw 발견 1건을 실제로 강등했다**(F20 신설).
5. ①은 아직 총이익에 못 닿는다 — 시간대 축(D-NAO-198 승계)은 물론, 이번에 적재한 A5/A6도 전환·매출 없이 **설정 스냅샷일 뿐**이라 「차단이 옳았는가」를 판정할 원료가 아니다.

---

# 8. 새 세션 시작 프롬프트

```
.claude/memory/HANDOFF_media-blacklist-ingested_20260819.md 읽고 이어서 작업해줘.
§1(다음 세션이 할 일)과 §6(★정정된 것)을 먼저 볼 것.

★★§0의 두 게이트를 먼저 읽을 것(D-NAO-200): ①새 일 착수 «전»에 목표 적합성 3값 판정을 출력하고 진행
  ②닫기 «전»에 별도 Sonnet 완료 QA — 선판정 2개(진도 N/5·층2 M/36 병기) 먼저.
★목표는 §1-0의 Jino 11:38 원문을 그대로 앵커 `목표:`에 복사 — 새로 발명 금지.
★도착지는 §1-0b(D-NAO-199): 수집 → Fable 연관성 → 광고운영 반영. ③은 별도 계약(§7-5).
★완료 정의는 D-NAO-197로 확정, 현재 진도 **2/5**(①③ 완료).
  「몇 % 됐나」엔 반드시 어느 층의 분모인지 병기할 것(층2 = 17/36).
  ★「완료」 ≠ 「모든 데이터」 — 5개를 다 해도 층1 대비 범위 밖이 남는다.

착수 순서는 §1-2대로 — 비용이 정한다:
  1) ② CRITERION 365일 백필 ★엔티티 스윕(482콜 감각) 짜기 전에 StatReport CRITERION/
     CRITERION_CONVERSION 벌크 경로부터 재 볼 것(응답 스키마·성공 여부 여전히 미확인).
  2) ⑤ 커머스 75건 개봉 → 그 결과로 ④ C10. ★순서 지킬 것

★②는 마이그레이션 포함 → safe_deploy.sh --migrate 순서 강제(§2-2).
★새 집계 SQL마다 `__backfill__` 배제를 다시 적을 것(공용 필터 없음, 43개 파일이 각자 인라인).
★새 테이블 grain을 정할 때 ref 73에서 그 축을 참조하는 셀을 먼저 grep할 것(교훈 #315).
★숫자를 적을 땐 창을 병기할 것(교훈 #316 — A6 「퇴화」가 표본 533→전수 1,013에서 98.5%→78.0%로 뒤집혔다).
★「편승하면 공짜」류 설계는 편승 지점의 대상 그룹 수 ÷ 목표 모집단을 먼저 세라(교훈 #317).
★한 사실을 여러 표에 쓸 때 실패 경로 방향이 전부 같은지 확인하라(교훈 #318).
★인계 숫자는 실측 전엔 믿지 말 것 — 이 세션도 위임문 배경 사실 1건이 틀렸다.
★prod 디스크 92%다(§7-1, 이번 세션 재측정 — 불변). ②가 또 테이블을 늘린다 — 착수 전에 재확인할 것.
★origin 미푸시 8개 중 1개(`be8aeeba`)는 병행 세션 것이다 — push 시 함께 올라간다는 것을 인지하고 진행할 것.
★작업트리에 이 세션과 무관한 미커밋 변경 3개 파일이 있다(§7-1) — 손대지 말고 git status로 먼저 확인할 것.
```
