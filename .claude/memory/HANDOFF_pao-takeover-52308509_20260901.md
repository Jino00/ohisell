# HANDOFF — 52308509를 PAO가 돌리게 한다 (2026-09-01)

> 체인 `배송비-자` **n=2** (같은 세션의 두 번째 목표) · 세션 `d02f7c89` · 14:4x ~ 17:0x KST
> 목표이름: **PAO 인수 목표 (52308509)** · **D-NAO-284**
> 🔁 **목적 전환 선언 있음**: 「배송비 자(尺) 정합」(종결·판정 보존) → 이 목표

## 0. Jino 지시 원문 (다음 세션은 발명하지 말고 옮길 것)

> **2026-08-31**: *"그룹상품번호 52308509 제품을 pao광고 운영하고 싶어"*
> **2026-09-01 14:4x**: **"나는 그룹상품 52308509 pao 광고 돌리고 싶다고"**
> **15:2x**: **"진행해"**

★Jino가 세 번 말하는 동안 내가 세 번 되물었다(범위 메뉴 2회 + 계약 승인 1회). *"또 계약을 왜 쓴느데?"*가 그 지적이다. **두 번째 발언이 이미 승인이었다.**

## 1. ★인계 2건이 라이브로 반증됐다 — 이게 이 세션의 첫 산출물이다

| 인계 주장 (📄) | 실측 |
|---|---|
| 「52308509는 **어떤 광고 소재에도 실린 적 없다**」 | **틀렸다.** `13687558206·207·208`이 `● 01. 갤럭시_지문방지_사생활`(SHOPPING·on)에 소재로 등록돼 **집행 중**이었다 |
| 「optimizer='none' **9/9** · auto_operate **0**」 | **틀렸다.** `● 01. 갤럭시_지문방지_TPU`가 `optimizer='ours'`·`auto_operate=1`로 계정 광고비의 **21.6~26.8%**를 쓰고 있었다 |

**원인**: n=1이 **대행사 변경이력**(`change_log raw`)을 뒤졌고 **소재 매핑 테이블**(`naver_adgroup_product`)은 안 봤다 — 다른 표에 다른 질문. 나는 그걸 검증 없이 이어받아 **세션 전체를 틀어놨다**(광고 밖인 `209`·`210`을 합격기준 ①의 표적으로 삼았고, 그래서 ①이 미달로 닫혔다).

⇒ **교훈**: 「없다」는 주장은 **어느 표에서 없는지**를 같이 적어야 한다.

## 2. PAO 21일 무집행의 원인 — 절반이 시계 버그였다 (D-NAO-284)

`_bleeding_hold_reason`이 `expected_asof = today − 1`을 요구하는데 그 as-of를 만드는 크론은 **08:30 KST**(`"30 8 * * *"`). ⇒ **KST 00:00~08:30은 원리적으로 만족 불가능**이고, 시간별 레인 24회 중 **9회가 매일 통째로 죽었다.**

| 라이브 실측 (7일) | |
|---|---|
| 「소급채점 stale」 차단 | **KST 00~08시에만 매시 정확히 109건 · 09시 이후 0건** |
| 비중 | stale 981건의 **전부** · 전체 차단 2,041건의 **48%** |
| retro 자체 | **하루도 안 빠졌다**(08-20~08-31 전일 생성) ⇒ 데이터 실패가 아니라 **시계 불일치** |

★★**부수 발견 — 두 표의 시각 기준이 다르다**: `ops_diary_entries.created_at`은 **UTC**, `scheduler_state.last_run_at`은 **KST**. 확정 근거: 같은 실행을 diary가 `05:20:30`, scheduler가 `14:20:30.745132`로 적는다(같은 초·9시간 차). **UTC로 읽으면 「저녁 15~23시에 막힌다」로 오독된다** — 내가 처음에 그렇게 오독했다.

### 완화가 아니라 «정정»으로 고쳤다
`_RETRO_ASOF_MAX_LAG_DAYS`는 **1 그대로**(주석이 *"이 값을 키우는 것은 안전 완화다"*라 금지). 대신 `_expected_retro_asof`가 «지금 존재할 수 있는 가장 신선한 as-of»를 정의한다 — 08:30 이후면 `today−1`(없으면 진짜 실패 → **fail-closed 유지**), 이전이면 `today−2`.
★`_RETRO_CRON_HOUR/MINUTE_KST`는 scheduler의 `"30 8 * * *"`와 **짝이다** — 갈라지면 그날부터 조용히 창이 다시 생긴다.

## 3. 캠페인 인수 — 블라스트 반경을 좁혔다

`naver_campaign_settings` 1행 + `naver_adgroup_scope` **3행**(prod DB 직접 쓰기, **git에 없다**). 쓰기 시각 **16:03:12 KST**.

★그 캠페인은 광고그룹 **26개 · 하루 57,613~89,585원**이라 통째 인수는 요청보다 크다 ⇒ 스코프 행으로 **3개만 PAO · 23개 대행사 유지**(Jino가 8/28 폴드8 와이드에 쓴 방식).
★`base_daily_budget` **미시드 = 예산 무접촉, 입찰만**. QA가 추가로 확인: `adgroup_scope.campaign_level_allowed_now()`가 「스코프 행이 있으면 캠페인 레벨(예산) 액션 무조건 불허」라 **예산 레버는 이중으로 막혀 있다**.
★**되돌리기: `optimizer='none'` 한 줄.**

## 4. ★★라이브 결과 — PAO가 실제로 돌렸다

```
16:03:12 KST  인수 쓰기
16:20:35 KST  PAO 집행 — 17분 뒤
```
- 소재 `nad-a001-02-000000558107660` (`mallProductId=13687558206` = **52308509 그룹의 4매입 폴드8**)
- 입찰가 **910 → 1,040원** (+14.3%) · actor `hourly`
- 근거 원문: *「[시간당밴드] ROAS-UP(순위 무관, D-NAO-66) — 정착창 실측(정착창 보정ROAS **2.3095** >= 목표 **1.9554**)」*
- ★**직전 자동 입찰 집행 2026-07-30 01:20 ⇒ 33일 만의 첫 `update_bid` execute**(그 사이 유일한 execute는 08-11 `search_term_exclude`, actor=`console`=수동)

나머지 두 광고그룹은 `bleeding으로 판정됨`으로 차단 — **정당한 방어**이자 스코프가 의도대로 열렸다는 증거.

## 5. 완료 QA 판정 — 원문 그대로 (별도 Sonnet · 읽기 전용 · 16:5x)

```
판정(새 목표 합격기준 6항목): 부분달성 — ①②③④ 달성(전부 라이브 재현) ·
  ⑤ 판정불능(적대 리뷰 2R PASS가 자기보고일 뿐 저장소에 증거 없음) · ⑥ 미체크가 타당
판정(Jino 지시 원문): 달성 — 「돌린다」를 ①쓰기 권한 ∧ ②실집행 ≥1건으로 정의해 판정.
  지속 운영은 경과 25분이라 아직 판정 대상 아님
판정(트랙 궁극 목표): 부분달성 — M4에 정확히 해당하는 첫 실집행이나 합격 관측
  「1주 연속 무위반」이 원리적으로 아직 불가. 진행률 2/7 불변.
  ★M4 체크를 안 찍은 것이 북극성 원문과 정확히 부합
판정(종합): 부분달성
★「내가 재현해 본 만든 쪽의 주장 중 어긋난 것 — 없음」
```

★**⑤ 판정불능은 그 자리에서 상환했다** — 적대 리뷰 1R/2R 판정문을 **PR #631 코멘트**로 게시(`#issuecomment-5490682449`). 서브에이전트 판정이 저장소에 안 남는다는 것이 구조적 구멍이었다.

★**QA 신규 지적(그대로 보존)**: 북극성 §8-② 「대행사와 소유권 분리」에 대한 **이번 건의 명시적 Jino 승인은 없다** — 8/28 선례를 준용했다. QA 판단으로는 표류가 아니라 보수적 선택이고 가역적이라 §7 금지선 미저촉.
★**QA가 확인 못 한 것**: 「④자동화 38세션째 0」 연속 카운트의 정확성 · §7 대칭 숫자의 창 정의(QA가 다른 창으로 재구성 시 **1,915 vs 32** — 방향은 동일, 절대값은 다름).

## 6. 적대 리뷰

**1R FAIL(P1 1) → 상환 → 2R PASS(P1 미해소 0).** 판정문 전문은 PR #631 코멘트.

★1R P1은 **내가 이 diff에서 처음 넣은 라이브 시계 의존성**이었다. 종전 `today − lag`는 순수 함수라 100% 결정적이었는데, `_expected_retro_asof(today)`를 `now` 없이 불러 레인이 08:50을 주입해도 게이트가 벽시계를 읽었다. 리뷰어가 `kst_now`를 05:00으로 patch해 **codex 4R[P1] 안전 회귀 테스트에서 진짜 stale이 통과하고 bid_up이 집행되는 것**을 재현했다.
★1R P2-1: **표면 절단 변이 생존** — 이 작업의 표면은 화면이 아니라 `ops_diary_entries`의 사유 문자열인데, 테스트가 `"stale" in reason`만 봐서 사유를 「hold」로 뭉뚱그리는 변이가 178건 전건 초록을 통과했다.
★2R에서 리뷰어가 변이 2종 재주입해 각각 사망 확인 + `now` 관통이 **호출부 전건**임을 검증.

## 7. 트랙 진행률

- 트랙 `docs/tracks/active/track_naver-ad-optimization.md` · **시작 2/7 → 종료 2/7 (불변)**
- ★**M4 체크박스를 안 찍었다** — 합격 관측이 *「실집행 diary 행 + 가드레일 위반 0 + 대행사 되돌림 0을 **1주 연속**」*이라 오늘 1건으로는 못 찍는다. QA가 그 자제를 **북극성 원문과 정확히 부합**한다고 확인했다.
- **북극성 대비**: ①**M4 그 자체** ②5요소 중 **④자동화 운영이 처음 움직였다**(관할 캠페인 1→**2** · 스코프 그룹 1→**4**) ③**§7 대칭 = 액셀 쪽**(막힌 행동의 ~98%가 `bid_up` 계열) ④§목차 훑고 §1·§3·§6 M4행·§6-b·§7·§8 정독

## 8. 다음 세션이 «내일» 확인할 것

1. ★**시계 수리가 9시간을 되찾았는가** — **내일 KST 00:20 회차부터** 찍힌다. 확인: `ops_diary_entries`에서 `rationale LIKE '%소급채점 stale%'` 이고 `datetime(created_at,'+9 hours')`의 시가 00~08인 행이 **0건**이면 성공(오늘 이전엔 매시 109건).
2. **M4 1주 연속 관측 시작** — 그 3그룹에서 집행이 이어지는가 · 가드레일 위반 0 · 대행사 되돌림 0. 1주 채우면 M4 체크 가능.
3. bleeding 차단이 계속되면 그건 **정당한 방어**다 — 「돌린다 = 광고비가 는다」가 아니다.

## 9. 이월 (전역 §7 — 기록만)

- ↗️ **레인이 `enable:false`·`PAUSED` 소재의 입찰도 올린다** — 같은 16:20 레인이 `nad-...558104404`(`status:PAUSED` · `statusReason:AD_ABNORMAL_INTERLOCK` · `DEL_FLAG:1`)의 입찰을 1,240→1,420으로 올렸다. 멈춘 광고라 지출은 없으나 **레인이 소재 활성 여부를 안 본다** → 소관: 체인 `pao-논의`
- ↗️ **209·210은 아직 소재가 아니다** — **우리 코드에 소재 생성 경로 0건**(실측: `naver_sa_writer` 쓰기 전수 = 제외키워드·입찰·예산·잠금뿐, `/ncc/ads`는 전부 `_get`. `create_ad`류는 **대행사 변경이력에서 «읽는» 이벤트명**일 뿐) → 소관: **Jino 콘솔 작업**. 등록 시 **광고그룹 분리 권고**
- ↗️ 「④자동화 N세션째 0」 카운트의 정확성 미검증 → 소관: 하네스 주기 감사
- ↗️ §7 대칭 숫자의 «창 정의»가 문서마다 다르다(1,707+172 vs 26 / 1,915 vs 32) → 소관: `pao-논의`

## 10. 착지

- 커밋 `4298a7fa`(시계 수리) → `5f349386`(트랙) → `0bc576b5`(리뷰 1R 상환) → `bdf15865`(트랙·라이브) → 병합
- **PR #631 머지 `1ec7d114`** · CI **3/3** · **`--force` 미사용**
- prod: `auto_operator.py` 배포 2회(CAS 통과·무중단 0초) · DB 4행 쓰기
- 정정 경로: `git revert -m 1 1ec7d114` + `optimizer='none'` 한 줄(DB 쓰기 원복)

## 11. 하네스 관측

- 훅 `[트랙 기록 누락]` **오탐 3회** — 공유 메인 폴더가 `211decbc`로 약 400커밋 stale. 확인줄은 브랜치·origin/main에 실재(검증함). 공유 폴더엔 쓰지 않았다
- 훅 `[체인] ⛔` **오탐 상시** — 같은 원인
- ★**서브에이전트 판정이 저장소에 안 남는 것이 구조적 구멍**이다. 완료 QA가 적대 리뷰 결과를 **판정불능**으로 처리했고, 그 지적이 옳았다. ⇒ **적대 리뷰 판정문은 PR 코멘트로 남기는 것을 관례로 할 것**
- prod 포트 배포마다 뒤집힌다(8001↔8011) — 조회 스크립트는 **활성 포트를 스스로 찾아야** 한다

---

# 부록 — 17:0x 이후 (2026-09-01 21:1x KST 갱신)

> 위 본문은 17:0x에 썼다. 그 뒤 Jino 질문 2건에 답하며 작업이 이어졌고 PAO 레인이 5회 더 돌았다.
> ⚠️ 체인 등록부 `end_kst`는 17:0x로 먼저 채워졌는데 실제 종료는 이 시각이다 — **아래 A-6에서 정정**한다.

## A-1. ★PAO 집행이 «반복»됐다 — 1회성이 아니다

오늘 계정 전체 `execute` **4건**, 전부 폴드8 그룹(`070991863`)·actor `hourly`:

| KST | 소재 | 입찰가 | 상태 |
|---|---|---|---|
| 16:20:34 | `13684462601` (2매입 폴드8) | 1,240 → **1,420** | ⚠️ **PAUSED** |
| 16:20:35 | `13687558206` (**4매입 폴드8**) | 910 → **1,040** | ELIGIBLE |
| 19:20:32 | `13684462601` | 1,420 → **1,630** | ⚠️ **PAUSED** |
| 19:20:33 | `13687558206` (**4매입 폴드8**) | 1,040 → **1,190** | ELIGIBLE |

★**Jino가 지목한 상품의 입찰가가 3시간 만에 910 → 1,190원(+30.8%)까지 올라갔다.**
★20:20 레인은 **가드레일이 막았다** — *「쿨다운 중 — 마지막 변경 1.0시간 전(최소 2시간 필요)」*. 방어가 정상 작동한다는 증거다.

## A-2. ★7시리즈 스코프 진입이 «작동»했음이 증명됐다

Jino 지시 *"209, 210도 추가해줘"*로 `폴드7`(`...059832147`)·`플립7`(`...059832150`) 광고그룹을 스코프에 넣었다(18:2x, prod DB 2행).

**검증 — 차단 사유가 바뀌었다**: 두 그룹 모두 18:20·19:20·20:20 레인에서 **`스코프 밖` 차단 0건 · `bleeding` 차단 3건**.
스코프에 못 들어갔으면 `자동운영 스코프 밖 광고그룹(...) — D-NAO-244`로 막혔을 것이다(같은 레인에서 다른 21개 그룹이 정확히 그 사유로 막힌다). ⇒ **두 그룹은 이제 PAO의 판단 대상이고, PAO가 「지금은 올리지 마라」고 판정하고 있다.**

★이건 예측대로다 — 플립7 그룹은 최근 7일 광고비 7,850원·**전환 0**이었다. **「돌린다 = 광고비가 는다」가 아니다.**

## A-3. ★★이것은 「209·210 추가」가 «아니다» (다음 세션이 오독하기 쉬운 자리)

넣은 것은 **광고그룹**이고, 그 그룹의 현재 소재는 **2매입**(`12084320270`·`12084320268`)이다.
**209·210(4매입)은 여전히 광고 소재가 아니다.** 효과는 「Jino가 4매일 소재를 넣는 순간 PAO가 대기 없이 관리한다」뿐이다.

## A-4. 그룹 52308509의 5개 옵션 — 확정

| 채널상품 | 기종 | 광고 소재 | 네이버 주문 | BEP | 출처 |
|---|---|---|---|---|---|
| `13687558206` | **폴드8** 4매입 | ✅ | **177건** | 1.8942 | orders/orders |
| `13687558207` | **폴드8** 4매입 | ✅ | 22건 | 2.0453 | orders/orders |
| `13687558208` | **플립8** 4매입 | ✅ | 9건 | 1.6991 | orders/orders |
| `13687558209` | **폴드7** 4매입 | ❌ | **0건** | 1.7267 | **meta/sibling(추정)** |
| `13687558210` | **플립7** 4매입 | ❌ | **0건** | 1.504 | **meta/sibling(추정)** |

★**209·210의 BEP는 실측이 아니라 추정이다** — 실거래 0건이라 D-NAO-283의 형제 물류비 + 스토어 할인가로 만든 값. **실측처럼 인용하지 말 것.**
★7시리즈 주문 0건이 **광고 부재 탓인지 수요 부재 탓인지 이 데이터로는 못 가른다**(교락). 태울지는 사업 판단.

## A-5. 소재 생성이 우리 몫이 아닌 이유 — 실측 2건

1. 생성엔 **`referenceKey`**(네이버쇼핑 상품ID, 206의 예 `91232069492`)가 필요한데 **209·210 것을 우리 DB 어디에도 갖고 있지 않다.** 이미 광고에 실린 상품의 ad raw JSON 안에만 존재하고, `naver_adgroup_product`·`naver_entity`·`naver_entity_snapshot` 전부 referenceKey 컬럼이 없다.
2. `naver_sa_ad_fetcher`의 `/ncc/ads`는 **전부 `_get`**. `POST /ncc/ads` 미구현.

⇒ **「구현하면 된다」가 아니라 ID를 알아내는 조회부터 없다.** 만들려면 상품 조회 엔드포인트를 API 문서에서 찾아 검증 + 광고 계정에 **처음 하는 «생성» 쓰기** 배선 ⇒ 계약 + 적대 리뷰 필요. Jino가 원하면 착수 가능하다고 알려 뒀다.

★**광고그룹 구조 실측**: 이 캠페인의 26개 그룹은 **기종별**(S20~S25·노트20·폴드7·폴드8·폴드8울트라·플립7·플립8)이고 그룹당 소재가 **1개가 아니라 5~15개**다. **`폴드7`·`플립7` 그룹이 이미 존재하고 `on`** — 만들 필요가 없고 **소재만 넣으면 된다.**

## A-6. Jino 콘솔 작업 (2분)

- **`폴드7` 광고그룹**(`grp-a001-02-000000059832147`) → 소재 추가 `13687558209` (폴드7 4매입, 19,900원)
- **`플립7` 광고그룹**(`grp-a001-02-000000059832150`) → 소재 추가 `13687558210` (플립7 4매입, 16,900원)

그룹도 스코프도 준비돼 있어 **넣는 즉시 PAO가 관리**한다.

## A-7. 최종 라이브 상태 (21:1x KST)

- PAO 관할: 캠페인 **2개** · 광고그룹 **5개**(폴드7·폴드8·폴드8울트라·플립7·플립8) · 대행사 유지 21개
- 인수 후 이벤트: `execute update_bid` **4** · `blocked bid_up` 94 · `bid_up_explore` 15 · `update_bid`(가드레일) 6
- 시간별 레인 마지막 `2026-09-01 20:20:43 ok`

## A-8. 추가 착지

- PR **#636** 머지 `a19e6884` (5개 옵션 확정 기록)
- PR **#638** 머지 `f6148de4` (7시리즈 스코프 2행 기록 — prod DB 쓰기는 git에 없으므로 그 확인줄이 정본)
- 트랙 확인줄 누적 **7개**(전부 origin/main)

## A-9. 내일 볼 것 (본문 §8을 이걸로 갱신)

1. ★**시계 수리 검증** — `stale` 차단이 **KST 00~08시에 0건**이면 성공. 확인:
   `SELECT COUNT(*) FROM ops_diary_entries WHERE rationale LIKE '%소급채점 stale%' AND CAST(strftime('%H', datetime(created_at,'+9 hours')) AS INT) < 9 AND created_at >= '2026-09-01 15:00:00'`
   (어제까지는 그 창에서 **매시 정확히 109건**)
2. **M4 1주 연속 관측** — 5개 그룹에서 집행 지속 · 가드레일 위반 0 · 대행사 되돌림 0. 채우면 트랙 M4 체크 가능
3. **폴드8 입찰가가 어디까지 오르나** — 910 → 1,190원(3시간, +30.8%). 자동 상향 누적 상한 2.0배가 방어선이다

## A-10. 되돌리는 법

| 무엇 | 방법 |
|---|---|
| 인수 전체 | `naver_campaign_settings` `optimizer='none'` 한 줄 |
| 7시리즈만 | `naver_adgroup_scope`에서 `059832147`·`059832150` 2행 삭제 |
| 코드(시계 수리) | `git revert -m 1 1ec7d114` |
| BEP(배송비 자) | `git revert -m 1 637dce61` + 이전 커밋 재배포 + `calculate_bep` 재실행 |

---

# 부록 B — ★다음 세션의 첫 일 (Jino 지시 2026-09-01 22:5x)

> **Jino 원문: "다음 세션에서 너가 생성 기능을 네이버 공식api로 만들어봐"**

## B-1. 무엇을 만드나

네이버 검색광고 공식 API로 **광고 엔티티 «생성» 쓰기**를 배선한다. 지금 우리 쓰기는 **전부 «수정»뿐**이다.

**현재 `naver_sa_writer`가 네이버에 보내는 요청 전수 8건** (2026-09-01 실측):

| 메서드 | 경로 | 하는 일 |
|---|---|---|
| `PUT` | `/ncc/adgroups/{id}` | 그룹 입찰가·잠금 |
| `PUT` | `/ncc/keywords/{id}` | 키워드 입찰가·잠금 |
| `PUT` | `/ncc/targets/{id}` | 쇼핑 제외 설정 |
| `POST` | `/ncc/adgroups/{id}/restricted-keywords` | 제외키워드 추가 |
| `DELETE` | 〃 | 제외키워드 삭제 |

⇒ **생성 경로 0건.** `/ncc/ads`는 `naver_sa_ad_fetcher`에서 **전부 `_get`**이다. `tools/`의 브라우저 자동화(`ad_cost_browser_fetcher.py` 등)도 전부 **수집**이고 조작이 아니다.

## B-2. 순서 — 쉬운 것부터

**① 광고그룹 생성 (`POST /ncc/adgroups`)** — 먼저. 필요한 것이 캠페인 ID·이름·입찰가 정도라 자족적이다.

**② 소재 생성 (`POST /ncc/ads`)** — 더 어렵다. **`referenceKey`(네이버쇼핑 상품ID, 예: `13687558206`의 것은 `91232069492`)가 필요한데 우리가 그 조회 표면 자체를 안 갖고 있다.** 그 ID는 **이미 광고에 실린 소재의 raw JSON 안에만** 존재하고, `naver_adgroup_product`·`naver_entity`·`naver_entity_snapshot` 어디에도 컬럼이 없다.
⇒ **상품 조회 엔드포인트 발굴·검증이 선행**한다(공식 도움말 `ads.naver.com/help/faq/520`: *「네이버 쇼핑에 등록된 상품을 «조회»하여 광고로 등록」*).

## B-3. 첫 실전 대상 (이미 정해져 있다)

Jino가 2026-09-01 22:5x에 **캠페인 `[PAO] 외부 사생활_내부 지문방지필름 플립/폴드`를 콘솔에서 생성**했다. 여기에 넣을 것:

| 광고그룹(기종별) | 소재로 넣을 상품 | |
|---|---|---|
| 폴드8 | `13687558206` | 19,900원 |
| 폴드8 울트라 | `13687558207` | 19,900원 |
| 플립8 | `13687558208` | 16,900원 |
| 폴드7 | `13687558209` | 19,900원 |
| 플립7 | `13687558210` | 16,900원 |

★**기종별로 나눈다** — 광고그룹이 키워드·타겟팅 단위이고 폴드8과 플립7은 검색어가 다르다.
★그룹상품 **52308509** = 「외부 사생활보호 + 내부 지문방지, 내/외부 2매씩」 **4매입**. **캠페인 15의 「외부 전면액정 2매입」과는 다른 제품이다**(이번 세션에서 내가 이름만 보고 틀렸다 — B-6 참조).

## B-4. 반드시 지킬 것

- **이건 광고 계정에 «처음» 하는 생성 쓰기다.** 북극성 §7이 *「신규 캠페인 생성·캠페인 재구축·예산 상한 인상은 항상 사람 Confirm」*(D-NAO-5·42)으로 묶어 둔 축이다 ⇒ **계약 1장 + 적대 리뷰** 필수.
- **중복 생성 방지 가드**가 핵심이다. 수정 쓰기는 멱등이지만 생성은 아니다 — 같은 이름 그룹이 두 번 생기면 되돌리기가 삭제다.
- **롤백 경로를 먼저 적는다**(생성한 것의 ID를 기록 → 실패 시 삭제 또는 off).
- `NAVER_CS_DRY_RUN`이 prod `.env`에 **`0`(실쓰기 허용)**으로 살아 있다 — dry-run 전제를 코드 기본값에서만 확인하면 틀린다.

## B-5. 함께 처리할 열린 건

- 대행사 캠페인(`01. 갤럭시_지문방지_사생활`)의 **4매입 소재 3개를 끌지** — 안 끄면 새 캠페인과 같은 상품에 광고가 둘이라 자기 경쟁
- 오늘 PAO가 올린 입찰 **910 → 1,360원 되돌릴지** (Jino 미결)
- 새 캠페인을 PAO 스코프에 넣을 때 **`auto_operate`는 꺼두고 시작** — 새 캠페인은 데이터 0이라 오늘의 「14클릭 +49%」 문제가 더 심해진다

## B-6. ★이번 세션에서 내가 제품을 두 번 잘못 짚었다 — 같은 실수 반복 금지

이 계정은 상품명이 **「사생활」·「지문방지」로 크게 겹친다.** **이름 유사도로 제품 동일성을 판정하면 안 된다** — 구성(매입 수·외부/내부)과 `group_product_no`로 갈라야 한다.

1. `13687558207`을 「폴드8」이라 적었다 → 실제 **폴드8 울트라**
2. 「캠페인 15를 재활용하라」고 권고했다 → 캠페인 15는 **「외부 전면액정 2매입」**으로 **다른 제품**. Jino 지적으로 철회
