# 세션 인수인계: 채점기 목적함수 어긋남 발견·기록 (D-NAO-222)
> 저장일시: 2026-08-22 00:1x KST · 체인 「PAO 논의 **31**」 (세션 `6b9376b0`)
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## 1. 프로젝트 위치 및 환경
- 로컬: `~/Library/Mobile Documents/com~apple~CloudDocs/1Personal/AI Program/Ohiselling` (**main**)
- prod: **`sellc.ohitech.co.kr`** — ★ssh 별칭은 **반드시 FQDN**(`sellc`는 해석 실패)
- prod DB: `ssh sellc.ohitech.co.kr "sqlite3 -readonly /home/ubuntu/ohisell/backend/ohisell.db \"<SQL>\""`
- 배포: `scripts/safe_deploy.sh <파일…> [--migrate] --restart` / 병합: `scripts/safe_merge.sh <PR> [--force]`
- prod 마이그 head = **`m2b2devw1eight`**(불변 — 이번 세션 스키마 변경 0건) · **디스크 95%**(91G/97G, 여유 **5.8G** — 재확인)
- 테스트 인터프리터는 저장소 전체에 **하나뿐**(직전 인계에서 승계, 이번 세션 미사용):
  `.claude/worktrees/naver-ad-execution-loop-6cc75b/backend/.venv/bin/python3` (py3.14.3 / pytest 9.1.1)
  ⚠️`--timeout` 옵션은 `pytest-timeout` 미설치로 **인자 오류 즉시 종료**. 옵션 없이 돌릴 것.
- ⚠️**병행 세션이 있다**(쿠팡 손익 정합 트랙): 세션 중 `docs/references/89_rg_ledger_gap_root_cause_20260821.md`(신규)·
  `docs/tracks/active/track_coupang-rocket-1p.md`(수정)가 나타났다. **내 커밋에 안 섞었다**(경로 명시 `git add`).
  다음 세션도 커밋 전 `git status` 확인하고 **남의 파일을 자기 커밋에 담지 말 것**(issue #247).

## 2. 이번 세션 완료 목록
- ✅ 체인 등록부 `n=31` append → 마감(§7)
- ✅ **D-NAO-222 — 채점기 목적함수 어긋남 발견·기록**. 근거 `docs/references/90_scorer_objective_function_mismatch_20260821.md`
- ✅ 교훈 **#344** · 트랙 `확인:` 줄 1건(누적 **23건**) · 커밋 **`bd27e2f8`**(4파일)
- ✅ 완료 QA **3대조 = 판정 3줄**(§2-1) · QA 실결함 지적 1건 **정정 완료**
- ✅ **코드 변경 0건 · 배포 0건 · prod 쓰기 0건**(전건 `-readonly`)
- ❌ **push 안 함** — origin 미푸시 커밋 있음(§5-6)

## 2-1. 완료 QA (판정 원문 그대로 — 미달 포함)

### 작업 목적(정본 원문 — 트랙 계약 헤더 `목표:`)
*"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."* (Jino 2026-07-19 · D-NAO-59)

- **판정(앵커 합격기준 ①~⑥): 부분달성** — ①②③④⑤ 달성 / **⑥ 부분미달**. QA 원문: *"HANDOFF 파일 부재 + 체인 등록부 n=31이 「마감」이 아니라 「착수」 스텁으로 남아 있다."*
  ★①~⑤는 QA가 **prod·코드를 전건 독립 재현**했고 숫자가 전부 일치했다(425·194·174·20·61·43·10·8·52 및 `improved` 4행 원문·RPC 재계산).
- **판정(Jino 지시 원문 2건): 부분달성** — 23:26 *"우리가 대행사보다 잘 운영할 수 있는 준비가 되었냐가 중요하지"* 대비 **달성**(실측으로 답했다) / 23:31 *"채점기 목적함수 어긋난 거 문서로 남기고 handoff하자"*는 **앞절 달성·뒷절 미달**(판정 시점에 HANDOFF 부재).
- **판정(트랙 궁극 목표 D-NAO-59): 미달** — QA 원문: *"코드·배포·prod 쓰기 0건이라 총이익에 관측 가능한 기여가 없다(순수 0) … **발견의 크기와 무관하게 관측 가능한 총이익 기여는 여전히 0**"*. 직전 두 세션(`20532846`·`f67f21b7`)의 같은 기준을 **세 번째로** 적용.
  ★**이 미달은 낮추지 않는다** — 「조사·발견을 목표 진전으로 착각」하는 경로가 열리기 때문이다.
- **「안 함」·금지선 침범 0건**: `git show --stat bd27e2f8` = 4파일 · **.py/.ts/.tsx 변경 0** · 트랙 diff **14 insertions / 0 deletions**(기존 확인 줄·D-NAO 블록 무손상) · `- [ ] M2` 미체크 유지 · 처분 미지정 · 교락 숫자 미생산 · 병행 세션 파일 3건 미혼입. **목적 전환 없음.**

### ★QA가 잡은 실결함 1건 (정정 완료)
§4 표의 코드 좌표가 «판정식이 있는 줄»이 아니라 «함수 `def` 줄»이었다 — `gave_score.py:21`→**`:60-63`** · `retro_scorer.py:47`→**`:58-60`**. 좌표 표기 규약 상자를 ref 90 §4-3 위에 신설했고, **나머지 25행은 그 관점에서 재검증되지 않았음**을 §8-A [미상] 4번으로 남겼다.

### ★QA가 재현 실패한 숫자 1건 (좌표 제시로 해소)
「외부변경 **4,937** = keyword 4,885 + adgroup 44 + campaign 8 / WEB_SITE 4,903 · SHOPPING 34」 — QA가 `naver_agency_op`·`naver_adgroup_criterion_change`를 뒤졌으나 **정본은 `naver_change_log` ∧ `action LIKE 'external%'`**다. 재조회로 전건 일치를 확인하고 **재현 명령을 ref 90 §9-④에 박았다.**

### ★⑥ 부분미달의 성격 — 내 합격기준 설계 오류다 (바를 낮추지 않고 기록)
완료 QA는 구조상 HANDOFF **앞**에 돈다(HANDOFF가 QA 판정을 싣는다). 그런데 내가 「HANDOFF 작성」을 합격기준 ⑥에 넣었다 ⇒ **판정 시점에 원리적으로 충족 불가능한 항목**이었다. §2의 「같은 QA 기에 그 항목만 재판정 1회」는 QA 기가 in-process 서브에이전트라 **닿지 않았다**(`ListAgents` 미표시). 따라서 **부분달성인 채로 닫는다.** ⑥의 실물(이 HANDOFF·체인 마감)은 판정 뒤 완료했고 그 사실만 병기한다.
⇒ **다음에 앵커를 쓸 때: 「HANDOFF·체인 마감」을 합격기준에 넣지 말 것.** 그건 세션 종료 절차이지 작업의 합격 조건이 아니다.

### QA 미확인 4건 (그대로 기록)
§4 표 나머지 25행의 좌표 정밀도 · §8-B 커버리지 자백 자체의 진위(열람 이력 미재구성) · id 221·222 동일값의 정체 · `outcome='success'` 2행의 출처.

## 2-2. 트랙 진행률
- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: §2-1과 동일(D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음.** M2 슬라이스가 아니다 — Jino 질문에서 파생된 **발견 1건의 기록**이다. 증거: 커밋 `bd27e2f8` · ref 90
- **확인 줄**: 1건 추가(23:5x) — 누적 **23건**
- **트랙 종결 여부**: **미도달**(2/7)

## 3. 확정된 결정사항
- **D-NAO-223 — D-NAO-222의 처분: 「M3 계약 §4 합격기준의 «명문 항목»으로」.** (2026-08-22 00:08, 완료 QA **이후**)
  Jino 원문: *"그렇게 가자, 다음 세션이 M3 계약에 박도록 인계에 적어줘"* · 상세·기각 사유는 §6-③.
  ★**QA 판정은 소급 변경하지 않는다** — §2-1의 3대조 판정(부분달성/부분달성/미달)이 그대로 정본이다.
- **D-NAO-222 — 채점기 목적함수 어긋남.** 읽기 전용 조사(새 승인 지점 아님). 처분은 **D-NAO-223으로 확정됨.**
  - ★**핵심**: 목적함수는 **총이익 절대액**(D-NAO-59)인데, 훑은 범위에서 «성패»를 찍는 지점 **19개가 효율 비율**(ROAS·RPC·CPC 배수)을 재고 **절대액은 4개**뿐이다.
  - ★★**라이브 증거 — `outcome='improved'` 전건 4건이 «전부» 매출 감소**. **매출이 늘어서 improved가 된 사례 0건.**
  - ★**id 761이 D-NAO-85(2026-07-23 ROAS +7%·매출 −52%)의 축소판**인데 채점기는 그것을 「개선」으로 적었다. **같은 사고가 재발해도 계기판은 초록이다.**

## 4. 핵심 파일 목록
| 파일 | 역할 |
|------|------|
| `docs/references/90_scorer_objective_function_mismatch_20260821.md` | ★**이 발견의 정본** — 판정식 좌표·라이브 4건·19/4/4 스윕표·재현 명령 §9 |
| `backend/app/services/naver_ad/proposal_scoreboard.py` | `:59` `rpc=conv_amt/clk` · `:97-103` 판정 · `:33-34` 상수 |
| `backend/app/services/naver_ad/wisdom_candidates.py` | `:70 _outcome_direction` — `good = roas_c >= target_roas`(지혜 승격 게이트) |
| `backend/app/services/naver_ad/gave_score.py` | `:60-63` **올바른 모양**(`penalty×revenue`) — **정지 중** |
| `docs/references/82_pao_north_star_20260819.md` | 마일스톤 M0~M6(§6) · 위험·금지선(§7) · Jino 결정 9건(§8) |
| `docs/PLAN_naver-m2-l2-wiring.md` | M2 계약 정본(D-NAO-214) |
| `docs/references/88_m2_s1s2_residual_census_20260821.md` | ★**M2-z의 입력 정본**(직전 세션) |
| `.claude/anchors/6b9376b0-….md` | 이번 세션 앵커(판정 3줄 + 이월 5건) |

## 5. 알려진 이슈 / 주의사항
1. ★★**「우리가 대행사보다 나은가」는 지금 «잴 수 없다»** — 두 경로가 다 막혀 있다: ①창 비교는 **교락**(우리 집행 창 07-17~07-30이 휴가시즌 7/20~8/15 + Z폴드8 출시와 겹침 · D-NAO-183 준-공선 ⇒ **단독 귀속 금지**) ②단위 조치별 채점은 **채점기가 효율을 잰다**(D-NAO-222). 어느 쪽 숫자도 만들지 마라.
2. ★★**소유권 분리(북극성 §8-②)는 데이터로 못 고른다** — 외부변경 감지 4,937건 중 **4,885건(98.9%)이 키워드 단위**인데 쇼핑엔 등록 키워드가 없다(WEB_SITE 4,903 vs SHOPPING 34). ⇒ **「최근 30일 외부변경 0건인 쇼핑 13캠페인」은 깨끗함의 증거가 아니라 못 본다는 뜻일 수 있다.** ★**Jino가 「대행사 통보는 문제없다」고 확정**(23:26)했으므로 ②의 남은 절반은 **「어느 단위를 우리 단독으로 가를지」 합의**뿐이다.
3. ★**집행은 22일째 정지**: `naver_campaign_settings` **9행 전부 `optimizer='none'`**·`auto_operate` 합 **0**인데 **지출 캠페인은 29개**(20개는 settings **행조차 없음** — 카나리 켜려면 행 생성부터). 우리 실집행 마지막 = `update_bid` **2026-07-30 10:20**. 그 사이 제안 **4,483건이 `expired`**로 소멸, pending 362건 누적(오늘도 72건 신규).
4. ★**액셀이 막혀 있다**: `update_bid` 425건 중 **194건(45.6%) 미집행**, 차단 주체는 **우리 가드레일 174건(89.7%)** vs 네이버 API 20건. **「BEP 미달 증액 금지」 61건**(보정ROAS 0.0 = 창 안 전환 0) — D-NAO-59가 잡으라는 구간이 정확히 거기다. ⚠️07-30 창 관측이고 **현재 파라미터로 재현 안 함**(`update_guardrail_params` 08-10·08-11 2건 있음).
5. ★**ref 90 §4 표의 좌표 25행은 「판정식 줄」인지 미재검증** — 인용 전 그 줄을 직접 열 것(§2-1 QA 지적).
6. ⚠️**origin 미푸시** — `bd27e2f8` 포함. 병행 세션 커밋(`ce98e56e` 등)도 같이 올라간다. push 여부는 Jino 확인 후.
7. **CI 빨강은 결제 정지**(steps 0개·2~4초)지 코드 신호가 아니다.
8. **다음 구조 감사 트리거 = 08-25 이후**(마지막 §4 감사 = `docs/references/69_audit_pao_drift_20260818.md`, 08-18).

## 6. 다음에 할 작업 (미완료)
- **이어지는 작업의 목적(원문)** — 트랙 계약 헤더 `목표:` 줄 그대로:
  *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."*
  이번 칸 = **M2 = L2 배선**(ref 65 S1+S2), 계약 `docs/PLAN_naver-m2-l2-wiring.md`(승인됨).
- **남은 슬라이스**: **M2-z**(08-22 09:30 이후) · **M2-d**(08-28 이후)

- [ ] ①**★아침 관측 4건 — 묻지 말고 진행.** 명령·기대값은 **ref 88 §4 표**에 있다(직전 세션이 만든 정본).
      **07:35** `SELECT COUNT(*) FROM naver_entity WHERE pc_bid_weight IS NOT NULL;`(기대 ≈871 / 전체 92,235) → M2-b2 ①
      **08:12** `sweep_naver_adgroup_criterion` `last_run_at`·`last_status`(★**무인** 첫 발화)
      **08:25** `SELECT COUNT(*) FROM naver_search_term_exclusion WHERE match_type IS NOT NULL;` → S1①
      **08:50** ★S1⑤ — ref 88 §5-5의 **좁힌** 카운트로 셀 것(원 카운트면 거짓 달성)
      **09:30** ★S1② — `write_naver_pooled_estimates.last_run_at`이 09:30대로 바뀌었는지 + `target_date` distinct 증가
- [ ] ②**M2-z** — M2 종결 QA(별도 Sonnet·읽기 전용, **16항목** 라이브 대조). **입력 정본 = ref 88.** ★**09:30 이전 착수 금지**
- [ ] ③★★**M3 계약 1장 작성 — D-NAO-222를 §4 합격기준의 «명문 항목»으로 박는다. 처분은 이미 확정됐다(D-NAO-223) — 묻지 말고 진행.**
      **Jino 원문**(2026-08-22 00:08): *"그렇게 가자, 다음 세션이 M3 계약에 박도록 인계에 적어줘"*
      ⚠️**이 결정은 D-NAO-222 완료 QA «이후»에 왔다** — 그 판정(부분달성/부분달성/미달)은 소급 변경하지 않는다.
      - **무엇을 하라는 건가**: M3 계약을 쓸 때 §4 합격기준에 **채점기 교정을 별도 항목으로 명시**한다. 「M3에 포함」이 아니라 「§4에 문장으로」다.
      - ★**왜 「그냥 흡수」면 안 되나**: M3의 현 합격기준(*"승격 지혜 ≥1건에 성적 행이 붙고 … 항등식이 표본 1주에서 일치"*)은 **채점기가 무엇을 재든 충족된다.** 명문화 없이 넣으면 담당은 M3인데 판정은 아무도 안 하는 상태 = **교훈 #343 「담당은 완료가 아니다」의 그대로 재발**이다. 이게 이 결정의 전부다.
      - ★**왜 별도 마일스톤이 아닌가**: 채점기 교정의 라이브 증거는 **새로 채점될 실집행 행**을 요구하는데(`proposal_scoreboard.run_daily`는 `dry_run=False`만 채점) 우리 실집행은 **07-30 이후 0건**이고 재개가 **M4**다 ⇒ 별도 M은 **원리적으로 종결 불가능**해져 분모만 `2/7→2/8`로 늘고 안 닫힌다.
        ⚠️**이 전제는 코드 구조 추론이지 실행 확인이 아니다** — **M3 계약의 첫 확인 항목으로 둘 것.** 틀렸으면 별도 슬롯이 되살아난다.
      - ★**왜 M3와 한 몸인가**: 채점기만 고치면 소비처가 화면 표시+로그뿐이라 **총이익 기여 0**이다. 그 값을 읽는 성적표가 M3다. 따로 떼면 **`gave_score`처럼 「올바른데 정지 중」인 부품이 하나 더** 생긴다.
      - **기각된 것과 이유**(다시 상정하지 말 것): **ⓒM2 편입** = M2-z 분모 16항목이 흔들린다(ref 88이 막으려던 것) · **ⓓ부채 기록만** = M3 착수 순간 어차피 부딪히고 그때 결정 라운드를 또 쉰다.
      - ⇒ **M3 계약 §8에 넣을 미결 1건**: **기존 150건의 `outcome`을 새 식으로 소급 재채점할 것인가.** 소급하면 과거 기록이 바뀌고(**교훈 #274**가 `backfill_outcomes` 강제 실행을 경고한 자리), 안 하면 새 식의 증거가 M4까지 없다.
      - **시점**: **M2-z가 M2를 닫은 뒤.** M2∥M3 병렬은 북극성 §6이 허용하고 M2-z는 판정이라 충돌하지 않는다. 계약이므로 **Jino 승인 1회 필요**(§1 승인 지점 ①).
- [ ] ④**M2-d** — S2 전체. 진입 = M2-a 배포 +7일(**2026-08-28 이후**) & 추정치 지속 생성 관측
- [ ] ⑤**계약 §8-Q5 — 콘솔 캡처 1장**. 좌표 확정: 캠페인「아이폰 17프로 필름」/그룹「아이폰필름」(`grp-a001-02-000000063093697`) 연령 탭, `AG5054=130`. **Jino 개입이 필요한 유일한 항목**
- [ ] ⑥**이미 쌓인 496 MB의 처분**(DELETE+VACUUM) — **디스크 95%**. 크론 정지는 순증만 멈춘다
- [ ] ⑦**크론 정리 2차(선택)** — `proposal_scoreboard`는 **소비처 미발견**이 이번 세션에 다시 확인됐다(롤업이 로그로만 남는다, `scheduler_service.py:629`). `bid_rank_curve`·`estimate_calibrator`는 소비처 있음(단 `optimizer='none'` 게이트로 실행 막힘) · `hourly_pattern`은 `flight_loop`(관측 전용)
- [ ] ⑧**`conversion_maturity`가 쌓기만 하고 안 쓴다** — 유일 소비처가 `MATURITY_CORRECTION_ENABLED=False`
- [ ] ⑨**토큰화 규칙 중복 정의 해소** — `_build_whitelist`(search_term_judge)와 `_tokens_from`(semantic_units). 잔여 no-op 불변식이 이 둘의 동기화에 의존
- [ ] ⑩**사전 구멍 「액정」 처분** · ⑪**「관련어인데 적자」의 처분**(계약 §8-Q4)
- [ ] ⑫**`search_term` rejected 141건** 증가 관측 재확인 — `created_at`이 07-19~07-29 전건 옛것이라 창이 안 맞는다. 좌표 `auto_operator.py:1077`
- [ ] ⑬신설 카운터(`shopping_over_cap`·`semantic_*`)가 크론 `log.info`(`scheduler_service.py:909-917`)에 안 실린다
- [ ] ⑭**워크트리 24개**(최근 PAO 7개는 병합 확인, 나머지 17개 미등재) · **`.venv.broken-py314`** 방치
- [ ] ⑮**CLAUDE.md 미커밋 11줄** + `.claude/settings.local.json.bak-20260821` — 어느 HANDOFF에도 기록 없이 누적. **Jino 판단**
- [ ] ⑯**ref 90 §4 표 25행 좌표 재검증**(§5-5) · **`outcome='success'` 2행의 출처**(고아 값)

## 7. 새 세션 시작 프롬프트
```
.claude/memory/HANDOFF_scorer-objective-mismatch_20260821.md 읽고 이어서 작업해줘
```
(체인을 이어받으려면: `/session-relay PAO 논의` — 이번이 **31**번이었다)
