# HANDOFF — 하네스 §2 개정 + 「정정이 전파되지 않는다」 (2026-08-30, 세션 7a9fe16b / 체인 PAO 논의 71)

## 0. 이 인계의 중심 — 먼저 읽을 것

**Jino가 내 말을 믿기 어렵다고 했다.** 그게 이 인계의 핵심이고, 기술 항목보다 먼저다.

Jino 원문 두 번:
1. (11:2x) *"너가 이렇게 오탐을 많이 하니까 너가 하는 말을 이제는 믿을수가 없네.. 이거 어떻게 개선하지?"*
2. (15:0x) *"진짜 미치겠다. 너 왜이렇게 잘못 진행하는게 많아? 너를 이제 믿기가 어렵잖아"*

**둘 사이에 내가 또 틀렸다.** 그게 문제의 전부다 — 아침에 오류 12건을 세고 처방을 만들었는데, 그 처방이 **오후의 실패를 못 막았다.**

---

## 1. ★내 처방이 틀렸다 (다음 세션이 반복하지 말 것)

**아침 처방**: 전역 `~/.claude/CLAUDE.md` §2에 「말할 때 «안 잰 것»에 꼬리표를 단다」(`📄`/`🧠`) 신설. 커밋 `202778b`. **이건 그대로 유효하다** — 다만 **이번 병에는 안 듣는다.**

**왜 안 듣나**: 오후에 틀린 「11건」은 **내가 잰 숫자**였다. 꼬리표를 붙였어도 똑같이 틀렸다.

**진짜 모양**:
> **정정이 「발견한 자리」에만 반영되고, 그 사실에 기대고 있는 다른 자리로는 안 퍼진다.**

- 오전: 완료 QA가 「11건은 08:50이 아니라 08:00 산출물」이라 반증 → 내가 `docs/PAO_OPS.md` **§1-b에 정정을 씀**
- **그런데 같은 문서 §0-★2의 A안에 낡은 이해가 그대로 남았다** — 「①쇼핑 검색어 제외를 콘솔에서 Jino가 Confirm**(지금 11건 대기)**」
- 나는 「고쳤다」고 여기고 넘어갔다
- Jino가 그 A안을 읽고 *"이제 그 부분은 내가 손을 안대도 되는 자동화가 된거지?"*라고 물었다
- **그제서야 발견했다**

★**grep 한 번이면 잡혔다.** §1-b를 쓸 때 「11건」이 문서 어디에 또 나오는지 안 셌다.

**같은 병의 다른 사례(세션을 건너뛴 전파)**: n=68이 *「자동 발사되는 유일한 레버 = 검색어 제외」*에 **「파워링크 한정」을 안 붙였고**, n=69(나)가 그대로 인계했고, 오늘 라이브가 반증했다. **틀린 문장은 문서 사이·세션 사이를 타고 번진다.**

## 2. ★가장 나쁜 패턴 — 큰 그림은 맞고 세부 좌표가 틀린다

「점화했는데 카나리가 쏠 총알이 없다」는 **맞았고**, 이 트랙이 한 달 동안 못 본 것이었다. 그런데 그 **근거를 세 번 틀렸다**(11건의 출처 / 창 라벨 「30일」 / A안의 대상).

⇒ Jino 입장에서 이게 최악이다. **큰 그림이 맞으니 믿게 되는데, 세부가 틀려 그대로 행동하면 헛수고**가 된다. *"11건 Confirm하면 되겠네"*가 정확히 그 자리였다.

## 3. 이 세션(n=71)이 한 것

| | 무엇 | 좌표 |
|---|---|---|
| ① | **전역 하네스 §2 개정** — 「안 잰 것에 꼬리표」(Jino 승인 「ⓐ로 가자」) | `~/.claude/CLAUDE.md` 커밋 `202778b`(로컬 repo, 원격 없음) |
| ② | 트랙 확인줄 + 체인 n=71 재개 등록 | PR #582 머지 `bf7d0963` |
| ③ | **A안 정정** — 「11건 Confirm」은 눌러도 실패한다 + §1-c 실측 절 신설 | PR #587 머지 `65c6cccf` |

**앵커 없음** — 단일~2파일 문서 작업이라 §1 앵커 발동 조건(3파일+·반나절+) 밖. ⇒ **완료 QA 대상이 아니다**(§2 「둘 다 없으면 잡일」). 정직하게 적어 둔다.

### ⚠️내 규율 위반 1건 (자백)
n=70 등록부를 **닫은 뒤에도** PR #581을 커밋·머지했다 — **죽은 세션의 쓰기**. n=67 note가 *「2단계 규약의 취지가 여기서 뚫린다」*고 이미 지적한 구멍이다. 발견 즉시 n=71로 재개 등록했다.

## 4. 라이브 실측 (2026-08-30 14:51 KST) — 자동화된 것은 없다

| | 값 |
|---|---|
| 스위치 | 08-29 12:53 점화 그대로 — **변화 없음**(`optimizer='ours'`·`auto_operate=1`, 카나리 1개) |
| 오늘 A 레인 산출물(`search_term_exclude`) | **0건**(계정 전체) |
| 오늘 우리 엔진 실쓰기(`flight_pacing` 제외) | **0건** |
| 제외 원장 | **08-17 이후 신규 0**(총 3,990행) |
| 그 11건 | 여전히 `pending` — **눌러도 실패한다**(카나리는 SHOPPING, B의 실행 경로는 WEB_SITE 전용 API) |

⇒ **카나리는 「발화는 하되 아무것도 집행하지 않는」 상태로 M4의 7일 창을 흘려보내고 있다.**

## 5. ★다음 세션 0순위 — Jino가 「돌려라」 하면

**`docs/PAO_OPS.md` 전수 재검증.** 별도 기(읽기 전용)에게 §0~§12의 **모든 사실 주장을 라이브로 재현**시키고, 재현 안 되는 것을 목록으로 받는다.

- 근거: 오늘 내 오류 12건 중 **10건을 잡은 것이 정확히 이 방식**이다(적대 리뷰 4·완료 QA 4·라이브 2). 자기검증은 2건뿐이었다.
- 산출: **「틀린 게 몇 개인지」 숫자**. 내가 「고쳤다」고 말하는 것보다 재현된 목록이 낫다.
- ⚠️**또 몇 건 나올 것이다**(🧠). 나오는 게 정상이고, 안 나오면 재검증이 얕은 것이다.

★**Jino가 아직 「돌려라」라고 하지 않았다.** 물었고 답을 못 받은 채 이 세션이 닫혔다 — **묻지 말고 진행하지 말 것.** 대안으로 *"지금은 문서를 덮어 두고 다른 걸 하자"*도 함께 제시했다.

## 6. 미결 — Jino 결정 대기

### A. 카나리에서 무엇으로 실집행을 만들 것인가 (5안, 넷 다 계약 사안)
① A 레인이 후보를 낼 때 Confirm — ⚠️**지금은 후보 0건이라 누를 게 없다** ②카나리를 파워링크로 교체 ③C2를 「Confirm 경유」로 개정 ④「구조상 0」을 M4 판정으로 기록 ⑤**쇼핑 자동 발사를 연다**(§3-b)

**⑤에 대한 세션 권고**: 「켜기 전에 **되돌리는 손**을 먼저 만든다」. 되돌림 **배선은 이미 있다**(재심사 루프가 실제로 네이버에서 제외키워드를 삭제, `_PROBATION_DAYS=14`). 없는 건 **사람이 즉시 부르는 입구**(재개방 API 0건). ⇒ 새로 만드는 게 아니라 **버튼 연결**에 가깝다. 잘못 자르면 **최대 14일** 그 검색어 매출을 잃는다.

### B. 그 외 (문서 §8)
소유권 분리(**08-19부터 11일째**, 셋을 동시에 막음) · ADVoost/GFA 취급 · 스마트스토어 대조축 · n-gram 처분 · A3 회색 토큰 4건 · WEB_SITE 승계 3건 · ref94 하한 음수 처분

## 7. 트랙 진행률 (전역 §6 의무)

- 트랙: `docs/tracks/active/track_naver-ad-optimization.md`
- **시작 2/7 → 종료 2/7 (불변)**. 이 세션은 하네스 개정·문서 정정이라 M을 전진시키지 않는다.
- 확인줄 2회 누적(11:4x 하네스 개정 · 이 종결분)
- 북극성 5요소 중 이동: **없음**(정직한 답)

## 8. 남은 부채

| | 무엇 |
|---|---|
| **미배포** | PR #573(죽은 카드 수리)이 머지됐으나 prod 미배포 — 원가 메뉴 트랙 마이그 `pgprice1s1a` 대기. **남의 마이그레이션을 대신 적용하지 말 것** |
| **죽은 카드** | 141건(08:5x) → **154건**(10:17) — 배포 전까지 시간당 누적 |
| **레거시 파이프라인** | `negative_keyword`/`proposal_writer`가 게이트 0겹으로 매일 제안 생성, 전 기간 실행 0건. **PAO 소관인지 미상** |
| **문서 §9** | 문서·코드·라이브 어긋남 6건 미처분 |

## 9. 다음 가용 번호

**D-NAO-278** · 교훈 **#376**

## 착지

커밋: 하네스 `202778b`(별도 repo) · PR **#582** 머지 `bf7d0963` · PR **#587** 머지 `65c6cccf`. CI 각 3/3 실통과 · `--force` 전건 미사용. **멈춘 단계: 없음.**
⚠️ 단 **n=69의 prod 배포는 여전히 멈춰 있다** — 재개 명령은 `HANDOFF_dead-card-single-door_20260830.md` 「착지」 절.
