# HANDOFF — 승인 버튼이 «안 보이는 레시피»를 승인하던 결함 + 부자재 탭 도달성 + 계약 S4 신설

2026-08-24 (KST) · 세션 `1e34245f` · 체인 **「SellC 원가 메뉴」 n=5**
워크트리 `/Users/jino/.claude-worktrees/ohiselling/cost-menu-n5` (브랜치 `feat/cost-menu-n5`)
**PR #394 `4cd3de25`(코드·배포) · PR #395(기록물)** 병합 · **`f32cb611`(계약 개정) 미푸시**

---

## 0. 목표 — 다시 쓰지 마라

★**이 트랙의 목표에는 이름이 있다 — 「SellC 원가 메뉴 목표」.**
**정본은 `.claude/memory/chains/sellc-원가-메뉴.GOAL.md`다.** 목표를 다시 쓰지 말고 그 파일을 열어
앵커 `목표:`에 원문을 옮긴다. 트랙 헤더와 갈라지면 **카드가 이긴다.**

> ***"SellC에 원가 메뉴를 하나 붙이자. 여기에는 우리가 현재 알고있는 모든 원가가 있잖아? 그 원가를
> 가지고 계산되는 방법이 첨부된 파일처럼 나오고 그 계산이 되면 되지 않을까? 그리고 수입건에 대해서는
> 두번째, 세번째 첨부자료처럼 수입한 금액을 알려주면 너가 이런것들을 모아서 회계적으로 계산되는
> 원가를 만들면 되잖아. 그리고 그런 원가를 만들려면 재고사항이 필요할꺼고, 그건 ecount와 연결하면
> 될꺼고."*** / ***"목표는 정확한 원가 산출이니까?"*** (Jino 2026-08-22)

## 0-A. ★운영 원칙 — Jino 지시 (2026-08-23 13:00, n=2→n=5 승계)

> ***"다음 세션에서부터는 SellC 원가 메뉴 목표를 달성하는 위주로 진행되게 목표잡기와,
> 다른 트랙의 일을 집어와서 여기에 섞지 말라"***

**① 목표잡기는 역산이다** — 착수 질문은 「무엇을 할까」가 아니라 **「합격 6항목 중 어느 것을
[x]로 만드는가」**다. 앵커 `이번 세션:`에 겨냥 항목을 적는다.
**② 다른 트랙 일을 끌고 오지 않는다** — **같은 트랙 안의 계약 B도 남의 일이다.**

---

## 1. 30초 요약

**진행률 2/6 불변.** A1은 이번에도 안 열렸다 — **Jino의 승인 클릭이 아직 없다.**

그런데 이번 세션은 **A1을 누르면 안 되는 상태였던 것을 고쳤다.**

| 무엇 | 상태 |
|---|---|
| ★**승인 버튼이 «화면에 안 보이는» 레시피를 승인할 수 있었다** | 수리·배포 완료 |
| 부자재 탭 도달성 4건(스크롤·폭·드롭다운·참고값 열) | 배포 완료 |
| 엑셀 참고값이 화면에 원리적으로 못 닿던 것 | 배포 완료 |
| 레시피 탭 스크롤 · 수입/비수입 구분 | **계약 S4로 신설, 미구현** |

---

## 2. ★다음 세션이 «가장 먼저» 할 일

### 2-A. A1 라이브 관측 (n=3→n=5 승계, 여전히 열림)

`https://sellc.ohitech.co.kr/cost` → **레시피 탭**
1. 제품 검색 **「빛반사」** → 제품 「…매트 필름 3매」 → 폼팩터 **`bar`**
2. 구성 **9줄** + **참고값 열**(600·30·22·60·8·13·98·6·100) 확인 · **합계는 「—」여야 한다**
3. **「이 구성을 승인한다」** → **「엑셀 참고값을 단가로 채택」**
4. **표준원가 보드** → 같은 제품 → **2,350.70원**

관측되면 → 트랙 `- [ ] A1` → `- [x]`(증거 좌표 병기) → **진행률 3/6**.
★**승인 클릭은 계약 §2-2가 사람 몫으로 못 박은 자리다**(교훈 #274). 이번 세션도 안 눌렀고,
그 증거로 배포 후 `cost_recipe` 100건이 전부 `draft`임을 확인했다.

### 2-B. S4 구현 (계약 개정 승인분 — 합격 10~13 겨냥)

**★착수 전 `f32cb611`을 push·PR·병합하라 — 계약 개정이 아직 origin/main에 없다.**

- **㉮ 레시피 탭 목록 독립 스크롤** — `MaterialList` 호출부엔 `md:sticky md:top-4
  md:max-h-[calc(100vh-14rem)] md:overflow-y-auto`가 있는데 `RecipeList` 호출부엔 **없다**
- **㉯ 수입/비수입 구분** — 판별은 새 필드·마이그 없이 「이 종에 대응하는 원장 라인이 있는가」

| | 비수입 (현재 128종) | 수입 (현재 cleaning kits 1종) |
|---|---|---|
| 엑셀 값 | **정본** 「엑셀 단가(미확정)」 | **대조값** |
| 안내하는 길 | 확정 1개(채택 또는 수동 입력) | 원장 연결 우선 |
| 원장 부자재 라인 표 | **안 그린다** | 그린다 |

★**합격 13이 안전장치다** — 종별 표를 걷어내면서 **미매칭 전건 감시를 통째로 없애지 마라.**
「원장에 부자재 라인이 있는데 어느 종에도 안 붙었다」가 화면에서 사라지면 단가 이력이 조용히 빈다.
**고치는 게 아니라 «자리를 옮기는» 것**이다 — 별도 접힌 섹션으로.

---

## 3. 무엇을 만들었나

### 3-1. ★결함 — 승인 버튼이 «안 보이는» 레시피를 승인할 수 있었다 (`e74b6611`)

Jino: *"100W C타입 to C타입 PD 고속충전TPE 케이블 부분은 레시피에서 제품이 바뀌었는데, 왜 안바뀌고
그대로 있어?"*

제품 필터로 왼쪽 목록이 3건으로 좁혀져도 **오른쪽 상세 패널이 필터 밖 레시피를 계속 붙들었다.**
그 상태에서 「이 구성을 승인한다」를 누르면 **화면에 없는 엉뚱한 레시피가 승인된다** — A1 판정의
바로 그 버튼이다.

원인: `selectedRecipe`가 `filteredRecipes`가 아니라 전체 `recipes`에서 조회 + 필터 변경 시
선택을 재검사하는 코드 **부재**.

★**같은 병의 아홉 번째.** n=4가 「유령 선택」으로 닫은 건 **셀렉트 박스**였고 **상세 패널은 그
수리에서 빠졌다.** 테스트 736건 전건 초록이던 이유 = 이 성질을 재는 테스트가 **0개**.

고침: `reconcileSelectedId<T>` 단일 함수 + `useLayoutEffect` 단일 호출부. `load()`는 선택을
안 건드린다(진실의 원천 **둘 → 하나**).

### 3-2. 부자재 탭 도달성 (`4a358768`)

| | Jino 원문 | 무엇을 했나 |
|---|---|---|
| A | *"부자재 종만 내려가고 전체 화면은 고정되게"* | `sticky` + `max-h` + `overflow-y-auto` |
| B | *"오른쪽 빈 곳이 넓어 … 옆으로 키우자"* | `260px` → `minmax(22rem,28rem)`. 증상은 이름 2~3줄 접힘·「미확인」 배지 «미/확/인» 세로 깨짐 |
| C | *"부자재 종도 드랍다운"* | 공용 `ProductOptionPicker` 재사용 — 폼팩터 8칸 + `part` 2단 |

★**C에서 사실을 숨기지 않았다** — prod 129종 중 **83종이 `part` 공백**이라 「(부품 미지정) (N)」로
건수를 라벨에 박았다. 안 그러면 2단 필터가 왜 대부분 안 걸리는지 아무도 모른다.

### 3-3. ★엑셀 참고값이 화면에 원리적으로 못 닿고 있었다 (`4a358768`)

Jino: *"각 부자재에 대한 단가도 들어가야 하는거 아닌가?"*

```
recipe_parser.py:9 주석 : "excel_ref_price에 참고값으로 실려 «화면에 보이기만 하고»"
실측               : grep -rn "excel_ref" frontend/src/  →  0건 · API 응답  →  0건
prod               : 단가 보유 종 1/129  vs  엑셀 참고값 보유 종 128/129
목록이 하는 말      : 전부 「단가 없음 — 원장 연결 또는 수동 입력 필요」
```

**할 일을 둘로 제시하는데 실제로는 셋이고, 빠진 셋째가 가장 싼 길이었다.**

고침: 부자재 API에 `excel_ref_price` 노출 · 계산 내역에 **「엑셀 참고값(채택 전 · VAT 제외)」 열**
· 안내문 정정.
★**없는 조작을 지시하지 않는다** — 「채택」은 레시피 단위 동작이라 부자재 탭엔 그 버튼이 없다.
문구가 그 사실을 직접 밝힌다.
★**VAT 기준을 이름에 박았다**(리드 결정) — 이 화면 기본 표기가 VAT **포함**(D-CPP-51)이라 기준을
안 적으면 참고값이 반대로 읽힌다. `adopt_excel_prices`가 이 값을 `unit_price_ex_vat`로 쓰므로
**VAT 제외가 사실**이고, `recipe_parser.py:415`가 `Σ(ref×수량)×1.1 ≈ 엑셀 제품원가(+VAT)`로
독립 검산한다.

### 3-4. 계약 A′ 개정 — S4 신설 (`f32cb611`, Jino 승인 00:18)

Jino: *"이것도 계약에 같이 넣어줘"* → **순수 추가 96줄 · 삭제 0줄**(§1 목표·§3 금지선·S1~S3·
합격 1~9 바이트 단위 불변).

- **§0-C 정정 블록** — 아래 §5 참조
- **§6 S4** 「화면 도달성 + 수입/비수입 구분」, S3와 **독립**(계산·저장 무변경)
- **§7 합격 10~13** — 전부 「Jino가 화면에서 본다」로. 10번엔 ★「jsdom이 원리적으로 못 잰다 —
  라이브 눈 확인이 판정이다」 명시
- **§8** S4 몫 1줄 ⚠️「세션 1회」 상한은 **지시에 없던 값을 위임받은 기가 정했다**(문서 내부
  모순 방지). 승인 전 Jino에게 밝혔다.

★**계약 §3 금지선은 유지된다** — 그 조항은 *"원장 파생이거나 **Jino가 입력·승인한 값**"*이고
「채택」 클릭이 곧 그 «입력·승인»이다. 바뀐 것은 금지선이 아니라 **화면이 어느 길을 안내하고
값을 어떻게 부르는가**다.

---

## 4. 적대 리뷰 — 2라운드, 1R FAIL → 2R PASS

### 라운드 1 (커밋 `e74b6611`)
**P1**: 「이 구성을 승인한다」 버튼을 **742건 중 어느 하나도 안 지킴** — 버튼 절단 · 상태 분기
반전(`=== "approved"` → `!==`) · 영구 비활성 3종이 **전부 초록 생존**. 전수 grep 결과
「이 구성을 승인한다」·「승인 취소」가 테스트 코드에 **0회 등장**.

★★**리드(나)의 위임문이 그 구멍을 반쯤 만들었다** — 표면 절단 변이를 요구하며 예시로
「상세 패널 통째 제거」 같은 **굵은 절단**을 들었고 구현 기가 그걸 쳐서 KILLED를 받았다.
**합격기준이 지목한 «버튼 한 개짜리 절단»은 아무도 안 쳤다.**
⇒ **표면 절단 변이는 «가장 굵은 절단»이 아니라 «합격기준이 지목한 그 위젯»을 끊어야 한다.**

### 라운드 2 (커밋 `4a358768`, 변이 **24종**)
- **P1-1**: 필터 걸린 채 「+ 종 추가」 → **초록 「추가됨」이 뜨는데 새 종이 화면 어디에도 없다**
  (`create_material`이 `form_factor`를 **항상 null**로 둬 필터 밖으로 떨어진다). **이 배치의
  드롭다운이 만든 결함**이고 리뷰어가 재현했다. 두 번 누르면 「이미 등록돼 있다」 빨간 에러.
- **P1-2**: 부자재 탭 「승인」·「+ 수동 단가 입력」 표면 가드 0 — **호출부 배선 절단**까지 생존.
  ★M22는 **이 배치가 자초했다**: 새로 쓴 안내문이 *"③「+ 수동 단가 입력」"*이라고 **그 버튼의
  존재를 단언**하면서, 경로 ①만 「없는 버튼을 가리키면 거짓말」 검사를 하고 **자기가 새로 단언한
  ③은 안 붙들었다.**
- **§3 금지선 누수 경로 0건** — 백엔드 6경로·프론트 4경로 개별 확인 + **반대 방향 변이 2종**
  (합계에 Σ(참고값×수량) 더하기 · 백엔드 `_resolve_line` 참고값 폴백) **둘 다 KILLED**.

### 2R (커밋 `038de271`) — **PASS, 미해소 P1 0건**
변이 6종 전부 KILLED. 특히 **M23(호출부 배선 절단)**이 죽은 이유는 새 테스트가 버튼 텍스트가
아니라 `patchCostMaterial`의 **실호출 여부**를 재기 때문이다 — 텍스트만 재는 테스트로는 원리적으로
못 잡을 자리다. **R2**(필터가 없었는데 「해제했다」고 말하는 변이)도 KILLED — 안 한 일을 했다고
말하는 거짓말이 가드되고 있다.

⚠️ **M16은 여전히 SURVIVED**이고 그게 정직하다 — `useLayoutEffect`가 페인트 전에 선택을 동기
재조정하므로 두 조회식이 **관측 가능한 차이를 못 만든다**(1R 리뷰어 판정과 일치). 테스트는 조회식이
아니라 **그 방어층이 지키는 화면 결과**를 잡았다.

---

## 5. ★★Jino 정정 — 리드의 확대 인용이 오류였다 (원문 보존)

> ***"내가 말한 「사실은 fix 될 숫자는 제품원가(CYN) 뿐이고 나머지는 계속 이렇게 업데이트가 되는게
> 맞거든」 이 부분은 수입하는 경우 환율, 운송비용이 바뀌기 때문에 원가밖에 fix된게 없다는 거였어.
> 수입하는게 아닌 물건은 엑셀파일에 있던 값이 맞고, 단가가 조정되면 그때 수정을 별도로 하면 되지"***
> (2026-08-24 00:06)

**내 오류**: 그 문장을 **모든 부자재에 대한 엑셀 불신**으로 확대 인용해 「엑셀 참고값(단가 아님)」
문구의 근거로 삼았다. 실제 범위는 **수입건**이다.

**prod 실측이 정정을 뒷받침한다**:
```
확정 수입건 2건 · 수입 라인 17건
  material  2건 → 전부 「cleaning kits」 (부자재 1종)
  product  15건 → Glass_* 강화유리 계열
cost_material.match_rule 보유 종 = 0
```
⇒ **수입 부자재는 1종뿐, 나머지 128종은 비수입 = 엑셀이 정본이다.**

**그래서 지금 화면이 128종에게 틀린 말을 한다**: ①「단가 아님」이 «값이 못 미덥다»로 읽히는데
실제로는 **아직 «넣지» 않았을 뿐** ②「원장 연결」을 첫 번째 길로 안내하는데 그 종엔 원장 라인이
**0건이라 영영 안 오는 길**이다.

★**Jino 질문 ②(원장 표가 모든 종에 뜸)와 이 정정은 별개 결함 둘이 아니라
「수입산과 비수입산을 안 가른다」 하나다.**

---

## 6. ★★이 세션의 교훈 — 「같은 병을 고칠 때 «한쪽만» 고친다」 다섯 번

| # | 어디를 고쳤나 | 무엇을 빠뜨렸나 | 누가 잡았나 |
|---|---|---|---|
| ① | 「유령 선택」을 **셀렉트 박스**에서 (n=4) | **상세 패널** | Jino의 눈 |
| ② | 승인 버튼 가드를 **레시피 쪽**에 | **부자재 쪽** | 적대 리뷰 |
| ③ | 안내문이 **경로 ①**만 실재 검사 | 자기가 새로 단언한 **③** | 적대 리뷰 |
| ④ | `reconcileSelectedId` 제네릭화(**로직** 통합) | **가드**는 레시피 쪽에만 | 적대 리뷰 |
| ⑤ | 독립 스크롤을 **부자재 탭**에 | **레시피 탭** | Jino의 눈 |

**일반형: 수리·단언을 «한 곳»에 적용했으면, 같은 형태가 있는 «나머지 곳»을 그 자리에서 세어라.
로직 통합이 가드 통합을 자동으로 주지 않는다.**

★**⑤는 앞의 넷과 성격이 다르다 — 내가 만든 구멍이다.** 위임문에 「부자재 탭」이라고 **좁게
지목**했고 구현 기는 그 좁은 범위를 **정확히 지켰다.**
⇒ **위임문판: 표면을 좁게 지목하면 구현 기는 그 좁은 범위를 정확히 지킨다 — 그게 곧 다음 결함이
된다. 「같은 구조가 이 화면에 몇 군데 있는가」를 위임문이 먼저 세야 한다.**
(프로젝트 CLAUDE.md D-NAO-227 「집행 지점을 좁게 쓰면, 규칙을 지켜도 목적이 샌다」의 위임문판.)

---

## 7. 완료 QA — 판정 원문 그대로

별도 서브에이전트(Sonnet·읽기 전용)가 3대조를 **각각 따로** 판정했다. **원문 그대로 옮긴다 —
미달·판정불능도 그대로다.**

> **판정(계약 A′ §7): 부분달성 — 달성 6(항목 1·2·3전반·4·8·9) / 판정불능 4(항목3후반·5의
> `cost_drift`·12·13) / 미달 5(항목6·7 — 범위 밖 기존 처분 · 항목10·11 — 이번 세션 신설 기준
> 미구현). 항목12는 미달이 아니라 판정불능으로 분류했다(우연히 관측치는 참이나 설계 의도인
> 분기 로직 자체가 없다). (2026-08-24 00:23 KST)**

> **판정(Jino 지시 원문): 부분달성 — 달성 6(①③④⑤⑨·운영원칙) / 미달 3(⑥⑦⑧ — 전부 이 세션
> 후반부 라이브 발의 중 "다음 세션 대기"로 앵커가 스스로 자백한 항목) / 판정불능 1(② 코드
> 포함은 확인했으나 라이브 클릭 재현 미실시). 목표 원문의 궁극 문장 「정확한 원가 산출」은
> 승인(사람 몫)이 아직 없어 미완이고, ④(재고·ecount)는 계약 §1이 처음부터 범위 밖으로 명시한
> 것이라 전환이 아니다. (2026-08-24 00:23 KST)**

> **판정(트랙 궁극 목표): 부분달성 — 달성 2/6(B1·B2, 승계·라이브 재확인) / 미달 4/6(A1·A2·
> C1·C2). 진행률 2/6 불변, 체크박스 이동 0건. A1 미달은 태만이 아니라 「승인 클릭은 사람 몫」
> (계약 §2-2·전역 교훈 #274)을 이 판정기도 지킨 결과이고, `cost_recipe` 전건 draft·
> `cost_standard` 0행이라는 라이브 DB가 그 사실과 정확히 일치한다(유령 아님).
> (2026-08-24 00:23 KST)**

### ★판정기가 잡은 메타 발견 (리드가 놓친 것)

> *"항목 10~13의 **정본 자체가 미병합이다.** `git log origin/…..HEAD` = 커밋 `f32cb611`
> (§7 합격 10~13을 추가한 계약 개정) 1건, **push 0회**. 즉 이 세션은 «10~13을 구현했는지»와
> 별개로 **그 합격기준 자체를 아직 origin에 싣지 않았다** — 위 표의 미달·판정불능은 "구현
> 실패"라기보다 "**구현 착수 전에 QA를 받았다**"는 사실을 그대로 반영한다."*

**리드 주석**: 맞는 지적이다. 계약 개정은 Jino 승인 직후 **커밋만** 했고(Jino 지시가
*"커밋하고 다음 세션으로 넘겨줘"*였다) 세션 종료 착지에서 push·병합한다. **다음 세션은
S4 착수 전에 `f32cb611`이 `origin/main`에 있는지부터 확인하라**(§2-B).

### 판정기가 확인하지 못한 것 (원문)

1. **라이브 UI 클릭 재현 전부** — `/cost`는 인증이 필요한 화면이라 로그인하지 않았다(승인 클릭
   금지와 같은 이유). 위 판정은 전부 **서버 API·DB·배포 번들의 정적 증거**이며, "브라우저에서
   스크롤이 실제로 고정돼 보이는가"·"필터 걸고 승인 버튼을 눌러도 유령 레시피가 안 걸리는가"
   같은 **jsdom이 원리적으로 못 재는 항목은 코드 포함 여부(git ancestry)로만 대리 확인**했다.
2. **적대 리뷰 1R FAIL→2R PASS의 리뷰 원문** — PR #394에 리뷰 코멘트가 없음을 확인했을 뿐
   트랜스크립트를 열람하지 않았다. **앵커의 서술을 그대로 신뢰하지 않았고**, 최종 산출물
   (배포된 커밋의 API 응답)로 결과만 독립 재현했다.
3. **`cost_drift` 배포 전 기준값** — 현재 `null`이 "이번 배포로 바뀐 것"인지 "원래도 null"인지
   판별할 배포 전 스냅샷이 없다.
4. **레시피 68 이외 나머지 99건의 개별 breakdown** — 표본 1건만 확인, 전수 미검사.
5. **항목 13(미매칭 「별도 섹션」)의 구조** — 문자열 존재만 확인, 새 섹션인지 기존 문구의 잔재인지
   번들 grep만으로 구별 불가.

★**3번은 다음 세션이 닫을 수 있는 부채다** — 배포 전 `cost_drift` 스냅샷을 안 떴다. 무해성
기준선을 `product_master` 두 값으로만 잡아 온 관례의 빈틈이다.

---

## 8. 트랙 진행률

- 트랙: `docs/tracks/active/track_cost-truth-ledger.md`
- **시작 2/6 → 종료 2/6 (불변)**
- 달성 유지: **B1**(원장 스키마+3중 검산) · **B2**(8/18 실건 라이브)
- 미달: **A1**(승인 클릭 대기) · A2(S3) · C1 · C2
- 이번 세션이 움직인 것: **체크박스 0개.** 대신 **A1을 누르면 안 되는 상태였던 것을 고쳤고**,
  도달성 4건 + 참고값 노출을 배포했으며, **계약에 S4·합격 10~13을 신설**했다
  — 증거: PR #394 `4cd3de25` · PR #395 · `f32cb611`(미푸시)
- 트랙 목표 원문: §0과 동일
- 다음 세션 후보: **§2-A(A1 관측)** → **§2-B(S4 구현)**

---

## 9. 산출물 좌표

- `a77bc322` n=4 종결 등록 이관 → `e74b6611` 상세 패널 결함+폭 → `3ed01a7d` 1R P1 해소
  → `4a358768` 배치 A~G → `038de271` 1R P1-1·P1-2·P2-1 해소 → 기록물 → **PR #394 `4cd3de25`**
  → **PR #395**(기록물) → **`f32cb611`**(계약 개정, **미푸시**)
- **prod 배포 완료** — 백엔드 3파일 `--restart`(무중단 블루-그린, 다운타임 0초) + 프론트
  `--frontend` CAS 통과. 스탬프 `4cd3de25 dirty=0`
- ★**배포 전 뒤처짐 20커밋** 발견 → 병합 → 재빌드 → 배포(프론트는 `dist` 통짜 rsync라 병합 없이
  올리면 그 창의 다른 세션 작업이 prod에서 사라진다 — 교훈 #350, **4세션 연속 적중**)
- ⚠️ `safe_merge.sh --force` 2회 — CI 3건 FAILURE지만 `steps:0`·2초 종료·로그 없음으로
  **결제 정지 서명을 실측 확인**(코드 결함 아님). 자백 로그 `$TMPDIR/safe_merge.log`
- 테스트: 프론트 **778 passed / 58 files**(병합 후) · `tsc -b` 통과 · 백엔드 cost_menu·
  import_cost **156 passed** · 전체 6397 passed / **2 failed는 사전 존재**(`git stash` 대조,
  헬스 라우트 소관)
- **무해성**: `product_master` **963행 · SUM 3642052.1** — 배포 전후 완전 동일
- **승인 0건 불변**: `cost_recipe` 100건 전부 `draft`·`approved_at NULL` · `cost_material_price`
  2행 — **모델이 승인·채택을 대신 누르지 않았다는 증거**

---

## 10. 이월 (고치지 않고 운반 — 소관 지목)

**이번 세션이 새로 만든 것**
- **M16 SURVIVED** — 부자재 `selected` 조회식 자체는 가드 안 됨(관측 가능한 차이가 없어 P2로
  분류). 소관: 이 계약
- `line_count === 0` 비활성 사유 문구가 버튼과 `aria-describedby`로 **묶여 있지 않다**. 소관: 이 계약
- **P2 기록 2건**: 안내 경로 ②의 「연결」 버튼은 원장 대응 라인이 있는 종에만 뜬다 ·
  `breakdown_payload`는 `"600"`, `standard_payload`는 `"600.00"`(기존 관례와 동일)

**★훅 오탐 계수 (계약 §6 재심 안건 — 실측됐다)**
- `review-surface-mutation.sh`: **누적 2건**(직전 세션 수정 위임 1 + 이번 **배포 위임** 1).
  이번 건은 위임문에 「리뷰」 문자열조차 없고 적대 리뷰가 이미 2R PASS로 끝난 뒤였다. 소관: 하네스
- `track-progress-sync.sh`: 이번 세션에서만 **10회 이상 발동, 전건 오탐**. 뿌리 실측 —
  공유 메인 폴더 HEAD가 `origin/main`보다 **184커밋 뒤처졌고**(미커밋 35파일 = 병행 세션들 작업)
  훅이 읽는 트랙 파일이 그 낡은 사본이다. **달래려고 공유 폴더에 줄을 덧쓰지 않았다** — 184커밋
  뒤진 파일 위에 줄을 얹으면 그 폴더가 pull할 때 다른 세션들이 append하는 같은 파일에서 충돌이
  난다. **3세션 연속**. 소관: 하네스(전역 훅)

**n=3·n=4에서 승계 (여전히 유효)**
- `scripts/safe_deploy.sh`가 **prod에 없는 신규 디렉터리를 만들지 않는다** → 소관: 배포 스크립트
- `scripts/next_ids.sh`가 **D-CPP를 발급하지 않는다** → 소관: 스크립트(별건). 이번 계약 개정의
  결정 번호를 `D-CPP-미발급`으로 둔 이유다
- **M7**(핀 고정 제품 중복 렌더 커버리지 공백) · **P2 3건**(드래그 플리커·정적 `aria-label`·
  `role="button"` 중첩) — 실브라우저·AT 확인 선행
- **P2-2**: `board()`가 레시피마다 재계산 → S3에서 재검토
- **계약 B 소관**(고치지 않음): 수입건 id=1 관세 `is_duty` 미표시 · 자금정산서 파서 미구현 ·
  13개월 소급 적재
- **계약 §9 이월**: 전역 `PRAGMA foreign_keys` 미설정 · 단가 없는 라인 거부 무테스트
- AI_office 수입원가 트랙 중복 → **Jino 결정 대기**

---

## 11. Jino 답 대기 1건 (n=2→n=5 승계, 여전히 열림)

상품명 하나에 옵션이 최대 **163종**(갤럭시Z 56 · 아이폰 45 · 갤럭시S 34 · 갤럭시A 17 · LG V40).
**부품 «종류»는 같아도 필름 «면적»은 기종마다 다르다.** 계약은 **폼팩터 9종**으로 가르기로 했으나
**같은 폼팩터 안의 기종 차이**는 열려 있다. 답은 **A2(S3)의 정밀도**를 정한다(A1·S4는 답 없이 진행 가능).

---

## 12. 못 한 것

- **A1** — 승인·채택은 사람 몫이라 누르지 않았다. 이번 세션도 열지 못했다.
- **S4 구현** — 계약에 실었을 뿐 코드는 0줄. `f32cb611`도 **미푸시**다.
- **jsdom이 못 재는 4건의 라이브 눈 확인** — 종 목록만 스크롤되는가 · 「미확인」 배지가 세로로
  안 깨지는가 · 폭 768~1000px에서 안 겹치는가 · 최초 진입 깜빡임이 없는가. **배포는 됐고 관측이
  안 됐다.**

🤖 Generated with [Claude Code](https://claude.com/claude-code)
