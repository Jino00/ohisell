# 세션 인수인계: 「점화준비 목표」 S4 완주 — 학습 가능 파라미터 3→5, 그런데 다섯 다 브레이크였다 — 「PAO 논의 60」

> 저장일시: 2026-08-27 18:4x KST · 체인 `pao-논의` **n=60** · 세션 `e833f21c`
> 새 대화 시작 시 이 파일을 먼저 읽을 것

## ★관측 명령 (계약 §1 ⓔ — 최상단 고정)

```bash
curl -s "https://sellc.ohitech.co.kr/api/naver/ad/settings/guardrail-params" | python3 -m json.tool
```

⚠️ **API prefix는 `/api/naver/ad`다** — `/api/naver-ad`가 아니다(PR #503 본문에 내가 틀리게 적었다가 라이브에서 404로 잡혔다).

2026-08-27 18:3x 실행 시:

```
params (5행):
  cooldown_hours            value=2   source=code  봉투=[1, 24]
  max_daily_auto_bid_downs  value=3   source=code  봉투=[1, 8]
  max_auto_up_multiple      value=2.0 source=code  봉투=[1.0, 3.0]
  pl_min_click              value=5   source=code  봉투=[5, 10]     ← S4 신규
  pl_window_days            value=30  source=code  봉투=[14, 90]    ← S4 신규
window_coverage (2행):
  파워링크 제외 판정  expkeyword  promoted=true   상한 90일  결손 0일  covered=true
  쇼핑 제외 판정      shopping    promoted=false  상한 14일  결손 0일  covered=true
```

★`source=code`가 전부인 것이 **정상**이다 — `naver_account_settings`에 `guardrail_params` 행이 아직 없다(아무도 값을 넣은 적 없음). 라이브 `source`와 DB 상태가 일치하는 것이 이 표면의 자기검증이다.

---

## 1. 프로젝트 위치 및 환경

- 워크트리: `~/.claude-worktrees/Ohiselling/pao-n60` (브랜치 `feat/pao-n60`, base `origin/main`)
- prod: `ssh sellc.ohitech.co.kr` · 앱 `/home/ubuntu/ohisell/backend` · 인터프리터 **`.venv/bin/python3`**
- ★**DB 함정**: `/home/ubuntu/ohisell/ohisell.db`는 **4KB 미끼**다. 실 DB는 `/home/ubuntu/ohisell/backend/ohisell.db`(2.7GB). 날짜 컬럼은 `stat_date`가 아니라 **`ad_date`**
- 활성 프로세스: `ohisell-backend-8001`(:8001) — 이번 배포로 8011→8001 전환. **블루-그린이라 다음 배포에서 또 바뀐다**
- ⚠️ 이 워크트리엔 `frontend/node_modules`·백엔드 venv가 없다:
  - `ln -sfn /Users/jino/.claude-worktrees/ohiselling/accel-opening/frontend/node_modules frontend/node_modules`
  - `/Users/jino/.claude-worktrees/ohiselling/cost-menu-n9/backend/.venv/bin/python3`

## 2. 이번 세션 완료 목록

- ✅ **S4-a SPECS 승격 2건** — `guardrail_params.SPECS` 3→5(`pl_min_click`[5,10]·`pl_window_days`[14,90]). 게이트(`search_term_judge:618`·`:554`)와 **사람이 읽는 사유 문장**(`_pl_reason`) 둘 다 배선
- ✅ **S4-b 분산 import 검사** — 파라미터별 기록(승격 2·보류 2 + 각 좌표·사유)
- ✅ **#14 창 재료 커버리지** — `window_coverage`를 GET 응답 + 콘솔 화면에
- ✅ 적대 리뷰 **1R PASS(P1 0건)** · P2 3건 트리아지(1 채택·1 이월·1 기각)
- ✅ **PR #503 머지** · CI **3/3 실통과** · `--force` 미사용
- ✅ **prod 무중단 배포**(백엔드 CAS 3/3 + 프론트, **마이그 0**, 다운타임 0초) · 라이브 관측 · **행위 불변 실증**

## 2-1. 완료 QA (판정 원문 그대로 — 별도 Sonnet·읽기 전용)

- **작업 목적(정본 원문 — 계약 `docs/contracts/CONTRACT_ignition_readiness.md` §1)**: *"2026-08-26 실측으로 「임대 제외·재개방 실험·파라미터 학습·초기 탐색 허용」 운영 방식이 합의됐으나, 그 방식이 서기 위한 배선 11개가 없다. 되면 — 점화 결정이 나는 순간 — ⓐ탐색이 stale 벽 없이 돌고 ⓑ새 제외마다 등급·만료일이 붙고 ⓒ재개방이 일기→지혜 사슬에 타며 ⓓ학습이 게이트 숫자 4개를 승인 카드로 조정할 수 있고 ⓔJino가 명령 한 줄로 각 부품의 생존을 확인한다. 이 계약의 산출물은 성과가 아니라 «준비 완료 상태»다."*
- **합격기준(원문 — §4-C 중 S4 몫)**: S4-a 승인 카드/성적표 표면(prod GET 응답)에 새 파라미터 4개가 현행값·봉투와 함께 뜬다 — Jino가 그 응답에서 4행을 본다 / S4-b 분산 import 검사 결과가 파라미터별로 기록돼 있다(승격 4 · 보류 시 사유) / 공통 광고계정 쓰기 0 · 적대 리뷰 P1=0 · 마이그 `--migrate` 순서

> 대조 대상 4개를 **각각** 판정(전역 §2 강화 조항).

판정(계약 §4-C S4): **부분달성** — S4-b·공통(쓰기 0·마이그 0·CI 3/3·리뷰 P1=0)은 달성. S4-a는 라이브 표면 자체는 확인되나 리터럴 「4행」 대비 **2행만** 충족(§4-B⑤가 사전 승인한 분기이며 checklist 원문은 안 고치고 미체크 유지 — 과대주장 없음) (2026-08-27 18:5x KST)

판정(Jino 지시 원문): **달성** — 계약 §6 자기규정(「완료 슬라이스 건너뛰고 첫 미완부터」·「묻지 말고 진행」) 그대로 집행. n=59 생존판정(`end_kst` null) 확인 후 착수, F-3 스코프 밖 건 안건화 안 함, §7 대칭 미달을 축소 없이 보고. 「다시 봐봐」의 지시 대상은 트랜스크립트 밖이라 판정불능 성분 포함 (2026-08-27 18:5x KST)

판정(트랙 헤더 합격 M항목): **달성** — TRACK-CONTRACT 블록 실측 `- [` 7개 중 `- [x]` 2개(M0·M1), 착수 2/7 → 종료 **2/7 불변**. 자기보고와 라이브 카운트 일치 (2026-08-27 18:5x KST)

판정(북극성 §1·§6·§7): **달성** — `auto_operate=0`/`optimizer='none'` 9행 전건 prod 실측으로 ④자동화 0 확인. §7 액셀·브레이크 대칭 검사를 실제로 수행해 「승격 2개가 둘 다 브레이크」임을 코드(`ParamSpec.direction`)로 **자체 반증**했고 봉합하지 않고 미달로 남김 — D-NAO-226·227이 요구한 검토 방식의 정확한 이행 (2026-08-27 18:5x KST)

**종합 판정: 부분달성**

- **QA가 확인하지 못한 것(그대로 보존)**:
  1. 적대 리뷰가 실제로 «만든 쪽과 다른» 별도 서브에이전트였는지 — 정황상 신빙성 있으나 독립 재현 불가
  2. Jino 「다시 봐봐」가 구체적으로 무엇을 가리켰는지 — QA에 트랜스크립트 미제공
  3. 백엔드 전건 6,985 스위트를 QA가 직접 재실행하지 않음 — CI 3/3(PR #503 자체 실행분)으로 대체
  4. 콘솔 화면을 브라우저로 시각 확인하지 않음(인증 필요)
- **목적 전환 여부**: 없음(선언 없음). 「안 함」 6항 침범 0건.

## 2-2. 트랙 진행률

- **트랙**: `docs/tracks/active/track_naver-ad-optimization.md`
- **트랙 목표 원문**: *"무조건 이익스팟 순위에 있어서 매출 증가가 없는것보다 Roas는 떨어지지만 매출이 늘어서 총 이익이 늘어나는 경우, 구간도 분명히 있거든. 우리의 최종목표는 이거야. 이게 우리가 만든 MOP프로그램의 최종 목적이고 목표야."* (Jino 2026-07-19, D-NAO-59)
- **진행률**: 시작 **2/7** → 종료 **2/7** (불변) — 달성 M0·M1 / 미달 M2·M3·M4·M5·M6
- **이번 세션이 움직인 항목**: **없음.** 계약 §7-1이 *"어느 M 체크박스도 직접 못 움직인다"*고 자기규정. S4는 **간극 4(학습, L4)** 인접
- ⚠️**진행률 계수 함정**: 트랙 파일 전체를 `^- [`로 세면 **14**가 나온다(구식 P0~P5 체크리스트가 섞임). 반드시 `<!-- TRACK-CONTRACT -->` 블록 안에서만 셀 것
- **헤더에 남긴 확인 줄**: 2건 누적(구현·리뷰 결과 / 북극성 대비 검토)
- **다음 세션 후보**(★n=61 갱신 — **두 축을 구분해서 고를 것**): 점화를 앞당기려면 **S5 / S6 / A-7**(배선 잔여, 1~2세션) · 트랙 진행률 2/7을 움직이려면 **M2-z 종결 판정**(2/7→3/7의 유일 후보, 새 코딩 아님 — M2-a·b·b2·c가 08-21 완료·QA됐는데 종결 판정이 미아). **M3·M4·M5는 어느 쪽을 해도 안 움직인다** — 점화(C-1)에 잠긴 닫힌 고리. 전체 배치는 `docs/references/104_pao_ignition_checkpoint_20260827.md` **§0-A**
- **트랙 종결 여부**: **미도달**(2/7)

## 2-3. 착지

- **완료 단계**: **전 단계 완주.** 커밋 → push → PR → 적대 리뷰 1R → 머지 → prod 배포(백엔드+프론트) → 라이브 관측 → 완료 QA
- **멈춘 단계**: **없음**
- **좌표**: `a88b3dd0`(SPECS 승격) → `5cd6b149`(착수 기록) → `aa698b9e`(#14) → `c25bc55c`(P2-1 프론트) → `9be0ce56`·`f678e591`(origin/main 병합) / PR **#503** 머지 · CI **3/3 실통과**(py3.10 9m36s·py3.14 7m34s·frontend 1m24s)
- **리뷰 판정**: **1R PASS(P1 0건)**
- **배포**: 백엔드 `safe_deploy.sh --restart`(CAS 3/3 · 무중단 0초 · 활성 `:8001`) + `--frontend`(스탬프 `f678e591dfb0`). **마이그 0**
- **L4**: `origin/main`이 PR #502로 앞서 있어 **배포 전 병합**(`f678e591`)
- **L5**: 「main에 세워둔다」 **생략** — 로컬 `main`이 공유 메인 폴더에 점유됨. 다음 세션은 `git switch -c <새> origin/main`

## 3. 확정된 결정사항

- **D-NAO-262** — 파워링크 제외 게이트 2종 SPECS 승격 + 창 재료 커버리지 표면.
- ★**승격은 4개가 아니라 2개다** — 계약 §4-B⑤ *"분산돼 있으면 승격하지 않고 기록만 한다"*의 집행. 보류 2건의 분산은 `max_change_pct` 전례와 **형태가 다르다**: 저건 「풀면 레버가 죽는」 것이고 이건 **「승인했는데 반영이 반만 되는」** 것이다.
  - `_SS_WINDOW_DAYS` — `naver_execution_harness.py:1245`가 직접 읽는다(GATE ⑥ 실행 재검증). 그 재사용은 주석이 밝히듯 «판정↔실행 신선도 갭»을 메우려고 **일부러** 한 것이라, 갈리면 그 목적이 정확히 깨진다
  - `_SS_MIN_CLICK` — `search_term_exclusion_list.py:48`에 **복제 리터럴 `MIN_CLICK = 10`**, 그 위 주석이 «SS 게이트와 같은 값»이라 **단언**. 카드에서 내려도 하류가 안 따라온다 ⇒ **승인 카드가 거짓말을 한다**
  - 되살리려면 그 두 자리를 먼저 배선하고(S2·S3가 쓴 «자리를 하나로 묶고 인구조사 테스트» 모양), 가드 테스트(`test_분산이_남은_SS_게이트_2종은_아직_SPECS에_없다`)를 「배선됐음」을 재는 것으로 바꿀 것
- ★★**#14의 전제가 반증됐다**(§4-B④ⓑ — 목표 유지·구현만 사실에 맞춤·**합격기준 원문 무수정**): 「원본 보존 16일」은 우리 DB 보존이 아니라 **네이버 stat-reports 보관 기한**이다(ref 21 §10). 우리 쪽엔 purge가 **없어서** prod는 이미 shopping **53일**·expkeyword **373일**을 갖고 있고, 봉투 상한 90일 창의 **결손 0일**이었다 ⇒ 「연장」은 할 것이 없다. 실질은 **「늘리기」가 아니라 「늘려도 되는지 상시 보이게 하기」**.
- ★`promoted`와 `note`를 가른다 — 「봉투 없음(승격 보류)」과 「재료 없음」은 **직교하는 사실**이고 한 문자열에 담으면 하나가 다른 하나를 덮는다(테스트가 잡았다).

## 4. 핵심 파일 목록

| 파일 | 역할 |
|---|---|
| `docs/contracts/CONTRACT_ignition_readiness.md` | **정본 계약** — §4-A 슬라이스 · §4-B 자동 판단 · §4-C 합격기준 |
| `backend/app/services/naver_ad/guardrail_params.py` | SPECS 3→5. 승격 2건 + **보류 2건의 사유·좌표 주석**(S4-b 산출물) |
| `backend/app/services/naver_ad/search_term_judge.py` | `_pl_params(db)` 신설(지연 import로 순환 차단) · 게이트·`_pl_reason` 배선 |
| `backend/app/routers/naver_ad.py` | `guardrail_params_window_coverage` 신설 + GET 응답 배선 |
| `backend/tests/test_pl_gate_specs_promotion.py` | **16건** — 등재·게이트·사유 문장·폴백·인구조사·커버리지 |
| `frontend/src/pages/naverAdGuardrailWindowCoverage.test.tsx` | **5건** — 화면 렌더(결손 표기·승격 보류 배지) |

## 5. 알려진 이슈 / 주의사항

1. ⚠️★**시크릿 재발급 (Jino 손, n=55·n=58 승계)** — API 시크릿 3세트 + `CLAUDE_CODE_OAUTH_TOKEN`. **`/home/ubuntu/.pm2/dump.pm2`를 열지 말 것**
2. ⚠️ **학습 재료 만료 2026-10-28** — `blocked` 일기 3,863건이 90일 수확 창 이탈. 오늘 기준 **D-62**
3. ⚠️ **훅 오탐 상습** — 공유 메인 폴더가 `origin/main`보다 **558커밋** stale이라 `[체인] ⛔`·`[트랙 기록 누락]`이 매 턴 뜬다. 하네스 소관
4. ★**출처 미상 커밋 2건** — `a88b3dd0`(17:36)·`5cd6b149`(17:38)이 **내가 `git commit`을 실행하지 않았는데** 만들어져 있었다. 내용은 정확히 내 작업이고 남의 파일이 섞이지 않았으며, `~/.claude/hooks/*.sh`에 `git commit`은 없다. 작성자 `dgfrty00@gmail.com`은 저장소 `user.email` 설정값이라 «누가»를 못 가린다. **다음 세션은 커밋 이력을 자기 것으로 단정하지 말 것**
5. ★**`window_coverage`는 창을 `latest` 기준으로 잡는다** — 「오늘」 기준이 아니다. 그래서 원본이 D-2로 낡아도 `covered=true`가 나온다(실제로 두 source 다 최신이 08-25 = D-2). 신선도는 같은 응답의 `retro_freshness`가 따로 재지만 **그건 소급채점 축이지 검색어 원본 축이 아니다** → 이월
6. **P2-2 이월** — `window_coverage` 쿼리가 GET마다 ~0.23초 추가(`source` 컬럼 무색인, 테이블 314만 행). 온디맨드 호출·폴링 없어 지금은 문제 아님 → 주기 감사
7. **P2-3 기각(기록만)** — `spec.default is judge._PL_MIN_CLICK` identity 비교는 CPython 소정수 캐시에 기댄다. 기존 3종 SPECS 테스트의 관례 재사용이고, `==`로 바꾸면 「복사 아닌 참조」 단언의 의미가 사라져 기각

## 5-1. ★이 세션이 배운 것

1. ★★**인계가 「대칭에 직결」이라 적었는데, 열어 보니 여는 것도 브레이크였다.** n=58·n=59가 S4를 *「학습 가능 파라미터 3개가 전부 브레이크인 봉투를 여는 유일한 슬라이스」*로 연속 권고했다. 실제로는 계약 §4-B⑤가 지정한 4개가 **전부 제외 게이트**라, 승격 후에도 **5개 전부 브레이크 계열**이다(`cooldown_hours` 진동방어·`max_daily_auto_bid_downs` 하향상한·`max_auto_up_multiple` 상향 **상한**·`pl_min_click`·`pl_window_days` 제외 게이트). «브레이크의 세기»는 학습으로 조절 가능해졌으나 **«액셀 자체»(승격·확장)의 학습 가능 파라미터는 여전히 0**이다. **인계의 「이걸 하면 X가 해결된다」는 그 자체가 실측 대상이다.**
2. **기존 인구조사 테스트가 나를 먼저 잡았다** — `test_every_param_actually_wins_over_code_constant`가 SPECS 3→5로 늘자 빨개졌다. 「키를 등재만 하고 «DB가 이긴다»를 증명 안 하면 통과 못 한다」는 가드였고, 단언을 약화하지 않고 새 키 2개를 증명 대상에 넣어 해소했다. **좋은 인구조사는 다음 사람을 막아 세운다.**
3. **변이 M9가 살아남은 이유는 테스트가 «한쪽 분기만» 밟았기 때문** — 빈 DB로만 재서 `latest is None` 경로만 검사했고, 데이터가 있는 경로의 `promoted`는 아무도 안 지켰다. **fixture가 밟는 분기가 곧 테스트의 실제 커버리지다.**
4. ★**내 실수: 변이 원복에 `git checkout --`를 써서 미커밋 수정을 날렸다.** 이후 전부 `cp` 백업으로 원복했다. **미커밋 작업이 있는 트리에서 `git checkout --`는 파괴 명령이다.**

## 6. 다음에 할 작업 (미완료)

- **이어지는 작업의 목적(원문 — 계약 §1, 받는 세션은 이걸 앵커 `목표:`로 그대로 복사한다)**:
  > *"2026-08-26 실측으로 「임대 제외·재개방 실험·파라미터 학습·초기 탐색 허용」 운영 방식이 합의됐으나, 그 방식이 서기 위한 배선 11개가 없다. 되면 — 점화 결정이 나는 순간 — ⓐ탐색이 stale 벽 없이 돌고 ⓑ새 제외마다 등급·만료일이 붙고 ⓒ재개방이 일기→지혜 사슬에 타며 ⓓ학습이 게이트 숫자 4개를 승인 카드로 조정할 수 있고 ⓔJino가 명령 한 줄로 각 부품의 생존을 확인한다. 이 계약의 산출물은 성과가 아니라 «준비 완료 상태»다."*
★**갱신(n=61, 19:0x) — 다음 세션이 «두 축»을 구분해서 고를 것.** Jino 질문(*"트랙 진행율이 아직도 2/7이면 이거 언제 끝나냐?"* / *"PAO가 시작될 수 있는 조건의 작업과 시작되어야 그 후에 완료되는것을 분리해서 보여줘"*)에 답하며 실측한 결과다:
- **점화를 앞당기려면 → S5 · S6 · A-7**(배선 잔여 3건, 1~2세션). 이걸 닫으면 「지금 켜면 생기는 일」 목록에서 기술 항목이 빈다. 그 뒤 남는 건 **C-1(Jino 결정)**과 **P2·P3 절차**뿐 — 점화의 «게이트»는 C-1 하나다(계약Ⓒ 헤더 원문).
- **트랙 진행률 2/7을 움직이려면 → M2-z 종결 판정**. M2-a·b·b2·c가 2026-08-21에 완료·QA됐는데 **종결 판정이 미아가 됐다**(그 뒤 세션들이 점화준비 계약으로 넘어감). 새 코딩이 아니라 판정 한 번이고, 2/7 → 3/7을 만들 **유일한** 후보다. n=47이 *"실제 S1 잔여 = ①②⑤ 3항목"*으로 좁혀 놨다.
- ★**M3·M4·M5는 세션을 더 돌려도 안 움직인다** — 셋 다 점화(C-1)에 잠긴 «닫힌 고리»다(실집행 0 → 일기 0 → 성적 0 → M3 잠김 → M5 잠김). 승격 지혜는 **이미 2건 있고**, 없는 것은 지혜가 아니라 «적용 이력»이다.
- 전체 배치는 **`docs/references/104_pao_ignition_checkpoint_20260827.md` §0-A**(n=61이 신설)에 있다 — 갈래 A(점화 전)/갈래 B(점화 후) 두 표.

- **남은 슬라이스** (계약 §4-A · §6 *"완료된 슬라이스는 건너뛰고 첫 미완 슬라이스부터"* · **묻지 말고 진행**):
- [ ] **S5** 파워링크 광고 제목·설명 수집(`get_ads` 응답의 버려진 절반) + ref 103 §10-3 정정. 마이그 1. ⚠️WEB_SITE 그룹 수 착수 실측 필요(A5/A6 스윕 1,013콜/일보다 크면 착수 전 표면화하고 멈춤)
- [ ] **S6** 그룹별 제외 사용률·소진 예상일 표면 + 스코프 0행 선행 검사 + 관할 등록(`auto_operate=0`으로만). 마이그 0

**Jino 손이 필요한 것** (묻지 말고 진행 대상 «아님»):
- [ ] ★**시크릿 재발급** — n=55 승계 3세트 + `CLAUDE_CODE_OAUTH_TOKEN`
- [ ] 의미단위 등록 표현 실증 — 콘솔 수동 1~2건(2026-08-26 승낙분)
- [ ] 확장 「메이커 셀링 도우미」 검색광고 API 키 3종 교체

**Jino 결정 대기** (전부 ref 104에 조건으로 등재 — **내가 안건화하지 않는다**):
- [ ] **점화**(북극성 §8-①) — 2026-10-28 전이 유리
- [ ] **소유권 분리**(§8-②) · `conv≥1` 절대보호 개정 · 정식 키워드 등록 쓰기 손
- [ ] ★★**F-3 승격(액셀) 실행 경로 — 소관 미배정**(ref 104 §8). **이번 세션이 그 간극을 수치로 확인했다**: 학습 가능 파라미터 5개가 전부 브레이크, 액셀은 0개. 대칭 회복은 이 계약 안에 자리가 없다
- [ ] **GFA 취급**(§8-④)

**이월 (이 세션 스코프 밖 — 기록만)**:
- `window_coverage`의 창이 `latest` 기준이라 원본 stale(현재 D-2)을 못 본다 → 다음 슬라이스 또는 주기 감사
- P2-2 쿼리 0.23초 · P2-3 identity 비교(기각, 기록만)
- P2-H 인구조사 잔여 형태(현재 실재 0건)
- 순이익 정본 이원화 → 소관 `track_coupang-rocket-1p.md`
- 훅 오탐 상습(공유 폴더 stale) → 하네스 소관
- **다음 가용 번호: D-NAO-263 · 교훈 #371** (이 세션은 D-NAO-262 사용, 교훈 미사용)

## 7. 새 세션 시작 프롬프트

```
/session-relay PAO 논의
```
