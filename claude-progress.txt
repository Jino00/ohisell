# claude-progress.txt
# 매 sprint 완료 시 Claude가 이 파일을 갱신합니다.
# 새 세션 시작 시 이 파일을 가장 먼저 읽으세요.

## 현재 상태
- 날짜: 2026-06-05
- 단계: ★활성 트랙 = **쿠팡 RG 발송관제**(docs/tracks/active/track_coupang-rg-replenishment.md). **S5 완료 + codex pass(0블로킹) + prod 라이브 엔드포인트 검증 성공(2026-06-05)** — 5/7. rg_replenishment Harness(읽기전용, 새테이블X, 원칙18-6 정보유통 허브). estimate_sales_velocities·estimate_lead_times·_load_inventory를 **각 1회** 산출 → 옵션별 calc_replenishment(current_stock=·velocity=·lead_time= 주입)로 배치 역산(N×전체스캔 제거, 원칙18-8). 모집단=rg_inventory 보유 옵션. ★등가성 계약: 배치 주입 결과 = calc 미주입(_UNSET, 단일 SA 직접호출)과 정확히 동일 — 어댑터 2곳(_velocity_for: 글로벌 segment_factors·trust_days 병합[안 붙이면 confidence 강제 low 오판], base_source none→None / _lead_for: 입고표본0 옵션은 배치 options에 없음→글로벌 폴백 재현). 엔드포인트 GET /api/coupang/ops/replenishment-plan?account_key=&target_days=(Harness 경유, 원칙18-7). codex: No blocking(1차 pass), nit3건(①등가성 회귀테스트 부재=부분수용·라이브 self-verify로 대체 ②_sort_key int-only=하드닝 ③_summarize 미지status=유지). self-verify(prod DB사본 784): build_plan items vs calc(_UNSET) 전수대조 불일치0(배치==단일). 라이브(GET): 784건 summary reorder4/ok5/well2/insufficient773/low_conf11, sort monotonic. 다음 = S6 UI 컬럼(로켓그로스 탭)+프론트. ★코드 커밋: S2=b8b6fa5, S3=0dd51f7, S4=0a3b496, S5=곧 feat. prod scp+restart 완료. HANDOFF: .claude/memory/HANDOFF_ohisell-coupang-rg-replenishment-S4_20260605.md
- 네이버 풀통합 트랙: 완료(active→completed 이동). 네이버 클레임 실처리 7건 완료(반품4+교환수거3). ★미처리 1건=정기용 교환 재배송(한진 송장 대기).
- 최신 커밋(main): **49d99bd**(S4 docs), 0a3b496(S4 feat), 0b23c52·0dd51f7(S3), ae3a32a·b8b6fa5(S2), fdc5492(RG주문 paidDateTo 버그수정). S5 코드+문서(progress/TRACKS/track)는 곧 커밋.

## ★쿠팡 RG 매출 누락 버그 수정 (2026-06-05, prod 배포 완료, 커밋 fdc5492)
- 증상: 어제 오픽스 매출 시스템 159,300 vs 윙 480,400(3배 누락).
- 원인: RG주문 API paidDateTo 배타적(끝날짜 당일 제외). rg_order_sync._windows가 window1 끝=어제로 분할→어제 RG 전량 누락.
- 수정: paidDateTo=win_end+1일(span 최대30일 유지). 검증: 수정후 어제 RG 23건/388,700, sales-summary 497,300(윙과 일치). Failure Memory 기록.

## ★쿠팡 RG 발송관제 트랙 (2026-06-05 시작, S0 완료)
- 목표: RG 현재고+일판매속도+입고 리드타임으로 "언제·몇 개 발송" 역산. FC 목표재고 2~3일치.
- 구조(승인): Agent 쿠팡RG관제 → Harness rg_replenishment → SA 6개(rg_inbound_sync★신규, rg_inventory_sync, rg_order_sync, lead_time_estimator★, sales_velocity_estimator★, replenishment_calc★). UI=로켓그로스 탭 컬럼(현재고|일판매|리드타임|며칠치|권장발송일·수량).
- D-1 입고=Wing내부API 연결(D-14 수정). D-2 목표 2~3일치. D-3 평일/주말→점진세분화. D-4 지표제시(실행은 Jino). D-5 쿠키 수동붙여넣기 시작+측정→필요시 자동화.
- ★S0 실증: GET wing.coupang.com/tenants/rfm-inbound/data/inbound/search, 전체쿠키+x-xsrf-token 헤더→prod서버 200/68KB(Akamai봇쿠키 불필요, IP비귀속 확인). 리드타임=shipmentStatusHistory statusId3(발송)→7(판매개시), 실측1~4.5일. 입고이력 6건.
- 다음 S1: 테이블 coupang_rg_inbound + 쿠키 시크릿저장(설정칸,cURL→쿠키추출) + rg_inbound_sync SA(일일,302시 fail-soft+🔴) + codex.

## 네이버 (이전 트랙, 완료)
- N1~N8 완료(N2 skip). 읽기+쓰기(발주/발송·클레임·판매상태변경) 전부 prod·codex pass. 트랙 completed/ 이동.
- 클레임 실처리 7건 완료. 미처리: 정기용 교환재배송(product_order_id 2026052876140291, 한진 송장 대기).
